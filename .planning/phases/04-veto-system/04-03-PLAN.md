---
phase: 04-veto-system
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - js/app.js
autonomous: true
requirements: [VETO-03]
tags: [real-time, toast, onSnapshot]

must_haves:
  truths:
    - "When another family member vetoes a title, a warm toast appears on every OTHER device within 2s naming the vetoer and the title"
    - "The vetoer's OWN device does NOT show a toast for their own veto (avoids self-echo — already see the result inline)"
    - "The real-time toast diff survives midnight re-subscription — stale title keys from yesterday's session do NOT trigger spurious toasts (Assumption A3)"
    - "Existing subscribeSession behavior (session state assignment + renderTonight) continues to fire on every mutation; toast emission is additive, not replacing"
    - "Post-spin auto re-spin propagation: Tab B (watching the spin modal) sees a 'spinning again…' shimmer when Tab A vetoes, even though Tab B didn't initiate the veto"
  artifacts:
    - path: "js/app.js"
      provides: "Extended subscribeSession with prev-veto-keys diff + flashToast emission; cross-device shimmer trigger via the same diff"
      contains: "prevVetoKeys"
  key_links:
    - from: "subscribeSession onSnapshot callback"
      to: "flashToast"
      via: "diff of incoming.vetoes keys against prevVetoKeys set"
      pattern: "prevVetoKeys"
    - from: "subscribeSession"
      to: "showRespinShimmer (from Plan 02)"
      via: "when a new veto is observed AND the spin modal is open, apply shimmer"
      pattern: "spin-modal-bg"
---

<objective>
Finish Phase 4 by making veto events feel real-time across every family device: a warm "Sam passed on Inception — spinning again…" toast on all OTHER devices within 2s, plus a cross-device shimmer on any device whose spin modal is currently open. Small surface, tight scope — this is the polish that makes the ritual feel shared instead of solo.

Purpose: Deliver VETO-03 (real-time who-vetoed surfacing) without adding a new listener — extend the existing `subscribeSession` onSnapshot callback to diff `vetoes` keys and emit toasts on net-new entries authored by someone other than `state.me`.

Output: An extended `subscribeSession` function that maintains a `prevVetoKeys` set across snapshots (reset on each re-subscribe to avoid Assumption A3 leakage), emits `flashToast` for each new veto whose `memberId !== state.me.id`, and triggers `showRespinShimmer()` on the vetoed pick when the spin modal is open on the observing device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-veto-system/04-CONTEXT.md
@.planning/phases/04-veto-system/04-RESEARCH.md
@.planning/phases/04-veto-system/04-PATTERNS.md
@.planning/phases/04-veto-system/04-UI-SPEC.md
@.planning/phases/04-veto-system/04-01-SUMMARY.md
@.planning/phases/04-veto-system/04-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Existing subscribeSession lives at js/app.js:1487-1494. Current body:

    function subscribeSession() {
      if (state.unsubSession) { state.unsubSession(); state.unsubSession = null; }
      state.sessionDate = todayKey();
      state.unsubSession = onSnapshot(sessionRef(state.sessionDate), s => {
        state.session = s.exists() ? s.data() : { vetoes: {} };
        renderTonight();
      }, e => {});
    }

From Plan 02: `showRespinShimmer()` is defined (module-level function near spinPick). It applies `.sk` to the poster inside `#spin-modal-content`.

From Plan 01: each veto entry carries `memberName` — use it directly in the toast (no members lookup needed).

Toast copy (UI-SPEC):
- Format: "{memberName} passed on {titleName} — spinning again…"
- Warm, lowercase except proper nouns, em-dash
- flashToast(message, { kind: 'info' })

Spin modal open detection:
- document.getElementById('spin-modal-bg')?.classList.contains('on') — truthy iff the spin modal is currently visible on this device
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend subscribeSession to diff veto keys, emit warm toasts on net-new others' vetoes, and trigger shimmer on open spin modals</name>
  <files>js/app.js</files>
  <read_first>
    - js/app.js offset=1485 limit=22 (subscribeSession + scheduleMidnightRefresh)
    - js/app.js offset=4388 limit=10 (showRespinShimmer from Plan 02 — confirms the helper is defined at the top level)
    - .planning/phases/04-veto-system/04-RESEARCH.md §"Code Examples" (real-time toast diff reference implementation)
    - .planning/phases/04-veto-system/04-RESEARCH.md §"Assumptions Log" A3 (prevVetoKeys must reset on re-subscription)
    - .planning/phases/04-veto-system/04-CONTEXT.md D-13 + D-14 (toast + shimmer on re-spin)
    - .planning/phases/04-veto-system/04-UI-SPEC.md §Copywriting Contract (exact toast format)
    - .planning/phases/04-veto-system/04-02-SUMMARY.md (confirm showRespinShimmer is present and its implementation)
  </read_first>
  <behavior>
    - On EVERY snapshot of the session doc, compute the set of veto keys in the incoming `vetoes` map
    - For each key present in incoming but NOT present in the snapshot-local `prevVetoKeys` set:
      - If the veto's `memberId === state.me.id`: skip (no self-echo)
      - Otherwise: flashToast with format "{memberName} passed on {titleName} — spinning again…", kind info. Resolve titleName via state.titles.find → `.name`, fallback "a title"
      - AND if the local spin modal is open (#spin-modal-bg has class `on`): call showRespinShimmer() to surface D-14 cross-device shimmer
    - Update `prevVetoKeys` to the new set after processing
    - On `subscribeSession` re-entry (midnight rollover), reset `prevVetoKeys` to empty BEFORE subscribing — prevents yesterday's keys leaking into today's listener (Assumption A3)
    - First snapshot after subscribe emits NO toasts (initial state seeded as "already seen")
    - Existing behaviors preserved: `state.session = s.exists() ? s.data() : { vetoes: {} }`, `renderTonight()` still fires on every snapshot
  </behavior>
  <action>
    Step 1 — Add module-scoped state. Immediately ABOVE `function subscribeSession()` at js/app.js:1487, insert:

        let prevVetoKeys = new Set();
        let vetoSubscribeIsFirstSnapshot = true;

    Step 2 — Replace the entire existing `subscribeSession` function (js/app.js:1487-1494) with the new body below. The new body preserves every existing behavior (cleanup of the old listener, sessionDate assignment, onSnapshot registration, `state.session` assignment, `renderTonight()` call) and ADDS the diff + toast + shimmer logic.

    Replacement body:

        function subscribeSession() {
          if (state.unsubSession) { state.unsubSession(); state.unsubSession = null; }
          // Assumption A3: reset diff state on every (re-)subscribe so yesterday's keys don't leak through midnight.
          prevVetoKeys = new Set();
          vetoSubscribeIsFirstSnapshot = true;
          state.sessionDate = todayKey();
          state.unsubSession = onSnapshot(sessionRef(state.sessionDate), s => {
            const incoming = s.exists() ? s.data() : { vetoes: {} };
            const incomingVetoes = incoming.vetoes || {};
            const incomingKeys = new Set(Object.keys(incomingVetoes));
            // D-13: warm toast for vetoes authored by OTHER members. Skip the very first snapshot so we
            // don't flood on subscribe (the initial state is the baseline, not a delta).
            if (!vetoSubscribeIsFirstSnapshot) {
              for (const titleId of incomingKeys) {
                if (prevVetoKeys.has(titleId)) continue;
                const v = incomingVetoes[titleId];
                if (!v || v.memberId === (state.me && state.me.id)) continue;
                const t = state.titles.find(x => x.id === titleId);
                const titleName = (t && t.name) || 'a title';
                flashToast(v.memberName + ' passed on ' + titleName + ' — spinning again…', { kind: 'info' });
                // D-14: if the observing device has the spin modal open, surface the cross-device shimmer
                const bg = document.getElementById('spin-modal-bg');
                if (bg && bg.classList.contains('on')) {
                  showRespinShimmer();
                }
              }
            }
            prevVetoKeys = incomingKeys;
            vetoSubscribeIsFirstSnapshot = false;
            state.session = incoming;
            renderTonight();
          }, e => {});
        }

    Key invariants:
    - `vetoSubscribeIsFirstSnapshot = true` is set inside `subscribeSession` and flipped to `false` after the first snapshot processes — suppresses flood-on-subscribe
    - `prevVetoKeys` is reset BEFORE the onSnapshot call (ensures midnight re-subscribe starts clean)
    - Inner callback captures `incoming` once and mutates `state.session` exactly once
    - `renderTonight()` still fires unconditionally at the end

    Do NOT modify `scheduleMidnightRefresh` (js/app.js:1496-1505). It still calls `subscribeSession()` unchanged, which triggers the new reset logic.

    Do NOT add a second `onSnapshot` call anywhere — the phase budget is one listener per document, and this plan explicitly reuses the existing subscription per RESEARCH §Real-Time Propagation Confirmation.
  </action>
  <verify>
    <automated>grep -n "let prevVetoKeys" js/app.js ; grep -n "vetoSubscribeIsFirstSnapshot" js/app.js ; grep -n "passed on " js/app.js ; grep -c "onSnapshot(sessionRef" js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let prevVetoKeys" js/app.js` returns `1`
    - `grep -c "vetoSubscribeIsFirstSnapshot" js/app.js` returns `3` (declaration + reset inside subscribeSession + flip after first snapshot) — if it's 4 that's also fine (extra reference is harmless) but 1–2 indicates a missing control point
    - `grep -n "' passed on '" js/app.js` returns ≥1 line (toast literal inside subscribeSession)
    - `grep -n "' — spinning again…'" js/app.js` returns ≥1 line (em-dash + ellipsis preserved)
    - `grep -c "onSnapshot(sessionRef" js/app.js` returns exactly `1` (NO new listener added)
    - `grep -n "bg.classList.contains('on')" js/app.js` returns ≥1 line (shimmer open-modal gate)
    - `grep -c "showRespinShimmer()" js/app.js` returns `2` (one call in submitVeto from Plan 02, one in subscribeSession from this plan)
    - `grep -n "prevVetoKeys = incomingKeys" js/app.js` returns 1 line (diff-state update)
    - `grep -n "v.memberId === (state.me && state.me.id)" js/app.js` returns 1 line (self-echo guard)
    - Existing behaviors preserved:
      - `grep -n "state.session = incoming" js/app.js` or `grep -n "state.session = s.exists()" js/app.js` — exactly one of these (the new body uses `state.session = incoming`)
      - `grep -n "renderTonight();" js/app.js` count is unchanged from pre-plan baseline (verify: renderTonight still called inside subscribeSession)
    - Manual multi-tab QA (primary surface mobile Safari):
      1. Two tabs, Alice + Bob, same family. Bob pre-spin vetoes "Inception" → within 2s, Alice's tab shows toast "Bob passed on Inception — spinning again…". Bob's tab shows NO toast (self-echo guard)
      2. On subscribe (page reload), existing vetoes already on the session doc do NOT produce toasts — vetoSubscribeIsFirstSnapshot guard works
      3. Cross-device shimmer: Alice has the spin modal open on a landed pick. Bob post-spin vetoes (from Bob's device). Within 2s, Alice's spin modal poster gets the `.sk` shimmer class applied (inspect DOM or observe the visual). Toast also fires
      4. Midnight rollover: simulate by advancing system clock past midnight and letting `scheduleMidnightRefresh` fire. A fresh subscribe starts `prevVetoKeys` empty. A fresh veto today triggers a toast; yesterday's stale keys do NOT
      5. Regression: pre-spin veto still removes title from candidate list on both tabs within 2s. Vetoed-tonight divider still renders. Activity feed 'vetoed' entry still renders
  </acceptance_criteria>
  <done>`subscribeSession` emits a warm info toast for every net-new veto authored by OTHER members, triggers the cross-device shimmer on open spin modals, avoids self-echo, suppresses subscribe-flood via the first-snapshot flag, and resets cleanly across midnight re-subscription. No new onSnapshot listener introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → client (onSnapshot) | Incoming veto entries authored by other members cross into our render path and populate flashToast text (`memberName` + `titleName`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure (XSS via memberName or titleName in toast) | flashToast call in subscribeSession | mitigate | `flashToast` uses `toast.textContent = message` at utils.js:27 — inherently XSS-safe regardless of source. No HTML rendering of the message. Confirmed by reading utils.js during planning |
| T-04-13 | Spoofing (fake memberName in veto entry) | onSnapshot read path | accept | Family-code-scoped Firestore rules are the trust boundary. Anyone in the family can write any memberName to vetoes (already true pre-Phase 4). Not a new surface |
| T-04-14 | DoS (toast flood if malicious client writes many vetoes) | flashToast stacking | mitigate | `flashToast` caps visible toasts to 3 (utils.js:30 `while (toastContainer.children.length > 3) toastContainer.removeChild(toastContainer.firstChild)`) — flood is visually bounded |
| T-04-15 | Tampering (stale prevVetoKeys after midnight) | diff state | mitigate | Explicit reset of `prevVetoKeys` and `vetoSubscribeIsFirstSnapshot` at the top of every `subscribeSession` call — runs on `scheduleMidnightRefresh` re-entry |
</threat_model>

<verification>
Automated (grep):
- `grep -c "let prevVetoKeys" js/app.js` → 1
- `grep -c "vetoSubscribeIsFirstSnapshot" js/app.js` → 3
- `grep -c "onSnapshot(sessionRef" js/app.js` → 1 (no new listener)
- `grep -c "showRespinShimmer()" js/app.js` → 2

Manual (multi-tab, primary surface mobile Safari):

Sequence A — Real-time toast on other devices (VETO-03 D-13):
1. Tab A (Alice) + Tab B (Bob), same family, both on Tonight screen
2. Bob pre-spin vetoes "Inception"
3. Within 2s, Alice's tab shows toast "Bob passed on Inception — spinning again…"
4. Bob's tab shows NO toast (self-echo guard)
5. Both tabs show the veto under "Vetoed tonight" divider and in the activity feed (existing behaviors preserved)

Sequence B — Self-echo suppression:
1. Alice vetoes a title
2. Alice sees no toast. Alice sees the existing activity feed entry and the "Vetoed tonight" card

Sequence C — Subscribe-flood suppression:
1. A family already has several vetoes from earlier in the session
2. Open a fresh tab and join the family → NO toasts fire (first-snapshot flag)
3. First new veto AFTER the subscribe → toast fires normally

Sequence D — Cross-device shimmer (VETO-03 D-14):
1. Alice taps Spin → spin modal open on Alice's device, landed on title X
2. Bob taps "Pass on it" from Bob's spin modal (Bob also had the modal open)
3. Within 2s: Alice sees the toast; Alice's spin modal poster also gets the shimmer class briefly
4. Alice's spin modal stays open throughout the transition (existing behavior — modal is closed only by acceptSpin / closeSpinModal)

Sequence E — Midnight rollover clean state:
1. Alice vetoes "Inception" at 11:58pm
2. System time advances past midnight — `scheduleMidnightRefresh` calls `subscribeSession`
3. `prevVetoKeys` is reset. A new day's session doc is empty. No spurious toast for yesterday's "Inception" veto

Sequence F — Full-phase end-to-end (all 6 VETO-* reqs together):
1. Alice pre-spin vetoes title X → title disappears from both tabs' pool (VETO-01)
2. Bob taps Spin → lands on Y
3. Bob post-spin vetoes Y with comment "too long" (VETO-04)
4. Bob's tab: auto re-spin fires with shimmer → lands on Z (VETO-02)
5. Alice's tab: toast "Bob passed on Y — spinning again…" appears; Alice's spin modal (if open) shimmers (VETO-03)
6. Bob's Tonight Spin button is now disabled with tooltip "Someone else spins this one" (VETO-06)
7. Firestore: `families/{code}/vetoHistory/` has 2 new docs (X and Y) with sessionDate=today (VETO-05)
8. Alice taps Spin → lands on W → Bob's Spin button re-enables (VETO-06 D-07)
</verification>

<success_criteria>
- subscribeSession extended with diff logic + toast + shimmer WITHOUT adding a second onSnapshot
- Self-echo suppressed (vetoer doesn't toast themselves)
- Subscribe-flood suppressed (first snapshot is baseline, not delta)
- Midnight rollover resets diff state cleanly (Assumption A3 mitigated)
- All Verification sequences A–F pass on two-tab QA
- Full phase end-to-end (Sequence F) demonstrates all 6 VETO-* requirements together
- No regressions to pre-spin veto (VETO-01), activity feed, or `.tc-note.veto` card display
</success_criteria>

<output>
After completion, create `.planning/phases/04-veto-system/04-03-SUMMARY.md` documenting:
- Exact lines changed in `js/app.js` (prevVetoKeys declaration + vetoSubscribeIsFirstSnapshot declaration + rewritten subscribeSession body)
- Manual QA Sequence A–F results
- Confirm: no new onSnapshot listener; grep shows exactly 1 `onSnapshot(sessionRef`
- Any tuning of toast copy (timing, emoji, punctuation) done for mobile-Safari legibility
- Readiness note for `/gsd-verify-work` — all 6 VETO-* requirements should be demonstrable end-to-end via Sequence F
</output>
