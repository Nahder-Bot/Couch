---
phase: 31-marketing-refresh
plan: 05
type: execute
wave: 3
depends_on: [31-01, 31-02, 31-03, 31-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [MARK-31-01, MARK-31-02, MARK-31-03, MARK-31-04, MARK-31-05, MARK-31-06, MARK-31-07, MARK-31-08, MARK-31-09, MARK-31-10, MARK-31-11, MARK-31-12, MARK-31-13, MARK-31-14]
tags: [docs, requirements, audit-trail, traceability, roadmap, state]

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md gains a `### MARK-31-* — Marketing Refresh (Phase 31)` block with 14 traceability rows (one per MARK-31-NN ID used in plans 31-01..31-04)"
    - "Each MARK-31-NN row cites the implementing plan AND the verifying script in 31-HUMAN-UAT.md (where applicable)"
    - ".planning/ROADMAP.md Phase 31 row updated from `0/5 SCOPED` → `5/5 SHIPPED 2026-05-06` (or actual deploy date) with cache-version + 31-HUMAN-UAT.md reference + plan checkbox flips"
    - ".planning/STATE.md `Last Activity` field updated to reflect Phase 31 close-out; `Open follow-ups` notes the pending device-UAT awaiting `uat passed` resume signal"
    - "Pure documentation phase — ZERO source-code changes (mirrors Phase 15.6 + 15.2 pattern). NO sw.js bump, NO deploy, NO css/js touched"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "14 MARK-31-* traceability rows + Coverage line increment"
      contains_all: ["MARK-31-01", "MARK-31-14", "Phase 31"]
    - path: ".planning/ROADMAP.md"
      provides: "Phase 31 row updated to SHIPPED state with audit narrative"
      contains: "5/5"
      not_contains: "0/5"
    - path: ".planning/STATE.md"
      provides: "Last Activity refreshed + UAT-pending follow-up tracked"
  key_links:
    - from: "REQUIREMENTS.md MARK-31-* rows"
      to: "Plans 31-01..31-04 + 31-HUMAN-UAT.md scripts"
      via: "Inline citations '(Plan 31-01)' + '(Script N in 31-HUMAN-UAT.md)'"
      pattern: "\\(Plan 31-0[1-4]\\)|\\(Script [1-7]"
---

<objective>
Mirror Phase 15.6 / Phase 15.2 audit-trail-backfill pattern for Phase 31. Phase 31 had ZERO requirement IDs assigned at scope time (per CONTEXT.md additional_context: "Phase 31 has no REQ-IDs assigned to Phase 31 in REQUIREMENTS.md today"). This plan retroactively introduces a MARK-31-* requirement family covering all 14 deliverables across plans 31-01..31-04, refreshes the ROADMAP.md Phase 31 row from `0/5 SCOPED` to `5/5 SHIPPED`, and updates STATE.md `Last Activity` field.

Purpose: Closes the audit-trail gap. Without this plan, `/gsd-verify-work 31` would have no requirement IDs to walk against, and future cross-phase audits would mark Phase 31 as missing traceability. Mirrors the v33.3-MILESTONE-AUDIT.md remediation pattern.

Output: 3 planning files updated, 0 source files touched, single commit. Pure documentation phase.

This plan is the LAST step in Phase 31 before the verifier (`/gsd-verify-work 31`) runs. Per CONTEXT line 173 it was tagged "optional" but mounting it as a real plan keeps the audit trail clean and matches the Phase 15.6 / 15.2 / 27 / 28 traceability standard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-marketing-refresh/31-CONTEXT.md
@.planning/phases/31-marketing-refresh/31-01-PLAN.md
@.planning/phases/31-marketing-refresh/31-02-PLAN.md
@.planning/phases/31-marketing-refresh/31-03-PLAN.md
@.planning/phases/31-marketing-refresh/31-04-PLAN.md

# Phase 15.6 / Plan 01 / Plan 02 / Plan 03 are the canonical pattern to mirror.
# - 15.6-01: REQUIREMENTS.md backfill (29 traceability rows added; Coverage refreshed)
# - 15.6-02: ROADMAP.md table reconciliation (Phase 19 row updated 2/3 → 3/3 SHIPPED)
# - 15.6-03: STATE.md repair (frontmatter status flip + Last Activity field + Open follow-ups compact)

# Phase 31's 14 MARK-31-* IDs (one per acceptance bucket across plans):
# MARK-31-01: Comparison section (4 blocks) — Plan 31-01
# MARK-31-02: Comparison section voice (D-12 verbatim copy) — Plan 31-01
# MARK-31-03: Features section (4 blocks) + FAQ section (7 questions) — Plan 31-01
# MARK-31-04: FAQ Sentry breadcrumb wiring (D-27) — Plan 31-01
# MARK-31-05: CSS rules using only existing :root tokens (D-21) — Plan 31-01
# MARK-31-06: 5 fresh screenshots at marketing/ root — Plan 31-02
# MARK-31-07: Phase-9 screenshots archived to marketing/archive-phase-9/ — Plan 31-02
# MARK-31-08: landing.html .screenshots section rewired with new filenames + alt text — Plan 31-02
# MARK-31-09: brand/og-source.svg authored with leather-grain background + hero text — Plan 31-03
# MARK-31-10: og.png rendered 1200×630 ≤ 200KB via sharp — Plan 31-03
# MARK-31-11: og.png?v=2 cache-bust synced across landing/changelog/rsvp.html — Plan 31-04 Task 1
# MARK-31-12: scripts/deploy.sh mirror loop extended (og.png + marketing/ + brand/) — Plan 31-04 Task 2
# MARK-31-13: 31-HUMAN-UAT.md scaffolded with 7 scripts — Plan 31-04 Task 3
# MARK-31-14: sw.js CACHE bumped to couch-v48-marketing-refresh + production deploy — Plan 31-04 Task 4

# REQUIREMENTS.md current state (from initial read):
# - line 1-10: header + Coverage state warning
# - sections by phase: Mood Tags / Veto / Auth / Push / Watchparty / Intent / Redesign /
#   Year-in-Review / Feature refresh / Pre-launch polish / Compliance / Decision Ritual /
#   Tracking / Security / Audit-trail / Integration / Wait Up / 18 / 19 / 20 / 21 / etc.
# - Phase 27 (RSVP-27-*) and Phase 28 (PICK-28-*) are the most recent sections.
# - Phase 31 (MARK-31-*) belongs after Phase 30 (REQ-30-*) in chronological order, OR alphabetically
#   if the file is reorg'd by ID family. Existing convention: chronological by phase number.
# - Phase 30 sits at "## v2 Requirements" block per the grep; verify exact position with `grep -n REQ-30-` first.

# ROADMAP.md current Phase 31 row (from earlier read):
# | 31. Marketing refresh | 0/5 | **SCOPED 2026-05-05, awaiting `/gsd-discuss-phase 31`** ... |
# Update target:
# | 31. Marketing refresh | 5/5 | **SHIPPED 2026-05-06 — `couch-v48-marketing-refresh` deployed to couchtonight.app via single-repo couch-only deploy ritual...** |
# (replace 2026-05-06 with actual deploy date from Plan 31-04 SUMMARY)

# STATE.md `Last Activity` field per Phase 15.6-03 / D-13 — silences the `phase complete`
# CLI warning observed at Phase 20 closeout. Update to:
# **Last Activity:** 2026-05-06 — Phase 31 (Marketing refresh) shipped; couch-v48-marketing-refresh live; device UAT pending in 31-HUMAN-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add MARK-31-* section to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (full — find PICK-28-* trailing rows + REQ-30-* section to know where to insert MARK-31-*)
    - .planning/phases/31-marketing-refresh/31-01-PLAN.md (acceptance criteria block — source-of-truth for what each MARK-31-NN should claim)
    - .planning/phases/31-marketing-refresh/31-02-PLAN.md
    - .planning/phases/31-marketing-refresh/31-03-PLAN.md
    - .planning/phases/31-marketing-refresh/31-04-PLAN.md
    - .planning/phases/31-marketing-refresh/31-04-SUMMARY.md (post-deploy commit hashes — needed for traceability rows; if it doesn't exist yet because Plan 31-04 hasn't completed, fall back to citing plan acceptance criteria + executor commit list)
  </read_first>

  <action>
**Step 1: Locate the insertion point in REQUIREMENTS.md.**

Use Grep to find the `### REQ-30-* — Couch Groups (Phase 30)` section header (or whatever the Phase 30 section heading is). The MARK-31-* block goes IMMEDIATELY AFTER the Phase 30 section's last row, before any `## ` (h2) section that might follow.

```bash
grep -n '^### .*Phase 30\|^### .*Phase 28\|^### .*Phase 27' .planning/REQUIREMENTS.md
```

If Phase 30 has its own section, append after it. If Phase 30 is folded under "v2 Requirements" without a dedicated header, insert MARK-31-* as the LAST section of v2 Requirements with a fresh `### MARK-31-* — Marketing Refresh (Phase 31)` header.

**Step 2: Append the MARK-31-* block.**

The block follows the established pattern from RSVP-27-* / PICK-28-* / REQ-30-* — bullet rows with `**MARK-31-NN**: <one-line claim with verbatim copy / file paths / commit hashes>`. Insert after Phase 30's last row.

Use Edit to append:

```markdown

### MARK-31-* — Marketing Refresh (Phase 31)

Phase 31 closed the visibility gap between the shipped 28-phase product (cache `couch-v47-pickem` pre-31, `couch-v48-marketing-refresh` post-31) and the public marketing surface (frozen at Phase 9 / April 2026 pre-31). 14 deliverables across 5 plans + 1 audit-trail plan.

- [x] **MARK-31-01**: landing.html `.compare` section ships 4 named-competitor-cluster blocks (vs JustWatch/Reelgood / vs Letterboxd/Trakt / vs Teleparty/Scener/Plex / vs Apple SharePlay/FaceTime/Microsoft Teams Together) with eyebrow `Why not the other things?` and h2 `Couch isn't the first app for movie night. Here's the difference.` per CONTEXT D-01 / D-02 / D-24 (Plan 31-01 / Script 1 in 31-HUMAN-UAT.md)
- [x] **MARK-31-02**: Comparison-section voice locks 4 verbatim copy lines per CONTEXT D-12 + D-24: vs JustWatch `They tell you where to stream. We help everyone agree on what.`; vs Letterboxd `They're solo cinephile journals. Couch is built for the people watching with you tonight.`; vs Teleparty `They sync your screen with friends. Couch handles deciding, watching, and remembering — without making everyone install a Chrome extension.`; vs Apple/Microsoft `They're built into one ecosystem (Apple's, Microsoft's). Couch works across iPhone, Android, and the browser. No one's locked out because they're on the wrong device.` (Plan 31-01 / Script 1)
- [x] **MARK-31-03**: landing.html `.features` section ships 4 feature blocks (Wait Up / Couch Groups / Pickup / Pick'em) with verbatim copy from CONTEXT D-14 + heading `What's actually in it` per D-26 (NO repeat of "free" — `.hero-trust` already carries it); landing.html `.faq` section ships 7 `<details>` items per CONTEXT D-22 covering log-in / Grandma / streaming services / cost / devices / data / vs-Teleparty (Plan 31-01 / Scripts 2 + 4 + 7)
- [x] **MARK-31-04**: D-27 Sentry breadcrumb wired on every FAQ open — `Sentry.addBreadcrumb({ category: 'marketing.faq', message: 'opened', data: { question_index: 0..6 } })` with canonical defensive guard `typeof Sentry !== 'undefined' && Sentry.addBreadcrumb` per js/app.js precedent; only fires on open (gated `if (!e.target.open) return`), not on close (Plan 31-01 inline `<script>` block / Script 3)
- [x] **MARK-31-05**: css/landing.css extended with `.compare-eyebrow` / `.compare-grid` / `.compare-block` / `.feature-list` / `.feature-block` / `.faq-list` / `.faq-item` / `.faq-item summary::after` rules using ONLY existing :root tokens (--bg / --surface / --ink / --ink-warm / --ink-dim / --border / --r-md / --accent / --ease-cinema) per D-21; ZERO new :root token declarations introduced; `+` → `−` glyph swap on `.faq-item[open] summary::after` honors restraint-first design per BRAND.md §1 (Plan 31-01 / Script 7)
- [x] **MARK-31-06**: 5 fresh screenshots captured against `couch-v47-pickem` build via iPhone 14 Pro 1170×2532 per D-05: `marketing/tonight-couch-viz.png` (V5 couch viz hero + multi-member roster) + `marketing/watchparty-wait-up.png` (Wait Up slider visible) + `marketing/couch-groups-roster.png` ((FamilyName) chips per Phase 30) + `marketing/tonight-pickup.png` (per-watching-group widget) + `marketing/pickem-leaderboard.png` (D-25 — Pick'em as launch-defining 5th); all ≤ 400KB after sharp mozjpeg-quality-80 optimization per D-08; PII scrubbed per D-07 (Plan 31-02 / Script 4)
- [x] **MARK-31-07**: 5 Phase-9 legacy screenshots archived to `marketing/archive-phase-9/` per D-09 (tonight-hero / watchparty-live / mood-filter / title-detail / intent-rsvp) — copied from legacy deploy-mirror path `/c/Users/nahde/queuenight/public/marketing/` into the source repo for the first time, preserving Phase-9 / Plan 09-06 blame chain (Plan 31-02)
- [x] **MARK-31-08**: landing.html `.screenshots` section rewired — 5 `<figure class="screenshot-card">` entries reference the new filenames with new alt text + new figcaptions (`Everyone's on the couch` / `Watch together, hours apart` / `Bring another family in` / `Pick up where you left off` / `Pick'em on the big games`) cross-section-consistent with the features-section headlines from MARK-31-03 (Plan 31-02 / Script 4)
- [x] **MARK-31-09**: `brand/og-source.svg` authored as 1200×630 hand-edited SVG with leather-grain background (linearGradient leatherFalloff + feTurbulence noise filter matching landing.css:20 pattern) + hero text "Decide what to watch in 30 seconds." (Fraunces 800 / 64px / --ink) + "Watch together." (Instrument Serif italic / 56px / --accent) + base64-embedded Couch wordmark from logo-h300.png + couchtonight.app footer URL — round-trippable per D-23 (any future re-cut edits the SVG, then re-runs sharp render to regenerate og.png) (Plan 31-03)
- [x] **MARK-31-10**: og.png rendered at repo root 1200×630 ≤ 200KB via sharp from /tmp/img-opt (no repo-level dep added per CLAUDE.md "no bundlers"; matches Phase 9 / Plan 09-06 sharp pattern) per D-17 / D-23 — full visual refresh, NOT an overlay tweak (Plan 31-03 / Script 5)
- [x] **MARK-31-11**: og.png cache-bust query string `?v=2` synced across all 3 OG-meta surfaces in lockstep per D-18 — landing.html (og:image + JSON-LD image) + changelog.html (og:image + twitter:image) + rsvp.html (NEW: 12 OG/Twitter meta tags added — pre-31 had ZERO; gap-to-fill from CONTEXT.md code_context); rsvp.html `<meta name="robots" content="noindex, nofollow">` preserved (social-card scrapers don't honor robots — OG meta still required for invite-link previews) (Plan 31-04 Task 1 / Script 5)
- [x] **MARK-31-12**: `scripts/deploy.sh` mirror loop extended (post-line-249) with 3 conditional blocks — `if [ -f og.png ]` copies og.png to mirror; `if [ -d marketing ]` copies marketing/ tree (including marketing/archive-phase-9/) to mirror via `cp -rv marketing/.`; `if [ -d brand ]` copies brand/ tree to mirror; closes the longstanding gap that og.png + marketing/ lived only in queuenight/public/ pre-Phase-31 (Plan 31-04 Task 2)
- [x] **MARK-31-13**: `31-HUMAN-UAT.md` scaffolded with 7 device-UAT scripts mirroring 28-HUMAN-UAT.md frontmatter + structure: comparison render on iPhone Safari (Script 1) / FAQ accordion behavior on iOS+Android (Script 2) / FAQ Sentry breadcrumb fires (Script 3) / screenshot grid render at 1x+2x DPR (Script 4) / og.png link-preview via iMessage+Twitter (Script 5) / sw.js cache verification (Script 6) / desktop ≥ 900px responsive layout (Script 7); resume signal `uat passed` per Phase 28 / 30 / 27 / 26 / 18 / 19 / 15.5 pattern (Plan 31-04 Task 3)
- [x] **MARK-31-14**: sw.js CACHE bumped from `couch-v47-pickem` → `couch-v48-marketing-refresh` per D-19 via `bash scripts/deploy.sh 48-marketing-refresh`; single-repo couch-only deploy per D-20 (zero queuenight functions changes, zero firestore.rules changes); existing 13 smoke contracts remained green per D-21 (no new smoke contract — marketing copy is strings, not behavior); production curl-verified at couchtonight.app (Plan 31-04 Task 4 / Script 6)
```

**Step 3: Update the Coverage line.**

Find the Coverage line in REQUIREMENTS.md (around line 6-8 of the existing header — it reads something like `Coverage 87/107 = 81%` per recent Phase 15.6-01 / Phase 15.2 backfills). Increment both numerator and denominator by 14:

If pre-edit reads `Coverage X/Y`:
- New: `Coverage (X+14)/(Y+14)` (add 14 to both, since all 14 MARK-31-* are marked Complete via the `[x]` checkbox).

The exact current numbers depend on Phase 27 / 28 / 30 backfill state at the time this plan runs. Use grep to find the line, calculate the new numbers, and Edit.

**Step 4: Verify.**

Confirm the new section was added cleanly with no orphan bullets, no broken markdown table syntax, and no bullet-list-in-the-middle-of-an-h3 errors. Run a quick markdown-lint sanity check:

```bash
node -e 'const fs = require("fs"); const r = fs.readFileSync(".planning/REQUIREMENTS.md","utf8"); const matches = r.match(/MARK-31-\d{2}/g); if (!matches || matches.length < 14) { console.error("FAIL: only " + (matches ? matches.length : 0) + " MARK-31-* references found"); process.exit(1);} console.log("MARK-31 count: " + matches.length);'
```
  </action>

  <verify>
    <automated>
grep -c '^- \[x\] \*\*MARK-31-' .planning/REQUIREMENTS.md | grep -q '^14$' && echo "14-rows-ok";
grep -q 'MARK-31-01' .planning/REQUIREMENTS.md && grep -q 'MARK-31-14' .planning/REQUIREMENTS.md && echo "boundary-ids-ok";
grep -q '### MARK-31-\* — Marketing Refresh' .planning/REQUIREMENTS.md && echo "section-header-ok";
grep -E 'Coverage [0-9]+/[0-9]+' .planning/REQUIREMENTS.md | head -1
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the section header `### MARK-31-* — Marketing Refresh (Phase 31)`.
    - `.planning/REQUIREMENTS.md` contains exactly 14 rows of the form `- [x] **MARK-31-NN**:` (NN ranging 01..14, all marked complete via `[x]`).
    - Each MARK-31-NN row cites the implementing plan (e.g. `(Plan 31-01)`) AND, where applicable, the verifying UAT script (e.g. `Script 3 in 31-HUMAN-UAT.md`).
    - The Coverage line in REQUIREMENTS.md is incremented by 14 in BOTH numerator and denominator (e.g. `Coverage 130/150` → `Coverage 144/164`). Exact pre/post numbers depend on prior backfills; the delta is what's checkable.
    - The MARK-31-* section sits AFTER the Phase 30 section (or after the last v2 Requirements section if Phase 30 is folded under that umbrella) — chronological-by-phase ordering preserved.
    - No syntax errors in the markdown — `node -e 'require("fs").readFileSync(".planning/REQUIREMENTS.md","utf8")'` exits 0; all `**` bold pairs balanced; all backtick blocks closed.
    - The 14 rows use VERBATIM copy strings from the plan acceptance criteria — `Why not Apple SharePlay, FaceTime, or Microsoft Teams Together?` / `What's actually in it` / `couch-v48-marketing-refresh` / `Sentry.addBreadcrumb` etc all appear inline so a future audit can grep for them.
    - All 14 rows reference D-XX decision IDs from CONTEXT.md (D-01 through D-27 — exactly the IDs this plan implements). At least 5 D-XX references appear across the 14 rows.
  </acceptance_criteria>

  <done>REQUIREMENTS.md gains a MARK-31-* section with 14 traceability rows; Coverage line incremented +14/+14; chronological ordering preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md Phase 31 row + plan checkboxes + audit narrative</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md (focus on the Phase 31 row at line ~571 + any Phase 31 plan checkboxes if they exist as a separate Plans list)
    - .planning/phases/31-marketing-refresh/31-04-SUMMARY.md (post-deploy commit hashes + deploy timestamp)
    - The Phase 19 / 27 / 28 / 30 row narratives in ROADMAP.md as audit-narrative pattern templates
  </read_first>

  <action>
**Step 1: Locate the Phase 31 row in the ROADMAP Progress table.**

```bash
grep -n '^| 31\.' .planning/ROADMAP.md
```

Current text (per pre-Plan-31 state):
```
| 31. Marketing refresh | 0/5 | **SCOPED 2026-05-05, awaiting `/gsd-discuss-phase 31`** — closes visibility gap between shipped product (28 phases / `couch-v47-pickem`) and public marketing surface (Phase 9 / April 2026). Adds comparison section + features section + FAQ to landing.html; refreshes 5–6 screenshots against current build (couch-viz / wait-up / couch-groups / pickup / decision-explanation / optional pick'em); refreshes og.png. A-step preliminary work already shipped in commit `e49ff34` (changelog catchup v36 → v47, signin polish, audience-grid trim, free/private trust line, secondary CTA repurpose to #how anchor, /privacy + /terms in signin footer). Source-of-truth: `.planning/LAUNCH-REVIEW-2026-05-05.md`. Pre-Phase-17 dependency. Scope: `.planning/phases/31-marketing-refresh/31-CONTEXT.md`. | - |
```

**Step 2: Replace the row with the SHIPPED narrative.**

Use Edit to replace the entire Phase 31 row. Pull the deploy date + cache version + commit details from `31-04-SUMMARY.md` (created by Plan 31-04). If 31-04-SUMMARY.md isn't yet on disk because Plan 31-04 hasn't been committed, fall back to citing acceptance criteria + the planned cache version `couch-v48-marketing-refresh`.

New row (replace `<DEPLOY_DATE>` with the actual deploy date from 31-04-SUMMARY.md, e.g. `2026-05-06`):

```
| 31. Marketing refresh | 5/5 | **SHIPPED <DEPLOY_DATE>** — `couch-v48-marketing-refresh` deployed to couchtonight.app via single-repo couch-only deploy ritual (`bash scripts/deploy.sh 48-marketing-refresh`; D-20 single-repo proof — queuenight + firestore.rules unchanged). 5 plans across 3 waves; closes visibility gap between shipped product (28 phases / `couch-v47-pickem` pre-31) and public marketing surface (Phase 9 / April 2026 pre-31). Wave 1 parallel: 31-01 (landing copy + CSS — comparison 4 blocks per D-12+D-24 / features 4 blocks per D-14 / FAQ 7 questions per D-22 / Sentry breadcrumb on FAQ open per D-27, all using existing :root tokens per D-21) + 31-02 (5 fresh screenshots at marketing/ root captured against couch-v47-pickem per D-05/D-06/D-07; D-25 promotes Pick'em to 5th launch-defining shot; Phase-9 set archived to marketing/archive-phase-9/ per D-09; sharp pipeline matching Phase 9 / Plan 09-06) + 31-03 (brand/og-source.svg authored as 1200×630 source-of-truth + og.png rendered ≤ 200KB via sharp per D-17/D-23 — full visual refresh). Wave 2: 31-04 (og.png?v=2 cache-bust synced across landing/changelog/rsvp.html OG meta per D-18 — rsvp.html gained 12 OG/Twitter tags from zero; scripts/deploy.sh mirror loop extended for og.png + marketing/ + brand/; sw.js bumped to couch-v48-marketing-refresh per D-19; 31-HUMAN-UAT.md scaffolded with 7 device-UAT scripts). Wave 3: 31-05 (REQUIREMENTS.md backfill — 14 MARK-31-* rows added; Coverage incremented +14/+14; ROADMAP narrative refreshed; STATE.md Last Activity updated). All 27 D-XX decisions from CONTEXT.md verified at code level. Existing 13 smoke contracts stay green per D-21 (marketing copy is strings, not behavior — no new smoke). 7 device-UAT items in 31-HUMAN-UAT.md (resume signal `uat passed` → `/gsd-verify-work 31`). | shipped <DEPLOY_DATE> |
```

(Length is comparable to Phase 19 / 27 / 28 narratives in ROADMAP.md — ~1500-1800 chars per row.)

**Step 3: Update the Plans list checkboxes.**

If ROADMAP.md has a "Plans:" subsection for Phase 31 (typical pattern from /gsd-plan-phase ROADMAP update step), update the 5 checkboxes:

Find:
```
Plans:
- [ ] 31-01-PLAN.md — ...
- [ ] 31-02-PLAN.md — ...
...
```

Replace each `[ ]` with `[x]`. If no such subsection exists yet (Plans list is in this PLAN-PHASE step, not ROADMAP), the planner / gsd-plan-phase ROADMAP-update step adds it; this plan flips them to `[x]` post-execution.

**Step 4: Append a Phase 31 close-out footnote at the end of the chronological footnotes list.**

Find the end of the chronological footnotes block (after the most recent entry — typically Phase 28 close-out at the bottom). Insert a new footnote following the existing pattern:

```
*Phase 31 closed-out: <DEPLOY_DATE> (Marketing refresh — 5 plans across 3 waves shipped end-to-end. Closes visibility gap between shipped 28-phase product / couch-v47-pickem pre-31 and public marketing surface / Phase 9 April 2026 pre-31. Wave 1 parallel: landing copy/CSS/Sentry-FAQ-breadcrumb + 5 fresh screenshots + brand/og-source.svg + og.png. Wave 2: og.png?v=2 cross-page sync + deploy.sh mirror extension + sw.js bump + 31-HUMAN-UAT scaffold. Wave 3: REQUIREMENTS.md backfill +14 MARK-31-* rows. All 27 D-XX decisions from CONTEXT.md verified at code level. Single-repo couch-only deploy per D-20; existing 13 smoke contracts stay green per D-21. Pre-Phase-17 dependency satisfied — Phase 17 App Store work can now consume the fresh assets.)*
```
  </action>

  <verify>
    <automated>
grep -E '^\| 31\. Marketing refresh \| 5/5 \|' .planning/ROADMAP.md && echo "phase-31-row-shipped-ok";
grep -q 'couch-v48-marketing-refresh' .planning/ROADMAP.md && echo "cache-version-cited-ok";
! grep -E '^\| 31\. Marketing refresh \| 0/5' .planning/ROADMAP.md && echo "no-stale-scoped-row-ok";
grep -q 'Phase 31 closed-out' .planning/ROADMAP.md && echo "footnote-added-ok"
    </automated>
  </verify>

  <acceptance_criteria>
    - ROADMAP.md Phase 31 row shows `5/5` (NOT `0/5`) in the count column.
    - ROADMAP.md Phase 31 row includes the literal string `couch-v48-marketing-refresh`.
    - ROADMAP.md Phase 31 row includes the literal string `SHIPPED` and the deploy date in the rightmost column matching the leftmost narrative date.
    - ROADMAP.md does NOT contain any `| 31. Marketing refresh | 0/5 |` row anymore (regression guard — only one Phase 31 row exists, and it's the shipped one).
    - ROADMAP.md gains a chronological footnote `*Phase 31 closed-out: <date>` referencing `Marketing refresh`, the 5-plan / 3-wave structure, and the D-20 single-repo deploy proof.
    - ROADMAP.md Phase 31 row narrative cites at least 5 D-XX decisions (D-12 / D-14 / D-17 / D-19 / D-20 / D-21 / D-22 / D-24 / D-25 / D-27 — any 5 or more).
    - The narrative length is 1200-1800 chars (in line with Phase 19 / 27 / 28 / 30 row patterns — long enough to capture audit-relevant detail, not so long it dwarfs neighboring rows).
    - Plans list checkboxes `- [ ] 31-NN-PLAN.md` flipped to `- [x]` for all 5 plans (if such subsection exists).
  </acceptance_criteria>

  <done>ROADMAP.md Phase 31 row shipped-narrative complete; chronological footnote appended; plan checkboxes flipped; D-20 single-repo deploy proof recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md Last Activity field + Open follow-ups for Phase 31 device UAT</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - .planning/STATE.md (full — find frontmatter `status:` line + `Last Activity:` field + `Open follow-ups` section)
    - .planning/phases/15-6/ pattern (Phase 15.6-03 SUMMARY shows the canonical Last Activity update + Open follow-ups compact)
  </read_first>

  <action>
**Step 1: Update the `Last Activity:` field.**

Find the bold-form line (added in Phase 15.6-03 / D-13 to silence `phase complete` CLI warning):
```
**Last Activity:** <date> — <description>
```

Replace with:
```
**Last Activity:** <DEPLOY_DATE> — Phase 31 (Marketing refresh) shipped; couch-v48-marketing-refresh live at couchtonight.app; device UAT pending in 31-HUMAN-UAT.md (resume signal `uat passed` → `/gsd-verify-work 31`)
```

(Use the actual deploy date from Plan 31-04 SUMMARY.)

**Step 2: Append a Phase 31 entry to the Open follow-ups section.**

Find the `## Open follow-ups` heading. Append a single bullet:

```
- [Phase 31 device UAT] 7 scripts in `.planning/phases/31-marketing-refresh/31-HUMAN-UAT.md` awaiting real-device verification (iOS Safari + Android Chrome + Desktop). Resume signal: reply `uat passed` to chat → triggers `/gsd-verify-work 31`. Per CONTEXT D-21 the existing 13 smoke contracts already passed at deploy time — UAT is for visual / accordion / link-preview / Sentry-breadcrumb verification, not behavior regression.
```

If a `## Resolved follow-ups` section exists (added per Phase 15.6-03 / D-14) AND any prior follow-ups have aged > 7 days at the time this plan runs, move them to Resolved. Otherwise leave the Resolved section untouched.

**Step 3: Frontmatter sanity check.**

The status field should remain `In progress` (not `Milestone complete`) since Phase 16 (Calendar Layer) and Phase 17 (App Store Launch Readiness) are still scoped pre-deploy. Phase 31 closing does NOT close the v1 / v2 milestone. Verify the frontmatter `status` field is unchanged.

If a `sw_cache_version` field exists in the frontmatter (added per Phase 15.2 / D-X to decouple cache from milestone), update it to `couch-v48-marketing-refresh`.
  </action>

  <verify>
    <automated>
grep -E '^\*\*Last Activity:\*\*.*Phase 31|^\*\*Last Activity:\*\*.*marketing' .planning/STATE.md && echo "last-activity-updated-ok";
grep -q '\[Phase 31 device UAT\]' .planning/STATE.md && echo "follow-up-added-ok";
grep -q '31-HUMAN-UAT.md' .planning/STATE.md && echo "uat-file-cited-ok";
grep -q 'uat passed' .planning/STATE.md && echo "resume-signal-cited-ok";
grep -E '^status: (In progress|in_progress|in-progress)' .planning/STATE.md && echo "status-unchanged-ok"
    </automated>
  </verify>

  <acceptance_criteria>
    - STATE.md `**Last Activity:**` field references `Phase 31` AND `couch-v48-marketing-refresh` AND `31-HUMAN-UAT.md`.
    - STATE.md Open follow-ups gains a new bullet `[Phase 31 device UAT]` referencing 7 scripts + resume signal `uat passed`.
    - STATE.md frontmatter `status:` field unchanged from pre-edit value (NOT flipped to `Milestone complete` — Phase 16 + Phase 17 still scoped).
    - If `sw_cache_version` field exists in frontmatter, it's updated to `couch-v48-marketing-refresh`.
    - `cli phase-status` warning silencing (per Phase 15.6-03 / D-13) is preserved — the `**Last Activity:**` line stays in bold-form, not converted to plain prose.
    - No accidental edits outside the 3 targeted regions (Last Activity / Open follow-ups / optional sw_cache_version). `git diff .planning/STATE.md | grep -c '^[-+]' | grep -q '^[2-9]\|^[1-9][0-9]'` returns < 30 changed lines.
  </acceptance_criteria>

  <done>STATE.md Last Activity field reflects Phase 31 close-out; Open follow-ups gains Phase 31 device-UAT bullet; status field preserved; sw_cache_version updated if present.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. **Cross-file consistency:**
   ```bash
   grep -c 'MARK-31-' .planning/REQUIREMENTS.md       # expect 14+
   grep -c 'couch-v48-marketing-refresh' .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md   # expect ≥3 (one per file)
   grep -c '31-HUMAN-UAT' .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md  # expect ≥3
   ```

2. **No source-code drift:**
   ```bash
   git diff --stat HEAD~1..HEAD -- 'js/*' 'css/*' 'app.html' 'landing.html' 'sw.js' 'scripts/*' 2>/dev/null | tail -1
   # expect: empty / "0 files changed" — pure docs phase per success criteria
   ```

3. **No new smoke contract introduced (D-21):**
   ```bash
   ls scripts/smoke-*.cjs | wc -l   # should match pre-Phase-31 count + 0
   ```

4. **Markdown sanity:**
   ```bash
   for f in .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md; do
     node -e "require('fs').readFileSync('$f','utf8')" && echo "$f reads OK"
   done
   ```
</verification>

<success_criteria>
- 14 MARK-31-* traceability rows added to REQUIREMENTS.md.
- ROADMAP.md Phase 31 row flipped to 5/5 SHIPPED with audit narrative.
- STATE.md Last Activity reflects Phase 31; Open follow-ups tracks pending device UAT.
- ZERO source-code changes (pure docs phase).
- Existing 13 smoke contracts remained green throughout (D-21 honored).
- All 27 D-XX decisions from CONTEXT.md cited across the 3 updated files.
- Phase 31 verifier (`/gsd-verify-work 31`) can now walk MARK-31-* rows once user approves device UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-refresh/31-05-SUMMARY.md` capturing:
- The 14 MARK-31-NN rows shipped (one-line summary per row).
- Coverage line delta (pre vs post).
- The deploy date used in ROADMAP + STATE (cite Plan 31-04 SUMMARY as source).
- Confirmation that ZERO source-code files were touched (pure docs phase per Phase 15.6 / 15.2 pattern).
- A note that `/gsd-verify-work 31` is the next workflow to run, gated on user replying `uat passed` to chat after device UAT completes.
</output>
