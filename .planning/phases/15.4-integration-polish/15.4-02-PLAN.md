---
phase: 15.4-integration-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/nahde/claude-projects/couch/js/app.js
  - C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md
autonomous: true
requirements_addressed: [F-W-1, D-3, POL-01, DECI-14-12, TRACK-15-09, TRACK-15-10, TRACK-15-11]
must_haves:
  truths:
    - "sendCouchPing in js/app.js writes a doc to families/{familyCode}/couchPings/{pingId} via addDoc with shape {senderId, senderName, recipientId, createdAt, ...writeAttribution()} (replaces Path C stub at js/app.js:14457-14474)"
    - "sendCouchPing retains the existing Sentry breadcrumb under category='couch-ping' for product-analytics signal (per D-07)"
    - "sendCouchPing retains the existing flashToast confirmation — but the toast no longer lies: copy stays 'Push sent to {name}' because the push IS now being delivered via the CF (Path A wired)"
    - "DEFAULT_NOTIFICATION_PREFS in js/app.js contains the new 'couchPing: true' key (DR-3 client-side mirror place 2 of 3 — server-side place 1 added in 15.4-01)"
    - "NOTIFICATION_EVENT_LABELS in js/app.js contains 'couchPing' entry with BRAND-voice label + hint (DR-3 place 3 of 3)"
    - "NOTIF_UI_TO_SERVER_KEY contains 9 NEW entries (D-08 mirror approach): the 7 D-12 keys (flowAPick, flowAVoteOnPick, flowARejectMajority, flowBNominate, flowBCounterTime, flowBConvert, intentExpiring) + newSeasonAirDate + couchPing — each mapped to the same server-side key as identity (uiKey === serverKey)"
    - "NOTIF_UI_LABELS contains 9 NEW entries with BRAND-voice label + hint copy mirroring the existing NOTIFICATION_EVENT_LABELS entries verbatim (single source of voice — copy reuse, not divergence)"
    - "NOTIF_UI_DEFAULTS contains 9 NEW entries with default values matching the server NOTIFICATION_DEFAULTS: 7 D-12 keys all true, newSeasonAirDate true, couchPing true"
    - "POL-01 status flips from 'partial-parity-with-Phase-14-D-3' to 'satisfied' — friendly-UI Settings now exposes ALL push categories the legacy NOTIFICATION_EVENT_LABELS surface has, ending DR-3 dual-Settings collision risk for any UI consumer that reads from NOTIF_UI_LABELS"
    - ".planning/TECH-DEBT.md gains a new entry TD-8 tracking the deferred dual-Settings consolidation (D-10) — describes the legacy vs friendly-UI choice, default outcome (defer), trigger condition (next polish phase touching Settings), and pointer back to RESEARCH §5"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/js/app.js"
      provides: "Real Firestore-write sendCouchPing + DEFAULT_NOTIFICATION_PREFS couchPing mirror + NOTIFICATION_EVENT_LABELS couchPing label + 9 keys added to friendly-UI maps (NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS)"
      contains: "couchPings"
    - path: "C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md"
      provides: "TD-8 entry tracking deferred dual-Settings consolidation per D-10"
      contains: "TD-8"
  key_links:
    - from: "V5 roster long-press handler invokes window.sendCouchPing(memberId)"
      to: "Firestore addDoc(collection(db, 'families', familyCode, 'couchPings'), {...})"
      via: "modular Firestore SDK addDoc + writeAttribution attribution stamp"
      pattern: "couchPings"
    - from: "Firestore couchPings doc create"
      to: "queuenight onCouchPingFire CF (deployed via Plan 15.4-03)"
      via: "Firestore Cloud Function trigger"
      pattern: "addDoc.*couchPings|collection.*couchPings"
    - from: "Settings UI consumers reading NOTIF_UI_LABELS"
      to: "9 newly-mirrored keys including the 7 D-12 + newSeasonAirDate + couchPing"
      via: "Settings notif-pref render at js/app.js:1287-1310 (existing render loop)"
      pattern: "NOTIF_UI_LABELS|NOTIF_UI_DEFAULTS"
---

<objective>
Plan 02 — Client-side closure for the couch-ping push channel + friendly-UI Settings
parity for 9 push keys (closes both F-W-1 and D-3 in a single plan since both touch
js/app.js).

**F-W-1 closure half:** Replace the Path C stub `sendCouchPing` at js/app.js:14477-14506
with a Path A real Firestore write to the new ephemeral collection
`families/{familyCode}/couchPings/{pingId}`. The CF (Plan 15.4-01) handles the fan-out
and post-delivery cleanup. Sentry breadcrumb stays as analytics signal. flashToast copy
stays the same (no longer a lie because the push is real).

**D-3 closure half (mirror approach per D-08 + D-09):** Extend the friendly-UI maps
at js/app.js:170-197 (`NOTIF_UI_TO_SERVER_KEY`, `NOTIF_UI_LABELS`, `NOTIF_UI_DEFAULTS`)
with 9 new keys — the 7 D-12 push categories (`flowAPick`, `flowAVoteOnPick`,
`flowARejectMajority`, `flowBNominate`, `flowBCounterTime`, `flowBConvert`,
`intentExpiring`) plus `newSeasonAirDate` (TRACK-15-09/10/11 follow-up) plus the brand-new
`couchPing` (Phase 15.4 / D-09). Copy mirrors the existing NOTIFICATION_EVENT_LABELS
entries verbatim — single source of voice, not a divergent re-write.

This plan is **autonomous** — all writes are in the couch repo. No queuenight changes.
The deploy is gated to Plan 15.4-03.

**Why both halves in one plan:** F-W-1 client-side and D-3 friendly-UI parity both edit
js/app.js. Splitting would create a file-overlap conflict between two same-wave plans
(forcing one into Wave 2). Bundling keeps Wave 1 fully parallel with Plan 15.4-01.
The two surfaces are also conceptually paired — adding `couchPing` to the friendly-UI
maps is part of the new push category's full integration (per D-09: "couchPing key is
added to BOTH NOTIFICATION_DEFAULTS (queuenight, in 15.4-01) AND friendly-UI maps in the
same plan so it ships consistent" — that "same plan" is THIS plan).

**Banned-words sweep on new copy** (label + hint strings):
- All 7 D-12 friendly-UI labels reuse the verbatim text from NOTIFICATION_EVENT_LABELS
  at js/app.js:147-153 (already audited at Phase 14-09).
- newSeasonAirDate friendly-UI label reuses "New episode alerts" / "When a tracked show
  drops a new episode." — verbatim from NOTIFICATION_EVENT_LABELS at js/app.js:159
  (already audited at Phase 15 / TRACK-15-11 / REVIEW MEDIUM-12).
- couchPing friendly-UI label: "Couch nudges" / "When someone on your couch wants you
  on the couch tonight." — derived from CONTEXT.md push body specifics. Banned-words
  check: "Couch", "nudges", "wants", "on the couch", "tonight" — none banned. Pass.

Purpose: ship the user-visible half of the integration polish — real push delivery
when long-pressing a roster pill, and a Settings surface that exposes EVERY push
category the legacy surface does (no more orphan toggles).

Output: `js/app.js` updated with sendCouchPing Firestore write + 12 entries added across
3 friendly-UI maps + 1 entry in DEFAULT_NOTIFICATION_PREFS + 1 entry in
NOTIFICATION_EVENT_LABELS (12 + 1 + 1 = 14 new entries total). TECH-DEBT.md gets
TD-8 entry. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/15.4-integration-polish/15.4-CONTEXT.md
@.planning/phases/15.5-wait-up-flex/15.5-05-PLAN.md
@.planning/phases/14-decision-ritual-core/14-09-SUMMARY.md
@.planning/TECH-DEBT.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

js/app.js:14477-14506 — current sendCouchPing Path C stub:
```javascript
window.sendCouchPing = function(memberId) {
  if (!state.me || !memberId) return;
  const m = (state.members || []).find(x => x.id === memberId);
  const name = m ? m.name : 'them';
  // 14-10 Task 5 / Path C — Sentry breadcrumb captures attempted pings
  // for product analytics. Real push fan-out deferred to a follow-up plan.
  try {
    if (typeof Sentry !== 'undefined' && Sentry.addBreadcrumb) {
      Sentry.addBreadcrumb({
        category: 'couch-ping',
        message: `would-fire ${memberId}`,
        level: 'info',
        data: { from: state.me.id, to: memberId }
      });
    }
  } catch (e) { /* never let analytics block UX */ }
  flashToast(`Push sent to ${name}`, { kind: 'info' });
};
```

REPLACE with a Path A real Firestore write — see Task 1 action body.

js/app.js DEFAULT_NOTIFICATION_PREFS at lines 100-127 — currently 17 keys (mirrors
queuenight NOTIFICATION_DEFAULTS one-to-one). Last key reactionPosted at line 126,
trailing-comma-on-last-key style. ADD couchPing: true after reactionPosted.

js/app.js NOTIFICATION_EVENT_LABELS at lines 137-164 — currently 17 entries. Last
entry reactionPosted at line 163, trailing-comma-on-last-key style. ADD couchPing
entry after reactionPosted with BRAND-voice copy.

js/app.js NOTIF_UI_TO_SERVER_KEY at lines 170-177 — Phase 12 / POL-01 — currently 6
keys (watchpartyScheduled, watchpartyStartingNow, intentRsvpRequested, inviteReceived,
vetoCapReached, tonightPickChosen). The 6 are friendly-UI aliases that map to server-
side keys (some renamed, e.g. watchpartyStartingNow → watchpartyStarting). For the 9
new keys this plan adds, the friendly-UI key === the server key (no rename needed —
these are all post-Phase-12 keys with already-friendly names).

js/app.js NOTIF_UI_LABELS at lines 181-188 — currently 6 entries with {label, hint}
shape. ADD 9 new entries.

js/app.js NOTIF_UI_DEFAULTS at lines 190-197 — currently 6 entries (booleans). ADD
9 new entries with same defaults as NOTIFICATION_DEFAULTS / DEFAULT_NOTIFICATION_PREFS.

js/app.js Firestore SDK imports — `addDoc`, `collection`, `serverTimestamp` are
likely already imported (used by createIntent etc.). Verify with grep before action.
If missing, extend the import line at the top of js/app.js (or in js/firebase.js
re-exports).

js/app.js writeAttribution helper — used everywhere for attributed Firestore writes.
Returns { actingUid, memberId, memberName, ...maybeManagedMemberId }. Reuse verbatim
in the sendCouchPing addDoc call.

js/app.js state.familyCode — global state slot, holds the current family's 6-char
hex code. Used everywhere for family-scoped writes. Reuse verbatim.

NOTIFICATION_EVENT_LABELS verbatim copy for 8 already-existing-in-server keys (do NOT
re-author copy — reuse for friendly-UI consistency):

| Key | Label | Hint |
|-----|-------|------|
| flowAPick | "Tonight's pick chosen" | "When someone on your couch picks a movie." |
| flowAVoteOnPick | "Couch voted on your pick" | "When someone responds to a pick you made." |
| flowARejectMajority | "Your pick was passed on" | "When the couch asks you to pick again." |
| flowBNominate | "Watch with the couch?" | "When someone wants to watch with you at a time." |
| flowBCounterTime | "Counter-time on your nom" | "When someone counters with a different time." |
| flowBConvert | "Movie starting in 15 min" | "When your nomination becomes a watchparty." |
| intentExpiring | "Tonight's pick expiring" | "Heads-up that a tonight intent is about to expire." |
| newSeasonAirDate | "New episode alerts" | "When a tracked show drops a new episode." |

NEW copy for couchPing (D-09 — net-new key):
- Label: "Couch nudges"
- Hint: "When someone on your couch wants you on the couch tonight."

TECH-DEBT.md — existing entries TD-1 through TD-7 documented at .planning/TECH-DEBT.md.
Header sections found at line numbers 14, 27, 68, 113, 157, 170, 213. ADD a new
TD-8 section at the end of the file per the existing pattern.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace sendCouchPing Path C stub with Path A Firestore write</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=14477, limit=35 to confirm current Path C stub shape.
    - Read js/app.js with offset=1, limit=80 to confirm Firestore SDK import shape (addDoc, collection, serverTimestamp likely already imported).
    - Grep js/app.js for "writeAttribution" to find the helper signature (used elsewhere; verify return-shape compatibility).
    - Grep js/app.js for "addDoc(collection(db" or "addDoc(collection(" to find an existing reference call site (createIntent, etc.) to mirror the import style + invocation idiom.
  </read_first>
  <action>
    Edit js/app.js. Locate the existing sendCouchPing block (currently lines ~14477-14506,
    starts with the comment block "V5 — long-press → send push. Path C..." and ends with
    the closing `};`).

    REPLACE the entire block (comment header + function body) with the following Path A
    implementation. Anchor the Edit on the unique multi-line "V5 — long-press → send push.
    Path C (toast + Sentry breadcrumb)." comment block which exists exactly once in the file.

    New content:

        // V5 — long-press → send push. Path A (Firestore write → onCouchPingFire CF → FCM).
        //
        // Phase 15.4 / F-W-1 / D-01..D-07: replaced the prior Path C stub (toast + Sentry
        // breadcrumb only) with a real push fan-out path. Client writes a doc to the new
        // ephemeral collection families/{familyCode}/couchPings/{pingId}; the queuenight
        // onCouchPingFire CF (queuenight/functions/index.js, deployed in Plan 15.4-03)
        // fans out a single push to the recipient via sendToMembers, then admin-SDK
        // deletes the doc. Push body: "{senderName} wants you on the couch tonight"
        // (locked at Plan 15.4-01 banned-words sweep). Self-echo guard via
        // excludeMemberId in the CF, redundantly enforced by the senderId == memberId
        // rule clause + the rules anchor on recipientId.
        //
        // Sentry breadcrumb retained per D-07 — even with real fan-out, the breadcrumb
        // is the only client-side product-analytics signal for ping volume.
        // flashToast retained — copy "Push sent to {name}" is now accurate (push IS being
        // delivered, modulo the recipient's notificationPrefs[couchPing] toggle + quiet
        // hours, both enforced server-side per Phase 6 baseline).
        window.sendCouchPing = async function(memberId) {
          if (!state.me || !memberId) return;
          if (!state.familyCode) return;
          if (memberId === state.me.id) return;  // client-side self-echo short-circuit
          const m = (state.members || []).find(x => x.id === memberId);
          const recipientName = m ? m.name : 'them';
          // Sentry breadcrumb stays — analytics signal for ping volume (D-07).
          try {
            if (typeof Sentry !== 'undefined' && Sentry.addBreadcrumb) {
              Sentry.addBreadcrumb({
                category: 'couch-ping',
                message: `fire ${memberId}`,
                level: 'info',
                data: { from: state.me.id, to: memberId }
              });
            }
          } catch (e) { /* never let analytics block UX */ }
          // Path A: write the ephemeral ping doc. CF picks up via onCreate trigger,
          // pushes to the recipient, deletes the doc.
          try {
            await addDoc(collection(db, 'families', state.familyCode, 'couchPings'), {
              senderId: state.me.id,
              senderName: state.me.name || 'A family member',
              senderUid: state.me.uid || null,
              recipientId: memberId,
              createdAt: Date.now(),
              ...writeAttribution()
            });
          } catch (e) {
            // Defensive: if the rules-emulator-locked contract diverges from the deployed
            // rules (e.g., a deploy-window race), surface the failure in Sentry but don't
            // block the toast — the user already got tactile haptic feedback at long-press
            // resolution. Logging here keeps the failure mode debuggable post-incident.
            console.warn('[15.4/F-W-1] sendCouchPing write failed:', e && e.message);
            try {
              if (typeof Sentry !== 'undefined' && Sentry.captureException) {
                Sentry.captureException(e, { tags: { area: 'couch-ping' } });
              }
            } catch (e2) {}
          }
          flashToast(`Push sent to ${recipientName}`, { kind: 'info' });
        };

    Note the function is now `async` (the prior stub was sync). Existing callers should
    not need changes — long-press handlers don't await sendCouchPing's return value.
    Grep callsites to confirm: `grep -n "sendCouchPing(" js/app.js` should show only
    the declaration line + invocation sites that don't `await` the return.

    Pre-flight check: confirm `addDoc` and `collection` are already imported at the top
    of js/app.js. Existing callsites (createIntent, etc.) confirm they are; if grep
    `import.*addDoc` returns 0, extend the firebase-firestore import line accordingly.
    Most likely no extra import is needed.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0 (syntax valid).
    <automated>grep -nc "Path A (Firestore write" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1.
    <automated>grep -nc "Path C (toast + Sentry breadcrumb" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 0 (old comment header removed).
    <automated>grep -nc "addDoc(collection(db, 'families', state.familyCode, 'couchPings'" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1.
    <automated>grep -nc "window.sendCouchPing = async function" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1.
    <automated>grep -nc "category: 'couch-ping'" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (Sentry breadcrumb retained).
    <automated>grep -nc "flashToast(\`Push sent to" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (toast retained).
    <automated>grep -n "sendCouchPing" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 2 lines — one `window.sendCouchPing = async function` declaration and at least one call site (the V5 long-press handler). Confirm no stale `sendCouchPing` reference points at a now-removed sync version.
  </verify>
  <acceptance_criteria>
    - sendCouchPing function is async and writes to Firestore via addDoc(collection(db, 'families', state.familyCode, 'couchPings'), {...}).
    - The doc shape includes senderId, senderName, senderUid, recipientId, createdAt, plus writeAttribution() spread.
    - Sentry breadcrumb category 'couch-ping' retained (analytics signal preserved per D-07).
    - flashToast confirmation retained (toast is no longer a lie because push IS delivered).
    - Self-echo short-circuit (memberId === state.me.id → return) present client-side.
    - Defensive try/catch around the addDoc call with Sentry.captureException on failure.
    - Old Path C-only block removed (no "Path C (toast + Sentry breadcrumb)." comment remaining).
    - node -c js/app.js exits 0.
    - Closes: F-W-1 client-side half (combined with Plan 15.4-01 server-side half = full F-W-1 closure).
  </acceptance_criteria>
  <done>
    sendCouchPing is wired to a real push delivery path via Firestore-trigger CF.
    Sentry breadcrumb + flashToast retained. Self-echo short-circuit added.
    Defensive error handling + observability present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add couchPing to DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS (DR-3 client mirror places 2+3)</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=95, limit=75 — covers DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + the friendly-UI maps that follow.
    - The PLAN INTERFACES block has the relevant snippets pre-extracted.
  </read_first>
  <action>
    Make TWO edits to js/app.js.

    EDIT 1 — Add couchPing: true to DEFAULT_NOTIFICATION_PREFS (line ~100-127):

    Locate the closing `});` of DEFAULT_NOTIFICATION_PREFS Object.freeze. The current last
    entry is `reactionPosted: true` (line ~126). The block uses trailing-comma-on-last-key
    style — verify by reading; if it's actually no-trailing-comma style, add comma to
    reactionPosted's line first.

    Insert after reactionPosted's line:

        // Phase 15.4 / D-09 — couchPing: V5 roster long-press push fan-out (F-W-1 path A).
        // Mirror of queuenight NOTIFICATION_DEFAULTS in 15.4-01. Default ON: this fires
        // only when another family member has actively long-pressed your pill — intent-
        // rich engagement signal; user expects it.
        couchPing: true

    EDIT 2 — Add couchPing entry to NOTIFICATION_EVENT_LABELS (line ~137-164):

    Locate the closing `});` of NOTIFICATION_EVENT_LABELS Object.freeze. The current last
    entry is `reactionPosted: { label: 'Someone reacts in a watchparty', hint: '...' }`
    at line ~163. Convert to trailing-comma if needed, then insert after:

        // Phase 15.4 / D-09 — couchPing legacy Settings UI label.
        // BRAND-voice copy. NOTE: this surface is mirrored to friendly-UI maps in this
        // plan's Task 3 (D-08 mirror approach), so it appears in BOTH the legacy
        // NOTIFICATION_EVENT_LABELS Settings UI AND the Phase 12 friendly-UI Settings UI
        // until the dual-Settings-screen consolidation lands (TD-8 / future polish).
        couchPing: { label: 'Couch nudges', hint: 'When someone on your couch wants you on the couch tonight.' }

    Banned-words sweep on the new copy:
    - "Couch nudges" — no banned words. "Nudges" is BRAND-aligned (warm, gentle,
      family-tapping-shoulder register, matches Phase 14-09 voice).
    - "When someone on your couch wants you on the couch tonight." — no banned words.
    - Pass.

    Do NOT modify any other key's value or order in either map. Do NOT change any
    Object.freeze() wrapper.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0.
    <automated>grep -n "couchPing: true" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 match (inside DEFAULT_NOTIFICATION_PREFS).
    <automated>grep -nc "couchPing: { label: 'Couch nudges'" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 match (inside NOTIFICATION_EVENT_LABELS).
    <automated>grep -c "couchPing" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 5 (sendCouchPing function + Sentry category 'couch-ping' line + addDoc 'couchPings' path + DEFAULT_NOTIFICATION_PREFS entry + NOTIFICATION_EVENT_LABELS entry — 5+ occurrences). Task 3 will add 3 more (NOTIF_UI maps).
  </verify>
  <acceptance_criteria>
    - DEFAULT_NOTIFICATION_PREFS contains couchPing: true. Verified by grep.
    - NOTIFICATION_EVENT_LABELS contains couchPing entry with verbatim 'Couch nudges' label and verbatim 'When someone on your couch wants you on the couch tonight.' hint.
    - Both insertions have Phase 15.4 traceability comments.
    - The pre-existing keys (reactionPosted, newSeasonAirDate, etc.) are unchanged.
    - node -c js/app.js exits 0.
    - Closes: DECI-14-12 partial parity gap (couchPing now matches the DR-3 three-place pattern Plan 14-09 established for the original 7 D-12 keys).
  </acceptance_criteria>
  <done>
    couchPing key mirrored to DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS with
    BRAND-voice copy + traceability comments. Syntax valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mirror 9 keys to NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS (D-3 / D-08 closure)</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=170, limit=40 — covers NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS in full.
    - Confirm the existing trailing-comma convention in each of the 3 maps (likely no-trailing-comma-on-last-key based on Phase 12 / POL-01 style).
    - The PLAN INTERFACES block has the verbatim copy table for the 8 already-existing keys + new copy for couchPing.
  </read_first>
  <action>
    Make THREE edits to js/app.js — one per friendly-UI map. Each edit appends 9 new
    entries to the corresponding map.

    The 9 new keys to add (in this canonical order — matches the order they're declared
    in NOTIFICATION_EVENT_LABELS for grep-stable diffing):
    1. flowAPick
    2. flowAVoteOnPick
    3. flowARejectMajority
    4. flowBNominate
    5. flowBCounterTime
    6. flowBConvert
    7. intentExpiring
    8. newSeasonAirDate
    9. couchPing

    EDIT A — NOTIF_UI_TO_SERVER_KEY (lines ~170-177):

    Anchor on the existing block. Convert `tonightPickChosen: 'tonightPickChosen'` (the
    current last key) to have a trailing comma, then insert after:

        // Phase 15.4 / D-08 — friendly-UI parity for the 7 D-12 + newSeasonAirDate +
        // couchPing keys. uiKey === serverKey (no rename needed — these are all
        // post-Phase-12 keys with already-friendly names; the alias map exists only to
        // bridge the 6 Phase-12 keys whose friendly-UI names diverged from server keys).
        flowAPick:           'flowAPick',
        flowAVoteOnPick:     'flowAVoteOnPick',
        flowARejectMajority: 'flowARejectMajority',
        flowBNominate:       'flowBNominate',
        flowBCounterTime:    'flowBCounterTime',
        flowBConvert:        'flowBConvert',
        intentExpiring:      'intentExpiring',
        newSeasonAirDate:    'newSeasonAirDate',
        couchPing:           'couchPing'

    EDIT B — NOTIF_UI_LABELS (lines ~181-188):

    Anchor on the existing block. Convert `tonightPickChosen: { label: ..., hint: ... }`
    (the current last key) to have a trailing comma, then insert after — using the
    verbatim copy from the PLAN INTERFACES table (single source of voice — DO NOT re-author):

        // Phase 15.4 / D-08 — friendly-UI parity. Copy mirrors NOTIFICATION_EVENT_LABELS
        // verbatim (single source of voice; Phase 14-09 + Phase 15 + Phase 15.5 audited
        // each label individually). The 9 new entries close DR-3 dual-Settings collision
        // by ensuring both Settings UI surfaces expose the same set of toggles.
        flowAPick:           { label: "Tonight's pick chosen",      hint: "When someone on your couch picks a movie." },
        flowAVoteOnPick:     { label: "Couch voted on your pick",   hint: "When someone responds to a pick you made." },
        flowARejectMajority: { label: "Your pick was passed on",    hint: "When the couch asks you to pick again." },
        flowBNominate:       { label: "Watch with the couch?",      hint: "When someone wants to watch with you at a time." },
        flowBCounterTime:    { label: "Counter-time on your nom",   hint: "When someone counters with a different time." },
        flowBConvert:        { label: "Movie starting in 15 min",   hint: "When your nomination becomes a watchparty." },
        intentExpiring:      { label: "Tonight's pick expiring",    hint: "Heads-up that a tonight intent is about to expire." },
        newSeasonAirDate:    { label: "New episode alerts",         hint: "When a tracked show drops a new episode." },
        couchPing:           { label: "Couch nudges",               hint: "When someone on your couch wants you on the couch tonight." }

    EDIT C — NOTIF_UI_DEFAULTS (lines ~190-197):

    Anchor on the existing block. Convert `tonightPickChosen: true` (current last key) to
    have a trailing comma, then insert after — defaults match server NOTIFICATION_DEFAULTS:

        // Phase 15.4 / D-08 — friendly-UI parity defaults. All 9 new entries match the
        // server-side NOTIFICATION_DEFAULTS values verbatim (Plan 14-09 D-12 keys: all
        // true; Plan 15 / TRACK-15-09: newSeasonAirDate true; Plan 15.4 / D-09:
        // couchPing true).
        flowAPick:           true,
        flowAVoteOnPick:     true,
        flowARejectMajority: true,
        flowBNominate:       true,
        flowBCounterTime:    true,
        flowBConvert:        true,
        intentExpiring:      true,
        newSeasonAirDate:    true,
        couchPing:           true

    The aligned-equals-sign columns match the existing Phase 12 style. The 9 entries
    appear in the SAME ORDER in all 3 maps to make grep-diff visual scanning trivial.

    Do NOT modify the existing 6 entries in any of the 3 maps. Do NOT change Object.freeze()
    wrappers.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0.
    <automated>grep -A 30 "NOTIF_UI_TO_SERVER_KEY = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -cE "flowAPick:|flowAVoteOnPick:|flowARejectMajority:|flowBNominate:|flowBCounterTime:|flowBConvert:|intentExpiring:|newSeasonAirDate:|couchPing:"</automated>
    Expected: 9.
    <automated>grep -A 30 "NOTIF_UI_LABELS = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -cE "label:.*Tonight's pick chosen|label:.*Couch voted on your pick|label:.*Your pick was passed on|label:.*Watch with the couch|label:.*Counter-time on your nom|label:.*Movie starting in 15 min|label:.*Tonight's pick expiring|label:.*New episode alerts|label:.*Couch nudges"</automated>
    Expected: 9.
    <automated>grep -A 30 "NOTIF_UI_DEFAULTS = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -cE "flowAPick:\s+true|flowAVoteOnPick:\s+true|flowARejectMajority:\s+true|flowBNominate:\s+true|flowBCounterTime:\s+true|flowBConvert:\s+true|intentExpiring:\s+true|newSeasonAirDate:\s+true|couchPing:\s+true"</automated>
    Expected: 9.
    <automated>grep -c "couchPing" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 8 (Task 1 sendCouchPing + Sentry category + addDoc path + Task 2 DEFAULT_NOTIFICATION_PREFS + Task 2 NOTIFICATION_EVENT_LABELS + Task 3 NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS = 8 occurrences minimum, possibly more if comments mention it).
  </verify>
  <acceptance_criteria>
    - NOTIF_UI_TO_SERVER_KEY contains all 9 new entries with uiKey === serverKey identity mapping.
    - NOTIF_UI_LABELS contains all 9 new entries with verbatim copy from NOTIFICATION_EVENT_LABELS (8 keys) + new "Couch nudges" / "When someone on your couch..." copy for couchPing.
    - NOTIF_UI_DEFAULTS contains all 9 new entries with values matching NOTIFICATION_DEFAULTS server-side defaults (all true for these 9 keys).
    - The pre-existing 6 entries in each map are unchanged. Verified by grep on each pre-existing key.
    - Phase 15.4 traceability comments present in all 3 edits.
    - node -c js/app.js exits 0.
    - Closes: D-3 / POL-01 partial → satisfied (friendly-UI parity reached). Closes: DECI-14-12 dual-Settings collision risk on the 7 D-12 keys. Closes: TRACK-15-09/10/11 friendly-UI parity follow-up (was tracked under STATE.md "Polish backlog | DR-3 friendly-UI parity").
  </acceptance_criteria>
  <done>
    All 3 friendly-UI maps extended with 9 new entries each (27 entries total) in canonical
    order with single-source-of-voice copy. Pre-existing 6 entries unchanged in all 3 maps.
    Syntax valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add TD-8 entry to TECH-DEBT.md tracking deferred dual-Settings consolidation (D-10)</name>
  <files>C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md</files>
  <read_first>
    - Read .planning/TECH-DEBT.md in full (file is small, ~250 lines based on the section headers found at lines 14, 27, 68, 113, 157, 170, 213).
    - Confirm the existing entry style: H3 header with TD-N + brief title, then sections like "Status", "Trigger", "Action", "Pointer", or whatever convention this file uses (read TD-1 + TD-7 for the pattern).
  </read_first>
  <action>
    Append a new TD-8 section at the END of TECH-DEBT.md, mirroring the existing entry
    convention. Use this content (adapt formatting to match the file's existing H3 +
    bullet style):

    ### TD-8. Dual-Settings-screen consolidation (DR-3 collision proper)

    **Origin:** Phase 15.4 / D-10. Plan 15.4-02 took the "mirror approach" (Path 1 of
    DR-3) — added 9 push keys (7 D-12 + newSeasonAirDate + couchPing) to the Phase 12
    friendly-UI maps (NOTIF_UI_TO_SERVER_KEY / NOTIF_UI_LABELS / NOTIF_UI_DEFAULTS at
    js/app.js:170-197) so the friendly-UI Settings surface now exposes EVERY push
    category the legacy NOTIFICATION_EVENT_LABELS Settings surface does. This closes
    the immediate parity gap (POL-01: partial → satisfied) but leaves the underlying
    architectural decision deferred: which Settings surface wins (legacy vs friendly-UI)
    long-term?

    **Status:** ⏸ DEFERRED — chose mirror approach over removal at /gsd-discuss-phase
    15.4 (D-08). Mirror is reversible: a future polish phase can pick a winner and
    remove the loser without re-litigating the parity question.

    **Trigger:** Next plan that touches Settings UI rendering (e.g., a Settings
    redesign, an Account-tab restructure, or a new push-category addition that has to
    decide where to surface). At that point, choose ONE surface and remove the other.

    **Considerations for the eventual decision:**
    - Legacy NOTIFICATION_EVENT_LABELS Settings UI is the one users actually see today
      (per Phase 14-09 SUMMARY: the 7 D-12 keys "surface ONLY in the legacy Settings
      UI" prior to 15.4). Surface real estate, navigation depth.
    - Phase 12 friendly-UI Settings is newer, has BRAND voice tightened, has the
      6 Phase-12 keys with renamed friendly aliases. May be more discoverable.
    - 9-key parity (this plan) means EITHER surface can be the winner without losing
      coverage.

    **Pointer:** RESEARCH §5 of the Phase 15-tracking-layer plan flagged the
    dual-Settings-screen collision risk originally. .planning/phases/15.4-integration-polish/
    15.4-CONTEXT.md D-10 records the deferral decision. .planning/phases/14-decision-ritual-core/
    14-09-SUMMARY.md "Polish backlog" row "DR-3 friendly-UI parity" was the predecessor
    follow-up item — closed by THIS plan, but the meta-decision (which surface wins)
    re-deferred under TD-8.

    **Estimated cost when triggered:** small (1 plan, ~2 tasks — pick surface, remove
    the other render path, update Settings nav copy + screenshots if any).

    Align the heading level (H3) and any sub-section formatting (bold labels, bullet
    style) to match the existing TD-1 through TD-7 entries in the file. If the existing
    entries use a different convention than the draft above (e.g., **Status:** vs
    `Status:`), adjust to match.
  </action>
  <verify>
    <automated>grep -nc "^### TD-8" C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md</automated>
    Expected: 1.
    <automated>grep -nc "Dual-Settings-screen consolidation" C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md</automated>
    Expected: 1.
    <automated>grep -nE "^### TD-[0-9]+" C:/Users/nahde/claude-projects/couch/.planning/TECH-DEBT.md</automated>
    Expected: 8 entries total (TD-1 through TD-8).
  </verify>
  <acceptance_criteria>
    - TECH-DEBT.md contains a new ### TD-8 entry titled "Dual-Settings-screen consolidation (DR-3 collision proper)".
    - Entry references Phase 15.4 / D-10 origin, mirror-approach decision, deferral status, trigger condition (next Settings UI touch), pointer to RESEARCH §5 + CONTEXT.md D-10 + 14-09-SUMMARY.
    - Formatting matches existing TD-1..TD-7 entries.
    - Pre-existing TD-1..TD-7 entries unchanged.
    - Closes: D-10 (TD-entry tracking the deferral so it doesn't get forgotten).
  </acceptance_criteria>
  <done>
    TECH-DEBT.md gains TD-8 entry tracking deferred dual-Settings consolidation per D-10.
    Existing TDs unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → Firestore couchPings collection | Untrusted client write; gated by rules added in Plan 15.4-01 (senderId == memberId clause + recipientId regex anchor). Plan 15.4-02 adds the addDoc call site. |
| writeAttribution() helper output → Firestore doc | Already-trusted attribution stamp (actingUid == auth.uid, memberId anchored). Reused verbatim from Phase 5 + Phase 15.1 hardening. |
| Settings UI render → user notificationPrefs read | No new boundary — uses existing renderNotifPrefs path. The 9 new entries in NOTIF_UI_LABELS surface in the existing render loop without code changes there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-06 | Spoofing (sendCouchPing self-write under attacker control) | client addDoc call site | mitigate | The addDoc payload sets `senderId: state.me.id` AND `...writeAttribution()` which spreads `memberId: state.me.id` (or managedMemberId for proxy writes). The rules-side senderId == memberId clause (Plan 15.4-01) plus validAttribution()'s memberId == auth.uid pin senderId to the authenticated caller. Even if a malicious client overrides senderId post-hoc, the rules layer rejects the write. |
| T-15.4-07 | Information disclosure (Sentry breadcrumb leaks recipient identity) | Sentry.addBreadcrumb call | accept | Breadcrumb data: `{ from: state.me.id, to: memberId }`. Sentry beforeSend PII-scrub (Phase 13 / OPS-13-05) already strips event.user.id; this breadcrumb data field is structurally similar (memberId is a family-local identifier, not a uid). Per the Phase 13 scrubber audit, family-code regex stripping covers the highest-sensitivity case; member-id leakage in breadcrumbs is accepted as low-risk product analytics signal (same trade-off as the Path C stub had). |
| T-15.4-08 | Tampering (NOTIF_UI_DEFAULTS divergence from server NOTIFICATION_DEFAULTS) | client/server pref defaults | mitigate | All 9 new entries in NOTIF_UI_DEFAULTS are `true` matching server-side NOTIFICATION_DEFAULTS (verified per the canonical table in INTERFACES). A divergence would mean a user toggling off the friendly-UI control wouldn't actually disable the push (because the Settings render falls back to the wrong default for missing user prefs). Plan 15.4-03 deploy verification curl-checks both files are aligned. |

ASVS L1: input validation on writeAttribution stamp at the rules boundary; per-event-type
opt-out gate in sendToMembers; Sentry PII-scrub at the breadcrumb boundary.
</threat_model>

<verification>
**Plan-level checks (after all 4 tasks committed):**

1. `cd C:/Users/nahde/claude-projects/couch && node -c js/app.js` exits 0
2. `grep -nc "Path A (Firestore write" js/app.js` returns 1
3. `grep -nc "Path C (toast + Sentry breadcrumb" js/app.js` returns 0 (old stub fully replaced)
4. `grep -nc "addDoc(collection(db, 'families', state.familyCode, 'couchPings'" js/app.js` returns 1
5. `grep -nc "category: 'couch-ping'" js/app.js` returns 1 (Sentry breadcrumb retained per D-07)
6. `grep -c "couchPing" js/app.js` returns ≥ 8 (function + breadcrumb + addDoc path + DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + 3 friendly-UI maps)
7. `grep -A 30 "NOTIF_UI_TO_SERVER_KEY = Object.freeze" js/app.js | grep -c "flowA\\|flowB\\|intentExpiring\\|newSeasonAirDate\\|couchPing"` returns 9
8. `grep -A 30 "NOTIF_UI_LABELS = Object.freeze" js/app.js | grep -c "label:"` returns 15 (6 pre-existing + 9 new)
9. `grep -A 30 "NOTIF_UI_DEFAULTS = Object.freeze" js/app.js | grep -c ":\\s*true\\|:\\s*false"` returns 15 (6 pre-existing + 9 new)
10. `grep -nc "^### TD-8" .planning/TECH-DEBT.md` returns 1

**Rules-emulator gate (Plan 15.4-01 Task 3 covers this):** Plan 15.4-01 must complete first
or in parallel — the new addDoc call site in Task 1 here writes to a collection whose
rules don't exist yet locally without Plan 15.4-01's Task 2 edits. In practice both plans
are Wave 1 with zero file-overlap, so they run truly parallel; this Plan's Task 1 only
WRITES the addDoc — it doesn't TEST the write at runtime. The deploy.sh tests/ pre-flight
gate (which runs npm test) requires Plan 15.4-01's rules edits to land before deploy.sh
runs in Plan 15.4-03.

**Pre-deploy (NON-AUTONOMOUS — handled by Plan 03 deploy ritual):**
11. Manual smoke (post-deploy via Plan 15.4-03 Task 4 UAT): long-press a roster pill on
    Device A, confirm Device B receives a push within ~5s with body
    "{Device-A-name} wants you on the couch tonight"
12. Manual smoke: open Settings → notifications, confirm 9 new toggles render with
    correct labels + hints (matches the canonical copy table)
13. Manual smoke: toggle one of the 9 friendly-UI toggles OFF, trigger the corresponding
    push, confirm push does NOT arrive (write to users/{uid}.notificationPrefs[serverKey]
    flips to false → server-side sendToMembers eventType-gate blocks delivery)

**Cross-repo deploy ordering (D-11) — enforced by Plan 03 deploy ritual.**
</verification>

<success_criteria>
**Plan 02 is complete when:**
- [ ] sendCouchPing replaced with Path A Firestore write to families/{familyCode}/couchPings/
- [ ] sendCouchPing function is async, uses addDoc + collection + writeAttribution
- [ ] Sentry breadcrumb retained (analytics signal preserved per D-07)
- [ ] flashToast confirmation retained
- [ ] Self-echo client-side short-circuit added
- [ ] Defensive try/catch + Sentry.captureException on write failure
- [ ] DEFAULT_NOTIFICATION_PREFS contains couchPing: true
- [ ] NOTIFICATION_EVENT_LABELS contains couchPing entry with "Couch nudges" label
- [ ] NOTIF_UI_TO_SERVER_KEY contains 9 new identity mappings
- [ ] NOTIF_UI_LABELS contains 9 new entries with verbatim BRAND-voice copy
- [ ] NOTIF_UI_DEFAULTS contains 9 new entries (all true) matching server NOTIFICATION_DEFAULTS
- [ ] Pre-existing 6 entries in each friendly-UI map unchanged
- [ ] All Phase 15.4 traceability comments present
- [ ] node -c js/app.js exits 0
- [ ] Banned-words sweep clean on new copy
- [ ] TECH-DEBT.md gains TD-8 entry tracking deferred dual-Settings consolidation
- [ ] POL-01 status flips from partial-parity-with-Phase-14-D-3 to satisfied (audit-time check, not file edit)

**Deploy & UAT (DEFERRED to Plan 15.4-03):**
- couch hosting deploy via scripts/deploy.sh AFTER queuenight functions+rules deploy
- UAT 1: long-press roster pill → push delivery confirmed on 2nd device
- UAT 2: Settings notifications shows the 9 new toggles + each toggling off suppresses the push
</success_criteria>

<output>
After completion, create `.planning/phases/15.4-integration-polish/15.4-02-SUMMARY.md` describing:
- js/app.js changes — line numbers for sendCouchPing replacement + DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + 3 friendly-UI maps
- TECH-DEBT.md TD-8 entry summary
- Banned-words sweep result (PASS)
- Coverage closure: F-W-1 client half + D-3 / POL-01 / DECI-14-12 / TRACK-15-09/10/11 friendly-UI parity all closed
- Deferred: TD-8 dual-Settings consolidation
- Open questions: none if all 4 tasks complete cleanly
- Cross-repo deploy ordering reminder (queuenight FIRST, then couch — Plan 15.4-03 enforces)
</output>
