---
phase: 15.4-integration-polish
plan: 03
type: execute
wave: 2
depends_on: [15.4-01, 15.4-02]
files_modified:
  - C:/Users/nahde/claude-projects/couch/sw.js
autonomous: false
requirements_addressed: [F-W-1, D-3, POL-01, DECI-14-06, DECI-14-12, TRACK-15-09, TRACK-15-10, TRACK-15-11]
user_setup:
  - service: firebase-functions + firestore:rules (queuenight repo)
    why: "Cross-repo deploy ordering — queuenight functions + firestore:rules deploy MUST precede couch hosting deploy per RUNBOOK §H. Without this order, clients in the new build will write to /couchPings under rules that don't allow it OR the CF won't be live to fan out, breaking the F-W-1 path A end-to-end during the deploy gap."
    deploy_steps:
      - "Step 1 (queuenight, FIRST): cd ~/queuenight && firebase deploy --only functions,firestore:rules --project queuenight-84044"
      - "Step 1 verify: gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --gen 1 | grep updateTime — must be newer than current live timestamp; if --gen 1 is wrong (firebase-functions v2 SDK migration deferred per TD-1), try without --gen flag"
      - "Step 2 (couch, SECOND): cd ~/claude-projects/couch && bash scripts/deploy.sh 35.6-integration-polish"
      - "Step 2 auto-bumps sw.js CACHE to 'couch-v35.6-integration-polish' (idempotent if Task 1 of this plan already set it), runs tests/npm test pre-flight gate (must pass 46/46), mirrors couch repo to couch-deploy-mirror, runs firebase deploy --only hosting"
      - "Step 3 (UAT): Two-device long-press → push delivery confirmation + Settings UI smoke (9 new toggles render correctly)"
must_haves:
  truths:
    - "sw.js CACHE constant bumped to 'couch-v35.6-integration-polish' (per CONTEXT.md D-12)"
    - "scripts/deploy.sh tests/ pre-flight gate passes (46/46 rules tests including 3 new couchPings tests from Plan 15.4-01)"
    - "Cross-repo deploy ordering followed: queuenight FUNCTIONS + FIRESTORE:RULES deploy precedes couch HOSTING deploy"
    - "queuenight functions deploy includes onCouchPingFire as a newly-created CF (visible in firebase functions:list)"
    - "queuenight firestore:rules deploy includes the new /couchPings/{pingId} rule branch"
    - "Live cache version visible at couchtonight.app/sw.js matches the deployed tag 'couch-v35.6-integration-polish'"
    - "Live couchtonight.app/app loads without errors on a test device"
    - "Two-device UAT confirms long-press a roster pill on Device A delivers a push to Device B with body '{Device-A-name} wants you on the couch tonight' within ~5s"
    - "Settings UI on a logged-in device renders the 9 new push toggles with correct labels (Tonight's pick chosen / Couch voted on your pick / Your pick was passed on / Watch with the couch? / Counter-time on your nom / Movie starting in 15 min / Tonight's pick expiring / New episode alerts / Couch nudges)"
    - "Toggling Couch nudges OFF, then long-pressing a roster pill from another device, results in NO push arriving on the toggled-off device (server-side sendToMembers eventType-gate verified end-to-end)"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/sw.js"
      provides: "Bumped CACHE constant"
      contains: "couch-v35.6-integration-polish"
  key_links:
    - from: "Plan 15.4-01 queuenight functions + rules edits"
      to: "deployed CF + rules in us-central1 / queuenight-84044"
      via: "firebase deploy --only functions,firestore:rules"
      pattern: "firebase deploy --only functions"
    - from: "Plan 15.4-02 couch js/app.js edits + Task 1 sw.js bump"
      to: "deployed hosting at couchtonight.app"
      via: "bash scripts/deploy.sh 35.6-integration-polish"
      pattern: "bash scripts/deploy.sh"
    - from: "Long-press roster pill (V5 Couch viz)"
      to: "Push notification on recipient's device"
      via: "Firestore /couchPings write → onCouchPingFire CF → FCM"
      pattern: "wants you on the couch tonight"
---

<objective>
Plan 03 — Final wave-2 closeout. Bump sw.js CACHE so installed PWAs invalidate on next
online activation, run the cross-repo deploy ritual per RUNBOOK §H + Phase 15.5
precedent, then run two-device UAT to confirm F-W-1 (push delivery) + D-3 (Settings
UI parity) end-to-end.

Deploy ordering is critical (D-11 in CONTEXT.md):

1. **queuenight functions + firestore:rules FIRST** (`firebase deploy --only
   functions,firestore:rules --project queuenight-84044` from `~/queuenight/`) — installs
   Plan 15.4-01's onCouchPingFire CF + couchPing in NOTIFICATION_DEFAULTS + the new
   /couchPings rule branch. Without this, the new client (after step 2 ships) would
   write to /couchPings under rules that DENY (because the new branch isn't live yet)
   OR write successfully but no CF picks up the trigger — either failure mode breaks the
   F-W-1 path A end-to-end during the deploy gap.

2. **couch hosting SECOND** (`bash scripts/deploy.sh 35.6-integration-polish` from couch
   repo) — ships the UI changes from Plan 15.4-02 (sendCouchPing addDoc + 9 friendly-UI
   keys + DEFAULT_NOTIFICATION_PREFS / NOTIFICATION_EVENT_LABELS additions) + the bumped
   sw.js CACHE.

3. **UAT** — two-device test (long-press → push delivery) + Settings UI smoke (9 new
   toggles) + Settings opt-out smoke (toggle off → push suppressed).

Plan 03 is **non-autonomous** — the executor cannot deploy or run real-device UAT
without user-side action. Tasks split:

- **Task 1 (autonomous):** bump sw.js CACHE in source.
- **Task 2 (auto, deploy authorized):** run `firebase deploy --only functions,firestore:rules`
  from `~/queuenight/` per `feedback_deploy_autonomy.md` MEMORY note (Claude may run
  firebase deploy to queuenight-84044 without per-deploy approval for the Couch project).
  If auth fails, escalate to checkpoint:human-action.
- **Task 3 (auto, deploy authorized):** run `bash scripts/deploy.sh 35.6-integration-polish`.
  Same authorization basis. If auth fails, escalate to checkpoint:human-action.
- **Task 4 (checkpoint:human-verify):** UAT — two-device push delivery + Settings UI
  smoke. The user is the only one who can long-press a real device and observe push
  delivery on a second device.

Per `feedback_deploy_autonomy.md`: "For Couch project, Claude may run firebase deploy
to queuenight-84044 without per-deploy approval." This is exactly the precedent the
Phase 15.5-07 ritual followed. The pragmatic shape: Claude attempts the deploy directly
via Bash; if the firebase CLI returns an auth error or network error, the executor
surfaces the failure and pauses for user intervention. Otherwise the deploy runs
autonomously.

Purpose: Ship the F-W-1 push channel end-to-end + the D-3 Settings parity to production
in the correct cross-repo order with PWA cache invalidation, gated by UAT.

Output: sw.js CACHE bumped; live deploys to queuenight-84044 (functions + rules) +
couchtonight.app (hosting); two-device UAT confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/15.4-integration-polish/15.4-CONTEXT.md
@.planning/phases/15.4-integration-polish/15.4-01-PLAN.md
@.planning/phases/15.4-integration-polish/15.4-02-PLAN.md
@.planning/phases/15.5-wait-up-flex/15.5-07-PLAN.md
@.planning/phases/15.5-wait-up-flex/15.5-07-SUMMARY.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

sw.js current source line 8:
  const CACHE = 'couch-v35.5.5-tonight-couch-abstain-ok';

This was bumped post-15.5 ship by an interim hotfix (per STATE.md trail v35.5 → v35.5.5).
Plan 15.4-03 bumps to:
  const CACHE = 'couch-v35.6-integration-polish';

Tag rationale per CONTEXT.md D-12: "couch-v35.6-integration-polish" — next decimal in the
v35.x.y stream after v35.5.5. Phase number reflects 15.4-integration-polish slug.

scripts/deploy.sh — existing deploy script per RUNBOOK §H + Phase 13 / OPS-13-02. Behaviors
(verified by reading deploy.sh:1-110 in the planning step):
- Resolves $COUCH_DEPLOY_PATH (default: ../../couch-deploy; legacy fallback ../../queuenight)
- Aborts if working tree dirty (--allow-dirty escape)
- Runs `cd tests && npm test` if tests/ exists — MUST pass before continuing
- Runs `node --check` on every shipping JS file
- Runs scripts/smoke-position-transform.cjs + smoke-tonight-matches.cjs if present
- Optionally bumps sw.js CACHE if a tag is passed (idempotent if source already at that value)
- Mirrors files to ${COUCH_DEPLOY_PATH}/public/
- Auto-stamps BUILD_DATE in mirror's js/constants.js ONLY (source tree clean)
- Aborts if app.html / landing.html still contain Sentry DSN placeholder
- Runs firebase deploy --only hosting --project queuenight-84044
- Curl-based smoke tests against couchtonight.app

`firebase deploy --only functions,firestore:rules --project queuenight-84044` from
~/queuenight/ — separate command (NOT covered by deploy.sh). Per RUNBOOK §H:
  "Cloud Functions deploy is separate (NOT covered by deploy.sh — too risky to bundle)"
  "Firestore rules deploy: cd $QUEUENIGHT_PATH && firebase deploy --only firestore:rules
   --project queuenight-84044"

The two `--only` filters can be combined: `--only functions,firestore:rules` — verified
this is a valid firebase CLI invocation per the Phase 14 deploy ritual at 14-09-SUMMARY.md
(separate calls were used historically; combining is an optimization for this single
phase that ships both surfaces in one cross-repo step).

UAT inputs needed (Task 4):
- Two devices (a phone + tablet, or two phones) on the same family
- Each device signed in as a different family member
- Both devices have notifications enabled (granted permission)
- Neither device is in quiet hours (or both are NOT in quiet hours for the test window)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump sw.js CACHE constant to couch-v35.6-integration-polish</name>
  <files>C:/Users/nahde/claude-projects/couch/sw.js</files>
  <read_first>
    - Read sw.js with offset=1, limit=15 — confirm current CACHE constant value at line 8.
    - Confirm via grep that no other line in sw.js declares CACHE.
  </read_first>
  <action>
    Edit sw.js line 8. Current:
      const CACHE = 'couch-v35.5.5-tonight-couch-abstain-ok';

    Change to:
      const CACHE = 'couch-v35.6-integration-polish';

    Rationale per CONTEXT.md D-12: bump CACHE to a phase-aligned tag. Phase 15.4 ships
    multiple user-visible changes — sendCouchPing real push delivery (visible via
    push notification on receiver's device + Sentry breadcrumb pattern flip from
    "would-fire" to "fire") + 9 new Settings toggles (visible in Account → notifications).
    The CACHE bump is mandatory per CLAUDE.md: "any user-visible app change requires a
    sw.js CACHE bump."

    If `bash scripts/deploy.sh 35.6-integration-polish` is run in Task 3, deploy.sh will
    RE-BUMP this same CACHE constant — idempotent. Bumping in source first is defensive
    (lets `bash scripts/deploy.sh` without args still ship the right tag) and matches
    the Phase 15.5-07 close-out pattern.
  </action>
  <verify>
    <automated>grep -nc "^const CACHE = 'couch-v35.6-integration-polish';" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 1 match (line 8).
    <automated>grep -nc "couch-v35.5.5-tonight-couch-abstain-ok" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 0 (old tag fully replaced).
    <automated>grep -nc "^const CACHE" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 1 (no duplicate CACHE declarations introduced).
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c sw.js</automated>
    Expected: exit 0.
  </verify>
  <acceptance_criteria>
    - sw.js line 8 contains: const CACHE = 'couch-v35.6-integration-polish';
    - Old couch-v35.5.5-tonight-couch-abstain-ok tag is REMOVED.
    - Exactly ONE const CACHE declaration in sw.js.
    - node -c sw.js exits 0.
  </acceptance_criteria>
  <done>
    sw.js CACHE bumped to couch-v35.6-integration-polish in source.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deploy queuenight functions + firestore:rules FIRST (cross-repo ordering)</name>
  <files></files>
  <read_first>
    - Read .planning/RUNBOOK.md §H (offset=226, limit=90) — cross-repo deploy ordering reference.
    - Read .planning/phases/15.5-wait-up-flex/15.5-07-PLAN.md Task 2 — Phase 15.5 deploy precedent.
    - Confirm Plan 15.4-01 changes are committed in BOTH repos (couch + queuenight) before running deploys. If queuenight has no .git on this machine (matches Phase 14-09 + 15.5-05 pattern), surface this as a deviation and pause for user to commit on a queuenight-equipped session — proceed with deploy only after commit confirmation.
  </read_first>
  <action>
    Per `feedback_deploy_autonomy.md` MEMORY note ("For Couch project, Claude may run
    firebase deploy to queuenight-84044 without per-deploy approval"), this task is
    AUTHORIZED to run firebase deploy directly. The shape:

    1. Pre-flight verification — confirm Plan 15.4-01 changes landed in queuenight:
       <automated>grep -nc "couchPing: true" C:/Users/nahde/queuenight/functions/index.js</automated>
       <automated>grep -nc "exports.onCouchPingFire" C:/Users/nahde/queuenight/functions/index.js</automated>
       <automated>grep -nc "match /couchPings" C:/Users/nahde/queuenight/firestore.rules</automated>
       All 3 expected to return 1.

    2. Pre-flight commit check — `cd C:/Users/nahde/queuenight && git status --short`. If
       not empty, this means Plan 15.4-01's queuenight edits are uncommitted on this
       machine (matches the Phase 14-09 + 15.5-05 deviation pattern when queuenight has
       no .git here). Surface this in the executor output — pause for the user to commit
       from a queuenight-equipped session before continuing. The commit message format:
       `feat(15.4-01): add couchPing event type + onCouchPingFire CF + couchPings rules`

    3. Run the cross-repo deploy:
       <automated>cd C:/Users/nahde/queuenight && firebase deploy --only functions,firestore:rules --project queuenight-84044</automated>
       Expected output: ends with "Deploy complete!" and lists `onCouchPingFire(us-central1)`
       in the deployed-functions list. firestore:rules deploy outputs "compiled successfully"
       + the deploy timestamp.

    4. Verify deploy timestamps:
       <automated>cd C:/Users/nahde/queuenight && firebase functions:list --project queuenight-84044 2>&1 | grep -i couchping</automated>
       Expected: a row containing "onCouchPingFire" with a recent updateTime.

       OR via gcloud:
       <automated>gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --gen 1 --format="value(updateTime)" 2>&1 || gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --format="value(updateTime)"</automated>
       Expected: timestamp within the last few minutes.

    5. Verify firestore.rules deploy via the Firebase Console UI link in the deploy
       output, OR via:
       <automated>cd C:/Users/nahde/queuenight && firebase deploy --only firestore:rules --project queuenight-84044 --debug 2>&1 | head -10</automated>
       Expected: "Deploy complete!" — second invocation should be a no-op since the rules
       were just deployed in step 3, but if there was a partial failure earlier this
       confirms the rules half landed.

    Failure modes:
    - `Error: Authentication Error: Your credentials are no longer valid` → user must
      `firebase login` first. Surface error verbatim, escalate to checkpoint:human-action.
    - `Error: HTTP Error: 403, Permission denied` → IAM issue, surface verbatim, escalate.
    - `firestore-emulator-suite missing` → ignore (only matters for local emulator, not
      for deploy).

    If steps 1-5 all succeed, mark Task 2 complete and proceed to Task 3.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/queuenight && firebase functions:list --project queuenight-84044 2>&1 | grep -ci onCouchPingFire</automated>
    Expected: ≥ 1 (the function appears in the deployed list).
    <automated>gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --gen 1 --format="value(updateTime)" 2>&1 | head -1</automated>
    Expected: an ISO timestamp within the deploy window (last ~30 min).
  </verify>
  <acceptance_criteria>
    - firebase deploy --only functions,firestore:rules --project queuenight-84044 exited 0 with "Deploy complete!" output.
    - onCouchPingFire function appears in firebase functions:list with a recent updateTime.
    - The deploy ran from C:/Users/nahde/queuenight (NOT from couch repo).
    - Plan 15.4-01's queuenight edits were committed before deploy (NOT just applied in-place — that was the Phase 14-09 + 15.5-05 deviation pattern; this plan should not repeat the deviation if avoidable).
    - Closes/partially-closes: F-W-1 deploy half (server side live).
  </acceptance_criteria>
  <done>
    queuenight functions + firestore:rules deployed. onCouchPingFire CF live in us-central1.
    /couchPings rule branch live. Cross-repo ordering preserved (this task runs BEFORE Task 3).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Deploy couch hosting SECOND via scripts/deploy.sh</name>
  <files></files>
  <read_first>
    - Read .planning/RUNBOOK.md §H (already in context from Task 2).
    - Confirm Task 2 succeeded (queuenight deploy timestamp recent).
    - Confirm Plan 15.4-02 changes are committed in couch repo + Task 1 sw.js bump committed.
  </read_first>
  <action>
    Per `feedback_deploy_autonomy.md`, this task is AUTHORIZED to run the couch deploy
    directly. Shape:

    1. Confirm Task 2 succeeded (queuenight functions + rules live):
       <automated>gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --gen 1 --format="value(updateTime)" 2>&1 | head -1</automated>
       Expected: very recent timestamp (within last hour). If Task 2 was skipped or failed,
       DO NOT proceed — there will be a failure window during which clients in the new
       build write to /couchPings but no CF + no rules cover the path.

    2. Confirm couch repo is cleanly committed:
       <automated>cd C:/Users/nahde/claude-projects/couch && git status --short</automated>
       Expected: empty (clean working tree). Plans 15.4-01 (couch tests + rules side) +
       15.4-02 + Task 1 sw.js bump should all be committed.

    3. Confirm sw.js CACHE bump landed:
       <automated>grep -n "^const CACHE = 'couch-v35.6-integration-polish';" C:/Users/nahde/claude-projects/couch/sw.js</automated>
       Expected: 1 match (Task 1 of this plan).

    4. Confirm Plan 15.4-02 client changes landed:
       <automated>grep -nc "Path A (Firestore write" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
       <automated>grep -A 30 "NOTIF_UI_LABELS = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -c "label:"</automated>
       Expected: 1 (sendCouchPing replaced) and ≥ 15 (6 pre-existing + 9 new entries).

    5. Run the hosting deploy:
       <automated>cd C:/Users/nahde/claude-projects/couch && bash scripts/deploy.sh 35.6-integration-polish</automated>
       This invokes the full deploy ritual:
       - cd tests && npm test (rules-emulator suite — MUST pass 46/46 including the 3 new couchPings tests from Plan 15.4-01)
       - node --check on all shipping JS files
       - smoke-position-transform.cjs + smoke-tonight-matches.cjs (smoke gates)
       - sw.js CACHE confirmed (no-op since Task 1 set it)
       - Mirrors couch root + js/ + css/ to ${COUCH_DEPLOY_PATH}/public/
       - Auto-stamps BUILD_DATE in mirror's js/constants.js
       - Aborts if app.html / landing.html still contain Sentry DSN placeholder
       - firebase deploy --only hosting --project queuenight-84044
       - Curl-based smoke tests against couchtonight.app
       Expected output ends with "Deploy complete!" and a couchtonight.app project URL.

    6. Verify live cache version:
       <automated>curl -s https://couchtonight.app/sw.js | grep "^const CACHE"</automated>
       Expected: const CACHE = 'couch-v35.6-integration-polish';

    7. Verify Sentry DSN substituted (NOT placeholder):
       <automated>curl -s https://couchtonight.app/app | grep -oE 'sentry\\.io/[0-9]+' | head -1</automated>
       Expected: a real Sentry project URL like sentry.io/4511281871454208.

    8. Quick rendering smoke check by curl:
       <automated>curl -sI https://couchtonight.app/app | head -3</automated>
       Expected: HTTP/1.1 200 OK.

    Failure modes:
    - tests/npm test fails → likely a regression introduced by Plan 15.4-01 or 15.4-02.
      Surface output, escalate to checkpoint:human-action.
    - `working tree is dirty` → user has uncommitted edits. Surface, ask user to commit.
    - Auth error → surface, escalate.
    - DSN placeholder leak → surface, escalate (this is a Plan 13 / OPS-13-05 guard).

    If steps 1-8 all succeed, proceed to Task 4.
  </action>
  <verify>
    <automated>curl -s https://couchtonight.app/sw.js | grep -c "couch-v35.6-integration-polish"</automated>
    Expected: 1 (live deploy reflects the bumped tag).
    <automated>curl -sI https://couchtonight.app/app 2>&1 | head -1</automated>
    Expected: contains "200 OK" (app shell loads).
    <automated>curl -s https://couchtonight.app/app | grep -oE 'sentry\\.io/[0-9]+' | head -1</automated>
    Expected: a real Sentry DSN URL (NOT __SENTRY_DSN__).
  </verify>
  <acceptance_criteria>
    - bash scripts/deploy.sh 35.6-integration-polish exited 0.
    - tests/ pre-flight gate passed (46/46 including new couchPings tests).
    - Live cache version at couchtonight.app/sw.js is couch-v35.6-integration-polish.
    - couchtonight.app/app loads with HTTP 200.
    - Sentry DSN is a real URL, not the placeholder.
    - Closes: D-12 (PWA cache invalidation live).
  </acceptance_criteria>
  <done>
    couch hosting deployed via scripts/deploy.sh. sw.js bumped + tests passed + live deploy
    verified via curl. Cross-repo ordering preserved (this task runs AFTER Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Two-device UAT — push delivery + Settings UI parity</name>
  <what-built>
    All Phase 15.4 user-visible behavior is now live at couchtonight.app at cache version
    couch-v35.6-integration-polish:
    - F-W-1 closure: sendCouchPing now fires a real push via the new onCouchPingFire CF +
      /couchPings ephemeral collection. Long-pressing a roster pill on the V5 Couch viz
      sends a push to that recipient with body "{senderName} wants you on the couch
      tonight".
    - D-3 closure: Friendly-UI Settings (Account → notifications) now exposes 9 additional
      push toggles: Tonight's pick chosen, Couch voted on your pick, Your pick was passed
      on, Watch with the couch?, Counter-time on your nom, Movie starting in 15 min,
      Tonight's pick expiring, New episode alerts, Couch nudges. Each is opt-out via the
      friendly-UI Settings AND the legacy Settings UI (DR-3 collision deferred per TD-8).
  </what-built>
  <how-to-verify>
    Two-device test required (the long-press → push delivery path cannot be automated
    from a single browser — the receiver-side push delivery only triggers on a real OS-
    level subscription). Use a phone + tablet OR two phones, both signed in as different
    family members of the same family.

    Pre-flight (single device):
    1. Open couchtonight.app/app on Device A. Hard-refresh to pick up the new SW (or
       wait ~10s for the SW activate handler to clear old caches per sw.js:22-28).
    2. Confirm couchtonight.app/app version. Open DevTools > Application > Service
       Workers — confirm the active SW shows CACHE name 'couch-v35.6-integration-polish'.
       OR run in the JS console: `caches.keys().then(console.log)` — expect to see
       'couch-v35.6-integration-polish' in the list.

    UAT 1 — Settings UI shows 9 new toggles (single device):
    3. On Device A, open Settings → notifications (or Account → notifications,
       depending on app navigation).
    4. Confirm 9 NEW toggles render with these labels:
       a. "Tonight's pick chosen"
       b. "Couch voted on your pick"
       c. "Your pick was passed on"
       d. "Watch with the couch?"
       e. "Counter-time on your nom"
       f. "Movie starting in 15 min"
       g. "Tonight's pick expiring"
       h. "New episode alerts"
       i. "Couch nudges"
    5. Tap each label briefly to confirm the hint text expands (or check via long-press
       / tooltip — UI behavior depends on the Phase 12 friendly-UI render path):
       a. "When someone on your couch picks a movie."
       b. "When someone responds to a pick you made."
       c. "When the couch asks you to pick again."
       d. "When someone wants to watch with you at a time."
       e. "When someone counters with a different time."
       f. "When your nomination becomes a watchparty."
       g. "Heads-up that a tonight intent is about to expire."
       h. "When a tracked show drops a new episode."
       i. "When someone on your couch wants you on the couch tonight."
    6. Confirm all 9 toggles default to ON (matches NOTIF_UI_DEFAULTS = true for each).

    UAT 2 — F-W-1 push delivery (two devices):
    7. On Device A, navigate to the Tonight tab. Confirm the V5 Couch viz renders with
       member pills.
    8. On Device B (signed in as a DIFFERENT family member than Device A — let's call
       them Member-B), confirm permission for notifications is granted.
    9. On Device A, long-press the Member-B pill in the Couch viz. Expected behavior:
       - Pill shows progress underline animation (D-04 from Plan 14-10)
       - On 700ms long-press resolution, flashToast appears: "Push sent to {Member-B-name}"
       - Haptic feedback fires on Device A
    10. Within ~5s, Device B receives a push notification:
        - Title: "{Device-A-member-name}" (e.g., "Member" or "Owner")
        - Body: "{Device-A-member-name} wants you on the couch tonight"
    11. Tap the push on Device B — confirm it deep-links to /app (the app shell).
    12. Verify Sentry breadcrumb fired (optional, requires Sentry inbox access):
        - Sentry → couch project → Issues filtered by category=couch-ping
        - Confirm a breadcrumb of category 'couch-ping' message 'fire {Member-B-id}'
          appeared in the recent feed.

    UAT 3 — Opt-out verification (two devices):
    13. On Device B, open Settings → notifications. Toggle "Couch nudges" OFF.
    14. On Device A, long-press Member-B pill again.
    15. Confirm Device A still sees flashToast "Push sent to {Member-B-name}" (the toast
        is unchanged — the client doesn't know the recipient's pref state).
    16. Confirm Device B does NOT receive a push notification (server-side
        sendToMembers eventType-gate at queuenight/functions/index.js:138 silently drops
        the send when notificationPrefs[couchPing] === false).
    17. On Device B, toggle "Couch nudges" back ON to leave Settings in default state.

    UAT 4 — Self-echo guard (single device, optional):
    18. On Device A, attempt to long-press OWN pill (the "ME" pill, your own member entry).
        Expected: client-side short-circuit (memberId === state.me.id → return) prevents
        the addDoc write. flashToast still appears for tactile confirmation OR is
        suppressed (depending on UX choice — observe what actually happens).
    19. Confirm Device A does NOT receive a push to itself even if rules-side enforcement
        had a gap.

    All 4 UAT scripts must pass before declaring Phase 15.4 SHIPPED.

    If any script reveals a bug (push doesn't arrive, label is wrong, opt-out doesn't
    suppress, self-echo fires a push), surface the failure mode verbatim. Decide together
    whether to gap-close (new plan 15.4-04) or back out the deploy.

    Optional post-deploy soak: within ~24h, monitor Sentry for unexpected
    [15.4/F-W-1] error breadcrumbs from in-flight pre-v35.6 PWAs that haven't yet picked
    up the new sw.js. Service worker auto-revalidates on next online activation; small
    spike from old PWAs would be expected and benign. Persistent spike past 1h triggers
    investigation.
  </how-to-verify>
  <resume-signal>Type "uat passed" to mark Phase 15.4 fully shipped. If any script failed, paste the failure mode and we'll triage gap-closure plans vs rollback.</resume-signal>
  <acceptance_criteria>
    - All 4 UAT scripts (1 + 2 + 3 + 4) PASSED.
    - User confirmation via "uat passed" or equivalent.
    - F-W-1 push delivery confirmed end-to-end on real devices.
    - D-3 / POL-01 friendly-UI Settings parity confirmed (9 toggles render correctly).
    - Opt-out path verified (toggle off → push suppressed).
    - Self-echo guard verified (no push to self).
    - No regressions in adjacent features (existing push categories still work; Settings UI for the original 6 Phase-12 toggles unchanged).
    - Closes: F-W-1, D-3, POL-01, DECI-14-06, DECI-14-12, TRACK-15-09/10/11 friendly-UI parity follow-up.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy pipeline | Cross-repo deploy ordering enforces D-11 (no rules-write-window between hosting and functions/rules deploys). |
| Live FCM push delivery | Per-receiver eventType-gate at sendToMembers verifies the deployed CF + the deployed user prefs are in sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-09 | Information disclosure / Tampering (R-8 deploy ordering analog) | Cross-repo deploy gap | mitigate | Task 2 (queuenight functions+rules) MUST complete before Task 3 (couch hosting). The task sequencing in this plan enforces the order. If Task 2 fails, Task 3 does not run — no rules-deny window where new clients write to /couchPings under old rules that DENY the write (or worse, allow but with no CF to fan out). |
| T-15.4-10 | Tampering (old PWA caches serving stale js/app.js calling old sendCouchPing) | sw.js CACHE bump | mitigate | Task 1 bumps CACHE to couch-v35.6-integration-polish. Installed PWAs invalidate on next online activation. Old PWAs continue to work in degraded mode (Path C stub — toast + Sentry breadcrumb only, no real push) until cache flushes. Acceptable graceful degradation. |
| T-15.4-11 | Denial of service (post-deploy malformed couchPings docs) | onCouchPingFire CF cleanup | mitigate | Plan 15.4-01 Task 1 added defensive cleanup: malformed pings (missing senderId or recipientId) trigger `snap.ref.delete()` so the doc doesn't linger. Task 4 UAT confirms the happy-path cleanup also works (delete-after-fan-out). Storage cost remains bounded. |

ASVS L1: deploy pipeline ordering enforced by sequential task dependencies; Sentry DSN
production-boundary guard enforced by deploy.sh; rules-emulator pre-flight gate enforced
by deploy.sh tests/ npm test invocation.
</threat_model>

<verification>
**Plan-level checks (after all 4 tasks committed and deployed):**

1. `grep -nc "couch-v35.6-integration-polish" sw.js` returns 1 (source bumped — Task 1)
2. `curl -s https://couchtonight.app/sw.js | grep -c CACHE` returns 1 with the new tag (live deploy — Task 3)
3. `gcloud functions describe onCouchPingFire --project queuenight-84044 --region us-central1 --gen 1 --format="value(updateTime)"` returns a timestamp within the deploy window (Task 2)
4. firestore.rules in production includes the /couchPings block — verifiable indirectly via Task 4 UAT 2 (push delivery succeeds = rules allowed the create write)
5. UAT 1-4 PASSED per user confirmation (Task 4)
6. Phase 15.4 SHIPPED marker added to STATE.md by post-execution workflow (separate `/gsd-verify-work` step)
</verification>

<success_criteria>
**Plan 03 is complete when:**
- [ ] sw.js CACHE bumped to couch-v35.6-integration-polish
- [ ] queuenight functions deployed (Plan 15.4-01 changes live in us-central1) — onCouchPingFire CF visible in functions:list
- [ ] queuenight firestore:rules deployed (/couchPings rule branch live)
- [ ] couch hosting deployed via scripts/deploy.sh (Plan 15.4-02 + Task 1 live at couchtonight.app)
- [ ] tests/ pre-flight gate passed (46/46 rules tests)
- [ ] Cross-repo deploy ordering followed (functions+rules → hosting, NOT the reverse)
- [ ] Live cache tag verified via curl
- [ ] UAT 1-4 (Settings UI + push delivery + opt-out + self-echo) all PASSED
- [ ] No regressions in adjacent features
- [ ] Sentry post-deploy soak window monitored (24h, separate follow-up note)
</success_criteria>

<output>
After completion (post-UAT-pass), create `.planning/phases/15.4-integration-polish/15.4-03-SUMMARY.md` describing:
- Cache version before/after (couch-v35.5.5-tonight-couch-abstain-ok → couch-v35.6-integration-polish)
- queuenight deploy timestamp + functions:list output (onCouchPingFire visible)
- couch deploy timestamp + live tag verification (curl output)
- UAT script results (1/2/3/4 pass/fail per script)
- Any post-deploy issues observed (rollback decisions, gap-closure plan candidates)
- Sentry soak window outcome (after 24h — separate follow-up note)
- All Phase 15.4 closures: F-W-1 + D-3 + POL-01 + DECI-14-12 friendly-UI parity follow-up + TRACK-15-09/10/11 friendly-UI parity follow-up — all CLOSED end-to-end
- STATE.md sw_cache_version field updated to couch-v35.6-integration-polish (handled by post-execution workflow, not this plan directly)

Phase 15.4 is now SHIPPED. Trigger `/gsd-verify-work 15.4` to formalize the verification record.
</output>
