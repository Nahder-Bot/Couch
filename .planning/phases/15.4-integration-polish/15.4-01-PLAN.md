---
phase: 15.4-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/nahde/queuenight/functions/index.js
  - C:/Users/nahde/queuenight/firestore.rules
  - C:/Users/nahde/claude-projects/couch/firestore.rules
  - C:/Users/nahde/claude-projects/couch/tests/firestore.rules
  - C:/Users/nahde/claude-projects/couch/tests/rules.test.js
autonomous: true
requirements_addressed: [F-W-1, DECI-14-06, DECI-14-12]
user_setup:
  - service: firebase-functions (queuenight repo, sibling to couch repo)
    why: "New Cloud Function `onCouchPingFire` + `couchPing` key in NOTIFICATION_DEFAULTS + new firestore rules branch for ephemeral `couchPings` subcollection. Source edits land in this plan; the deploy is gated to Plan 15.4-03 per RUNBOOK §H cross-repo ordering."
    env_vars: []
    dashboard_config: []
    deploy_steps:
      - "Plan 15.4-03 Task 2 will run `firebase deploy --only functions,firestore:rules --project queuenight-84044` from `~/queuenight/` BEFORE the couch hosting deploy. This plan only writes the source — it does NOT deploy."
      - "Per `feedback_deploy_autonomy.md` MEMORY note, Claude is authorized to run `firebase deploy` to queuenight-84044 without per-deploy approval — but only at Wave 2 / Plan 15.4-03."
must_haves:
  truths:
    - "queuenight/functions/index.js NOTIFICATION_DEFAULTS map contains a new key `couchPing: true`"
    - "queuenight/functions/index.js exports a new Cloud Function `onCouchPingFire` triggered on document create at families/{familyCode}/couchPings/{pingId}"
    - "onCouchPingFire reads sender + recipient member docs, calls sendToMembers([recipientId], payload, {excludeMemberId: senderId, eventType: 'couchPing'})"
    - "onCouchPingFire push body is verbatim '{senderName} wants you on the couch tonight' (no banned words, BRAND voice)"
    - "onCouchPingFire respects quiet hours via the existing sendToMembers eventType-gated path (which calls isInQuietHours under the hood)"
    - "onCouchPingFire applies self-echo guard via excludeMemberId: senderId (mirrors Phase 6 PUSH-05 pattern)"
    - "onCouchPingFire deletes the ping doc after fan-out (auto-clean ephemeral collection per Discretion default)"
    - "onCouchPingFire emits a console.log delivery-attempt line replacing the Sentry breadcrumb's role on the server side (per D-07)"
    - "queuenight/firestore.rules has a new `match /couchPings/{pingId}` block: allow create iff attributedWrite() AND senderId+recipientId both anchored to '^m_[A-Za-z0-9_-]+$'; allow read for any family member; allow update/delete never (CF admin SDK bypasses rules for cleanup)"
    - "couch/firestore.rules and couch/tests/firestore.rules are byte-identical mirrors of queuenight/firestore.rules (3-way lockstep maintained)"
    - "tests/rules.test.js extended with 3 new tests — #41 authed member writes own couchPing → ALLOWED, #42 forged senderId mismatching memberId → DENIED, #43 forged recipientId='.*' regex injection → DENIED"
    - "tests/rules.test.js continues to pass 43+3=46 cases via `cd tests && npm test`"
  artifacts:
    - path: "C:/Users/nahde/queuenight/functions/index.js"
      provides: "couchPing in NOTIFICATION_DEFAULTS + new onCouchPingFire CF"
      contains: "couchPing: true"
    - path: "C:/Users/nahde/queuenight/firestore.rules"
      provides: "match /couchPings/{pingId} block — create attributed, read family-scoped, no client update/delete"
      contains: "match /couchPings"
    - path: "C:/Users/nahde/claude-projects/couch/firestore.rules"
      provides: "Lockstep mirror of queuenight rules — couchPings block present"
      contains: "match /couchPings"
    - path: "C:/Users/nahde/claude-projects/couch/tests/firestore.rules"
      provides: "Lockstep mirror of queuenight rules — couchPings block present"
      contains: "match /couchPings"
    - path: "C:/Users/nahde/claude-projects/couch/tests/rules.test.js"
      provides: "3 new tests #41-#43 covering couchPings ALLOWED/DENIED scenarios"
      contains: "#41"
  key_links:
    - from: "Client write to families/{familyCode}/couchPings/{pingId} (Plan 15.4-02)"
      to: "onCouchPingFire CF onCreate trigger"
      via: "Firestore Cloud Function trigger"
      pattern: "functions\\.firestore[\\s\\S]*couchPings"
    - from: "onCouchPingFire CF body"
      to: "sendToMembers([recipientId], payload, {excludeMemberId, eventType:'couchPing'})"
      via: "existing reusable push fan-out helper"
      pattern: "sendToMembers\\([^,]+,\\s*\\[[^\\]]+\\]"
    - from: "couchPing eventType in sendToMembers"
      to: "NOTIFICATION_DEFAULTS[couchPing] = true gate + isInQuietHours check"
      via: "existing eventType-gated sendToMembers branch (lines ~132-148)"
      pattern: "couchPing: true"
---

<objective>
Plan 01 — Server-side foundation for the couch-ping push channel. Cross-repo: writes
to `queuenight/functions/index.js` + `queuenight/firestore.rules` + the byte-identical
mirrors at `couch/firestore.rules` + `couch/tests/firestore.rules`, plus extends
`couch/tests/rules.test.js` with 3 new test cases for the new ephemeral `couchPings`
subcollection.

**The architectural shape (per CONTEXT.md D-01..D-07):** sendCouchPing currently
toasts "Push sent to {name}" but fires no push (Path C stub at js/app.js:14457-14474).
This plan builds the server side of Path A: client (in 15.4-02) will write a doc
to `families/{familyCode}/couchPings/{pingId}`; this plan adds the
`onCouchPingFire` Cloud Function that triggers on that create, fans out a single
push to the recipient (excluding the sender per self-echo guard), respects quiet
hours via the existing sendToMembers eventType pipeline, deletes the ping doc
after fan-out (ephemeral retention), and emits a delivery-attempt log.

**Why a new dedicated CF (per D-03)** rather than overloading `onIntentCreated`:
couch-pings have no persistent intent doc, no counter-time, no auto-convert,
no flow discriminator — bundling into intents would break invariants the rules
file enforces (status enum, type enum, counterChainDepth bound). A separate
Firestore-trigger CF on a separate ephemeral subcollection keeps the surface
narrow and the existing intent invariants intact.

**Three-way rules lockstep (CRITICAL):** `couch/firestore.rules` (root, deployed source),
`queuenight/firestore.rules` (deploy mirror), and `couch/tests/firestore.rules` are all
byte-identical pre-plan (verified via `node` byte-compare). The `tests/package.json`
pretest script runs `require('fs').copyFileSync('../firestore.rules','firestore.rules')`
automatically before each `npm test`, so editing `couch/firestore.rules` (root) keeps
tests in sync. But `scripts/deploy.sh` does NOT mirror firestore.rules to queuenight —
queuenight is a separate repo and gets edited explicitly. So this plan writes ALL THREE
files directly to keep them in lockstep, and the verify step performs a byte-compare.

Plan 01 is **autonomous** — all writes are local; the deploy gate is in Plan 03.

**Banned-words sweep on push body** "{senderName} wants you on the couch tonight":
- "wants" — neutral verb, no banned terms
- "you on the couch" — direct, BRAND voice (warm, restraint, family-tapping-shoulder)
- "tonight" — temporal anchor, not banned
- No theatrical words ("summons", "is calling")
- No banned-list words ("buffer", "queue", "sync", "lag", "latency", "playback")
PASS.

Purpose: build the server-side push fan-out half of Path A so that when Plan 02
wires the client write site, the path delivers a real push end-to-end.

Output: queuenight CF + rules edits committed; couch rules + tests edits committed
in the couch repo. Test suite passes 46/46 in the local emulator. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/15.4-integration-polish/15.4-CONTEXT.md
@.planning/phases/15.5-wait-up-flex/15.5-05-PLAN.md
@.planning/phases/14-decision-ritual-core/14-09-SUMMARY.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

queuenight/functions/index.js NOTIFICATION_DEFAULTS at lines 74-106 — 17 keys:
- watchpartyScheduled, watchpartyStarting, titleApproval, inviteReceived,
  vetoCapReached, tonightPickChosen
- intentProposed, intentMatched (Phase 8)
- flowAPick, flowAVoteOnPick, flowARejectMajority, flowBNominate,
  flowBCounterTime, flowBConvert, intentExpiring (Phase 14 D-12)
- newSeasonAirDate (Phase 15)
- reactionPosted (Phase 15.5 — currently the LAST key, no trailing comma)

ADD AFTER reactionPosted: `couchPing: true` (Phase 15.4 / D-02) — convert
reactionPosted's line to have a trailing comma, then add the new key without one.

queuenight/functions/index.js sendToMembers at lines 114-171 — reusable as-is. Signature:
  async sendToMembers(familyCode, memberIds, payload, options = {})
  options: { excludeUid, excludeMemberId, eventType }
  - eventType-gated: looks up users/{uid}.notificationPrefs[eventType], skips if false
  - quiet-hours: calls isInQuietHours(prefs.quietHours) inside the eventType branch
  - prunes dead 410/404 subscriptions

queuenight/functions/index.js isInQuietHours at lines 45-72 — referenced indirectly via
sendToMembers; couchPing inherits quiet-hours respect by passing eventType: 'couchPing'.

queuenight/functions/index.js exports.onIntentCreated at lines 438-528 — reference template
for shape (Firestore-trigger CF, v1 generation, async handler signature, members fetch
+ filter pattern, sendToMembers call). The new onCouchPingFire follows this shape but
much shorter — no flow discriminator, single recipient, no counter-time logic.

queuenight/firestore.rules match /watchparties/{wpId} at lines 561-565 — simplest
reference pattern (allow read if member; allow create/update if attributedWrite; allow
delete if owner OR member-during-grace). couchPings inserts AFTER this block.

queuenight/firestore.rules match /intents/{intentId} at lines 581-655 — most-similar
reference pattern (uses attributedWrite, has create-only validation, has affectedKeys()
update branches, has `allow delete: if false`). couchPings inserts BEFORE this block.

queuenight/firestore.rules validAttribution(familyCode) at lines 69-82 — already enforces
`memberId is string && memberId.matches('^m_[A-Za-z0-9_-]+$')` (Phase 15.1 / SEC-15-1-02
anchor). For senderId, attributedWrite already covers it (senderId == memberId == auth.uid
transitively). For recipientId (a NEW field) — apply the same anchor inline.

couch/tests/rules.test.js — UID_OWNER, UID_MEMBER, UID_STRANGER fixtures + member/owner/
stranger/anon Firestore contexts already in scope at the run() function level
(rules.test.js:121-136). assertSucceeds/assertFails imported at top of file from
@firebase/rules-unit-testing. Existing test file has 43 cases; the last describe block
("Phase 15.1 — Security Hardening") ends at line 761 with await testEnv.cleanup() at line
763. Insert the new describe block BEFORE the cleanup() line.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add couchPing to NOTIFICATION_DEFAULTS + add onCouchPingFire CF in queuenight</name>
  <files>C:/Users/nahde/queuenight/functions/index.js</files>
  <read_first>
    - Read C:/Users/nahde/queuenight/functions/index.js with offset=74, limit=35 to confirm NOTIFICATION_DEFAULTS shape.
    - Read C:/Users/nahde/queuenight/functions/index.js with offset=438, limit=95 to study onIntentCreated as the reference template.
    - Read C:/Users/nahde/queuenight/functions/index.js with offset=114, limit=60 to confirm sendToMembers signature.
    - The PLAN INTERFACES block has the relevant snippets pre-extracted; you do not need to grep further.
  </read_first>
  <action>
    Make TWO edits to C:/Users/nahde/queuenight/functions/index.js:

    EDIT 1 — Add couchPing key to NOTIFICATION_DEFAULTS:

    Locate the closing `});` of the NOTIFICATION_DEFAULTS Object.freeze block. The current
    last entry is `reactionPosted: true` with NO trailing comma. Convert that line to have
    a trailing comma, and insert the new key on its own line immediately after, with the
    comment block above it for traceability:

        // Phase 15.4 / D-02 + D-09: couchPing — push fan-out for V5 roster long-press.
        // Default ON: this fires only when the user has actively been long-pressed by
        // another family member (intent-rich engagement signal); user expects it.
        // Mirror in client DEFAULT_NOTIFICATION_PREFS (Plan 15.4-02).
        couchPing: true
      });

    EDIT 2 — Add the new onCouchPingFire CF AFTER onIntentCreated:

    Locate the `exports.onIntentCreated = ...` block at queuenight/functions/index.js:438-528.
    Insert the new CF block IMMEDIATELY AFTER the closing `});` of onIntentCreated and BEFORE
    the next `exports.onIntentUpdate` declaration at line 543. Use this verbatim shape (with
    two leading blank lines + one trailing blank line preserving the inter-CF convention):


    // === Trigger: couch-ping fan-out (Phase 15.4 / F-W-1 path A) ===
    // Fires when a client writes to families/{familyCode}/couchPings/{pingId}. The doc carries
    // {senderId, senderName, recipientId, createdAt, ...attribution}. We push to the recipient
    // (excluding sender per self-echo guard, respecting quiet hours via sendToMembers eventType
    // pipeline), then DELETE the ping doc — couchPings is an ephemeral mailbox, not a log.
    // Sentry-side analytics breadcrumb stays on the client (Plan 15.4-02); this CF emits a
    // console.log delivery-attempt line for server-side observability.
    exports.onCouchPingFire = functions.firestore
      .document('families/{familyCode}/couchPings/{pingId}')
      .onCreate(async (snap, context) => {
        const ping = snap.data();
        if (!ping) return null;
        const { familyCode, pingId } = context.params;
        const senderId = ping.senderId || '';
        const senderName = (ping.senderName && String(ping.senderName).trim()) || 'A family member';
        const recipientId = ping.recipientId || '';
        if (!senderId || !recipientId) {
          console.warn('[15.4/F-W-1] onCouchPingFire malformed ping', { familyCode, pingId, senderId, recipientId });
          // Best-effort cleanup so a malformed doc doesn't linger forever.
          try { await snap.ref.delete(); } catch (e) {}
          return null;
        }
        // Self-echo guard (D-05): never push to sender. sendToMembers also enforces this via
        // excludeMemberId, but we short-circuit here to skip the members fetch entirely.
        if (recipientId === senderId) {
          try { await snap.ref.delete(); } catch (e) {}
          return null;
        }
        console.log('[15.4/F-W-1] onCouchPingFire delivering', { familyCode, pingId, senderId, recipientId });
        try {
          await sendToMembers(familyCode, [recipientId], {
            title: senderName,
            body: senderName + ' wants you on the couch tonight',
            tag: 'couch-ping-' + familyCode + '-' + recipientId,
            url: '/app'
          }, {
            excludeUid: ping.senderUid || null,
            excludeMemberId: senderId,
            eventType: 'couchPing'
          });
        } catch (e) {
          console.warn('[15.4/F-W-1] onCouchPingFire push delivery failed:', e && e.message);
        }
        // Auto-delete the ping doc after fan-out (Discretion default — ephemeral retention).
        // Sentry breadcrumb on the client side (Plan 15.4-02) preserves analytics signal.
        try {
          await snap.ref.delete();
        } catch (e) {
          console.warn('[15.4/F-W-1] onCouchPingFire delete-after-fanout failed:', e && e.message);
        }
        return null;
      });


    Do NOT modify any other CF block. Do NOT change ordering of any other CF.

    Body-template invariants per CONTEXT D-04 + D-07:
    - Title: senderName (matches Phase 15.5 onWatchpartyUpdate Branch 2 pattern)
    - Body: senderName + ' wants you on the couch tonight' (verbatim per CONTEXT specifics)
    - Tag: 'couch-ping-' + familyCode + '-' + recipientId (coalesces rapid pings)
    - URL: '/app' (generic deep-link to app shell; no specific intent/wp doc to anchor to)

    Banned-words sweep on the new strings:
    - "wants you on the couch tonight" — no banned words. Pass.
    - "A family member" — no banned words. Pass.
    - Code-internal: senderName, recipientId, couchPing are field/event-type names, exempt.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/queuenight && node -c functions/index.js</automated>
    Expected: exit 0 (syntax valid).
    <automated>grep -nc "couchPing: true" C:/Users/nahde/queuenight/functions/index.js</automated>
    Expected: 1 (one occurrence — inside NOTIFICATION_DEFAULTS).
    <automated>grep -nc "exports.onCouchPingFire" C:/Users/nahde/queuenight/functions/index.js</automated>
    Expected: 1.
    <automated>grep -nc "wants you on the couch tonight" C:/Users/nahde/queuenight/functions/index.js</automated>
    Expected: 1 (push body template).
    <automated>grep -nc "eventType: 'couchPing'" C:/Users/nahde/queuenight/functions/index.js</automated>
    Expected: 1 (sendToMembers call inside onCouchPingFire).
    <automated>grep -nc "couchPings" C:/Users/nahde/queuenight/functions/index.js</automated>
    Expected: 1 (Firestore document path in the .document() call).
  </verify>
  <acceptance_criteria>
    - couchPing: true appears exactly once in NOTIFICATION_DEFAULTS, after reactionPosted: true,
    - exports.onCouchPingFire is declared as a Firestore onCreate trigger on families/{familyCode}/couchPings/{pingId}
    - The CF body calls sendToMembers(familyCode, [recipientId], payload, options) exactly once with excludeMemberId: senderId and eventType: 'couchPing'
    - Push body template ends with the verbatim string "wants you on the couch tonight"
    - The CF deletes the ping doc after fan-out (snap.ref.delete() called in success path AND in malformed-doc cleanup path)
    - Self-echo guard short-circuit (if recipientId === senderId then return) present
    - Server-side delivery log console.log('[15.4/F-W-1] onCouchPingFire delivering', ...) present
    - node -c functions/index.js exits 0 from C:/Users/nahde/queuenight/
    - Other CFs (onIntentCreated, onWatchpartyUpdate, etc.) unchanged — verified by git diff scope being bounded to the new block + the trailing-comma flip on reactionPosted
    - Closes/partially-closes: F-W-1 (server-side push delivery half — client-side write in 15.4-02)
  </acceptance_criteria>
  <done>
    NOTIFICATION_DEFAULTS contains couchPing key. onCouchPingFire CF added with correct
    Firestore-trigger shape, push body, self-echo guard, eventType wiring, and ephemeral cleanup.
    Syntax valid. No other CFs modified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add /couchPings/{pingId} rules block to all 3 firestore.rules mirrors</name>
  <files>C:/Users/nahde/queuenight/firestore.rules, C:/Users/nahde/claude-projects/couch/firestore.rules, C:/Users/nahde/claude-projects/couch/tests/firestore.rules</files>
  <read_first>
    - Read C:/Users/nahde/queuenight/firestore.rules with offset=555, limit=110 — covers watchparties + intents blocks; couchPings inserts BETWEEN them.
    - Read C:/Users/nahde/queuenight/firestore.rules with offset=69, limit=15 — validAttribution helper.
    - The 3 rules files are byte-identical pre-plan — verified via node byte-compare in the planning step.
  </read_first>
  <action>
    Insert the same `match /couchPings/{pingId}` block into all 3 rules files. The 3 files
    must remain byte-identical post-edit.

    Insertion point (in all 3 files): AFTER the `match /watchparties/{wpId}` block which
    ends with `}` around line 565, and BEFORE the `// =====` separator preceding the
    `// Intents — Phase 8 Watch-Intent Flows...` comment block around line 567-580.

    Block to insert (verbatim, into all 3 files — preserving the 6-space indent of the
    surrounding family-subcollection scope):


      // ========================================================
      // Couch pings — Phase 15.4 / F-W-1 path A
      // ========================================================
      // Ephemeral mailbox: client (V5 roster long-press in js/app.js sendCouchPing) writes a
      // doc here; the onCouchPingFire CF (queuenight/functions/index.js) fans out a single
      // push to the recipient via sendToMembers + then admin-SDK deletes the doc. Doc shape:
      //   { senderId, senderName, recipientId, createdAt, ...writeAttribution() fields }
      // Rules: family member can CREATE their own ping (senderId == self via validAttribution +
      // recipientId anchored to '^m_[A-Za-z0-9_-]+$' to defang regex meta). Family members
      // can READ (so a future "you got pinged" inbox UI is reachable) but cannot UPDATE or
      // DELETE — the CF owns lifecycle. Mirrors the SEC-15-1-02 anchor pattern from
      // validAttribution for the new recipientId field which is NOT covered by the helper.

      match /couchPings/{pingId} {
        allow read: if isMemberOfFamily(familyCode);
        allow create: if attributedWrite(familyCode)
          && request.resource.data.senderId is string
          && request.resource.data.senderId.matches('^m_[A-Za-z0-9_-]+$')
          && request.resource.data.recipientId is string
          && request.resource.data.recipientId.matches('^m_[A-Za-z0-9_-]+$')
          && request.resource.data.senderId == request.resource.data.memberId;
        // No client update path — single-shot doc, deleted by CF after fan-out.
        allow update: if false;
        // No client delete — CF (admin SDK) bypasses rules to clean up after delivery.
        allow delete: if false;
      }


    Note on senderId == memberId clause: validAttribution() already verifies
    memberId == request.auth.uid (or the proxy variant), so requiring senderId == memberId
    transitively pins senderId to the authenticated caller. This blocks the trivial spoof
    where Member A writes a ping with senderId=Member-B (so the recipient sees a fake "B
    wants you").

    3 files to edit — apply the same insertion to each:

    1. C:/Users/nahde/queuenight/firestore.rules
    2. C:/Users/nahde/claude-projects/couch/firestore.rules
    3. C:/Users/nahde/claude-projects/couch/tests/firestore.rules

    Use the Edit tool 3 times with the same old/new content (the file content is identical
    pre-edit, so the same Edit operation works on all 3). Anchor the Edit on a UNIQUE
    multi-line string near the insertion point. The cleanest anchor is the closing `}` of
    `match /watchparties/{wpId}` followed by the blank line + the `// ===...` separator
    leading into Intents — that exact span exists nowhere else in the file.

    Sanity verification post-edit: all 3 files MUST be byte-identical after the edits, OR
    the rules-emulator test will run against the wrong copy. The verify block performs a node
    byte-compare across all 3 paths.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('C:/Users/nahde/queuenight/firestore.rules','utf8'); const b=fs.readFileSync('C:/Users/nahde/claude-projects/couch/firestore.rules','utf8'); const c=fs.readFileSync('C:/Users/nahde/claude-projects/couch/tests/firestore.rules','utf8'); if (a !== b || b !== c) { console.error('MISMATCH between rules files'); process.exit(1); } console.log('all 3 rules files byte-identical OK');"</automated>
    Expected: exit 0, prints "all 3 rules files byte-identical OK".
    <automated>grep -nc "match /couchPings" C:/Users/nahde/queuenight/firestore.rules</automated>
    Expected: 1.
    <automated>grep -nc "match /couchPings" C:/Users/nahde/claude-projects/couch/firestore.rules</automated>
    Expected: 1.
    <automated>grep -nc "match /couchPings" C:/Users/nahde/claude-projects/couch/tests/firestore.rules</automated>
    Expected: 1.
    <automated>grep -nc "Phase 15.4 / F-W-1 path A" C:/Users/nahde/claude-projects/couch/firestore.rules</automated>
    Expected: 1 (traceability comment present).
    <automated>grep -nE "match /watchparties|match /couchPings|match /intents" C:/Users/nahde/claude-projects/couch/firestore.rules</automated>
    Expected: 3 lines, ordered watchparties → couchPings → intents (couchPings inserted between).
  </verify>
  <acceptance_criteria>
    - All 3 rules files contain a match /couchPings/{pingId} block. Verified by grep on each.
    - The 3 files are byte-identical post-edit (node byte-compare passes).
    - The block sits AFTER /watchparties and BEFORE /intents. Verified by ordered grep.
    - allow read, create, update, delete branches present per the spec — read scoped to family member, create attributed + anchored, update/delete denied for clients.
    - Phase 15.4 traceability comment present in all 3.
    - The pre-existing match /watchparties + match /intents blocks are unchanged.
    - Closes/partially-closes: F-W-1 (rules half — write authorization for the client-side doc write in 15.4-02).
  </acceptance_criteria>
  <done>
    All 3 firestore.rules mirrors contain the new /couchPings/{pingId} block with attributed
    create + family-scoped read + denied client update/delete. Byte-identical lockstep
    maintained across queuenight + couch root + couch tests copies.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 3 rules-emulator tests #41-#43 covering /couchPings ALLOWED + DENIED scenarios</name>
  <files>C:/Users/nahde/claude-projects/couch/tests/rules.test.js</files>
  <read_first>
    - Read C:/Users/nahde/claude-projects/couch/tests/rules.test.js with offset=600, limit=170 — covers the existing Phase 15.1 describe block (#33-#40) + the testEnv.cleanup() at line 763 which marks the insertion point.
    - The PLAN INTERFACES block has the relevant fixtures + assert helpers pre-extracted.
  </read_first>
  <action>
    Edit C:/Users/nahde/claude-projects/couch/tests/rules.test.js. Insert a new describe
    block after the existing `Phase 15.1 — Security Hardening (SEC-15-1-* isolation)` block
    closes at line ~761 (`});` of that describe), and BEFORE `await testEnv.cleanup();` at
    line 763.

    Use this exact insertion (the indentation matches the existing 2-space describe-block
    indent inside the run() function):


      // === Phase 15.4 — Integration polish — couch-ping ephemeral collection ===
      // F-W-1 path A: families/{code}/couchPings/{pingId} is an ephemeral mailbox.
      // Tests #41 (happy-path create), #42 (senderId-spoof DENIED), #43 (regex injection DENIED).
      await describe('Phase 15.4 — couchPings ephemeral collection', async () => {
        await it('#41 authed member writes own couchPing → ALLOWED (F-W-1 / D-04)', async () => {
          // senderId == memberId == auth.uid (validAttribution passes).
          // recipientId is a valid m_-anchored memberId.
          await assertSucceeds(
            member.doc('families/fam1/couchPings/p_test_41').set({
              senderId: 'm_UID_MEMBER',
              senderName: 'Member',
              recipientId: 'm_UID_OWNER',
              createdAt: 1234567890,
              actingUid: UID_MEMBER,
              memberId: 'm_UID_MEMBER',
              memberName: 'Member',
            })
          );
        });
        await it('#42 authed member spoofs senderId != memberId → DENIED (F-W-1 / senderId == memberId clause)', async () => {
          // The senderId == memberId rule clause blocks Member A from writing a ping
          // claiming to be from Member B (which would surface as a fake "B wants you"
          // push to the recipient). Without this clause, attribution alone would only
          // verify memberId == auth.uid — it would not prevent senderId forgery.
          await assertFails(
            member.doc('families/fam1/couchPings/p_test_42').set({
              senderId: 'm_UID_OWNER',
              senderName: 'Owner',
              recipientId: 'm_UID_MEMBER',
              createdAt: 1234567890,
              actingUid: UID_MEMBER,
              memberId: 'm_UID_MEMBER',
              memberName: 'Member',
            })
          );
        });
        await it("#43 forged recipientId='.*' regex injection → DENIED (SEC-15-1-02 anchor extended)", async () => {
          // recipientId.matches('^m_[A-Za-z0-9_-]+$') rejects '.' and '*' because
          // they're not in the allowed character class. Mirrors test #35 which
          // covers the same anchor on memberId.
          await assertFails(
            member.doc('families/fam1/couchPings/p_test_43').set({
              senderId: 'm_UID_MEMBER',
              senderName: 'Member',
              recipientId: '.*',
              createdAt: 1234567890,
              actingUid: UID_MEMBER,
              memberId: 'm_UID_MEMBER',
              memberName: 'Member',
            })
          );
        });
      });


    The leading + trailing blank lines preserve the existing one-blank-line-between-describe-
    blocks convention. Do NOT modify the testEnv.cleanup() line that follows. Do NOT renumber
    any existing test (#1-#40 stay verbatim).
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c tests/rules.test.js</automated>
    Expected: exit 0 (syntax valid).
    <automated>grep -nc "Phase 15.4 — couchPings ephemeral collection" C:/Users/nahde/claude-projects/couch/tests/rules.test.js</automated>
    Expected: 1.
    <automated>grep -nE "#41 authed member writes own couchPing|#42 authed member spoofs senderId|#43 forged recipientId" C:/Users/nahde/claude-projects/couch/tests/rules.test.js</automated>
    Expected: 3 matches (one per new test name).
    <automated>cd C:/Users/nahde/claude-projects/couch/tests && npm test 2>&1 | tail -5</automated>
    Expected: output ending with "46 passing, 0 failing".
  </verify>
  <acceptance_criteria>
    - tests/rules.test.js contains a new "Phase 15.4 — couchPings ephemeral collection" describe block.
    - 3 new tests (#41, #42, #43) inside the new block, with the exact names specified above.
    - Test #41 uses assertSucceeds, tests #42 and #43 use assertFails.
    - The block sits BEFORE await testEnv.cleanup() (verified by line ordering).
    - Pre-existing tests #1-#40 unchanged.
    - cd tests && npm test exits 0 with all 46 tests passing (43 pre-existing + 3 new).
    - node -c tests/rules.test.js exits 0.
    - Closes: F-W-1 (rules-test coverage half — locks the contract for the new ephemeral collection).
  </acceptance_criteria>
  <done>
    3 new rules-emulator tests covering couchPings ALLOWED + senderId-spoof DENIED + regex-injection DENIED.
    All 46 tests pass. Pre-existing tests unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore couchPings collection | Untrusted client write; gated by rules with senderId == memberId clause + recipientId regex anchor |
| Firestore couchPings doc → onCouchPingFire CF | Server-validated read; CF runs with admin SDK privileges, bypasses rules for the post-fan-out delete |
| onCouchPingFire CF → FCM push delivery | Push body computed server-side from doc fields; client cannot inject body content directly (only senderId / senderName which come from the writer's own attribution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-01 | Spoofing (sender impersonation) | couchPings create rule | mitigate | Rules require `senderId == request.resource.data.memberId` AND validAttribution() requires `memberId == request.auth.uid`. So senderId is transitively pinned to the authenticated caller — Member A cannot write a ping claiming to be from Member B. Test #42 locks the contract. |
| T-15.4-02 | Spoofing (regex injection via recipientId) | couchPings create rule | mitigate | Rules require `recipientId.matches('^m_[A-Za-z0-9_-]+$')` — same SEC-15-1-02 anchor pattern as memberId. Defangs '.' / '*' / regex meta-characters. Test #43 locks the contract. |
| T-15.4-03 | Information disclosure (recipient identity leak) | onCouchPingFire push body | accept | Push body surfaces senderName ("{senderName} wants you on the couch tonight"). The recipient already knows who is in their family (family-scoped membership), so no new info is disclosed. Same trade-off as Phase 15.5 reactionPosted (T-15.5-03 sender-name leak — accepted). |
| T-15.4-04 | Denial of service (couchPings spam) | couchPings collection growth | accept | Per CONTEXT Deferred Ideas, rate limiting is deferred until signal of abuse. The ephemeral retention (CF deletes after fan-out) bounds the steady-state size to in-flight pings only — typically 0-1 per family at rest. Burst writes are bounded by client UX (long-press 700ms gate at js/app.js sendCouchPing). |
| T-15.4-05 | Tampering (untrusted senderName injected into push body) | onCouchPingFire body assembly | mitigate | `String(ping.senderName).trim()` defensively coerces; missing or non-string senderName falls back to 'A family member'. The senderId == memberId clause means an attacker can only write a ping under their own identity, so senderName is bounded by the write-attribution discipline. |

ASVS L1: input validation on senderId + recipientId at the rules boundary (regex anchor); attribution-required write; per-receiver push body assembly; eventType-gated user opt-out.
</threat_model>

<verification>
**Plan-level checks (after all 3 tasks committed):**

queuenight side:
1. `cd C:/Users/nahde/queuenight && node -c functions/index.js` exits 0
2. `grep -nc "couchPing: true" functions/index.js` returns 1
3. `grep -nc "exports.onCouchPingFire" functions/index.js` returns 1
4. `grep -nc "wants you on the couch tonight" functions/index.js` returns 1
5. `grep -nc "match /couchPings" firestore.rules` returns 1

couch side:
6. `node -c tests/rules.test.js` exits 0
7. `grep -nc "match /couchPings" firestore.rules` returns 1
8. `grep -nc "match /couchPings" tests/firestore.rules` returns 1
9. byte-compare passes across all 3 firestore.rules files

Rules emulator (gated by deploy.sh — also covered as Plan 03 pre-flight):
10. `cd tests && npm test` returns "46 passing, 0 failing"

Pre-deploy (NON-AUTONOMOUS — handled by Plan 03 deploy ritual):
11. `firebase deploy --only functions,firestore:rules --project queuenight-84044` from queuenight repo
12. CF emulator smoke (optional): from queuenight, `firebase emulators:start --only functions,firestore`, write a ping doc, confirm onCouchPingFire fires + push goes to recipient + doc deletes

Cross-repo deploy ordering (D-11) — enforced by Plan 03 deploy ritual.
</verification>

<success_criteria>
**Plan 01 is complete when:**
- [ ] queuenight/functions/index.js NOTIFICATION_DEFAULTS contains couchPing: true
- [ ] queuenight/functions/index.js exports onCouchPingFire CF on families/{familyCode}/couchPings/{pingId} onCreate
- [ ] onCouchPingFire calls sendToMembers exactly once with eventType: 'couchPing' + excludeMemberId: senderId
- [ ] Push body verbatim "{senderName} wants you on the couch tonight"
- [ ] CF deletes ping doc after fan-out (success path AND malformed-doc cleanup path)
- [ ] Self-echo guard short-circuit + delivery log present
- [ ] All 3 firestore.rules files contain the new /couchPings/{pingId} block
- [ ] All 3 firestore.rules files are byte-identical (queuenight + couch root + couch tests)
- [ ] tests/rules.test.js extended with 3 new tests #41-#43
- [ ] cd tests && npm test passes 46/46
- [ ] node -c functions/index.js + node -c tests/rules.test.js both exit 0
- [ ] Banned-words sweep clean on the new push body string

**Deploy & UAT (DEFERRED to Plan 15.4-03):**
- queuenight functions + firestore:rules deploy via firebase CLI from ~/queuenight/ BEFORE couch hosting deploy
- UAT (long-press a roster pill on a real device, confirm push delivery on a 2nd device)
</success_criteria>

<output>
After completion, create `.planning/phases/15.4-integration-polish/15.4-01-SUMMARY.md` describing:
- queuenight/functions/index.js changes (NOTIFICATION_DEFAULTS line + onCouchPingFire CF location, line numbers)
- 3 firestore.rules edits (location of the new /couchPings block in each)
- tests/rules.test.js — 3 new tests added, total now 46
- npm test output (46 passing)
- queuenight commit reminder: queuenight repo edits must be committed locally on a queuenight-equipped session before Plan 03 deploy (matches Phase 14-09 + 15.5-05 deviation pattern if queuenight has no .git on this machine — surface this in the SUMMARY)
- Cross-repo deploy ordering reminder (queuenight FIRST, then couch — Plan 15.4-03 enforces)
- Open questions: none if all 3 tasks complete cleanly
</output>
