---
phase: 15.1-security-hardening
plan: 03
type: execute
wave: 3
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - sw.js
  - changelog.html
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
task_count: 3
requirements_addressed:
  - SEC-15-1-01
  - SEC-15-1-02
  - SEC-15-1-03
  - SEC-15-1-04
  - SEC-15-1-05
requirements:
  - SEC-15-1-01
  - SEC-15-1-02
  - SEC-15-1-03
  - SEC-15-1-04
  - SEC-15-1-05
user_setup:
  - service: firebase
    why: "Cross-repo deploy ritual REVERSED from Phase 15 — Phase 15.1 TIGHTENS existing fields, so writers must update FIRST and rules must update SECOND. Otherwise legitimate prod writes break for the duration of the rule deploy lag. User authenticates via firebase login from each repo (couch + queuenight) and runs the 2-step deploy in the locked order."
    env_vars: []
    dashboard_config:
      - task: "Verify Phase 15.1 / SEC-15-1-* rule changes deploy to live rules — validAttribution() anchor + 4th title-doc sub-rule (progress isolation) + replaced 5th family-doc branch (participant check)"
        location: "Firebase Console → Firestore → Rules — https://console.firebase.google.com/project/queuenight-84044/firestore/rules"

must_haves:
  truths:
    - "sw.js CACHE constant updated from couch-v35.0.1-stale-fix → couch-v35.1-security-hardening (or whatever the current production CACHE value is at the time, bumped to v35.1-security-hardening)"
    - "changelog.html contains a new <article class=\"release\"> block for v35.1 above the existing v35 article"
    - "User runs the 2-step REVERSED cross-repo deploy ritual: (1) bash scripts/deploy.sh 35.1-security-hardening from couch repo (HOSTING FIRST); (2) firebase deploy --only firestore:rules --project queuenight-84044 from ~/queuenight (RULES SECOND)"
    - "After deploy, the changelog.html v35.1 article's [deploy date YYYY-MM-DD] placeholder is replaced with the actual deploy date"
    - ".planning/REQUIREMENTS.md Traceability table rows for SEC-15-1-01 / SEC-15-1-02 / SEC-15-1-03 / SEC-15-1-04 / SEC-15-1-05 are flipped from `Pending` to `Complete` and the plan reference updated from `Phase 15.1 (TBD plan)` to the actual plan number (15.1-01, 15.1-02, 15.1-03)"
    - ".planning/ROADMAP.md row 15.1 has its plan count updated (`0/?` → `3/3`) and audit-trail section gets a Phase 15.1 close-out narrative line"
  artifacts:
    - path: "sw.js"
      provides: "CACHE bumped to couch-v35.1-security-hardening (auto-bumped via scripts/deploy.sh)"
      contains: "couch-v35.1-security-hardening"
    - path: "changelog.html"
      provides: "New v35.1 release article above the v35 entry"
      contains: 'release-version">v35.1'
    - path: ".planning/REQUIREMENTS.md"
      provides: "5 Traceability rows flipped to Complete; plan references updated"
      contains: "SEC-15-1-01 | Phase 15.1-02 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15.1 row plan count updated + audit-trail narrative line"
      contains: "Phase 15.1 closed-out"
  key_links:
    - from: "sw.js CACHE bump"
      to: "Installed PWAs invalidating on next online activation"
      via: "Service Worker re-fetches /app + /css/app.css + /js/app.js when CACHE constant changes"
      pattern: "couch-v35.1"
    - from: "Reversed cross-repo deploy ritual"
      to: "Production at couchtonight.app (hosting) + queuenight-84044 (rules)"
      via: "HOSTING FIRST guarantees prod writers conform before rules tighten — opposite of Phase 15's rules-first ritual"
      pattern: "scripts/deploy\\.sh.*35\\.1-security-hardening"
    - from: "REQUIREMENTS.md Traceability flip to Complete"
      to: "Audit-trail closure for the four 15-SECURITY/15-REVIEW open findings (F-OPEN-01, F-OPEN-02, WR-02, WR-03)"
      via: "Each SEC-15-1-* requirement maps to specific plan tasks that delivered the fix"
      pattern: "Complete"
---

<objective>
Phase 15.1 — Security Hardening — Wave 3: Close out. Bundle the cross-cutting
artifacts that span every requirement in the phase: sw.js CACHE bump, the
changelog.html v35.1 release article, the REVERSED cross-repo deploy ritual
(HOSTING FIRST, then RULES — opposite of Phase 15), the REQUIREMENTS.md
Traceability flip, and the ROADMAP.md audit-trail narrative.

Per RESEARCH §Q6 + Pitfall 5 — Phase 15.1 deploy ordering is REVERSED from
Phase 15. Phase 15 was rules-first (it ADDED new writeable fields; rules
had to permit them BEFORE writers used them). Phase 15.1 TIGHTENS existing
fields — writers must update BEFORE rules constrain them. The 2-step
ritual is non-negotiable:
1. `cd ~/claude-projects/couch && bash scripts/deploy.sh 35.1-security-hardening`
   (hosting deploy with auto-bumped sw.js CACHE)
2. `cd ~/queuenight && firebase deploy --only firestore:rules --project queuenight-84044`
   (rules deploy from queuenight; user must commit the mirror first)

Per Phase 14-06 deviation 1 + STATE.md "Two-repo discipline" rows — the user
runs the queuenight commits + deploys from a queuenight-equipped session.
This plan's checkpoint pauses for the user to confirm `approved-deploy`.

A subtle wrinkle from 15.1-01 Task 2: the actingTupleKey-stamped writes
(setTupleName + cv15CoWatchPromptDecline) will be REJECTED by the
unmodified prod 5th-branch allowlist between step 1 and step 2 of the
deploy. Mitigations: (a) those writers are low-frequency (~0-1/family/day
per RESEARCH §Q4); (b) cv15CoWatchPromptDecline already has a silent-
failure catch path; (c) the deploy gap should be ≤1 hour. If the user
prefers zero-write-failure during the deploy gap, the executor MAY recommend
a brief feature-flag guard during the checkpoint — but the locked plan is
ship-without-flag.

REQ-IDs traceable in this plan (all 5 — closed across the 3 plans):
- SEC-15-1-01: Per-member progress isolation → 15.1-01 Task 1 (writer) + 15.1-02 Task 1 (rule)
- SEC-15-1-02: memberId regex anchor → 15.1-02 Task 1 (validAttribution extension)
- SEC-15-1-03: tupleNames participant check → 15.1-01 Task 2 (writer) + 15.1-02 Task 2 (rule)
- SEC-15-1-04: Rules tests #33-#35 (plus #36-#38 regression) → 15.1-02 Task 3
- SEC-15-1-05: Pencil-glyph hide + setBy attribution → 15.1-01 Task 3

Output: 4 file edits (sw.js, changelog.html, REQUIREMENTS.md, ROADMAP.md) +
1 human-action checkpoint for the 2-step REVERSED deploy ritual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-security-hardening/15.1-CONTEXT.md
@.planning/phases/15.1-security-hardening/15.1-RESEARCH.md
@.planning/phases/15.1-security-hardening/15.1-01-PLAN.md
@.planning/phases/15.1-security-hardening/15.1-02-PLAN.md

# Reference: prior phase patterns this plan reverses (15-08) or extends
@.planning/phases/15-tracking-layer/15-08-PLAN.md
@.planning/RUNBOOK.md
</context>

<interfaces>
<!-- Existing artifact shapes the executor needs. -->

From sw.js (CACHE bump landmark):

```js
// sw.js line 8 (current)
const CACHE = 'couch-v35.0.1-stale-fix';
```

The bash scripts/deploy.sh command does this bump automatically when called
with a short-tag arg. Per CLAUDE.md and RUNBOOK §H: `bash scripts/deploy.sh
35.1-security-hardening` will set CACHE = `'couch-v35.1-security-hardening'`
(prefix `'couch-v'` is fixed in the deploy script). The executor should NOT
manually edit sw.js — let scripts/deploy.sh handle it.

From changelog.html (v35 article shape — the v35.1 article should mirror):

```html
<!-- changelog.html — current v35 article block (lines ~67-80, structure-only sketch) -->
<article class="release">
  <header>
    <span class="release-version">v35</span>
    <span class="release-date">[deploy date YYYY-MM-DD]</span>
  </header>
  <div class="release-body">
    <!-- ... v35 highlights ... -->
  </div>
</article>
```

The v35.1 article goes ABOVE the v35 article (newest first). The
[deploy date YYYY-MM-DD] placeholder gets replaced with the real date
during the deploy step.

From REQUIREMENTS.md Traceability table (current rows around line 332):

```
| SEC-15-1-01 | Phase 15.1 (TBD plan) | Pending |
| SEC-15-1-02 | Phase 15.1 (TBD plan) | Pending |
| SEC-15-1-03 | Phase 15.1 (TBD plan) | Pending |
| SEC-15-1-04 | Phase 15.1 (TBD plan) | Pending |
| SEC-15-1-05 | Phase 15.1 (TBD plan) | Pending |
```

These get updated to specific plan numbers + flipped to Complete.

From ROADMAP.md row 15.1 (line 256):

```
| 15.1. Security Hardening | 0/? | **SCOPED 2026-04-27** — ... |
```

The `0/?` plan count gets updated to `3/3`. The Phase 15.1 close-out
narrative line gets appended to the ROADMAP audit-trail section
(currently around lines 281-283 with the existing narrative lines for
Phase 14/15/15.1 inserted).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add changelog.html v35.1 release article + mark sw.js CACHE target</name>
  <files>changelog.html, sw.js</files>
  <read_first>
    - C:\Users\nahde\claude-projects\couch\changelog.html (full file — read enough to confirm v35 article structure; the file is ~hundreds of lines; use offset/limit for the v35 article block at lines ~67-95)
    - C:\Users\nahde\claude-projects\couch\sw.js (full file — ~105 lines, safe to read in full)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15.1-security-hardening\15.1-CONTEXT.md (Cross-cutting §"No sw.js CACHE bump" rule — but Phase 15.1 SHIPS user-visible UI from 15.1-01 Task 3 pencil-hide + setBy attribution, so a bump IS required)
    - C:\Users\nahde\claude-projects\couch\scripts\deploy.sh (read first 60 lines to confirm the CACHE-bump arg handling)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-08-PLAN.md (Phase 15 close-out — for changelog v35 article style + sw.js CACHE bump method)
  </read_first>
  <action>
TWO sub-edits.

**Sub-edit A — Verify sw.js does NOT need a manual edit:**

`bash scripts/deploy.sh 35.1-security-hardening` (run in Task 2's
checkpoint) will auto-bump sw.js CACHE to `'couch-v35.1-security-hardening'`.
Per CLAUDE.md: "auto-bumped via `bash scripts/deploy.sh <short-tag>`".

This task does NOT manually edit sw.js. Verify the deploy script handles it
by reading scripts/deploy.sh lines 1-60 and confirming the CACHE-bump logic
is present. If for any reason the deploy script does NOT auto-bump (e.g.,
script changed since CLAUDE.md was last updated), surface that as a
checkpoint to the user before proceeding.

If verification confirms the auto-bump works:
- Leave sw.js UNTOUCHED in this commit.
- Document in Task 2's checkpoint that the CACHE bump is contingent on the
  deploy script run.

If verification reveals the auto-bump does NOT work (e.g., scripts/deploy.sh
no longer handles short-tag args), then ALSO manually edit sw.js line 8
from `const CACHE = 'couch-v35.0.1-stale-fix';` to
`const CACHE = 'couch-v35.1-security-hardening';` and document the deviation
in Task 2's checkpoint.

**Sub-edit B — Insert v35.1 release article in changelog.html:**

INSERT a new `<article class="release">` block ABOVE the existing v35
article block. The new article describes the SEC-15-1-* fixes in
user-facing language (the audit trail is in REQUIREMENTS.md / SECURITY.md).

Per CLAUDE.md "warm dark · Cinematic · Lived-in. Restraint is the
principle." — keep the v35.1 release notes brief + grounded in user value.

Verbatim AFTER shape (placed ABOVE the v35 article; the existing v35 article
remains unchanged):

```html
  <article class="release">
    <header>
      <span class="release-version">v35.1</span>
      <span class="release-date">[deploy date YYYY-MM-DD]</span>
    </header>
    <div class="release-body">
      <p><strong>Security &amp; trust.</strong> Tightened the rules behind
      tracking — episode progress, couch nicknames, and "stop nagging me"
      records are now sealed to their owner per row, not just per family.
      Nobody can rewrite anyone else's progress or rename a couch they're
      not on. The rename pencil hides for couches you're not part of, and
      named couches now show "Renamed by &lt;name&gt;" attribution. Quiet
      hardening — nothing to do; a fresh open updates the cached app.</p>
      <p class="release-meta">Closes 4 findings from the post-Phase-15
      holistic audit (F-OPEN-01, F-OPEN-02, WR-02, WR-03). Full audit trail:
      <code>.planning/phases/15-tracking-layer/15-SECURITY.md</code> +
      <code>15-REVIEW.md</code>.</p>
    </div>
  </article>
```

The `[deploy date YYYY-MM-DD]` placeholder remains until Task 2's checkpoint
fires; the executor replaces it with the actual ISO date when the user
confirms `approved-deploy`. Per Phase 15-08 pattern (15-08-PLAN.md must_have
"After deploy, the changelog.html v35 article's [deploy date YYYY-MM-DD]
placeholder is replaced with the actual deploy date").

**Why brief copy:** Per CLAUDE.md and BRAND.md, marketing/changelog copy is
restraint-first. The user's hands-on edit during the changelog is allowed —
if the user wants to substitute their own copy at the checkpoint, that's
fine; this verbatim is the safe default.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "release-version\">v35\\.1" changelog.html | wc -l | awk '{ if (\$1 == 1) print "PASS: v35.1 article inserted"; else { print "FAIL: v35.1 article missing or duplicated (got " \$1 ")"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && awk '/release-version">v35\\.1/{found_v351=NR} /release-version">v35[^.]/{found_v35=NR} END { if (found_v351 > 0 && found_v35 > 0 && found_v351 < found_v35) print "PASS: v35.1 article appears ABOVE v35 article"; else { print "FAIL: ordering wrong (v35.1 line=" found_v351 " v35 line=" found_v35 ")"; exit 1 } }' changelog.html</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "Closes 4 findings" changelog.html | wc -l | awk '{ if (\$1 >= 1) print "PASS: audit-trail reference present"; else { print "FAIL: audit-trail reference missing"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -E "CACHE\\s*=" sw.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `changelog.html` contains a NEW `<article class="release">` block with `<span class="release-version">v35.1</span>` somewhere in its header
    - The new v35.1 article appears BEFORE (lower line number than) the existing v35 article (newer-first ordering)
    - The new v35.1 article body contains the literal phrase "Security & trust." or "Security &amp; trust."
    - The new v35.1 article body contains the literal phrase "Renamed by" (referencing the SEC-15-1-05 attribution surface)
    - The new v35.1 article body references the audit trail (e.g., "F-OPEN-01" or "Closes 4 findings")
    - The new v35.1 article contains literal `[deploy date YYYY-MM-DD]` placeholder (to be replaced post-deploy in Task 2)
    - The existing v35 + v34 + v33 + earlier articles are UNCHANGED (verify by reading changelog.html offset=80 limit=200 — no edits below the new v35.1 block)
    - `sw.js` either is unchanged (auto-bump path) OR has CACHE updated to `'couch-v35.1-security-hardening'` (manual-bump fallback path)
    - If sw.js is unchanged, the executor has read scripts/deploy.sh and verified the auto-bump path works for the `35.1-security-hardening` short-tag arg
    - `scripts/deploy.sh` exists and is executable (verify via `ls -la scripts/deploy.sh`)
  </acceptance_criteria>
  <done>changelog.html has a new v35.1 release article above the v35 article with the [deploy date YYYY-MM-DD] placeholder; sw.js handling is either deferred to scripts/deploy.sh auto-bump (preferred) OR manually bumped (fallback). Either path is documented for Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: REVERSED cross-repo deploy ritual — HOSTING FIRST, then RULES</name>
  <read_first>
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15.1-security-hardening\15.1-CONTEXT.md (Cross-cutting §"Cross-repo deploy ordering" — note this phase is REVERSED from Phase 15)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15.1-security-hardening\15.1-RESEARCH.md §Q6 + Pitfall 5 (deploy reversal rationale)
    - C:\Users\nahde\claude-projects\couch\.planning\RUNBOOK.md §H (deploy walkthrough)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-08-PLAN.md (Phase 15 close-out — REVERSED ordering reference)
  </read_first>
  <what-built>
    All Phase 15.1 artifacts are READY to deploy:
    - 15.1-01: dotted-path writers + actingTupleKey stamping + pencil-glyph hide + setBy attribution (committed to js/app.js)
    - 15.1-02: anchor in validAttribution() + 4th title-doc sub-rule + replaced 5th family-doc branch + 6 new tests (committed to firestore.rules SOURCE + ~/queuenight/firestore.rules MIRROR + tests/rules.test.js — both rule files byte-identical, all 38 tests green via `cd tests && npm test`)
    - 15.1-03 Task 1: changelog.html v35.1 article (committed; placeholder for deploy date)

    Pre-deploy gate verifications the executor SHOULD run before pausing for
    the user:
    1. `cd C:/Users/nahde/claude-projects/couch && cd tests && npm test` —
       confirm 38 tests pass (32 existing + 6 new)
    2. `diff C:/Users/nahde/claude-projects/couch/firestore.rules C:/Users/nahde/queuenight/firestore.rules` —
       confirm exit code 0 (byte-identical mirror)
    3. `git log --oneline -10` in the couch repo — confirm 15.1-01 + 15.1-02
       commits are present
    4. The user must have BOTH `~/claude-projects/couch` AND `~/queuenight`
       repos checked out + authenticated (firebase login in each)
  </what-built>
  <how-to-verify>
    **STEP 1 — Pre-deploy verification (executor runs):**
    ```bash
    cd C:/Users/nahde/claude-projects/couch && cd tests && npm test
    # Expected: 38 passing, 0 failing

    diff C:/Users/nahde/claude-projects/couch/firestore.rules C:/Users/nahde/queuenight/firestore.rules
    # Expected: no output (exit 0)

    cd C:/Users/nahde/claude-projects/couch && git log --oneline -10
    # Expected: 15.1-01 + 15.1-02 + 15.1-03 Task 1 commits visible
    ```

    If any verification fails, do NOT proceed to deploy. Surface the failure
    to the user.

    **STEP 2 — User commits queuenight rules mirror (HUMAN-only step, runs
    from a queuenight-equipped session per STATE.md two-repo discipline):**
    ```bash
    cd ~/queuenight
    git add firestore.rules
    git commit -m "feat(15.1-02): SEC-15-1-* rules tightening — anchor + per-member progress + tupleNames participant check"
    # Optional: git push if user pushes queuenight to a remote
    ```

    **STEP 3 — DEPLOY HOSTING FIRST (this is the REVERSAL from Phase 15):**
    ```bash
    cd C:/Users/nahde/claude-projects/couch
    bash scripts/deploy.sh 35.1-security-hardening
    ```
    Expected output:
    - sw.js CACHE bumped to `couch-v35.1-security-hardening`
    - couch repo files mirrored to `~/queuenight/public/`
    - `firebase deploy --only hosting` runs successfully
    - Live at https://couchtonight.app within ~30s of the deploy completing

    Verify by visiting https://couchtonight.app/app + opening DevTools →
    Application → Service Workers — the active worker should report the
    new CACHE name. The PWA's pencil-hide + setBy attribution should
    render after a hard refresh (or wait for the next online activation
    on installed PWAs — sw.js auto-revalidates).

    **STEP 4 — DEPLOY RULES SECOND (after Step 3 confirmed live):**
    ```bash
    cd ~/queuenight
    firebase deploy --only firestore:rules --project queuenight-84044
    ```
    Expected output:
    - `✔ cloud.firestore: rules file firestore.rules compiled successfully`
    - `✔ Deploy complete!`

    Verify in Firebase Console at
    https://console.firebase.google.com/project/queuenight-84044/firestore/rules
    that the live rules now show:
    - `request.resource.data.memberId.matches('^m_[A-Za-z0-9_-]+$')` in
      validAttribution()
    - The 4th sub-rule for `progress` in the title-doc UPDATE branch
    - The replaced 5th family-doc branch with actingTupleKey echo +
      participant regex

    **STEP 5 — Smoke-test in production (HUMAN-VERIFY):**
    1. From a signed-in account, open a TV title detail modal where
       progress exists for a tuple I'm in → pencil button visible →
       tap → rename → save → flashToast "Saved" → "Renamed by <my name>"
       attribution renders.
    2. Same modal viewing a tuple I'm NOT in → no pencil visible.
    3. (Manual SDK test, advanced — optional) Attempt a direct Firestore
       write that violates the new rules (e.g., write
       `progress.{m_OTHER_USER}` from my account). Should fail with
       permission_denied. Sentry should NOT show a spike of legitimate
       writes failing.
    4. Within ~5 minutes of step 4, monitor Sentry breadcrumb category
       `progress.write.failed` and `tupleNames.write.failed` for
       unexpected spikes. A small spike from in-flight v35.0.1 PWAs that
       haven't yet picked up the new sw.js is expected and benign — the
       service worker auto-revalidates on next online activation. If the
       spike persists past 1 hour, investigate.

    **STEP 6 — Replace [deploy date YYYY-MM-DD] placeholder in
    changelog.html:**

    Once Steps 3 + 4 are confirmed live, the EXECUTOR (post-checkpoint)
    edits changelog.html to replace `[deploy date YYYY-MM-DD]` in the
    v35.1 article with the actual deploy date in YYYY-MM-DD format. Then
    runs `git add changelog.html && git commit -m "docs(15.1-03): replace
    deploy date in v35.1 changelog article"`. Then optionally re-runs
    `bash scripts/deploy.sh 35.1.1-changelog-date` to ship the date update
    (typically deferred to the next minor change).

    **STEP 7 — Mark phase complete:**
    Proceed to Task 3 to update REQUIREMENTS.md + ROADMAP.md.

    ---

    **CRITICAL LANDMINES (acknowledge before deploying):**
    - **Reversed ordering vs Phase 15:** Phase 15 was rules-first because
      it ADDED writeable fields. Phase 15.1 TIGHTENS existing fields.
      Wrong order = legitimate prod writes break for the gap window.
    - **Source/mirror parity:** Step 2's queuenight commit ships the
      mirror file. The source/mirror MUST be byte-identical at deploy
      time. The pre-deploy diff in Step 1 is the gate.
    - **`scripts/deploy.sh` does NOT auto-deploy queuenight rules** — that
      step is manual and runs from `~/queuenight`. Don't expect a single
      command to do everything.
    - **actingTupleKey deploy gap (SEC-15-1-03 side of the ordering tradeoff):**
      between Step 3 (hosting live) and Step 4 (rules live), `setTupleName`
      and `cv15CoWatchPromptDecline` writes will be REJECTED by the
      unmodified prod rules (because the new writer payload includes
      actingTupleKey but the unmodified prod 5th-branch allowlist does not
      yet accept it). The gap should be ≤1 hour. Mitigations: low write
      frequency for those writers (~0-1/family/day per RESEARCH §Q4), and
      cv15CoWatchPromptDecline already silently catches failures with
      Sentry breadcrumbs.
    - **The deploy-ordering tradeoff is REAL — SEC-15-1-01 and SEC-15-1-03
      have OPPOSITE optimal orderings.** Be explicit when explaining this
      to the user during the checkpoint:
      • **HOSTING FIRST (Option H, the default):** safe for SEC-15-1-01
        because old prod writers still doing full-map `progress` writes
        roll out the new dotted-path writer immediately on next PWA
        activation; no in-flight legitimate write is rejected. UNSAFE for
        SEC-15-1-03 during the gap window — actingTupleKey-stamped writes
        get rejected by the prod 5th-branch allowlist (low-frequency, has
        silent catches). **Net impact: low.**
      • **RULES FIRST (Option R, alternative):** safe for SEC-15-1-03
        because the new family-doc 5th-branch already allowlists
        actingTupleKey + does the participant check (the new writer's
        payload passes immediately). UNSAFE for SEC-15-1-01 during the
        gap — old in-flight `progress` writes from PWA-cached old hosting
        still do full-map writes; the new rule's 4th sub-rule denies any
        diff with >1 affected key. **Net impact: HIGHER** because manual
        rate flow + Trakt-sync ingest produce visible toast errors to
        end users (no silent catch like cv15CoWatchPromptDecline has).
      • **LOCKSTEP (Option L, zero gap):** deploy hosting and rules within
        the same minute by running both commands back-to-back. Eliminates
        BOTH gap windows. Slight risk of in-progress write hitting either
        mid-deploy state, but Firestore's atomic rule-engine update
        means each write evaluates against ONE consistent rule version.
        **Recommended if user has both terminals open.**

      **Default to Option H (HOSTING FIRST)** because the SEC-15-1-01
      blast radius (Option R's downside) is materially worse than
      SEC-15-1-03's (Option H's downside). The narrative above is
      surfaced to the user during the checkpoint so they can pick L
      if they prefer zero gap.
    - **CACHE bump propagation:** Installed PWAs roll over to the new
      sw.js on the next online activation, not instantly. PWAs that
      stay offline could see stale code for hours.
  </how-to-verify>
  <resume-signal>
    Reply `approved-deploy` once steps 1-5 above are complete and
    production is verified at https://couchtonight.app/app. The executor
    then proceeds to Step 6 (date replacement in changelog) and Task 3
    (REQUIREMENTS.md + ROADMAP.md updates).

    OR reply with specific issues encountered (e.g., "tests fail",
    "queuenight not authed", "rule deploy errors") and the executor will
    debug rather than proceed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark requirements complete + update ROADMAP audit trail</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - C:\Users\nahde\claude-projects\couch\.planning\REQUIREMENTS.md (lines 178-200 for the SEC-15-1-* spec block + lines 326-345 for the Traceability table)
    - C:\Users\nahde\claude-projects\couch\.planning\ROADMAP.md (lines 252-285 for the Phase 15.1 row + audit-trail section)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15.1-security-hardening\15.1-CONTEXT.md (close-out cross-cutting items — for the narrative line phrasing)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-08-PLAN.md (Phase 15 close-out — for ROADMAP audit-trail format reference)
  </read_first>
  <action>
TWO sub-edits.

**Sub-edit A — Flip Traceability rows in REQUIREMENTS.md (lines ~332-336):**

Update the 5 SEC-15-1-* rows from `Pending` to `Complete` and update the
plan reference column from `Phase 15.1 (TBD plan)` to the specific plan
number that delivered each requirement. Verbatim AFTER shape (replacing
the 5 existing rows):

```
| SEC-15-1-01 | Phase 15.1-01 + 15.1-02 | Complete |
| SEC-15-1-02 | Phase 15.1-02 | Complete |
| SEC-15-1-03 | Phase 15.1-01 + 15.1-02 | Complete |
| SEC-15-1-04 | Phase 15.1-02 | Complete |
| SEC-15-1-05 | Phase 15.1-01 | Complete |
```

Also update the Coverage block at REQUIREMENTS.md lines 338-341. The
existing line reads (per current state):
```
- v1 requirements: 107 total (55 complete across Phases 3+4+7+9+11+14+15; 52 pending across Phases 5/6/8/12/13/14/15.1 + deferred Phase 10)
```
After Phase 15.1 closes, 5 more requirements are Complete and 5 fewer
are Pending. Update verbatim:
```
- v1 requirements: 107 total (60 complete across Phases 3+4+7+9+11+14+15+15.1; 47 pending across Phases 5/6/8/12/13/14 + deferred Phase 10)
```
(Note: Phase 15.1 is REMOVED from the pending list because all 5 of its
requirements just flipped to Complete.)

If the executor finds the existing Coverage line in REQUIREMENTS.md has
slightly different counts (because shipped phases may have advanced since
this plan was written), surface the actual counts and adjust accordingly —
the principle is: 5 Pending → 5 Complete; 15.1 OUT of the Pending phase
list.

**Sub-edit B — Update ROADMAP.md (line 256 for the row + lines ~281-285
for audit-trail):**

**Edit 1 of 2 in ROADMAP.md — Update the Phase 15.1 row:**

Find the row matching `| 15.1. Security Hardening | 0/?` at line 256 and
update the plan count. Verbatim AFTER shape (replacing the prefix part of
the row; keep the existing `**SCOPED 2026-04-27**...` body):

```
| 15.1. Security Hardening | 3/3 | **CLOSED-OUT <DEPLOY DATE>** — Inserted post-Phase-15 audit. Closed 4 findings: F-OPEN-01 HIGH (`t.progress[X].source` forgeable — Trakt-overlap trust break) + WR-02 HIGH (regex injection via attacker-controlled `memberId`) + F-OPEN-02 MEDIUM (`tupleNames` not write-scoped to participants) + WR-03 MEDIUM (no rules-test coverage for `coWatchPromptDeclined`). Five SEC-15-1-* requirements all shipped in 3 plans across 3 waves: 15.1-01 (writer prep — dotted-path conversion + actingTupleKey stamping + pencil-glyph hide + setBy attribution), 15.1-02 (rules tightening — validAttribution() anchor + 4th title-doc sub-rule + replaced 5th family-doc branch + 6 new tests #33-#38), 15.1-03 (close-out — REVERSED cross-repo deploy ritual: HOSTING FIRST, then RULES). All 38 tests green; source/mirror parity verified. **Inserted as decimal phase (mirrors 03.5 precedent), NOT a slot reassignment.** | <DEPLOY DATE> |
```

**IMPORTANT** — replace `<DEPLOY DATE>` literal placeholder text with the
actual deploy date from Task 2 (the same YYYY-MM-DD that replaces the
[deploy date YYYY-MM-DD] placeholder in changelog.html). If the deploy
hasn't yet completed when this task runs, leave `<DEPLOY DATE>` as the
literal placeholder; Step 6 of Task 2 will substitute it.

**Edit 2 of 2 in ROADMAP.md — Append a Phase 15.1 close-out audit-trail
narrative line below the existing Phase 15 narrative (currently around
line 282):**

INSERT a new line BELOW the existing Phase 15.1 inserted-narrative line
(currently at line 283: `*Phase 15.1 inserted: 2026-04-27 (Security
Hardening — closes 2 HIGH + 2 MEDIUM open findings...)*`). Verbatim
INSERT (with one leading blank line for readability):

```

*Phase 15.1 closed-out: <DEPLOY DATE> (Security Hardening — 3 plans across 3 waves shipped in 1 deploy day. SEC-15-1-01..05 all Complete; closes F-OPEN-01 HIGH + WR-02 HIGH + F-OPEN-02 MEDIUM + WR-03 MEDIUM from the Phase 15 holistic audit. Cross-repo deploy ordering REVERSED from Phase 15: hosting first → rules second per RESEARCH §Q6. 38 rules-unit tests pass; source/mirror parity enforced.)*
```

Same `<DEPLOY DATE>` placeholder rule — substitute the actual date when
deploy completes.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "SEC-15-1-01.*Phase 15\\.1.*Complete" .planning/REQUIREMENTS.md | wc -l | awk '{ if (\$1 == 1) print "PASS: SEC-15-1-01 Complete"; else { print "FAIL: SEC-15-1-01 row not flipped"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "SEC-15-1-0[2-5].*Complete" .planning/REQUIREMENTS.md | wc -l | awk '{ if (\$1 == 4) print "PASS: SEC-15-1-02..05 all flipped to Complete"; else { print "FAIL: only " \$1 " of SEC-15-1-02..05 flipped"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "SEC-15-1-.*Pending" .planning/REQUIREMENTS.md | wc -l | awk '{ if (\$1 == 0) print "PASS: no SEC-15-1-* rows still Pending"; else { print "FAIL: " \$1 " SEC-15-1-* rows still Pending"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "15\\.1\\. Security Hardening \\| 3/3" .planning/ROADMAP.md | wc -l | awk '{ if (\$1 == 1) print "PASS: ROADMAP row 15.1 plan count = 3/3"; else { print "FAIL: ROADMAP row 15.1 not updated to 3/3"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "Phase 15\\.1 closed-out" .planning/ROADMAP.md | wc -l | awk '{ if (\$1 == 1) print "PASS: ROADMAP audit-trail Phase 15.1 closed-out narrative present"; else { print "FAIL: closed-out narrative missing or duplicated (got " \$1 ")"; exit 1 } }'</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -nE "REVERSED from Phase 15|hosting first → rules second" .planning/ROADMAP.md | wc -l | awk '{ if (\$1 >= 1) print "PASS: deploy reversal documented in audit trail"; else { print "FAIL: deploy reversal not documented"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` row `SEC-15-1-01 | Phase 15.1-01 + 15.1-02 | Complete` is present
    - `.planning/REQUIREMENTS.md` row `SEC-15-1-02 | Phase 15.1-02 | Complete` is present
    - `.planning/REQUIREMENTS.md` row `SEC-15-1-03 | Phase 15.1-01 + 15.1-02 | Complete` is present
    - `.planning/REQUIREMENTS.md` row `SEC-15-1-04 | Phase 15.1-02 | Complete` is present
    - `.planning/REQUIREMENTS.md` row `SEC-15-1-05 | Phase 15.1-01 | Complete` is present
    - `.planning/REQUIREMENTS.md` does NOT contain any literal `Pending` on a SEC-15-1-* row (grep test confirms)
    - `.planning/REQUIREMENTS.md` Coverage block reflects 5 more complete requirements + 5 fewer pending requirements + Phase 15.1 OUT of the pending phase list
    - `.planning/ROADMAP.md` row 15.1 plan count is `3/3` (was `0/?`)
    - `.planning/ROADMAP.md` row 15.1 narrative starts with `**CLOSED-OUT <YYYY-MM-DD or 2026-04-27 placeholder>**`
    - `.planning/ROADMAP.md` audit-trail section contains a new line `*Phase 15.1 closed-out: ...*` BELOW the existing `*Phase 15.1 inserted: 2026-04-27 ...*` line
    - The new audit-trail line references "REVERSED from Phase 15" or "hosting first → rules second"
    - The placeholders `<DEPLOY DATE>` are present until Task 2's deploy completes; if the deploy already completed, they're replaced with the actual ISO date
    - No other ROADMAP.md rows or REQUIREMENTS.md rows are modified (verify by reading offset/limit around the edits — only the 5 SEC-15-1-* Traceability rows + 1 Coverage line + 1 ROADMAP row + 1 ROADMAP audit-trail line should change)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has all 5 SEC-15-1-* rows flipped to Complete with specific plan numbers, the Coverage block updated; ROADMAP.md row 15.1 shows 3/3 plans + CLOSED-OUT narrative; ROADMAP.md audit-trail has a Phase 15.1 closed-out line below the inserted-line. Phase 15.1 is auditably closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Couch repo deploy → Production hosting | scripts/deploy.sh propagates code; CACHE bump triggers PWA invalidation |
| Queuenight repo deploy → Production rules | firebase deploy --only firestore:rules propagates rules; takes effect immediately on the running Firestore instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-07 | Repudiation | Cross-repo deploy ordering — wrong order breaks legitimate prod writes | mitigate | Deploy ritual REVERSAL is documented in Task 2 read_first + how-to-verify; user gates via `approved-deploy` reply after running both steps |
| T-15.1-08 | Repudiation | Source/mirror rules drift between Wave 2 commits and deploy | mitigate | Pre-deploy diff check in Task 2 STEP 1 fails the gate if `diff` returns non-zero; user must commit + push queuenight mirror before running deploy |
| T-15.1-09 | Repudiation | actingTupleKey-stamped writes rejected during the deploy gap | accept | Brief gap (≤1 hour); silent-failure catch path on cv15CoWatchPromptDecline; alternative lockstep-deploy ordering surfaced to user during checkpoint |
| T-15.1-10 | Tampering | Sentry breadcrumb spike during deploy ritual could mask a real attack | accept | Deploy window monitored manually in Step 5 of Task 2; spikes that persist >1h trigger investigation |
| T-15.1-11 | Information Disclosure | Changelog v35.1 article surfaces audit findings to public users | accept | Public repo per STATE.md; audit findings already in `.planning/phases/15-tracking-layer/15-SECURITY.md` (also public). User-facing copy is restraint-first — describes what changed, not exploit details. |
</threat_model>

<verification>
**Phase 15.1 / Plan 03 acceptance:**
- changelog.html v35.1 article inserted above v35; deploy date placeholder
  replaced with actual date post-deploy
- sw.js CACHE bumped to `couch-v35.1-security-hardening` (auto-bumped by
  scripts/deploy.sh)
- Cross-repo deploy ritual (HOSTING FIRST, then RULES) executed by user;
  `approved-deploy` reply received
- Production smoke test passed (pencil-hide visible to participants only;
  setBy attribution renders for named tuples; rule-side denials confirmed
  via direct SDK test or Sentry monitoring)
- REQUIREMENTS.md Traceability table: 5 SEC-15-1-* rows flipped to Complete
  with specific plan numbers
- REQUIREMENTS.md Coverage block adjusted (Phase 15.1 OUT of pending list)
- ROADMAP.md row 15.1: 3/3 plans + CLOSED-OUT narrative
- ROADMAP.md audit-trail: Phase 15.1 closed-out line appended
- All 5 SEC-15-1-* requirements traceable from REQUIREMENTS.md to specific
  plan tasks
</verification>

<success_criteria>
- All 5 SEC-15-1-* requirements covered in this plan's `requirements_addressed` (traceability close-out): the deploy + audit-trail makes ALL 5 traceable to delivered work
- Cross-repo deploy ordering REVERSAL documented in BOTH the changelog
  release article AND the ROADMAP audit-trail narrative
- Source/mirror parity is verified pre-deploy via the pre-checkpoint
  verification step
- Phase 15.1 is auditably closed (REQUIREMENTS.md + ROADMAP.md both updated)
- Production smoke test confirms the four open findings (F-OPEN-01,
  F-OPEN-02, WR-02, WR-03) are CLOSED in live production
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-security-hardening/15.1-03-SUMMARY.md`
documenting:
- Pre-deploy gate verifications run + their results (test count, mirror diff,
  git log)
- Whether sw.js auto-bump worked (auto-bump path) or needed manual edit
  (fallback path)
- Actual deploy timestamps for each step (hosting deploy time + rules deploy
  time + the gap duration)
- Any Sentry breadcrumb spikes observed during the deploy gap (and whether
  they resolved within the expected window)
- The actual deploy date replacing `<DEPLOY DATE>` placeholders in
  changelog.html + ROADMAP.md
- Any deviations from the locked plan (e.g., user opted for lockstep deploy
  instead of reversed ordering, or pencil-hide UI surfaced an unexpected
  CSS issue requiring follow-up)
- Confirmation that Phase 15.1 is auditably closed via REQUIREMENTS.md
  Traceability + ROADMAP audit-trail
- Any follow-up items spawned (e.g., add `cv15-tuple-setby` CSS polish,
  Phase 5 hardening pass for non-Phase-15 rule branches per CONTEXT
  §Deferred Ideas)
</output>
