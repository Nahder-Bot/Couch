---
phase: 15.1-security-hardening
verified: 2026-04-27T13:35:00Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
goal_findings_closed:
  - id: F-OPEN-01
    severity: HIGH
    title: "t.progress[X].source forgeable — Trakt-overlap trust break"
    closed_by: ["SEC-15-1-01"]
  - id: WR-02
    severity: HIGH
    title: "regex injection via attacker-controlled memberId"
    closed_by: ["SEC-15-1-02"]
  - id: F-OPEN-02
    severity: MEDIUM
    title: "tupleNames not write-scoped to participants"
    closed_by: ["SEC-15-1-03", "SEC-15-1-05"]
  - id: WR-03
    severity: MEDIUM
    title: "no rules-test coverage for coWatchPromptDeclined"
    closed_by: ["SEC-15-1-04"]
requirements_satisfied:
  - id: SEC-15-1-01
    plans: ["15.1-01", "15.1-02"]
    evidence: "Dotted-path writers at js/app.js:754, 8427, 8450 + 4th title-doc sub-rule at firestore.rules:494-516"
  - id: SEC-15-1-02
    plans: ["15.1-02"]
    evidence: "validAttribution anchor regex at firestore.rules:73 + 78 ('^m_[A-Za-z0-9_-]+$' applied to memberId AND managedMemberId)"
  - id: SEC-15-1-03
    plans: ["15.1-01", "15.1-02"]
    evidence: "actingTupleKey stamps at js/app.js:1070 + 8564 + family-doc 5th-branch participant check at firestore.rules:197-261"
  - id: SEC-15-1-04
    plans: ["15.1-02"]
    evidence: "Tests #33-#38 at tests/rules.test.js:609-712 + #23/#1/#2 backfill; full suite 38 passing 0 failing"
  - id: SEC-15-1-05
    plans: ["15.1-01"]
    evidence: "Pencil-glyph isParticipant gate at js/app.js:8871/8893 + cv15ShowRenameInput head guard at js/app.js:8963 + setBy attribution at js/app.js:8887"
---

# Phase 15.1: Security Hardening Verification Report

**Phase Goal:** Close 4 findings from the Phase 15 holistic audit (F-OPEN-01 HIGH, WR-02 HIGH, F-OPEN-02 MEDIUM, WR-03 MEDIUM) by shipping 5 SEC-15-1-* requirements via 3 plans across 3 waves with REVERSED cross-repo deploy ritual.
**Verified:** 2026-04-27T13:35:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement — Goal-Backward Check

For each of the 4 audit findings, the codebase state now BLOCKS the original exploit path:

| Finding | Severity | Original Exploit | Codebase Block (Verified) | Status |
|---------|----------|------------------|---------------------------|--------|
| F-OPEN-01 | HIGH | Member A writes `t.progress.{m_VICTIM} = { source: 'trakt', ... }` via full-map merge, forging victim's Trakt history → poisons Trakt-overlap detector trust at js/app.js:951-994 | (a) Title-doc UPDATE rule 4th sub-rule (firestore.rules:494-516) `affectedKeys().hasOnly([memberId|managedMemberId])` rejects multi-key progress writes; (b) writeMemberProgress (js/app.js:8427), clearMemberProgress (js/app.js:8450), Trakt-sync ingest (js/app.js:754) all write via dotted-path single-inner-key shape — `progress: prevProgress` full-map writers verified GONE (`grep "progress: prevProgress" js/app.js` returns 0 matches) | CLOSED |
| WR-02 | HIGH | Attacker writes `memberId: '.*'` so the runtime regex `(^|.*,)' + memberId + '(,.*|$)'` becomes `(^|.*,).*(,.*\|$)` → matches every tupleKey, bypassing the actingTupleKey echo check | validAttribution() anchor at firestore.rules:72-73 + 77-78 enforces `request.resource.data.memberId.matches('^m_[A-Za-z0-9_-]+$')` (and same anchor on managedMemberId). Test #35 in tests/rules.test.js:640 directly attempts `memberId: '.*'` and confirms DENIED. Character class `[A-Za-z0-9_-]` excludes `.`, `*`, `\`, `(`, `)`, `^`, `$`, `\|`, `[`, `]` — every RE2 metacharacter useful for injection | CLOSED |
| F-OPEN-02 | MEDIUM | Member A writes `tupleNames.{m_OTHER,m_ANOTHER} = { name: 'rude' }` for a tuple they aren't in (silent vandalism); same pattern for `coWatchPromptDeclined.{tk}` | (a) Family-doc 5th-branch (firestore.rules:197-261) actingTupleKey echo + comma-boundary regex `'(^|.*,)' + memberId + '(,.*\|$)'` rejects writes where actor's memberId isn't at a comma boundary in actingTupleKey; (b) `actingTupleKey` self-sanitization regex `'^[A-Za-z0-9_,-]+$'` defangs RE2 meta in actingTupleKey itself; (c) tupleNames + coWatchPromptDeclined diff inner keys constrained `hasOnly([request.resource.data.actingTupleKey])`; (d) writer stamps actingTupleKey at js/app.js:1070 (cv15CoWatchPromptDecline) + 8564 (setTupleName); (e) UI defense-in-depth: pencil-glyph isParticipant gate at js/app.js:8871/8893 + cv15ShowRenameInput head-guard at js/app.js:8963; (f) setBy attribution surface at js/app.js:8887 turns silent vandalism into attributable action. Test #34 confirms server-side DENIED for non-participant coWatchPromptDeclined writes | CLOSED |
| WR-03 | MEDIUM | No rules-test coverage for coWatchPromptDeclined → silent regressions on the cross-session decline path 15-08 enabled | tests/rules.test.js:609-639 ships #33 (ALLOWED — own tuple) + #34 (DENIED — non-participant tuple). Test suite full output: 38 passing, 0 failing. Tests #36-#38 add regression coverage for SEC-15-1-01 progress isolation. Test #35 covers SEC-15-1-02 anchor | CLOSED |

All 4 findings have demonstrable codebase blocks for their original exploit paths.

---

## Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | F-OPEN-01 closed: `t.progress` writes constrained to single-inner-key per actor | VERIFIED | Rule at firestore.rules:494-516 + dotted-path writers verified absent of `progress: prevProgress` (grep = 0); Test #36 ALLOWED + #37 DENIED |
| 2 | WR-02 closed: memberId regex injection defanged via anchor | VERIFIED | firestore.rules:72-73 + 77-78 anchor `^m_[A-Za-z0-9_-]+$`; Test #35 confirms `memberId: '.*'` DENIED |
| 3 | F-OPEN-02 closed server-side: tupleNames + coWatchPromptDeclined participant check | VERIFIED | firestore.rules:197-261 5th-branch with 5-part check (attribution + allowlist + actingTupleKey echo + tupleNames hasOnly + coWatchPromptDeclined hasOnly); Tests #33 ALLOWED + #34 DENIED |
| 4 | F-OPEN-02 closed client-side: pencil-glyph hidden + setBy attribution surfaced | VERIFIED | js/app.js:8871/8893 (isParticipant gate) + 8963 (cv15ShowRenameInput guard) + 8887 (setBy attribution) |
| 5 | WR-03 closed: 6 new tests #33-#38 + full suite green | VERIFIED | tests/rules.test.js:609-712 — all 6 tests; full suite 38 passing 0 failing |
| 6 | Source/mirror parity: couch/firestore.rules byte-identical to ~/queuenight/firestore.rules | VERIFIED | `diff` exit 0 |
| 7 | Production live with new CACHE | VERIFIED | curl https://couchtonight.app/sw.js → `const CACHE = 'couch-v35.1-security-hardening';`; curl HTTP/1.1 200 OK |
| 8 | REQUIREMENTS.md Traceability: all 5 SEC-15-1-* rows show Complete with specific plan refs | VERIFIED | Lines 332-336: SEC-15-1-01 (15.1-01 + 15.1-02), 02 (15.1-02), 03 (15.1-01 + 15.1-02), 04 (15.1-02), 05 (15.1-01) — all Complete; no `Phase 15.1 (TBD plan)` or `Pending` matches in Traceability |
| 9 | ROADMAP.md row 15.1 = 3/3 with CLOSED-OUT narrative | VERIFIED | Line 256 `15.1. Security Hardening \| 3/3 \| **CLOSED-OUT 2026-04-27** ...`; line 285 audit-trail closed-out narrative present |
| 10 | Locked decisions honored: anchor regex shape | VERIFIED | Exactly `^m_[A-Za-z0-9_-]+$` at firestore.rules:73 + 78 (per CONTEXT.md §Specific Ideas) |
| 11 | Locked decisions honored: progress isolation mirrors mutedShows pattern | VERIFIED | firestore.rules:494-516 4th sub-rule — same diff().affectedKeys().hasOnly([...]) shape as mutedShows at firestore.rules:474-493 |
| 12 | Locked decisions honored: dotted-path single-inner-key writes | VERIFIED | All 3 progress writers use `[`progress.${memberId\|meId}`]:` shape (3 grep matches at lines 754, 8427, 8450); 0 full-map progress writes remain |
| 13 | Locked decisions honored: REVERSED cross-repo deploy (HOSTING FIRST → RULES SECOND) | VERIFIED | 15.1-03-SUMMARY.md deploy timeline: hosting at 13:20 UTC, rules at 13:21 UTC (gap <1 min — effectively lockstep); ROADMAP audit-trail line 285 documents "hosting first → rules second" |
| 14 | Deferred items NOT touched: memberId anchor on couchSeating/couchInTonight branches | VERIFIED | firestore.rules:178-196 couchSeating + couchInTonight branches retain original `hasOnly(['couchSeating', 'actingUid', 'managedMemberId', 'memberId', 'memberName'])` allowlist — no new anchor logic added (the validAttribution() helper they call DOES enforce the anchor as a side-effect, which is the intended scope per CONTEXT §WR-02 Locked block) |
| 15 | Deferred items NOT touched: get()-based members/{memberId}.uid == uid() proof | VERIFIED | grep `members/\$\(.*memberId\).*\.uid == uid\(\)` returns 0 matches in firestore.rules — Option B from CONTEXT §Deferred Ideas correctly NOT added |
| 16 | Deferred items NOT touched: legacyGraceWrite() unmodified | VERIFIED | firestore.rules:86-90 legacyGraceWrite() body unchanged — same 5-line shape as pre-phase |
| 17 | actingTupleKey stamping on writers | VERIFIED | js/app.js:1070 (cv15CoWatchPromptDecline → c.tupleKey), 8564 (setTupleName → tupleKeyStr); plus existing writeTupleProgress (line 8493) and clearTupleProgress (line 8514) |
| 18 | sw.js CACHE bumped via deploy.sh auto-bump | VERIFIED | sw.js:8 `const CACHE = 'couch-v35.1-security-hardening';` |

**Score: 18/18 truths verified**

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `firestore.rules` (source) | SEC-15-1-01 + SEC-15-1-02 + SEC-15-1-03 changes | VERIFIED | Lines 55-82 (validAttribution anchor), 197-261 (5th-branch participant check), 494-516 (4th title-doc sub-rule); 659 lines total |
| `~/queuenight/firestore.rules` (mirror) | Byte-identical to source | VERIFIED | `diff` exit 0 |
| `tests/rules.test.js` | 6 new tests #33-#38 + #23/#1/#2 backfill | VERIFIED | Tests at 609-712; describe block at line 608 with comment markers `Phase 15.1 — Security Hardening (SEC-15-1-* isolation)`; full suite 38 passing 0 failing |
| `js/app.js` | 7 edit regions (writers + UI defense) | VERIFIED | Lines 754 (Trakt-sync), 1070 (cv15CoWatchPromptDecline), 8427 (writeMemberProgress), 8450 (clearMemberProgress), 8564 (setTupleName), 8871/8893 (renderCv15TupleProgressSection), 8963 (cv15ShowRenameInput) |
| `sw.js` | CACHE = couch-v35.1-security-hardening | VERIFIED | Line 8 |
| `changelog.html` | v35.1 article above v35 with stamped deploy date | VERIFIED | Lines 67-78 (v35.1) above lines 80-92 (v35); deploy date "2026-04-27 · Security & trust" stamped (no `[deploy date YYYY-MM-DD]` placeholder remaining) |
| `.planning/REQUIREMENTS.md` | 5 SEC-15-1-* rows Complete with specific plan refs | VERIFIED | Lines 332-336 |
| `.planning/ROADMAP.md` | Row 15.1 = 3/3 CLOSED-OUT + audit-trail line | VERIFIED | Lines 256 (row) + 285 (audit-trail closed-out narrative) |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| writeMemberProgress dotted-path write | 4th title-doc sub-rule (per-member progress isolation) | single-inner-key affectedKeys() → hasOnly([memberId]) | WIRED | Test #36 ALLOWED confirms; Test #37 DENIED confirms inverse |
| setTupleName actingTupleKey stamp | family-doc 5th-branch participant regex | rule reads request.resource.data.actingTupleKey + comma-boundary regex | WIRED | js/app.js:8564 stamps; firestore.rules:229-238 reads + validates |
| cv15CoWatchPromptDecline actingTupleKey stamp | family-doc 5th-branch coWatchPromptDeclined hasOnly | rule constrains diff inner key to actingTupleKey | WIRED | js/app.js:1070 stamps; firestore.rules:251-260 enforces |
| validAttribution() anchor regex | title-doc UPDATE actingTupleKey runtime regex | anchored memberId prevents RE2 meta-character injection | WIRED | firestore.rules:72-73 + 77-78 anchor; firestore.rules:464-471 builds runtime regex from anchored memberId; Test #35 confirms `.*` injection rejected |
| pencil-glyph participant gate | F-OPEN-02 client-side defense | pencil never renders for non-participants in DOM | WIRED | js/app.js:8893 conditional renameBtn; setByMarkup at 8887 surfaces attribution |
| Reversed cross-repo deploy ritual | Production at couchtonight.app + queuenight-84044 | HOSTING FIRST → RULES SECOND | WIRED | Hosting deploy at ~13:20 UTC, rules deploy at ~13:21 UTC; gap <1 min (lockstep effective) |
| Source/mirror parity contract | Production rules accuracy | `diff couch/firestore.rules ~/queuenight/firestore.rules` exit 0 | WIRED | Verified at gate |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Production hosting reachable | `curl -sI https://couchtonight.app/` | HTTP/1.1 200 OK | PASS |
| Production sw.js CACHE bumped | `curl -s https://couchtonight.app/sw.js \| grep "CACHE = "` | `const CACHE = 'couch-v35.1-security-hardening';` | PASS |
| Source/mirror byte-identity | `diff firestore.rules ~/queuenight/firestore.rules` | exit 0 (no output) | PASS |
| Rules unit test suite | `cd tests && npm test` | 38 passing, 0 failing | PASS |
| Test #33 (own coWatchPromptDeclined ALLOWED) | runner output | passed | PASS |
| Test #34 (non-participant coWatchPromptDeclined DENIED) | runner output | passed | PASS |
| Test #35 (forged memberId='.*' DENIED) | runner output | passed | PASS |
| Test #36 (own progress dotted-path ALLOWED) | runner output | passed | PASS |
| Test #37 (someone-else progress DENIED) | runner output | passed | PASS |
| Test #38 (non-progress field ALLOWED — no regression) | runner output | passed | PASS |
| All 32 prior tests still passing | runner output | passed (#1-#32) | PASS |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SEC-15-1-01 | 15.1-01 (writer) + 15.1-02 (rule) | Per-member progress isolation | SATISFIED | Dotted-path writers (js/app.js:754, 8427, 8450) + 4th title-doc sub-rule (firestore.rules:494-516); Tests #36 + #37 |
| SEC-15-1-02 | 15.1-02 | memberId regex anchor | SATISFIED | validAttribution anchor (firestore.rules:72-73 + 77-78); Test #35 |
| SEC-15-1-03 | 15.1-01 (writer) + 15.1-02 (rule) | tupleNames participant check (server) | SATISFIED | actingTupleKey stamps (js/app.js:1070 + 8564) + family-doc 5th-branch (firestore.rules:197-261); Tests #33 + #34 |
| SEC-15-1-04 | 15.1-02 | Rules tests #33-#38 + regression | SATISFIED | tests/rules.test.js:609-712 + #1/#2/#23 backfill; full suite 38 passing |
| SEC-15-1-05 | 15.1-01 | Pencil-glyph hide + setBy attribution | SATISFIED | js/app.js:8871/8893 (isParticipant gate) + 8963 (cv15ShowRenameInput guard) + 8887 (setBy attribution markup) |

**No orphaned requirements.** REQUIREMENTS.md Traceability table maps all 5 SEC-15-1-* IDs to specific plans (no `Phase 15.1 (TBD plan)` rows remain).

---

## Anti-Patterns Found

None blocking. Files modified during phase 15.1 (firestore.rules, ~/queuenight/firestore.rules, js/app.js, tests/rules.test.js, sw.js, changelog.html, REQUIREMENTS.md, ROADMAP.md) were scanned for stub patterns:

| File | Pattern Searched | Finding | Severity |
|------|-----------------|---------|----------|
| firestore.rules | TODO/FIXME/PLACEHOLDER | 0 matches in 15.1 edit regions | n/a |
| js/app.js (15.1 regions) | `=> {}` or `console.log only` handlers | 0 stub handlers in edit regions | n/a |
| js/app.js (15.1 regions) | full-map `progress: prevProgress` writes | 0 matches (grep) — old pattern fully replaced | n/a |
| changelog.html | `[deploy date YYYY-MM-DD]` placeholder | 0 matches in v35.1 article (replaced with `2026-04-27`) | n/a |
| ROADMAP.md | `<DEPLOY DATE>` placeholder | 0 matches (replaced) | n/a |
| REQUIREMENTS.md | `Phase 15.1 (TBD plan)` or `Pending` for SEC-15-1-* | 0 matches | n/a |

Note: pre-existing TODO/FIXME comments elsewhere in app.js (unrelated to 15.1 edit regions) are NOT this phase's responsibility.

---

## Locked Decisions Honored (CONTEXT.md cross-check)

| Decision | Locked Pattern | Verification |
|----------|---------------|--------------|
| F-OPEN-01 rules side | 4th sub-rule mirroring mutedShows pattern | firestore.rules:494-516 — identical structure to mutedShows at firestore.rules:474-493 |
| F-OPEN-01 writer side | dotted-path single-inner-key writes for ALL progress writers | 3 grep matches at js/app.js:754, 8427, 8450; 0 full-map writes remain |
| WR-02 anchor | exactly `^m_[A-Za-z0-9_-]+$` (anchors mandatory; `-` last in char class) | firestore.rules:73 + 78 verbatim |
| WR-02 cross-validate scope | applied to title-doc + family-doc 5th-branch (Phase-15-touched surfaces) | validAttribution() is shared helper; couchSeating/couchInTonight branches unchanged but inherit anchor via attributedWrite() |
| F-OPEN-02 server-side | family-doc 5th-branch participant check + actingTupleKey stamps required | firestore.rules:197-261 + js/app.js:1070 + 8564 |
| F-OPEN-02 client-side | pencil-glyph gate + setBy attribution | js/app.js:8871/8893 + 8887 |
| WR-03 tests | tests #33-#35 mirroring #23-#32 style + regression tests | tests/rules.test.js:609-712 — 6 tests in canonical describe-block style |
| Cross-cutting CACHE bump | `couch-v35.1-security-hardening` via deploy.sh | sw.js:8 verified |
| Cross-cutting deploy order REVERSAL | HOSTING FIRST → RULES SECOND | Documented at ROADMAP audit-trail line 285; deploy timeline in 15.1-03-SUMMARY.md confirms hosting at 13:20, rules at 13:21 |
| Cross-cutting source/mirror parity | byte-identical via diff exit 0 | Verified |

## Deferred Items NOT Touched (CONTEXT §Deferred Ideas cross-check)

| Deferred Item | Should NOT exist | Verification |
|---------------|-----------------|--------------|
| memberId anchor on `couchSeating`/`couchInTonight`/`tupleNames` allowlist beyond participant check, `players` | No new anchor logic in those branches | firestore.rules:178-196 couchSeating + couchInTonight branches unchanged; no new explicit anchor calls (anchor inherited via validAttribution helper, which is the locked scope) |
| `get()`-based `members/$(memberId).uid == uid()` proof | No such get() call in validAttribution | Grep for `members/\$\(.*memberId\).*\.uid == uid\(\)` returns 0 matches |
| Trakt overlap detector defense-in-depth (consistency check) | js/app.js:951-994 unchanged at the detector level | The rule is now the security boundary; no client-side detector changes per locked decision |
| CI-level diff check between source and mirror | No new CI tooling | None added; deferred per CONTEXT |
| `setBy` richer audit log (rename history modal) | Inline attribution only, no history view | js/app.js:8887 ships inline only |
| `legacyGraceWrite()` extension | Body unchanged | firestore.rules:86-90 — 5-line shape preserved |

All deferred items correctly remain undone.

---

## Production Verification

| Check | Result |
|-------|--------|
| https://couchtonight.app/ HTTP status | 200 OK |
| https://couchtonight.app/sw.js CACHE | `couch-v35.1-security-hardening` |
| Firestore rules deployed to queuenight-84044 | Confirmed via 15.1-03-SUMMARY.md Step 4 (`firestore: released rules firestore.rules to cloud.firestore` + `Deploy complete!`) — supplementary live-rules check would require Firebase Console access |
| Deploy timeline | Hosting 13:20 UTC, Rules 13:21 UTC (gap <1 min, effectively lockstep) |

---

## Commits Verified

Commit chain present in couch repo (latest first):

| Plan | Commits | Status |
|------|---------|--------|
| 15.1-01 | 1443fa6 (writers) + 2e61547 (actingTupleKey) + 8dc9c52 (pencil + setBy) + 6593357 (SUMMARY) | All present |
| 15.1-02 | 7ab3015 (anchor + 4th sub-rule) + 5b5fdb7 (5th-branch) + 5d605f4 (tests) + 105d6de (SUMMARY) | All present |
| 15.1-03 | 341cac9 (changelog) + ff57f71 (sw.js bump) + 2d7089d (deploy date) + a481138 (REQUIREMENTS+ROADMAP) + 8e755f9 (SUMMARY) | All present |

queuenight repo commit `c9e27c4` (mirror commit) referenced in 15.1-03-SUMMARY.md but in a sibling repo (not verified directly here — out of working-tree scope; mirror byte-identity is the actual gate and was verified).

---

## Gaps Summary

**No gaps found.** All 18 must-haves verified, all 5 requirements satisfied, all 4 audit findings closed with demonstrable codebase blocks for the original exploit paths, all locked decisions honored, all deferred items correctly NOT touched, source/mirror parity enforced, full test suite green (38/38), and production live at couchtonight.app with the expected CACHE.

The phase delivered exactly what its goal demanded: 5 SEC-15-1-* requirements shipped via 3 plans across 3 waves, with REVERSED deploy ritual successfully executed (effectively lockstep — gap <1 min). Phase 15.1 is auditably closed.

---

*Verified: 2026-04-27T13:35:00Z*
*Verifier: Claude (gsd-verifier)*
