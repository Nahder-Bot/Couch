---
phase: 15
plan: 08
type: execute
wave: 7
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05", "15-06", "15-07"]
files_modified:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - "sw.js"
  - "changelog.html"
  - "~/queuenight/firestore.rules"
autonomous: false
requirements:
  - TRACK-15-13
  - TRACK-15-14
user_setup:
  - service: firebase
    why: "Cross-repo deploy ritual — Phase 15 spans the couch repo (hosting + sw.js + changelog) AND the queuenight deploy-mirror (firestore:rules, firestore:indexes, functions). User authenticates via firebase login from each repo and runs the 4-step deploy in order."
    env_vars: []
    dashboard_config:
      - task: "Verify Phase 15 / D-02 5th UPDATE branch deploys to live rules; verify watchparties (titleId asc, startAt asc) composite index builds successfully; verify Phase 15 title-doc HIGH-1 isolation rules + family-doc coWatchPromptDeclined allowlist extension are live"
        location: "Firebase Console → Firestore → Rules + Indexes — https://console.firebase.google.com/project/queuenight-84044"

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md contains a new ### Tracking Layer (Phase 15) section with TRACK-15-01 through TRACK-15-14 entries below the existing ### Decision Ritual Core (Phase 14) section"
    - "REQUIREMENTS.md Traceability table contains a row for each TRACK-15-* requirement mapped to its plan number"
    - ".planning/ROADMAP.md row 15 narrative is scrubbed to say '14 TRACK-15-*' (was '13 TRACK-15-*'); per REVIEW LOW-narrative-fix, this is a single-phrase scrub, NOT a structural edit"
    - "REVIEW MEDIUM-9 cross-plan coordination: ~/queuenight/firestore.rules family-doc 5th UPDATE branch allowlist is extended to include 'coWatchPromptDeclined' before the rules deploy step (Task 4 below)"
    - "sw.js CACHE constant updated from couch-v34.1.3-selectedmembers-shim → couch-v35.0-tracking-layer"
    - "changelog.html contains a new <article class=\"release\"> block for v35 above the existing v34 article"
    - "User runs the 4-step cross-repo deploy ritual: (1) firebase deploy --only firestore:rules from queuenight; (2) firebase deploy --only firestore:indexes from queuenight; (3) firebase deploy --only functions from queuenight; (4) bash scripts/deploy.sh 35.0-tracking-layer from couch repo"
    - "After deploy, the changelog.html v35 article's [deploy date YYYY-MM-DD] placeholder is replaced with the actual deploy date"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "14 new TRACK-15-* requirement IDs in a new ### Tracking Layer (Phase 15) section + Traceability table updates"
      contains: "TRACK-15-01"
    - path: ".planning/ROADMAP.md"
      provides: "Row 15 narrative scrubbed: '13 TRACK-15-*' → '14 TRACK-15-*' (REVIEW LOW-narrative-fix one-phrase change)"
      contains: "14 TRACK-15-*"
    - path: "sw.js"
      provides: "CACHE bumped to couch-v35.0-tracking-layer"
      contains: "couch-v35.0-tracking-layer"
    - path: "changelog.html"
      provides: "New v35 release article above the v34 entry"
      contains: 'release-version">v35'
    - path: "~/queuenight/firestore.rules"
      provides: "5th UPDATE branch allowlist extended to include coWatchPromptDeclined (REVIEW MEDIUM-9 cross-plan coordination)"
      contains: "coWatchPromptDeclined"
  key_links:
    - from: "sw.js CACHE bump"
      to: "Installed PWAs invalidating on next online activation"
      via: "Service Worker re-fetches /app + /css/app.css + /js/app.js when CACHE constant changes"
      pattern: "couch-v35"
    - from: "Cross-repo deploy ritual"
      to: "Production at couchtonight.app"
      via: "rules → indexes → CFs → app.html — strict ordering required"
      pattern: "couch-v35.0-tracking-layer"
    - from: "REVIEW MEDIUM-9 allowlist extension in this plan's Task 4"
      to: "15-07 cv15CoWatchPromptDecline write path"
      via: "extends 15-01 5th branch's hasOnly([...]) allowlist before deploy"
      pattern: "coWatchPromptDeclined"
---

<objective>
Close out Phase 15 — Tracking Layer.

<!-- Reviews 2026-04-26: REVIEW MEDIUM-9 cross-plan coordination (Task 4 — extend 15-01's family-doc 5th UPDATE branch allowlist to include coWatchPromptDeclined before the rules deploy step). REVIEW LOW-narrative-fix (Task 1.5 — scrub the single stale "13 TRACK-15-*" phrase from ROADMAP.md row 15 narrative; the count was minted at 14 in commits bf35c64 + e7b4a73 but a phrase was missed). The "13 vs 14 requirements" inconsistency that Codex flagged is purely narrative — no structural defect, no double-count anywhere; just one stale phrase to scrub. -->

This plan ships the cross-cutting artifacts that span every requirement in
the phase: the requirement IDs minted in REQUIREMENTS.md, the sw.js CACHE
bump, the changelog.html v35 release article, the cross-plan rules
allowlist extension that 15-07's coWatchPromptDeclined writes need, the
ROADMAP narrative scrub, and the human-verify checkpoint for the 4-step
cross-repo deploy ritual.

Per RESEARCH §Cross-Repo Deploy Ordering + Phase 14 LEARNINGS, Phase 15
spans BOTH repos. The deploy ORDER is non-negotiable:
1. `cd ~/queuenight && firebase deploy --only firestore:rules --project queuenight-84044`
   (must include 15-01 Task 1 + Task 1.5 + the Task 4 allowlist extension below)
2. `cd ~/queuenight && firebase deploy --only firestore:indexes --project queuenight-84044`
   — wait for build to complete (1-5 minutes)
3. `cd ~/queuenight && firebase deploy --only functions`
   — CF cannot be deployed until step 2 is live
4. `cd ~/claude-projects/couch && bash scripts/deploy.sh 35.0-tracking-layer`

Per Phase 14-06 deviation 1 + STATE.md "Two-repo discipline" rows, the user
runs the queuenight commits + deploys from a queuenight-equipped session.
This plan's checkpoint pauses for the user to confirm `approved-deploy`.

Per RESEARCH §Q5 + Risks #4, the index build can take 1-5 minutes — the
deploy ritual MUST wait for it before deploying CFs.

REQ-IDs minted (14 total — REVIEW LOW-narrative-fix scrubs the "13"
mention from ROADMAP):
- TRACK-15-01: Firestore data foundation (rules + index + tests + REVIEW HIGH-1 title-doc tightening) → 15-01
- TRACK-15-02: Tuple read/write helpers + family-doc snapshot hydration + REVIEW HIGH-2 isSafeTupleKey + REVIEW MEDIUM-6 tupleCustomName + REVIEW MEDIUM-8 optimistic state → 15-02
- TRACK-15-03: D-01 watchparty-end auto-track stash with REVIEW MEDIUM-5 episode-resolution waterfall → 15-03
- TRACK-15-04: S2 detail-modal "Your couch's progress" section with REVIEW MEDIUM-6 placeholder fix + REVIEW MEDIUM-7 data-* delegated listeners → 15-04
- TRACK-15-05: S3 inline tuple rename → 15-04
- TRACK-15-06: S6 per-show kill-switch → 15-04
- TRACK-15-07: S1 Tonight tab "Pick up where you left off" widget → 15-05
- TRACK-15-08: CF live-release sweep with REVIEW HIGH-3 hourly throttle + HIGH-4 stale guard + LOW/MEDIUM-11 widened window + MEDIUM-12 push framing → 15-06
- TRACK-15-09: NOTIFICATION_DEFAULTS server triad place 1 of 3 → 15-06
- TRACK-15-10: DEFAULT_NOTIFICATION_PREFS triad place 2 of 3 → 15-07
- TRACK-15-11: NOTIFICATION_EVENT_LABELS triad place 3 of 3 with REVIEW MEDIUM-12 reworded label → 15-07
- TRACK-15-12: Trakt episode-timestamp + co-watch overlap S5 prompt with REVIEW MEDIUM-9 decline persistence + REVIEW MEDIUM-10 single-comparator → 15-07
- TRACK-15-13: sw.js CACHE bump + changelog v35 + cross-repo deploy ritual + ROADMAP narrative scrub + REVIEW MEDIUM-9 cross-plan allowlist extension → 15-08 (this plan)
- TRACK-15-14: D-07 Trakt opt-in Settings disclosure → 15-07

Output: 4 file edits + 1 cross-plan rules patch + 1 ROADMAP narrative scrub +
2 human-action checkpoints (push-copy approval + cross-repo deploy ritual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\nahde\claude-projects\couch\CLAUDE.md
@C:\Users\nahde\claude-projects\couch\.planning\PROJECT.md
@C:\Users\nahde\claude-projects\couch\.planning\STATE.md
@C:\Users\nahde\claude-projects\couch\.planning\REQUIREMENTS.md
@C:\Users\nahde\claude-projects\couch\.planning\ROADMAP.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-CONTEXT.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-RESEARCH.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-PATTERNS.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-UI-SPEC.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-REVIEWS.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-01-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-02-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-03-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-04-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-05-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-06-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint TRACK-15-* requirement IDs in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 130-155 (the Phase 14 ### Decision Ritual Core section — clone-as-sibling source)
    - .planning/REQUIREMENTS.md lines 270-285 (the Traceability table tail — DECI-14-* rows)
    - 15-CONTEXT.md (the 16 D-XX decisions that map to TRACK-15-* requirements)
    - 15-PATTERNS.md (the per-plan TRACK-15-* mapping)
  </read_first>
  <action>
Open `.planning/REQUIREMENTS.md`. Find `### Decision Ritual Core (Phase 14)`
(verified at line 134) and read forward to the `### v2 Requirements` section
header at line 152.

INSERT a new section IMMEDIATELY AFTER the closing of the Phase 14 section
(after the last DECI-14-* bullet at line 150) and BEFORE `## v2 Requirements`
at line 152.

The block to insert:

```markdown

### Tracking Layer (Phase 15)

Scoped via /gsd-discuss-phase 15 (2026-04-26) — 16 user decisions locked
(D-01..D-16). Per-watching-group watch progress (tracks GROUPS, not just
individuals — Couch's primary differentiator vs Trakt) + new-episode air-date
push notifications + live-release watchparty auto-prompts. Builds on Phase 14's
intents collection + 7-key push framework (extends to 8 keys). 14 TRACK-15-*
requirements minted at /gsd-plan-phase 15 (2026-04-26); revised at /gsd-plan-phase
15 --reviews (2026-04-26) to incorporate cross-AI peer feedback (REVIEW HIGH-1
through MEDIUM-12).

- [ ] **TRACK-15-01**: Firestore data foundation — 5th UPDATE branch on `/families/{familyCode}` permitting attributedWrite of `tupleNames` map (D-02) AND `coWatchPromptDeclined` map (REVIEW MEDIUM-9 cross-plan coordination via 15-08 Task 4); composite index on `watchparties (titleId asc, startAt asc)` for D-16 suppression query; 10 sibling tests (#23-#32) covering tupleNames + per-member tupleProgress isolation + per-member mutedShows isolation + liveReleaseFiredFor client-DENY (REVIEW HIGH-1). (D-02 / D-06 / D-12 / D-16 / REVIEW HIGH-1)
- [ ] **TRACK-15-02**: Tuple read/write helpers in js/app.js — `tupleKey` (with REVIEW HIGH-2 isSafeTupleKey character-safety regex), `tupleProgressFromTitle`, `tuplesContainingMember`, `tupleDisplayName` (derived fallback), `tupleCustomName` (REVIEW MEDIUM-6 — returns null when no custom name set, fixing the placeholder render gate in 15-04), `writeTupleProgress`/`clearTupleProgress`/`setTupleName`/`writeMutedShow`. setTupleName performs REVIEW MEDIUM-8 optimistic local state update. State.family.tupleNames hydrated from family-doc onSnapshot. Coexists with existing per-individual t.progress[memberId] for v1. (D-02 / D-03 / D-08 / REVIEW HIGH-2 / REVIEW MEDIUM-6 / REVIEW MEDIUM-8)
- [ ] **TRACK-15-03**: D-01 watchparty-end auto-track stash — extends `openPostSession` with `state._pendingTupleAutoTrack` populated via REVIEW MEDIUM-5 episode-resolution waterfall (wp.episode → wp.queuedEpisode → wp.intent → host-progress-plus-1 → null). Records `sourceField` for confidence telemetry. Plus `window.toggleMutedShow(titleId)` click handler. (D-01 / REVIEW MEDIUM-5)
- [ ] **TRACK-15-04**: S2 detail-modal "YOUR COUCH'S PROGRESS" section — `renderCv15TupleProgressSection(t)` with REVIEW MEDIUM-6 placeholder gate (uses tupleCustomName(tk) === null) + REVIEW MEDIUM-7 data-* attributes + delegated event listener (cv15HandleDetailModalClick on #detail-modal-body). Rows sorted watchedAt desc, max 4 visible, "View all (N)" affordance for >4. (D-04 / Q2 / REVIEW MEDIUM-6 / REVIEW MEDIUM-7)
- [ ] **TRACK-15-05**: S3 inline tuple rename — pencil glyph reveals input via delegated listener; Enter/blur saves via setTupleName (REVIEW MEDIUM-8 optimistic local state means re-render reads new value immediately). flashToast on success/error per UI-SPEC. (D-02 / Q3 / REVIEW MEDIUM-7 / REVIEW MEDIUM-8)
- [ ] **TRACK-15-06**: S6 per-show kill-switch — `renderCv15MutedShowToggle(t)` text-link with REVIEW MEDIUM-7 data-cv15-action attribute. Off: "Stop notifying me about this show"; on: "Notifications off · Re-enable". (D-12 / Q5 / REVIEW MEDIUM-7)
- [ ] **TRACK-15-07**: S1 Tonight tab "PICK UP WHERE YOU LEFT OFF" widget — `<div id="cv15-pickup-container">` between #couch-viz-container and #flow-a-entry-container; `renderPickupWidget()` cross-show roll-up filtering tuples-containing-me, sorted desc, max 3 visible. Hides on zero tuples. Coexists with legacy #continue-section. (D-04 / Q1 / Q7)
- [ ] **TRACK-15-08**: CF live-release sweep — per-titles loop in queuenight/functions/index.js watchpartyTick AFTER intents loop, BEFORE outer-family-loop close. REVIEW HIGH-3 hourly throttle (families/{code}.liveReleaseSweepLastRunAt cursor — once per hour, not every 5 min). REVIEW HIGH-4 stale-data guard (skip titles with t.nextEpisodeRefreshedAt > 7 days, Sentry breadcrumb). REVIEW LOW/MEDIUM-11 widened window (T-26h to T-23h, was T-25h). REVIEW MEDIUM-12 push framing ("New episode tonight/tomorrow/{Weekday}" — describes per-EPISODE prompt). D-14 ≥2 trackers, D-16 already-scheduled-wins, idempotency via t.liveReleaseFiredFor. CF admin SDK bypasses 15-01's HIGH-1 client-DENY rule for liveReleaseFiredFor. (D-11 / D-13 / D-14 / D-15 / D-16 / REVIEW HIGH-3 / REVIEW HIGH-4 / REVIEW LOW-MEDIUM-11 / REVIEW MEDIUM-12)
- [ ] **TRACK-15-09**: Server NOTIFICATION_DEFAULTS triad place 1 of 3 — adds `newSeasonAirDate: true` 8th key to queuenight/functions/index.js:74-96. Lockstep contract with client DEFAULT_NOTIFICATION_PREFS (TRACK-15-10). Per Phase 14-09 DR-3, NOT mirrored to friendly-UI maps. (D-09 / D-12 / DR-3)
- [ ] **TRACK-15-10**: Client DEFAULT_NOTIFICATION_PREFS triad place 2 of 3 — adds `newSeasonAirDate: true` 8th key. Default-on per D-09. (D-09 / D-12 / DR-3)
- [ ] **TRACK-15-11**: Client NOTIFICATION_EVENT_LABELS triad place 3 of 3 — adds entry with REVIEW MEDIUM-12 reworded label "New episode alerts" + hint "When a tracked show drops a new episode." (was "New season air dates" — Codex flagged the original label as overpromising). KEY stays 'newSeasonAirDate' for back-compat. (D-12 / DR-3 / REVIEW MEDIUM-12)
- [ ] **TRACK-15-12**: Trakt co-watch overlap detection + S5 prompt — extends trakt.ingestSyncData with `lastWatchedAt` capture; `trakt.detectAndPromptCoWatchOverlap` walks state.titles after every sync. REVIEW MEDIUM-10 single-comparator (cv15EpisodeOrdinal + cv15SelectHigherProgress) — both season AND episode copied from the winning progress object (fixes S5E1-vs-S4E10 mismatch bug). REVIEW MEDIUM-9 durable decline persistence at families/{code}.coWatchPromptDeclined[tupleKey] — declined pairs do NOT re-prompt on subsequent syncs. S5 modal verbatim from UI-SPEC. (D-05 / D-06 / REVIEW MEDIUM-9 / REVIEW MEDIUM-10)
- [ ] **TRACK-15-13**: sw.js CACHE constant bumped to `couch-v35.0-tracking-layer` and deployed via the cross-repo 4-step ritual. REQUIREMENTS.md mint + Traceability table updates + ROADMAP.md narrative scrub (REVIEW LOW-narrative-fix: "13 TRACK-15-*" → "14 TRACK-15-*") + REVIEW MEDIUM-9 cross-plan allowlist extension to firestore.rules + changelog.html v35 article + scripts/deploy.sh invocation. (cross-cutting / REVIEW MEDIUM-9 / REVIEW LOW-narrative-fix)
- [ ] **TRACK-15-14**: D-07 Trakt opt-in Settings disclosure — `renderTraktCard()` disconnected branch prepends literal `JUMP-START YOUR COUCH'S HISTORY WITH TRAKT` eyebrow + italic Instrument Serif `Optional — tracking works without it.` sub-line above the existing `Already tracking on Trakt?` body and Connect button at app.html:679. Permanent (not dismissable). Verbatim from UI-SPEC §Discretion Q9. (D-07 / Q9)
```

THEN locate the Traceability table at line 185-282. Find the last DECI-14-13
row. INSERT 14 new rows IMMEDIATELY AFTER:

```markdown
| TRACK-15-01 | Phase 15 Plan 01 | Pending |
| TRACK-15-02 | Phase 15 Plan 02 | Pending |
| TRACK-15-03 | Phase 15 Plan 03 | Pending |
| TRACK-15-04 | Phase 15 Plan 04 | Pending |
| TRACK-15-05 | Phase 15 Plan 04 | Pending |
| TRACK-15-06 | Phase 15 Plan 04 | Pending |
| TRACK-15-07 | Phase 15 Plan 05 | Pending |
| TRACK-15-08 | Phase 15 Plan 06 | Pending |
| TRACK-15-09 | Phase 15 Plan 06 | Pending |
| TRACK-15-10 | Phase 15 Plan 07 | Pending |
| TRACK-15-11 | Phase 15 Plan 07 | Pending |
| TRACK-15-12 | Phase 15 Plan 07 | Pending |
| TRACK-15-13 | Phase 15 Plan 08 | Pending |
| TRACK-15-14 | Phase 15 Plan 07 | Pending |
```

Update the Coverage line near end-of-file (currently "v1 requirements: 88 total"):

```markdown
- v1 requirements: 102 total (41 complete across Phases 3+4+7+9+11+14; 61 pending across Phases 5/6/8/12/13/14/15 + deferred Phase 10)
```

(88 + 14 = 102.)

Update the "Last updated" line at end-of-file with a 2026-04-26 entry (append, don't replace).
  </action>
  <verify>
    <automated>cd ~/claude-projects/couch &amp;&amp; grep -c "### Tracking Layer (Phase 15)" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do grep -q "TRACK-15-$n" .planning/REQUIREMENTS.md || (echo "missing TRACK-15-$n"; exit 1); done &amp;&amp; grep -c "TRACK-15-13 | Phase 15 Plan 08" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; grep -c "TRACK-15-14 | Phase 15 Plan 07" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; grep -c "v1 requirements: 102 total" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Tracking Layer (Phase 15)" .planning/REQUIREMENTS.md` returns `1`
    - All 14 TRACK-15-* IDs present
    - Traceability table contains 14 new TRACK-15-* rows — `grep -c "| TRACK-15-" .planning/REQUIREMENTS.md` returns `14`
    - Coverage line updated to "v1 requirements: 102 total"
    - All prior phase rows unmodified
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains a new ### Tracking Layer (Phase 15) section with
    14 TRACK-15-* IDs (numbering reflects all REVIEW patches) + 14 Traceability
    rows + Coverage 102 total. Prior content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 1.5 (REVIEW LOW-narrative-fix): Scrub ROADMAP.md row 15 stale "13 TRACK-15-*" phrase</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md row 15 (line 255 — verified by orchestrator). The narrative contains a single phrase "13 TRACK-15-* requirements" which is stale; the count was minted at 14 in commits bf35c64 + e7b4a73 but a single phrase was missed during prior planning passes.
    - 15-REVIEWS.md "Codex Review" line 107-108 (the LOW concern: "Requirement counts are inconsistent in the prompt"). Per orchestrator's edit_constraints "Items to NOT act on" + the orchestrator's resolution: this is a single-phrase scrub, NOT a structural defect — handled here as a one-line edit.
  </read_first>
  <action>
Open `.planning/ROADMAP.md`. Locate line 255 (the Phase 15 row). Use Grep to
confirm the exact stale phrase:

```bash
grep -n "13 TRACK-15-\\*" .planning/ROADMAP.md
```

Expected: 1 match (or possibly 2 — the row narrative). REPLACE every match
within row 15 with `14 TRACK-15-*`.

If the phrase appears in the form "13 TRACK-15-* requirements", change to
"14 TRACK-15-* requirements". The count was always 14 in the requirements
themselves; only the narrative was stale.

Do NOT modify any other row. Do NOT modify the Phase-slot history section
or any other phase's row. Do NOT change the "0/8" plan-count notation
(plans count is 8; requirement count is 14 — different concepts).

Verify:
```bash
grep -c "13 TRACK-15-\\*" .planning/ROADMAP.md   # should now be 0
grep -c "14 TRACK-15-\\*" .planning/ROADMAP.md   # should be at least 1
```
  </action>
  <verify>
    <automated>cd ~/claude-projects/couch &amp;&amp; grep -c "13 TRACK-15-\*" .planning/ROADMAP.md | grep -q "^0$" &amp;&amp; grep -c "14 TRACK-15-\*" .planning/ROADMAP.md | awk '$1 &gt;= 1 {print "OK"}' | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "13 TRACK-15-\\*" .planning/ROADMAP.md` returns `0` (stale phrase scrubbed)
    - `grep -c "14 TRACK-15-\\*" .planning/ROADMAP.md` returns at least `1` (correct count present)
    - Only line 255 (Phase 15 row) is modified — `git diff --stat .planning/ROADMAP.md` shows minimal change (1-2 lines)
    - All other phase rows unmodified
    - Phase-slot history section unchanged
  </acceptance_criteria>
  <done>
    .planning/ROADMAP.md row 15 narrative now correctly says "14 TRACK-15-*"
    instead of the stale "13 TRACK-15-*". REVIEW LOW-narrative-fix resolved
    as a single-phrase scrub.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump sw.js CACHE constant + add v35 changelog.html article</name>
  <files>sw.js, changelog.html</files>
  <read_first>
    - sw.js (full read — only 109 lines per CLAUDE.md "tiny, read freely")
    - changelog.html (full read — only 149 lines)
    - 15-PATTERNS.md §F19 + §F20
    - 15-UI-SPEC.md §Surface inventory
    - 15-REVIEWS.md MEDIUM-12 — note the changelog body should describe per-EPISODE alerts (matches the reworded label)
  </read_first>
  <action>
**Part A — sw.js CACHE bump:**

Open `sw.js`. Find `const CACHE = 'couch-v34.1.3-selectedmembers-shim';` at
line 8. REPLACE with:

```javascript
const CACHE = 'couch-v35.0-tracking-layer';
```

Do NOT modify any other line.

**Part B — changelog.html v35 article:**

Open `changelog.html`. Find `<article class="release">` at line 67 (the v34
article). The new v35 article goes IMMEDIATELY ABOVE it.

INSERT this block between the closing `</section>` of the changelog-hero and
the v34 article opening:

```html
      <article class="release">
        <header class="release-h">
          <span class="release-version">v35</span>
          <span class="release-date">[deploy date YYYY-MM-DD] · Tracking layer</span>
        </header>
        <p class="release-summary"><em>See where every couch is in every show &mdash; together or apart.</em></p>
        <ul class="release-list">
          <li><strong>Pick up where you left off:</strong> tonight tab now surfaces what every couch is mid-watch.</li>
          <li><strong>Group your watches:</strong> auto-track who's on the couch when a watchparty ends. Name the couch ("Date night") if you want.</li>
          <li><strong>New episode alerts:</strong> tracked shows ping the couch a day before a fresh episode drops. Per REVIEW MEDIUM-12 — describes per-episode prompts (not new-season-only).</li>
          <li><strong>Trakt overlap nudge:</strong> when two of you watch the same episode around the same time, we'll ask if you're a couch. Decline once and we won't re-ask.</li>
        </ul>
      </article>
```

The placeholder `[deploy date YYYY-MM-DD]` is replaced in Task 3.

The "New episode alerts" + "Decline once and we won't re-ask" copy aligns
with REVIEW MEDIUM-12 (label rework) + REVIEW MEDIUM-9 (durable decline).

Do NOT modify the v34 article or any prior article.
  </action>
  <verify>
    <automated>cd ~/claude-projects/couch &amp;&amp; grep -c "couch-v35.0-tracking-layer" sw.js | grep -q "^1$" &amp;&amp; grep -c "couch-v34.1.3-selectedmembers-shim" sw.js | grep -q "^0$" &amp;&amp; grep -c '&lt;span class="release-version"&gt;v35&lt;/span&gt;' changelog.html | grep -q "^1$" &amp;&amp; grep -c "Pick up where you left off:" changelog.html | grep -q "^1$" &amp;&amp; grep -c "New episode alerts:" changelog.html | grep -q "^1$" &amp;&amp; grep -c "Decline once and we won't re-ask" changelog.html | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "couch-v35.0-tracking-layer" sw.js` returns `1`
    - `grep -c "couch-v34.1.3-selectedmembers-shim" sw.js` returns `0`
    - `grep -c '<span class="release-version">v35</span>' changelog.html` returns `1`
    - `grep -c '<span class="release-version">v34</span>' changelog.html` returns `1` (existing v34 still present)
    - The v35 article sits ABOVE v34
    - **REVIEW MEDIUM-12 alignment:** `grep -c "New episode alerts:" changelog.html` returns `1`
    - **REVIEW MEDIUM-9 alignment:** `grep -c "Decline once and we won't re-ask" changelog.html` returns `1`
    - `grep -c "Tracking layer" changelog.html` returns `1`
    - `grep -c "\\[deploy date YYYY-MM-DD\\]" changelog.html` returns `1` (placeholder pending Task 3)
  </acceptance_criteria>
  <done>
    sw.js CACHE bumped. changelog.html v35 article above v34 with REVIEW
    MEDIUM-9 + MEDIUM-12 aligned copy.
  </done>
</task>

<task type="auto">
  <name>Task 4 (REVIEW MEDIUM-9 cross-plan): Extend ~/queuenight/firestore.rules family-doc 5th UPDATE branch allowlist to include coWatchPromptDeclined</name>
  <files>~/queuenight/firestore.rules</files>
  <read_first>
    - `~/queuenight/firestore.rules` lines 180-220 (the Phase 15 / D-02 5th UPDATE branch added by 15-01 Task 1; the allowlist `.hasOnly([...])` line is the patch target)
    - 15-01-SUMMARY.md (confirms the exact line range where the 5th branch was inserted)
    - 15-07-SUMMARY.md "CRITICAL CROSS-PLAN COORDINATION NOTE" — drives this task; 15-07's cv15CoWatchPromptDecline writes to families/{code}.coWatchPromptDeclined which is NOT in the original 15-01 allowlist
    - 15-REVIEWS.md MEDIUM-9 — the upstream concern requiring durable decline persistence
  </read_first>
  <action>
This task closes a cross-plan coordination gap surfaced by 15-07's REVIEW
MEDIUM-9 patch. 15-07's `cv15CoWatchPromptDecline` writes to
`families/{code}.coWatchPromptDeclined.${tupleKey}` via the dotted-path
update form — but 15-01's family-doc 5th UPDATE branch allowlist does NOT
include `coWatchPromptDeclined`. Without this patch, the decline write
will be REJECTED by rules with permission_denied.

This task lives in 15-08 (not 15-01) for two reasons:
1. 15-01 was already validated and committed; rather than re-spawning the
   15-01 planner+checker cycle, we patch the file once here right before
   the rules deploy step (Task 3 below).
2. 15-08 owns the deploy ritual; the rules edit must land BEFORE Task 3
   Step 1 (`firebase deploy --only firestore:rules`) so the new field's
   write path is permitted from day one of the v35 release.

Use Grep to locate the Phase 15 / D-02 5th UPDATE branch in
`~/queuenight/firestore.rules`:

```bash
grep -n "Phase 15 / D-02" ~/queuenight/firestore.rules
```

Expected: 1 match (the comment header at the top of the 5th branch). Then
read the 5-key allowlist line below it:

```bash
grep -n "hasOnly(\\['tupleNames'" ~/queuenight/firestore.rules
```

Expected: 1 match showing:
```javascript
            .hasOnly(['tupleNames', 'actingUid', 'managedMemberId', 'memberId', 'memberName'])
```

REPLACE this single line with:

```javascript
            .hasOnly(['tupleNames', 'coWatchPromptDeclined', 'actingUid', 'managedMemberId', 'memberId', 'memberName'])
```

(Adds `'coWatchPromptDeclined'` as the second element after `'tupleNames'`.)

Also update the comment header above the 5th branch to reflect the new field.
Find the existing comment:

```javascript
        // === Phase 15 / D-02 (TRACK-15-01) — tupleNames write ===
```

REPLACE with:

```javascript
        // === Phase 15 / D-02 + D-06 (TRACK-15-01 / REVIEW MEDIUM-9) — tupleNames + coWatchPromptDeclined writes ===
```

And add an additional comment line below the existing comment block describing
the new field, before the `attributedWrite(familyCode)` line:

```javascript
        // REVIEW MEDIUM-9 (15-07 / 15-08 cross-plan coordination) —
        // coWatchPromptDeclined is the durable decline record for Trakt overlap
        // prompts. Shape: families/{code}.coWatchPromptDeclined =
        // { [tupleKey]: <timestamp> }. Written by cv15CoWatchPromptDecline in
        // 15-07 via dotted-path update; read on detector entry to skip
        // already-declined pairs.
```

Do NOT modify any OTHER branch. Do NOT touch the title-doc HIGH-1 sub-rules
from 15-01 Task 1.5. Do NOT touch the indexes file. Verify the rules file
still validates:

```bash
cd ~/queuenight && firebase emulators:exec --only firestore "echo OK" 2>&1 | grep -i "rules" || true
```

(Best-effort smoke; the actual validation is the rules emulator running the
test suite from Task 3.)
  </action>
  <verify>
    <automated>cd ~/queuenight &amp;&amp; grep -c "'coWatchPromptDeclined'" firestore.rules | grep -q "^1$" &amp;&amp; grep -c "REVIEW MEDIUM-9 (15-07 / 15-08 cross-plan coordination)" firestore.rules | grep -q "^1$" &amp;&amp; grep -c "hasOnly(\['tupleNames', 'coWatchPromptDeclined'" firestore.rules | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'coWatchPromptDeclined'" ~/queuenight/firestore.rules` returns `1`
    - `grep -c "hasOnly(\\['tupleNames', 'coWatchPromptDeclined'" ~/queuenight/firestore.rules` returns `1` (allowlist properly extended in correct order)
    - `grep -c "REVIEW MEDIUM-9" ~/queuenight/firestore.rules` returns at least `1` (audit-trail comment)
    - The original 5-key allowlist (without coWatchPromptDeclined) is GONE — `grep -c "hasOnly(\\['tupleNames', 'actingUid'" ~/queuenight/firestore.rules` returns `0`
    - The HIGH-1 title-doc rules from 15-01 Task 1.5 are unmodified — `grep -c "REVIEW HIGH-1 — explicit DENY for liveReleaseFiredFor" ~/queuenight/firestore.rules` still returns `1`
    - `grep -c "match /titles/{titleId}" ~/queuenight/firestore.rules` is still `1` (no duplicate match blocks)
  </acceptance_criteria>
  <done>
    `~/queuenight/firestore.rules` family-doc 5th UPDATE branch allowlist now
    includes `'coWatchPromptDeclined'` as the second key (after `'tupleNames'`).
    The audit comment surfaces the cross-plan coordination. 15-07's
    cv15CoWatchPromptDecline writes will now succeed against production rules.
    All other branches unmodified. Deploy step (Task 3 Step 1) will pick up
    this rule change.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2.5 (id 15-08-T2.5): User-approve substituted push copy + REVIEW MEDIUM-12 framing alignment</name>
  <what-built>
    Per RESEARCH §Q6 + Open Question Q1, TMDB API exposes only date-only precision
    for episode air dates and does not reliably expose per-episode streaming
    provider for upcoming releases. Per REVIEW MEDIUM-12, the customer-facing
    copy + push body in 15-06 + the NOTIFICATION_EVENT_LABELS entry in 15-07 have
    been reworded to describe per-EPISODE prompts ("New episode tonight" /
    "New episode alerts") rather than the original "New season air dates" /
    "{Show} S{N} hits {Provider} {day}" framing.

    The KEY remains 'newSeasonAirDate' for back-compat with the D-12 push
    framework, but the user-visible copy now matches what the implementation
    actually delivers.
  </what-built>
  <how-to-verify>
**Locked CONTEXT.md templates (cannot be honored verbatim with TMDB v1):**
- D-11 push payload: `{Show} S{N} hits {Provider} {day}. Watch with the couch?`
- Specifics: `{Show} S{N}E{M} airs {day} at {time}. Watch with the couch?`

**Substituted copy actually shipping:**
- Push title (15-06): `New episode {tonight|tomorrow|Weekday}`
- Push body (15-06): `{Show} S{N}E{M} — watch with the couch?`
- Settings label (15-07): `New episode alerts`
- Settings hint (15-07): `When a tracked show drops a new episode.`

**Drops vs locked:**
- {Provider} dropped — TMDB does not reliably expose per-episode provider
- {time} dropped — TMDB exposes air_date as date-only
- "season" framing replaced with "episode" framing per REVIEW MEDIUM-12

**Three response options:**

1. Reply `approved-push-copy` to PROCEED.

2. Reply `add-provider-fetch` to BOUNCE BACK — addendum plan to add CF-side
   TMDB watch-providers lookup so {Provider} can ship. Still no {time}.

3. Reply `block` to STOP the deploy.
  </how-to-verify>
  <resume-signal>Reply `approved-push-copy` to proceed to Task 3 deploy. Reply `add-provider-fetch` to bounce back. Reply `block` to stop.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User runs cross-repo deploy ritual + replaces changelog [deploy date YYYY-MM-DD] placeholder</name>
  <what-built>
    Phase 15 — Tracking Layer code is complete across both repos:
    - couch repo: js/app.js (5 plans worth of edits including REVIEW HIGH-2 / MEDIUM-5 / MEDIUM-6 / MEDIUM-7 / MEDIUM-8 / MEDIUM-9 / MEDIUM-10 / MEDIUM-12 patches) + css/app.css + app.html + sw.js + changelog.html + REQUIREMENTS.md + ROADMAP.md
    - queuenight repo: firestore.rules (REVIEW HIGH-1 title-doc tightening + REVIEW MEDIUM-9 5th-branch allowlist extension via Task 4) + firestore.indexes.json + functions/index.js (REVIEW HIGH-3 throttle + HIGH-4 stale guard + LOW/MEDIUM-11 widened window + MEDIUM-12 framing)
  </what-built>
  <how-to-verify>
1. Verify queuenight repo edits are committed locally:
   ```bash
   cd ~/queuenight
   git status               # should show modified firestore.rules, firestore.indexes.json, functions/index.js
   git diff --stat          # confirm scope: 5th UPDATE branch + Task 1.5 title-doc tightening + Task 4 coWatchPromptDeclined extension + composite index + watchpartyTick extension + NOTIFICATION_DEFAULTS 8th key
   git add firestore.rules firestore.indexes.json functions/index.js
   git commit -m "feat(15): tracking layer — rules + index + CF sweep + push category (with REVIEW patches)"
   ```

2. Step 1 — Deploy rules (includes REVIEW HIGH-1 + REVIEW MEDIUM-9 extensions):
   ```bash
   cd ~/queuenight
   firebase deploy --only firestore:rules --project queuenight-84044
   ```
   Expected: "✔  firestore: released rules". Without this, Phase 15 PWA writes fail with permission_denied.

3. Step 2 — Deploy composite index:
   ```bash
   cd ~/queuenight
   firebase deploy --only firestore:indexes --project queuenight-84044
   firebase firestore:indexes               # observe build status
   ```
   Expected: index build COMPLETES. WAIT for live status before Step 3.

4. Step 3 — Deploy CFs (with REVIEW HIGH-3 throttle, HIGH-4 stale guard, LOW/MEDIUM-11 window, MEDIUM-12 framing):
   ```bash
   cd ~/queuenight
   firebase deploy --only functions
   ```
   Verify via:
   ```bash
   firebase functions:log --only watchpartyTick --limit 50
   ```

5. Step 4 — Deploy hosting (couch repo):
   ```bash
   cd ~/claude-projects/couch
   bash scripts/deploy.sh 35.0-tracking-layer
   ```
   Expected: BUILD_DATE auto-stamp + sw.js CACHE auto-bumped + mirror to queuenight/public/ + `firebase deploy --only hosting`. Visit https://couchtonight.app/app and confirm the page loads.

6. After Step 4 succeeds, replace the changelog placeholder:
   ```bash
   cd ~/claude-projects/couch
   sed -i 's/\[deploy date YYYY-MM-DD\] · Tracking layer/2026-04-27 · Tracking layer/' changelog.html
   bash scripts/deploy.sh 35.0-tracking-layer-changelog
   ```

7. Smoke tests:
   - V5 couch viz still renders (Phase 14 regression)
   - Open a TV title's detail modal with tuples; "YOUR COUCH'S PROGRESS" section renders between cast and reviews; placeholder shimmer fires correctly when no custom name (REVIEW MEDIUM-6); pencil glyph reveals input via delegated listener (REVIEW MEDIUM-7)
   - Settings → Notifications shows "New episode alerts" toggle (REVIEW MEDIUM-12 reworded label)
   - Trakt overlap: decline a candidate → re-trigger trakt.sync → candidate does NOT re-prompt (REVIEW MEDIUM-9)
   - Trakt overlap: with member A at S5E1 and member B at S4E10, candidate resolves to S5E1 (REVIEW MEDIUM-10 — NOT S5E10)
   - watchpartyTick CF runs at MOST hourly per family (REVIEW HIGH-3); titles with stale nextEpisode are skipped + Sentry breadcrumb (REVIEW HIGH-4)
   - Push body reads "New episode tonight" (REVIEW MEDIUM-12 framing)
   - Try to write t.mutedShows[someone-else.id] from devtools → DENIED (REVIEW HIGH-1)
   - Try to write t.liveReleaseFiredFor from devtools → DENIED (REVIEW HIGH-1)
   - Open changelog.html or visit /changelog; verify v35 article renders above v34

8. Reply `approved-deploy` to confirm successful end-to-end deploy.
  </how-to-verify>
  <resume-signal>Reply `approved-deploy` after verifying all smoke tests pass on couchtonight.app, OR describe specific failures. If any step fails, the deploy is INCOMPLETE and the corresponding TRACK-15-* requirement stays Pending.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → Firebase Hosting (production) | Step 4 deploy crosses here |
| Local repo → Firestore rules + indexes (production) | Steps 1+2 cross here; includes REVIEW HIGH-1 + MEDIUM-9 rule extensions |
| Local repo → Cloud Functions runtime (production) | Step 3 crosses here; includes REVIEW HIGH-3 / HIGH-4 / LOW-MEDIUM-11 / MEDIUM-12 patches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | Tampering | malformed firestore.rules deploy denies all writes | mitigate | Step 1 deploys ONLY rules; rollback by deploying prior commit. REVIEW HIGH-1 + MEDIUM-9 changes are ADDITIONS — Phase 1-14 paths preserved. |
| T-15-08-02 | DoS | composite index never completes | mitigate | Step 2 includes `firebase firestore:indexes` status verification. |
| T-15-08-03 | EoP | CF deploy succeeds but client triad missing | mitigate | Steps 3 + 4 bundled in same release window. |
| T-15-08-04 | Information disclosure | sw.js CACHE bump leaks version | accept | Already public. |
| T-15-08-05 | Tampering | deploy.sh corrupted | accept | Phase 13 OPS-13-02 audit trail. |
| T-15-08-06 | Repudiation | failed deploy partially applies | mitigate | Strict ordering — rules + index permit new field shapes BEFORE clients write. CF gates on NOTIFICATION_DEFAULTS. New rules are SUPERSET of pre-Phase-15. |
| T-15-08-07 | Tampering | REVIEW MEDIUM-9 allowlist extension forgotten | mitigate | Task 4 explicitly adds coWatchPromptDeclined to 15-01's 5th branch BEFORE Task 3 Step 1. Acceptance criteria verify the field is present. |
</threat_model>

<verification>
- `grep -c "TRACK-15-13" .planning/REQUIREMENTS.md` returns at least `2`
- `grep -c "TRACK-15-14" .planning/REQUIREMENTS.md` returns at least `2`
- `grep -c "couch-v35.0-tracking-layer" sw.js` returns `1`
- `grep -c '<span class="release-version">v35</span>' changelog.html` returns `1`
- **REVIEW MEDIUM-9 cross-plan:** `grep -c "'coWatchPromptDeclined'" ~/queuenight/firestore.rules` returns `1`
- **REVIEW LOW-narrative-fix:** `grep -c "13 TRACK-15-\\*" .planning/ROADMAP.md` returns `0` after Task 1.5
- After Task 3 deploy + date fill-in: `grep -c "\\[deploy date YYYY-MM-DD\\]" changelog.html` returns `0`
- Production smoke (after approved-deploy): https://couchtonight.app/app loads; sw CACHE shows couch-v35.0-tracking-layer
- All REVIEW patches verified in production smoke tests per Task 3 Step 7
</verification>

<success_criteria>
1. .planning/REQUIREMENTS.md contains 14 TRACK-15-* IDs (TRACK-15-01..14) with REVIEW patch attribution in each item.
2. Traceability table contains 14 new rows.
3. Coverage line updated to "v1 requirements: 102 total".
4. **REVIEW LOW-narrative-fix:** .planning/ROADMAP.md row 15 narrative says "14 TRACK-15-*" (was "13").
5. **REVIEW MEDIUM-9 cross-plan:** ~/queuenight/firestore.rules family-doc 5th branch allowlist includes 'coWatchPromptDeclined' (Task 4).
6. sw.js CACHE bumped from `couch-v34.1.3-selectedmembers-shim` to `couch-v35.0-tracking-layer`.
7. changelog.html contains a new v35 article with REVIEW MEDIUM-9 + MEDIUM-12 aligned copy.
8. User runs the 4-step cross-repo deploy ritual end-to-end and replies `approved-deploy`.
9. After deploy, the changelog `[deploy date YYYY-MM-DD]` placeholder is replaced.
10. https://couchtonight.app/app loads; sw CACHE matches couch-v35.0-tracking-layer.
11. All REVIEW patches verified in production smoke tests.
</success_criteria>

<output>
After completion (post `approved-deploy` signal), create
`.planning/phases/15-tracking-layer/15-08-SUMMARY.md` recording: (a) the actual
deploy date; (b) the queuenight commit SHA(s) — confirm both REVIEW HIGH-1 +
REVIEW MEDIUM-9 rules extensions landed in the deployed rules; (c) the couch
commit SHA(s); (d) confirmation that ROADMAP.md row 15 narrative is scrubbed
to "14 TRACK-15-*" (REVIEW LOW-narrative-fix); (e) any deploy deviations or
rollback events; (f) a 1-line summary that "Phase 15 Tracking Layer SHIPPED
2026-{deploy date} — couch-v35.0-tracking-layer live on couchtonight.app with
all REVIEW patches (HIGH-1 / HIGH-2 / HIGH-3 / HIGH-4 / MEDIUM-5..MEDIUM-12)
incorporated."

ALSO update `.planning/ROADMAP.md` row 15 from "0/8" to "8/8" with deploy date.

ALSO update `.planning/STATE.md` "Current Position" + "sw.js cache version" to
reflect the v35 release.

ALSO mark each TRACK-15-* requirement in REQUIREMENTS.md from `[ ]` to `[x]`
with `Complete` status.
</output>
</content>