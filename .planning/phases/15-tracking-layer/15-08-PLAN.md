---
phase: 15
plan: 08
type: execute
wave: 7
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05", "15-06", "15-07"]
files_modified:
  - ".planning/REQUIREMENTS.md"
  - "sw.js"
  - "changelog.html"
autonomous: false
requirements:
  - TRACK-15-13
user_setup:
  - service: firebase
    why: "Cross-repo deploy ritual — Phase 15 spans the couch repo (hosting + sw.js + changelog) AND the queuenight deploy-mirror (firestore:rules, firestore:indexes, functions). User authenticates via firebase login from each repo and runs the 4-step deploy in order."
    env_vars: []
    dashboard_config:
      - task: "Verify Phase 15 / D-02 5th UPDATE branch deploys to live rules; verify watchparties (titleId asc, startAt asc) composite index builds successfully"
        location: "Firebase Console → Firestore → Rules + Indexes — https://console.firebase.google.com/project/queuenight-84044"

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md contains a new ### Tracking Layer (Phase 15) section with TRACK-15-01 through TRACK-15-13 entries below the existing ### Decision Ritual Core (Phase 14) section"
    - "REQUIREMENTS.md Traceability table contains a row for each TRACK-15-* requirement mapped to its plan number"
    - "sw.js CACHE constant updated from couch-v34.1.3-selectedmembers-shim → couch-v35.0-tracking-layer"
    - "changelog.html contains a new <article class=\"release\"> block for v35 above the existing v34 article (newest-first ordering)"
    - "User runs the 4-step cross-repo deploy ritual: (1) firebase deploy --only firestore:rules from queuenight; (2) firebase deploy --only firestore:indexes from queuenight; (3) firebase deploy --only functions from queuenight; (4) bash scripts/deploy.sh 35.0-tracking-layer from couch repo"
    - "After deploy, the changelog.html v35 article's [deploy date YYYY-MM-DD] placeholder is replaced with the actual deploy date"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "13 new TRACK-15-* requirement IDs in a new ### Tracking Layer (Phase 15) section + Traceability table updates"
      contains: "TRACK-15-01"
    - path: "sw.js"
      provides: "CACHE bumped to couch-v35.0-tracking-layer"
      contains: "couch-v35.0-tracking-layer"
    - path: "changelog.html"
      provides: "New v35 release article above the v34 entry"
      contains: 'release-version">v35'
  key_links:
    - from: "sw.js CACHE bump"
      to: "Installed PWAs invalidating on next online activation"
      via: "Service Worker re-fetches /app + /css/app.css + /js/app.js when CACHE constant changes"
      pattern: "couch-v35"
    - from: "Cross-repo deploy ritual"
      to: "Production at couchtonight.app"
      via: "rules → indexes → CFs → app.html (mirror to queuenight/public) — strict ordering required per Phase 14 LEARNINGS"
      pattern: "couch-v35.0-tracking-layer"
---

<objective>
Close out Phase 15 — Tracking Layer. This plan ships the cross-cutting
artifacts that span every requirement in the phase: the requirement IDs minted
in REQUIREMENTS.md, the sw.js CACHE bump that invalidates installed PWAs, the
changelog.html v35 release article, and the human-verify checkpoint for the
4-step cross-repo deploy ritual.

Per RESEARCH §Cross-Repo Deploy Ordering + Phase 14 LEARNINGS "approved-deploy"
lesson, Phase 15 spans BOTH repos:
- `~/queuenight` ships: firestore.rules (15-01) + firestore.indexes.json (15-01)
  + tests/rules.test.js (15-01 — though the source is in couch and mirrored)
  + functions/index.js (15-06 sweep + 15-06 NOTIFICATION_DEFAULTS extension)
- `~/claude-projects/couch` ships: js/app.js (15-02..15-05, 15-07) + css/app.css
  (15-04) + app.html (15-05) + sw.js + changelog.html

The deploy ORDER is non-negotiable:
1. `cd ~/queuenight && firebase deploy --only firestore:rules --project queuenight-84044`
2. `cd ~/queuenight && firebase deploy --only firestore:indexes --project queuenight-84044`
   — wait for build to complete (1-5 minutes — observable via
   `firebase firestore:indexes`)
3. `cd ~/queuenight && firebase deploy --only functions`
   — CF cannot be deployed until the index from step 2 is live (otherwise
   suppression query throws FAILED_PRECONDITION)
4. `cd ~/claude-projects/couch && bash scripts/deploy.sh 35.0-tracking-layer`
   — auto-bumps sw.js CACHE → `couch-v35.0-tracking-layer` (this plan's
   Task 2 hand-bump is a defensive fallback in case the user runs deploy.sh
   without the short-tag arg); mirrors couch repo into queuenight/public/;
   runs `firebase deploy --only hosting`

Per Phase 14-06 deviation 1 + STATE.md "Two-repo discipline" rows, the user is
expected to run the queuenight commits + deploys from a queuenight-equipped
session. This plan's checkpoint pauses for the user to confirm
`approved-deploy` after running the ritual.

Per RESEARCH §Q5 + Risks #4, the index build can take 1-5 minutes — the deploy
ritual MUST wait for it to complete before deploying CFs. The
human-verify checkpoint task includes a verification step for the index status.

Per CONTEXT.md (no `### Tracking Layer (Phase 15)` REQ-IDs minted yet — this
plan mints them inline). 13 IDs corresponding to:
- TRACK-15-01: Firestore data foundation (rules + index + tests) → 15-01
- TRACK-15-02: Tuple read/write helpers + family-doc snapshot hydration → 15-02
- TRACK-15-03: D-01 watchparty-end auto-track stash → 15-03
- TRACK-15-04: S2 detail-modal "Your couch's progress" section → 15-04
- TRACK-15-05: S3 inline tuple rename → 15-04
- TRACK-15-06: S6 per-show kill-switch → 15-04
- TRACK-15-07: S1 Tonight tab "Pick up where you left off" widget → 15-05
- TRACK-15-08: CF live-release sweep (D-13/D-14/D-15/D-16) → 15-06
- TRACK-15-09: NOTIFICATION_DEFAULTS server triad place 1 of 3 → 15-06
- TRACK-15-10: DEFAULT_NOTIFICATION_PREFS triad place 2 of 3 → 15-07
- TRACK-15-11: NOTIFICATION_EVENT_LABELS triad place 3 of 3 → 15-07
- TRACK-15-12: Trakt episode-timestamp + co-watch overlap S5 prompt → 15-07
- TRACK-15-13: sw.js CACHE bump + changelog v35 + cross-repo deploy ritual → 15-08 (this plan)

Output: 3 file edits + 1 human-verify checkpoint with deploy-script invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\nahde\claude-projects\couch\CLAUDE.md
@C:\Users\nahde\claude-projects\couch\.planning\PROJECT.md
@C:\Users\nahde\claude-projects\couch\.planning\STATE.md
@C:\Users\nahde\claude-projects\couch\.planning\REQUIREMENTS.md
@C:\Users\nahde\claude-projects\couch\.planning\ROADMAP.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-CONTEXT.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-RESEARCH.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-PATTERNS.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-UI-SPEC.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-01-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-02-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-03-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-04-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-05-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-06-SUMMARY.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\15-tracking-layer\15-07-SUMMARY.md

<interfaces>
<!-- Existing Phase 14 REQUIREMENTS.md section (verified at lines 134-150) — clone-as-sibling source: -->
```markdown
### Decision Ritual Core (Phase 14)

Scoped via /gsd-discuss-phase 14 (2026-04-25) — 12 user decisions locked
(D-01..D-12) + 3 reconciled DRs ... 13 DECI-* requirements minted at
/gsd-plan-phase 14 (2026-04-25).

- [x] **DECI-14-01**: Strict member-aware "already-watched" filter ...
- [x] **DECI-14-02**: 3-tier candidate filter ...
...
- [x] **DECI-14-13**: sw.js CACHE constant bumped to couch-v34.0-decision-ritual ...
```

<!-- Existing Traceability table at REQUIREMENTS.md:185-282: -->
```markdown
| Requirement | Phase | Status |
|-------------|-------|--------|
| ... |
| DECI-14-13 | Phase 14 Plan 09 | Complete — DEPLOY-DEFERRED ... |
```

<!-- Existing sw.js (verified, 109 lines): -->
```javascript
// sw.js:8
const CACHE = 'couch-v34.1.3-selectedmembers-shim';
```

<!-- Existing changelog.html v34 article (verified at lines 67-79): -->
```html
<article class="release">
  <header class="release-h">
    <span class="release-version">v34</span>
    <span class="release-date">[deploy date YYYY-MM-DD] · Decision ritual</span>
  </header>
  <p class="release-summary">A new way to pick what to watch &mdash; together.</p>
  <ul class="release-list">
    <li><strong>The couch:</strong> tap a cushion to seat yourself. See who's in.</li>
    <li><strong>Two flows:</strong> rank-pick when you're together, or nominate-and-invite when you're apart.</li>
    <li><strong>Tile redesign:</strong> see who wants what at a glance. Tap the tile for what to do next.</li>
    <li><strong>Your queue:</strong> drag to reorder. Voting Yes adds to the bottom &mdash; we'll show you.</li>
  </ul>
</article>
```

<!-- Existing scripts/deploy.sh from Phase 13 (CLAUDE.md and OPS-13-02): -->
```
bash scripts/deploy.sh <short-tag>   # auto-bumps sw.js CACHE if short-tag passed; mirrors to queuenight/public; runs firebase deploy --only hosting
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint TRACK-15-* requirement IDs in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 130-155 (the Phase 14 ### Decision Ritual Core section — clone-as-sibling source for the new ### Tracking Layer section)
    - .planning/REQUIREMENTS.md lines 270-285 (the Traceability table tail — DECI-14-* rows are the analog format)
    - 15-CONTEXT.md (the 16 D-XX decisions that map to TRACK-15-* requirements)
    - 15-PATTERNS.md (the per-plan TRACK-15-* mapping in §Plan slot suggested column)
  </read_first>
  <action>
Open `.planning/REQUIREMENTS.md`. Find the line `### Decision Ritual Core (Phase 14)`
(verified at line 134) and read forward to find where it closes (the `### v2
Requirements` section header at line 152).

INSERT a new section IMMEDIATELY AFTER the closing of the Phase 14 section
(i.e., after the last DECI-14-* bullet at line 150 and BEFORE `## v2 Requirements`
at line 152).

The block to insert:

```markdown

### Tracking Layer (Phase 15)

Scoped via /gsd-discuss-phase 15 (2026-04-26) — 16 user decisions locked
(D-01..D-16). Per-watching-group watch progress (tracks GROUPS, not just
individuals — Couch's primary differentiator vs Trakt) + new-season air-date
push notifications + live-release watchparty auto-prompts. Builds on Phase 14's
intents collection + 7-key push framework (extends to 8 keys). 13 TRACK-15-*
requirements minted at /gsd-plan-phase 15 (2026-04-26).

- [ ] **TRACK-15-01**: Firestore data foundation — 5th UPDATE branch on `/families/{familyCode}` permitting attributedWrite of `tupleNames` map (D-02), composite index on `watchparties (titleId asc, startAt asc)` for D-16 suppression query, and 6 sibling tests (#23-#28) in tests/rules.test.js covering happy/non-attributed/non-allowlisted-field/stranger + tupleProgress/mutedShows regression. Title-doc rules at firestore.rules:317-319 are NOT modified — `t.tupleProgress` and `t.mutedShows` accepted under existing permissive branch. (D-02 / D-16)
- [ ] **TRACK-15-02**: Tuple read/write helpers in js/app.js — `tupleKey(memberIds)` (sorted-comma-join, idempotent), `tupleProgressFromTitle(t)`, `tuplesContainingMember(t, memberId)`, `tupleDisplayName(tk, members)` (with derived "Wife and me" / "You (solo)" / "You + 2" fallback), `writeTupleProgress(titleId, memberIds, season, episode, source)`, `clearTupleProgress`, `setTupleName(tupleKeyStr, name)` (dotted-path family-doc write), `writeMutedShow(titleId, memberId, muted)`. State.family.tupleNames hydrated from family-doc onSnapshot. Per RESEARCH §Foundational Finding A5, coexists with existing per-individual t.progress[memberId] for v1. (D-02 / D-03 / D-08)
- [ ] **TRACK-15-03**: D-01 watchparty-end auto-track stash — extends `openPostSession` at js/app.js:10438 to compute `state._pendingTupleAutoTrack = { titleId, memberIds, season, episode, sourceWpId }` from wp.participants (members with startedAt) + inferred episode (host's progress + 1) when t.kind === 'TV'. Plus `window.toggleMutedShow(titleId)` click handler that flips `t.mutedShows[me.id]` via writeMutedShow. NO silent auto-write — 15-04's UI confirmation step closes the loop per D-06. (D-01)
- [ ] **TRACK-15-04**: S2 detail-modal "YOUR COUCH'S PROGRESS" section — `renderCv15TupleProgressSection(t)` between renderReviewsForTitle and renderTmdbReviewsForTitle in renderDetailShell. Rows sorted watchedAt desc, max 4 visible per UI-SPEC §Cross-tuple visual handling, "View all (N)" affordance for >4. Hides entirely when no TV / no tuples / watched. (D-04 / Q2)
- [ ] **TRACK-15-05**: S3 inline tuple rename — pencil glyph (`✎`) reveals `<input maxlength="40">` overlay on tap; Enter saves via setTupleName, Esc reverts, blur saves; flashToast "Saved" on success / "Couldn't save name — try again" on error per UI-SPEC §Copywriting Contract. Decoration only (D-02 — name doesn't change tuple identity). (D-02 / Q3)
- [ ] **TRACK-15-06**: S6 per-show kill-switch — `renderCv15MutedShowToggle(t)` text-link at bottom of S2 section. Off state: "Stop notifying me about this show" in --ink-warm; on state: "Notifications off · Re-enable" in --bad. NO confirmation modal (recoverable affordance). (D-12 / Q5)
- [ ] **TRACK-15-07**: S1 Tonight tab "PICK UP WHERE YOU LEFT OFF" widget — `<div id="cv15-pickup-container">` between #couch-viz-container and #flow-a-entry-container in app.html. `renderPickupWidget()` cross-show roll-up filtering tuples-containing-me, sorted by most-recent-tuple updatedAt desc, max 3 visible. Hides entirely on zero tuples per UI-SPEC §Discretion Q7. Coexists with legacy #continue-section. Hooked into renderTonight() after renderFlowAEntry. (D-04 / Q1 / Q7)
- [ ] **TRACK-15-08**: CF live-release sweep in queuenight/functions/index.js watchpartyTick — per-titles loop after intents loop, before outer-family-loop close. Subscriber set: union of comma-split tuple keys minus members in t.mutedShows. Gates: D-14 ≥2 trackers + D-13 24h-before window (T-25h to T-23h relative to airTs from `airDate + 'T21:00:00'`). D-16 suppression: existing watchparty for this title within ±90min marks epKey 'suppressed_existing_wp'. Idempotency flag t.liveReleaseFiredFor[`s{N}e{M}`] set BEFORE push (flag-then-fire). Push body day-only ("airs Friday" — RESEARCH §Q6 v1 fallback, no time-of-day from TMDB). (D-11 / D-13 / D-14 / D-15 / D-16)
- [ ] **TRACK-15-09**: Server NOTIFICATION_DEFAULTS triad place 1 of 3 — adds `newSeasonAirDate: true` 8th key to queuenight/functions/index.js:74-96. Lockstep contract with client DEFAULT_NOTIFICATION_PREFS (TRACK-15-10). Per Phase 14-09 DR-3 follow-up override, NOT mirrored to friendly-UI maps. (D-09 / D-12 / DR-3)
- [ ] **TRACK-15-10**: Client DEFAULT_NOTIFICATION_PREFS triad place 2 of 3 — adds `newSeasonAirDate: true` 8th key to js/app.js:100-121. Default-on per D-09 (auto-subscribe on watch). (D-09 / D-12 / DR-3)
- [ ] **TRACK-15-11**: Client NOTIFICATION_EVENT_LABELS triad place 3 of 3 — adds `newSeasonAirDate: { label: "New season air dates", hint: "When a tracked show's next season hits a streamer." }` 8th entry to js/app.js:131-148 with verbatim BRAND-voice copy from UI-SPEC §Copywriting Contract. (D-12 / DR-3)
- [ ] **TRACK-15-12**: Trakt co-watch overlap detection + S5 prompt — extends trakt.ingestSyncData seasons.episodes loop with parallel `lastWatchedAt = ep.last_watched_at` capture; persists to t.progress[meId].lastWatchedAt. After every trakt.sync, `trakt.detectAndPromptCoWatchOverlap` walks state.titles finding member pairs with `source === 'trakt'` AND lastWatchedAt within ±3hr (RESEARCH §Q10 — tunable post-launch). Filters out pairs already in t.tupleProgress (we don't ask twice). Queues prompts in state._coWatchPromptQueue; manager surfaces ONE modal at a time. S5 modal uses .modal-bg shell + verbatim UI-SPEC §Copywriting copy: heading "Watched together?" + italic Instrument Serif body + "Yes, group us" / "No, keep separate" with No defaulted-focus. Outside-tap = No. On Yes: writeTupleProgress(titleId, [meId, otherId], season, episode, 'trakt-overlap'). Defaults to No per D-06. (D-05 / D-06)
- [ ] **TRACK-15-13**: sw.js CACHE constant bumped to `couch-v35.0-tracking-layer` and deployed via the cross-repo 4-step ritual (rules → indexes → CFs → app). REQUIREMENTS.md mint + Traceability table updates + changelog.html v35 release article + scripts/deploy.sh invocation. Cross-cutting requirement covering all user-visible Phase 15 changes. Bump and deploy at the end of phase, NOT per-plan, to avoid CACHE thrash. (D-11 / D-13 / cross-cutting)
```

THEN locate the Traceability table at line 185-282. Find the last DECI-14-13
row (verified at line 273). INSERT 13 new rows IMMEDIATELY AFTER it (each
TRACK-15-* row mapping to its plan):

```markdown
| TRACK-15-01 | Phase 15 Plan 01 | Pending |
| TRACK-15-02 | Phase 15 Plan 02 | Pending |
| TRACK-15-03 | Phase 15 Plan 03 | Pending |
| TRACK-15-04 | Phase 15 Plan 04 | Pending |
| TRACK-15-05 | Phase 15 Plan 04 | Pending |
| TRACK-15-06 | Phase 15 Plan 04 | Pending |
| TRACK-15-07 | Phase 15 Plan 05 | Pending |
| TRACK-15-08 | Phase 15 Plan 06 | Pending |
| TRACK-15-09 | Phase 15 Plan 06 | Pending |
| TRACK-15-10 | Phase 15 Plan 07 | Pending |
| TRACK-15-11 | Phase 15 Plan 07 | Pending |
| TRACK-15-12 | Phase 15 Plan 07 | Pending |
| TRACK-15-13 | Phase 15 Plan 08 | Pending |
```

Update the Coverage line near the end of the file (currently "v1 requirements:
88 total"):

```markdown
- v1 requirements: 101 total (41 complete across Phases 3+4+7+9+11+14; 60 pending across Phases 5/6/8/12/13/14/15 + deferred Phase 10)
```

(88 + 13 = 101; 47 + 13 = 60.)

Update the "Last updated" line at end-of-file with a 2026-04-26 entry:

```markdown
*Last updated: 2026-04-26 — Phase 15 (Tracking Layer) added with 13 TRACK-15-* items minted at /gsd-plan-phase 15 (2026-04-26 — 16 D-XX decisions locked across CONTEXT.md). Per RESEARCH §Foundational Finding, this generalizes the existing per-individual t.progress[memberId] primitive to tuple-keyed t.tupleProgress[tupleKey] — additive, both shapes coexist for v1. Cross-repo deploy ordering (rules → indexes → CFs → app) encoded in 15-08 close-out plan. {existing Last-updated content preserved}*
```

(Don't truncate the existing Last-updated content — append, don't replace.)

Do NOT modify any prior phase's section. Do NOT renumber existing rows.
  </action>
  <verify>
    <automated>cd ~/claude-projects/couch &amp;&amp; grep -c "### Tracking Layer (Phase 15)" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; for n in 01 02 03 04 05 06 07 08 09 10 11 12 13; do grep -q "TRACK-15-$n" .planning/REQUIREMENTS.md || (echo "missing TRACK-15-$n"; exit 1); done &amp;&amp; grep -c "TRACK-15-13 | Phase 15 Plan 08" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; grep -c "v1 requirements: 101 total" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Tracking Layer (Phase 15)" .planning/REQUIREMENTS.md` returns `1`
    - All 13 TRACK-15-* IDs present: `for n in 01 02 03 04 05 06 07 08 09 10 11 12 13; do grep -q "TRACK-15-$n" .planning/REQUIREMENTS.md; done` succeeds
    - The new section sits AFTER ### Decision Ritual Core (Phase 14) — verify via `grep -B1 "### Tracking Layer (Phase 15)" .planning/REQUIREMENTS.md` showing the prior context closes the Phase 14 block
    - The new section sits BEFORE `## v2 Requirements` — verify via `grep -A30 "TRACK-15-13" .planning/REQUIREMENTS.md | grep -q "## v2 Requirements"`
    - Traceability table contains 13 new TRACK-15-* rows — verify via `grep -c "| TRACK-15-" .planning/REQUIREMENTS.md` returns `13`
    - Coverage line updated to "v1 requirements: 101 total" — verify via `grep -c "v1 requirements: 101 total" .planning/REQUIREMENTS.md` returns `1`
    - Last-updated line includes a 2026-04-26 Phase 15 entry — verify via `grep -c "Phase 15 (Tracking Layer) added" .planning/REQUIREMENTS.md` returns `1`
    - All prior DECI-14-* + REFR-* + COMP-13-* + OPS-13-* + POL-* + DESIGN-* + AUTH-* + INTENT-* + PARTY-* + PUSH-* + VETO-* + MOOD-* + YEAR-* rows are unmodified
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains a new ### Tracking Layer (Phase 15) section with
    13 TRACK-15-* IDs + 13 Traceability table rows + Coverage line bumped to
    101 total. All prior content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump sw.js CACHE constant + add v35 changelog.html article</name>
  <files>sw.js, changelog.html</files>
  <read_first>
    - sw.js (full read — only 109 lines per CLAUDE.md "tiny, read freely")
    - changelog.html (full read — only 149 lines)
    - 15-PATTERNS.md §F19 (sw.js CACHE bump) + §F20 (changelog v35 article)
    - 15-UI-SPEC.md §Surface inventory (the 6 surfaces + the deferred-to-plan-phase tagline)
  </read_first>
  <action>
**Part A — sw.js CACHE bump:**

Open `sw.js`. Find the line `const CACHE = 'couch-v34.1.3-selectedmembers-shim';`
at line 8 (verified). REPLACE with:

```javascript
const CACHE = 'couch-v35.0-tracking-layer';
```

Do NOT modify any other line in sw.js. The version naming convention from
CLAUDE.md is `couch-v{N}-{milestone-or-fix-shorthand}` — this matches.

NOTE: per CLAUDE.md OPS-13-02, the deploy.sh script auto-bumps the CACHE if you
pass a short-tag arg like `bash scripts/deploy.sh 35.0-tracking-layer`. This
hand-bump is a defensive fallback so the CACHE is correct EVEN IF the user
forgets the arg. If deploy.sh runs with the arg, it will overwrite this with
the same value (idempotent — no harm).

**Part B — changelog.html v35 article:**

Open `changelog.html`. Find `<article class="release">` at line 67 (the v34
article — the most recent prior release). The new v35 article goes
IMMEDIATELY ABOVE it (newest-first ordering).

INSERT this block between the closing `</section>` of the previous element
(line 65 `<section class="changelog-hero">` close) and the opening
`<article class="release">` of v34:

```html
      <article class="release">
        <header class="release-h">
          <span class="release-version">v35</span>
          <span class="release-date">[deploy date YYYY-MM-DD] · Tracking layer</span>
        </header>
        <p class="release-summary"><em>See where every couch is in every show &mdash; together or apart.</em></p>
        <ul class="release-list">
          <li><strong>Pick up where you left off:</strong> tonight tab now surfaces what every couch is mid-watch.</li>
          <li><strong>Group your watches:</strong> auto-track who's on the couch when a watchparty ends. Name the couch ("Date night") if you want.</li>
          <li><strong>New season alerts:</strong> tracked shows ping the couch a day before a fresh episode drops.</li>
          <li><strong>Trakt overlap nudge:</strong> when two of you watch the same episode around the same time, we'll ask if you're a couch.</li>
        </ul>
      </article>
```

Match the existing indentation (typically 6 spaces inside the changelog body
container).

The placeholder `[deploy date YYYY-MM-DD]` is replaced with the actual date in
Task 3 below (after the deploy completes).

Do NOT modify the v34 article or any prior article. Do NOT modify the
`<section class="changelog-hero">` block above.

Verify both files:
- `grep -c "couch-v35.0-tracking-layer" sw.js` returns `1`
- `grep -c '<span class="release-version">v35</span>' changelog.html` returns `1`
- `grep -c "Tracking layer" changelog.html` returns `1` (verify the date-line tagline)
  </action>
  <verify>
    <automated>cd ~/claude-projects/couch &amp;&amp; grep -c "couch-v35.0-tracking-layer" sw.js | grep -q "^1$" &amp;&amp; grep -c "couch-v34.1.3-selectedmembers-shim" sw.js | grep -q "^0$" &amp;&amp; grep -c '&lt;span class="release-version"&gt;v35&lt;/span&gt;' changelog.html | grep -q "^1$" &amp;&amp; grep -c '&lt;span class="release-version"&gt;v34&lt;/span&gt;' changelog.html | grep -q "^1$" &amp;&amp; grep -c "Pick up where you left off:" changelog.html | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "couch-v35.0-tracking-layer" sw.js` returns `1`
    - `grep -c "couch-v34.1.3-selectedmembers-shim" sw.js` returns `0` (old version is gone — single CACHE constant overwritten)
    - sw.js line 8 contains the new CACHE value — `sed -n '8p' sw.js` (or grep `^const CACHE`) shows `couch-v35.0-tracking-layer`
    - `grep -c '<span class="release-version">v35</span>' changelog.html` returns `1`
    - `grep -c '<span class="release-version">v34</span>' changelog.html` returns `1` (existing v34 article still present)
    - The v35 article sits ABOVE the v34 article — verify via `grep -B30 'release-version">v34<' changelog.html | grep -q 'release-version">v35<'`
    - `grep -c "Pick up where you left off:" changelog.html` returns `1`
    - `grep -c "Group your watches:" changelog.html` returns `1`
    - `grep -c "New season alerts:" changelog.html` returns `1`
    - `grep -c "Trakt overlap nudge:" changelog.html` returns `1`
    - `grep -c "Tracking layer" changelog.html` returns `1` (the release-date line tagline)
    - `grep -c "\\[deploy date YYYY-MM-DD\\]" changelog.html` returns `1` (placeholder pending Task 3 deploy)
  </acceptance_criteria>
  <done>
    sw.js CACHE bumped to couch-v35.0-tracking-layer. changelog.html contains
    a new v35 release article above v34 with verbatim Phase 15 tagline +
    4 bullets matching UI-SPEC §Copywriting voice. Old CACHE removed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User runs cross-repo deploy ritual + replaces changelog [deploy date YYYY-MM-DD] placeholder</name>
  <what-built>
    Phase 15 — Tracking Layer code is complete across both repos:
    - couch repo: js/app.js (5 plans worth of edits) + css/app.css (Phase 15 namespace block from 15-04) + app.html (cv15-pickup-container from 15-05) + sw.js CACHE bumped to couch-v35.0-tracking-layer + changelog.html v35 article
    - queuenight repo: firestore.rules (5th UPDATE branch from 15-01) + firestore.indexes.json (composite index from 15-01) + tests/rules.test.js (6 new tests from 15-01 — actually mirrored from couch repo) + functions/index.js (NOTIFICATION_DEFAULTS extension + watchpartyTick sweep from 15-06)

    The 4-step cross-repo deploy ritual is the gate that takes Phase 15 from code-complete to live on couchtonight.app.
  </what-built>
  <how-to-verify>
1. Verify queuenight repo edits are committed locally:
   ```bash
   cd ~/queuenight
   git status               # should show modified firestore.rules, firestore.indexes.json, functions/index.js
   git diff --stat          # confirm scope: 5th UPDATE branch + composite index + watchpartyTick extension + NOTIFICATION_DEFAULTS 8th key
   git add firestore.rules firestore.indexes.json functions/index.js
   git commit -m "feat(15): tracking layer — rules + index + CF sweep + push category"
   ```
   (per Phase 14 deviation pattern, queuenight is typically uncommitted on the dev machine until the user commits from a queuenight-equipped session)

2. Step 1 — Deploy rules:
   ```bash
   cd ~/queuenight
   firebase deploy --only firestore:rules --project queuenight-84044
   ```
   Expected: "✔  firestore: released rules ... to cloud.firestore" — without this, the next steps' writes from Phase 15 PWAs would fail with permission_denied.

3. Step 2 — Deploy composite index:
   ```bash
   cd ~/queuenight
   firebase deploy --only firestore:indexes --project queuenight-84044
   firebase firestore:indexes               # observe build status
   ```
   Expected: index build COMPLETES (not "Building..."). This may take 1-5 minutes — RESEARCH §Risks #4. WAIT for the index to be live before proceeding to Step 3, otherwise the CF suppression query throws FAILED_PRECONDITION.

4. Step 3 — Deploy CFs:
   ```bash
   cd ~/queuenight
   firebase deploy --only functions
   ```
   Expected: "Function watchpartyTick ... successful update operation". Verify the new newSeasonAirDate event-type is recognized by NOTIFICATION_DEFAULTS via:
   ```bash
   firebase functions:log --only watchpartyTick --limit 50
   ```
   (no errors related to "newSeasonAirDate" or missing index).

5. Step 4 — Deploy hosting (couch repo):
   ```bash
   cd ~/claude-projects/couch
   bash scripts/deploy.sh 35.0-tracking-layer
   ```
   Expected: BUILD_DATE auto-stamp + sw.js CACHE auto-bumped (idempotent — should match the hand-bump from Task 2 above) + Sentry DSN guard + mirror to queuenight/public/ + `firebase deploy --only hosting`. Visit https://couchtonight.app/app and confirm the page loads (no JS errors in console; service worker CACHE shows `couch-v35.0-tracking-layer` in DevTools → Application → Service Workers).

6. After Step 4 succeeds, replace the changelog placeholder with the actual deploy date:
   ```bash
   cd ~/claude-projects/couch
   # Replace [deploy date YYYY-MM-DD] with today's date in changelog.html
   # Example for 2026-04-27:
   sed -i 's/\[deploy date YYYY-MM-DD\] · Tracking layer/2026-04-27 · Tracking layer/' changelog.html
   bash scripts/deploy.sh 35.0-tracking-layer-changelog   # re-deploy with the date filled in
   ```
   (sed pattern matches the verbatim placeholder set in Task 2.)

7. Smoke tests:
   - Open https://couchtonight.app/app in incognito; verify the V5 couch viz still renders correctly (Phase 14 regression check)
   - Open a TV title's detail modal; if any tuple progress exists for the family, verify "YOUR COUCH'S PROGRESS" section renders between cast and reviews
   - Open Settings → Notifications; verify "New season air dates" toggle appears (default-on)
   - Open changelog.html or visit /changelog; verify v35 article renders above v34 with the actual deploy date

8. Reply with `approved-deploy` to confirm successful end-to-end deploy.
  </how-to-verify>
  <resume-signal>Reply `approved-deploy` after verifying all 7 smoke tests pass on couchtonight.app, OR describe specific failures (push didn't fire / detail-modal section missing / index build stuck / etc.). If any step fails, the deploy is INCOMPLETE and the corresponding TRACK-15-* requirement stays Pending.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → Firebase Hosting (production) | Step 4 deploy crosses here; same boundary as every prior phase |
| Local repo → Firestore rules + indexes (production) | Steps 1+2 deploy cross here; same boundary as Phase 14-04 / 14-10 firestore:rules deploys |
| Local repo → Cloud Functions runtime (production) | Step 3 deploy crosses here; same boundary as Phase 14-09 functions deploy |
| User-supplied deploy.sh script | trusted; sourced from the repo per Phase 13 OPS-13-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | Tampering | malformed firestore.rules deploy denies all writes (production outage) | mitigate | Step 1 deploys ONLY rules — `firebase deploy --only firestore:rules` is idempotent. If it fails or causes regressions, `firebase deploy --only firestore:rules` from a prior commit rolls back (existing Phase 14-10 4th branch + couchSeating 3rd branch are preserved untouched). |
| T-15-08-02 | DoS | composite index build never completes; CF suppression query never works | mitigate | Step 2 includes `firebase firestore:indexes` status verification. The user is instructed to WAIT for build completion before Step 3. If the index gets stuck >10 minutes, the user can re-deploy or contact Firebase support — the deploy ritual halts until index is live. |
| T-15-08-03 | EoP | CF deploy succeeds but `newSeasonAirDate` push fan-out fails silently due to missing client triad | mitigate | Steps 3 + 4 are bundled — both ship in the same release window. If user runs Step 3 but skips Step 4, the CF still gates correctly via NOTIFICATION_DEFAULTS (no push delivered to clients without the prefs key); recovery is just running Step 4. |
| T-15-08-04 | Information disclosure | sw.js CACHE bump leaks internal version → no impact (already public) | accept | Same posture as every prior CACHE bump; existing CSP + service worker are unchanged |
| T-15-08-05 | Tampering | deploy.sh corrupted to inject malicious code | accept | Phase 13 OPS-13-02 audit-trail in place; user reviews `git diff scripts/deploy.sh` before invocation if the script changed (it has not in this phase) |
| T-15-08-06 | Repudiation | failed deploy partially applies (rules live but app not) | mitigate | The 4-step ordering MITIGATES this — rules + index permit the new field shapes BEFORE clients write them; CF gates on NOTIFICATION_DEFAULTS independently. If Step 4 fails, the live state is "rules + indexes + CFs deployed but old client code" — the new client code is the ONLY consumer of t.tupleProgress so no backwards-incompatibility; the new rules are a SUPERSET (5 branches vs 4) so old client code keeps working. |
</threat_model>

<verification>
- `grep -c "TRACK-15-13" .planning/REQUIREMENTS.md` returns at least `2` (one in the section + one in the Traceability table)
- `grep -c "couch-v35.0-tracking-layer" sw.js` returns `1`
- `grep -c '<span class="release-version">v35</span>' changelog.html` returns `1`
- After Task 3 (deploy + date fill-in): `grep -c "\\[deploy date YYYY-MM-DD\\]" changelog.html` returns `0` (placeholder replaced)
- Production smoke (after Task 3 approved-deploy): https://couchtonight.app/app loads without JS errors; sw CACHE shows couch-v35.0-tracking-layer in DevTools
- No JS errors in browser console matching pattern `cv15|tupleKey|tupleProgress|newSeasonAirDate` after a fresh load
- Phase 14 regression: V5 couch viz still renders; Flow A entry still works; Flow B nominate still works
</verification>

<success_criteria>
1. .planning/REQUIREMENTS.md contains a new ### Tracking Layer (Phase 15) section with 13 TRACK-15-* IDs (TRACK-15-01 through TRACK-15-13).
2. Traceability table contains 13 new TRACK-15-* rows mapped to their plans (15-01..15-08; some plans cover multiple requirements like 15-04 covering TRACK-15-04..06 and 15-07 covering TRACK-15-10..12).
3. Coverage line updated to "v1 requirements: 101 total".
4. sw.js CACHE bumped from `couch-v34.1.3-selectedmembers-shim` to `couch-v35.0-tracking-layer`.
5. changelog.html contains a new v35 release article above v34 with the Tracking layer tagline + 4 BRAND-voice bullets covering S1/S2/D-11/D-06.
6. User runs the 4-step cross-repo deploy ritual end-to-end (rules → indexes → CFs → app) and replies `approved-deploy`.
7. After deploy, the changelog `[deploy date YYYY-MM-DD]` placeholder is replaced with the actual date.
8. https://couchtonight.app/app loads without JS errors; sw CACHE matches couch-v35.0-tracking-layer.
9. Smoke tests pass: detail-modal section renders for TV titles with tuple progress; Settings notifications shows "New season air dates" toggle; Phase 14 regressions clear.
</success_criteria>

<output>
After completion (post `approved-deploy` signal), create
`.planning/phases/15-tracking-layer/15-08-SUMMARY.md` recording: (a) the actual
deploy date that replaced the changelog placeholder; (b) the queuenight commit
SHA(s) that landed (firestore.rules + indexes + functions/index.js); (c) the
couch commit SHA(s) for sw.js + changelog.html + REQUIREMENTS.md; (d) any deploy
deviations or rollback events; (e) a 1-line summary that "Phase 15 Tracking
Layer SHIPPED 2026-{deploy date} — couch-v35.0-tracking-layer live on
couchtonight.app".

ALSO update `.planning/ROADMAP.md` row 15 from "0/?" to "8/8" status with the
deploy date and shipped attribution.

ALSO update `.planning/STATE.md` "Current Position" + "sw.js cache version"
to reflect the v35 release.

ALSO mark each TRACK-15-* requirement in REQUIREMENTS.md from `[ ] **TRACK-15-*`
to `[x] **TRACK-15-*` with the matching `Complete` status in the Traceability
table.
</output>
</content>
</invoke>