---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.html
  - css/app.css
  - js/app.js
  - sw.js
autonomous: false
requirements: [REFR-11, REFR-12]
requirements_addressed: [REFR-11, REFR-12]
tags: [tab-restructure, family-tab, account-tab, regrouping, html-reorder]
commit_strategy: 2 atomic commits — (1) REFR-11 Family tab 6→5 sections, (2) REFR-12 Account tab 9→3 cognitive clusters + sw.js CACHE bump bundled with commit 2
must_haves:
  truths:
    - "Family tab renders 5 sections in order: Tonight status / Approvals / Members (split active vs sub-profiles) / Couch history (consolidated F5+F6) / Group settings footer"
    - "Family tab F4 picker section is GONE entirely (HTML removed) — consequence of REFR-02; backend rotation still writes (matches Plan 11-01 Task 2)"
    - "Account tab renders 3 cognitive cluster eyebrows: 'YOU' / 'YOUR COUCH' / 'ADMIN' — using existing .tab-section-h pattern"
    - "Account tab YIR card hidden by default until state.family.yirReady === true (Phase 10 not yet shipped)"
    - "Pure reorganization — no feature removed, no data path changed, every existing renderer (renderApprovalsCard, renderMembersList, renderStats, renderFamilyFavorites, renderServicesPicker, renderTraktCard, renderOwnerSettings) still runs against the same DOM IDs"
    - "PWA cache version bumped from couch-v22-11-01-ux-tightening (or whichever the current value is post-Plan-11-01) to couch-v23-11-02-tab-restructure"
  artifacts:
    - path: "app.html"
      provides: "Reordered #screen-family (5 sections, picker block REMOVED) + reorganized #screen-settings (existing sections wrapped in 3 cluster groups with .tab-section-h eyebrow dividers)"
      contains: "data-cluster=\"you\""
    - path: "css/app.css"
      provides: "Optional small new rules for cluster-divider gap (cluster-to-cluster vs within-cluster spacing); reuse existing .tab-section-h"
      contains: ".tab-cluster-gap"
    - path: "js/app.js"
      provides: "Optional new render of Tonight-status block in Family tab (#family-tonight-status); YIR conditional un-hide guarded on state.family.yirReady"
      contains: "yirReady"
    - path: "sw.js"
      provides: "CACHE bumped to 'couch-v23-11-02-tab-restructure'"
      contains: "couch-v23-11-02"
  key_links:
    - from: "app.html #screen-family section order"
      to: "js/app.js renderTonight + renderApprovalsCard + renderMembersList + renderStats + renderFamilyFavorites"
      via: "DOM IDs preserved (renderers find their containers by ID, order-independent)"
      pattern: "id=\"family-"
    - from: "app.html #screen-settings 3-cluster wrappers"
      to: "js/app.js renderSettings + renderServicesPicker + renderTraktCard + renderOwnerSettings"
      via: "DOM IDs preserved within new cluster wrappers"
      pattern: "id=\"settings-"
---

<objective>
Restructure Family tab from 6 → 5 sections (CUT picker per REFR-02 consequence; SPLIT members into active vs sub-profiles; CONSOLIDATE F5 family-favorites + F6 per-member-breakdown into single "Couch history" section; REFRAME F7 invite into "Group settings footer"). Restructure Account tab from 9 flat sections → 3 cognitive clusters (YOU / YOUR COUCH / ADMIN) using existing .tab-section-h eyebrow pattern. PURE reorganization — zero feature removal, zero data path changes. Every existing renderer keeps running against the same DOM IDs.

Purpose: Cognitive cleanup. Match the "declutter" pillar of Phase 11. Owner-only group admin gets sub-grouped (Security / Members / Lifecycle) so it stops being one wall of dense settings.

Output: Cleaner Family + Account tab IA. ADDs (Tonight status block as full content, calendar, recent watchparties drilldown, notification prefs detail, data export, theme prefs, about/version) deferred to Phase 12 per CONTEXT.md decision 5 option (b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BRAND.md
@.planning/phases/11-feature-refresh-and-streamline/11-CONTEXT.md
@.planning/phases/11-feature-refresh-and-streamline/11-UI-SPEC.md
@.planning/phases/11-feature-refresh-and-streamline/11-PATTERNS.md
@.planning/phases/11-feature-refresh-and-streamline/11-APPENDIX-TABS-AUDIT.md
@CLAUDE.md

<interfaces>
<!-- Existing DOM contract — extracted from app.html via Read in chunks. Renderers find containers by ID. -->

From app.html lines 437-505 (#screen-family current section structure):
- F1 hero+stats — `<section class="tab-hero">...</section>` + `#family-hero-stats`
- F2 approvals — `<div id="approvals-card">` (rendered by renderApprovalsCard)
- F3 members — `<div id="members-list">` (rendered by renderMembersList)
- F4 picker — `<section><h2 id="picker-heading">Who picks tonight</h2><div id="picker-strip">...</div></section>` (REMOVE entirely)
- F5 family favorites — `<section><div id="family-favorites">...</div></section>`
- F6 per-member breakdown — `<section><div id="per-member-stats">...</div></section>`
- F7 share section — `<section><div class="share-section">...</div></section>` or similar

From app.html lines 507-652 (#screen-settings current section structure):
- A1 hero — `<section class="tab-hero">` + `#settings-hero` + identity strip
- A2 YIR card — `<div id="yir-card">` or similar (currently has style="display:none")
- A-legacy-claim — conditional banner
- A-signin-methods — conditional
- A3 streaming services — `<div id="services-picker">`
- A4 Trakt — `<div id="trakt-card">` (conditional via renderTraktCard)
- A5 notifications — `<div id="notif-card" style="display:none">` (un-hidden by renderSettings)
- A6 sub-profiles — `<div id="subprofile-list">`
- A7 owner-only admin — `<div id="owner-admin">` (rendered by renderOwnerSettings; conditional on isOwner)
- A8 keyboard shortcuts — small static block
- A9 sign out / leave — footer pills

From css/app.css lines 1056-1062 (.tab-section-h eyebrow — reuse for cluster dividers):
```css
.tab-section-h{font-family:'Inter',sans-serif;font-size:var(--t-eyebrow);
  color:var(--ink-dim);letter-spacing:0.12em;text-transform:uppercase;margin-bottom:var(--s3);
  display:flex;align-items:baseline;justify-content:space-between;gap:var(--s3)}
```

From js/app.js renderSettings at line ~4440 — un-hides #notif-card via conditional:
```javascript
// pattern to copy for YIR conditional un-hide
if (state.family && state.family.yirReady) {
  const yirEl = document.getElementById('yir-card');
  if (yirEl) yirEl.style.display = '';  // un-hide when ready
}
```

From .planning/phases/11-feature-refresh-and-streamline/11-APPENDIX-TABS-AUDIT.md (LOCKED option b):
- Family target: 5 sections (Tonight status / Approvals / Members split / Couch history / Group settings footer)
- Account target: 3 clusters (YOU / YOUR COUCH / ADMIN) with existing sections regrouped
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REFR-11 — Family tab 6→5 sections (HTML reorder + picker CUT + members split + Couch history consolidation)</name>
  <files>app.html, js/app.js, css/app.css</files>
  <read_first>
    - app.html lines 437-505 (current #screen-family section structure — FULL READ this range, do NOT skip)
    - app.html lines 656-672 (#picker-sheet-bg modal — KEEP this; it's still functional even though hidden via Plan 11-01 body class. Plan 11-02 only removes the picker SECTION inside #screen-family, NOT the modal at the bottom of app.html)
    - js/app.js Grep for `renderApprovalsCard|renderMembersList|renderStats|renderFamilyFavorites|renderShareSection|renderFamilyHeader` to confirm renderer call sites
    - js/app.js Grep for `family-hero-stats|approvals-card|members-list|family-favorites|per-member-stats` to confirm DOM IDs renderers target
    - .planning/phases/11-feature-refresh-and-streamline/11-APPENDIX-TABS-AUDIT.md §"Family tab — proposed restructure" (line 75-83)
    - .planning/phases/11-feature-refresh-and-streamline/11-UI-SPEC.md §Copywriting Contract REFR-11 (lines 454-461)
    - .planning/phases/11-feature-refresh-and-streamline/11-PATTERNS.md §REFR-11 (lines 776-799 — pure reorder, DOM IDs preserved)
  </read_first>
  <behavior>
    - #screen-family contains 5 sections in this exact order: (1) Tonight-status (hero + new tonight-status block) (2) Approvals (3) Members split into "On the couch" (active) + "Sub-profiles" (4) Couch history (consolidated F5+F6) (5) Group settings footer (F7 reframed)
    - F4 picker section ENTIRELY REMOVED from app.html (#picker-heading + #picker-strip block)
    - All existing DOM IDs preserved EXCEPT #picker-heading + #picker-strip (which are GONE)
    - All existing renderers (renderApprovalsCard, renderMembersList, renderStats, renderFamilyFavorites, renderShareSection if present) still execute and find their containers by ID
    - The #picker-sheet-bg MODAL at the bottom of app.html is NOT removed (it's referenced by openPickerSheet etc. and Plan 11-01 already hides it via body class)
    - New Tonight-status block adds a small `#family-tonight-status` div above the hero with a single render call from renderTonight or a new tiny renderTonightStatus function
  </behavior>
  <action>
    **Part A — HTML reorder (app.html lines 437-505):** Replace the entire #screen-family inner content with 5 sections in this exact order. Use semantic `<section class="tab-section">` wrappers per existing pattern. Preserve EVERY data-bearing DOM ID listed in interfaces section above EXCEPT #picker-heading and #picker-strip.

    Concrete target structure (DOM-ID-faithful skeleton — fill in copy from UI-SPEC §Copywriting Contract REFR-11):

    ```html
    <section id="screen-family" class="screen-section" style="display:none;">
      <!-- (1) Tonight status — replaces bare hero with status-aware top block -->
      <section class="tab-hero">
        <!-- existing hero structure preserved (eyebrow + serif title) -->
        <div class="tab-eyebrow">Your couch</div>
        <h1 class="tab-hero-title">Everyone on the couch</h1>
        <!-- NEW: tonight-status block immediately after title -->
        <div id="family-tonight-status" class="family-tonight-status">
          <!-- populated by renderTonightStatus(); empty placeholder by default -->
        </div>
        <!-- existing stats grid -->
        <div id="family-hero-stats"></div>
      </section>

      <!-- (2) Approvals — unchanged; renderApprovalsCard targets #approvals-card -->
      <section class="tab-section" id="family-approvals-section">
        <div id="approvals-card"></div>
      </section>

      <!-- (3) Members — split into Active vs Sub-profiles -->
      <section class="tab-section" id="family-members-section">
        <h2 class="tab-section-h"><span>On the couch</span></h2>
        <div id="members-list-active"></div>
        <h2 class="tab-section-h" id="members-subprofiles-h" style="margin-top:var(--s5);"><span>Sub-profiles</span></h2>
        <div id="members-list-subprofiles"></div>
        <!-- BACKWARD COMPAT: legacy #members-list ID kept as a hidden alias (renderMembersList may still target it) -->
        <div id="members-list" style="display:none;"></div>
      </section>

      <!-- (4) Couch history — consolidates F5 family-favorites + F6 per-member-breakdown -->
      <section class="tab-section" id="family-couch-history-section">
        <h2 class="tab-section-h"><span>Couch history</span></h2>
        <div id="family-favorites"></div>
        <div id="per-member-stats" style="margin-top:var(--s5);"></div>
      </section>

      <!-- (5) Group settings footer — F7 reframed -->
      <section class="tab-section" id="family-group-settings-section">
        <h2 class="tab-section-h"><span>Group settings</span></h2>
        <!-- preserve existing share-section content + future group-name editing (deferred to Phase 12) -->
        <div class="share-section">
          <!-- existing invite-code + share UI moved here verbatim -->
        </div>
      </section>
    </section>
    ```

    **CRITICAL:** Read the existing app.html lines 437-505 FULLY before writing. Preserve every existing inner structure (button onclicks, classes, micro-styles) when moving content into the new section wrappers. Move existing content blocks (don't re-author from scratch). The above is a SKELETON showing order + new section wrappers — fill the inside of each section with the existing HTML from its old position.

    **REMOVE:** The entire F4 picker block (the `<section>` containing `<h2 id="picker-heading">Who picks tonight</h2>` + `#picker-strip`). Delete from app.html. Do NOT remove `#picker-sheet-bg` (lives later in app.html, ~line 654-669, outside #screen-family).

    **Part B — JS (js/app.js):** Add a tiny new render function `renderTonightStatus()` that fills `#family-tonight-status` with one of three states based on existing state:

    ```javascript
    function renderTonightStatus() {
      const el = document.getElementById('family-tonight-status');
      if (!el) return;
      // Priority 1: active watchparty
      const activeWp = state.activeWatchpartyId ? null /* lookup if needed */ : null;
      // Priority 2: pending intent
      // Priority 3: next scheduled watchparty
      // Priority 4: empty state
      // For v1: simple text. Detailed surface deferred to Phase 12 per CONTEXT.md.
      let body = '';
      if (state.activeWatchpartyId) {
        body = '<div class="family-tonight-status-active"><em>Active watchparty in session.</em></div>';
      } else {
        body = '<div class="family-tonight-status-empty"><em>Nothing scheduled yet.</em></div>';
      }
      el.innerHTML = body;
    }
    ```

    Wire `renderTonightStatus()` into the existing Family-tab render path. Strategy:
    1. `grep -n "renderFamily\|showFamilyTab\|renderFamilyHeader" js/app.js` to find the Family-tab orchestration function.
    2. Add `renderTonightStatus()` call at the start of that function (or after renderFamilyHeader if that's how the tab orchestrates).

    **Members split:** `renderMembersList()` at js/app.js:4579 currently emits all members into `#members-list`. Update it to:
    1. Filter members into two arrays: `active` (no managedBy, has uid) and `subprofiles` (managedBy && !uid OR temporary).
    2. Emit active members into `#members-list-active`.
    3. Emit subprofiles into `#members-list-subprofiles`.
    4. ALSO emit the full combined list into the legacy `#members-list` (kept hidden) so any other code grepping for `members-list` still finds something — backward-compat shim.
    5. Conditionally hide `#members-subprofiles-h` heading + `#members-list-subprofiles` when there are zero subprofiles (avoid empty section noise).

    **Part C — CSS (css/app.css):** Add minimal new rules (use semantic tokens only):
    ```css
    /* Phase 11 / REFR-11 — Family tab tonight-status block + sub-profile heading rhythm */
    .family-tonight-status{margin-top:var(--s3);font-family:var(--font-serif);font-style:italic;font-size:var(--t-body);color:var(--ink-warm)}
    .family-tonight-status-active{color:var(--accent)}
    .family-tonight-status-empty{color:var(--ink-dim)}
    ```

    DO NOT modify existing .tab-section, .tab-hero, .tab-section-h, .share-section, .approvals-card, .members-list rules. The reorder is HTML-only at the section level; existing component styles apply unchanged.

    Per D-XX from CONTEXT.md (REFR-11 LOCKED option b — restructure now, defer ADDs to Phase 12). Per 11-APPENDIX-TABS-AUDIT.md target structure.
  </action>
  <verify>
    <automated>cd "C:/Users/nahde/claude-projects/couch" && grep -c 'id="family-tonight-status"' app.html && grep -c 'id="family-approvals-section"' app.html && grep -c 'id="family-members-section"' app.html && grep -c 'id="family-couch-history-section"' app.html && grep -c 'id="family-group-settings-section"' app.html && grep -c 'id="picker-heading"' app.html && grep -c 'id="picker-strip"' app.html && grep -c 'id="picker-sheet-bg"' app.html && grep -c 'id="approvals-card"' app.html && grep -c 'id="members-list-active"' app.html && grep -c 'id="members-list-subprofiles"' app.html && grep -c 'id="family-favorites"' app.html && grep -c 'id="per-member-stats"' app.html && grep -c 'renderTonightStatus' js/app.js && node --check js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="family-tonight-status"' app.html` returns 1 (NEW)
    - `grep -c 'id="family-approvals-section"' app.html` returns 1 (NEW wrapper)
    - `grep -c 'id="family-members-section"' app.html` returns 1
    - `grep -c 'id="family-couch-history-section"' app.html` returns 1
    - `grep -c 'id="family-group-settings-section"' app.html` returns 1
    - `grep -c 'id="picker-heading"' app.html` returns 0 (REMOVED — F4 cut)
    - `grep -c 'id="picker-strip"' app.html` returns 0 (REMOVED — F4 cut)
    - `grep -c 'id="picker-sheet-bg"' app.html` returns >= 1 (PRESERVED — modal at bottom of app.html stays)
    - `grep -c 'id="approvals-card"' app.html` returns >= 1 (PRESERVED — DOM ID stays)
    - `grep -c 'id="members-list-active"' app.html` returns 1 (NEW split)
    - `grep -c 'id="members-list-subprofiles"' app.html` returns 1 (NEW split)
    - `grep -c 'id="members-list"' app.html` returns >= 1 (legacy alias preserved as hidden div for backward compat)
    - `grep -c 'id="family-favorites"' app.html` returns >= 1 (PRESERVED)
    - `grep -c 'id="per-member-stats"' app.html` returns >= 1 (PRESERVED)
    - `grep -c "renderTonightStatus" js/app.js` returns >= 2 (definition + at least 1 call site)
    - `grep -c "members-list-active\|members-list-subprofiles" js/app.js` returns >= 2 (renderMembersList updated to emit into split containers)
    - `node --check js/app.js` exits 0
    - HTML well-formedness: balance check `grep -c "<section" app.html` equals `grep -c "</section>" app.html`
  </acceptance_criteria>
  <done>
    Family tab renders 5 sections in target order. F4 picker block removed; #picker-sheet-bg modal preserved (still hidden by Plan 11-01 body class). Members split visually into active vs sub-profiles. F5+F6 consolidated under "Couch history". Backward-compat #members-list preserved as hidden alias. 1 atomic commit: `feat(11-02): restructure Family tab 6→5 sections (REFR-11)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REFR-12 — Account tab 9 sections → 3 cognitive clusters (YOU / YOUR COUCH / ADMIN) + sw.js CACHE bump</name>
  <files>app.html, js/app.js, css/app.css, sw.js</files>
  <read_first>
    - app.html lines 507-652 (FULL READ — current #screen-settings section structure)
    - js/app.js renderSettings function at line ~4440 (+ surrounding 60 lines for context)
    - js/app.js renderOwnerSettings at line ~2554 (+ 80 lines)
    - js/app.js renderTraktCard at line ~4477
    - js/app.js renderServicesPicker at line ~4343
    - css/app.css lines 1056-1062 (existing .tab-section-h pattern — reuse for cluster eyebrows)
    - .planning/phases/11-feature-refresh-and-streamline/11-APPENDIX-TABS-AUDIT.md §"Account tab — proposed restructure" (lines 165-185)
    - .planning/phases/11-feature-refresh-and-streamline/11-UI-SPEC.md §Copywriting Contract REFR-12 (lines 463-470)
    - .planning/phases/11-feature-refresh-and-streamline/11-PATTERNS.md §REFR-12 (lines 802-832)
    - sw.js full file (line 8 has CACHE constant — should be `couch-v22-11-01-ux-tightening` after Plan 11-01)
    - CLAUDE.md CACHE bump rule
  </read_first>
  <behavior>
    - #screen-settings contains 3 cluster wrappers, each preceded by a .tab-section-h eyebrow heading: "YOU" / "YOUR COUCH" / "ADMIN"
    - Cluster 1 (YOU): A1 identity + A6 sub-profiles
    - Cluster 2 (YOUR COUCH): A3 streaming + A4 Trakt (under "Integrations" sub-heading) + A5 notifications + A2 YIR (hidden until state.family.yirReady)
    - Cluster 3 (ADMIN): A7 owner admin (3 sub-headings: Security, Members, Lifecycle) + A8 keyboard shortcuts + A9 sign out + Leave family (with new confirmation modal)
    - Cluster-to-cluster gap is var(--s7) (40px); within-cluster gap is existing var(--s6) (28px)
    - All existing DOM IDs preserved (services-picker, trakt-card, notif-card, subprofile-list, owner-admin, etc.)
    - YIR card hidden by default (display:none); unhidden conditionally if state.family && state.family.yirReady
    - Leave family button opens confirmation modal (two-tap destructive per UI-SPEC §Destructive actions)
  </behavior>
  <action>
    **Part A — HTML restructure (app.html lines 507-652):** Wrap existing sections into 3 cluster groups. Use the existing `.tab-section-h` eyebrow pattern for cluster dividers. Add a custom data attribute `data-cluster="you|couch|admin"` on each cluster container for CSS targeting + future testability.

    Concrete target structure (DOM-ID-faithful skeleton — fill in inner content from existing HTML):

    ```html
    <section id="screen-settings" class="screen-section" style="display:none;">
      <!-- Existing hero (A1) — outside the 3 clusters; stays at top -->
      <section class="tab-hero">
        <!-- existing identity strip + greeting + #group-pill etc. preserved -->
      </section>

      <!-- A-legacy-claim + A-signin-methods conditional banners stay at top, before cluster 1 -->

      <!-- CLUSTER 1: YOU (identity + sub-profiles) -->
      <div class="settings-cluster" data-cluster="you">
        <h2 class="tab-section-h"><span>YOU</span></h2>
        <!-- A6 sub-profiles -->
        <section class="tab-section" id="settings-subprofiles-section">
          <div id="subprofile-list"></div>
        </section>
      </div>

      <!-- CLUSTER 2: YOUR COUCH (services, trakt, notifs, YIR) -->
      <div class="settings-cluster" data-cluster="couch">
        <h2 class="tab-section-h"><span>YOUR COUCH</span></h2>

        <!-- A3 streaming services -->
        <section class="tab-section" id="settings-services-section">
          <div id="services-picker"></div>
        </section>

        <!-- A4 Trakt under "Integrations" sub-heading -->
        <section class="tab-section" id="settings-integrations-section">
          <h3 class="settings-subcluster-h">Integrations</h3>
          <div id="trakt-card"></div>
        </section>

        <!-- A5 notifications (existing display:none default un-hidden by renderSettings) -->
        <section class="tab-section" id="settings-notifs-section">
          <div id="notif-card" style="display:none;"></div>
        </section>

        <!-- A2 YIR (hidden until Phase 10 ships; un-hide gated on state.family.yirReady) -->
        <section class="tab-section" id="settings-yir-section" style="display:none;">
          <div id="yir-card" class="tab-accent-card">
            <!-- existing YIR placeholder content preserved -->
          </div>
        </section>
      </div>

      <!-- CLUSTER 3: ADMIN (owner admin sub-grouped, shortcuts, sign out, leave family) -->
      <div class="settings-cluster" data-cluster="admin">
        <h2 class="tab-section-h"><span>ADMIN</span></h2>

        <!-- A7 owner admin sub-grouped (3 sub-headings) -->
        <section class="tab-section" id="settings-owner-admin-section">
          <div id="owner-admin">
            <!-- renderOwnerSettings emits into here; will be sub-grouped via Part B JS edit -->
          </div>
        </section>

        <!-- A8 keyboard shortcuts -->
        <section class="tab-section" id="settings-shortcuts-section">
          <!-- existing kbd-shortcuts block preserved -->
        </section>

        <!-- A9 sign out + leave family (footer) -->
        <section class="tab-section" id="settings-footer-section">
          <button id="signout-btn" class="pill" onclick="signOut()">Sign out</button>
          <button id="leave-family-btn" class="pill" onclick="confirmLeaveFamily()">Leave family</button>
        </section>
      </div>
    </section>

    <!-- New confirmation modal for leave-family destructive action (placed near other modals at bottom of app.html) -->
    <div class="modal-bg" id="leave-family-confirm-bg">
      <div class="modal">
        <h3>Leave family?</h3>
        <p>Are you sure? You'll lose access to this couch and your votes stay with the group.</p>
        <div class="modal-actions-row">
          <button class="pill" onclick="closeLeaveFamilyConfirm()">Cancel</button>
          <button class="pill leave-family-confirm" onclick="performLeaveFamily()">Leave</button>
        </div>
      </div>
    </div>
    ```

    Read existing app.html lines 507-652 fully and MOVE existing content blocks into new wrappers (don't re-author). Preserve every existing onclick handler, class, and conditional-display state.

    **Part B — JS (js/app.js):**

    1. **YIR conditional un-hide** — In renderSettings (or wherever appropriate), add:
    ```javascript
    const yirSection = document.getElementById('settings-yir-section');
    if (yirSection) {
      yirSection.style.display = (state.family && state.family.yirReady) ? '' : 'none';
    }
    ```

    2. **Owner admin sub-grouping** — Update renderOwnerSettings at js/app.js:2554 to emit 3 sub-blocks within #owner-admin instead of one wall:
    ```javascript
    el.innerHTML = `
      <div class="owner-subcluster" data-subcluster="security">
        <h4 class="settings-subcluster-h">Security</h4>
        <!-- password set/unset + guest invites HTML moved here -->
      </div>
      <div class="owner-subcluster" data-subcluster="members">
        <h4 class="settings-subcluster-h">Members</h4>
        <!-- claim members + ownership transfer HTML moved here -->
      </div>
      <div class="owner-subcluster" data-subcluster="lifecycle">
        <h4 class="settings-subcluster-h">Lifecycle</h4>
        <!-- grace banner + delete-related controls HTML moved here -->
      </div>
    `;
    ```
    Preserve every existing button onclick, every existing data attribute, every existing functional element. The grouping is a wrapping change.

    3. **Leave-family confirmation modal handlers** — Add new functions:
    ```javascript
    window.confirmLeaveFamily = function() {
      document.getElementById('leave-family-confirm-bg').classList.add('on');
    };
    window.closeLeaveFamilyConfirm = function() {
      document.getElementById('leave-family-confirm-bg').classList.remove('on');
    };
    window.performLeaveFamily = async function() {
      // Call the EXISTING leave-family logic. Strategy: grep for existing leaveFamily / signOutFamily / detachFamily
      // and invoke that here. Do NOT re-implement leave logic.
      // ...
      document.getElementById('leave-family-confirm-bg').classList.remove('on');
    };
    ```
    Strategy: `grep -n "leaveFamily\|leaveGroup\|detachFamily" js/app.js` to find the existing leave handler. The `performLeaveFamily` wrapper calls that existing function. Keep the original leaveFamily function intact for backward compat.

    **Part C — CSS (css/app.css):** Add minimal cluster-spacing rules:
    ```css
    /* Phase 11 / REFR-12 — Account tab 3-cluster regroup */
    .settings-cluster{margin-top:var(--s7)}
    .settings-cluster:first-of-type{margin-top:var(--s6)}
    .settings-cluster .tab-section{margin-top:var(--s6)}
    .settings-cluster .tab-section:first-of-type{margin-top:var(--s3)}
    .settings-subcluster-h{font-family:var(--font-sans);font-size:var(--t-meta);font-weight:600;color:var(--ink-warm);margin-bottom:var(--s2);margin-top:var(--s4);text-transform:none;letter-spacing:0}
    .owner-subcluster{margin-bottom:var(--s5)}
    .owner-subcluster:last-child{margin-bottom:0}
    .leave-family-confirm{background:var(--bad);color:var(--bg);border-color:transparent;font-weight:700}
    ```

    **Part D — sw.js CACHE bump:** Update sw.js line 8:
    - From: `const CACHE = 'couch-v22-11-01-ux-tightening';` (current after Plan 11-01)
    - To: `const CACHE = 'couch-v23-11-02-tab-restructure';`

    Per D-XX from CONTEXT.md (REFR-12 LOCKED option b). Per 11-APPENDIX-TABS-AUDIT.md target structure.
  </action>
  <verify>
    <automated>cd "C:/Users/nahde/claude-projects/couch" && grep -c 'data-cluster="you"' app.html && grep -c 'data-cluster="couch"' app.html && grep -c 'data-cluster="admin"' app.html && grep -c '<span>YOU</span>' app.html && grep -c '<span>YOUR COUCH</span>' app.html && grep -c '<span>ADMIN</span>' app.html && grep -c 'id="services-picker"' app.html && grep -c 'id="trakt-card"' app.html && grep -c 'id="notif-card"' app.html && grep -c 'id="subprofile-list"' app.html && grep -c 'id="owner-admin"' app.html && grep -c 'id="yir-card"' app.html && grep -c 'id="leave-family-confirm-bg"' app.html && grep -c "confirmLeaveFamily" js/app.js && grep -c "performLeaveFamily" js/app.js && grep -c 'data-subcluster="security"' js/app.js && grep -c 'data-subcluster="members"' js/app.js && grep -c 'data-subcluster="lifecycle"' js/app.js && grep -c "yirReady" js/app.js && grep -c "couch-v23-11-02-tab-restructure" sw.js && node --check js/app.js && node --check sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-cluster="you"' app.html` returns 1
    - `grep -c 'data-cluster="couch"' app.html` returns 1
    - `grep -c 'data-cluster="admin"' app.html` returns 1
    - `grep -c '<span>YOU</span>' app.html` returns 1
    - `grep -c '<span>YOUR COUCH</span>' app.html` returns 1
    - `grep -c '<span>ADMIN</span>' app.html` returns 1
    - All existing DOM IDs PRESERVED: services-picker, trakt-card, notif-card, subprofile-list, owner-admin, yir-card each return >= 1
    - `grep -c 'id="leave-family-confirm-bg"' app.html` returns 1 (NEW destructive confirmation modal)
    - `grep -c "confirmLeaveFamily" js/app.js` returns >= 2 (definition + onclick reference)
    - `grep -c "performLeaveFamily" js/app.js` returns >= 2
    - `grep -c "yirReady" js/app.js` returns >= 1 (conditional gate added)
    - `grep -c 'data-subcluster="security"' js/app.js` returns >= 1
    - `grep -c 'data-subcluster="members"' js/app.js` returns >= 1
    - `grep -c 'data-subcluster="lifecycle"' js/app.js` returns >= 1
    - `grep -c "couch-v23-11-02-tab-restructure" sw.js` returns >= 1 (cache bumped)
    - `grep -c "couch-v22-11-01-ux-tightening" sw.js` returns 0 (old cache replaced)
    - `node --check js/app.js` exits 0
    - `node --check sw.js` exits 0
    - `grep -c "<section" app.html` equals `grep -c "</section>" app.html` (HTML balance preserved)
  </acceptance_criteria>
  <done>
    Account tab renders 3 cognitive clusters with eyebrow dividers. Owner admin sub-grouped into Security/Members/Lifecycle. YIR hidden until Phase 10. Leave family is two-tap destructive with confirmation modal. sw.js cache bumped to v23. 1 atomic commit: `feat(11-02): restructure Account tab into 3 cognitive clusters + bump sw.js CACHE (REFR-12)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify both restructured tabs</name>
  <what-built>
    Family tab 6→5 sections (picker CUT, members split, history consolidated, settings footer reframed) + Account tab 9→3 cognitive clusters (YOU / YOUR COUCH / ADMIN) with owner admin sub-grouped + Leave family two-tap confirmation + sw.js cache v23. Total: 2 atomic commits, 4 files modified.
  </what-built>
  <how-to-verify>
    Open http://localhost:5500/app or deploy to a Firebase preview channel and visit the URL.

    **REFR-11 verification (Family tab):**
    1. Tap Family tab. Confirm sections appear in this exact order top-to-bottom:
       a. Hero with "Everyone on the couch" title + new tonight-status block below it (italic dim text)
       b. Approvals card (only if there are pending approvals — otherwise hidden as before)
       c. "On the couch" heading + active members list + (if any sub-profiles) "Sub-profiles" heading + sub-profile list
       d. "Couch history" heading + family favorites + per-member breakdown
       e. "Group settings" heading + share/invite UI
    2. Confirm NO "Who picks tonight" section anywhere. Confirm picker logic is still functional (try opening picker modal via dev console: `openPickerSheet()` — modal should still appear because Plan 11-01 only hid via body class, not removed; but Plan 11-02 leaves modal definition intact).
    3. Confirm Approvals card behaves same as before (hidden when empty).

    **REFR-12 verification (Account tab):**
    4. Tap Account tab. Confirm:
       a. Hero with identity strip + greeting at top (unchanged)
       b. Then "YOU" eyebrow → sub-profiles section
       c. Then "YOUR COUCH" eyebrow → streaming services + Integrations (Trakt) + Notifications + YIR (HIDDEN unless yirReady; verify via Firestore inspection that yirReady is false → YIR card invisible)
       d. Then "ADMIN" eyebrow → owner admin (if you're the owner — sub-grouped into Security / Members / Lifecycle) + keyboard shortcuts + Sign out + Leave family
    5. As an owner: confirm Owner admin shows 3 sub-headings (Security, Members, Lifecycle) with the existing controls grouped under each.
    6. Tap "Leave family": confirm a confirmation modal appears with "Are you sure?" + Cancel + red "Leave" button. Tap Cancel: modal dismisses. (DO NOT actually leave the family.)
    7. As a non-owner: confirm ADMIN cluster doesn't show owner admin block (existing isOwner gate preserved).

    **PWA cache:**
    8. DevTools → Application → Service Workers → confirm version `couch-v23-11-02-tab-restructure`.

    **Functional regression smoke (CRITICAL):**
    9. Open services-picker → toggle a streaming service → verify it persists (existing render path unchanged).
    10. Open Trakt section if you have it connected → confirm sync status displays.
    11. Open Approvals (set up a kid sub-profile + request something to test) → confirm renderApprovalsCard still emits.
    12. (If owner) → set/unset password works; create guest invite works.

    Type "approved" if all 12 checks pass; describe specific failures otherwise.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address before deploy</resume-signal>
</task>

</tasks>

<threat_model>
Pure UI reorganization plan. NEW security surface: Leave family confirmation modal. Existing security surfaces (firestore.rules, ownership transfer flow, password CFs) remain unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Leave family confirmation could be bypassed via direct call to existing leaveFamily function in console | accept | The confirmation modal is product-UX defense, not a security boundary. Existing `leaveFamily` server-side write path remains gated by firestore.rules + writeAttribution. Determined console-user can still leave; that's working-as-intended (it's their data). |
| T-11-02-02 | Information Disclosure | YIR card hidden via display:none could be revealed by user before Phase 10 ships | accept | YIR card today is a static placeholder with NO real data (Phase 10 hasn't shipped). Revealing the placeholder exposes nothing. Once Phase 10 ships and writes real data into yir-card, the yirReady gate becomes a true content-conditional, not a security gate. |
| T-11-02-03 | Elevation of Privilege | Owner admin section visibility relies on existing isOwner check | mitigate | No change to authorization — sub-grouping the existing owner-only block into 3 sub-headings is presentation-layer only. Existing renderOwnerSettings continues to early-return when `state.auth.uid !== state.ownerUid` (verify pattern at js/app.js:2557 unchanged in this plan). |
</threat_model>

<verification>
1. `node --check js/app.js` exits 0.
2. `node --check sw.js` exits 0.
3. HTML well-formed: `<section` open count equals `</section>` close count in app.html.
4. All renderers still target their containers: `grep -c "getElementById('approvals-card'\|getElementById('members-list'\|getElementById('services-picker'\|getElementById('trakt-card'\|getElementById('notif-card'\|getElementById('subprofile-list'\|getElementById('owner-admin'\|getElementById('yir-card'" js/app.js` returns >= 7 hits (one per existing renderer).
5. F4 picker section gone: `grep -c 'id="picker-heading"' app.html` returns 0.
6. F4 picker modal preserved: `grep -c 'id="picker-sheet-bg"' app.html` returns 1.
7. PWA SW version registered in browser is `couch-v23-11-02-tab-restructure`.
</verification>

<success_criteria>
- 2 atomic commits land on master
- Family tab renders 5 sections in target order; F4 picker block REMOVED from HTML; backend rotation writes preserved
- Account tab renders 3 cognitive clusters with eyebrow dividers; owner admin sub-grouped 3 ways
- YIR card hidden via state.family.yirReady gate (Phase 10 not shipped)
- Leave family is two-tap destructive with confirmation modal
- All existing renderers (renderApprovalsCard, renderMembersList, renderStats, renderFamilyFavorites, renderServicesPicker, renderTraktCard, renderOwnerSettings) execute unchanged
- sw.js CACHE bumped to v23
- Both REFR-11 + REFR-12 close
- Zero functional regressions (verified via human-verify checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/11-feature-refresh-and-streamline/11-02-SUMMARY.md` documenting:
- 2 commit SHAs + brief description each
- Confirmed REFR-11 + REFR-12 closures
- sw.js CACHE bump (v22 → v23-11-02-tab-restructure)
- Verification matrix from human-verify checkpoint
- Any deviations from plan (e.g. how leave-family handler was wired into the existing function) + rationale
- File modification summary (app.html section reorder summary, js/app.js render function deltas, css/app.css new rules added, sw.js cache version)
- Note on deferred ADDs (Phase 12: Tonight status full content, calendar, recent watchparties drilldown, notification prefs detail, data export, theme prefs, about/version) per CONTEXT.md decision 5 option (b)
</output>
