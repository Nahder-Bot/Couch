---
phase: 07-watchparty
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - .planning/phases/07-watchparty/07-UAT-RESULTS.md
autonomous: false
requirements: [PARTY-02, PARTY-03, PARTY-04, PARTY-05, PARTY-06, PARTY-07]
must_haves:
  truths:
    - "07-UAT-RESULTS.md exists with per-scenario PASS/FAIL"
    - "Scheduled→active flip verified: schedule a party 2min out from one device, wait; status flips and watchpartyStarting push arrives"
    - "Tonight banner verified live on iPhone + second device"
    - "Reactions verified <2s cross-device"
    - "Orphan prevention verified: scheduled→stale archives after 6h (use a shortened test window)"
    - "PARTY-06 — session ends cleanly on all participants leaving"
  artifacts:
    - path: .planning/phases/07-watchparty/07-UAT-RESULTS.md
      provides: "Phase 7 UAT results"
      min_lines: 60
---

<objective>
Hands-on UAT of Phase 7. Flagship: scheduled→active flip + push (unblocks Phase 6's watchpartyStarting that couldn't be tested before). Verifies banner + reactions + orphan cleanup.

Purpose: Phase 7 closes the Phase 6 loop and ships the full watchparty UX. UAT is the gate.
Output: `07-UAT-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-watchparty/07-CONTEXT.md
@.planning/phases/06-push-notifications/06-UAT-RESULTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Scheduled→active flip + watchpartyStarting push (flagship)</name>
  <how-to-verify>
    Second device schedules a watchparty 2 minutes out (NOT "Now" — we want the real state transition). Leave the app closed on iPhone PWA. At the scheduled time, within ~5s, iPhone should receive **"Watchparty starting"** push with body "[title] is starting now". This is the Phase 6 flagship event that couldn't fire until Phase 7's flip mechanism shipped.
  </how-to-verify>
  <resume-signal>
    - `flagship-pass` with latency noted
    - `fail: <details>` — route back to 07-01 for fix
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Tonight banner + Start-from-Pick CTA</name>
  <how-to-verify>
    From iPhone, spin on Tonight → pick lands in modal → tap "Watch together" button → start modal opens pre-populated with the picked title → tap Now → Start. On second device open Couch → Tonight tab → banner shows the live party with Join button → tap Join → routed to live modal.
  </how-to-verify>
  <resume-signal>
    - `banner-pass`
    - `banner-fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reactions real-time cross-device</name>
  <how-to-verify>
    Both devices in the same live watchparty. iPhone taps 🎉 → second device shows "[iPhone member] 🎉" within 2 seconds. Repeat with 2–3 emoji rapid-fire → no dropped reactions. Participant timer strip updates visibly on both devices.
  </how-to-verify>
  <resume-signal>
    - `reactions-pass`
    - `reactions-fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Orphan prevention (use short test windows)</name>
  <how-to-verify>
    Schedule a watchparty 1 minute out, then don't join. After 1 min, status should be `active`. Wait 30 min (or temporarily tweak the 30-min threshold in CF to 2 min for this test), verify it auto-archives. OR: set Firestore rule-test via Firebase Console — edit the watchparty doc to have startAt = 7h ago + status = scheduled, wait for next CF cron tick, verify it archives with `archivedReason: 'stale_scheduled'`.
  </how-to-verify>
  <resume-signal>
    - `orphan-pass`
    - `orphan-pending` (if you don't want to wait the 30min)
    - `orphan-fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Write 07-UAT-RESULTS.md</name>
  <how-to-verify>
    Create the file with the standard UAT structure (see `06-UAT-RESULTS.md` for template). All 4 scenarios + recommendation. Commit with `docs(07-04): UAT results`.
  </how-to-verify>
</task>

</tasks>

<verification>
1. `07-UAT-RESULTS.md` exists and is committed
2. All scenarios have outcomes (PASS / FAIL / PENDING)
</verification>

<success_criteria>
- Flagship watchpartyStarting push arrives on iOS (unblocks Phase 6)
- Banner + Start CTA verified
- Reactions real-time verified
- Orphan prevention verified
- Phase 7 ready for /gsd-verify-work
</success_criteria>

<output>
`07-UAT-RESULTS.md`.
</output>
