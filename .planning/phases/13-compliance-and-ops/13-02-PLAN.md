---
phase: 13-compliance-and-ops
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - C:\Users\nahde\claude-projects\couch\app.html
  - C:\Users\nahde\claude-projects\couch\landing.html
  - C:\Users\nahde\claude-projects\couch\sw.js
  - C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md
autonomous: true
requirements: [OPS-13-05]
threat_model_severity: high
user_setup:
  - service: sentry
    why: "Sentry account + project required to obtain DSN before deploy can show real telemetry"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard -> Projects -> couch -> Client Keys (DSN)"
        public_by_design: true
    dashboard_config:
      - task: "Create free-tier Sentry account at sentry.io"
        location: "https://sentry.io/signup/"
      - task: "Create a Browser JavaScript project named 'couch'"
        location: "Sentry Dashboard -> Projects -> Create Project -> Platform: Browser JavaScript"
      - task: "Copy the loader-script DSN URL (format: https://<PUBLIC_KEY>@o<ORGID>.ingest.us.sentry.io/<PROJECTID>)"
        location: "Sentry Dashboard -> Settings -> Projects -> couch -> Client Keys (DSN) -> Loader Script"

must_haves:
  truths:
    - "Sentry loader script is present in app.html <head> and landing.html <head>; it loads asynchronously without blocking app boot."
    - "Window-level uncaught errors and unhandled promise rejections are captured by Sentry's auto-instrumentation (no manual addEventListener needed per RESEARCH.md docs citation)."
    - "Explicit PII fields are stripped from the event payload before Sentry transmits: event.user.id (uid), event.user.email, event.user.username, event.user.ip_address. Family-code regex (/\\b[a-f0-9]{6}\\b/gi) and route-token regex (?invite=, ?claim=, /rsvp/<token>) scrub message text, exception values, request URLs, and breadcrumb URLs (review fix MEDIUM-9 — wording softened from 'all PII stripped' to 'explicit fields stripped + defensive scrubbing on known-leak surfaces')."
    - "Sentry stack frames, request headers, tags not directly mutated by beforeSend, and any future Replay metadata MAY still carry identifiers (e.g., variable names, header values, DOM-node IDs). Review the Sentry inbox periodically (every ~30 days post-launch) to confirm the actual leak surface; tighten beforeSend if new identifier sources surface (review fix MEDIUM-9 — explicit acknowledgement of residual surface)."
    - "Session Replay is DISABLED for v1 launch (review fix MEDIUM-6 — privacy-conservative default; replaysSessionSampleRate AND replaysOnErrorSampleRate both set to 0 OR omitted entirely). Re-enable post-launch +30 days after the privacy review confirms PII surface is acceptable; tracked as TECH-DEBT.md entry 'Sentry Replay deferred'."
    - "Firestore transport noise (WebChannelConnection / FirebaseError / firestore.googleapis) is filtered from Sentry breadcrumbs to avoid dashboard flood (Pitfall 5 defense)."
  artifacts:
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\app.html"
      provides: "Sentry loader script + sentryOnLoad init block in <head>; Replay disabled (review fix MEDIUM-6)"
      contains: "sentryOnLoad"
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\landing.html"
      provides: "Sentry loader (parallel install — landing page is also instrumented); Replay disabled (review fix MEDIUM-6)"
      contains: "sentryOnLoad"
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\sw.js"
      provides: "CACHE bumped to v33.1 (additive on top of 13-01's v33 bump)"
      contains: "couch-v33"
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\.planning\\TECH-DEBT.md"
      provides: "New TECH-DEBT entry — 'Sentry Replay deferred' (review fix MEDIUM-6 — re-enable post-launch +30 days after privacy review)"
      contains: "Sentry Replay"
  key_links:
    - from: "app.html <head> sentryOnLoad block"
      to: "Sentry ingest endpoint"
      via: "Sentry.init({dsn, beforeSend, ...}) — runs after CDN script loads"
      pattern: "Sentry\\.init"
    - from: "Sentry beforeSend hook"
      to: "Stripped event payload"
      via: "Synchronous event mutator before any HTTP POST to ingest"
      pattern: "beforeSend"
---

<objective>
Ship OPS-13-05: Third-party error reporting via Sentry. The loader script (CDN, zero-bundler) is added to both `app.html` and `landing.html` `<head>`; auto-instrumentation captures `window.onerror` + `unhandledrejection` events; a `beforeSend` hook scrubs explicit PII fields (uid, email, username, ip_address, family codes, tokens in URLs) plus breadcrumb URLs before transmission; a `beforeBreadcrumb` hook drops Firestore transport noise. Sentry is the locked vendor (CONTEXT.md addendum #1) — Crashlytics has no web SDK as of 2026.

**Review fixes incorporated:**
- **MEDIUM-6:** Session Replay disabled for v1. `replaysOnErrorSampleRate` and `replaysSessionSampleRate` lines dropped from the init config. Re-enable post-launch +30 days after privacy review (tracked in TECH-DEBT.md).
- **MEDIUM-9:** Must-have wording softened from "all PII stripped" to "explicit fields stripped (uid, email, username, ip_address) + defensive scrubbing on known-leak surfaces (message, exception value, request URL, breadcrumb URL, family-code regex). Stack frames, headers, and replay metadata may still carry identifiers — review periodically." The actual `beforeSend` code is unchanged; only the claim is now accurate.

Purpose: Without error reporting, prod errors are invisible until users complain. Sentry's free tier (5K errors/month) covers Couch v1 scale.

Output: Two HTML head-script blocks (one per surface), each with the same Sentry config + PII scrubber (Replay disabled). SW CACHE bumped so installed PWAs pick up the new shell. New TECH-DEBT entry for Replay deferral.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-CONTEXT.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-RESEARCH.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-PATTERNS.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-REVIEWS.md
@C:\Users\nahde\claude-projects\couch\CLAUDE.md

<interfaces>
<!-- Sentry loader script signature (CDN-versioned, no pinning needed): -->
```html
<script>
  window.sentryOnLoad = function () { Sentry.init({ /* config */ }); };
</script>
<script src="https://js.sentry-cdn.com/<PUBLIC_KEY>.min.js" crossorigin="anonymous"></script>
```

<!-- Sentry.init config shape (review fix MEDIUM-6 — Replay dropped from v1):
- `dsn`: string from Sentry dashboard
- `environment`: 'production' if hostname === 'couchtonight.app' else 'dev'
- `tracesSampleRate`: 0.1 (10% of transactions, cheap on free tier)
- `integrations`: [browserTracingIntegration]   ← replayIntegration REMOVED for v1
- `beforeSend(event, hint)`: PII scrubber (verbatim block in Task 1 action)
- `beforeBreadcrumb(breadcrumb, hint)`: Firestore noise filter (Pitfall 5 defense)

NOTE on Replay omission (review fix MEDIUM-6):
- `replaysSessionSampleRate` and `replaysOnErrorSampleRate` keys are NOT present in v1 init.
- `Sentry.replayIntegration({...})` is NOT in the integrations array.
- Re-enabling Replay post-launch is tracked in TECH-DEBT.md "Sentry Replay deferred". -->

<!-- Existing landing.html inline-script-in-head pattern (the analog) at landing.html:35-44: -->
```html
<!-- Installed-PWA + deep-link redirect. Runs before DOMContentLoaded, so no flash -->
<script>
(function() {
  var search = window.location.search || '';
  var isStandalone = (window.matchMedia && window.matchMedia('(display-mode: standalone)').matches) || window.navigator.standalone === true;
  var isDeepLink = search.indexOf('invite=') !== -1 || search.indexOf('claim=') !== -1;
  if (isStandalone || isDeepLink) {
    window.location.replace('/app' + search + window.location.hash);
  }
})();
</script>
```

<!-- app.html <head> currently has NO inline script — Sentry block goes BEFORE the </head>
     and BEFORE/AFTER (planner choice; RESEARCH.md says AFTER) the existing
     <link rel="stylesheet" href="/css/app.css"> reference. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sentry loader + sentryOnLoad init block to app.html and landing.html (Replay disabled per review fix MEDIUM-6)</name>
  <files>
    C:\Users\nahde\claude-projects\couch\app.html (MODIFY — insert Sentry block in <head>),
    C:\Users\nahde\claude-projects\couch\landing.html (MODIFY — insert Sentry block in <head>)
  </files>

  <read_first>
    - C:\Users\nahde\claude-projects\couch\app.html (lines 1-60 — read entire `<head>` block; locate the `<link rel="stylesheet" href="/css/app.css">` line and the closing `</head>`; the new Sentry block goes between them)
    - C:\Users\nahde\claude-projects\couch\landing.html (lines 1-60 — read entire `<head>` block; locate the existing inline `<script>` IIFE for standalone redirect at lines 35-44 + the JSON-LD block at line 47; the new Sentry block goes between those two — AFTER the redirect IIFE, BEFORE the JSON-LD)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-PATTERNS.md (sections "app.html <head> Sentry loader script" + "js/app.js PII-scrub helper for Sentry beforeSend" + "landing.html <head> Sentry loader" — note: PATTERNS.md still includes the Replay integration; this plan diverges per review fix MEDIUM-6)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-RESEARCH.md (sections "Pattern 4: Sentry loader script init" + "Pitfall 5: Sentry breaks Firestore real-time listeners by intercepting console.error" + "OPS-05" item rows in tables)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-REVIEWS.md (review fixes MEDIUM-6 [Replay disabled] and MEDIUM-9 [PII wording softened])
  </read_first>

  <action>
**Step 1.1 — Modify `C:\Users\nahde\claude-projects\couch\app.html`.**

Insert the following block in `<head>`, AFTER `<link rel="stylesheet" href="/css/app.css">` and BEFORE `</head>` (verified location per PATTERNS.md). NOTE: This block diverges from the PATTERNS.md verbatim by review fix MEDIUM-6 — Replay integration and replay sample-rate keys are REMOVED. The `beforeSend` body is unchanged from PATTERNS.md (only the surrounding claim wording was softened per MEDIUM-9):

```html
<!-- Phase 13 / OPS-13-05 — Sentry loader (CDN, zero-bundler).
     Source: docs.sentry.io/platforms/javascript/install/loader/
     Explicit-fields PII strip in beforeSend per RESEARCH.md Pattern 4 + review fix MEDIUM-9.
     Session Replay DISABLED for v1 (review fix MEDIUM-6); re-enable post-launch +30 days
     after privacy review — tracked in TECH-DEBT.md "Sentry Replay deferred".
     CSP-compatible: js.sentry-cdn.com is allow-listed in queuenight/firebase.json (Plan 13-05).
     DSN is public-by-design (rate-limited per project per docs.sentry.io). -->
<script>
  window.sentryOnLoad = function () {
    Sentry.init({
      dsn: 'https://<SENTRY_PUBLIC_KEY>@o<SENTRY_ORGID>.ingest.us.sentry.io/<SENTRY_PROJECTID>',
      environment: (location.hostname === 'couchtonight.app') ? 'production' : 'dev',
      tracesSampleRate: 0.1,
      // Review fix MEDIUM-6: Replay disabled for v1.
      // No replaysSessionSampleRate, no replaysOnErrorSampleRate, no replayIntegration.
      // See TECH-DEBT.md "Sentry Replay deferred" for re-enable plan.
      integrations: [
        Sentry.browserTracingIntegration()
      ],
      // T-6 mitigation: explicit-fields PII scrub before any network call to Sentry ingest.
      // Review fix MEDIUM-9: this is NOT all-PII-strip; stack frames, headers, tags not
      // touched here may still carry identifiers — review inbox periodically.
      beforeSend: function (event, hint) {
        // 1. Strip user.id (uid), user.email, user.username, ip_address (never off-platform)
        if (event.user) {
          delete event.user.id;
          delete event.user.email;
          delete event.user.username;
          delete event.user.ip_address;
        }
        // 2. Strip family codes (6 lowercase hex chars) from messages + exception values
        var FAMILY_RE = /\b[a-f0-9]{6}\b/gi;
        if (event.message) event.message = String(event.message).replace(FAMILY_RE, '<FAMILY>');
        if (event.exception && event.exception.values) {
          for (var i = 0; i < event.exception.values.length; i++) {
            var ex = event.exception.values[i];
            if (ex && ex.value) ex.value = String(ex.value).replace(FAMILY_RE, '<FAMILY>');
          }
        }
        // 3. Strip tokens from request URLs (?invite= ?claim= /rsvp/<token>)
        if (event.request && event.request.url) {
          event.request.url = String(event.request.url)
            .replace(/[?&](invite|claim)=[^&]+/g, function (_, k) { return '?' + k + '=<REDACTED>'; })
            .replace(/\/rsvp\/[^/?]+/g, '/rsvp/<TOKEN>');
        }
        // 4. Strip same patterns from breadcrumb URLs
        if (event.breadcrumbs && event.breadcrumbs.length) {
          for (var b = 0; b < event.breadcrumbs.length; b++) {
            var bc = event.breadcrumbs[b];
            if (bc && bc.data && bc.data.url) {
              bc.data.url = String(bc.data.url)
                .replace(/[?&](invite|claim)=[^&]+/g, function (_, k) { return '?' + k + '=<REDACTED>'; })
                .replace(/\/rsvp\/[^/?]+/g, '/rsvp/<TOKEN>');
            }
          }
        }
        return event;
      },
      // Pitfall 5 defense: drop Firestore transport noise from breadcrumbs.
      beforeBreadcrumb: function (breadcrumb, hint) {
        if (!breadcrumb) return breadcrumb;
        var msg = breadcrumb.message || (breadcrumb.data && breadcrumb.data.message) || '';
        if (/WebChannelConnection|FirebaseError|firestore\.googleapis/.test(String(msg))) {
          return null;
        }
        return breadcrumb;
      }
    });
  };
</script>
<script src="https://js.sentry-cdn.com/<SENTRY_PUBLIC_KEY>.min.js" crossorigin="anonymous"></script>
```

**Critical placeholders:** The strings `<SENTRY_PUBLIC_KEY>`, `<SENTRY_ORGID>`, `<SENTRY_PROJECTID>` are literal placeholders that the user MUST replace with the real values from the Sentry dashboard before deploy. Plan 13-03's deploy.sh will ABORT a production deploy if any of these placeholders remain in `app.html` or `landing.html` at deploy time (review fix MEDIUM-5; cross-reference). Document this in `13-02-SUMMARY.md` under "Pre-deploy steps for the operator."

**Step 1.2 — Modify `C:\Users\nahde\claude-projects\couch\landing.html`.**

Insert the SAME block in `<head>`, AFTER the closing `</script>` of the existing standalone-redirect IIFE (line ~44) and BEFORE the `<script type="application/ld+json">` block (line ~47).

The block is identical to the app.html version verbatim — same DSN, same beforeSend (including the symmetric `delete event.user.id;` line — landing has no auth so it's a no-op there but harmless and keeps the pattern symmetric across surfaces), same beforeBreadcrumb, same Replay-disabled posture (review fix MEDIUM-6). **De-duplication is acceptable for v1** (~50 lines duplicated across two files). RESEARCH.md flags this as a Milestone 2 refactor target (extract to `js/sentry-config.js`), explicitly deferred.

**Step 1.3 — Sentry SRI hash decision (T-7 mitigation).**

The orchestrator threat list T-7 flagged: "Sentry CDN script tampering. Mitigation: SRI hash + crossorigin=\"anonymous\" on the loader script tag."

**Decision:** Apply `crossorigin="anonymous"` (already in the snippet above). Do NOT add an `integrity="sha384-..."` SRI hash — Sentry's loader script is auto-versioned at the CDN edge ("latest" per project), and adding an SRI hash would lock us to a single immutable build, defeating the auto-update purpose of the loader pattern.

Document this T-7 mitigation choice in `13-02-SUMMARY.md` ("disposition: accept" with the rationale above).

**Constraints:**
- The Sentry block goes in `<head>`, NOT in `<body>` (Sentry must register `captureException` before the first app-code error can fire).
- The block uses ES5-flavored syntax (function expressions, var, no arrow functions) to mirror the existing landing.html IIFE pattern.
- **Replay disabled (review fix MEDIUM-6):** the integration array MUST contain only `Sentry.browserTracingIntegration()`. There MUST be NO `Sentry.replayIntegration({...})` call, NO `replaysSessionSampleRate` key, and NO `replaysOnErrorSampleRate` key in the init object.
- Zero raw hex in any new content (this is HTML; the only hex-like literals are in the regex patterns, which are intentional family-code matchers).
- Do NOT touch any existing HTML — additive only. Both surfaces preserve their existing `<head>` content verbatim.
- The block MUST appear BEFORE any other `<script>` that imports Firebase SDK or app.js, so Sentry's `captureException` is wired up before app-code errors can fire.

**Commit:** `feat(13-02): add Sentry loader + PII-scrubbing beforeSend (Replay disabled per review)`
  </action>

  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -c "sentryOnLoad" app.html && grep -c "sentryOnLoad" landing.html && grep -c "js.sentry-cdn.com" app.html && grep -c "js.sentry-cdn.com" landing.html && grep -c "beforeSend" app.html && grep -c "beforeSend" landing.html && grep -c "beforeBreadcrumb" app.html && grep -c "beforeBreadcrumb" landing.html && grep -c "WebChannelConnection" app.html && grep -c "FAMILY_RE" app.html && grep -c "delete event.user.id" app.html && grep -c "delete event.user.id" landing.html && grep -c "crossorigin=\"anonymous\"" app.html && grep -c "crossorigin=\"anonymous\"" landing.html && ! grep -q "replaysSessionSampleRate" app.html && ! grep -q "replaysSessionSampleRate" landing.html && ! grep -q "replaysOnErrorSampleRate" app.html && ! grep -q "replaysOnErrorSampleRate" landing.html && ! grep -q "Sentry.replayIntegration" app.html && ! grep -q "Sentry.replayIntegration" landing.html</automated>
  </verify>

  <acceptance_criteria>
    - `app.html` contains exactly 1 occurrence of literal `window.sentryOnLoad = function ()` AND exactly 1 `<script src="https://js.sentry-cdn.com/` AND literal `crossorigin="anonymous"` (T-7 mitigation) on the same script tag
    - `app.html` contains literal `delete event.user.id` (T-6 PII scrub — uid stripped from event.user.id BEFORE Sentry network send) AND literal `delete event.user.email` AND literal `delete event.user.username` AND literal `delete event.user.ip_address` (T-6 PII scrub — email/username/ip)
    - `landing.html` contains literal `delete event.user.id` (symmetric pattern across surfaces; landing has no auth so this is a no-op there but mirrors app.html for refactor symmetry)
    - `app.html` contains literal `/\b[a-f0-9]{6}\b/gi` (family-code regex) AND literal `'<FAMILY>'` AND literal `?invite=` AND literal `?claim=` AND literal `/rsvp/` (token strip patterns)
    - `app.html` contains literal `WebChannelConnection|FirebaseError|firestore\.googleapis` (Pitfall 5 — Firestore noise filter)
    - **Review fix MEDIUM-6: `app.html` does NOT contain `replaysSessionSampleRate`, does NOT contain `replaysOnErrorSampleRate`, and does NOT contain `Sentry.replayIntegration` anywhere.**
    - **Review fix MEDIUM-6: `landing.html` does NOT contain `replaysSessionSampleRate`, does NOT contain `replaysOnErrorSampleRate`, and does NOT contain `Sentry.replayIntegration` anywhere.**
    - `app.html` contains literal `(location.hostname === 'couchtonight.app')` (environment gate)
    - `landing.html` contains identical literals to all of the above (de-duplicated init block) including the same Replay-omission pattern
    - The new Sentry `<script>` tag in `app.html` appears in document order BEFORE any other `<script src=` that loads Firebase SDK / app.js — verify via reading the head section line numbers and confirming sentry block precedes the body script tags
    - The new Sentry block in `landing.html` appears AFTER the standalone-redirect IIFE closing `</script>` and BEFORE the `<script type="application/ld+json">` block — verify by line ordering
    - Both files still parse as valid HTML — verify by `grep -c "</head>" app.html` returns exactly 1 AND `grep -c "</head>" landing.html` returns exactly 1
    - DSN placeholders `<SENTRY_PUBLIC_KEY>` AND `<SENTRY_ORGID>` AND `<SENTRY_PROJECTID>` are present (a real DSN can be substituted in by the user before deploy; Plan 13-03 deploy.sh aborts production deploys if these literals remain — review fix MEDIUM-5)
  </acceptance_criteria>

  <done>Sentry loader installed in both surfaces with Replay disabled (review fix MEDIUM-6). Explicit-fields PII scrubbing (review fix MEDIUM-9 — wording softened, code unchanged) + Firestore noise filtering active. Ready for sw.js bump.</done>
</task>

<task type="auto">
  <name>Task 2: Bump sw.js CACHE for Sentry shell change</name>
  <files>
    C:\Users\nahde\claude-projects\couch\sw.js (MODIFY — bump CACHE)
  </files>

  <read_first>
    - C:\Users\nahde\claude-projects\couch\sw.js (entire file — confirm current CACHE constant; per Plan 13-02 frontmatter `wave: 2` + `depends_on: ['13-01']`, the value at task time is the post-13-01 CACHE — `couch-v33-comp-13-01-account-deletion`)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-PATTERNS.md (section "sw.js CACHE bump")
  </read_first>

  <action>
**Wave-2 sequencing note:** Plan 13-02 ships AFTER Plan 13-01 (frontmatter `wave: 2`, `depends_on: ['13-01']`). At task time, `sw.js` already contains the post-13-01 CACHE constant (e.g. `couch-v33-comp-13-01-account-deletion`). Bump deterministically to `couch-v33.1-sentry`.

**Step 2.1 — Read sw.js line 8.** Confirm the current CACHE value is `couch-v33-comp-13-01-account-deletion` (the post-13-01 state). Replace that line with:

```js
const CACHE = 'couch-v33.1-sentry';
```

If 13-01's actual landed CACHE differs from `couch-v33-comp-13-01-account-deletion` (e.g. 13-01 chose `couch-v33-account-deletion` instead), follow the same `+0.1` minor-bump convention against whatever value is actually present, but the new value MUST end with `-sentry` so this plan's intent is greppable.

**Constraints:**
- The previous CACHE string MUST NOT appear anywhere else in sw.js after this edit (only one CACHE constant declaration exists per CLAUDE.md convention).
- `node --check sw.js` MUST pass.
- The new tag MUST follow `couch-v{N}[.M]-{short-tag}` convention.
- The new value MUST be `couch-v33.1-sentry` (deterministic per the wave-2 ordering above).

**Commit:** `chore(13-02): bump sw.js CACHE to couch-v33.1-sentry for Sentry shell change`
  </action>

  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node --check sw.js && grep -c "const CACHE = 'couch-v33.1-sentry'" sw.js</automated>
  </verify>

  <acceptance_criteria>
    - `sw.js` line containing `const CACHE =` matches the literal value `'couch-v33.1-sentry'` (deterministic post-13-01 minor bump per the wave-2 ordering note in the action above)
    - `sw.js` does NOT contain the literal `couch-v33-comp-13-01-account-deletion` after this task completes (the previous-state string is fully replaced)
    - `sw.js` does NOT contain the literal `couch-v32.2-asset-cache-control` after this task completes (defense-in-depth — neither pre-13-01 nor post-13-01 prior states should remain)
    - `node --check sw.js` exits 0
  </acceptance_criteria>

  <done>sw.js CACHE bumped to couch-v33.1-sentry. Wave-2 sequencing satisfied (13-01 already merged at task time). Ready for Task 3 (TECH-DEBT entry).</done>
</task>

<task type="auto">
  <name>Task 3: Add TECH-DEBT.md entry — "Sentry Replay deferred" (review fix MEDIUM-6)</name>
  <files>
    C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md (MODIFY — append new active entry)
  </files>

  <read_first>
    - C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md (entire file — locate the "## Active" heading; new entries append at the top of the active list per the document's preamble: "new items append at the top")
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-REVIEWS.md (review fix MEDIUM-6 — the source of this entry)
  </read_first>

  <action>
**Step 3.1 — Append a new active TECH-DEBT entry.**

Per the file preamble, "new items append at the top" of the Active section. Insert this new entry immediately after the `## Active` heading and BEFORE the existing `### TD-1` entry. Renumber subsequent entries IS NOT required — TD numbering is sparse and append-style; this entry gets the next available TD-N (read the file to determine; if TD-1 through TD-N exist, this becomes TD-(N+1)). For this revision, suggest the placeholder `TD-Sentry-Replay`; the executor may renumber to a sequential TD-N if file convention requires it.

Entry body to insert (verbatim except for the TD-N number, which the executor finalizes against the actual file state):

```markdown
### TD-N. Sentry Replay deferred (post-launch +30 days)

**Severity:** low · **Effort:** 30-60 min (re-enable + privacy review writeup) · **Risk:** low (Replay is opt-in; flipping it back on doesn't break anything)

**Source:** Phase 13 / Plan 13-02 cross-AI peer review (Codex MEDIUM-6).

**Current state**
- Phase 13 / OPS-13-05 ships Sentry with Replay DISABLED for v1.
- `replaysSessionSampleRate` and `replaysOnErrorSampleRate` keys are absent from `Sentry.init()` in both `app.html` and `landing.html`.
- `Sentry.replayIntegration({...})` is NOT in the integrations array.

**Why deferred**
Codex review flagged that even masked Replay (`maskAllText: true, blockAllMedia: true`) captures DOM structure, user flows, timestamps, and event metadata. Couch is a family app — household members, photo albums, watchparty timing — and we have not done a privacy review confirming the Replay surface is acceptable for that audience. Errors-only telemetry at v1 launch covers the operational need (visibility into prod errors) without the privacy ambiguity.

**Re-enable plan (target: post-launch +30 days)**
1. Sentry inbox should have ~30 days of real-world error data; review the captured payload shape to confirm `beforeSend` actually catches the PII surfaces we expect.
2. Decide on replay sample rates:
   - Conservative: `replaysSessionSampleRate: 0` (no proactive replay) + `replaysOnErrorSampleRate: 0.1` (10% of error sessions get replay).
   - More conservative: `replaysOnErrorSampleRate: 0.05` (5% — half the originally-planned rate).
3. Add `Sentry.replayIntegration({maskAllText: true, blockAllMedia: true})` back to the integrations array in `app.html` AND `landing.html`.
4. Test: induce an error in dev, confirm replay captures with masked text + blocked images.
5. Update privacy.html if Replay's data-collection profile materially differs from the existing telemetry disclosure.

**Re-enable code diff sketch**
```javascript
Sentry.init({
  // ...existing config...
  replaysSessionSampleRate: 0,         // no proactive replay
  replaysOnErrorSampleRate: 0.05,      // 5% of error sessions
  integrations: [
    Sentry.browserTracingIntegration(),
    Sentry.replayIntegration({ maskAllText: true, blockAllMedia: true })
  ],
});
```

**Why NOT do it before launch**
Privacy posture is a launch-blocker concern; Replay's risk surface (DOM structure capture) is non-trivial and there's no rollback if a user complains post-launch. Errors-only Sentry covers the v1 operational need. Re-enable Replay deliberately with a privacy-review checklist, not as a launch task.

---
```

**Constraints:**
- Append-only — preserve all existing TECH-DEBT entries verbatim.
- The new entry MUST be in the "## Active" section.
- The TD-N number MUST be unique (highest existing TD number + 1).
- The entry MUST contain literal `Sentry Replay` AND literal `post-launch +30 days` AND literal `replaysOnErrorSampleRate` (for grep gates).

**Commit:** `docs(13-02): add TECH-DEBT entry for Sentry Replay deferral (review fix MEDIUM-6)`
  </action>

  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && grep -c "Sentry Replay" .planning/TECH-DEBT.md && grep -c "post-launch +30 days" .planning/TECH-DEBT.md && grep -c "replaysOnErrorSampleRate" .planning/TECH-DEBT.md && grep -c "MEDIUM-6" .planning/TECH-DEBT.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/TECH-DEBT.md` contains literal `Sentry Replay deferred` exactly 1 time (heading-level)
    - `.planning/TECH-DEBT.md` contains literal `post-launch +30 days`
    - `.planning/TECH-DEBT.md` contains literal `replaysOnErrorSampleRate` (the deferred-but-documented config key)
    - `.planning/TECH-DEBT.md` contains literal `MEDIUM-6` (review-finding traceability)
    - The new entry is in the `## Active` section (verify by grepping line numbers: the new heading appears between `## Active` and `## Closed` if Closed exists, OR between `## Active` and end-of-file)
    - Existing TECH-DEBT entries (TD-1 firebase-functions, TD-2 storage rules, etc.) are preserved verbatim — `grep -c "TD-1\." .planning/TECH-DEBT.md` returns the same count as before this task
  </acceptance_criteria>

  <done>TECH-DEBT entry for Sentry Replay deferral landed (review fix MEDIUM-6). Plan 13-02 complete: Sentry shipped with Replay disabled + explicit-fields PII strip + softened wording (MEDIUM-9) + TECH-DEBT trail for the post-launch re-enable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → js.sentry-cdn.com (CDN) | External script load; integrity envelope via crossorigin attribute |
| Sentry SDK → o<ORGID>.ingest.us.sentry.io | Outbound POST of error events; explicit-fields PII scrubbed by beforeSend pre-flight; replay surface absent in v1 (review fix MEDIUM-6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | I (Information disclosure) | Explicit PII fields (uid, email, username, ip_address) leaked to third party (Sentry) | mitigate | `beforeSend` hook in Task 1 explicitly deletes `event.user.id` (uid), `event.user.email`, `event.user.username`, `event.user.ip_address` BEFORE the SDK transmits. Family-code regex `/\b[a-f0-9]{6}\b/gi` catches stack-trace + message text leakage. Token regex strips `?invite=`, `?claim=`, `/rsvp/<token>` from request URLs and breadcrumb URLs. (Severity: HIGH per orchestrator T-6.) **Review fix MEDIUM-9 caveat:** this mitigates the explicit-named-fields surface only. Stack frames, request headers, tags not directly mutated, and any future Replay metadata MAY still carry identifiers. Periodic Sentry-inbox review (every ~30 days post-launch) is required to detect new identifier sources; tighten beforeSend if any surface. |
| T-13-02-02 | T (Tampering) | Sentry CDN script tampering (compromised CDN injects malicious code into our app) | accept | `crossorigin="anonymous"` envelope is the standard Couch posture for CDN scripts. SRI hash REJECTED because it would lock us to a single immutable build, defeating the loader-script auto-update pattern that docs.sentry.io explicitly recommends. Risk surface: Sentry CDN compromise → Couch ships malicious JS to all users for the duration of the compromise. Trade-off: SRI would require manual re-pinning on every Sentry SDK version bump (operationally infeasible for a solo dev). Documented disposition: accept; revisit if Sentry has a CDN incident. (Severity: MEDIUM per orchestrator T-7; downgraded to LOW operational risk.) |
| T-13-02-03 | I (Information disclosure) | Sentry breadcrumbs pick up Firestore real-time transport noise → dashboard spam masks real errors | mitigate | `beforeBreadcrumb` hook drops any breadcrumb whose message matches `/WebChannelConnection|FirebaseError|firestore\.googleapis/`. Pitfall 5 defense from RESEARCH.md. (Severity: LOW; operational not security.) |
| T-13-02-04 | I (Information disclosure) | Sentry session replay captures sensitive UI state | accept (mitigation = absence) | **Review fix MEDIUM-6: Replay is DISABLED entirely for v1.** No `Sentry.replayIntegration` in integrations array; no `replaysSessionSampleRate` or `replaysOnErrorSampleRate` keys. The Replay capture surface does not exist in v1, so this threat has no realization. Re-enable post-launch +30 days after privacy review per TECH-DEBT.md "Sentry Replay deferred"; that re-enable will require re-evaluating this threat with the conservative-sample-rate configuration documented in the TECH-DEBT entry. |
| T-13-02-05 | E (Elevation) | Inline `sentryOnLoad` script bypasses CSP if `'unsafe-inline'` is later removed from `script-src` | accept | Plan 13-05 ships CSP in REPORT-ONLY mode with `'unsafe-inline'` allowed; the Sentry init block runs cleanly. When Couch tightens CSP to enforcement (Milestone 2), this inline block must move to an external file or get a nonce. Documented as Milestone 2 follow-up in TECH-DEBT.md by Plan 13-05's owner. |
</threat_model>

<verification>
**Per-task verify** runs grep gates + `node --check sw.js` + TECH-DEBT entry presence + Replay-absence assertions. After deploy:

1. Mirror app.html + landing.html + sw.js to `${QUEUENIGHT_PATH}/public/` and `firebase deploy --only hosting --project queuenight-84044`. Plan 13-03's deploy.sh handles this AND the placeholder-DSN guard (review fix MEDIUM-5 cross-cut).
2. Curl smoke: `curl -s https://couchtonight.app/app | grep -c "sentryOnLoad"` → 1; `curl -s https://couchtonight.app/ | grep -c "sentryOnLoad"` → 1.
3. Curl smoke for Replay-absence (review fix MEDIUM-6): `curl -s https://couchtonight.app/app | grep -c "replaysSessionSampleRate"` → 0; same for replaysOnErrorSampleRate and Sentry.replayIntegration.
4. Open the Sentry dashboard inbox. Open the deployed app, force a JS error in DevTools console: `throw new Error('Phase 13-02 Sentry smoke test')` — verify the error appears in the Sentry dashboard within ~30s with NO email/uid/family code in the explicit-fields payload.
5. Force a browser console message that includes a known-fake family code like `abc123` to verify the regex strips it: `throw new Error('test family abc123 leaked')` — Sentry should show `'test family <FAMILY> leaked'`.

**Manual UAT deferred**: Firestore noise filter regression (verify no breadcrumb spam in Sentry inbox over 24h of normal use). Replay UAT is N/A in v1 (review fix MEDIUM-6 — Replay disabled).
</verification>

<success_criteria>
- All 5+ must_haves.truths verifiable via the smoke-test script above (including the Replay-absence and softened-PII-claim truths added by review fixes MEDIUM-6 and MEDIUM-9).
- Sentry inbox receives the smoke-test error with explicit PII fields redacted.
- App boot is not slowed measurably (loader script lazy-loads the SDK on first error per docs.sentry.io).
- TECH-DEBT.md has the new "Sentry Replay deferred" entry with re-enable plan.
- Phase 11 + Phase 12 features (RSVP, lobby, sports, notif prefs UI, ABOUT footer, changelog page) continue to work — no regressions from the head-script addition.
</success_criteria>

<output>
After completion, create `.planning/phases/13-compliance-and-ops/13-02-SUMMARY.md` documenting:
- Commits landed
- Files modified with diff stats
- Sentry account creation steps performed by user (DSN substituted in, project name, region) — note that Plan 13-03's deploy.sh would have aborted production deploys if placeholders remained (review fix MEDIUM-5 cross-cut verification)
- T-7 SRI-hash decision documented as "accept" with rationale
- Review fixes applied with traceable evidence: MEDIUM-6 (Replay disabled — show grep proving absence of `replaysSessionSampleRate`, `replaysOnErrorSampleRate`, `Sentry.replayIntegration` in both HTML files) and MEDIUM-9 (PII-scrub wording softened — show diff of must_haves.truths)
- TECH-DEBT entry text for "Sentry Replay deferred"
- Smoke-test outcome (Sentry inbox screenshot or curl-based dashboard probe)
- Deferred items: extract sentry-config to a shared module (Milestone 2), CSP nonce migration (when Plan 13-05 flips to enforcement), Sentry Replay re-enable (post-launch +30 days per TECH-DEBT.md)
</output>
