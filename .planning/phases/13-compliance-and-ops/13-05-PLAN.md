---
phase: 13-compliance-and-ops
plan: 05
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - C:\Users\nahde\queuenight\firebase.json
  - C:\Users\nahde\claude-projects\couch\sw.js
  - C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md
autonomous: true
requirements: [OPS-13-04]
threat_model_severity: low
user_setup: []

must_haves:
  truths:
    - "couchtonight.app/* responses include Content-Security-Policy-Report-Only header on all paths matching **/*."
    - "The CSP allows Firebase SDK + TMDB image CDN + Trakt API + Google Fonts + Sentry CDN + the queuenight Cloud Functions endpoint."
    - "Existing security headers (X-Content-Type-Options + Referrer-Policy + X-Frame-Options) are preserved verbatim — additive only."
    - "Browser console shows CSP violation reports (not blocks) for the first 2 weeks; reviewing those reports informs a future tightening pass."
    - "The 2-week observation window explicitly tests Trakt OAuth, photo upload (couch-albums), watchparty scheduling, RSVP submission, install flow, and Sentry error transmission (review fix LOW — Codex called out these flows as common-miss audit targets). Findings update TECH-DEBT.md TD-4 before the enforcement flip."
    - "TECH-DEBT.md TD-4 status flips to acknowledge that CSP shipped in Report-Only mode; enforcement deferred to Milestone 2 with rationale + audit-flow checklist."
  artifacts:
    - path: "C:\\Users\\nahde\\queuenight\\firebase.json"
      provides: "Content-Security-Policy-Report-Only header in the existing **/* headers block (additive 4th entry)"
      contains: "Content-Security-Policy-Report-Only"
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\sw.js"
      provides: "CACHE bumped"
      contains: "couch-v33"
    - path: "C:\\Users\\nahde\\claude-projects\\couch\\.planning\\TECH-DEBT.md"
      provides: "TD-4 status update reflecting CSP Report-Only ship + Milestone-2 enforcement deferral + Codex audit-flow checklist (review fix LOW)"
      contains: "Report-Only"
  key_links:
    - from: "Firebase Hosting **/* response"
      to: "Browser CSP enforcement engine"
      via: "Content-Security-Policy-Report-Only HTTP header"
      pattern: "Content-Security-Policy-Report-Only"
---

<objective>
Ship OPS-13-04: Content Security Policy in Report-Only mode. Add a `Content-Security-Policy-Report-Only` HTTP header to the existing `**/*` headers block in `queuenight/firebase.json` covering Firebase SDK + TMDB image CDN + Trakt API + Google Fonts + Sentry CDN (Plan 13-02) + queuenight Cloud Functions (Plan 13-01 callables). Start in Report-Only so violations are logged but not blocked — gives 2 weeks of console-visibility before the enforcement flip.

**Review fix incorporated (LOW):** Codex flagged that CSP directive lists commonly miss `blob:` for workers, Sentry envelope variants, Firebase Auth handler paths, Google identity endpoints, YouTube trailer embeds, and TMDB poster redirects. Report-Only mode is exactly the right tool to catch these — but the verification + the 2-week audit window must EXPLICITLY exercise the flows that are most likely to surface misses. The plan now lists those flows in `<verification>` and TD-4 audit checklist (Step 1.3), so the executor doesn't blow past the observability window without testing the right paths. The CSP also pre-allows `blob:` in `worker-src` to cover blob-URL worker spawn (a documented common miss).

Purpose: Defense-in-depth for XSS. Couch is XSS-disciplined (no `innerHTML` from user content; everything goes through `escapeHtml`), so the actual XSS surface is small — CSP further tightens the failure radius. CSP3 requires header (not meta) for Report-Only mode (RESEARCH.md anti-pattern).

Output: One additive 4th header object in firebase.json; sw.js CACHE bumped; TECH-DEBT.md TD-4 status updated with the Codex audit-flow checklist (review fix LOW).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-CONTEXT.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-RESEARCH.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-PATTERNS.md
@C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-REVIEWS.md
@C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md
@C:\Users\nahde\claude-projects\couch\CLAUDE.md

<interfaces>
<!-- Existing firebase.json **/* headers block (lines 71-78 per PATTERNS.md verification): -->
```json
{
  "source": "**/*",
  "headers": [
    { "key": "X-Content-Type-Options", "value": "nosniff" },
    { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
    { "key": "X-Frame-Options", "value": "DENY" }
  ]
}
```

<!-- Domains the CSP must allow (compiled from grep + RESEARCH.md): -->
- Sentry CDN (Plan 13-02): https://js.sentry-cdn.com, https://*.sentry-cdn.com, https://browser.sentry-cdn.com, https://*.ingest.us.sentry.io
- Firebase SDK: https://www.gstatic.com, https://*.firebaseapp.com
- Firebase services: https://*.googleapis.com, https://*.firebaseio.com, https://identitytoolkit.googleapis.com, https://securetoken.googleapis.com, https://firestore.googleapis.com, https://firebasestorage.googleapis.com, https://firebaseinstallations.googleapis.com, https://firebase.googleapis.com
- Cloud Functions: https://us-central1-queuenight-84044.cloudfunctions.net
- TMDB: https://api.themoviedb.org, https://image.tmdb.org
- Trakt: https://api.trakt.tv (OAuth flow lives elsewhere — popup window inherits parent CSP)
- Google Fonts: https://fonts.googleapis.com (CSS), https://fonts.gstatic.com (font files)
- Google user content: https://*.googleusercontent.com (avatar fallback)

<!-- TD-4 current state (from TECH-DEBT.md lines 116-129): -->
TD-4 is "Active" with severity low. Phase 12 partially closed it (XCTO + Referrer-Policy + X-Frame-Options shipped). Phase 13 / OPS-13-04 closes the bigger half (CSP) but in Report-Only mode; full enforcement is deferred to Milestone 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Content-Security-Policy-Report-Only header to queuenight/firebase.json + bump sw.js + update TECH-DEBT.md TD-4 (with Codex audit-flow checklist per review fix LOW)</name>
  <files>
    C:\Users\nahde\queuenight\firebase.json (MODIFY -- additive 4th header in existing **/* block),
    C:\Users\nahde\claude-projects\couch\sw.js (MODIFY -- CACHE bump),
    C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md (MODIFY -- TD-4 status update with audit-flow checklist)
  </files>

  <read_first>
    - C:\Users\nahde\queuenight\firebase.json (entire file -- read existing structure; locate the **/* headers block at lines 71-78 per PATTERNS.md; ALSO confirm there is no existing CSP entry that would conflict)
    - C:\Users\nahde\claude-projects\couch\sw.js (entire file -- read current CACHE; this plan is Wave 3 and 13-01 + 13-02 may have already bumped sw.js; check the actual current state at task time and bump from there)
    - C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md (entire file -- locate TD-4 entry at lines ~116-129; that's the entry to update)
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-PATTERNS.md (sections "queuenight/firebase.json (MODIFY -- CSP-Report-Only header)" + "sw.js CACHE bump" + ".planning/TECH-DEBT.md (MODIFY -- close partial entries)")
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-RESEARCH.md (section "Example 3: firebase.json CSP-Report-Only header" + "Pitfall 4: CSP report-only breaks Trakt OAuth popup" + "Anti-Patterns to Avoid")
    - C:\Users\nahde\claude-projects\couch\.planning\phases\13-compliance-and-ops\13-REVIEWS.md (review fix LOW — Codex's CSP-coverage audit-flow concerns)
    - C:\Users\nahde\claude-projects\couch\app.html (lines 1-100 — confirm whether the file has any inline `<script>` blocks that would trigger 'unsafe-inline' violations)
  </read_first>

  <action>
**Step 1.1 — Modify `C:\Users\nahde\queuenight\firebase.json`.**

Read the current file. Locate the existing `**/*` headers block (lines 71-78 per PATTERNS.md). The existing block has 3 header entries (XCTO + Referrer-Policy + X-Frame-Options); add a 4th — `Content-Security-Policy-Report-Only` — preserving the existing 3 verbatim.

Per CONTEXT.md addendum #4: HTTP header NOT meta tag (CSP3 forbids Report-Only via meta). Per RESEARCH.md "Open Questions #5" recommendation: ship report-only WITHOUT a sink (no `report-uri` directive) for v1; rely on browser console for first 2 weeks.

The full new block (replace lines 71-78 with):

```json
    {
      "source": "**/*",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "DENY" },
        {
          "key": "Content-Security-Policy-Report-Only",
          "value": "default-src 'self'; script-src 'self' 'unsafe-inline' https://js.sentry-cdn.com https://*.sentry-cdn.com https://browser.sentry-cdn.com https://www.gstatic.com https://*.firebaseapp.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://image.tmdb.org https://*.googleusercontent.com https://firebasestorage.googleapis.com; connect-src 'self' https://*.googleapis.com https://*.firebaseio.com https://firebaseinstallations.googleapis.com https://firebase.googleapis.com https://identitytoolkit.googleapis.com https://securetoken.googleapis.com https://firestore.googleapis.com https://firebasestorage.googleapis.com https://us-central1-queuenight-84044.cloudfunctions.net https://api.themoviedb.org https://image.tmdb.org https://api.trakt.tv https://*.ingest.us.sentry.io; frame-src 'self' https://queuenight-84044.firebaseapp.com https://*.firebaseapp.com; worker-src 'self' blob:; manifest-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"
        }
      ]
    }
```

**Critical compliance notes:**
- Header key MUST be `Content-Security-Policy-Report-Only` (NOT `Content-Security-Policy`) per CONTEXT.md addendum #4.
- NO `report-uri` directive (per Open Questions #5 — ship sink-less for v1).
- `'unsafe-inline'` in `script-src` is the Couch-specific weakening documented in RESEARCH.md "Note on 'unsafe-inline' in script-src".
- **Review fix LOW:** `worker-src 'self' blob:` — `blob:` added because service workers and Firebase SDK can spawn worker scripts from blob URLs (Codex noted this is a common miss).
- All 3 existing headers preserved verbatim.
- Result MUST be valid JSON: verify via `node -e "JSON.parse(require('fs').readFileSync('C:/Users/nahde/queuenight/firebase.json','utf8'))"` exits 0.

**Step 1.2 — Modify `C:\Users\nahde\claude-projects\couch\sw.js`.**

Read the current CACHE constant. This plan is Wave 3 — Plan 13-01 set `couch-v33-comp-13-01-account-deletion`, and Plan 13-02 set `couch-v33.1-sentry`. This plan's bump goes to `couch-v33.2-csp`.

Use a single-line replacement with the form:
```javascript
const CACHE = 'couch-v33.2-csp';
```

If the actual prior CACHE differs, follow the same `+0.1` minor-bump convention against whatever value is present, but the new value MUST end with `-csp` so this plan's intent is greppable.

**Step 1.3 — Modify `C:\Users\nahde\claude-projects\couch\.planning\TECH-DEBT.md`.**

Locate the TD-4 entry at lines ~116-129. Update the entry to reflect Phase 13 / OPS-13-04 closure of the CSP half. Keep TD-4 active (since enforcement is deferred), but flip the wording AND add the Codex audit-flow checklist per review fix LOW.

Replace the existing TD-4 body with this updated version (keep the heading line `### TD-4. CSP / security-header coverage` and the severity line):

```markdown
### TD-4. CSP / security-header coverage

**Severity:** low · **Effort:** 30-60 min for the enforcement flip · **Risk:** medium (could break flows we haven't audited if CSP is enforced before fixing inline-script reliance)

**Status (post-Phase-13):** Partial close — CSP shipped in Report-Only mode; enforcement deferred.

**What shipped:**
- Phase 12: XCTO + Referrer-Policy + X-Frame-Options (3 quick-win headers).
- Phase 13 / OPS-13-04: Content-Security-Policy-Report-Only header on `**/*` covering Firebase SDK + TMDB + Trakt + Google Fonts + Sentry CDN + Cloud Functions endpoint. Browser logs violations to console for solo-dev introspection. No report-uri sink (deferred per RESEARCH.md Open Question #5).
- Phase 13 / OPS-13-04: Strict-Transport-Security inherited from Firebase Hosting default (was already live).
- Phase 13 / OPS-13-04: `worker-src 'self' blob:` allows blob-URL workers (review fix LOW — common miss).

**Codex audit-flow checklist (review fix LOW — exercise these during the 2-week observation window):**

The following flows are the most likely to surface CSP violations that the initial directive list missed. Check the browser console after each:

1. **Trakt OAuth round-trip** — open Account → Trakt sync → start OAuth → complete in popup → return to Couch. Watch for `frame-src` or `connect-src` violations on the Trakt callback.
2. **Photo upload to couch-albums** — start a watchparty session → upload a photo. Watch for `connect-src` violations against `firebasestorage.googleapis.com` or any redirect chains.
3. **Watchparty scheduling** — schedule a watchparty (intent → CF write → push). Watch for `connect-src` violations against the cloudfunctions.net endpoint or push-notification SDK.
4. **RSVP submit** — submit an RSVP from the rsvp.html surface. Watch for `connect-src` violations on the rsvp callable CF.
5. **Install flow** — install Couch as a PWA from a fresh browser. Watch for `manifest-src`, `worker-src` (sw.js registration), and `script-src` violations during the install handshake.
6. **Sentry error transmission** — induce a JS error. Watch for `connect-src` violations on `*.ingest.us.sentry.io` envelope endpoints (Sentry occasionally adds new ingest hostnames; Report-Only is the early-warning).
7. **TMDB poster load + CDN redirects** — browse the catalog. Watch for `img-src` violations on `image.tmdb.org` redirect chains.
8. **YouTube trailer embed (if used)** — Couch v1 doesn't embed YouTube trailers; if a future change does, audit `frame-src` for `https://www.youtube.com` and `https://www.youtube-nocookie.com`.

After 2 weeks, summarize the violation set in 13-05-SUMMARY.md (or a follow-up RETROSPECTIVE entry) and decide on the enforcement-flip plan based on the actual data.

**What still needs work (the enforcement flip):**
1. **Drop `'unsafe-inline'` from `script-src`.** Couch has multiple inline `<script>` blocks: Sentry `sentryOnLoad` config (app.html + landing.html), landing.html standalone-redirect IIFE, inline PWA manifest data URL. Options: move each inline script to an external `js/<name>.js` file (cheapest; recommended for Milestone 2), OR add nonce-based whitelisting (requires server-side per-request nonce — not supported by Firebase Hosting).
2. **Drop `'unsafe-inline'` from `style-src`.** Smaller surface — most Couch styling is in css/app.css. Audit any inline `style=""` attributes.
3. **Optional: add a report-uri sink** (Cloudflare Worker per RESEARCH.md "Don't Hand-Roll"; ~30 min; only if console review proves insufficient).
4. **Add Permissions-Policy header** (camera/microphone/geolocation: `()`) — defense-in-depth for the small permission surface Couch uses.

**Path to enforcement:**
- Watch the browser console for 2 weeks of CSP violation reports — exercise the Codex audit-flow checklist above weekly.
- Fix the inline-script allowance.
- Flip the header key from `Content-Security-Policy-Report-Only` to `Content-Security-Policy` in `queuenight/firebase.json`.
- Deploy + monitor.

**Why we don't enforce on day 1:** Couch has 11+ inline `<script type="module">` blocks in app.html that no one has stress-tested against a strict policy. Trakt OAuth callback handler also has inline messaging. Report-Only mode FLAGS these without breaking — that's the value (RESEARCH.md "Anti-Pattern: Ship CSP enforcement (not Report-Only) on day 1").
```

The wording above is illustrative; the executor may rephrase as long as the substance is preserved: TD-4 stays active, status is "Partial close — Report-Only shipped; enforcement deferred", concrete next-steps are documented, AND the Codex audit-flow checklist (review fix LOW) is included.

**Constraints:**
- The CSP value MUST be a single-line JSON string (no embedded newlines).
- All 3 existing security headers preserved verbatim.
- `node -e "JSON.parse(...)"` MUST exit 0 on the modified firebase.json.
- `node --check sw.js` MUST pass.
- TD-4 entry MUST still exist and be marked Active.
- **Review fix LOW:** TD-4 MUST contain the Codex audit-flow checklist with at least the 7 active flows (Trakt OAuth, photo upload, watchparty schedule, RSVP submit, install flow, Sentry, TMDB; YouTube embed listed as future-only).
- **Review fix LOW:** the CSP value MUST contain `worker-src 'self' blob:` (blob-URL worker allowance).

**Commit:** `feat(13-05): add CSP Report-Only header + sw.js bump + TD-4 status update with Codex audit-flow checklist`
  </action>

  <verify>
    <automated>cd C:/Users/nahde/queuenight && node -e "JSON.parse(require('fs').readFileSync('firebase.json','utf8'))" && grep -c "Content-Security-Policy-Report-Only" firebase.json && grep -c "X-Content-Type-Options" firebase.json && grep -c "Referrer-Policy" firebase.json && grep -c "X-Frame-Options" firebase.json && grep -c "js.sentry-cdn.com" firebase.json && grep -c "image.tmdb.org" firebase.json && grep -c "api.trakt.tv" firebase.json && grep -c "us-central1-queuenight-84044.cloudfunctions.net" firebase.json && grep -c "worker-src 'self' blob:" firebase.json && cd /c/Users/nahde/claude-projects/couch && node --check sw.js && grep -E "const CACHE = 'couch-v33" sw.js && grep -c "Report-Only" .planning/TECH-DEBT.md && grep -c "audit-flow checklist" .planning/TECH-DEBT.md && grep -c "Trakt OAuth round-trip" .planning/TECH-DEBT.md</automated>
  </verify>

  <acceptance_criteria>
    - `C:\Users\nahde\queuenight\firebase.json` is valid JSON (parse exits 0)
    - `firebase.json` contains exactly 1 occurrence of literal `"Content-Security-Policy-Report-Only"` AND zero occurrences of literal `"Content-Security-Policy":` (the enforcement header — must NOT be present in v1; we're Report-Only only)
    - `firebase.json` STILL contains literal `"X-Content-Type-Options"` AND `"Referrer-Policy"` AND `"X-Frame-Options"` (Phase 12 headers preserved)
    - `firebase.json` CSP value contains literal `default-src 'self'` AND literal `script-src` AND literal `style-src` AND literal `img-src` AND literal `connect-src` AND literal `frame-src` AND literal `worker-src` AND literal `manifest-src` AND literal `base-uri 'self'` AND literal `form-action 'self'` AND literal `frame-ancestors 'none'`
    - **Review fix LOW:** `firebase.json` CSP value contains literal `worker-src 'self' blob:` (blob-URL worker allowance — Codex common-miss audit fix)
    - `firebase.json` CSP value contains all required allow-list entries: `https://js.sentry-cdn.com`, `https://*.sentry-cdn.com`, `https://browser.sentry-cdn.com`, `https://www.gstatic.com`, `https://*.firebaseapp.com`, `https://fonts.googleapis.com`, `https://fonts.gstatic.com`, `https://image.tmdb.org`, `https://*.googleusercontent.com`, `https://firebasestorage.googleapis.com`, `https://*.googleapis.com`, `https://*.firebaseio.com`, `https://firestore.googleapis.com`, `https://identitytoolkit.googleapis.com`, `https://securetoken.googleapis.com`, `https://us-central1-queuenight-84044.cloudfunctions.net`, `https://api.themoviedb.org`, `https://api.trakt.tv`, `https://*.ingest.us.sentry.io`, `https://queuenight-84044.firebaseapp.com`
    - `firebase.json` CSP value does NOT contain a `report-uri` directive (per Open Question #5 — sink-less v1)
    - `sw.js` line containing `const CACHE =` matches the regex `couch-v33(\.[0-9]+)?-` (any v33.X variant accepted; the new CACHE differs from whatever value was there before this task)
    - `node --check sw.js` exits 0
    - `.planning/TECH-DEBT.md` TD-4 entry still exists (heading `### TD-4. CSP / security-header coverage` is present)
    - `.planning/TECH-DEBT.md` TD-4 entry contains literal `Report-Only` AND literal `enforcement deferred` AND references `'unsafe-inline'` as the documented enforcement-flip blocker
    - **Review fix LOW:** `.planning/TECH-DEBT.md` TD-4 entry contains literal `audit-flow checklist` AND literal `Trakt OAuth round-trip` AND literal `Photo upload to couch-albums` AND literal `Watchparty scheduling` AND literal `RSVP submit` AND literal `Install flow` AND literal `Sentry error transmission` AND literal `TMDB poster load`
  </acceptance_criteria>

  <done>CSP Report-Only header live in firebase.json (with worker-src blob: per review fix LOW). sw.js bumped. TD-4 status reflects Phase 13 closure with Codex audit-flow checklist (review fix LOW). Plan 13-05 ready for executor verification + deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Firebase Hosting CDN response | CSP-Report-Only header attached to every response on **/* paths |
| Browser CSP engine -> third-party origins | Only the explicit allow-list domains can be loaded as scripts/styles/images/connect endpoints |
| Browser -> console (no report-uri sink) | Violations logged client-side only for v1; sink deferred per RESEARCH.md Open Question #5 |
| Browser worker boot path | `worker-src 'self' blob:` allows blob-URL workers (review fix LOW — Firebase SDK + sw.js registration occasionally use blob URLs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | T (Tampering) / E (Elevation) | Inline-script XSS via reflected user content | accept | (Per orchestrator T-9, severity LOW.) Report-only mode means no actual mitigation in v1; the value of this plan is OBSERVABILITY (we learn what would be blocked). Couch's XSS-discipline (escapeHtml on all user content per CLAUDE.md "✓ XSS-disciplined") + the Phase 12 X-Frame-Options + Referrer-Policy headers are the actual XSS defenses today. CSP report-only stages the enforcement flip for Milestone 2. Documented in TECH-DEBT.md TD-4. |
| T-13-05-02 | I (Information disclosure) | CSP report-uri sink could leak request paths to a third party | accept | We're not adding a sink in v1 (Open Question #5). If we later add a Cloudflare Worker sink, the URLs sent are the violating page URL — already redacted of tokens by the Sentry beforeSend regex (cross-cuts with Plan 13-02 PII strip). |
| T-13-05-03 | D (Denial of service) | Misconfigured CSP breaks Firebase SDK / TMDB / Trakt / Sentry | mitigate | Report-Only mode means violations are LOGGED, not blocked. App functionality is unaffected. **Review fix LOW:** the 2-week observation window now has an explicit audit-flow checklist (Trakt OAuth, photo upload, watchparty schedule, RSVP submit, install flow, Sentry, TMDB) so misses surface during testing rather than enforcement-flip. |
| T-13-05-04 | T (Tampering) | Trakt OAuth popup script can't message back if CSP blocks postMessage from popup origin | accept | (Per RESEARCH.md Pitfall 4.) Report-Only doesn't break this. The Trakt OAuth round-trip is item #1 in the Codex audit-flow checklist (review fix LOW) so any actual block at enforcement-flip time is caught during the observation window. |
| T-13-05-05 | D (Denial of service) | Worker / sw.js registration breaks under enforced CSP because blob: URL not allowed (review concern LOW) | mitigate | **Review fix LOW:** `worker-src 'self' blob:` pre-allowed in v1 directive. Firebase SDK occasionally uses blob-URL workers; sw.js registration uses self-origin; both paths are covered. The Install flow is item #5 in the audit-flow checklist for verification. |
</threat_model>

<verification>
**Per-task verify** runs JSON parse + grep gates + `node --check sw.js` + audit-flow checklist assertions (review fix LOW).

**After deploy:**
1. Mirror sw.js to `${QUEUENIGHT_PATH}/public/` then `firebase deploy --only hosting --project queuenight-84044` (the firebase.json change deploys with hosting; Plan 13-03 deploy.sh handles the mirror + the Sentry placeholder guard).
2. Curl smoke: `curl -sI https://couchtonight.app/ | grep -i 'content-security-policy-report-only'` → should show the CSP header value.
3. Curl smoke for blob worker allowance (review fix LOW): `curl -sI https://couchtonight.app/ | grep -i "worker-src 'self' blob:"` → should match.
4. Open the deployed app in DevTools. Console tab should show CSP violation reports for any inline scripts that don't yet match an allowance — these are FYI, not breaks.
5. **Review fix LOW: 2-week audit window — explicitly exercise these flows and watch the console:**
   - Trakt OAuth round-trip (sync from Account)
   - Photo upload via watchparty couch-albums
   - Watchparty scheduling end-to-end
   - RSVP submit via rsvp.html
   - PWA install from a fresh browser
   - Force a JS error (Sentry transmission test)
   - Browse catalog (TMDB image redirect chains)
6. After 2 weeks: review the violation set, decide on enforcement-flip plan (TECH-DEBT.md TD-4).

**Cross-plan smoke:** This plan ships AFTER (or in parallel with) Plan 13-02 Sentry. Verify Sentry's CDN load is allow-listed by the new CSP — `js.sentry-cdn.com` MUST be in `script-src` and `*.ingest.us.sentry.io` MUST be in `connect-src`. The acceptance criteria above enforce this.
</verification>

<success_criteria>
- All 6 must_haves.truths verifiable via the curl smoke-test + audit-flow exercise.
- No app functionality broken (Report-Only).
- All 3 existing Phase 12 headers preserved.
- TECH-DEBT.md TD-4 status reflects the partial close with Codex audit-flow checklist (review fix LOW).
- All Plan 13-02 Sentry domains are allow-listed in script-src + connect-src (cross-plan integration).
- Cloud Functions endpoint (us-central1-queuenight-84044.cloudfunctions.net) is allow-listed in connect-src so Plan 13-01 callables work.
- `worker-src 'self' blob:` covers blob-URL worker spawn (review fix LOW).
- Phase 11 RSVP / Phase 12 changelog page / landing page all continue to function (no regressions from the new header).
</success_criteria>

<output>
After completion, create `.planning/phases/13-compliance-and-ops/13-05-SUMMARY.md` documenting:
- Commit landed (firebase.json + sw.js + TECH-DEBT.md)
- Curl output of the CSP header from prod
- Initial violation report count from browser console (after deploy + 5 minutes of normal use)
- TD-4 status update text (paste the new TD-4 body INCLUDING the Codex audit-flow checklist per review fix LOW)
- All 5 STRIDE threat dispositions (T-13-05-05 added per review fix LOW for blob worker allowance)
- Audit-flow exercise log (which flows have been tested in the first 5 minutes; remaining flows scheduled across the 2-week observation window)
- Deferred items: enforcement flip (Milestone 2), report-uri sink (Milestone 2 if console review insufficient), Permissions-Policy header (Milestone 2; documented in TD-4)
</output>
