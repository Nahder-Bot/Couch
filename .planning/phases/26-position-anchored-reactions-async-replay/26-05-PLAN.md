---
phase: 26-position-anchored-reactions-async-replay
plan: 05
type: execute
wave: 5
depends_on:
  - 26-01
  - 26-02
  - 26-03
  - 26-04
files_modified:
  - scripts/smoke-position-anchored-reactions.cjs
  - .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md
  - sw.js
autonomous: true
requirements:
  - RPLY-26-17
  - RPLY-26-18
tags:
  - deploy
  - cache-bump
  - smoke-floor
  - human-uat
  - phase-close
must_haves:
  truths:
    - "scripts/smoke-position-anchored-reactions.cjs ships a meta-assertion that fails the gate when total assertion count < 13 (UI-SPEC §7 floor — RPLY-26-17)"
    - "scripts/smoke-position-anchored-reactions.cjs ships ≥40 assertions in total (Plans 01-04 sum + Plan 05 self-assertion = well above the 13-floor; researcher recommended ≥25)"
    - "After production deploy via `bash scripts/deploy.sh 38-async-replay`, sw.js source line 8 reads `const CACHE = 'couch-v38-async-replay';` (auto-bump per CLAUDE.md deploy convention) — RPLY-26-18"
    - "After production deploy, curl https://couchtonight.app/sw.js shows the new cache name in the response body"
    - ".planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md scaffolds ≥10 device-UAT scripts per VALIDATION.md Manual-Only Verifications matrix; mirrors Phase 18 / 19 / 24 HUMAN-UAT pattern"
    - "Cross-repo gate verification: firestore.rules NOT touched (single-repo couch-only deploy per D-07); the smoke firestore.rules sentinel from Plan 01 still passes after deploy; queuenight repo unchanged"
    - "Phase 26 ships end-to-end: schema additions + replay variant + Past parties extension + new title-detail section + smoke gate green + CACHE bump live + UAT scripts scaffolded"
  artifacts:
    - path: "scripts/smoke-position-anchored-reactions.cjs"
      provides: "Self-asserting smoke floor (≥13 assertions, fails if count drops below)"
      contains: "RPLY-26-17"
    - path: ".planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md"
      provides: "≥10 device-UAT scripts mirroring Phase 18/19/24 pattern; pending real-device verification post-deploy"
      min_lines: 80
    - path: "sw.js"
      provides: "CACHE bumped to couch-v38-async-replay (auto-bumped by deploy.sh during deploy command)"
      contains: "couch-v38-async-replay"
  key_links:
    - from: "scripts/smoke-position-anchored-reactions.cjs"
      to: "Total assertions floor"
      via: "self-reporting + meta-assertion that fails when N < 13"
      pattern: "Total assertions:"
    - from: "sw.js (post-deploy)"
      to: "couch-v38-async-replay"
      via: "deploy.sh §4 sed auto-bump"
      pattern: "couch-v38-async-replay"
---

<objective>
Close out Phase 26: lock the smoke floor sentinel, deploy the new cache version end-to-end via `bash scripts/deploy.sh 38-async-replay`, scaffold the HUMAN-UAT script set so the user can run device-UAT post-ship.

Purpose: Plans 01-04 built the entire feature surface + smoke gate. Plan 05 adds the meta-assertion that protects the smoke floor against future regressions (RPLY-26-17), runs the production deploy ritual (which auto-bumps `sw.js` CACHE per CLAUDE.md), and authors the UAT scaffold so the user has clear scripts to run on real devices. Same pattern as Plan 18-04 / 19-03 / 20-03 / 24-04 phase-close plans.

Output: smoke contract gets a self-asserting `Total assertions ≥ 13` meta-check (RPLY-26-17); production deploy runs successfully (sw.js auto-bumped to `couch-v38-async-replay`; production curl-verified); `26-HUMAN-UAT.md` scaffolds the 10 manual verifications from VALIDATION.md (replay-modal entry from both surfaces, scrubber drag + reaction fade-in at known position, compound-reaction posts at correct position, hide-when-empty deploy-day silence, Wait Up disabled in replay, video player render-no-auto-start, drift tolerance ±2s feel, scrub-backward persistence, post-deploy CACHE active).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-CONTEXT.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-RESEARCH.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-UI-SPEC.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-VALIDATION.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-01-SUMMARY.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-02-SUMMARY.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-03-SUMMARY.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-04-SUMMARY.md
@.planning/phases/24-native-video-player/24-04-SUMMARY.md
@.planning/phases/24-native-video-player/24-HUMAN-UAT.md
@.planning/phases/19-kid-mode/19-HUMAN-UAT.md
@.planning/phases/18-availability-notifications/18-04-SUMMARY.md
@CLAUDE.md
@sw.js
@scripts/deploy.sh

<interfaces>
<!-- All upstream context — Plans 01-04 ship the schema + UI; Plan 05 ships the deploy + UAT. -->

From Plans 01-04: scripts/smoke-position-anchored-reactions.cjs at ~38-43 assertions; all 22 of 24 RPLY-26-* IDs codified; renderPastParties + renderPastWatchpartiesForTitle entry points wired with mode:'revisit'.

CLAUDE.md deploy convention: `bash scripts/deploy.sh <short-tag>` auto-bumps `sw.js` line 8 const CACHE via `sed`. Phase 26 short-tag locked per RESEARCH §CACHE Bump Tag Proposal: `38-async-replay` produces `couch-v38-async-replay`.

Pre-Phase-26 sw.js CACHE: `couch-v37-native-video-player` (Phase 24's deploy 2026-05-01).

User feedback recorded in MEMORY.md: "For Couch project, Claude may run firebase deploy to queuenight-84044 without per-deploy approval" — applies to this plan's deploy task.

Phase 24 Plan 04 deploy ritual (last reference for the close-out shape): `bash scripts/deploy.sh 37-native-video-player` triggers smoke gate (now 9 contracts including Phase 26's), mirror to ../../couch-deploy/public, firebase deploy --only hosting, then curl-verify the new CACHE.

VALIDATION.md Manual-Only Verifications matrix (lines 87-101) — 10 device-UAT items the HUMAN-UAT.md scaffolds:
1. Replay-modal entry from Past parties tap (RPLY-26-08, 04)
2. Replay-modal entry from title-detail tap (RPLY-26-11, 04)
3. Scrubber drag + reaction fade-in at known position (RPLY-26-05, DRIFT)
4. Compound-reaction posts to Firestore at correct position (RPLY-26-07)
5. Hide-when-empty surfaces / deploy-day silence (RPLY-26-06, 09)
6. Wait Up disabled in replay (RPLY-26-14)
7. Video player renders but does NOT auto-start (RPLY-26-16)
8. Drift tolerance ±2s feel (RPLY-26-DRIFT)
9. Scrub-backward preserves shown reactions (UI-SPEC §3 persistence)
10. Post-deploy `couch-v38-async-replay` CACHE active (RPLY-26-18)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: Add smoke-floor meta-assertion (RPLY-26-17)</name>
  <read_first>
    - Read(scripts/smoke-position-anchored-reactions.cjs, full file — Plan 04 output) — locate the `// ===== Final report =====` block at the end; Plan 05 inserts the floor meta-assertion immediately BEFORE that block
    - 26-VALIDATION.md row RPLY-26-17 (meta-assertion: smoke self-reports total + exits 1 if N < 13)
    - 26-RESEARCH.md §Phase 26 Smoke Contract Specification Summary line 925 (assertion floor lock)
  </read_first>
  <behavior>
    - The smoke file's final report block is preceded by a meta-assertion: if `total < 13`, mark a FAIL with a clear "BELOW UI-SPEC §7 FLOOR" message
    - Smoke output's last line still shows `Total assertions: N; Failures: F` for legibility
    - When N is well above 13 (which it will be, ~40+ after Plans 01-04), the meta-assertion is silently green and counts itself as one passing assertion
    - The smoke file STILL exits 0 when there are zero failures
  </behavior>
  <action>
    Edit `scripts/smoke-position-anchored-reactions.cjs`. Find the existing `// ===== Final report =====` block near the end of the file (it currently contains `console.log('---'); console.log('Total assertions: ...'); if (fails) { ... } else { ... }`).

    Insert this block IMMEDIATELY BEFORE that block:

    ```javascript
      // ===== Plan 05 — RPLY-26-17 floor meta-assertion =====
      // UI-SPEC §7 locks the floor at 13 assertions (5 helper-behavior + 6 production-code
      // sentinels + 2 replay-list filter). This meta-assertion fails the deploy gate if a
      // future change accidentally drops the total below the floor.
      const PHASE_26_ASSERTION_FLOOR = 13;
      if (total < PHASE_26_ASSERTION_FLOOR) {
        failMsg('Phase 26 smoke meets UI-SPEC §7 floor (>=' + PHASE_26_ASSERTION_FLOOR + ' assertions) (RPLY-26-17)',
          'total=' + total + ' is below floor of ' + PHASE_26_ASSERTION_FLOOR);
      } else {
        pass('Phase 26 smoke meets UI-SPEC §7 floor (>=' + PHASE_26_ASSERTION_FLOOR + ' assertions) (RPLY-26-17) — total=' + total);
      }
    ```

    PRESERVE the existing `// ===== Final report =====` block exactly (the `console.log('---')`, the total/failures line, the `if (fails)` exit logic, the trailing `})().catch(err => { ... })` IIFE wrapper). Do NOT modify those.
  </action>
  <verify>
    <automated>node scripts/smoke-position-anchored-reactions.cjs > /tmp/smoke-out.txt 2>&1 && cat /tmp/smoke-out.txt | grep -E "Total assertions:|RPLY-26-17" && grep -c "RPLY-26-17" /tmp/smoke-out.txt | awk '{ if ($1 < 1) { print "FAIL no RPLY-26-17 mention"; exit 1 } else { print "OK floor meta-assertion present" } }'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-position-anchored-reactions.cjs` exits 0
    - Smoke output contains a line matching `OK ` followed by `Phase 26 smoke meets UI-SPEC §7 floor` followed by `RPLY-26-17` (the floor meta-assertion is itself counted)
    - Smoke output's final line shows `Total assertions: N; Failures: 0` where N is now Plan 04's count + 1 (≥39)
    - The smoke file's existing final-report block is unchanged
    - Re-running with a synthetically-corrupted assertion count would trigger the FAIL path (manual sanity check — not required to actually demonstrate; the predicate is `total < PHASE_26_ASSERTION_FLOOR` which is unambiguous)
  </acceptance_criteria>
  <done>RPLY-26-17 floor sentinel codified; smoke output reports total + meta-asserts floor; future regressions that drop assertion count below 13 will fail the deploy gate.</done>
</task>

<task type="auto">
  <name>Task 5.2: Author 26-HUMAN-UAT.md scaffold (10 device-UAT scripts per VALIDATION.md matrix)</name>
  <read_first>
    - Read(.planning/phases/24-native-video-player/24-HUMAN-UAT.md, full file) — Phase 24's UAT scaffold pattern (closest analog: ships 11 scripts + cache verification + visual checks)
    - Read(.planning/phases/19-kid-mode/19-HUMAN-UAT.md, full file) — Phase 19's pattern (8 scripts; mirrors smaller-feature shape)
    - 26-VALIDATION.md Manual-Only Verifications matrix (lines 87-101) — the 10 items to scaffold
    - 26-CONTEXT.md § Specifics (first-week-after-deploy framing — the deploy-day-empty-state UAT is real and important)
    - 26-UI-SPEC.md §1 (DOM placement diagram for visual recognition during UAT)
  </read_first>
  <behavior>
    - File created at `.planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md` (≥80 lines)
    - Contains 10 numbered UAT scripts mirroring the Phase 24 pattern (each script: title, what to test, steps, expected outcome, acceptance criterion, related RPLY-26-* IDs)
    - Resume signal at the top: `When complete, reply: "uat passed" → triggers /gsd-verify-work 26`
    - Each script is executable on a real iOS PWA device by the user without further explanation
    - Includes a section noting the deploy-day-empty-state expectation (Past parties surface + title-detail Past watchparties section will be EMPTY on deploy day for the existing family — this is correct per D-10 + D-11 + first-week framing; UAT for these surfaces requires creating a NEW watchparty after deploy and then posting reactions then archiving)
    - Includes a final CACHE-verification line item that the user runs via curl OR via PWA inspect
  </behavior>
  <action>
    Use the Write tool to create `.planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md` with the following content (long-form scaffold matching Phase 24 / 19 patterns):

    ```markdown
    ---
    phase: 26-position-anchored-reactions-async-replay
    type: human-uat
    status: pending-device-verification
    deployed: 2026-05-01  # update to actual deploy date during Task 5.3
    cache: couch-v38-async-replay
    resume_signal: "uat passed"
    ---

    # Phase 26 — Human-UAT scripts

    > Authored 2026-05-01 by Plan 26-05.
    > Resume signal when complete: reply `uat passed` → triggers `/gsd-verify-work 26`.
    > Mirrors the Phase 18 / 19 / 24 device-UAT scaffold pattern.
    >
    > **Deploy-day expectation:** the Past parties surface (Tonight tab inline link)
    > AND the title-detail "Past watchparties" section will be **empty for the existing
    > family on deploy day** because Phase 26 is a clean demarcation (D-11 — no backfill
    > of pre-Phase-26 reactions). This is the correct silent-UX behavior per D-10 +
    > first-week-after-deploy framing in CONTEXT specifics. To verify the surfaces
    > positively, scripts 5 + 1-4 require creating a NEW watchparty AFTER deploy, posting
    > Phase 26 reactions, archiving the wp (or waiting for auto-archive at WP_ARCHIVE_MS
    > age), and then revisiting via the Past parties row tap.

    ---

    ## Pre-UAT setup

    - **Device:** iPhone with Couch installed as PWA on home screen (primary surface).
    - **Backup device:** any second device signed in to the same family (for two-device
      verification of compound-reaction visibility — script 4).
    - **Force-reload PWA once before starting** so the new `couch-v38-async-replay`
      service-worker activates: hard-quit the PWA, re-launch from home-screen icon, then
      verify Settings shows the new cache via the existing about-screen footer (or via
      script 10 below).

    ## UAT scripts

    ### Script 1 — Replay-modal entry from Past parties tap (RPLY-26-08, RPLY-26-04)

    **What to test:** Tapping a row in the Past parties surface opens the replay variant
    of the live-watchparty modal (scrubber strip + REVISITING eyebrow + *together again*
    sub-line — NOT the live-mode chrome).

    **Setup (one-time, requires a freshly-archived Phase 26 wp — see Pre-UAT note):**
    1. On host device: start a watchparty for any title.
    2. Post 2-3 reactions during the wp (any emoji from the existing reaction picker).
    3. Either wait for the wp to auto-archive (age ≥ WP_ARCHIVE_MS) OR end the wp
       manually.
    4. Wait for the snapshot to update (~2-3 seconds) so the wp transitions to
       `status: 'archived'` in Firestore.

    **Steps:**
    1. Open the Tonight tab. Look for the inline link `Past parties (1) ›` below the
       active-watchparties banner area.
    2. Tap the link.
    3. The Past parties modal opens. Confirm the row for the freshly-archived wp shows:
       title, friendly date (`Started N hr ago` if < 24h, else weekday/date ladder),
       `N on the couch`, `N reactions` (3rd subtitle line — NEW in Phase 26).
    4. Tap the row.
    5. The Past parties modal closes AND the live-watchparty modal opens in REPLAY
       VARIANT.

    **Expected:** The live-modal chrome reads `REVISITING` in the eyebrow slot (NOT
    `LIVE` / `WATCHING`), the italic-serif sub-line *together again* sits below the
    title, the scrubber strip renders ABOVE the coordination header. The advisory
    live-mode timer + Wait Up chip strip + participants strip are all hidden.

    **Pass criterion:** All chrome deltas listed above are visible. Resume signal:
    `script 1 pass` (or describe any visual mismatch).

    ---

    ### Script 2 — Replay-modal entry from title-detail tap (RPLY-26-11, RPLY-26-04)

    **What to test:** A NEW section `Past watchparties` appears in the title-detail view
    when the family has ≥1 archived wp with replay-able reactions for that title.
    Tapping a row enters the replay variant.

    **Setup:** Same as script 1 — requires ≥1 freshly-archived Phase 26 wp.

    **Steps:**
    1. Open the title-detail view for the same title used in script 1's wp.
    2. Scroll down to find the section `Past watchparties` (NEW Phase 26 — distinct
       from the existing `Watchparties` heading which is now active-only).
    3. Confirm the section has the italic-serif sub-line *Catch up on what the family
       said.*
    4. Confirm the row shows poster + title + friendly-date subtitle + `N on the couch
       · N reactions` middle-dot line.
    5. Tap the row.
    6. Title-detail modal closes; replay variant of live-watchparty modal opens.

    **Expected:** Same replay variant chrome as script 1. The `Past watchparties` section
    does NOT render at all when the family has zero archived wps with replay-able
    reactions for the title (D-10 silent UX — verify this by visiting the detail view
    for a title the family has never watched).

    **Pass criterion:** Section renders + tap enters replay variant + empty-state silent
    behavior verified. Resume signal: `script 2 pass`.

    ---

    ### Script 3 — Scrubber drag + reaction fade-in at known position (RPLY-26-05, RPLY-26-DRIFT)

    **What to test:** Dragging the scrubber to a known reaction's `runtimePositionMs`
    causes the reaction to fade in within the ±2s drift window.

    **Setup:** Use the wp from script 1 (must have ≥1 reaction with a known
    `runtimePositionMs` — for an MP4/YouTube wp the reaction will have
    `runtimeSource: 'broadcast'` if posted while the player was active OR `'elapsed'`
    if posted without a player; either is fine for this script).

    **Steps:**
    1. Open the wp in replay variant via script 1 entry path.
    2. Note the scrubber starts at `0:00`.
    3. Drag the scrubber thumb to a position you BELIEVE is near a known reaction's
       runtime position. (You can check Firestore directly via the existing dev tools
       to find an actual `runtimePositionMs` value if needed — e.g., `wp.reactions[0].runtimePositionMs`.)
    4. Release the thumb.
    5. Within ~2 seconds (the drift tolerance), the reaction fades in at the bottom of
       the feed.

    **Expected:** Reaction fades in smoothly with the existing reaction-row recipe.
    Visually feels neither premature nor late.

    **Pass criterion:** Drift tolerance "felt right" — reaction appears within 2s of
    the scrubber dropping near its position. Resume signal: `script 3 pass` or
    describe perceived timing issue.

    ---

    ### Script 4 — Compound-reaction posts to Firestore at correct position (RPLY-26-07)

    **What to test:** Posting a reaction in replay mode stamps it with
    `runtimeSource: 'replay'` AND `runtimePositionMs` equal to the local replay clock
    position at post time. Future replayers see it at that position.

    **Setup:** Use the wp from script 1. You'll need access to either Firestore console
    OR a developer tools view to inspect the `wp.reactions[]` array shape after posting.

    **Steps:**
    1. Open the wp in replay variant.
    2. Drag scrubber to a deliberately-recognizable position (e.g., `25:00`).
    3. Use the existing reaction picker at the bottom of the modal to post any emoji.
    4. Optimistic mount: the reaction appears at the bottom of the feed immediately.
    5. Wait ~2-3 seconds for the snapshot round-trip.
    6. Inspect Firestore (or use a second device): the wp's `reactions[]` array now
       contains a new entry with `runtimeSource: 'replay'` and `runtimePositionMs`
       within ±1s of `25 * 60 * 1000 = 1500000`.
    7. On a SECOND device (still signed in to the same family), open the same wp in
       replay variant. Drag the scrubber to `25:00`. The compound reaction posted in
       step 3 fades in.

    **Expected:** Recursive-family-memory contract holds (D-05). The replay-posted
    reaction is visible to future replayers at the position it was stamped.

    **Pass criterion:** Firestore inspection confirms `runtimeSource: 'replay'`;
    second-device replay shows the new reaction at the correct position. Resume signal:
    `script 4 pass`.

    ---

    ### Script 5 — Hide-when-empty surfaces / deploy-day silence (RPLY-26-06, RPLY-26-09, RPLY-26-20)

    **What to test:** On deploy day BEFORE creating any new Phase 26 wps, the Tonight
    inline link does NOT render AND the title-detail `Past watchparties` section does
    NOT render. This is the correct silent-UX behavior per D-10 + first-week framing.

    **Setup:** Run this script BEFORE running scripts 1-4 (i.e., before creating any
    Phase 26 wps). If you've already run scripts 1-4, this script is moot for the
    current device — verify on a fresh device install or by waiting until existing
    Phase 26 wps age past WP_ARCHIVE_MS without replay-able reactions (won't naturally
    happen; this script is best-run pre-script-1).

    **Steps:**
    1. Open the Tonight tab. Verify the `Past parties (N) ›` inline link is NOT
       visible (the area below the active-watchparties banner shows nothing).
    2. Open the title-detail view for any title the family has watched in the past
       (Phase 7 / 15.5 era wps). Scroll down past the existing Watchparties section
       (which may show legacy archived wps). Verify NO `Past watchparties` heading
       appears.

    **Expected:** Both surfaces silent. No "no past parties yet" copy, no empty-state
    placeholder, just absence.

    **Pass criterion:** Both surfaces correctly silent. Resume signal: `script 5 pass`.

    ---

    ### Script 6 — Wait Up disabled in replay (RPLY-26-14)

    **What to test:** When the user has a `participants[me].reactionDelay > 0` set
    (Wait Up — Phase 7 / 15.5 mechanic), reactions in REPLAY variant appear at their
    stamped `runtimePositionMs` WITHOUT the delay being applied. The Wait Up chip strip
    / slider is hidden in replay variant.

    **Steps:**
    1. In a LIVE watchparty (any active wp), set Wait Up to a known offset like 30s
       via the existing Wait Up chip / slider.
    2. Verify Wait Up is active in live mode (your reactions feed shifts by 30s).
    3. Now open an ARCHIVED wp via script 1 entry path (replay variant).
    4. Confirm the Wait Up chip strip / slider is HIDDEN in the replay variant.
    5. Drag the scrubber to a known reaction's runtimePositionMs.
    6. Reaction fades in WITHOUT the 30s Wait Up delay being applied (the reaction
       appears at the runtimePositionMs the original poster intended, not 30s offset).

    **Expected:** No double-shift. Wait Up only applies in live mode.

    **Pass criterion:** Wait Up chrome hidden in replay; reaction fade-in not shifted
    by reactionDelay. Resume signal: `script 6 pass`.

    ---

    ### Script 7 — Video player renders but does NOT auto-start (RPLY-26-16)

    **What to test:** When the archived wp has `videoUrl` still attached AND the title
    is non-DRM, the player surface renders in the replay modal BUT does NOT begin
    playback automatically. The viewer can opt to play in-app via the native play button.

    **Setup:** Requires an archived wp where the host originally attached a `videoUrl`
    (YouTube or MP4 — Phase 24 schema). If you don't have one, create a new wp with a
    YouTube URL via the Phase 24 video URL field, post a reaction, archive.

    **Steps:**
    1. Open the wp in replay variant.
    2. Confirm the player surface (`#wp-video-surface`) renders with the iframe / video
       element.
    3. Confirm the player is IDLE — no audio plays, the YouTube embed shows the
       thumbnail with play button overlay (NOT auto-started), the MP4 video element
       shows the first frame poster (NOT playing).
    4. Tap the native play button — the player begins.

    **Expected:** Render-but-not-auto-start contract holds. No `playerVars.autoplay`,
    no `<video autoplay>` (sentinel-verified at smoke layer; this UAT is the visual
    confirmation).

    **Pass criterion:** Player visible + idle on entry; user-initiated play works.
    Resume signal: `script 7 pass`.

    ---

    ### Script 8 — Drift tolerance ±2s feel (RPLY-26-DRIFT)

    **What to test:** Subjective UX — the ±2s drift window for reaction fade-in feels
    neither premature nor late.

    **Steps:**
    1. Use the wp from script 1.
    2. Drag scrubber to a known reaction's runtimePositionMs MINUS 1.5s (just before).
    3. Reaction should fade in promptly.
    4. Drag scrubber backward past the reaction.
    5. Reaction stays visible (per script 9).
    6. Drag scrubber FORWARD past the reaction by 3-4s.
    7. Reaction stays visible (per script 9 — once seen, persistent).

    **Expected:** Drift tolerance feels right for the family-memory framing. NOT
    metronome-precise (±2s is intentionally generous per CONTEXT § Deferred — sub-second
    sync is out of scope).

    **Pass criterion:** "Felt natural" — no perceived premature or late mounting.
    Resume signal: `script 8 pass` or describe perceived issue.

    ---

    ### Script 9 — Scrub-backward preserves shown reactions (UI-SPEC §3 persistence)

    **What to test:** Once a reaction is shown in the visible window during a session,
    scrubbing backward does NOT cause it to disappear. The visible-set is additive per
    session, never destructive.

    **Steps:**
    1. Use the wp from script 1.
    2. Drag scrubber forward past 2-3 reactions, letting each fade in.
    3. Note all 2-3 reactions are visible in the feed.
    4. Drag scrubber backward to position 0 (or near 0).
    5. All 2-3 reactions stay visible (NOT unmounted).

    **Expected:** Persistent visible-set per UI-SPEC §3 lock — "revisiting feels
    additive, not a destructive timeline."

    **Pass criterion:** No unmount on scrub-backward. Resume signal: `script 9 pass`.

    ---

    ### Script 10 — Post-deploy `couch-v38-async-replay` CACHE active (RPLY-26-18)

    **What to test:** The new service-worker cache version `couch-v38-async-replay` is
    live on production AND has invalidated the previous `couch-v37-native-video-player`
    cache.

    **Steps:**
    1. From any device with the PWA installed, force-reload (hard-quit + re-launch).
    2. Either: (a) On the device, run a curl from a desktop terminal:
       ```
       curl -fsSL https://couchtonight.app/sw.js | grep "const CACHE"
       ```
       Expected output: `const CACHE = 'couch-v38-async-replay';`
    3. Or: open the PWA's "About" screen footer (which displays the cache version) and
       confirm it reads `couch-v38-async-replay` (NOT the previous `v37-native-video-player`).
    4. Verify previous cache is purged: open DevTools (desktop browser) → Application →
       Cache Storage → confirm only `couch-v38-async-replay` is present (older versions
       removed by service-worker activate handler).

    **Expected:** `couch-v38-async-replay` is live; older caches purged.

    **Pass criterion:** New cache live + curl-verified + old cache purged. Resume signal:
    `script 10 pass`.

    ---

    ## Final sign-off

    When all 10 scripts pass: reply with `uat passed` to trigger
    `/gsd-verify-work 26` which will mark Phase 26 as fully verified end-to-end.

    If any script fails: reply with the failing script number + a short description of
    the observed vs expected behavior. The orchestrator will route to the appropriate
    remediation flow (likely a Phase 26 gap-closure plan via `/gsd-plan-phase 26 --gaps`).
    ```

    Save with no further modification.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md && wc -l .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md | awk '{ if ($1 < 80) { print "FAIL too short: "$1" lines"; exit 1 } else { print "OK "$1" lines" } }' && grep -c "^### Script " .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md | awk '{ if ($1 < 10) { print "FAIL only "$1" scripts"; exit 1 } else { print "OK "$1" scripts" } }' && grep -q "uat passed" .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md && grep -q "couch-v38-async-replay" .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md && echo "OK resume-signal + cache name present"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md` exists
    - File is ≥80 lines
    - File contains exactly 10 sections matching `^### Script ` (one per VALIDATION.md Manual-Only Verifications row)
    - File contains the resume signal string `uat passed`
    - File contains the literal cache name `couch-v38-async-replay`
    - Each script has a `**Pass criterion:**` line and a `Resume signal:` instruction
    - The Pre-UAT setup section explicitly notes the deploy-day-empty-state expectation per CONTEXT specifics
    - Frontmatter has `phase`, `type: human-uat`, `status: pending-device-verification`, `resume_signal: "uat passed"`
  </acceptance_criteria>
  <done>UAT scaffold ready for user device-verification post-deploy; 10 scripts mirror Phase 18/19/24 pattern.</done>
</task>

<task type="auto">
  <name>Task 5.3: Run production deploy via bash scripts/deploy.sh 38-async-replay (auto-bumps sw.js CACHE)</name>
  <read_first>
    - Read(scripts/deploy.sh, full file) — confirm §2.5 smoke gate runs all 9 contracts including Phase 26's; §4 sed auto-bumps sw.js CACHE
    - Read(sw.js, lines 1-15) — confirm current CACHE is `couch-v37-native-video-player` (Phase 24 baseline) BEFORE deploy
    - 26-RESEARCH.md §CACHE Bump Tag Proposal (lines 978-989) — short-tag locked as `38-async-replay`
    - .planning/STATE.md note about user feedback: deploy autonomy granted for the Couch project (no per-deploy approval needed)
    - .planning/phases/24-native-video-player/24-04-SUMMARY.md — last reference for the deploy ritual shape (curl-verify after deploy)
  </read_first>
  <behavior>
    - Pre-flight: working tree must be clean OR `--allow-dirty` flag passed (the script aborts otherwise — check git status first; if dirty, commit Plans 01-04 work before running this task)
    - `bash scripts/deploy.sh 38-async-replay` runs successfully:
      1. §2.5 smoke gate runs all 9 smoke contracts (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player + position-anchored-reactions) — ALL pass
      2. §4 sed bumps `sw.js` line 8: `const CACHE = 'couch-v37-native-video-player';` → `const CACHE = 'couch-v38-async-replay';`
      3. Mirror to `../../couch-deploy/public/` (or fallback `../../queuenight/`)
      4. `firebase deploy --only hosting` succeeds
    - Post-deploy curl verification: `curl -fsSL https://couchtonight.app/sw.js | grep "const CACHE"` returns `const CACHE = 'couch-v38-async-replay';`
    - Cross-repo gate verification (negative — proves D-07 single-repo): `git status` in `~/queuenight` (sibling) shows NO uncommitted changes (Phase 26 ships couch-only)
  </behavior>
  <action>
    Five sub-steps — run in sequence:

    **Sub-step A — Pre-flight check.** Run `git status` in the couch repo. If working tree is dirty, COMMIT Plans 01-04 work first (the executor's atomic commits per each Plan's SUMMARY). Plan 05 should be the FIRST plan that needs a clean tree before deploy. If still dirty due to untracked `26-HUMAN-UAT.md` (just authored) + smoke updates — commit those FIRST in a `chore(26-05): smoke floor + UAT scaffold` commit. Do NOT pass `--allow-dirty` to deploy.sh; the working tree should be clean before deploy.

    **Sub-step B — Confirm pre-deploy CACHE.** `grep "const CACHE" sw.js` should return `const CACHE = 'couch-v37-native-video-player';` (the Phase 24 baseline). If it shows anything else, halt and surface to user.

    **Sub-step C — Run deploy.** Execute:
    ```
    bash scripts/deploy.sh 38-async-replay
    ```
    Watch the output. The deploy script will:
    - Run all 9 smoke contracts in §2.5 (expect all green)
    - Mirror app shell to the deploy-mirror repo
    - Bump sw.js via sed
    - Run `firebase deploy --only hosting`
    - Report a deploy URL

    If smoke gate fails: do NOT proceed. Halt and surface the failure to user — this means Plans 01-04 had a regression that the smoke now catches.

    If firebase deploy fails: surface to user; investigate (likely auth, project, or network issue).

    **Sub-step D — Post-deploy curl verification.** After deploy completes:
    ```
    curl -fsSL https://couchtonight.app/sw.js | grep "const CACHE"
    ```
    Expected output: `const CACHE = 'couch-v38-async-replay';`

    If the curl shows an old cache, wait 30 seconds (Firebase Hosting CDN propagation) and retry. If still wrong after 2 minutes, surface to user.

    **Sub-step E — Cross-repo gate verification.** Confirm Phase 26 is single-repo couch-only per D-07:
    1. Confirm `firestore.rules` was NOT modified during this phase: `git log --oneline -- firestore.rules` in the couch repo should show NO Phase 26 commits (last commit on this file should be from Phase 24 / REVIEWS M2 — `a2fd4b0`).
    2. Confirm sibling queuenight repo has no Phase 26 changes (manual: `cd ~/queuenight && git status` should show no Phase 26-related uncommitted work).
    3. Update the deployed date in `26-HUMAN-UAT.md` frontmatter `deployed:` field to the actual deploy date (today's `YYYY-MM-DD`).

    **Sub-step F — Update STATE.md follow-up.** Add a Phase 26 HUMAN-VERIFY row to `.planning/STATE.md` § Open follow-ups table (mirroring the Phase 24 row pattern):
    ```
    | HUMAN-VERIFY | Phase 26 UAT (10 scripts) — pending real-device verification post-2026-MM-DD deploy. Scripts cover replay-modal entry from both surfaces / scrubber drag + reaction fade-in / compound-reaction Firestore round-trip / deploy-day-empty silence / Wait Up off in replay / video player render-no-auto-start / drift tolerance feel / scrub-backward persistence / cache bump activation. Resume signal: "uat passed" → trigger /gsd-verify-work 26. | .planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md |
    ```

    Also update STATE.md `## sw.js cache version` section to reflect the new cache: append to the version-progression line `... → v37-native-video-player (P24) → v38-async-replay (P26)`. Update `Current source:` to the new value with the curl-verified date.

    Also update `Current Position:` from `Phase: 26 / Plan: Not started` → `Phase: 26 / Plan: All 5 ready for /gsd-verify-work 26 after device-UAT`.
  </action>
  <verify>
    <automated>grep "couch-v38-async-replay" sw.js && curl -fsSL https://couchtonight.app/sw.js 2>/dev/null | grep "couch-v38-async-replay" && echo "OK CACHE bumped + live + curl-verified" || echo "PENDING — deploy may not have run yet OR CDN propagation delay; re-verify in ~60s"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "couch-v38-async-replay" sw.js` returns the line `const CACHE = 'couch-v38-async-replay';`
    - `curl -fsSL https://couchtonight.app/sw.js | grep "const CACHE"` returns `const CACHE = 'couch-v38-async-replay';` (live production verified within ~2 min of deploy completion)
    - `git log --oneline -- firestore.rules` shows NO Phase 26 commits (D-07 single-repo proof)
    - `git status` in couch repo is clean after the deploy commit lands
    - `26-HUMAN-UAT.md` frontmatter `deployed:` field shows the actual deploy date (today's date)
    - `.planning/STATE.md` has a new HUMAN-VERIFY row for Phase 26 UAT in the Open follow-ups table
    - `.planning/STATE.md` `## sw.js cache version` section reflects the v38-async-replay bump
    - `.planning/STATE.md` `## Recent shipped phases` table has a new row for Phase 26 with status SHIPPED + deploy date + cache version + plan count + UAT-pending note
    - Deploy script output shows `Smoke contracts pass (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player + position-anchored-reactions).`
  </acceptance_criteria>
  <done>Phase 26 LIVE on couchtonight.app; couch-v38-async-replay curl-verified; STATE.md updated with HUMAN-VERIFY row + sw.js CACHE log + recent-shipped row; UAT scaffold ready for user device verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → firebase production hosting | Deploy command authorized via existing firebase login + gcloud auth (already established Phase 24); deploy autonomy granted for Couch project per MEMORY.md feedback |
| local CLI → npm registry / TMDB / Firestore | NO new network surface introduced by Phase 26; deploy is a static-file mirror + service worker bump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | Smoke gate accidentally weakened in future PRs (assertion count drops below floor) | mitigate | RPLY-26-17 floor meta-assertion in scripts/smoke-position-anchored-reactions.cjs fails the deploy gate when total < 13. CI / deploy.sh §2.5 runs the smoke before any production push. |
| T-26-08 | Repudiation | Deploy date in 26-HUMAN-UAT.md frontmatter `deployed:` field could be backdated/forwarded | accept | Pure documentation field; no security consequence. Sub-step E updates this honestly to the actual deploy date. |
| T-26-09 | Denial of Service | Wrong CACHE name shipped to production → installed PWAs fail to invalidate | mitigate | curl-verification in sub-step D catches mismatches within minutes of deploy. The CACHE-name tag is locked at deploy-time via the `38-async-replay` short-tag arg; sed is deterministic. RPLY-26-18 codifies the post-deploy verification. |
</threat_model>

<verification>
1. `node scripts/smoke-position-anchored-reactions.cjs` exits 0 with the floor meta-assertion line green AND total ≥ 39
2. `bash scripts/deploy.sh 38-async-replay` completes successfully + prints the updated 9-contract recap
3. `grep "couch-v38-async-replay" sw.js` returns the live CACHE constant
4. `curl -fsSL https://couchtonight.app/sw.js | grep "const CACHE"` returns the new cache (within 2 min of deploy)
5. `26-HUMAN-UAT.md` exists with 10 scripts + frontmatter resume signal
6. STATE.md updated with Phase 26 HUMAN-VERIFY row + cache version log + recent-shipped row
7. `git log --oneline -- firestore.rules` shows zero Phase 26 commits (D-07 single-repo proof)
</verification>

<success_criteria>
- All 3 tasks complete with `done` criteria met
- Phase 26 fully shipped end-to-end:
  - Schema additions + replay variant + Past parties extension + new title-detail section (Plans 01-04)
  - Smoke gate green with ≥39 assertions + floor sentinel locked (Plan 05 Task 5.1)
  - HUMAN-UAT.md scaffolds 10 device-UAT scripts (Plan 05 Task 5.2)
  - Production deploy live: `couch-v38-async-replay` curl-verified at couchtonight.app (Plan 05 Task 5.3)
  - STATE.md updated with cache log + HUMAN-VERIFY follow-up + recent-shipped row
  - D-07 single-repo couch-only deploy verified (firestore.rules untouched; queuenight unchanged)
- All 24 RPLY-26-* IDs codified in production source + smoke gate
- Phase awaits user device-UAT (resume signal `uat passed` → `/gsd-verify-work 26`)
</success_criteria>

<output>
After completion, create `.planning/phases/26-position-anchored-reactions-async-replay/26-05-SUMMARY.md` documenting:
- Atomic commits (target 3-4): smoke floor (Task 5.1), HUMAN-UAT scaffold (Task 5.2), deploy + STATE.md updates (Task 5.3 — typically split into deploy commit + STATE.md commit)
- Final smoke assertion count post-deploy
- Deploy URL + production cache curl-verification timestamp
- Confirmation that all 24 RPLY-26-* IDs are Complete in REQUIREMENTS.md (with traceability rows added — same pattern as Phase 24 / Plan 24-04 closing the VID-24-* IDs)
- Confirmation of D-07 single-repo proof (firestore.rules + queuenight git status both clean of Phase 26)
- Note that Phase 26 is now in the "awaiting device-UAT" state, mirroring Phase 18/19/20/24 close-out shape
</output>
