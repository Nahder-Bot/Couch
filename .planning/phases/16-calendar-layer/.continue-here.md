---
created: 2026-05-29T00:17:39Z
session_date: 2026-05-28
phase: 16-calendar-layer
phase_status: shipped + post-launch polish iteration
branch: hotfix/phase-30-cross-cutting-wave
production_cache: couch-v16.9-claim-fix
---

# Resume — Phase 16 post-launch polish (paused mid-iPhone-UAT)

## Where we are

User Nahder is doing iPhone UAT on `couchtonight.app/app`. Hit a series of issues; each was diagnosed + fixed + redeployed within minutes. Currently waiting on user to confirm whether **v16.9-claim-fix** resolved the claim-toast loop. If yes → continue UAT scripts 2-6. If no → diagnose further.

## What's open RIGHT NOW

User's last message: "Claiming account / Account claimed keeps popping up every save tap." Fixed in `couch-v16.9-claim-fix` (commit `0106f7e`) by optimistically updating `state.me.uid` + `claimedAt` before the await + reverting on Firestore reject (state.me was set from qn_me localStorage, never refreshed from members onSnapshot — so subsequent guard checks saw stale state and re-fired claim).

**First action on resume:** ask user if the new deploy fixed it. If iPhone still hits the loop, suspect that the members onSnapshot reassigns state.me from a fresh members array (clobbering our optimistic write). Check the snapshot handler at js/app.js around line 5101 area.

## What shipped today (9 deploys)

| Cache | Scope |
|---|---|
| v16.1-series-fix | TMDB autocomplete HTML quote-escape (HTML attribute injection broke onclick) |
| v16.2-bugs | welcome popup re-fire, group-switch teardown, iOS PWA nudge close |
| v16.3-roles | Bracket selector replaces Adult/Parent/maxTier triple. RATING_TIERS: TV-MA→tier 4, tier 5 renamed "All". `BRACKETS`/`memberBracket`/`bracketToFlags` in constants.js. `setMemberBracket` handler. CSS `.bracket-pill`. |
| v16.4-tonight | Tonight tab redesign A+B+C: compact chip-bar roster + cinematic top-match hero (#tonight-hero-container) + cleanup (removed legacy #continue-section; filter bar gated on titles>0; activity gated on len>0). |
| v16.5-review | N-1 hero empty container cleanup + N-2 switchToGroup missing 3 unsubs + N-4 44px touch targets |
| v16.6-security | **C-1 self-elevation:** member UPDATE rule got 5-branch field allowlist (privilege fields require owner or `isParentOfFamily`). **C-2 cross-family read:** `isMemberOfFamily` now OR-checks `members/m_<uid>` exists OR `users/{uid}/groups/{code}` + family doc exists. (Strict-only first attempt broke pre-Phase-5 families — Nahder's iPhone hit this.) |
| v16.7-cleanup | 11 deferred items: Trakt iOS PWA fallback, _bootIntoGroup phantom-family recovery, alert→toast, localStorage guards, snapshotErrorHandler('user-groups'), week-view 44px, .th-cta-secondary contrast, aria-live, AASA paths, Sentry name scrub, watchpartySeries memberUids ≤24 cap |
| v16.8-uat-fixes | UAT feedback: "A TV show"→"TV series", "Untitled/surprise"→"Decide together", couch default = just-me (not everyone), owner self-claim fast-path in guardReadOnlyWrite |
| v16.9-claim-fix | Optimistic state.me.uid update on owner self-claim (loop bug) |

## Outstanding for launch

### User action in ASC dashboard (no code)
1. Edit description to mention Phase 16 (recurring watchparties / calendar) — Apple §5.6 risk
2. Add Reviewer Notes posture for §1.2 UGC moderation: *"Couch is closed-family UGC. Family admins (isParent) can remove any member or content via the Family tab. Sub-profile UGC is parent-controlled. No cross-family discovery — users join only via explicit family-code share. Kid sub-profiles are created and managed by adult parents; no independent under-13 account creation flow exists."*
3. Confirm Release setting = Manual (not auto-release)
4. Confirm Privacy Nutrition Labels saved

### User action on device
1. Hard-refresh iPhone PWA so v16.9 installs
2. Complete Phase 16 HUMAN-UAT scripts 1-6 (`16-HUMAN-UAT.md`)
3. Spot-check chip-bar at 7-member family width
4. Spot-check bracket selector on Family tab
5. Delete unencrypted `~/Downloads/AuthKey_PFGQNA2UTR.p8` (Apple Sign-In .p8; archive at `~/Documents/Couch-secrets/Couch-Apple-Key-PFGQNA2UTR.7z`)

### User decision
- Click **Add for Review** in ASC when ready (Manual release = approved-but-unlaunched until user clicks Release)

## Outstanding tech debt logged this session

**TD-15** (new): Toast container overlaps modal Save buttons on small iPhones. `.toast` has `pointer-events: auto` for tap-to-dismiss; on small screens it steals taps from underlying modal CTAs. Fix: either set `.toast { pointer-events: none }` (auto-dismiss handles cleanup) OR shift `.toast-container` position up when a `.modal-bg.on` is active. User reported via iPhone screenshot 2026-05-28.

## Audit findings deferred (post-launch)

See `HANDOFF.json` for full list. Key items:
- **C-2 full close:** migration CF to retag legacy member doc ids to `m_<uid>` form, then drop the OR fallback in `isMemberOfFamily`.
- TD-15 toast/modal overlap (above)
- Hostile-conditions: notification denied affordance, modulepreload, uploadPostSessionPhoto silent return
- A11y polish: heading rotor, bracket-pill disabled cue, landing cta-secondary contrast, onboarding-overlay focus trap
- Apple compliance: rsvp.html first-touch push permission (defensible but flag-worthy)

## State of trust

- All today's commits are on `hotfix/phase-30-cross-cutting-wave` (80+ commits accumulated)
- PR #8 (594-commit catch-up to main) still open and unmerged
- Production live: `couch-v16.9-claim-fix` (curl-verified)
- Firebase Functions deployed: `watchpartyTick`, `watchpartySeriesTick`, `rsvpSubmit`, `gameResultsTick`
- Firestore rules: v16.6-revised live (m_<uid> OR self-attested + family-exists)
- iOS wrapper: Build 104 in TestFlight, Apple Sign-In E2E verified 2026-05-26, inherits today's web deploys via live URL
- Smoke gate: 67/67 sentinels green

## Resume protocol

1. **Read this file in full** before any action.
2. Ask user: "Did the v16.9-claim-fix deploy resolve the save loop?"
3. **If yes:** continue UAT scripts 2-6 from `16-HUMAN-UAT.md`.
4. **If no:** investigate state.me reassignment in members onSnapshot. Likely culprit: snapshot handler does `state.me = state.members.find(m => m.id === <my id>)` which would clobber our optimistic write.
5. **If save succeeds:** the rest of Phase 16 UAT can run cleanly (Entry 2 post-wp prompt, Edit cadence, Pause/Resume/Cancel, Week-view, 30-min reminder push).
6. **Before next deploy:** check if context budget allows. If tight, just ship rules-only or single-file changes; reserve bigger changes for fresh sessions.

## Critical anti-patterns from this session

| Pattern | Severity | What went wrong |
|---|---|---|
| Assumed `m_<uid>` member id convention was universal | advisory | C-2 strict fix broke pre-Phase-5 families. Always check actual data distribution before tightening a rules predicate. |
| Self-claim wrote to Firestore but didn't update local cached `state.me` | advisory | onSnapshot might be expected to refresh state.me but doesn't in this code path. Always do optimistic local updates alongside writes when the local state isn't a derived view of the snapshot. |
| Toast `pointer-events: auto` + modal Save in same vertical band on iPhone | advisory | Tested every flow on desktop where this never collided. UAT on real device caught it. |
