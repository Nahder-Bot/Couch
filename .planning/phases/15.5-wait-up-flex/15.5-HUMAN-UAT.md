---
status: partial
phase: 15.5-wait-up-flex
source: [15.5-VERIFICATION.md, 15.5-VALIDATION.md]
started: 2026-04-28T15:45:00Z
updated: 2026-04-28T15:45:00Z
---

## Current Test

[awaiting human testing — automated UAT complete; physical-device UAT pending]

## Tests

### 1. Script A — Sport-mode slider drag end-to-end (REQ-1, REQ-4, REQ-5, REQ-6 sport mode)
expected:
- Drag slider 0% → 100% on iPhone PWA standalone reaches readout `24 hr`
- Drag to ~7% reads `1 min`, ~22% reads `5 min`, ~47% reads `30 min`, ~67% reads `2 hr`
- Active-state readout reads italic `holding {value}`
- Idle-state readout at 0 reads plain `Live`
- `+5 min` button visible inline next to readout when offset > 0
- From 23h 56m, tap `+5 min` → clamps to 24h (no overflow past 86400)
result: [pending]

### 2. Script B — Movie-mode chip strip + Custom… picker (REQ-2, REQ-3, REQ-4, REQ-5, REQ-6 movie mode)
expected:
- Chip ladder renders 8 elements in order: Live · 15 sec · 1 min · 5 min · 15 min · 30 min · 1 hr · Custom…
- Tapping `15 min` writes 900 to Firestore + chip on-state + sub-line `waiting 15 min`
- Tapping Live writes 0 + sub-line + nudge button hide + chip on-state moves to Live
- Tapping `Custom…` opens bottom-sheet modal with title `How long should we wait up?`
- iOS native number keypad activates on tapping HOURS / MIN / SEC fields (real iPhone Safari)
- Entering `1 / 30 / 0` + tapping `Set wait` closes sheet + sub-line reads `waiting 1 hr 30 min`
- Tapping `+5 min` → value becomes `1 hr 35 min`
- Tapping a preset chip (e.g. `2 hr`) inside the picker auto-fills inputs and submits
- iPhone mini viewport (375px) — chips wrap to 2-3 rows, no horizontal overflow
- Backdrop tap dismisses modal WITHOUT committing
result: [pending]

### 3. Script C — Two-device per-receiver isolation (REQ-8)
expected:
- Device A: member with reactionDelay = 3600 (1 hr)
- Device B: member with reactionDelay = 0 (Live)
- Device B posts emoji reaction → Device B sees it immediately, Device A does NOT see it yet
- Wait 1 hr (or mock time forward via Firestore) → Device A sees the reaction
- Confirms render filter is per-receiver (not server-side, not affecting other viewers)
result: [pending]

### 4. Script D — Push body content stripping (REQ-7)
expected:
- Option A (CF emulator): from `~/queuenight/`, run `firebase emulators:start --only functions`, post a wp doc reaction. Emulator logs show TWO `sendToMembers` calls — one body `"{senderName} is reacting to {titleName}"` for receivers with reactionDelay > 0, one body `"{senderName}: {emoji|text}"` for receivers with reactionDelay === 0.
- Option B (live two-device): Device A reactionDelay = 60. Device B reactionDelay = 0. Device C posts reaction. Device A's lock-screen push reads `"{Cname} is reacting to {Title}"`. Device B's push reads `"{Cname}: {emoji|text}"` full content.
- Confirms REQ-7 spoiler-content stripping is live in queuenight functions deployed at 2026-04-28T15:33Z.
result: [pending]

### 5. Script E — 5h cutoff + Past parties surface (REQ-9, REQ-10)
expected:
- Pre-populate Firestore: create test wps with `startAt = Date.now() - 6h` and `Date.now() - 23h` via Firebase console
- Open Tonight tab — those wps do NOT appear in main banner
- Inline link `Past parties (2) ›` renders below the active banner section
- Tapping link opens tall-sheet modal with eyebrow "Still rolling" + title "Past parties" + close × button
- Both stale wps appear in list, sorted most-recent-first (6h ago at top, 23h ago below)
- Each row: poster + title + `Started 6 hr ago` (or 23) + `N on the couch`
- Tapping a row: Past parties closes AND the wp opens (joined or join flow)
- Reopen via link, tap × close → dismisses to Tonight tab
- With 0 stale wps in Firestore, the link does NOT render (zero clutter per UI-SPEC § 6a)
result: [pending]

### 6. 24-hour Sentry + CF-log post-deploy soak
expected:
- Within 24h of 2026-04-28 15:36Z deploy, Sentry shows no spike in `[15.5/REQ-7]` log markers (Plan 05 Task 2 console.warns)
- queuenight CF logs show no surge in `reactionPosted` push delivery errors
- Persistent spike past 1h triggers investigation (gap-closure plan candidate)
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
