---
phase: 15.5
slug: wait-up-flex
status: planner_populated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-28
populated: 2026-04-28
---

# Phase 15.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Couch has no automated test suite (single-file no-bundler PWA). All validation is manual UAT (mobile devices) or Cloud Functions emulator runs from the sibling `queuenight/` repo. Per-task table populated by the planner 2026-04-28.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None (manual UAT + Firebase Functions emulator) |
| **Config file** | None on couch side; `~/queuenight/firebase.json` for emulator |
| **Quick run command** | Manual: open `couchtonight.app/app` on test device after `bash scripts/deploy.sh <tag>` |
| **Full suite command** | Two-device UAT session — see Scripts A–E below |
| **CF emulator command** | `cd ~/queuenight && firebase emulators:start --only functions` |
| **Estimated runtime** | Manual UAT: ~25 min for full Scripts A–E pass on two devices |

---

## Sampling Rate

- **After every task commit:** No automated test → run `node -c <modified.js>` syntax check; for CSS, deploy preview channel (`firebase hosting:channel:deploy`) and visually confirm.
- **After every plan wave:** Full UAT script for any user-visible behavior touched in the wave (Scripts A–E below).
- **Before `/gsd-verify-work`:** All five UAT scripts pass on iPhone-mini-class device (375px) AND a tablet/desktop viewport. Push-stripping (Script D) verified on two physical devices OR the CF emulator with mocked receiver `reactionDelay`.
- **Max feedback latency:** ~30 seconds (deploy preview channel build + cache-bust open).

---

## Per-Task Verification Map

> Each task in the seven plans, mapped to its requirement(s), threat reference(s), and verification approach.

| Task ID | Plan | Wave | Requirement(s) | Threat Ref | Secure Behavior | Test Type | Automated Command (or UAT script) | File Exists | Status |
|---------|------|------|---------------|-----------|-----------------|-----------|------------------------------------|------------|--------|
| 01-T1 | 15.5-01 | 1 | REQ-1, REQ-2, REQ-5, REQ-6 | T-15.5-01 | setReactionDelay clamp widened 60→86400 | grep + node -c | `grep -nc "Math.min(86400" js/app.js` returns ≥1 in setReactionDelay range | yes | ⬜ pending |
| 01-T2 | 15.5-01 | 1 | REQ-1 | T-15.5-01 | setDvrOffset defensive 86400 clamp added | grep | `sed -n '10316,10340p' js/app.js \| grep -c "Math.min(86400"` returns 1 | yes | ⬜ pending |
| 01-T3 | 15.5-01 | 1 | REQ-1, REQ-4, REQ-6 | — | dvrReadoutText returns 'Live' + handles hour values | inline node smoke eval | full eval string in plan task verify (covers 0/15/60/75/300/3600/5400/43200/86400) | yes | ⬜ pending |
| 02-T1 | 15.5-02 | 2 | REQ-1 | T-15.5-01 | positionToSeconds + secondsToPosition non-linear transforms | dedicated smoke script | `node scripts/smoke-position-transform.cjs` exits 0 with `ALL ASSERTIONS PASSED` | yes (post-task) | ⬜ pending |
| 02-T2 | 15.5-02 | 2 | REQ-1, REQ-4, REQ-6 | T-15.5-03 | renderDvrSlider position-based + italic 'holding' readout + comma aria-label | grep + UAT Script A | `grep -nc 'max="100"' js/app.js` ≥1 in slider range; **UAT Script A at Wave 3 deploy preview** | yes | ⬜ pending — UAT Script A at deploy |
| 03-T1 | 15.5-03 | 2 | REQ-2, REQ-4, REQ-6 | T-15.5-01, T-15.5-03 | 8-element chip ladder + sub-line + nudge HTML render | grep + UAT Script B | `grep -n "delayPresets = \[0, 15, 60, 300, 900, 1800, 3600\]" js/app.js` (=1); **UAT Script B at deploy** | yes | ⬜ pending — UAT Script B at deploy |
| 03-T2 | 15.5-03 | 2 | REQ-5 | T-15.5-01 | addWaitUpNudge clamps current+300 at 86400 | grep + UAT Script B step 7 | `grep -n "window.addWaitUpNudge" js/app.js` (=1); **UAT Script B step 7 + step 10 clamp at deploy** | yes | ⬜ pending — UAT Script B at deploy |
| 03-T3 | 15.5-03 | 2 | REQ-2, REQ-5, REQ-6 | — | Chip touch-floor + sub-line + nudge + custom-chip dashed-border CSS | grep + UAT Script B visual | `grep -n "wp-delay-subline" css/app.css` ≥2; **UAT Script B visual + iPhone mini wrap (step 10) at deploy** | yes | ⬜ pending — UAT Script B at deploy |
| 04-T1 | 15.5-04 | 2 | REQ-3, REQ-8 | — | Picker DOM block + 3 numeric inputs + helper-line copy + role=dialog + eyebrow source `Or jump to` (CSS-uppercased) | grep | `grep -n 'id="wait-up-picker-bg"' app.html` (=1); `grep -c 'inputmode="numeric"' app.html` ≥3; `grep -nc '>Or jump to<' app.html` =1 | yes | ⬜ pending |
| 04-T2 | 15.5-04 | 2 | REQ-3, REQ-4, REQ-8 | T-15.5-01, T-15.5-03 | Picker open/close/submit/preset + R-1 scrollIntoView mitigation | grep + UAT Script B steps 4-6 | `grep -nc "window.openWaitUpPicker\|window.closeWaitUpPicker\|window.submitWaitUpPicker\|window.setPickerPreset" js/app.js` =4; **UAT Script B steps 4-6 + iOS keyboard overlap visual** | yes | ⬜ pending — UAT Script B at deploy |
| 04-T3 | 15.5-04 | 2 | REQ-3 | — | Picker CSS subclass + form fields + helper + actions + reduced-motion | grep + UAT Script B visual | `grep -n ".wait-up-picker-modal" css/app.css` ≥1; **UAT Script B visual at deploy** | yes | ⬜ pending — UAT Script B at deploy |
| 05-T1 | 15.5-05 | 2 | REQ-7 | — | NOTIFICATION_DEFAULTS.reactionPosted=true (server) | grep + node -c | `cd ~/queuenight && grep -nc "reactionPosted: true" functions/index.js` =1 | yes | ⬜ pending |
| 05-T2 | 15.5-05 | 2 | REQ-7, REQ-8 | T-15.5-03 (CRITICAL), T-15.5-01, T-15.5-04 | onWatchpartyUpdate Branch 2 with per-receiver fan-out (sendToMembers called 2× per new reaction inside the for-loop body) | grep + UAT Script D (CF emulator or two-device) | `grep -n "is reacting to" queuenight/functions/index.js` =1; **count `sendToMembers(` inside the for-loop body = exactly 2** (not 2 total — 2 per iteration); **UAT Script D at deploy** | yes | ⬜ pending — UAT Script D at deploy |
| 05-T3 | 15.5-05 | 2 | REQ-7 | — | Client-side DR-3 mirror (PLACES 1+2 only; PLACE 3 deferred to 15.4) | grep | `grep -c "reactionPosted" js/app.js` =2 (NOT 3) | yes | ⬜ pending |
| 06-T1 | 15.5-06 | 2 | REQ-9, REQ-10 | T-15.5-04 | WP_STALE_MS const + renderWatchpartyBanner fresh/stale split + Past parties link | grep + UAT Script E | `grep -nc "WP_STALE_MS" js/app.js` ≥3 (declaration + 2 uses); **UAT Script E steps 1-3 at deploy** | yes | ⬜ pending — UAT Script E at deploy |
| 06-T2 | 15.5-06 | 2 | REQ-10 | — | past-parties-bg modal-bg scaffold + eyebrow source `Still rolling` (CSS-uppercased to `STILL ROLLING`) | grep | `grep -n 'id="past-parties-bg"' app.html` (=1); `grep -nc '>Still rolling<' app.html` =1 | yes | ⬜ pending |
| 06-T3 | 15.5-06 | 2 | REQ-10 | T-15.5-01 | openPastParties + closePastParties + renderPastParties + escapeHtml on dynamic content | grep + UAT Script E steps 4-7 | `grep -n "function renderPastParties" js/app.js` (=1); **UAT Script E at deploy** | yes | ⬜ pending — UAT Script E at deploy |
| 06-T4 | 15.5-06 | 2 | REQ-10 | — | Past parties CSS (link + surface + row + meta color correction --ink-warm) | grep + UAT Script E visual | `grep -nc ".past-parties-" css/app.css` ≥14; **UAT Script E visual at deploy** | yes | ⬜ pending — UAT Script E at deploy |
| 07-T1 | 15.5-07 | 3 | REQ-11 | T-15.5-01 | sw.js CACHE bumped to couch-v35.5-wait-up-flex | grep | `grep -nc "couch-v35.5-wait-up-flex" sw.js` =1 | yes | ⬜ pending |
| 07-T2 | 15.5-07 | 3 | REQ-7, REQ-8 | T-15.5-03 (CROSS-REPO ORDER) | queuenight functions deployed FIRST per RUNBOOK §H | gcloud + UAT Script D | `gcloud functions describe onWatchpartyUpdate --project queuenight-84044 ...` updateTime within deploy window | live deploy | ⬜ pending — checkpoint:human-action |
| 07-T3 | 15.5-07 | 3 | REQ-11 | — | couch hosting deployed SECOND via scripts/deploy.sh | curl + smoke | `curl -s https://couchtonight.app/sw.js \| grep CACHE` returns couch-v35.5-wait-up-flex | live deploy | ⬜ pending — checkpoint:human-action |
| 07-T4 | 15.5-07 | 3 | REQ-1..REQ-11 (all) | T-15.5-01..T-15.5-04 (end-to-end) | UAT Scripts A+B+E (single-device) + C+D (two-device or emulator) all PASS | manual UAT on real devices | per-script step list in plan task verify; user types "uat passed" | live | ⬜ pending — checkpoint:human-verify |

*Status legend: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Coverage summary:** 22 tasks across 7 plans. Every REQ-1 through REQ-11 has at least one task. Every threat T-15.5-01 through T-15.5-04 referenced in the relevant tasks. Cross-repo deploy ordering enforced via Plan 07 Task 2 → Task 3 blocking gates.

---

## UAT Scripts (canonical — sourced from `15.5-RESEARCH.md` § Validation Architecture)

- **Script A** — Slider range + step ramp (sport mode, single device)
- **Script B** — Chip strip + custom picker + nudge (movie mode, single device, iPhone mini viewport)
- **Script C** — Per-receiver contract (two devices, REQ-8)
- **Script D** — Push body stripping (CF emulator OR two devices, REQ-7)
- **Script E** — Stale-wp 5h filter + Past parties surface (Tonight tab, REQ-9 + REQ-10)

Each script's step-by-step is in the RESEARCH.md document AND embedded verbatim in `15.5-07-PLAN.md` Task 4. The planner cross-references script IDs in task acceptance criteria rather than duplicating them.

---

## Wave 0 Requirements (now satisfied)

- [x] CF emulator runs locally — `cd ~/queuenight && firebase emulators:start --only functions` documented as the smoke check before Plan 05 push template work begins
- [x] Test devices available: iPhone (mini-class preferred for chip wrap UAT) + Android Chrome OR a second iOS device for two-device tests (Scripts C + D)
- [x] Firestore direct-edit access (Firebase console) for setting test wp `startAt` to past timestamps for Script E
- [x] No JS/CSS test framework added; per-task verification uses grep, node -c, dedicated smoke script (Plan 02 Task 1), and manual UAT scripts

*Existing infrastructure caveat:* No JS/CSS test framework exists, and the architecture lock (no bundler, no npm deps per `CLAUDE.md`) means none will be added in 15.5. Validation depth is bounded by manual UAT discipline + the cross-repo CF emulator + the dedicated `scripts/smoke-position-transform.cjs` for the Plan 02 transform math.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Slider thumb drag → readout | REQ-1 | Touch-input behavior; no headless equivalent matches PWA gesture | Script A steps 1–8 |
| Custom picker mobile keypad | REQ-3 | Native iOS keypad behavior is device-only | Script B steps 4–6 |
| Per-receiver render isolation | REQ-8 | Requires two synchronized devices and live Firestore | Script C |
| Push notification body content | REQ-7 | Requires native APNs/FCM delivery to lock screen OR CF emulator log inspection | Script D |
| Tonight tab 5h cutoff | REQ-9 | Requires aged `startAt` data; no headless run | Script E |
| Past parties navigation + back | REQ-10 | App-shell modal routing depends on hardware-back behavior; visual confirmation only | Script E |
| Brand-voice copy (no banned words) | constraint | Reviewer judgment; partial automation via grep | `grep -iE "delay\|buffer\|queue\|offset\|sync\|lag\|latency\|playback\|timer\|countdown" js/app.js` against new lines only (excluding code-internal `delay`/`offset` identifiers per UI-SPEC ledger) |
| `sw.js` CACHE bumped | REQ-11 | Single line; trivial check | `grep "^const CACHE = " sw.js` after `bash scripts/deploy.sh <tag>` — confirm tag matches |

---

## Validation Sign-Off

- [x] All tasks have either `<automated>` verify (grep, node -c, smoke script) OR documented manual UAT script reference
- [x] Sampling continuity: every plan wave has at least one UAT script before merging
- [x] Wave 0 prerequisites met (CF emulator + test devices + Firestore direct-edit access)
- [x] No watch-mode flags (none required — no test framework)
- [x] Per-task verification map populated (22 rows above) with task IDs + script references
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** populated 2026-04-28; pending UAT pass at Wave 3 deploy preview.

---

*Phase: 15.5-wait-up-flex*
*Validation populated: 2026-04-28 by planner*
