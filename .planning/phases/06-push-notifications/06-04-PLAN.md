---
phase: 06-push-notifications
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - js/app.js
  - queuenight/functions/index.js
autonomous: true
requirements: [PUSH-03, PUSH-04]
must_haves:
  truths:
    - "3 new CF triggers exist: onInviteCreated (invite-received), onSessionUpdate-vetoCap (veto-cap-hit), onSessionUpdate-tonightPick (tonight's-pick-chosen)"
    - "New triggers use sendToMembers as the single send path and pass eventType + self-echo args"
    - "quietHours block is fully wired: Settings UI has enabled toggle + start/end time pickers + detected timezone"
    - "sendToMembers checks quiet hours against the recipient's timezone and skips when enabled + current time falls within range"
    - "forceThroughQuiet: true flag on payload bypasses quiet hours (used by watchparty-starting when the recipient has RSVP'd)"
    - "No regression on existing 3 triggers"
  artifacts:
    - path: queuenight/functions/index.js
      provides: "3 new triggers + quiet-hours enforcement in sendToMembers"
      contains: "onInviteCreated"
      contains: "quietHours"
    - path: js/app.js
      provides: "Settings UI quiet-hours row with time pickers + timezone display"
      contains: "quietHours"
  key_links:
    - from: "new CF triggers"
      to: "sendToMembers(... { eventType, excludeUid, excludeMemberId })"
      via: "shared helper"
      pattern: "sendToMembers"
---

<objective>
Close the event-coverage gap (PUSH-03) by adding the 3 new Cloud-Function triggers for events whose source surfaces already exist today: invite-received, veto-cap-hit, and tonight's-pick-chosen. At the same time, wire up quiet-hours (PUSH-04) end-to-end — Settings UI picker + server-side enforcement in `sendToMembers`.

Purpose: After this plan the user-facing promise of Phase 6 is functionally complete — every event that should push, pushes; every user can opt out per-event-type OR silence a time window; self-echo doesn't bug them; the one flagship delivery event (watchparty-starting via existing CF) is proven to work in UAT.
Output: `queuenight/functions/index.js` gains 3 exported triggers + quiet-hours logic in `sendToMembers`; `js/app.js` quiet-hours UI + timezone detection + prefs helpers extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-push-notifications/06-CONTEXT.md
@.planning/phases/06-push-notifications/06-RESEARCH.md
@queuenight/functions/index.js
@js/app.js

<interfaces>
<!-- New CF triggers: -->

<!-- 1. onInviteCreated — fires on families/{code}/invites/{token} doc create -->
<!--
exports.onInviteCreated = functions.firestore
  .document('families/{familyCode}/invites/{token}')
  .onCreate(async (snap, context) => {
    const invite = snap.data();
    if (!invite || !invite.recipientUid) return null;  // code-only invites skip push
    const { familyCode } = context.params;
    // Look up recipient's memberId in this family; if not yet a member, skip (they'll see the link via share channel)
    const membersSnap = await db.collection('families').doc(familyCode).collection('members').where('uid', '==', invite.recipientUid).get();
    if (membersSnap.empty) return null;
    const recipientId = membersSnap.docs[0].id;
    // Resolve sender name
    const senderName = invite.senderName || 'A family member';
    return sendToMembers(familyCode, [recipientId], {
      title: 'Invite from ' + senderName,
      body: `${senderName} invited you to their family`,
      tag: `invite-${context.params.token}`,
      url: '/'
    }, {
      excludeUid: invite.senderUid,
      excludeMemberId: invite.senderMemberId,
      eventType: 'inviteReceived'
    });
  });
-->

<!-- 2. onSessionUpdate — single trigger covering both vetoCap and tonightPick by checking what transitioned -->
<!--
exports.onSessionUpdate = functions.firestore
  .document('families/{familyCode}/sessions/{sessionId}')
  .onUpdate(async (change, context) => {
    const before = change.before.data();
    const after = change.after.data();
    if (!before || !after) return null;
    const { familyCode, sessionId } = context.params;

    // Veto-cap transition
    const VETO_CAP = 5;  // matches VETO-04 cap; planner confirms exact value from Phase 4
    if ((before.vetoCount || 0) < VETO_CAP && (after.vetoCount || 0) >= VETO_CAP) {
      const ownerSnap = await db.collection('families').doc(familyCode).get();
      const ownerUid = ownerSnap.exists ? ownerSnap.data().ownerUid : null;
      if (ownerUid) {
        const ownerMember = (await db.collection('families').doc(familyCode).collection('members').where('uid', '==', ownerUid).get()).docs[0];
        if (ownerMember) {
          await sendToMembers(familyCode, [ownerMember.id], {
            title: 'Tonight is stuck',
            body: 'Your family vetoed 5 picks tonight — time to step in?',
            tag: `veto-cap-${sessionId}`,
            url: '/?screen=tonight'
          }, { eventType: 'vetoCapReached' });
          // No self-echo args — owner themselves cannot cause veto-cap without other members' vetoes
        }
      }
    }

    // Tonight's-pick-chosen transition
    if (!before.pickedTitleId && after.pickedTitleId && after.spinnerId) {
      const membersSnap = await db.collection('families').doc(familyCode).collection('members').get();
      const recipients = membersSnap.docs.map(d => d.id).filter(id => id !== after.spinnerId);
      // Resolve title name
      let titleName = 'tonight\'s pick';
      try {
        const t = await db.collection('families').doc(familyCode).collection('titles').doc(after.pickedTitleId).get();
        if (t.exists && t.data().name) titleName = t.data().name;
      } catch(e) {}
      await sendToMembers(familyCode, recipients, {
        title: 'Tonight\'s pick',
        body: `${after.spinnerName || 'The spinner'} landed on "${titleName}"`,
        tag: `pick-${sessionId}`,
        url: '/?screen=tonight'
      }, {
        excludeUid: after.spinnerUid,
        excludeMemberId: after.spinnerId,
        eventType: 'tonightPickChosen'
      });
    }

    return null;
  });
-->

<!-- quiet-hours in sendToMembers: -->
<!--
// After the prefs read, before iterating subs:
if (prefs.quietHours && prefs.quietHours.enabled && !payload.forceThroughQuiet) {
  const tz = prefs.quietHours.tz || 'America/Los_Angeles';
  const now = new Intl.DateTimeFormat('en-US', { timeZone: tz, hour: '2-digit', minute: '2-digit', hour12: false }).format(new Date());
  // Compare HH:mm string against quietHours.start / .end, handling wrap-around (e.g. 22:00 → 08:00)
  const [nowH, nowM] = now.split(':').map(Number);
  const [startH, startM] = prefs.quietHours.start.split(':').map(Number);
  const [endH, endM] = prefs.quietHours.end.split(':').map(Number);
  const nowMin = nowH * 60 + nowM;
  const startMin = startH * 60 + startM;
  const endMin = endH * 60 + endM;
  const inQuiet = (startMin < endMin)
    ? (nowMin >= startMin && nowMin < endMin)
    : (nowMin >= startMin || nowMin < endMin);  // wrap-around
  if (inQuiet) continue;
}
-->

<!-- Settings UI quiet-hours row: -->
<!--
  <div class="pref-row quiet-hours">
    <label><input type="checkbox" id="qh-enabled"> Quiet hours</label>
    <div class="qh-times" style="display:none;">
      From <input type="time" id="qh-start" value="22:00">
      to   <input type="time" id="qh-end"   value="08:00">
      <span class="qh-tz muted">(your timezone: {detected})</span>
    </div>
  </div>
-->

<!-- Client helper: updateQuietHours({ enabled, start, end, tz }) writes the full block. -->
</interfaces>

</context>

<tasks>

<task type="edit-code" gate="blocking">
  <name>Task 1: CF — add onInviteCreated trigger</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    `grep "exports.onInviteCreated" queuenight/functions/index.js` returns 1 match. Trigger listens on `families/{familyCode}/invites/{token}`, resolves recipient via `where('uid','==',invite.recipientUid)`, skips when recipient not a member, passes `eventType: 'inviteReceived'` + self-echo args.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 2: CF — add onSessionUpdate (veto-cap + tonight-pick branches)</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    `grep "exports.onSessionUpdate" queuenight/functions/index.js` returns 1 match. Body has two clearly-separated transition checks: veto-cap (on `vetoCount` crossing threshold) and tonight-pick (on `pickedTitleId` going from falsy to truthy + `spinnerId` present). Each uses `sendToMembers` with the matching `eventType` + self-echo.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 3: CF — quiet-hours check in sendToMembers</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    After the prefs read (from Plan 06-03) and before iterating subs, `sendToMembers` evaluates `prefs.quietHours.enabled`. If true and `payload.forceThroughQuiet` is not set, computes current time in recipient's `tz` via Intl.DateTimeFormat and skips if inside the quiet range. Handles wrap-around (22:00→08:00). No error if `prefs.quietHours` is undefined.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 4: onWatchpartyUpdate sets forceThroughQuiet for RSVP'd recipients</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    When firing watchparty-starting, if the recipient's memberId appears in `wp.rsvps || []`, the `sendToMembers` call for THAT recipient batch passes `forceThroughQuiet: true` in the payload. Implementation: split recipients into two groups (RSVP'd vs not) and send with different payloads. Alternatively: pass `forceThroughQuiet: true` globally when watchparty-starting because if a user opted-in to prefs.watchpartyStarting, quiet-hours shouldn't block the thing they're waiting for. Planner's call — prefer the simpler global approach if scope allows. Document the choice in the SUMMARY.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 5: Client — quiet-hours UI row in updateNotifCard</name>
  <files>js/app.js</files>
  <how-to-verify>
    Settings `notif-prefs` section renders a quiet-hours row with enabled checkbox + two `<input type="time">` pickers + detected-timezone caption. Timezone auto-detected via `Intl.DateTimeFormat().resolvedOptions().timeZone`. Changes call `updateQuietHours({ enabled, start, end, tz })` which writes `users/{uid}.notificationPrefs.quietHours`.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 6: Client — updateQuietHours helper</name>
  <files>js/app.js</files>
  <how-to-verify>
    `updateQuietHours({ enabled, start, end, tz })` exists. Writes to `users/{uid}.notificationPrefs.quietHours` via updateDoc. Supplies tz from `Intl.DateTimeFormat().resolvedOptions().timeZone` if not provided. Validates `start`/`end` are valid `HH:mm` strings.
  </how-to-verify>
</task>

<task type="verify-no-regression" gate="blocking">
  <name>Task 7: Verify no regression + syntax check</name>
  <how-to-verify>
    1. `node --check queuenight/functions/index.js` succeeds
    2. `grep -c "exports\\." queuenight/functions/index.js` shows +2 exports vs pre-plan count (onInviteCreated, onSessionUpdate)
    3. Existing 3 triggers unchanged
    4. All new `sendToMembers` calls pass `eventType`
  </how-to-verify>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Info Disclosure | Invite doc containing recipient email leaks to wrong recipient | mitigate | onInviteCreated resolves recipientUid → memberId and pushes only to that member's subscriptions |
| T-06-04-02 | DoS | Veto-cap trigger fires repeatedly on every vetoCount increment after crossing threshold | mitigate | Guard: `before.vetoCount < CAP && after.vetoCount >= CAP` ensures single-fire on crossing |
| T-06-04-03 | Timezone attack | Malicious user sets quietHours.tz to a bogus value to silence all pushes forever | accept | Intl.DateTimeFormat throws on invalid tz — caught by try/catch, defaults to sending. Server-side validation acceptable future work. |
</threat_model>

<verification>
1. Static checks above pass
2. `node --check queuenight/functions/index.js` succeeds
3. Commit: `feat(06-04): add 3 new event triggers + quiet-hours enforcement (PUSH-03, PUSH-04)`
</verification>

<success_criteria>
- onInviteCreated, onSessionUpdate (veto-cap + tonight-pick) all exported
- Quiet-hours UI works end-to-end on Settings screen
- Server respects quiet hours with RSVP override for watchparty-starting
- No regression on existing 3 triggers
</success_criteria>

<output>
Atomic commit. Plan 06-05 is UAT — checklist execution against the live deploy.
</output>
