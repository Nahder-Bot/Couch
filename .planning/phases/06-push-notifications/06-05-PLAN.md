---
phase: 06-push-notifications
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - .planning/phases/06-push-notifications/06-UAT-RESULTS.md
autonomous: false
requirements: [PUSH-03]
must_haves:
  truths:
    - "06-UAT-RESULTS.md exists with per-scenario pass/fail"
    - "iOS 16.4+ standalone PWA end-to-end push verified on at least one event (watchparty-starting)"
    - "Self-echo guard verified — actor does not receive their own event push"
    - "Per-event toggle verified — flipping a toggle OFF suppresses that event"
    - "Quiet-hours verified — push during quiet window is suppressed (with forceThroughQuiet override confirmed)"
    - "Android delivery verified OR explicitly marked PENDING-HARDWARE"
  artifacts:
    - path: .planning/phases/06-push-notifications/06-UAT-RESULTS.md
      provides: "Phase 6 UAT results"
      min_lines: 60
  key_links: []
---

<objective>
Produce the PUSH-03 proof-of-delivery artifact. Exercise the 6 event triggers against live prod, record per-scenario pass/fail with device + latency + notes. Any fail routes to the owning plan for fix.

Purpose: Without hands-on delivery proof on iOS standalone PWA, we cannot claim PUSH-03 is met. This is the only plan that requires the user to physically interact with their phone; everything else ships code-complete.
Output: `06-UAT-RESULTS.md` with 6 scenarios + overall phase sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-push-notifications/06-CONTEXT.md
@.planning/phases/06-push-notifications/06-RESEARCH.md
@.planning/phases/05-auth-groups/05-UAT-RESULTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: iOS standalone PWA push round-trip (flagship event)</name>
  <what-built>Plans 06-02 through 06-04 shipped the full push stack. This task forces physical-device verification for the watchparty-starting event, which is the flagship PUSH-03 acceptance.</what-built>
  <how-to-verify>
    Required: physical iPhone on iOS 16.4+ with couchtonight.app installed via Safari → Share → Add to Home Screen.

    1. Open PWA from home screen. Sign in.
    2. Settings → Notifications → Turn on (grant permission). Confirm "✓ This device is on" appears.
    3. From a second device (desktop browser or spouse's phone), schedule a watchparty starting ~2 minutes from now. Use an existing test family.
    4. Wait for the watchparty status to transition to 'active' (app fires this automatically on the `startAt` timestamp).
    5. Within ~5s of status flipping to 'active', the iPhone receives a push notification.

    Record: PASS / FAIL, latency (seconds from status-flip to notification showing), any error messages in iOS Safari Web Inspector console.
  </how-to-verify>
  <resume-signal>
    Reply:
    - `ios-pass` — iOS standalone PWA received the push within latency budget; continue to Task 2
    - `ios-fail: <details>` — route back to Plan 06-02/03/04 for fix; flag the Capacitor escape hatch if this is a systemic iOS issue
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Self-echo guard — actor does not receive own push</name>
  <how-to-verify>
    On the iPhone (where push is enabled), schedule a watchparty yourself. Confirm the iPhone does NOT receive a push for it (other family members receive theirs). Repeat with: vote-veto to exceed cap → owner (if you're not the owner, someone else tests) doesn't get push when the veto was theirs; vote the tonight's-pick — the spinner doesn't get a push.
  </how-to-verify>
  <resume-signal>
    Reply:
    - `self-echo-pass` — no self-pushes observed
    - `self-echo-fail: <which event fired incorrectly>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Per-event opt-out works</name>
  <how-to-verify>
    On iPhone: Settings → Notifications → toggle OFF "Watchparty scheduled". From second device, schedule a new watchparty (2 min out). Do NOT receive the "New watchparty" push. Toggle BACK ON. Schedule another watchparty. Push arrives.
  </how-to-verify>
  <resume-signal>
    Reply:
    - `per-event-pass`
    - `per-event-fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Quiet hours suppresses + forceThroughQuiet overrides</name>
  <how-to-verify>
    On iPhone: Settings → Notifications → enable Quiet hours, set start = current-time-minus-5-minutes, end = current-time-plus-5-minutes, timezone auto. From second device, schedule a NEW watchparty (NOT RSVP'd by you yet) — no push arrives while in quiet window. Now RSVP yes on the PWA. The `forceThroughQuiet` path should fire when the watchparty goes `active` — push does arrive even during quiet window (implementation decision per Plan 06-04 Task 4). Turn quiet hours OFF.
  </how-to-verify>
  <resume-signal>
    Reply:
    - `quiet-pass`
    - `quiet-fail: <which branch>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Android — code-complete check (PENDING-HARDWARE acceptable)</name>
  <how-to-verify>
    If an Android device is available: install PWA via Chrome → menu → Install app. Repeat Task 1 scenario. Latency budget same (~5s).

    If no Android device: mark as `PENDING-HARDWARE — Android delivery verified via web-push standard support and existing CF logs show successful sends to Android FCM endpoints. Device-verify deferred.`
  </how-to-verify>
  <resume-signal>
    Reply:
    - `android-pass`
    - `android-pending`
    - `android-fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Write 06-UAT-RESULTS.md</name>
  <how-to-verify>
    Create `.planning/phases/06-push-notifications/06-UAT-RESULTS.md` with the structure:
    ```markdown
    # Phase 6 — Push UAT Results

    **Tested:** YYYY-MM-DD
    **Tester:** name
    **Devices:** iPhone model + iOS version, second device, Android (if any)

    ## Scenarios

    | # | Scenario | Result | Latency | Notes |
    |---|---|---|---|---|
    | 1 | iOS standalone PWA watchparty-starting | PASS/FAIL | Xs | ... |
    | 2 | Self-echo guard (3 event types) | PASS/FAIL | — | ... |
    | 3 | Per-event opt-out | PASS/FAIL | — | ... |
    | 4 | Quiet hours + forceThroughQuiet override | PASS/FAIL | — | ... |
    | 5 | Android delivery | PASS / PENDING / FAIL | Xs | ... |

    ## Outstanding issues
    - ...

    ## Recommendation
    - Phase 6 ready for /gsd-verify-work: YES / NO / PARTIAL
    ```

    Commit with `docs(06-05): UAT results`.
  </how-to-verify>
  <resume-signal>
    Reply:
    - `uat-results-committed` — file committed, phase ready for /gsd-verify-work
    - `fail: <scenarios>` — route to owning plan for fix
  </resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Denial-of-service | UAT fails, blocks phase | mitigate | Resume-signal paths route back to owning plan; partial ships to 6.x follow-up |
</threat_model>

<verification>
1. `06-UAT-RESULTS.md` exists with all 5 scenario outcomes
2. File committed
</verification>

<success_criteria>
- At least iOS flagship event (watchparty-starting) delivers within latency budget
- Self-echo, per-event, quiet-hours all verified
- Android documented (PASS or PENDING-HARDWARE explicitly)
- Phase 6 can transition to /gsd-verify-work
</success_criteria>

<output>
`06-UAT-RESULTS.md`. No code changes.
</output>
