---
phase: 18-availability-notifications
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - C:/Users/nahde/claude-projects/couch/sw.js
autonomous: false
requirements_addressed: [REQ-18-09, REQ-18-10, REQ-18-01, REQ-18-02, REQ-18-03, REQ-18-04, REQ-18-05, REQ-18-06, REQ-18-07, REQ-18-08]
tags: [deploy, sw-cache, cross-repo, uat, phase-18]
user_setup:
  - service: firebase-functions (queuenight repo, sibling)
    why: "Cross-repo deploy ordering — queuenight functions deploy MUST precede couch hosting deploy per RUNBOOK §H + Phase 15.4 + 15.5 precedent. Without this order, clients in the new build will write notificationPrefs.titleAvailable + show the new Settings toggle while no providerRefreshTick CF exists yet, breaking REQ-18-01 + REQ-18-02 path during the deploy gap."
    deploy_steps:
      - "Step 1 (queuenight, FIRST): cd ~/queuenight && firebase deploy --only functions --project queuenight-84044"
      - "Step 1 verify: gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --gen 2 | grep updateTime — must be a fresh timestamp; if --gen 2 is wrong (firebase-functions v1 SDK still in use per TD-1), retry with --gen 1 or no flag"
      - "Step 1 NOTE: firestore.rules NOT deployed (D-23 — no rules changes for Phase 18)"
      - "Step 2 (couch, SECOND): cd ~/claude-projects/couch && bash scripts/deploy.sh 36-availability-notifs"
      - "Step 2 auto-bumps sw.js CACHE to 'couch-v36-availability-notifs', runs tests/npm test pre-flight gate, runs §2.5 smoke gate (NOW INCLUDES smoke-availability per Plan 18-03), mirrors couch repo to couch-deploy-mirror, runs firebase deploy --only hosting"
      - "Step 3 (UAT — checkpoint:human-verify): Settings UI parity smoke + manual-refresh affordance smoke + simulated-CF-tick smoke (the daily CF only fires once per 24h; manual trigger via gcloud or wait-for-cycle)"
must_haves:
  truths:
    - "sw.js CACHE constant bumped to 'couch-v36-availability-notifs' (per CONTEXT.md D-22)"
    - "scripts/deploy.sh tests/ pre-flight gate passes (existing 46 rules tests — no new rules in Phase 18 per D-23)"
    - "scripts/deploy.sh §2.5 smoke gate passes including the new smoke-availability.cjs invocation (Plan 18-03)"
    - "Cross-repo deploy ordering followed: queuenight FUNCTIONS deploy precedes couch HOSTING deploy"
    - "queuenight functions deploy includes providerRefreshTick as a newly-created scheduled CF (visible in firebase functions:list with schedule 'every 24 hours' in us-central1)"
    - "Live cache version visible at couchtonight.app/sw.js matches the deployed tag 'couch-v36-availability-notifs'"
    - "Live couchtonight.app/app loads without errors on a test device"
    - "UAT 1: Settings UI on a logged-in device renders the new 'Newly watchable' push toggle (default ON) with the verbatim hint 'When a title in your queue lands on a service in your pack.'"
    - "UAT 2: Title detail modal Where-to-watch section renders the '↻ Refresh availability' button (label updated from '↻ Refresh') AND the 'Provider data via TMDB' italic attribution footnote below the providers strip"
    - "UAT 3: Tapping '↻ Refresh availability' produces a 'Availability refreshed.' toast AND the title doc's lastProviderRefreshAt timestamp is updated in Firestore"
    - "UAT 4: Simulated CF tick (manual gcloud trigger OR wait 24h post-deploy) for a family with a queued title that has new providers added to TMDB results in a real push to the matching family member's device with body matching D-14 single-title or D-15 batch template"
    - "UAT 5: Toggling 'Newly watchable' OFF in Settings → simulated CF tick → no push arrives on the toggled-off device (server-side sendToMembers eventType-gate verified end-to-end)"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/sw.js"
      provides: "Bumped CACHE constant"
      contains: "couch-v36-availability-notifs"
  key_links:
    - from: "Plan 18-01 queuenight functions edits"
      to: "deployed providerRefreshTick CF + titleAvailable in NOTIFICATION_DEFAULTS in us-central1 / queuenight-84044"
      via: "firebase deploy --only functions"
      pattern: "firebase deploy --only functions"
    - from: "Plan 18-02 couch js/app.js edits + Plan 18-03 smoke + Task 1 sw.js bump"
      to: "deployed hosting at couchtonight.app"
      via: "bash scripts/deploy.sh 36-availability-notifs"
      pattern: "bash scripts/deploy.sh"
    - from: "Daily Cloud Scheduler tick → providerRefreshTick CF"
      to: "Push notification on member's device when t.providers gains new brand intersecting m.services"
      via: "TMDB watch/providers fetch → diff → sendToMembers eventType:'titleAvailable' → FCM"
      pattern: "just hit.*for your household|are now watchable"
---

<objective>
Plan 04 — Final wave-2 closeout for Phase 18. Bump sw.js CACHE so installed PWAs
invalidate on next online activation, run the cross-repo deploy ritual per
RUNBOOK §H + Phase 15.4/15.5 precedent, then run UAT to confirm REQ-18-01..10
end-to-end.

Deploy ordering is critical (D-21 in CONTEXT.md):

1. **queuenight functions FIRST** (`firebase deploy --only functions --project
   queuenight-84044` from `~/queuenight/`) — installs Plan 18-01's
   providerRefreshTick scheduled CF + titleAvailable in NOTIFICATION_DEFAULTS.
   Without this, the new client (after step 2 ships) would write
   `notificationPrefs.titleAvailable` to users/{uid} doc fine (no rules change
   needed there) BUT no CF would exist to fire pushes for the diff — REQ-18-01
   + REQ-18-02 would silently break during the deploy gap.

   NOTE: NO firestore.rules deploy (D-23 — Phase 18 has no rules changes).

2. **couch hosting SECOND** (`bash scripts/deploy.sh 36-availability-notifs` from
   couch repo) — ships the UI changes from Plan 18-02 (titleAvailable in 5
   client maps + manual-refresh affordance + via-TMDB attribution + lastProviderRefreshAt
   write) + the bumped sw.js CACHE + the new smoke gate from Plan 18-03.

3. **UAT** — Settings UI smoke + detail-modal affordance smoke + simulated CF
   tick smoke + opt-out verification.

Plan 04 is **non-autonomous** — the executor cannot deploy or run real-device
UAT without user-side action. Tasks split:

- **Task 1 (autonomous):** bump sw.js CACHE in source.
- **Task 2 (auto, deploy authorized):** run `firebase deploy --only functions`
  from `~/queuenight/` per `feedback_deploy_autonomy.md` MEMORY note (Claude may
  run firebase deploy to queuenight-84044 without per-deploy approval for the
  Couch project). If auth fails, escalate to checkpoint:human-action.
- **Task 3 (auto, deploy authorized):** run `bash scripts/deploy.sh 36-availability-notifs`.
  Same authorization basis. If auth fails, escalate to checkpoint:human-action.
- **Task 4 (checkpoint:human-verify):** UAT — Settings UI smoke + detail-modal
  affordance smoke + simulated CF tick smoke + opt-out smoke. The daily CF
  cadence (D-01) means real push delivery requires either a manual gcloud trigger
  OR a 24h wait. The user is the only one who can confirm push delivery on a
  real device.

Per `feedback_deploy_autonomy.md`: "For Couch project, Claude may run firebase
deploy to queuenight-84044 without per-deploy approval." This precedent landed
in Phase 15.4-03 + 15.5-07. Pragmatic shape: Claude attempts the deploy directly
via Bash; if the firebase CLI returns auth/network error, the executor surfaces
the failure and pauses for user intervention. Otherwise the deploy runs
autonomously.

**A note on the simulated CF tick (UAT 4):** the daily schedule means the CF
won't fire until ~24h after deploy unless manually triggered. Two options:

(a) **Wait for natural cycle** (24h soak, then check Sentry + member devices for
    push delivery). Simplest, lowest-effort. RECOMMENDED for steady-state UAT.
(b) **Manual gcloud trigger** (`gcloud scheduler jobs run firebase-schedule-providerRefreshTick-us-central1
    --project queuenight-84044`). Fires the CF immediately for testing. RECOMMENDED
    for first-deploy UAT to confirm wiring before the 24h window.

UAT 4 instructions cover both paths.

Purpose: Ship Phase 18 end-to-end + verify push delivery + Settings parity
+ manual refresh in production with PWA cache invalidation, gated by UAT.

Output: sw.js CACHE bumped; live deploys to queuenight-84044 (functions only)
+ couchtonight.app (hosting); UAT confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/18-availability-notifications/18-CONTEXT.md
@.planning/phases/18-availability-notifications/18-01-PLAN.md
@.planning/phases/18-availability-notifications/18-02-PLAN.md
@.planning/phases/18-availability-notifications/18-03-PLAN.md
@.planning/phases/15.4-integration-polish/15.4-03-PLAN.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

sw.js current source line 8:
  const CACHE = 'couch-v35.6-integration-polish';

This is the post-15.4-deploy cache version (live at couchtonight.app per STATE.md).
Plan 18-04 bumps to:
  const CACHE = 'couch-v36-availability-notifs';

Tag rationale per CONTEXT.md D-22: "couch-v36-availability-notifs" — bumps the
major version to v36 (Phase 18 is a new top-level capability, not a decimal
polish). Phase number reflects 18-availability-notifications slug.

scripts/deploy.sh — verified shape per Plan 18-03 wiring:
- §1 dirty-tree gate
- §2 tests/npm test pre-flight
- §2.5 pure-function smoke gate (NOW INCLUDES smoke-availability per Plan 18-03)
- §3 mirror existence check
- §4 sw.js CACHE bump (idempotent if Task 1 already bumped)
- §5 mirror to couch-deploy/public/
- §6 BUILD_DATE auto-stamp in mirror
- §7 Sentry DSN placeholder guard
- §8 firebase deploy --only hosting
- §9 curl-based smoke tests against couchtonight.app

`firebase deploy --only functions --project queuenight-84044` from `~/queuenight/`
— separate command (NOT covered by deploy.sh). Per RUNBOOK §H:
  "Cloud Functions deploy is separate (NOT covered by deploy.sh — too risky to bundle)"

NO `firestore:rules` deploy needed (D-23). The Phase 15.4-03 precedent's combined
`--only functions,firestore:rules` flag is unnecessary here.

UAT inputs needed (Task 4):
- One device signed in to a family that has at least one queued (yes-voted) unwatched
  title in the title catalog
- A second device signed in as a different family member (for opt-out push test)
- Both devices have notifications enabled (granted permission)
- Either patience for 24h cycle OR ability to run gcloud scheduler manual trigger

Manual gcloud trigger (alternative to 24h wait):
```bash
gcloud scheduler jobs run firebase-schedule-providerRefreshTick-us-central1 \
  --project queuenight-84044 \
  --location us-central1
```
The firebase CLI auto-creates a Scheduler job named
`firebase-schedule-{exportName}-{region}` per the v2 onSchedule API. This name
matches the export name `providerRefreshTick` from Plan 18-01.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump sw.js CACHE constant to couch-v36-availability-notifs</name>
  <files>C:/Users/nahde/claude-projects/couch/sw.js</files>
  <read_first>
    - Read sw.js with offset=1, limit=15 — confirm current CACHE constant value at line 8.
    - Confirm via grep that no other line in sw.js declares CACHE.
  </read_first>
  <action>
    Edit sw.js line 8. Current:
      const CACHE = 'couch-v35.6-integration-polish';

    Change to:
      const CACHE = 'couch-v36-availability-notifs';

    Rationale per CONTEXT.md D-22: bump CACHE to a phase-aligned tag. Phase 18 ships
    multiple user-visible changes — daily provider-refresh CF (visible via push
    notifications on receiver's device when titles become newly watchable) + 1 new
    Settings toggle (visible in Account → notifications) + refreshed manual-refresh
    affordance ("↻ Refresh availability" + "Provider data via TMDB" footnote in
    detail modal). The CACHE bump is mandatory per CLAUDE.md: "any user-visible
    app change requires a sw.js CACHE bump."

    If `bash scripts/deploy.sh 36-availability-notifs` is run in Task 3, deploy.sh
    will RE-BUMP this same CACHE constant — idempotent. Bumping in source first is
    defensive (lets `bash scripts/deploy.sh` without args still ship the right tag)
    and matches the Phase 15.4-03 + 15.5-07 close-out pattern.
  </action>
  <verify>
    <automated>grep -nc "^const CACHE = 'couch-v36-availability-notifs';" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 1 match (line 8).
    <automated>grep -nc "couch-v35.6-integration-polish" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 0 (old tag fully replaced).
    <automated>grep -nc "^const CACHE" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    Expected: 1 (no duplicate CACHE declarations introduced).
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c sw.js</automated>
    Expected: exit 0.
  </verify>
  <acceptance_criteria>
    - sw.js line 8 contains: const CACHE = 'couch-v36-availability-notifs';
    - Old couch-v35.6-integration-polish tag is REMOVED.
    - Exactly ONE const CACHE declaration in sw.js.
    - node -c sw.js exits 0.
    - Closes (partial): REQ-18-09.
  </acceptance_criteria>
  <done>
    sw.js CACHE bumped to couch-v36-availability-notifs in source.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deploy queuenight functions FIRST (cross-repo ordering)</name>
  <files></files>
  <read_first>
    - Read .planning/RUNBOOK.md §H (offset=226, limit=70) — cross-repo deploy ordering reference.
    - Read .planning/phases/15.4-integration-polish/15.4-03-PLAN.md Task 2 — Phase 15.4 deploy precedent.
    - Confirm Plan 18-01 changes are committed in the queuenight repo before running deploy. If queuenight has no .git on this machine (matches Phase 14-09 + 15.4-01 + 15.5-05 deviation pattern), surface this as a deviation and pause for user to commit on a queuenight-equipped session — proceed with deploy only after commit confirmation.
  </read_first>
  <action>
    Per `feedback_deploy_autonomy.md` MEMORY note ("For Couch project, Claude may
    run firebase deploy to queuenight-84044 without per-deploy approval"), this
    task is AUTHORIZED to run firebase deploy directly. Shape:

    1. Pre-flight verification — confirm Plan 18-01 changes landed in queuenight:
       <automated>grep -nc "titleAvailable: true" C:/Users/nahde/queuenight/functions/index.js</automated>
       <automated>grep -nc "exports.providerRefreshTick" C:/Users/nahde/queuenight/functions/index.js</automated>
       <automated>grep -nc "every 24 hours" C:/Users/nahde/queuenight/functions/index.js</automated>
       <automated>grep -nc "module.exports.addedBrandsFor" C:/Users/nahde/queuenight/functions/index.js</automated>
       All 4 expected to return 1.

    2. Pre-flight commit check — `cd C:/Users/nahde/queuenight && git status --short`.
       If not empty, this means Plan 18-01's queuenight edits are uncommitted on
       this machine (matches the Phase 14-09 + 15.4-01 + 15.5-05 deviation pattern
       when queuenight has no .git here). Surface this in the executor output —
       pause for the user to commit from a queuenight-equipped session before
       continuing. The commit message format:
       `feat(18-01): add providerRefreshTick CF + titleAvailable + addedBrands/buildPushBody helpers`

    3. Run the cross-repo deploy:
       <automated>cd C:/Users/nahde/queuenight && firebase deploy --only functions --project queuenight-84044</automated>
       Expected output: ends with "Deploy complete!" and lists `providerRefreshTick(us-central1)`
       in the deployed-functions list with its scheduler job auto-created.

    4. Verify deploy timestamps:
       <automated>cd C:/Users/nahde/queuenight && firebase functions:list --project queuenight-84044 2>&1 | grep -i providerrefresh</automated>
       Expected: a row containing "providerRefreshTick" with a recent updateTime.

       OR via gcloud (try gen2 first since onSchedule v2 API is used):
       <automated>gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --gen2 --format="value(updateTime)" 2>&1 || gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --format="value(updateTime)"</automated>
       Expected: timestamp within the last few minutes.

    5. Verify Cloud Scheduler job auto-created:
       <automated>gcloud scheduler jobs list --project queuenight-84044 --location us-central1 2>&1 | grep -i providerrefresh</automated>
       Expected: a job named `firebase-schedule-providerRefreshTick-us-central1`
       (or similar) with schedule "every 24 hours".

    6. Confirm TMDB_KEY env var is set in queuenight functions config (required for
       providerRefreshTick to actually fetch data — without it, the CF logs a
       warning and short-circuits):
       <automated>cd C:/Users/nahde/queuenight && cat functions/.env 2>&1 | grep -ci TMDB_KEY</automated>
       Expected: ≥ 1 (TMDB_KEY line present).
       If 0 → surface to user: "TMDB_KEY env var is missing from queuenight/functions/.env.
       Copy the same TMDB_KEY value from couch/js/constants.js TMDB_KEY into
       queuenight/functions/.env, then re-run `firebase deploy --only functions
       --project queuenight-84044` to bake it into the CF config."

    Failure modes:
    - `Error: Authentication Error: Your credentials are no longer valid` → user must
      `firebase login` first. Surface error verbatim, escalate to checkpoint:human-action.
    - `Error: HTTP Error: 403, Permission denied` → IAM issue, surface verbatim, escalate.
    - TMDB_KEY missing → providerRefreshTick will deploy successfully but log warnings on each tick
      until TMDB_KEY is set. Surface this to user as a follow-up to fix before the first daily tick.

    If steps 1-6 all succeed, mark Task 2 complete and proceed to Task 3.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/queuenight && firebase functions:list --project queuenight-84044 2>&1 | grep -ci providerRefreshTick</automated>
    Expected: ≥ 1 (the function appears in the deployed list).
    <automated>gcloud scheduler jobs list --project queuenight-84044 --location us-central1 --format="value(name)" 2>&1 | grep -ci providerrefresh</automated>
    Expected: ≥ 1 (the scheduler job auto-created by firebase v2 onSchedule deploy).
  </verify>
  <acceptance_criteria>
    - firebase deploy --only functions --project queuenight-84044 exited 0 with "Deploy complete!" output.
    - providerRefreshTick function appears in firebase functions:list with a recent updateTime.
    - Cloud Scheduler job auto-created in us-central1.
    - The deploy ran from C:/Users/nahde/queuenight (NOT from couch repo).
    - TMDB_KEY env var present in queuenight/functions/.env (or surfaced to user as a follow-up).
    - Plan 18-01's queuenight edits were committed before deploy (NOT just applied in-place — that was the 14-09 + 15.4-01 + 15.5-05 deviation pattern; this plan should not repeat the deviation if avoidable).
    - Closes (partial): REQ-18-10 functions half + REQ-18-01 deploy half.
  </acceptance_criteria>
  <done>
    queuenight functions deployed. providerRefreshTick CF live in us-central1 with
    auto-created scheduler job. titleAvailable in NOTIFICATION_DEFAULTS server-side
    gate live. Cross-repo ordering preserved (this task runs BEFORE Task 3).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Deploy couch hosting SECOND via scripts/deploy.sh</name>
  <files></files>
  <read_first>
    - Read .planning/RUNBOOK.md §H (already in context from Task 2).
    - Confirm Task 2 succeeded (queuenight deploy timestamp recent + providerRefreshTick visible).
    - Confirm Plan 18-02 changes are committed in couch repo + Plan 18-03 smoke is committed + Task 1 sw.js bump committed.
  </read_first>
  <action>
    Per `feedback_deploy_autonomy.md`, this task is AUTHORIZED to run the couch
    deploy directly. Shape:

    1. Confirm Task 2 succeeded (queuenight functions live):
       <automated>gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --gen2 --format="value(updateTime)" 2>&1 | head -1 || gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --format="value(updateTime)" 2>&1 | head -1</automated>
       Expected: very recent timestamp (within last hour). If Task 2 was skipped or
       failed, DO NOT proceed — there will be a failure window during which clients
       in the new build show the new Settings toggle but no CF exists to fire pushes.

    2. Confirm couch repo is cleanly committed:
       <automated>cd C:/Users/nahde/claude-projects/couch && git status --short</automated>
       Expected: empty (clean working tree). Plans 18-02 + 18-03 + Task 1 sw.js bump
       should all be committed.

    3. Confirm sw.js CACHE bump landed:
       <automated>grep -n "^const CACHE = 'couch-v36-availability-notifs';" C:/Users/nahde/claude-projects/couch/sw.js</automated>
       Expected: 1 match (Task 1 of this plan).

    4. Confirm Plan 18-02 client changes landed:
       <automated>grep -nc "Newly watchable" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
       <automated>grep -nc "↻ Refresh availability" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
       <automated>grep -nc "Provider data via TMDB" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
       <automated>grep -nc "lastProviderRefreshAt: Date.now()" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
       All 4 expected to return ≥ 1.

    5. Confirm Plan 18-03 smoke + deploy.sh wiring landed:
       <automated>ls C:/Users/nahde/claude-projects/couch/scripts/smoke-availability.cjs</automated>
       Expected: file exists.
       <automated>grep -c "smoke-availability.cjs" C:/Users/nahde/claude-projects/couch/scripts/deploy.sh</automated>
       Expected: 2 (the new gate wiring from Plan 18-03 Task 2).

    6. Run the hosting deploy:
       <automated>cd C:/Users/nahde/claude-projects/couch && bash scripts/deploy.sh 36-availability-notifs</automated>
       This invokes the full deploy ritual:
       - cd tests && npm test (rules-emulator suite — no new rules per D-23, so existing 46 tests apply unchanged)
       - node --check on all shipping JS files
       - smoke-position-transform.cjs + smoke-tonight-matches.cjs + smoke-availability.cjs (NEW from Plan 18-03)
       - sw.js CACHE confirmed (no-op since Task 1 set it)
       - Mirrors couch root + js/ + css/ to ${COUCH_DEPLOY_PATH}/public/
       - Auto-stamps BUILD_DATE in mirror's js/constants.js
       - Aborts if app.html / landing.html still contain Sentry DSN placeholder
       - firebase deploy --only hosting --project queuenight-84044
       - Curl-based smoke tests against couchtonight.app
       Expected output ends with "Deploy complete!" and a couchtonight.app project URL.

    7. Verify live cache version:
       <automated>curl -s https://couchtonight.app/sw.js | grep "^const CACHE"</automated>
       Expected: const CACHE = 'couch-v36-availability-notifs';

    8. Verify Sentry DSN substituted (NOT placeholder):
       <automated>curl -s https://couchtonight.app/app | grep -oE 'sentry\\.io/[0-9]+' | head -1</automated>
       Expected: a real Sentry project URL.

    9. Quick rendering smoke check by curl:
       <automated>curl -sI https://couchtonight.app/app | head -3</automated>
       Expected: HTTP/1.1 200 OK.

    Failure modes:
    - tests/npm test fails → existing rules suite regression (Phase 18 didn't touch
      rules, so this would surface a non-Phase-18 issue). Surface output, escalate.
    - smoke-availability.cjs FAIL → Plan 18-01 helpers broke contract. Surface assertion
      output, escalate to checkpoint:human-action.
    - `working tree is dirty` → user has uncommitted edits. Surface, ask user to commit.
    - Auth error → surface, escalate.
    - DSN placeholder leak → surface, escalate (Plan 13 / OPS-13-05 guard).

    If steps 1-9 all succeed, proceed to Task 4.
  </action>
  <verify>
    <automated>curl -s https://couchtonight.app/sw.js | grep -c "couch-v36-availability-notifs"</automated>
    Expected: 1 (live deploy reflects the bumped tag).
    <automated>curl -sI https://couchtonight.app/app 2>&1 | head -1</automated>
    Expected: contains "200 OK" (app shell loads).
    <automated>curl -s https://couchtonight.app/app | grep -oE 'sentry\\.io/[0-9]+' | head -1</automated>
    Expected: a real Sentry DSN URL (NOT __SENTRY_DSN__).
  </verify>
  <acceptance_criteria>
    - bash scripts/deploy.sh 36-availability-notifs exited 0.
    - tests/ pre-flight gate passed (existing 46 rules tests).
    - §2.5 smoke gate passed including smoke-availability.cjs.
    - Live cache version at couchtonight.app/sw.js is couch-v36-availability-notifs.
    - couchtonight.app/app loads with HTTP 200.
    - Sentry DSN is a real URL, not the placeholder.
    - Closes (partial): REQ-18-09 (PWA cache invalidation live), REQ-18-10 (cross-repo ordering preserved).
  </acceptance_criteria>
  <done>
    couch hosting deployed via scripts/deploy.sh. sw.js bumped + tests passed +
    new smoke gate ran + live deploy verified via curl. Cross-repo ordering preserved
    (this task runs AFTER Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — Settings UI parity + manual refresh affordance + simulated CF tick</name>
  <what-built>
    All Phase 18 user-visible behavior is now live at couchtonight.app at cache
    version couch-v36-availability-notifs:

    - REQ-18-04 closure: Settings (Account → notifications) now exposes a 'Newly
      watchable' push toggle (default ON) with hint 'When a title in your queue
      lands on a service in your pack.' Surfaces in BOTH legacy NOTIFICATION_EVENT_LABELS
      Settings UI AND Phase 12 friendly-UI Settings UI (TD-8 dual-Settings collision
      already deferred — no new collision).

    - REQ-18-03 closure: Title detail modal Where-to-watch section gains a refreshed
      manual-refresh affordance — '↻ Refresh availability' button (label updated
      from '↻ Refresh') AND 'Provider data via TMDB' italic attribution footnote
      below the providers strip. Manual refresh writes lastProviderRefreshAt so
      the daily CF round-robin stays accurate.

    - REQ-18-01 + REQ-18-02 closure: Daily Cloud Function `providerRefreshTick`
      runs every 24 hours in us-central1, iterates families → queued unwatched
      titles, refreshes TMDB watch/providers for up to 50 titles per tick (round-
      robin oldest-first via lastProviderRefreshAt), writes back providers +
      providersSchemaVersion + lastProviderRefreshAt with setBy:'system' marker,
      and fires a push to family members whose `m.services` intersects newly-added
      brands. Push body voice: D-14 single-title "Dune just hit Max for your
      household." or D-15 batch "{N} titles your couch wants are now watchable:
      ..." capped at 3 surfaced titles + 3 distinct brands.

    - REQ-18-05 + REQ-18-07 closure: Existing per-event opt-out via Settings UI
      suppresses pushes server-side (sendToMembers eventType-gate). Quiet-hours
      respected via existing isInQuietHours.

    - REQ-18-06 closure: System push (no sender) — self-echo guard logically n/a;
      members without matching service intersection get no push.

    - REQ-18-08 closure: TMDB rate-limit handling — 429 detection aborts remainder
      of tick, retry next 24h cycle.

    - REQ-18-09 + REQ-18-10 closure: sw.js CACHE bumped to couch-v36-availability-notifs;
      cross-repo deploy ritual (queuenight functions FIRST, couch hosting SECOND)
      followed per RUNBOOK §H.
  </what-built>
  <how-to-verify>
    Single-device UAT covers Settings UI + manual refresh affordance smoke.
    Two-device UAT (or single device with a second member's view) covers the
    push-delivery + opt-out smoke. The CF push UAT requires either a 24h soak OR
    a manual gcloud trigger.

    Pre-flight (single device):
    1. Open couchtonight.app/app on a test device. Hard-refresh OR wait ~10s for
       the SW activate handler to clear old caches. Then open DevTools >
       Application > Service Workers — confirm the active SW shows CACHE name
       'couch-v36-availability-notifs'. OR run in JS console:
       `caches.keys().then(console.log)` — expect 'couch-v36-availability-notifs'
       in the list.

    UAT 1 — Settings UI shows the new toggle (single device):
    2. On the test device, open Account → notifications (legacy Settings UI) AND
       any friendly-UI Settings surface that exists (the dual-Settings collision is
       a known deferred TD-8). In each surface, confirm a NEW toggle appears
       labeled 'Newly watchable' with hint 'When a title in your queue lands on a
       service in your pack.' (verbatim copy).
    3. Confirm the toggle defaults to ON (matches NOTIF_UI_DEFAULTS.titleAvailable
       = true and DEFAULT_NOTIFICATION_PREFS.titleAvailable = true).
    4. Tap the toggle OFF, confirm Firestore users/{uid}.notificationPrefs.titleAvailable
       is now false (verifiable via the Firebase console OR via reloading the
       Settings UI — toggle should still show OFF after reload, confirming
       persistence).
    5. Tap the toggle back ON to leave Settings in default state.

    UAT 2 — Detail-modal manual refresh affordance (single device):
    6. On the test device, navigate to any tile (Tonight tab or Library tab) for a
       TMDB-sourced title (id starts with 'tmdb_', not isManual) that has
       providersChecked: true.
    7. Tap the tile to open the detail modal. Scroll to the Where-to-watch section.
    8. Confirm the section displays:
       a. The provider strip(s) (Stream / Rent / Buy as applicable)
       b. A '↻ Refresh availability' button (NOT '↻ Refresh' — the label was updated
          per D-17)
       c. A 'Provider data via TMDB' italic + low-opacity footnote below the button
          (per D-16)
    9. Tap '↻ Refresh availability'. Expected behavior:
       - Within ~1-2s, a flashToast appears: 'Availability refreshed.'
       - The detail modal re-renders with fresh provider data
       - The title doc's lastProviderRefreshAt field is updated to a recent
         timestamp (verifiable via the Firebase console)
    10. (Optional) Open Firebase Console → Firestore → families/{code}/titles/{id} →
        confirm the lastProviderRefreshAt field is present and contains a recent
        epoch ms value matching the time of the click.

    UAT 3 — Simulated CF tick (push delivery smoke):
    11. CHOOSE ONE of two paths:

        PATH A (manual gcloud trigger — recommended for first-deploy UAT):
        Run from a gcloud-equipped session:
        ```bash
        gcloud scheduler jobs run firebase-schedule-providerRefreshTick-us-central1 \
          --project queuenight-84044 --location us-central1
        ```
        The CF fires within ~5 seconds. Watch the queuenight Logs Explorer:
        ```bash
        gcloud logging read 'resource.type="cloud_function" AND resource.labels.function_name="providerRefreshTick"' \
          --project queuenight-84044 --limit 50 --format=json
        ```
        Confirm a fresh log entry with '[18/providerRefreshTick] tick complete'
        and a non-zero `processed` count.

        PATH B (24h soak — recommended for steady-state UAT):
        Wait 24 hours after deploy. The CF fires automatically on its daily
        schedule. After 24h, check the Logs Explorer same as Path A. The
        `lastProviderRefreshAt` field on queued titles should also have advanced.

    12. To trigger an actual push in either path, you need a queued unwatched
        title whose TMDB providers actually CHANGED between the prior fetch and
        the current fetch. This is the realistic case (TMDB lag is 24-72h, so
        provider changes happen daily across a portfolio of 100+ queued titles
        across all families — expect 0-3 pushes/day).
        For a forced UAT (no waiting for natural changes):
        a. Manually edit a title's `t.providers` field in Firebase Console to
           REMOVE one brand (e.g., remove 'Max' from "Dune"'s providers).
        b. Wait for next CF tick (Path A or Path B above).
        c. The CF will re-fetch from TMDB and detect the brand has been "added back"
           (because TMDB still shows it). For a member with 'Max' in their
           m.services, a push fires.
        d. Push body should match D-14 verbatim: 'Dune just hit Max for your household.'

    13. Confirm the push body voice. Single-title body should match D-14 verbatim
        ('{titleName} just hit {brand} for your household.'). If multiple titles
        for the same member fired in the same tick, batch body should match D-15:
        '{N} titles your couch wants are now watchable: {t1}, {t2}, +{N-2} more on
        {brand1}, {brand2}…'.

    UAT 4 — Opt-out verification (single device):
    14. On the test device, toggle 'Newly watchable' OFF in Settings.
    15. Trigger a CF tick again (Path A from UAT 3) for a title where the device's
        member would otherwise receive a push.
    16. Confirm NO push arrives on the toggled-off device (server-side
        sendToMembers eventType-gate at queuenight/functions/index.js:138 silently
        drops the send when notificationPrefs.titleAvailable === false).
    17. Toggle 'Newly watchable' back ON to leave Settings in default state.

    UAT 5 — Optional: quiet-hours smoke (single device, optional):
    18. If the user has quiet hours configured, set them to include the current
        time. Trigger a CF tick. Confirm no push arrives. Outside quiet hours,
        push arrives. (Existing Phase 6 behavior; this just confirms eventType
        flow inherits it.)

    All 4 UAT scripts must pass before declaring Phase 18 SHIPPED. UAT 5 is
    optional.

    If any script reveals a bug (toggle missing, button label wrong, push doesn't
    arrive, opt-out doesn't suppress, body voice wrong), surface the failure mode
    verbatim. Decide together whether to gap-close (new plan 18-05) or back out
    the deploy.

    Optional post-deploy soak: monitor Sentry for unexpected
    [18/providerRefreshTick] error breadcrumbs over the first 7 days. TMDB 429s
    abort remainder of tick + log; persistent 429 spike past 1 day triggers
    investigation (rate-limit might mean per-tick cap should drop).
  </how-to-verify>
  <resume-signal>Type "uat passed" to mark Phase 18 fully shipped. If any script failed, paste the failure mode and we'll triage gap-closure plans vs rollback.</resume-signal>
  <acceptance_criteria>
    - All 4 UAT scripts (1 + 2 + 3 + 4) PASSED. UAT 5 optional.
    - User confirmation via "uat passed" or equivalent.
    - REQ-18-01..10 all confirmed end-to-end on real devices (or via simulated CF tick + Logs Explorer).
    - Settings parity confirmed (toggle renders correctly in surfaces).
    - Manual refresh affordance confirmed (button label + via-TMDB footnote + lastProviderRefreshAt write).
    - CF push delivery confirmed (via gcloud trigger OR 24h soak).
    - Opt-out path verified (toggle off → push suppressed).
    - No regressions in adjacent features (existing push categories still work; existing manual refreshProviders behavior preserved minus the new lastProviderRefreshAt write).
    - Closes: REQ-18-01..10 all 10 success criteria.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy pipeline | Cross-repo deploy ordering enforces D-21 (no rules-write-window between hosting and functions deploys; though D-23 means no rules changes here, the functions deploy still must precede so the CF is live when clients gain the new Settings toggle). |
| Cloud Scheduler tick | GCP-managed scheduler fires the CF on its 24h schedule. No external trigger surface. |
| Live FCM push delivery | Per-receiver eventType-gate at sendToMembers verifies the deployed CF + the deployed user prefs are in sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Tampering (deploy ordering inverted — couch hosting deploys before queuenight functions) | Cross-repo deploy gap | mitigate | Task 2 (queuenight functions) MUST complete before Task 3 (couch hosting). The task sequencing in this plan enforces the order. If Task 2 fails, Task 3 does not run — no failure window where new clients show the new Settings toggle but no CF exists to fire pushes. |
| T-18-15 | Tampering (old PWA caches serving stale js/app.js without titleAvailable in maps) | sw.js CACHE bump | mitigate | Task 1 bumps CACHE to couch-v36-availability-notifs. Installed PWAs invalidate on next online activation. Old PWAs continue to work in degraded mode (no 'Newly watchable' toggle visible; titleAvailable defaults to OFF for missing prefs since DEFAULT_NOTIFICATION_PREFS lookup falls back to NOTIFICATION_DEFAULTS server-side which DOES have titleAvailable: true). Acceptable graceful degradation. |
| T-18-16 | Tampering (post-deploy malformed title docs from manual edits) | providerRefreshTick CF | mitigate | Plan 18-01 includes per-doc try/catch (title fetch error doesn't cascade across family) + defensive isManual + tmdbId-bearing filters. Storage cost remains bounded by the per-tick cap of 50 titles. |
| T-18-17 | Configuration (TMDB_KEY missing from queuenight functions/.env at deploy time) | providerRefreshTick env-var read | mitigate | Task 2 step 6 verifies TMDB_KEY is present before completing deploy. If missing, surfaces to user as a follow-up before the first daily tick (CF will still deploy successfully but log a warning). |

ASVS L1: deploy pipeline ordering enforced by sequential task dependencies; Sentry
DSN production-boundary guard enforced by deploy.sh; rules-emulator pre-flight gate
enforced by deploy.sh (existing 46 tests, no new tests needed per D-23); pure-function
smoke gate enforced by Plan 18-03.
</threat_model>

<verification>
**Plan-level checks (after all 4 tasks committed and deployed):**

1. `grep -nc "couch-v36-availability-notifs" sw.js` returns 1 (source bumped — Task 1)
2. `curl -s https://couchtonight.app/sw.js | grep -c CACHE` returns 1 with the new tag (live deploy — Task 3)
3. `gcloud functions describe providerRefreshTick --project queuenight-84044 --region us-central1 --gen2 --format="value(updateTime)"` returns a timestamp within the deploy window (Task 2)
4. `gcloud scheduler jobs list --project queuenight-84044 --location us-central1` includes a `firebase-schedule-providerRefreshTick-us-central1` job (Task 2)
5. UAT 1-4 PASSED per user confirmation (Task 4)
6. Phase 18 SHIPPED marker added to STATE.md by post-execution workflow (separate `/gsd-verify-work` step)
</verification>

<success_criteria>
**Plan 04 is complete when:**
- [ ] sw.js CACHE bumped to couch-v36-availability-notifs
- [ ] queuenight functions deployed (Plan 18-01 changes live in us-central1) — providerRefreshTick CF visible in functions:list
- [ ] Cloud Scheduler job auto-created for the daily 24h cadence
- [ ] couch hosting deployed via scripts/deploy.sh (Plan 18-02 + Plan 18-03 + Task 1 live at couchtonight.app)
- [ ] tests/ pre-flight gate passed (existing 46 rules tests)
- [ ] §2.5 smoke gate passed including smoke-availability.cjs
- [ ] Cross-repo deploy ordering followed (functions → hosting, NOT the reverse)
- [ ] Live cache tag verified via curl
- [ ] UAT 1-4 (Settings UI + manual refresh affordance + simulated CF tick + opt-out) all PASSED
- [ ] No regressions in adjacent features
- [ ] TMDB_KEY env var confirmed in queuenight/functions/.env

**Optional follow-up:**
- 7-day Sentry soak window for [18/providerRefreshTick] errors
- 30-day usage analytics on titleAvailable opt-out rate (RESEARCH risk 2 — if opt-out rate >30%, revisit cadence/threshold per Deferred Ideas)
</success_criteria>

<output>
After completion (post-UAT-pass), create `.planning/phases/18-availability-notifications/18-04-SUMMARY.md` describing:
- Cache version before/after (couch-v35.6-integration-polish → couch-v36-availability-notifs)
- queuenight deploy timestamp + functions:list output (providerRefreshTick visible) + scheduler job name
- couch deploy timestamp + live tag verification (curl output)
- UAT script results (1/2/3/4 pass/fail per script + optional UAT 5)
- Any post-deploy issues observed (rollback decisions, gap-closure plan candidates)
- TMDB_KEY env-var status confirmed
- All Phase 18 closures: REQ-18-01..10 — all CLOSED end-to-end
- STATE.md sw_cache_version field updated to couch-v36-availability-notifs (handled by post-execution workflow, not this plan directly)
- New STATE.md entry: 7-day Sentry soak window for [18/providerRefreshTick]
- New STATE.md entry: 30-day usage analytics on titleAvailable opt-out rate (informs whether Deferred Ideas around per-list opt-out, departure notifs, etc. need to be promoted)

Phase 18 is now SHIPPED. Trigger `/gsd-verify-work 18` to formalize the verification record.
</output>
</content>
</invoke>