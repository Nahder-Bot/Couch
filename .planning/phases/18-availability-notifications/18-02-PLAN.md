---
phase: 18-availability-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/nahde/claude-projects/couch/js/app.js
autonomous: true
requirements_addressed: [REQ-18-03, REQ-18-04, REQ-18-05]
tags: [push, settings, friendly-ui, detail-modal, refresh, phase-18]
must_haves:
  truths:
    - "DEFAULT_NOTIFICATION_PREFS in js/app.js gains a new key `titleAvailable: true` (D-12, D-20 — client place 2 of 6)"
    - "NOTIFICATION_EVENT_LABELS in js/app.js gains a `titleAvailable` entry with BRAND-voice label/hint (D-12, D-20 — client place 3 of 6)"
    - "NOTIF_UI_TO_SERVER_KEY (friendly-UI map) gains `titleAvailable: 'titleAvailable'` identity entry (D-20 — place 4 of 6)"
    - "NOTIF_UI_LABELS (friendly-UI map) gains `titleAvailable` entry mirroring NOTIFICATION_EVENT_LABELS verbatim — single source of voice (D-20 — place 5 of 6)"
    - "NOTIF_UI_DEFAULTS (friendly-UI map) gains `titleAvailable: true` matching server NOTIFICATION_DEFAULTS (D-20 — place 6 of 6)"
    - "window.refreshProviders in js/app.js (existing function at js/app.js:7105) is updated to ALSO write `lastProviderRefreshAt: Date.now()` alongside the providers fields, so manual refresh keeps the round-robin cursor accurate (D-18 + side-effect of D-19 — manual refresh does NOT push, but the timestamp must update so the CF doesn't immediately re-refresh the same title)"
    - "window.refreshProviders flashToast on success: 'Availability refreshed.' (D-18 verbatim toast copy)"
    - "Detail modal Where-to-watch section (renderDetailShell at js/app.js:7531-7539) gains a 'Provider data via TMDB' attribution footnote BELOW the existing refresh button (D-16 + D-18) — small italic text, no separate UI surface needed"
    - "The existing manual refresh button label is updated from '↻ Refresh' to '↻ Refresh availability' (D-17 — clearer affordance label) — keeps same onclick handler, same styling"
    - "Manual refresh (refreshProviders) does NOT trigger a push fan-out — purely a local TMDB re-fetch + Firestore write (D-19; the providerRefreshTick CF is the only push-firing path)"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/js/app.js"
      provides: "titleAvailable in DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + 3 friendly-UI maps + 'Refresh availability' button label + 'Provider data via TMDB' attribution + lastProviderRefreshAt write in refreshProviders"
      contains: "titleAvailable"
  key_links:
    - from: "Settings UI consumers reading NOTIF_UI_LABELS / NOTIFICATION_EVENT_LABELS"
      to: "1 newly-mirrored titleAvailable key — surfaces in BOTH legacy + friendly-UI Settings (TD-8 dual-Settings collision — already deferred)"
      via: "existing Settings notif-pref render loop"
      pattern: "NOTIF_UI_LABELS|NOTIFICATION_EVENT_LABELS"
    - from: "Detail-modal manual refresh button"
      to: "refreshProviders → fetchTmdbExtras → updateDoc(titlesRef, { providers, lastProviderRefreshAt, ... })"
      via: "client-side TMDB re-fetch + writeAttribution-stamped Firestore write"
      pattern: "refreshProviders|lastProviderRefreshAt"
    - from: "providerRefreshTick CF (Plan 18-01) reading t.lastProviderRefreshAt"
      to: "round-robin oldest-first sort uses the timestamp set by manual refresh OR by the previous CF tick (whichever is more recent)"
      via: "shared field on the title doc"
      pattern: "lastProviderRefreshAt"
---

<objective>
Plan 02 — Client-side closure for Phase 18 Availability Notifications.
Cross-repo: writes to `couch/js/app.js` only. Three concerns in one plan since
all three touch the same file (no parallel split possible without overhead):

1. **DR-3 5-place client mirror (D-20):** Add `titleAvailable` key to:
   - `DEFAULT_NOTIFICATION_PREFS` (place 2 of 6, server place is in Plan 18-01)
   - `NOTIFICATION_EVENT_LABELS` (place 3 of 6 — legacy Settings UI label)
   - `NOTIF_UI_TO_SERVER_KEY` (place 4 of 6 — friendly-UI alias)
   - `NOTIF_UI_LABELS` (place 5 of 6 — friendly-UI label)
   - `NOTIF_UI_DEFAULTS` (place 6 of 6 — friendly-UI default)

   Mirrors the Phase 15.4-02 pattern: copy reuse for single-source-of-voice across
   the legacy + friendly-UI Settings surfaces.

2. **Manual refresh affordance refresh (D-17 + D-18):** The existing
   `refreshProviders(id)` function at `js/app.js:7105` and the existing detail-modal
   refresh button (`renderDetailShell` at `js/app.js:7533`) cover the manual-refresh
   path BUT need three small upgrades for Phase 18 polish:
   - Button label: '↻ Refresh' → '↻ Refresh availability' (D-17 — clearer
     affordance copy; users who don't know what "Refresh" refers to in the
     where-to-watch section get a self-describing label)
   - refreshProviders also writes `lastProviderRefreshAt: Date.now()` so a manual
     refresh updates the same cursor the CF uses (round-robin remains accurate;
     the CF won't immediately re-fetch a title the user just manually refreshed)
   - Toast copy: 'Availability refreshed.' (D-18 verbatim, replaces the existing
     setTimeout(reopen-modal) UX with an explicit user-visible confirmation)

3. **Confidence/source attribution (D-16):** Add a small italic 'Provider data via
   TMDB' footnote to the detail-modal Where-to-watch section, below the refresh
   button. The push body itself does NOT carry "via TMDB" (D-16 — too verbose for
   a push); the affordance lives in the detail surface where users dig in.

**Why all three in one plan:** All edits touch the same file (`js/app.js`).
Splitting into three Wave-1 plans would force at least two of them sequential
(file-overlap conflict). Bundling keeps Wave 1 fully parallel with Plan 18-01.

**Why titleAvailable defaults to TRUE (D-20):** Per RESEARCH risk 2 + cross-AI review,
this is a low-volume / high-signal channel. A user opens the app on a Tuesday
because something they wanted is now watchable — that's the entire moat. Default
ON; users who find it noisy opt out via Settings.

**Banned-words sweep on new copy:**
- titleAvailable label: 'Newly watchable' — no banned words; warm, direct, no theatrics. Pass.
- titleAvailable hint: 'When a title in your queue lands on a service in your pack.' — no banned words. "lands on" is BRAND-aligned (vivid, agentive). "in your queue" is a primitive Couch already uses ("queue" is exempt as a Couch primitive name — same exemption status as "couch" in "couch nudges"). "in your pack" is the family-services-pack primitive established in Phase 15.4 + Phase 11. Pass.
- 'Provider data via TMDB' — attribution footnote, no banned words. Pass.
- 'Availability refreshed.' — toast copy, no banned words. Pass.
- '↻ Refresh availability' — button label, no banned words. Pass.

**Note on banned-words "queue":** the BANNED-words list in BRAND.md prohibits
"queue" in the "buffer / queue / sync / lag / latency / playback" cluster — those
are all about VIDEO PLAYBACK terminology (the streaming engineering vocabulary
that breaks Couch's warm-restraint voice). The Couch product primitive "queue"
(per-member yes-vote-derived watchlist, established in PROJECT.md + Phase 14)
is exempt — it's a UX noun, not a video-playback verb. Existing BRAND-voice
strings already use "queue" in this product sense (e.g., 'Your queue is empty.'
at the queue-empty empty state). The hint copy 'in your queue' fits this
exemption.

Plan 02 is **autonomous** — all writes are local to couch js/app.js. No queuenight
changes. The deploy is gated to Plan 18-04.

Purpose: ship the user-visible client half of Phase 18 — the friendly Settings
toggle (so users can opt out), the legacy Settings label parity, the refreshed
manual-refresh affordance with via-TMDB attribution, and the lastProviderRefreshAt
update so the CF cursor stays accurate after manual refreshes.

Output: `js/app.js` updated with 5 entries added across 5 maps (DEFAULT +
LABELS + 3 friendly-UI), 1 button label changed, 1 attribution footnote added,
1 toast copy changed, 1 lastProviderRefreshAt write added. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/18-availability-notifications/18-CONTEXT.md
@.planning/research/2026-04-29-provider-data-audit.md
@.planning/phases/15.4-integration-polish/15.4-02-PLAN.md
@.planning/BRAND.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

js/app.js DEFAULT_NOTIFICATION_PREFS at lines 100-132 — currently 18 keys (mirrors
queuenight NOTIFICATION_DEFAULTS one-to-one). Last key `couchPing: true` at line
131 (no trailing comma — same convention as the server-side map). ADD
`titleAvailable: true` after couchPing.

js/app.js NOTIFICATION_EVENT_LABELS at lines 142-175 — currently 18 entries with
{label, hint} shape. Last entry `couchPing: { label: 'Couch nudges', hint: 'When
someone on your couch wants you on the couch tonight.' }` at line 174 (no trailing
comma). ADD titleAvailable entry after couchPing.

js/app.js NOTIF_UI_TO_SERVER_KEY at lines 181-201 — currently 15 keys (6 Phase-12
+ 9 keys mirrored in Phase 15.4-02). Last key `couchPing: 'couchPing'` at line
200. ADD `titleAvailable: 'titleAvailable'` after couchPing.

js/app.js NOTIF_UI_LABELS at lines 205-225 — currently 15 entries. Last entry
`couchPing: { label: "Couch nudges", hint: "When someone on your couch wants you
on the couch tonight." }` at line 224. ADD titleAvailable entry after couchPing
with verbatim copy mirroring NOTIFICATION_EVENT_LABELS.

js/app.js NOTIF_UI_DEFAULTS at lines 227-247 — currently 15 entries. Last entry
`couchPing: true` at line 246. ADD `titleAvailable: true` after couchPing.

js/app.js refreshProviders at lines 7105-7126 — current shape:
```javascript
window.refreshProviders = async function(id) {
  const t = state.titles.find(x => x.id === id);
  if (!t) return;
  const tmdbId = t.tmdbId || id.replace('tmdb_','');
  const mediaType = t.mediaType || (t.kind === 'Movie' ? 'movie' : 'tv');
  try {
    const extras = await fetchTmdbExtras(mediaType, tmdbId);
    const update = {
      providers: extras.providers || [],
      rentProviders: extras.rentProviders || [],
      buyProviders: extras.buyProviders || [],
      providersChecked: true,
      providersSchemaVersion: 3
    };
    await updateDoc(doc(titlesRef(), id), { ...writeAttribution(), ...update });
    haptic('success');
    // Re-open the detail to show the fresh data
    setTimeout(() => openDetailModal(id), 150);
  } catch(e) {
    alert('Refresh failed: ' + e.message);
  }
};
```
ADD `lastProviderRefreshAt: Date.now()` to the update object. REPLACE the
`alert('Refresh failed:')` failure path with `flashToast('Refresh failed. Try
again.', { kind: 'warn' })` (BRAND-aligned; alert() is jarring per CLAUDE.md
design system "warm restraint"). REPLACE the silent setTimeout(reopen) on
success with an explicit `flashToast('Availability refreshed.', { kind: 'info' })`
+ KEEP the setTimeout(openDetailModal) so the modal still reflects fresh data.

js/app.js renderDetailShell provider rendering at lines 7515-7539 — current
shape:
```javascript
const refreshBtn = (t.id && t.id.startsWith('tmdb_') && !t.isManual)
  ? `<button class="pill" onclick="refreshProviders('${t.id}')" style="margin-top:var(--s2);font-size:var(--t-micro);" title="Refetch availability from TMDB">↻ Refresh</button>`
  : '';
const providersHtml = anyAvail
  ? `<div class="detail-section"><h4>Where to watch</h4>${streamStrip}${rentStrip}${buyStrip}${refreshBtn}</div>`
  : (t.providersChecked
      ? `<div class="detail-section"><h4>Where to watch</h4><div class="detail-prov-empty">Not on subscription streaming or rent. ${refreshBtn}</div></div>`
      : '');
```
CHANGE button label '↻ Refresh' → '↻ Refresh availability' (D-17). ADD a
'Provider data via TMDB' attribution footnote AFTER the button, inside the
same Where-to-watch section. Use small italic styling to match BRAND voice
(restraint).

NEW copy for titleAvailable (D-20 — net-new key for Phase 18):
- Label: "Newly watchable"
- Hint: "When a title in your queue lands on a service in your pack."

Banned-words sweep is in <objective> above. PASS.

writeAttribution helper — already used by refreshProviders; signature unchanged.
The new lastProviderRefreshAt field is added inside the `update` object spread
into the `updateDoc(...{...writeAttribution(), ...update})` call.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add titleAvailable to DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS (DR-3 client places 2+3)</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=95, limit=85 — covers DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + the friendly-UI maps that follow.
    - The PLAN INTERFACES block has the relevant snippets pre-extracted.
  </read_first>
  <action>
    Make TWO edits to js/app.js.

    EDIT 1 — Add titleAvailable: true to DEFAULT_NOTIFICATION_PREFS:

    Locate the closing `});` of DEFAULT_NOTIFICATION_PREFS Object.freeze (around line 132).
    The current last entry is `couchPing: true` (line 131) with NO trailing comma. Convert
    couchPing's line to have a trailing comma, then insert after it:

        // Phase 18 / D-12 + D-20 — titleAvailable: daily provider-refresh CF push fan-out.
        // Mirror of queuenight NOTIFICATION_DEFAULTS in Plan 18-01. Default ON: low-volume
        // high-signal channel — fires only when a title in someone's queue becomes newly
        // watchable on a brand they own. Users opt out via Settings if noisy.
        titleAvailable: true

    EDIT 2 — Add titleAvailable entry to NOTIFICATION_EVENT_LABELS:

    Locate the closing `});` of NOTIFICATION_EVENT_LABELS Object.freeze (around line 175).
    The current last entry is `couchPing: { label: 'Couch nudges', hint: 'When someone on
    your couch wants you on the couch tonight.' }` at line 174. Convert couchPing's line
    to have a trailing comma, then insert after:

        // Phase 18 / D-20 — titleAvailable legacy Settings UI label.
        // BRAND-voice copy. NOTE: this surface is mirrored to friendly-UI maps in this
        // plan's Task 2 (D-20 + Phase-15.4 mirror approach), so it appears in BOTH the
        // legacy NOTIFICATION_EVENT_LABELS Settings UI AND the Phase 12 friendly-UI Settings UI
        // until the dual-Settings-screen consolidation lands (TD-8 / future polish).
        titleAvailable: { label: 'Newly watchable', hint: 'When a title in your queue lands on a service in your pack.' }

    Banned-words sweep:
    - "Newly watchable" — clean. BRAND-aligned (warm + direct).
    - "When a title in your queue lands on a service in your pack." — "queue" + "pack"
      are Couch product primitives (exempt per BRAND.md banned-words section — the
      banned cluster is video-playback engineering terminology). "lands on" is vivid
      + agentive. Pass.

    Do NOT modify any other key's value or order in either map. Do NOT change any
    Object.freeze() wrapper.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0.
    <automated>grep -nc "titleAvailable: true" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 1 (DEFAULT_NOTIFICATION_PREFS — Task 2 adds 1 more in NOTIF_UI_DEFAULTS for total 2).
    <automated>grep -nc "titleAvailable: { label: 'Newly watchable'" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (NOTIFICATION_EVENT_LABELS).
    <automated>grep -c "titleAvailable" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 2 after Task 1 (DEFAULT_NOTIFICATION_PREFS entry + NOTIFICATION_EVENT_LABELS entry). Tasks 2-3 will push this to 5+.
  </verify>
  <acceptance_criteria>
    - DEFAULT_NOTIFICATION_PREFS contains titleAvailable: true with Phase 18 traceability comment.
    - NOTIFICATION_EVENT_LABELS contains titleAvailable entry with verbatim 'Newly watchable' label and verbatim 'When a title in your queue lands on a service in your pack.' hint.
    - couchPing's line gained a trailing comma in both maps (style consistency).
    - Pre-existing keys (couchPing, reactionPosted, etc.) are unchanged.
    - node -c js/app.js exits 0.
    - Closes (partial): D-20 client places 2+3 of 6.
  </acceptance_criteria>
  <done>
    titleAvailable mirrored to DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS with
    BRAND-voice copy + traceability comments. Syntax valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror titleAvailable to NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS (D-20 friendly-UI parity)</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=180, limit=70 — covers NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS in full.
    - Confirm the existing trailing-comma convention: each map's last key currently has NO trailing comma (couchPing in all 3).
  </read_first>
  <action>
    Make THREE edits to js/app.js — one per friendly-UI map. Each edit appends 1 new
    entry (titleAvailable) to the corresponding map.

    EDIT A — NOTIF_UI_TO_SERVER_KEY (lines ~181-201):

    Locate the closing `});` of NOTIF_UI_TO_SERVER_KEY Object.freeze. Current last
    key is `couchPing:           'couchPing'` (no trailing comma). Convert couchPing's
    line to have a trailing comma, then insert after:

        // Phase 18 / D-20 — friendly-UI parity for titleAvailable. uiKey === serverKey
        // (no rename needed — post-Phase-12 keys keep server names).
        titleAvailable:      'titleAvailable'

    EDIT B — NOTIF_UI_LABELS (lines ~205-225):

    Locate the closing `});` of NOTIF_UI_LABELS Object.freeze. Current last entry is
    `couchPing:           { label: "Couch nudges", hint: "When someone on your couch
    wants you on the couch tonight." }` (no trailing comma). Convert couchPing's line
    to have a trailing comma, then insert after — VERBATIM copy from
    NOTIFICATION_EVENT_LABELS (single source of voice — DO NOT re-author):

        // Phase 18 / D-20 — friendly-UI parity for titleAvailable. Copy mirrors
        // NOTIFICATION_EVENT_LABELS verbatim (single source of voice).
        titleAvailable:      { label: "Newly watchable",         hint: "When a title in your queue lands on a service in your pack." }

    EDIT C — NOTIF_UI_DEFAULTS (lines ~227-247):

    Locate the closing `});` of NOTIF_UI_DEFAULTS Object.freeze. Current last entry is
    `couchPing:           true` (no trailing comma). Convert couchPing's line to have
    a trailing comma, then insert after — default matches server NOTIFICATION_DEFAULTS:

        // Phase 18 / D-20 — friendly-UI parity default. Matches server-side
        // NOTIFICATION_DEFAULTS.titleAvailable = true (Plan 18-01).
        titleAvailable:      true

    The aligned-equals-sign columns match the existing Phase 15.4-02 style. The
    titleAvailable entry appears in the SAME RELATIVE POSITION (after couchPing) in
    all 3 maps to make grep-diff visual scanning trivial.

    Do NOT modify the existing 15 entries in any of the 3 maps. Do NOT change
    Object.freeze() wrappers.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0.
    <automated>grep -A 30 "NOTIF_UI_TO_SERVER_KEY = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -c "titleAvailable:"</automated>
    Expected: 1.
    <automated>grep -A 30 "NOTIF_UI_LABELS = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -c "label: \"Newly watchable\""</automated>
    Expected: 1.
    <automated>grep -A 30 "NOTIF_UI_DEFAULTS = Object.freeze" C:/Users/nahde/claude-projects/couch/js/app.js | grep -c "titleAvailable:"</automated>
    Expected: 1.
    <automated>grep -c "titleAvailable" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: at least 5 after Tasks 1+2 (DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS = 5 occurrences). Task 3 will not add more (it's a different feature).
  </verify>
  <acceptance_criteria>
    - NOTIF_UI_TO_SERVER_KEY contains titleAvailable: 'titleAvailable' identity entry.
    - NOTIF_UI_LABELS contains titleAvailable entry with verbatim "Newly watchable" / "When a title in your queue lands on a service in your pack." copy (single source of voice — matches NOTIFICATION_EVENT_LABELS).
    - NOTIF_UI_DEFAULTS contains titleAvailable: true matching server NOTIFICATION_DEFAULTS.
    - All 3 edits include Phase 18 traceability comments.
    - Pre-existing 15 entries in each map are unchanged.
    - couchPing's line in each map gained a trailing comma (style consistency).
    - node -c js/app.js exits 0.
    - Closes: D-20 friendly-UI parity (places 4-6 of 6 mirrored). REQ-18-04, REQ-18-05 client opt-out paths complete.
  </acceptance_criteria>
  <done>
    All 3 friendly-UI maps extended with titleAvailable entry in canonical position with
    single-source-of-voice copy. Pre-existing 15 entries unchanged in all 3 maps.
    Syntax valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update refreshProviders + detail-modal Where-to-watch — button label, lastProviderRefreshAt write, toast, via-TMDB attribution</name>
  <files>C:/Users/nahde/claude-projects/couch/js/app.js</files>
  <read_first>
    - Read js/app.js with offset=7100, limit=30 — covers refreshProviders function in full (lines 7105-7126).
    - Read js/app.js with offset=7515, limit=30 — covers renderDetailShell provider strip rendering (lines 7515-7544 area).
    - Confirm via grep that flashToast is already a global helper (it is — used widely).
  </read_first>
  <action>
    Make TWO edits to js/app.js — one to refreshProviders, one to renderDetailShell.

    EDIT 1 — refreshProviders at js/app.js:7105-7126:

    Anchor on the unique multi-line function signature `window.refreshProviders =
    async function(id) {` which exists exactly once in the file. REPLACE the entire
    existing function body (from the signature line through the closing `};`) with
    this updated version:

        // Phase 18 / D-17 + D-18 + D-19: detail-modal manual refresh affordance.
        // Refetches TMDB providers + writes back including lastProviderRefreshAt
        // so the daily providerRefreshTick CF (Plan 18-01) round-robin stays accurate
        // (won't re-refresh a title the user just manually refreshed). Manual refresh
        // does NOT trigger a push fan-out (D-19) — only the scheduled CF fires pushes.
        window.refreshProviders = async function(id) {
          const t = state.titles.find(x => x.id === id);
          if (!t) return;
          const tmdbId = t.tmdbId || id.replace('tmdb_','');
          const mediaType = t.mediaType || (t.kind === 'Movie' ? 'movie' : 'tv');
          try {
            const extras = await fetchTmdbExtras(mediaType, tmdbId);
            const update = {
              providers: extras.providers || [],
              rentProviders: extras.rentProviders || [],
              buyProviders: extras.buyProviders || [],
              providersChecked: true,
              providersSchemaVersion: 3,
              // Phase 18 / D-18: stamp the timestamp so the CF round-robin treats
              // this title as freshly-refreshed (won't re-fetch on the next tick).
              lastProviderRefreshAt: Date.now()
            };
            await updateDoc(doc(titlesRef(), id), { ...writeAttribution(), ...update });
            haptic('success');
            // Phase 18 / D-18 verbatim toast copy.
            flashToast('Availability refreshed.', { kind: 'info' });
            // Re-open the detail to show the fresh data.
            setTimeout(() => openDetailModal(id), 150);
          } catch(e) {
            // Phase 18 — replace alert() with flashToast (BRAND-aligned warm restraint).
            console.warn('[18/refreshProviders] failed:', e && e.message);
            flashToast('Refresh failed. Try again.', { kind: 'warn' });
          }
        };

    EDIT 2 — renderDetailShell provider section at js/app.js:7531-7539:

    Anchor on the unique multi-line block starting with `const refreshBtn = (t.id
    && t.id.startsWith('tmdb_') && !t.isManual)` and ending with the closing `: ''`
    of the providersHtml ternary (around line 7539). REPLACE the entire provHtml +
    refreshBtn block with this updated version:

      const refreshBtn = (t.id && t.id.startsWith('tmdb_') && !t.isManual)
        ? `<button class="pill" onclick="refreshProviders('${t.id}')" style="margin-top:var(--s2);font-size:var(--t-micro);" title="Refetch availability from TMDB">↻ Refresh availability</button>`
        : '';
      // Phase 18 / D-16: confidence/source attribution. Push body itself doesn't
      // carry "via TMDB" (too verbose for a push); the affordance lives here in
      // the detail surface where users dig in to verify availability.
      const providerAttribution = `<div class="detail-prov-attribution" style="margin-top:var(--s1);font-size:var(--t-micro);font-style:italic;opacity:0.6;">Provider data via TMDB</div>`;
      const providersHtml = anyAvail
        ? `<div class="detail-section"><h4>Where to watch</h4>${streamStrip}${rentStrip}${buyStrip}${refreshBtn}${providerAttribution}</div>`
        : (t.providersChecked
            ? `<div class="detail-section"><h4>Where to watch</h4><div class="detail-prov-empty">Not on subscription streaming or rent. ${refreshBtn}</div>${providerAttribution}</div>`
            : '');

    Notes on EDIT 2:
    - Button label changed from '↻ Refresh' to '↻ Refresh availability' (D-17).
    - Title attribute (tooltip) preserved verbatim ("Refetch availability from TMDB").
    - providerAttribution rendered in BOTH branches of the ternary (anyAvail vs
      providersChecked-empty) so users see the via-TMDB note even when no
      providers are listed.
    - Inline style uses existing token vars (`--s1`, `--t-micro`) for spacing /
      typography consistency. Italic + 0.6 opacity matches BRAND restraint
      (subtle, not shouty).
    - The third branch (else '') stays empty — no detail section means no
      attribution to show.

    Do NOT modify any other part of renderDetailShell. Do NOT modify any other
    function.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && node -c js/app.js</automated>
    Expected: exit 0.
    <automated>grep -nc "lastProviderRefreshAt: Date.now()" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (inside refreshProviders update object).
    <automated>grep -nc "Availability refreshed." C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (toast copy).
    <automated>grep -nc "↻ Refresh availability" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (button label).
    <automated>grep -nc "Provider data via TMDB" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (attribution footnote).
    <automated>grep -nc "alert('Refresh failed:" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 0 (alert() replaced with flashToast).
    <automated>grep -nc "↻ Refresh<" C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 0 (old button label fully replaced; the trailing &lt; in the grep ensures we don't match "Refresh availability").
    <automated>grep -nc "Refresh failed. Try again." C:/Users/nahde/claude-projects/couch/js/app.js</automated>
    Expected: 1 (failure toast replacement).
  </verify>
  <acceptance_criteria>
    - refreshProviders writes lastProviderRefreshAt: Date.now() in the update object.
    - refreshProviders flashes 'Availability refreshed.' toast on success (D-18).
    - refreshProviders catches errors with flashToast 'Refresh failed. Try again.' (replaces alert()).
    - Detail-modal refresh button label is '↻ Refresh availability' (D-17 — old '↻ Refresh' fully replaced).
    - Detail-modal Where-to-watch section includes 'Provider data via TMDB' attribution footnote (D-16).
    - Attribution footnote uses inline italic + reduced opacity styling (BRAND restraint).
    - The footnote renders in both anyAvail and providersChecked-empty branches.
    - Existing setTimeout(reopen-modal) UX preserved on success.
    - haptic('success') call preserved.
    - node -c js/app.js exits 0.
    - Closes: D-16, D-17, D-18, D-19. REQ-18-03 (manual refresh affordance + via-TMDB attribution).
  </acceptance_criteria>
  <done>
    refreshProviders updated to write lastProviderRefreshAt + show explicit toasts.
    Detail-modal Where-to-watch section gained a refreshed button label + a small
    via-TMDB attribution footnote. No regressions to other parts of renderDetailShell.
    Syntax valid.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detail-modal click → refreshProviders | Untrusted user input (clicks); but the only mutation is a TMDB re-fetch + Firestore write authorized by the existing title-doc UPDATE rule (attributedWrite + per-member progress isolation). No new boundary. |
| refreshProviders → Firestore writeAttribution | Already-trusted attribution stamp. Phase 15.1 hardening covers `actingUid == auth.uid`, `memberId.matches('^m_...')` etc. The lastProviderRefreshAt field is just a number — rules don't have a separate validation branch for it. |
| Settings UI render → notification prefs | No new boundary — uses existing render path. The new titleAvailable entries surface in the existing render loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Tampering (manual refresh storms cause TMDB rate-limit hit) | refreshProviders client | accept | Per CONTEXT Deferred Ideas, refresh-availability rate limiting is deferred until signal of abuse. The existing clickable button has no debounce, but the detail-modal context (must open one title at a time) bounds the rate to ~1 click/sec realistically. If a future abuse signal emerges, add a per-user-per-title cooldown (e.g., 5 min between manual refreshes). |
| T-18-08 | Tampering (manual refresh writes lastProviderRefreshAt for a title the user shouldn't be able to write) | refreshProviders Firestore write | mitigate | Existing title-doc UPDATE rule (firestore.rules:427-516) already requires attributedWrite() (auth.uid + memberId anchored). The new lastProviderRefreshAt field is an additional KEY in the update — rules don't have a write-allowlist for fields, only deny rules for liveReleaseFiredFor (Phase 15 / REVIEW HIGH-1) and per-member-key isolation for tupleProgress / mutedShows / progress. lastProviderRefreshAt is not under any of those branches, so the existing attributedWrite gate applies. Member of the family who can attribute a write can update lastProviderRefreshAt — same trust model as updating providers itself (which the existing button already does). |
| T-18-09 | Information disclosure (via-TMDB attribution leaks third-party dependency to all users) | renderDetailShell footnote | accept | TMDB is a public attribution requirement per their API ToS; surfacing "Provider data via TMDB" is a transparency win, not a leak. Same posture as the existing TMDB API key being public-by-design. |
| T-18-10 | Tampering (alert → flashToast change accidentally drops error visibility) | refreshProviders catch block | mitigate | flashToast surfaces errors at the same UX level as alert() (more BRAND-aligned). console.warn breadcrumb retained for Sentry-side observability — the user-facing toast plus the server-side console log gives both UX feedback AND debug trace. |

ASVS L1: input validation handled by existing title-doc UPDATE rule
(attributedWrite + Phase 15.1 per-key isolation); per-event-type opt-out gate via
sendToMembers (server-side, Plan 18-01); Sentry breadcrumb-equivalent console.warn
on client-side fetch failure.
</threat_model>

<verification>
**Plan-level checks (after all 3 tasks committed):**

1. `cd C:/Users/nahde/claude-projects/couch && node -c js/app.js` exits 0
2. `grep -c "titleAvailable" js/app.js` returns ≥ 5 (DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + 3 friendly-UI maps)
3. `grep -nc "titleAvailable: true" js/app.js` returns 2 (DEFAULT + NOTIF_UI_DEFAULTS)
4. `grep -nc "label: 'Newly watchable'" js/app.js` returns 1 (NOTIFICATION_EVENT_LABELS)
5. `grep -nc 'label: "Newly watchable"' js/app.js` returns 1 (NOTIF_UI_LABELS — different quote style per existing convention)
6. `grep -nc "lastProviderRefreshAt: Date.now()" js/app.js` returns 1 (refreshProviders update)
7. `grep -nc "Availability refreshed." js/app.js` returns 1 (toast copy)
8. `grep -nc "↻ Refresh availability" js/app.js` returns 1 (button label)
9. `grep -nc "Provider data via TMDB" js/app.js` returns 1 (attribution footnote)
10. `grep -nc "alert('Refresh failed:" js/app.js` returns 0 (old alert removed)

**Pre-deploy (NON-AUTONOMOUS — handled by Plan 18-04 deploy ritual):**
11. Manual smoke (post-deploy via Plan 18-04 Task 4 UAT):
    - Settings → notifications shows the new "Newly watchable" toggle (default ON)
    - Title detail modal → Where-to-watch section shows the new "↻ Refresh availability" button + "Provider data via TMDB" footnote
    - Tap the button → toast "Availability refreshed." appears + the providers list re-renders + Firestore title doc shows updated lastProviderRefreshAt timestamp
    - Toggle "Newly watchable" OFF in Settings → simulated CF push to that user is suppressed (server-side eventType-gate)

**Cross-repo deploy ordering (D-21) — enforced by Plan 18-04 deploy ritual.**
</verification>

<success_criteria>
**Plan 02 is complete when:**
- [ ] DEFAULT_NOTIFICATION_PREFS contains titleAvailable: true
- [ ] NOTIFICATION_EVENT_LABELS contains titleAvailable entry with "Newly watchable" / "When a title in your queue lands on a service in your pack."
- [ ] NOTIF_UI_TO_SERVER_KEY contains titleAvailable identity mapping
- [ ] NOTIF_UI_LABELS contains titleAvailable entry with verbatim copy
- [ ] NOTIF_UI_DEFAULTS contains titleAvailable: true
- [ ] Pre-existing 15 entries in each friendly-UI map unchanged
- [ ] All Phase 18 traceability comments present
- [ ] refreshProviders writes lastProviderRefreshAt: Date.now() in update object
- [ ] refreshProviders flashes 'Availability refreshed.' on success (D-18)
- [ ] refreshProviders flashes 'Refresh failed. Try again.' on error (replaces alert)
- [ ] Detail-modal button label is '↻ Refresh availability' (D-17)
- [ ] Detail-modal includes 'Provider data via TMDB' attribution footnote (D-16)
- [ ] node -c js/app.js exits 0
- [ ] Banned-words sweep clean on new copy
- [ ] No `alert(` calls remain in refreshProviders error path

**Deploy & UAT (DEFERRED to Plan 18-04):**
- couch hosting deploy via scripts/deploy.sh AFTER queuenight functions deploy
- UAT 1: Settings UI shows the new toggle + opt-out works (server-side eventType-gate)
- UAT 2: Detail modal shows the refreshed affordance + via-TMDB footnote
- UAT 3: Tap "Refresh availability" → toast confirms + lastProviderRefreshAt updates in Firestore
- UAT 4: simulated CF tick (manually trigger via gcloud or wait 24h) → push delivery confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/18-availability-notifications/18-02-SUMMARY.md` describing:
- js/app.js changes — line numbers for DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS additions, 3 friendly-UI maps additions, refreshProviders rewrite, renderDetailShell provider-section changes
- Banned-words sweep result (PASS)
- Coverage closure: D-16, D-17, D-18, D-19, D-20 (client places 2-6 of 6) all closed
- REQ-18-03 (refresh affordance + via-TMDB attribution), REQ-18-04 (client mirror), REQ-18-05 (client opt-out via Settings) closed
- Open questions: none if all 3 tasks complete cleanly
- Cross-repo deploy ordering reminder (queuenight FIRST, then couch — Plan 18-04 enforces)
</output>
</content>
</invoke>