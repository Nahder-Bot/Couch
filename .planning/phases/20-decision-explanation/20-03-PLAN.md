---
phase: 20-decision-explanation
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - C:/Users/nahde/claude-projects/couch/sw.js
autonomous: false
requirements: [REQ-20-10, REQ-20-11]
must_haves:
  truths:
    - "sw.js CACHE constant bumped from 'couch-v36.1-kid-mode' to 'couch-v36.2-decision-explanation' (per D-17)"
    - "Deploy ritual is single-repo couch-only (NO queuenight changes; NO firestore.rules; NO Cloud Function deploy) per D-16"
    - "scripts/deploy.sh 36.2-decision-explanation runs the full 5-smoke gate (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation) before firebase deploy --only hosting; deploy aborts on any smoke failure"
    - "Live couchtonight.app serves CACHE 'couch-v36.2-decision-explanation' (verified via curl post-deploy)"
    - "Multi-condition device UAT on a real iPhone (or other handset) confirms all 3 surfaces render correctly: D-07 spin-pick italic serif sub-line / D-08 card dim footer with considerable variant / D-09 detail-modal section visible only for matches"
    - "BRAND voice check on the visible strings — no exclamation marks, no marketing language, no banned words ('buffer', 'delay', 'queue' in queue-UX context) per D-11 + D-12"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/sw.js"
      provides: "Bumped CACHE constant for PWA invalidation"
      contains: "couch-v36.2-decision-explanation"
  key_links:
    - from: "scripts/deploy.sh 36.2-decision-explanation"
      to: "sw.js CACHE auto-bump"
      via: "sed-in-place pattern at lines 116-125 of deploy.sh"
      pattern: "couch-v36.2-decision-explanation"
    - from: "scripts/deploy.sh 36.2-decision-explanation"
      to: "couchtonight.app live"
      via: "firebase deploy --only hosting (couch-only — D-16)"
      pattern: "firebase deploy --only hosting"
    - from: "live deploy"
      to: "device UAT"
      via: "iPhone Safari + iOS PWA cache-bust if needed"
      pattern: "couch-v36.2-decision-explanation"
---

<objective>
Plan 03 — Wave 3. Bump sw.js cache, deploy to production via the single-repo couch-only ritual, and run a real-device UAT covering all 3 surfaces.

Three tasks: (1) cache bump in sw.js, (2) deploy via `bash scripts/deploy.sh 36.2-decision-explanation`, (3) device UAT walkthrough as a `checkpoint:human-verify` task.

This plan is **non-autonomous** because UAT requires a real iPhone (or other physical handset) connecting to live couchtonight.app to visually verify the italic Instrument Serif rendering of the spin-pick sub-line + the dim-text card footer + the conditional detail-modal section. The italic serif rendering and the dim color tokens cannot be verified in unit smoke; they require browser + display.

Single-repo deploy ritual (D-16):
- NO queuenight changes
- NO firestore.rules update
- NO Cloud Function deploy
- ONLY: sw.js CACHE bump + deploy.sh tag run + curl-verify + device UAT

Purpose: ship Phase 20 to production + confirm end-to-end behavior against the 13 ROADMAP success criteria.

Output: sw.js CACHE bumped, production deployed, device UAT walkthrough captured in 20-03-SUMMARY.md, phase ready for `/gsd-verify-work 20`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-decision-explanation/20-CONTEXT.md
@.planning/phases/20-decision-explanation/20-RESEARCH.md
@.planning/phases/20-decision-explanation/20-01-SUMMARY.md
@.planning/phases/20-decision-explanation/20-02-SUMMARY.md
@CLAUDE.md
@.planning/BRAND.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump sw.js CACHE to couch-v36.2-decision-explanation</name>
  <files>C:/Users/nahde/claude-projects/couch/sw.js</files>
  <read_first>
    - C:/Users/nahde/claude-projects/couch/sw.js (full file — ~110 lines, safe to read in full)
    - C:/Users/nahde/claude-projects/couch/.planning/phases/20-decision-explanation/20-PATTERNS.md (§ "sw.js — CACHE bump")
    - C:/Users/nahde/claude-projects/couch/.planning/phases/20-decision-explanation/20-CONTEXT.md (D-17 — exact target string)
  </read_first>
  <action>
Edit `sw.js` line 8.

**Current line (line 8):**
```javascript
const CACHE = 'couch-v36.1-kid-mode';
```

**New line (replace string value only):**
```javascript
const CACHE = 'couch-v36.2-decision-explanation';
```

**Constraints:**
- DO NOT change anything else in sw.js (the SHELL array, the install/activate/fetch/push/notificationclick handlers must remain literally unchanged).
- The string format MUST be exactly `couch-v36.2-decision-explanation` per D-17. Decimal continuation from v36.1 (Phase 19) — does not bump major (still v36.x for the v1-commercial-release milestone).
- DO NOT use deploy.sh's auto-bump path for this task — bump manually via direct edit so the commit history captures the cache change as an explicit, reviewable artifact (matches Phase 19-03 pattern; deploy.sh's auto-bump is the safety net for forgotten bumps, not the primary path).
- After the edit, run `node --check sw.js` to confirm syntax integrity.

Commit with message `chore(20-03): bump sw.js CACHE to couch-v36.2-decision-explanation`.
  </action>
  <verify>
    <automated>node --check sw.js && grep -c "couch-v36.2-decision-explanation" sw.js</automated>
  </verify>
  <acceptance_criteria>
- `node --check sw.js` exits 0
- New CACHE string present: `grep -c "couch-v36.2-decision-explanation" sw.js` returns 1
- Old CACHE string absent: `grep -c "couch-v36.1-kid-mode" sw.js` returns 0 (no comment lines reference the old cache; the previous cache name is replaced cleanly)
- The CACHE constant declaration line is exactly: `grep -c "^const CACHE = 'couch-v36.2-decision-explanation';$" sw.js` returns 1
- SHELL array unchanged: `grep -c "const SHELL = \\['/app', '/css/app.css', '/js/app.js'\\];" sw.js` returns 1
- File line count is approximately the same as before (~110 lines): `wc -l sw.js` returns a number between 105 and 115
- No accidental introduction of other version strings: `grep -cE "couch-v[0-9]+" sw.js` returns 1 (only the new cache name)
  </acceptance_criteria>
  <done>sw.js CACHE constant updated to couch-v36.2-decision-explanation; node --check passes; old cache string fully replaced; no other modifications.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to production via bash scripts/deploy.sh 36.2-decision-explanation</name>
  <files>(no source edits — runs deploy.sh which mirrors source to deploy mirror and runs firebase deploy --only hosting)</files>
  <read_first>
    - C:/Users/nahde/claude-projects/couch/scripts/deploy.sh (full file — verify §2.5 has the 5th smoke block from Plan 01 Task 3, §4 sw.js auto-bump path, §5 mirror, §7 Sentry guard, §8 firebase deploy)
    - C:/Users/nahde/claude-projects/couch/.planning/STATE.md (sw.js cache version section — confirms current is v36.1-kid-mode and v36.2 is the next decimal step)
    - C:/Users/nahde/claude-projects/couch/CLAUDE.md (§ Do/Don't — `bash scripts/deploy.sh <short-tag>` is the canonical deploy ritual; couchtonight.app is the production target via Firebase Hosting deploy mirror)
  </read_first>
  <action>
Run the production deploy from the couch repo root. Single command, single shell session. The script orchestrates: working-tree-clean check → node --check on shipping JS → 5-smoke gate → couch-deploy mirror exists check → sw.js CACHE auto-bump (no-op since Task 1 already bumped it) → mirror file copies → BUILD_DATE auto-stamp on the mirror's constants.js → Sentry DSN placeholder guard → firebase deploy --only hosting → post-deploy curl smoke.

**Run this exact command** from the couch repo root:

```bash
bash scripts/deploy.sh 36.2-decision-explanation
```

**Expected sequence (script handles all of it; do not run sub-steps manually):**
1. Working tree dirty check passes (Task 1's commit must already be staged + committed before running deploy)
2. `node --check js/*.js sw.js scripts/stamp-build-date.cjs` — all pass (smoke contracts NOT included in this loop, they have their own §2.5 gate)
3. §2.5 5-smoke gate runs: positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation — all green; echo confirms `Smoke contracts pass (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation).`
4. couch-deploy sibling repo path validated (env COUCH_DEPLOY_PATH or default `../../couch-deploy`)
5. §4 sw.js CACHE auto-bump runs `sed` — should print `sw.js CACHE already at couch-v36.2-decision-explanation; skipping bump.` (since Task 1 already bumped it)
6. §5 mirror copies app.html, landing.html, changelog.html, rsvp.html, 404.html, sw.js, sitemap.xml, robots.txt, css/*.css, js/*.js to `${COUCH_DEPLOY_ROOT}/public/`
7. §6 BUILD_DATE auto-stamp targets the mirror copy of constants.js only (source untouched)
8. §7 Sentry DSN placeholder guard runs on app.html + landing.html — passes
9. §8 `firebase deploy --only hosting --project queuenight-84044` from couch-deploy/ — uploads to Firebase Hosting
10. §9 post-deploy curl checks log HTTP 200 + the live `const CACHE = 'couch-v36.2-decision-explanation';` line from prod sw.js

**Constraints:**
- Run ONLY `bash scripts/deploy.sh 36.2-decision-explanation`. Do NOT run firebase deploy directly. Do NOT mirror files manually. Do NOT run `npm run smoke` again — the deploy script's §2.5 already gates on it.
- If the script aborts on dirty tree at §0, commit any pending changes from Task 1 first (the cache bump must be on disk + committed before running deploy).
- If the script aborts on Sentry placeholder check at §7, that's an unrelated regression — surface it to the user, do not patch through it.
- If the script aborts on smoke gate at §2.5, halt the plan; the breaking smoke must be diagnosed before deploy retry.
- Per memory note "Couch deploy autonomy" + project decision: Claude may run `firebase deploy` to queuenight-84044 via deploy.sh without per-deploy approval. This is a sanctioned single-repo couch-only deploy.
- After the script completes successfully, run a final manual curl to lock the verification: `curl -s https://couchtonight.app/sw.js | grep "const CACHE"`. The output must include `couch-v36.2-decision-explanation`.

**Anti-pattern to avoid:** DO NOT run `firebase deploy --only functions` from queuenight (D-16: NO queuenight changes for this phase). DO NOT run `firebase deploy --only firestore:rules` (D-15: no Firestore writes; rules unchanged).

Capture deploy output to surface in 20-03-SUMMARY.md (script logs each phase with timestamps).
  </action>
  <verify>
    <automated>curl -s https://couchtonight.app/sw.js | grep -c "couch-v36.2-decision-explanation"</automated>
  </verify>
  <acceptance_criteria>
- `bash scripts/deploy.sh 36.2-decision-explanation` exits 0
- Deploy log contains `Smoke contracts pass (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation).` (5-smoke gate green)
- Deploy log contains `firebase deploy --only hosting` invocation (do not skip § 8)
- Deploy log contains `Bumped sw.js CACHE -> couch-v36.2-decision-explanation` OR `sw.js CACHE already at couch-v36.2-decision-explanation; skipping bump.` (one of the two — both are pass states)
- Post-deploy curl returns the new cache: `curl -s https://couchtonight.app/sw.js | grep -c "couch-v36.2-decision-explanation"` returns 1
- Post-deploy curl shows HTTP 200 from root: `curl -sI https://couchtonight.app/ | head -1 | grep -c "200"` returns 1
- The old cache name is gone from production: `curl -s https://couchtonight.app/sw.js | grep -c "couch-v36.1-kid-mode"` returns 0
- BUILD_DATE on production is today (UTC): `curl -s https://couchtonight.app/js/constants.js | grep -c "BUILD_DATE = '$(date -u +%Y-%m-%d)'"` returns 1
  </acceptance_criteria>
  <done>scripts/deploy.sh 36.2-decision-explanation completed cleanly; 5-smoke gate green; firebase deploy --only hosting succeeded; post-deploy curl confirms couch-v36.2-decision-explanation live at couchtonight.app; HTTP 200; BUILD_DATE stamped today; old cache name fully gone from prod.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Multi-condition device UAT — visually verify all 3 surfaces on a real iPhone</name>
  <what-built>
    Phase 20 ships an additive humility-voice "decision explanation" string on three surfaces of couchtonight.app:
    1. **Spin-pick result modal (D-07)** — italic Instrument Serif sub-line below the picked title's name (e.g. "Nahder + Zoey said yes · Available on Hulu · 1 hr 38 min").
    2. **Tonight matches list cards (D-08)** — single-line dim-text footer on every match card under the tile content, before the action buttons.
    3. **Tonight considerable list cards (D-10)** — same footer but voter phrase reads "Some of you said yes" for the 1-of-N case.
    4. **Detail modal "Why this is in your matches" section (D-09)** — italic serif h4 + dim p, conditionally rendered ONLY when the title is currently in matches list (omitted when opened from Library, History, or considerable list).

    All 4 surfaces compose from existing data (t.votes, t.providers, t.runtime, m.services). No new state, no Firestore writes, no new modals. Pure read-only display additive text.

    Backend / regression-locks: 15-assertion smoke contract (Plan 01) + 3 surface integrations + considerable variant flag plumbed through renderTonight → card() (Plan 02) + sw.js CACHE bumped to couch-v36.2-decision-explanation (Task 1 above) + production deploy via single-repo couch-only ritual (Task 2 above).
  </what-built>
  <how-to-verify>
    Run on a real iPhone (Safari + iOS PWA). If the PWA was already installed for couch-v36.1-kid-mode, force a cache refresh: open Safari → couchtonight.app → quit Safari fully → reopen. The service worker auto-revalidates on next online activation; the `couch-v36.2-decision-explanation` cache name confirms the activation.

    **Pre-flight check** (~30 sec):
    1. Open https://couchtonight.app/ on iPhone Safari.
    2. View page source (or in Safari Web Inspector if Mac handy) — confirm `couch-v36.2-decision-explanation` somewhere in the loaded sw.js. (Skipping this step is fine if the surface tests below pass; the curl verification in Task 2 already confirmed the live string.)
    3. Sign in to your test family with at least 2 couch members + at least one title voted "yes" by both members.

    **Surface 1 — Spin-pick result modal (D-07)** (~1 min):
    4. From the Tonight tab, tap "Spin" to trigger the spin-pick flow.
    5. When the result modal lands, verify a NEW italic-serif sub-line appears between the title's meta line (year · kind · runtime) and the providers strip.
    6. The text MUST read something like "Nahder + Zoey said yes · Available on Hulu · 1 hr 38 min" (your actual member names, your actual provider, your actual runtime).
    7. Visual check: Italic Instrument Serif font (cursive serif look, NOT Inter sans). Dim color (NOT warm-amber celebration color — that color belongs to the existing ✨ "A couch favorite" line below).
    8. Confirm the existing `.spin-reason` line ("✨ A couch favorite") is STILL present below the new sub-line. Both lines coexist.

    **Surface 2 — Match-card dim footer (D-08)** (~1 min):
    9. Close the spin modal. Scroll the Tonight matches list.
    10. Verify EVERY match card now shows a single-line dim-text footer immediately above the action-button row (the row with the primary action + ▶ Trailer + ⋯ buttons).
    11. The text MUST read e.g. "Nahder said yes · Available on Netflix · 95 min" or similar.
    12. Visual check: Dim color (`--ink-dim` per BRAND.md §2 — looks roughly the same as the meta text). Plain sans-serif font (NOT italic, NOT serif — that style is reserved for D-07 spin and D-09 detail modal).

    **Surface 3 — Considerable variant (D-10)** (~30 sec):
    13. Scroll past the matches list to the "Worth considering" section (titles where at least one couch member yes-voted but the couch isn't unanimous).
    14. For each considerable card with exactly 1 yes-voter on the couch, the footer text MUST read "Some of you said yes" (NOT the member's name). For 2+ yes-voters, it reads "{N1} + {N2} said yes" or "All of you said yes" (matches the matches-card behavior).
    15. If you don't have a considerable title in your test family, you can skip this step — the smoke A15 already locks the behavior, and the deploy is gated on smoke pass.

    **Surface 4 — Detail modal section gate (D-09)** (~1 min):
    16. Tap any match card to open the detail modal.
    17. Scroll inside the detail body. Verify a NEW section appears titled "Why this is in your matches" (italic Instrument Serif h4) followed by a dim-text phrase below it. Section sits between the kid-mode override row (if visible) and the TV progress section.
    18. The h4 text MUST be exactly "Why this is in your matches" — no exclamation, no marketing language.
    19. Visual check: h4 is italic serif (Instrument Serif, NOT all-caps Inter — there's a CSS override for that). Body text is plain dim, NOT italic.
    20. Close the detail modal. Now open the SAME title from the Library tab (or any non-matches surface). Open detail. Verify the "Why this is in your matches" section is ABSENT. Section omitted because t.id is not currently in matches list (that's the D-09 gate).

    **Voice / brand check** (~30 sec):
    21. On any of the rendered explanation strings, confirm: NO exclamation marks. NO marketing language ("amazing", "perfect", "awesome", etc.). NO banned words ("buffer", "delay", "queue" in queue-UX context — queue here refers to a play queue, but the explanation strings should not contain it). Per D-11 + D-12.
    22. Member-name interpolation: if your family has a member with a name containing `<`, `>`, `&`, or `'`, verify the name renders correctly (escaped, not as raw HTML). If no such member exists, this check is informational only.

    **Cross-device check** (optional, ~2 min):
    23. If Android Chrome handy, repeat steps 4-20 on Android — verify rendering is consistent (same text, same color tokens, italic serif still renders via fallback to Fraunces if Instrument Serif unavailable on the platform).

    **Total UAT time: ~5-7 minutes.** Resume signal: "uat passed" (then run `/gsd-verify-work 20`) or list any failures with surface number.
  </how-to-verify>
  <resume-signal>Type "uat passed" to confirm all 4 surfaces render correctly + voice check clean → triggers /gsd-verify-work 20. Or list failures by surface number (e.g. "Surface 2 footer not visible on iOS" / "h4 in surface 4 rendering all-caps Inter — CSS override didn't take").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production deploy → live couchtonight.app | bash scripts/deploy.sh runs firebase deploy --only hosting. The 5-smoke gate at §2.5 is the last automated check before code reaches users. Sentry DSN placeholder guard at §7 prevents a known-bad release. |

## STRIDE Threat Register

Phase 20 adds NO new attack surface — read-only display, no Firestore writes, no new state slots, no new auth surfaces (per RESEARCH § Security Domain line 676). Wave 3 deploy + UAT introduces no NEW threats beyond the existing deploy ritual.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | Deployed bundle integrity (sw.js + js/app.js + css/app.css mirrored to couch-deploy) | mitigate | scripts/deploy.sh §2 runs node --check on every shipping JS file; §2.5 runs 5-smoke gate; §7 Sentry placeholder guard; §9 post-deploy curl smoke. Mirror copies are atomic per file (cp -v in §5). |
| T-20-08 | Denial of Service | Stale PWA cache after deploy | mitigate | sw.js CACHE bump (Task 1) → activate handler at sw.js:22-28 deletes stale caches → clients.claim() fast-tracks new SW. Phase 19's same pattern verified-working. |
| T-20-09 | Spoofing | UAT performed on wrong build / wrong device | accept | Pre-flight check in UAT script confirms `couch-v36.2-decision-explanation` is in loaded sw.js before testing surfaces. Curl in Task 2 already independently verified the live string. |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# Source artifacts
grep -c "couch-v36.2-decision-explanation" sw.js                    # 1
grep -c "couch-v36.1-kid-mode" sw.js                                # 0

# Live deployment
curl -s https://couchtonight.app/sw.js | grep -c "couch-v36.2-decision-explanation"  # 1
curl -sI https://couchtonight.app/ | head -1 | grep -c "200"        # 1

# UAT
# Manually verified — see 20-03-SUMMARY.md for capture
```

If UAT surfaces a failure (Surface N broken), do NOT mark phase complete. Open a 20-04 gap-closure plan or fix in-place + redeploy.
</verification>

<success_criteria>
- sw.js CACHE bumped to couch-v36.2-decision-explanation (Task 1)
- Production deployed via `bash scripts/deploy.sh 36.2-decision-explanation`; 5-smoke gate green; firebase deploy --only hosting succeeded (Task 2)
- Live couchtonight.app serves new cache (curl-verified)
- Device UAT walkthrough completed: all 4 surfaces (D-07 + D-08 + D-09 + D-10 considerable variant) render correctly on real iPhone (Task 3)
- BRAND voice check clean: no exclamation marks, no banned words, no marketing language
- 1 atomic commit for sw.js bump (`chore(20-03): bump sw.js CACHE to couch-v36.2-decision-explanation`); deploy is not a commit
- REQ-20-10 covered by Task 1 + Task 2 curl verification; REQ-20-11 covered by deploy.sh's 5-smoke gate
- Phase 20 ready for `/gsd-verify-work 20` against the 13 ROADMAP success criteria
</success_criteria>

<output>
After UAT confirmation, create `.planning/phases/20-decision-explanation/20-03-SUMMARY.md` per the standard summary template. Then run `/gsd-verify-work 20` to formally close the phase against ROADMAP success criteria 1-13. Update STATE.md sw.js cache version line + Recent shipped phases lookback row + Open follow-ups (any deferred UAT items if surfaced during walkthrough).
</output>
