---
phase: 24-native-video-player
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - js/app.js
  - app.html
  - css/app.css
autonomous: true
requirements:
  - VID-24-01  # YouTube iframe player surface
  - VID-24-03  # HTML5 <video> player surface
  - VID-24-06  # URL field on scheduleWatchparty
  - VID-24-07  # URL field on scheduleSportsWatchparty
  - VID-24-08  # wp.videoUrl + wp.videoSource schema
  - VID-24-09  # currentTime broadcast (host-only, 5s)
  - VID-24-11  # iOS playsinline correctness (both branches)
  - VID-24-14  # player error inline overlay
tags:
  - phase-24
  - native-video-player
  - ui-surface
  - persistence
  - lifecycle
  - broadcast
must_haves:
  truths:
    - "On scheduleWatchparty modal, user can paste a YouTube link or .mp4 URL into 'Video URL (optional)' field"
    - "On scheduleSportsWatchparty modal, the same Video URL field appears and stores to the wp record"
    - "Submitting an invalid URL shows .field-error inline copy and blocks submit; submitting empty silently skips (today's behavior)"
    - "Submitting a valid URL persists wp.videoUrl + wp.videoSource on the wp record under existing host-write rules"
    - "When wp.videoUrl is set AND title has non-DRM path, renderWatchpartyLive renders .wp-video-frame as the modal's first child (player REPLACES poster header)"
    - "When wp.videoUrl is unset OR title is DRM-only, watchparty live modal renders byte-for-byte identical to today (silent dead-end branch — D-03)"
    - "YouTube branch loads via iframe with enablejsapi=1 + playsinline=1; HTML5 branch uses <video controls playsinline preload='metadata'>"
    - "Host-only currentTime broadcast writes wp.currentTimeMs + wp.currentTimeUpdatedAt to Firestore at 5s cadence"
    - "Closing the live modal tears down the player (clears broadcast timer, nulls YT.Player ref) BEFORE stopSportsScorePolling"
    - "Player load failure renders inline italic-serif error + Try-again link (no alert, no blocking modal)"
    - "iOS Safari plays inline (no fullscreen yank) on both branches"
  artifacts:
    - path: "app.html"
      provides: "schedule-modal Video URL field (+ help + error divs)"
      contains: "schedule-video-url"
    - path: "css/app.css"
      provides: ".wp-video-frame + .field-help + .field-error + .field-invalid + .wp-live-header--has-player + .wp-video-error rules"
      contains: ".wp-video-frame"
    - path: "js/app.js"
      provides: "Module import + render branches + lifecycle pair + schedule-modal validation + broadcast write + error overlay"
      contains: "wp.videoUrl"
  key_links:
    - from: "app.html #schedule-video-url input"
      to: "js/app.js confirmStartWatchparty + scheduleSportsWatchparty"
      via: "document.getElementById('schedule-video-url').value -> parseVideoUrl()"
      pattern: "schedule-video-url"
    - from: "js/app.js renderWatchpartyLive"
      to: "wp.videoUrl + titleHasNonDrmPath(t)"
      via: "playerHtml computed string concatenated as first child of el.innerHTML"
      pattern: "wp\\.videoUrl"
    - from: "js/app.js openWatchpartyLive"
      to: "attachVideoPlayer(wp) + Firestore updateDoc currentTimeMs"
      via: "host-only gate + lifecycle attach"
      pattern: "currentTimeMs"
    - from: "js/app.js closeWatchpartyLive"
      to: "teardownVideoPlayer (clear broadcast timer, null YT ref)"
      via: "lifecycle teardown pair"
      pattern: "teardownVideoPlayer"
---

<objective>
Wire Phase 24's user-visible surface end-to-end across `js/app.js`, `app.html`, `css/app.css`. The plan is split into FOUR sub-tasks to keep each within ~25-30% context cost (per CLAUDE.md, js/app.js is ~16k lines / ~190K tokens — never read in full; grep + offset/limit only).

**What ships in this plan:**
1. Persistence (D-04 + VID-24-06/07/08): Video URL field in schedule-modal app.html; wire confirmStartWatchparty + scheduleSportsWatchparty to parse + persist.
2. Render branches (D-01 + D-02 + VID-24-01/03/11): renderWatchpartyLive computes playerHtml from wp.videoUrl + titleHasNonDrmPath; YouTube and MP4 branches.
3. DRM-hide branch (D-03 + VID-24-05): silent zero-DOM when DRM-only; modal byte-for-byte identical to today.
4. Lifecycle (RESEARCH Pitfall 6): attachVideoPlayer in openWatchpartyLive after render; teardownVideoPlayer in closeWatchpartyLive before stopSportsScorePolling.
5. Broadcast (VID-24-09 + VID-24-10): host-only `makeIntervalBroadcaster(VIDEO_BROADCAST_INTERVAL_MS, ...)` wraps `updateDoc(watchpartyRef(id), { currentTimeMs, currentTimeUpdatedAt, ...writeAttribution() })`. Live-stream gate per RESEARCH Open Question 1.
6. Error overlay (VID-24-14): renderPlayerErrorOverlay + reloadWatchpartyPlayer (window-scoped for inline onclick).
7. CSS (UI-SPEC §1 + §2): .wp-video-frame, .field-help, .field-error, .field-invalid, .wp-live-header--has-player, .wp-video-error.

This plan implements VID-24-01, 03, 06, 07, 08, 09, 11, 14. Plan 04 ships sw.js bump + deploy + UAT.

After this plan completes, the watchparty live modal is fully functional end-to-end pre-deploy: schedule a wp with a URL -> open the live modal -> player renders + plays inline on iOS -> currentTime broadcast persists to Firestore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-native-video-player/24-CONTEXT.md
@.planning/phases/24-native-video-player/24-RESEARCH.md
@.planning/phases/24-native-video-player/24-PATTERNS.md
@.planning/phases/24-native-video-player/24-UI-SPEC.md
@CLAUDE.md
@.planning/phases/24-native-video-player/24-02-SUMMARY.md

<interfaces>
NEW import to add to js/app.js (alongside existing imports near line 7-14):

  import { parseVideoUrl, titleHasNonDrmPath, makeIntervalBroadcaster, VIDEO_BROADCAST_INTERVAL_MS } from './native-video-player.js';

Existing import line in js/app.js (line 7 area, verbatim):

  import { LEAGUES as SPORTS_FEED_LEAGUES, fetchSchedule as feedFetchSchedule, ... } from './sports-feed.js';

Existing wp record build in confirmStartWatchparty around js/app.js:10861-10887 (verbatim shape):

  const wp = {
    id, titleId: wpStartTitleId, titleName: t.name, titlePoster: t.poster || '',
    hostId: state.me.id, hostName: state.me.name, ...,
    participants: { [state.me.id]: { ... } }, reactions: []
  };
  await setDoc(watchpartyRef(id), { ...wp, ...writeAttribution() });

Existing renderWatchpartyLive header (verbatim, around js/app.js:11115-11138):

  function renderWatchpartyLive() {
    const el = document.getElementById('wp-live-content');
    if (!el) return;
    const wp = state.watchparties.find(x => x.id === state.activeWatchpartyId);
    if (!wp) { ...; return; }
    const isSport = !!wp.sportEvent;
    const liveTitleHtml = isSport ? ... : `<div class="wp-live-titlename">${escapeHtml(wp.titleName)}</div>`;
    const livePosterHtml = isSport ? ... : `<div class="wp-live-poster" ...></div>`;
    el.innerHTML = `<div class="wp-live-header">${livePosterHtml}<div class="wp-live-titleinfo">${liveTitleHtml}...`;
  }

Existing lifecycle pair (verbatim, around js/app.js:10983-11008):

  window.openWatchpartyLive = function(wpId) {
    state.activeWatchpartyId = wpId;
    renderWatchpartyLive();
    document.getElementById('wp-live-modal-bg').classList.add('on');
    const wp = state.watchparties && state.watchparties.find(x => x.id === wpId);
    if (wp && (wp.mode === 'game' || wp.sportEvent)) {
      startSportsScorePolling(wp);
      ...
    }
  };
  window.closeWatchpartyLive = function() {
    document.getElementById('wp-live-modal-bg').classList.remove('on');
    stopSportsScorePolling();
    state.activeWatchpartyId = null;
  };

Existing schedule-modal field rows in app.html (verbatim, around app.html:967-968):

  <div class="field"><label>Date & time</label><input type="datetime-local" id="schedule-datetime"></div>
  <div class="field"><label>Note (optional)</label><textarea id="schedule-note" placeholder="..."></textarea></div>

Existing CSS anchors:
- .schedule-modal .field family at css/app.css:675-689
- .trailer-frame at css/app.css:1608
- .wp-live-* family at css/app.css:737-773
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: app.html schedule-modal field + css/app.css rule blocks</name>
  <files>app.html, css/app.css</files>
  <read_first>
    - app.html (read offset 950, limit 30 — see existing schedule-modal field structure at lines 957-973)
    - css/app.css (read offset 670, limit 25 — see .schedule-modal .field family at 675-689)
    - css/app.css (read offset 1600, limit 15 — see .trailer-frame at 1608)
    - css/app.css (read offset 730, limit 50 — see .wp-live-* family at 737-773)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Layout Contracts §1 + §2 + Copywriting Contract — verbatim copy strings)
  </read_first>
  <action>
    A. app.html edit:

    Insert ONE new .field block in the schedule-modal BETWEEN existing Date & time field (app.html:967) and Note (optional) field (app.html:968). Verbatim shape from UI-SPEC §2 (DO NOT edit copy strings — locked):

      <div class="field">
        <label for="schedule-video-url">Video URL (optional)</label>
        <input type="url" inputmode="url" id="schedule-video-url"
          placeholder="YouTube link or .mp4 URL"
          autocomplete="off" spellcheck="false" />
        <div class="field-help" id="schedule-video-url-help">
          Paste a YouTube watch link or a direct .mp4 URL. We'll play it together.
        </div>
        <div class="field-error" id="schedule-video-url-error" role="alert" hidden>
          That link doesn't look like YouTube or an .mp4 file. Skip it or try another.
        </div>
      </div>

    Sports-modal handling: grep `grep -n "scheduleSportsWatchparty\|sports-picker-modal\|game-picker-modal" app.html` to see if sports flow uses a SEPARATE modal scaffold. If a separate sports modal exists with its own form fields, mirror the exact same .field block there with the SAME id `schedule-video-url` (only one modal is open at a time, so id collision is fine). If sports flow reuses #schedule-modal, no second insertion is needed.

    B. css/app.css edit:

    Insert TWO rule-blocks. Per UI-SPEC §Layout/Component Contracts §1 and §2.

    Block 1 — `.wp-video-frame` family (insert near .trailer-frame at css/app.css:1608; .trailer-frame stays UNCHANGED — sibling class with distinct radius):

      /* Phase 24 — Native video player surface inside watchparty live modal */
      .wp-video-frame{width:100%;aspect-ratio:16/9;background:#000;border-radius:0;overflow:hidden;position:relative;flex-shrink:0}
      .wp-video-frame iframe,.wp-video-frame video{width:100%;height:100%;border:none;display:block}
      .wp-live-modal > .wp-video-frame:first-child{border-radius:24px 24px 0 0}
      .wp-live-header--has-player .wp-live-poster{display:none}
      .wp-video-error{position:absolute;left:0;right:0;bottom:0;padding:var(--s3) var(--s4);background:linear-gradient(180deg,rgba(0,0,0,0) 0%,rgba(0,0,0,0.7) 100%);color:var(--ink);font-family:var(--font-sans);font-size:var(--t-meta);display:flex;align-items:center;gap:var(--s3);justify-content:space-between}
      .wp-video-error em{font-family:var(--font-serif);font-style:italic;color:var(--ink-warm)}
      .wp-video-error .link-like{font-size:var(--t-meta);color:var(--accent)}

    Block 2 — `.field-help` + `.field-error` + `.field-invalid` family (insert INSIDE the .schedule-modal block at css/app.css:675-689 right AFTER `.schedule-modal input:focus`):

      /* Phase 24 — generic input-validation utility classes */
      .field-help{font-family:var(--font-sans);font-size:var(--t-meta);font-weight:400;color:var(--ink-dim);margin-top:4px;line-height:1.4}
      .field-error{font-family:var(--font-sans);font-size:var(--t-meta);font-weight:400;color:var(--ink-warm);margin-top:6px;line-height:1.4}
      .schedule-modal input.field-invalid{border-color:var(--bad)}

    No new CSS custom properties. Every value uses an existing token (--t-meta, --ink-dim, --ink-warm, --bad, --font-sans, --font-serif, --accent, --s3, --s4).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const html=fs.readFileSync('app.html','utf8');if(!html.includes('schedule-video-url'))process.exit(1);if(!html.includes('Paste a YouTube watch link'))process.exit(2);if(!html.includes(\"That link doesn't look like YouTube\"))process.exit(3);const css=fs.readFileSync('css/app.css','utf8');if(!css.includes('.wp-video-frame'))process.exit(4);if(!css.includes('.field-help'))process.exit(5);if(!css.includes('.field-error'))process.exit(6);if(!css.includes('.field-invalid'))process.exit(7);if(!css.includes('.wp-live-header--has-player'))process.exit(8);if(!css.includes('.wp-video-error'))process.exit(9);console.log('html+css OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "schedule-video-url" app.html` returns at least 4 matches
    - `grep "Video URL (optional)" app.html` returns 1 match (label verbatim)
    - `grep "YouTube link or .mp4 URL" app.html` returns 1 match (placeholder verbatim)
    - `grep "Paste a YouTube watch link or a direct .mp4 URL" app.html` returns 1 match
    - `grep "That link doesn't look like YouTube or an .mp4 file" app.html` returns 1 match
    - `grep "type=\"url\"" app.html` returns at least 1 match
    - `grep "\.wp-video-frame" css/app.css` returns at least 3 matches
    - `grep "aspect-ratio:16/9" css/app.css` returns at least 2 matches (.trailer-frame still + new .wp-video-frame)
    - `grep "\.field-help" css/app.css` returns at least 1 match
    - `grep "\.field-error" css/app.css` returns at least 1 match
    - `grep "\.field-invalid" css/app.css` returns at least 1 match
    - `grep "\.wp-live-header--has-player" css/app.css` returns at least 1 match
    - `grep "\.wp-video-error" css/app.css` returns at least 2 matches
    - `node -e "require('child_process').execSync('echo')"` works (no broken JS evaluation)
  </acceptance_criteria>
  <done>
    HTML field inserted with verbatim UI-SPEC copy; CSS rules added with no new custom properties; .trailer-frame preserved unchanged; sports-modal field handled.
  </done>
</task>

<task type="auto">
  <name>Task 3.2: js/app.js import + schedule-modal validation + wp-record extension (movie + sports flows)</name>
  <files>js/app.js</files>
  <read_first>
    - .planning/phases/24-native-video-player/24-PATTERNS.md (lines 209-294 — wp-record extension pattern + parser-call pattern, both flows)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Schedule-modal Video URL field — validation timing, submit-blocked behavior)
    - .planning/phases/24-native-video-player/24-RESEARCH.md (Code Examples §3 — parser; Open Question 1 — sports-flow live-stream caveat)
    - js/native-video-player.js (Plan 02 output — confirms parseVideoUrl signature)
    - js/app.js: grep for the function start lines first via `grep -n "function confirmStartWatchparty\|window.confirmStartWatchparty\|function scheduleSportsWatchparty\|window.scheduleSportsWatchparty" js/app.js`. Then Read each match with offset=<line> limit=80. Per CLAUDE.md never read app.js in full.
    - js/app.js (read offset 1, limit 30 — see the existing import block to find exact line for new import)
  </read_first>
  <action>
    Three coordinated edits to js/app.js.

    A. Module import — add a new import statement alongside existing imports at line 7-14 area. Locate the existing `import ... from './sports-feed.js';` line via grep first, then insert the new import on the line immediately after it:

      import { parseVideoUrl, titleHasNonDrmPath, makeIntervalBroadcaster, VIDEO_BROADCAST_INTERVAL_MS } from './native-video-player.js';

    B. confirmStartWatchparty wp-record extension (movie creation, around js/app.js:10845-10887):

    Use grep to find the function start, then Read with offset=<found-line> and limit=80. Locate the `const wp = { id, titleId: wpStartTitleId, ... }` object literal.

    Insert this validation block IMMEDIATELY BEFORE that object literal:

      // Phase 24 — Video URL field (D-04). Optional; empty = today's coordination-only behavior.
      const videoUrlInput = document.getElementById('schedule-video-url');
      const rawVideoUrl = videoUrlInput ? videoUrlInput.value.trim() : '';
      let parsedVideoUrl = null;
      if (rawVideoUrl) {
        parsedVideoUrl = parseVideoUrl(rawVideoUrl);
        if (!parsedVideoUrl) {
          // Submit-blocked branch (UI-SPEC Submit-blocked): inline error + .field-invalid border.
          if (videoUrlInput) videoUrlInput.classList.add('field-invalid');
          const errEl = document.getElementById('schedule-video-url-error');
          if (errEl) errEl.removeAttribute('hidden');
          return;
        }
        if (videoUrlInput) videoUrlInput.classList.remove('field-invalid');
        const errEl = document.getElementById('schedule-video-url-error');
        if (errEl) errEl.setAttribute('hidden', '');
      }

    Then EXTEND the existing `const wp = { ... }` literal with two new fields placed AFTER `reactions: []`:

      videoUrl: parsedVideoUrl ? parsedVideoUrl.url : null,
      videoSource: parsedVideoUrl ? parsedVideoUrl.source : null

    The existing `await setDoc(watchpartyRef(id), { ...wp, ...writeAttribution() })` already authorizes the new fields under existing host-write rules per RESEARCH Assumption A3. Do NOT touch the setDoc call.

    C. scheduleSportsWatchparty wp-record extension (sports creation, around js/app.js:10199-10289):

    Use grep to find the function start, then Read with offset=<found-line> and limit=100. Locate the `const wp = { id, ... }` object literal.

    Insert the SAME validation block (verbatim — both flows read from `#schedule-video-url` since one modal is open at a time) IMMEDIATELY BEFORE the wp object literal.

    Then extend the wp literal with the same two new fields after `reactions: []`.

    Sports caveat (RESEARCH Open Question 1): Live-stream YouTube URLs have meaningless currentTime. Creation MUST still allow paste — the runtime gate in Task 3.4 skips broadcast writes when `YT.Player.getDuration()` returns 0 or Infinity. No creation-time validation.

    Do NOT touch any other line in either flow.
  </action>
  <verify>
    <automated>node --check js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check js/app.js` exits 0 (syntax)
    - `grep -c "from './native-video-player.js'" js/app.js` returns 1 (single import line)
    - `grep "parseVideoUrl, titleHasNonDrmPath, makeIntervalBroadcaster, VIDEO_BROADCAST_INTERVAL_MS" js/app.js` returns at least 1 match (the import line)
    - `grep -c "videoUrl: parsedVideoUrl" js/app.js` returns 2 (one per creation flow)
    - `grep -c "videoSource: parsedVideoUrl" js/app.js` returns 2 (one per creation flow)
    - `grep -c "classList.add('field-invalid')" js/app.js` returns at least 2
    - `grep -c "schedule-video-url-error" js/app.js` returns at least 4 (2 per flow: removeAttribute + setAttribute)
    - `grep "parseVideoUrl(rawVideoUrl)" js/app.js` returns at least 2 matches (one per creation flow)
  </acceptance_criteria>
  <done>
    Import added; both creation flows validate + persist videoUrl/videoSource; setDoc calls untouched; node --check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3.3: renderWatchpartyLive playerHtml branches + DRM-hide branch (js/app.js)</name>
  <files>js/app.js</files>
  <read_first>
    - .planning/phases/24-native-video-player/24-PATTERNS.md (lines 117-167 — render-branch pattern + analogs at js/app.js:7838 + :13667 + :11115)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Layout Contracts §1 — exact iframe/video shapes; §3 — DRM-hide branch silent zero-DOM)
    - .planning/phases/24-native-video-player/24-RESEARCH.md (Code Examples §1 + §2 — render shapes; Pitfall 1 — iOS playsinline)
    - js/app.js: grep `grep -n "function renderWatchpartyLive\|window.renderWatchpartyLive" js/app.js` to find the function start. Then Read with offset=<found-line> limit=80 to see the body.
    - js/app.js: grep `grep -n "trailer-frame.*youtube.com/embed" js/app.js` to confirm the existing trailer iframe analog pattern at lines 7838 + 13667.
  </read_first>
  <action>
    Single edit to renderWatchpartyLive in js/app.js (around line 11115-11147).

    Inside the function body, BEFORE the `el.innerHTML = ...` template-literal assignment, ADD a `playerHtml` computation block. Place this AFTER the `const isSport = !!wp.sportEvent;` line and AFTER any existing `livePosterHtml` / `liveTitleHtml` derivations.

      // Phase 24 — Native video player surface (D-01 + D-02 + D-03)
      // Gate: render player only when wp.videoUrl is set AND title has at least one non-DRM path.
      // DRM-hide branch (D-03 + CONTEXT.md <specifics>): when gate fails, render NOTHING.
      // Modal looks byte-for-byte identical to today.
      let playerHtml = '';
      let headerExtraClass = '';
      if (wp.videoUrl && wp.videoSource) {
        const t = state.titles && state.titles.find(x => x.id === wp.titleId);
        if (titleHasNonDrmPath(t)) {
          if (wp.videoSource === 'youtube') {
            // Extract YouTube ID from the persisted url. parser stores wp.videoUrl as raw paste;
            // re-run parser at render time so we get the canonical id back.
            const parsed = parseVideoUrl(wp.videoUrl);
            if (parsed && parsed.id) {
              const safeId = encodeURIComponent(parsed.id);
              playerHtml = '<div class="wp-video-frame">' +
                '<iframe id="wp-yt-player" class="wp-video-frame--youtube" ' +
                'src="https://www.youtube.com/embed/' + safeId + '?enablejsapi=1&playsinline=1" ' +
                'frameborder="0" allow="autoplay; encrypted-media; picture-in-picture" ' +
                'allowfullscreen></iframe></div>';
              headerExtraClass = ' wp-live-header--has-player';
            }
          } else if (wp.videoSource === 'mp4') {
            const safeUrl = escapeHtml(wp.videoUrl);
            playerHtml = '<div class="wp-video-frame">' +
              '<video id="wp-mp4-player" class="wp-video-frame--mp4" ' +
              'controls playsinline preload="metadata" src="' + safeUrl + '"></video></div>';
            headerExtraClass = ' wp-live-header--has-player';
          }
        }
      }

    Then UPDATE the existing `el.innerHTML = ...` assignment so:
    - playerHtml is concatenated as the FIRST CHILD (before the existing `<div class="wp-live-header">...</div>`)
    - the existing `<div class="wp-live-header">` opening tag is changed to `<div class="wp-live-header${headerExtraClass}">` so the `--has-player` class hides the redundant poster when player is shown

    Example shape (the existing template-literal currently looks like ``el.innerHTML = `<div class="wp-live-header">...```; rewrite to):

      el.innerHTML = playerHtml + '<div class="wp-live-header' + headerExtraClass + '">' + livePosterHtml + ...

    OR keep the existing template-literal style and inject `${playerHtml}` + `${headerExtraClass}` using template-substitutions. Either style works; preserve the rest of the template literal's content unchanged.

    XSS guards (per UI-SPEC §Registry Safety):
    - YouTube: extracted videoId wrapped in encodeURIComponent (raw user-paste never reaches iframe src)
    - MP4: wp.videoUrl wrapped in escapeHtml (already validated by parseVideoUrl which rejects non-http(s) protocols)

    Do NOT touch any code OUTSIDE renderWatchpartyLive. Do NOT modify livePosterHtml / liveTitleHtml — they are preserved (poster gets hidden via CSS when --has-player class is present).
  </action>
  <verify>
    <automated>node --check js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check js/app.js` exits 0
    - `grep "wp-video-frame" js/app.js` returns at least 2 matches (YouTube branch + MP4 branch wrappers)
    - `grep "wp-live-header--has-player" js/app.js` returns at least 1 match (headerExtraClass concat)
    - `grep "enablejsapi=1" js/app.js` returns at least 1 match
    - `grep -c "playsinline" js/app.js` returns at least 2 matches (one for YouTube URL `playsinline=1`, one for `<video playsinline`)
    - `grep "encodeURIComponent" js/app.js` returns at least 4 matches (existing trailer iframes at :7838 + :13667 + new YouTube branch + at least one other existing call)
    - `grep "titleHasNonDrmPath" js/app.js` returns at least 1 match (gate call site in renderWatchpartyLive)
    - `grep "wp.videoSource === 'youtube'\|wp.videoSource === \"youtube\"" js/app.js` returns at least 1 match
    - `grep "wp.videoSource === 'mp4'\|wp.videoSource === \"mp4\"" js/app.js` returns at least 1 match
    - `grep "wp-yt-player" js/app.js` returns at least 1 match
    - `grep "wp-mp4-player" js/app.js` returns at least 1 match
    - `grep "escapeHtml(wp.videoUrl)" js/app.js` returns at least 1 match (XSS guard on MP4 src)
  </acceptance_criteria>
  <done>
    renderWatchpartyLive computes playerHtml from wp.videoUrl + wp.videoSource + titleHasNonDrmPath; YouTube + MP4 branches both ship with iOS playsinline + XSS guards; DRM-hide branch is zero-DOM (silent dead-end); existing template-literal preserved otherwise; node --check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3.4: Lifecycle attach/teardown + currentTime broadcast + error overlay + reload (js/app.js)</name>
  <files>js/app.js</files>
  <read_first>
    - .planning/phases/24-native-video-player/24-PATTERNS.md (lines 168-204 — lifecycle attach/teardown pattern + Phase 23 analog at js/app.js:10983-11008; lines 296-318 — broadcast pattern; lines 596-617 — error overlay pattern)
    - .planning/phases/24-native-video-player/24-RESEARCH.md (Pattern 1 — YouTube IFrame API + ensureYouTubeApi; Pattern 2 — HTML5 video timeupdate; Pattern 3 — throttle; Pitfall 6 — modal-close race; Pitfall 7 — Phase 26 cadence; Open Question 1 — live-stream gate; Code Examples §1, §2, §5, §6)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Interaction States — Player surface error/reload behavior; Copywriting — error copy verbatim)
    - js/app.js: grep `grep -n "window.openWatchpartyLive\|window.closeWatchpartyLive\|stopSportsScorePolling\|startSportsScorePolling" js/app.js`. Then Read each match with offset=<line> limit=40 per CLAUDE.md.
    - js/firebase.js (read full — confirms `updateDoc`, `doc` exports already imported into app.js for Firestore writes)
  </read_first>
  <action>
    Multi-part edit to js/app.js, all near the existing lifecycle pair at js/app.js:10983-11008.

    Pre-flight: confirm the Firestore helpers `updateDoc` and `watchpartyRef` are already imported into js/app.js. Run `grep "updateDoc\|watchpartyRef" js/app.js` — both names should already appear (Phase 7 / Phase 15.1 imports). If the grep returns 0 for `updateDoc`, add it to the existing firebase.js import line in js/app.js (search for `from './firebase.js'` and append `updateDoc` to the named-import list). Same for `watchpartyRef` if missing. Do NOT add a duplicate import line.

    A. Module-scope state + ensureYouTubeApi helper. Add IMMEDIATELY ABOVE `window.openWatchpartyLive` (use grep to find that line). Place after any imports/constants but before the lifecycle pair:

      // Phase 24 — Native video player runtime state + helpers
      // (Co-located with openWatchpartyLive / closeWatchpartyLive lifecycle pair.)
      let _ytApiLoading = null;
      let _wpYtPlayer = null;             // YT.Player instance (null when no YouTube branch active)
      let _wpVideoBroadcaster = null;     // makeIntervalBroadcaster closure (host-only)
      let _wpVideoSampler = null;         // setInterval id for YouTube currentTime poll (host-only)
      let _wpVideoElement = null;         // <video> element ref for MP4 branch (for cleanup)

      function ensureYouTubeApi() {
        if (window.YT && window.YT.Player) return Promise.resolve();
        if (_ytApiLoading) return _ytApiLoading;
        _ytApiLoading = new Promise(resolve => {
          if (!document.getElementById('youtube-iframe-api-script')) {
            const tag = document.createElement('script');
            tag.id = 'youtube-iframe-api-script';
            tag.src = 'https://www.youtube.com/iframe_api';
            document.head.appendChild(tag);
          }
          window.onYouTubeIframeAPIReady = () => resolve();
        });
        return _ytApiLoading;
      }

      // Host-only broadcast: writes wp.currentTimeMs + wp.currentTimeUpdatedAt to Firestore.
      // Phase 26 reads wp.currentTimeMs as runtimePositionMs anchor for replay UX.
      // Schema is forward-compatible: { currentTimeMs: number, currentTimeUpdatedAt: number, ...writeAttribution() }.
      // Phase 26 must NOT need a schema migration to consume this — the field names + types are stable.
      async function broadcastCurrentTime(wpId, currentTimeSeconds) {
        if (!wpId || typeof currentTimeSeconds !== 'number' || isNaN(currentTimeSeconds)) return;
        try {
          await updateDoc(watchpartyRef(wpId), {
            currentTimeMs: Math.round(currentTimeSeconds * 1000),
            currentTimeUpdatedAt: Date.now(),
            ...writeAttribution()
          });
        } catch (e) {
          // Sentry breadcrumb only; don't crash the playback experience
          if (typeof Sentry !== 'undefined' && Sentry.addBreadcrumb) {
            Sentry.addBreadcrumb({ category: 'videoBroadcast.write.failed', level: 'warning', data: { wpId, error: String(e) } });
          }
        }
      }

      // YouTube live-stream gate per RESEARCH Open Question 1: if duration is 0 or Infinity,
      // the URL is a live broadcast — currentTime is meaningless for replay anchoring; skip writes.
      function ytPlayerHasFiniteDuration(player) {
        try {
          const d = player && typeof player.getDuration === 'function' ? player.getDuration() : 0;
          return d > 0 && isFinite(d);
        } catch (e) { return false; }
      }

      async function attachVideoPlayer(wp) {
        if (!wp || !wp.videoUrl || !wp.videoSource) return;
        const t = state.titles && state.titles.find(x => x.id === wp.titleId);
        if (!titleHasNonDrmPath(t)) return;
        const isHost = !!(state.me && state.me.id === wp.hostId);
        if (wp.videoSource === 'youtube') {
          await ensureYouTubeApi();
          const el = document.getElementById('wp-yt-player');
          if (!el) return; // modal closed during load (RESEARCH Pitfall 6)
          _wpYtPlayer = new YT.Player('wp-yt-player', {
            events: {
              onReady: () => {
                if (!isHost) return;
                // Live-stream gate: skip broadcast loop on infinite-duration streams
                if (!ytPlayerHasFiniteDuration(_wpYtPlayer)) return;
                _wpVideoBroadcaster = makeIntervalBroadcaster(VIDEO_BROADCAST_INTERVAL_MS, sec => broadcastCurrentTime(wp.id, sec));
                // Sample YT player at half-cadence; broadcaster throttles writes
                _wpVideoSampler = setInterval(() => {
                  try {
                    if (_wpYtPlayer && typeof _wpYtPlayer.getCurrentTime === 'function') {
                      _wpVideoBroadcaster(_wpYtPlayer.getCurrentTime());
                    }
                  } catch (e) { /* ignore */ }
                }, Math.max(1000, Math.floor(VIDEO_BROADCAST_INTERVAL_MS / 2)));
              },
              onError: () => renderPlayerErrorOverlay('wp-yt-player')
            }
          });
        } else if (wp.videoSource === 'mp4') {
          const video = document.getElementById('wp-mp4-player');
          if (!video) return;
          _wpVideoElement = video;
          if (isHost) {
            _wpVideoBroadcaster = makeIntervalBroadcaster(VIDEO_BROADCAST_INTERVAL_MS, sec => broadcastCurrentTime(wp.id, sec));
            video.addEventListener('timeupdate', () => {
              if (_wpVideoBroadcaster) _wpVideoBroadcaster(video.currentTime);
            });
          }
          video.addEventListener('error', () => renderPlayerErrorOverlay('wp-mp4-player'));
        }
      }

      function teardownVideoPlayer() {
        if (_wpVideoSampler) { clearInterval(_wpVideoSampler); _wpVideoSampler = null; }
        _wpVideoBroadcaster = null;
        if (_wpYtPlayer && typeof _wpYtPlayer.destroy === 'function') {
          try { _wpYtPlayer.destroy(); } catch (e) { /* ignore */ }
        }
        _wpYtPlayer = null;
        if (_wpVideoElement) {
          try { _wpVideoElement.pause(); } catch (e) { /* ignore */ }
          _wpVideoElement = null;
        }
      }

      // Per UI-SPEC Interaction States: italic-serif "Player couldn't load that link." + Try again link.
      function renderPlayerErrorOverlay(playerElementId) {
        const player = document.getElementById(playerElementId);
        if (!player) return;
        const wrap = player.closest('.wp-video-frame');
        if (!wrap) return;
        let overlay = wrap.querySelector('.wp-video-error');
        if (!overlay) {
          overlay = document.createElement('div');
          overlay.className = 'wp-video-error';
          wrap.appendChild(overlay);
        }
        overlay.innerHTML = "<em>Player couldn't load that link.</em>" +
          ' <a class="link-like" data-action="retry-video">Try again</a>';
      }

      // window-scoped so the inline onclick in renderPlayerErrorOverlay can reach it
      window.reloadWatchpartyPlayer = function() {
        const wp = state.watchparties && state.watchparties.find(x => x.id === state.activeWatchpartyId);
        if (!wp) return;
        teardownVideoPlayer();
        renderWatchpartyLive();
        attachVideoPlayer(wp);
      };

    B. Modify openWatchpartyLive to call attachVideoPlayer AFTER renderWatchpartyLive paints. Locate the existing function body and add the new call AFTER the existing `if (wp && (wp.mode === 'game' || wp.sportEvent))` block:

      // Phase 24 — wire native video player when wp.videoUrl is set + title is non-DRM.
      // Mirror of Phase 23 widened-gate pattern: attach pair for stop/start lifecycle on player.
      if (wp && wp.videoUrl) {
        attachVideoPlayer(wp);
      }

    C. Modify closeWatchpartyLive to call teardownVideoPlayer BEFORE stopSportsScorePolling. Add as the FIRST line inside the function body:

      teardownVideoPlayer();

    Do not modify any other code in either function. Preserve existing class manipulations and state.activeWatchpartyId assignment.
  </action>
  <verify>
    <automated>node --check js/app.js && node scripts/smoke-native-video-player.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check js/app.js` exits 0 (syntax)
    - `node scripts/smoke-native-video-player.cjs` exits 0 (Plan 01 smoke still green — proves no contract drift)
    - `grep -c "function attachVideoPlayer\|async function attachVideoPlayer" js/app.js` returns 1
    - `grep -c "function teardownVideoPlayer" js/app.js` returns 1
    - `grep "ensureYouTubeApi" js/app.js` returns at least 2 matches (declaration + call site)
    - `grep "broadcastCurrentTime" js/app.js` returns at least 2 matches (declaration + call sites in attach)
    - `grep "currentTimeMs" js/app.js` returns at least 2 matches (in updateDoc payload + comment)
    - `grep "currentTimeUpdatedAt" js/app.js` returns at least 1 match
    - `grep "VIDEO_BROADCAST_INTERVAL_MS" js/app.js` returns at least 2 matches (broadcaster construct + sampler interval)
    - `grep "state.me.id === wp.hostId\|state.me && state.me.id === wp.hostId" js/app.js` returns at least 1 match (host-only gate)
    - `grep "ytPlayerHasFiniteDuration" js/app.js` returns at least 2 matches (declaration + use — live-stream gate)
    - `grep "renderPlayerErrorOverlay" js/app.js` returns at least 3 matches (declaration + YT onError + video error event listener)
    - `grep "Player couldn't load that link" js/app.js` returns 1 match (verbatim error copy — single canonical form)
    - `grep "window.reloadWatchpartyPlayer" js/app.js` returns at least 1 match
    - `grep "attachVideoPlayer(wp)" js/app.js` returns at least 1 match (call site in openWatchpartyLive)
    - `grep "teardownVideoPlayer()" js/app.js` returns at least 2 matches (call site in closeWatchpartyLive + reloadWatchpartyPlayer)
    - `grep -B1 "stopSportsScorePolling()" js/app.js | grep teardownVideoPlayer` finds the teardown ABOVE stopSportsScorePolling (verifies ordering: teardown BEFORE stop-polling)
    - `grep "videoBroadcast.write.failed" js/app.js` returns at least 1 match (Sentry breadcrumb category)
    - `grep "updateDoc\|watchpartyRef" js/app.js` returns at least 2 matches (confirms both Firestore helpers already in scope from existing imports — no new import line needed)
  </acceptance_criteria>
  <done>
    Lifecycle pair attach + teardown live; YT IFrame API loads idempotently; host-only broadcast wired with live-stream gate; broadcast writes to Firestore via updateDoc with writeAttribution; error overlay + reload window-scoped function; closeWatchpartyLive tears down BEFORE stopSportsScorePolling. All grep acceptance criteria pass; smoke contract still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> schedule-modal field | Untrusted URL string crosses into parseVideoUrl + persisted to Firestore. |
| user input -> iframe src | Extracted YouTube videoId wrapped in encodeURIComponent before interpolation. |
| user input -> <video src> | Validated MP4 URL wrapped in escapeHtml before interpolation. |
| host client -> Firestore wp record | currentTime broadcast writes go through existing host-write rules (auth.uid == hostUid). |
| YouTube IFrame API -> client | Third-party script loaded from https://www.youtube.com/iframe_api at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering | XSS via crafted videoUrl in iframe src | mitigate | parseVideoUrl rejects non-http(s) protocols; YouTube videoId regex `/^[A-Za-z0-9_-]{6,}$/` rejects HTML/JS chars; encodeURIComponent on extracted id; raw user-paste never reaches iframe src (parser extracts canonical id, render uses the id). Smoke (Plan 01) asserts the rejection path. |
| T-24-03-02 | Tampering | XSS via crafted videoUrl in `<video src>` | mitigate | parseVideoUrl protocol allowlist; escapeHtml on wp.videoUrl before src interpolation (defense in depth — URL constructor + .mp4 check is the primary barrier). |
| T-24-03-03 | Information Disclosure | DRM-hide false positive (legit personal MP4 hidden) | accept | RESEARCH Pitfall 4 + UI-SPEC §3 — detection threshold is "ALL providers DRM" with rent/buy escape hatch. Plan 02's titleHasNonDrmPath returns true when no providers known (defensive on incomplete data). |
| T-24-03-04 | Denial of Service | Firestore write abuse (host writes flooding the wp doc) | mitigate | makeIntervalBroadcaster throttles writes to 5000ms minimum interval (D-09 cadence). Per RESEARCH Pitfall 2: 12 writes/min/wp; well under Firestore's 1-write/sec/document soft cap. Existing host-write rules (Phase 5+7+15.1) gate writes. |
| T-24-03-05 | Mixed-content | Plex/Jellyfin HTTP URLs blocked on HTTPS Couch | accept | Browser-policy enforced (cannot fix client-side). RESEARCH Pitfall 5: surface to user via in-modal `<video error>` event -> renderPlayerErrorOverlay. Stretch goal (deferred): detect http:// at parse time + surface specific schedule-modal error. |
| T-24-03-06 | Repudiation | Untrusted broadcast writes (non-host writing currentTime) | mitigate | Host-only gate: `state.me.id === wp.hostId` checked before constructing broadcaster. Plus existing writeAttribution() stamping uid + actingMember per Phase 5 contract — Firestore rules reject non-host writes. |
| T-24-03-07 | Spoofing | YT.Player.getCurrentTime returning untrusted values | accept | Host's local YT.Player reading; trust boundary already inside host's browser. If host's browser is compromised, larger problems. |
| T-24-03-08 | Elevation of Privilege | iframe sandbox bypass (YouTube iframe escape) | accept | YouTube embed is from a known origin (youtube.com); v2.1 generic-allowlist gate is deferred (CONTEXT.md `<deferred>`). For v2 launch with YouTube-only iframe surface, this is not a meaningful surface. |
| T-24-03-09 | Modal-close race | YT.Player binds to no-longer-existing DOM element | mitigate | RESEARCH Pitfall 6 — attachVideoPlayer checks `if (!el) return` before binding; teardownVideoPlayer destroys YT.Player + clears interval + nulls refs. |
</threat_model>

<verification>
- `node --check js/app.js` exits 0
- `node --check js/native-video-player.js` exits 0 (Plan 02 unchanged)
- `node scripts/smoke-native-video-player.cjs` exits 0 (Plan 01 smoke green — contract not drifted)
- `npm run smoke` exits 0 (full chain green)
- HTML + CSS validate via `node -e` syntax-snippets in Task 3.1
- Grep-level acceptance criteria all pass for Tasks 3.1, 3.2, 3.3, 3.4
</verification>

<success_criteria>
- All four sub-tasks pass their <verify> commands
- Plan 01 smoke remains green throughout (no contract drift)
- `js/app.js` parses cleanly after all four sub-tasks
- Schedule-modal persists `videoUrl` + `videoSource` on both creation flows
- renderWatchpartyLive renders 3 branches: YouTube / MP4 / silent zero-DOM
- openWatchpartyLive calls attachVideoPlayer; closeWatchpartyLive calls teardownVideoPlayer FIRST
- Broadcast writes use updateDoc with writeAttribution + 5s throttle + host-only gate + live-stream gate
- iOS playsinline asserted on both branches via grep
- Error overlay copy is verbatim from UI-SPEC
- No new CSS custom properties added
- Sports flow caveat handled (live-stream gate at runtime, not creation)
</success_criteria>

<output>
After completion, create `.planning/phases/24-native-video-player/24-03-SUMMARY.md` capturing:
- Final line counts: js/app.js delta, app.html delta, css/app.css delta
- Locations (line-numbers) of: import, validation block in confirmStartWatchparty, validation block in scheduleSportsWatchparty, playerHtml block in renderWatchpartyLive, attachVideoPlayer + teardownVideoPlayer + ensureYouTubeApi + broadcastCurrentTime + renderPlayerErrorOverlay + window.reloadWatchpartyPlayer
- Sports-modal field decision (single insertion vs separate modal)
- Verification commands + outputs
- Phase 26 schema contract reaffirmation: `wp.currentTimeMs: number (Math.round(seconds * 1000))` + `wp.currentTimeUpdatedAt: number (Date.now())` — Phase 26 must consume without migration
- Any deviation from this plan
</output>
