---
phase: 24-native-video-player
plan: 04
type: execute
wave: 3
depends_on:
  - 24-03
files_modified:
  - sw.js
  - .planning/phases/24-native-video-player/24-HUMAN-UAT.md
autonomous: false
requirements:
  - VID-24-13  # sw.js CACHE bump
tags:
  - phase-24
  - native-video-player
  - deploy
  - cache-bump
  - uat
must_haves:
  truths:
    - "sw.js CACHE constant reads couch-v37-native-video-player after deploy"
    - "couchtonight.app live curl returns the same v37 cache string in service worker fetch"
    - "All 8 smoke contracts pass before deploy gate fires (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player)"
    - "24-HUMAN-UAT.md exists with iOS Safari + iOS PWA standalone test scripts for both branches"
  artifacts:
    - path: "sw.js"
      provides: "PWA cache invalidation marker"
      contains: "couch-v37-native-video-player"
    - path: ".planning/phases/24-native-video-player/24-HUMAN-UAT.md"
      provides: "Human-side UAT scripts (iOS Safari in-tab + iOS PWA standalone for both YouTube and MP4 branches + DRM-hide silent-branch verification + currentTime broadcast verification)"
      min_lines: 60
  key_links:
    - from: "scripts/deploy.sh ${TAG}"
      to: "sw.js CACHE constant"
      via: "auto-bump via sed at deploy.sh:128-137"
      pattern: "couch-v37-native-video-player"
    - from: "Production curl"
      to: "Service worker"
      via: "PWA registration revalidates on next online activation"
      pattern: "v37-native-video-player"
---

<objective>
Close out Phase 24: bump sw.js CACHE, run pre-flight smoke gates, deploy to couchtonight.app, scaffold the HUMAN-UAT tracker.

Per CLAUDE.md: any user-visible app shell change requires a CACHE bump. Phase 24 introduces a NEW player surface inside the watchparty live modal -> user-visible -> bump REQUIRED.

Per RESEARCH §Project Constraints: recommended tag is `37-native-video-player` -> `couch-v37-native-video-player`. The deploy script auto-bumps via sed; manual edit is the fallback when not deploying immediately.

Per Phase 18 / 19 / 20 / 22 / 23 precedent: code-level acceptance + deferred device UAT in a HUMAN-UAT.md tracker. Device UAT items run after deploy on real iPhone hardware (in-Safari mode + home-screen PWA standalone mode for both YouTube and MP4 branches).

This plan has a CHECKPOINT before deploy because deploys are user-visible production changes — but per Couch deploy autonomy policy (user feedback in MEMORY.md: "for Couch project, Claude may run firebase deploy to queuenight-84044 without per-deploy approval"), the autonomous flag still allows the deploy to proceed without an explicit approval beat. The checkpoint is for verifying the smoke gates pass cleanly before pulling the trigger.

Output: sw.js bumped (auto via deploy.sh) + production deploy + HUMAN-UAT.md scaffold + couch-only deploy (no queuenight changes per RESEARCH §Project Constraints).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-native-video-player/24-CONTEXT.md
@.planning/phases/24-native-video-player/24-RESEARCH.md
@.planning/phases/24-native-video-player/24-PATTERNS.md
@.planning/phases/24-native-video-player/24-UI-SPEC.md
@CLAUDE.md
@.planning/phases/24-native-video-player/24-03-SUMMARY.md

<interfaces>
sw.js CACHE constant (current line 8 — verbatim):

  const CACHE = 'couch-v36.7-live-scoreboard';

Will become:

  const CACHE = 'couch-v37-native-video-player';

deploy.sh auto-bump pattern at scripts/deploy.sh:128-137 (verbatim — auto-handles the sed):

  if [ -n "$TAG" ]; then
    CACHE_NEW="couch-v${TAG}"
    if grep -q "const CACHE = '${CACHE_NEW}';" sw.js; then
      echo "sw.js CACHE already at ${CACHE_NEW}; skipping bump."
    else
      sed -i.bak -E "s|const CACHE = '[^']+';|const CACHE = '${CACHE_NEW}';|" sw.js
      rm -f sw.js.bak
      echo "Bumped sw.js CACHE -> ${CACHE_NEW}"
    fi
  fi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Pre-flight smoke gate verification</name>
  <files></files>
  <read_first>
    - package.json (read in full — confirm `npm run smoke` chain includes all 8 contracts)
    - scripts/deploy.sh (read offset 80, limit 50 — confirm 8th smoke if-block from Plan 01 still present)
    - .planning/phases/24-native-video-player/24-01-SUMMARY.md (Plan 01 close-out)
    - .planning/phases/24-native-video-player/24-02-SUMMARY.md (Plan 02 close-out)
    - .planning/phases/24-native-video-player/24-03-SUMMARY.md (Plan 03 close-out)
  </read_first>
  <action>
    Run the full smoke chain to verify the deploy gate will pass. This is a dry-run gate — no file changes, just invoke the test infrastructure.

    Execute:
    1. `npm run smoke` — must exit 0 with all 8 contracts green (~170 total assertions across all phases)
    2. `node --check js/app.js` — must exit 0 (app.js parses)
    3. `node --check js/native-video-player.js` — must exit 0 (module parses)
    4. `bash -n scripts/deploy.sh` — must exit 0 (deploy script syntax)
    5. `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — must exit 0 (package.json JSON valid)

    If ANY of the above fails, STOP. Report the failure to the user (in chat) and DO NOT proceed to deploy. The user must triage by re-running the appropriate plan execution before this plan resumes.
  </action>
  <verify>
    <automated>npm run smoke && node --check js/app.js && node --check js/native-video-player.js && bash -n scripts/deploy.sh && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run smoke` exits 0
    - All 8 smoke contracts run (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player)
    - `node --check js/app.js` exits 0
    - `node --check js/native-video-player.js` exits 0
    - `bash -n scripts/deploy.sh` exits 0
    - JSON.parse on package.json exits 0
  </acceptance_criteria>
  <done>
    All pre-flight gates green. Ready to deploy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.2: Pre-deploy human-verify gate (smoke summary review)</name>
  <what-built>
    Plans 01 + 02 + 03 are complete. The smoke chain has run green (all 8 contracts pass). js/app.js + js/native-video-player.js + app.html + css/app.css are syntactically valid. Phase 24 is staged for production deploy via `bash scripts/deploy.sh 37-native-video-player`.

    Per Couch deploy autonomy policy (MEMORY.md feedback): Claude MAY proceed with `firebase deploy` to queuenight-84044 without per-deploy approval. This checkpoint is a SHORT smoke-summary review — not a hold-and-wait gate. If the user does not respond within a reasonable window, treat absence-of-objection as approval (consistent with the Couch deploy-autonomy memory).
  </what-built>
  <how-to-verify>
    Confirm the smoke summary above looks reasonable. If the user wants to inspect a file before deploy, surface the file path.

    Specifically: any concern about
    - The cache tag (`couch-v37-native-video-player`)?
    - Iframe src construction or playsinline gate?
    - Schedule-modal copy strings (locked from UI-SPEC)?
    - The host-only broadcast cadence (5s)?

    If any concern surfaces, address it before proceeding to Task 4.3.
  </how-to-verify>
  <resume-signal>Type "approved" or "deploy" or describe issues. Absence of objection within session = treated as approval per Couch deploy autonomy.</resume-signal>
</task>

<task type="auto">
  <name>Task 4.3: Deploy to couchtonight.app via deploy.sh (auto-bumps sw.js)</name>
  <files>sw.js</files>
  <read_first>
    - scripts/deploy.sh (read offset 1, limit 50 — confirm the deploy ritual: BUILD_DATE stamp + sw.js bump + Sentry DSN guard + mirror to queuenight/public/ + firebase deploy --only hosting)
    - sw.js (read offset 1, limit 15 — confirm CACHE line 8 currently reads `couch-v36.7-live-scoreboard`)
  </read_first>
  <action>
    Run the canonical Couch deploy ritual (single-repo couch-only — no queuenight changes per RESEARCH §Project Constraints + CONTEXT.md canonical_refs):

    Execute: `bash scripts/deploy.sh 37-native-video-player`

    The script will:
    1. Auto-stamp BUILD_DATE in app.html if needed
    2. Auto-bump sw.js CACHE: `couch-v36.7-live-scoreboard` -> `couch-v37-native-video-player` (via sed at deploy.sh:128-137)
    3. Sentry DSN guard
    4. Run §2.5 smoke gate (8 contracts; will exit 1 if any fail — Task 4.1 already verified green)
    5. Mirror modified files (`app.html`, `js/`, `css/`, `sw.js`, etc.) to `queuenight/public/`
    6. `firebase deploy --only hosting` against project queuenight-84044

    Expected output: deploy completes; final line shows hosting deploy URL.

    If the deploy fails:
    - Sentry DSN missing -> Sentry dashboard env var; surface to user
    - Firebase auth missing -> `firebase login` prompt; surface to user (auth gate)
    - Smoke contract fails mid-deploy -> deploy aborts; investigate the contract regression

    On success, capture:
    - The deploy URL printed by firebase
    - The final sw.js CACHE line (should now read `couch-v37-native-video-player`)
    - The BUILD_DATE stamped in app.html

    Do NOT manually edit sw.js — let deploy.sh own the bump. If deploy.sh fails before the bump but after partial mirror, the user must triage manually before retry.
  </action>
  <verify>
    <automated>grep "couch-v37-native-video-player" sw.js && curl -fsSL https://couchtonight.app/sw.js | grep -q "couch-v37-native-video-player" && echo "deploy verified live"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "couch-v37-native-video-player" sw.js` returns 1 match (local sw.js bumped)
    - `curl -fsSL https://couchtonight.app/sw.js | grep "couch-v37-native-video-player"` returns 1 match (production sw.js serves new CACHE)
    - No `firebase deploy` errors in stdout
    - BUILD_DATE in app.html updated to today's date — assert via `grep "BUILD_DATE.*2026-04-30" app.html` returns at least 1 match (substitute the actual deploy date if running on a later day; or run `node -e "const m=require('fs').readFileSync('app.html','utf8').match(/BUILD_DATE[^']*'([^']+)'/);if(!m||m[1]!==new Date().toISOString().slice(0,10))process.exit(1)"` for a date-relative assertion)
    - sw.js previous tag (`couch-v36.7-live-scoreboard`) is GONE from sw.js (`grep -c "couch-v36.7-live-scoreboard" sw.js` returns 0)
  </acceptance_criteria>
  <done>
    couchtonight.app is live on couch-v37-native-video-player. PWA users will revalidate on their next online activation. Phase 24 is shipped at code level.
  </done>
</task>

<task type="auto">
  <name>Task 4.4: Create 24-HUMAN-UAT.md scaffold (device-UAT tracker)</name>
  <files>.planning/phases/24-native-video-player/24-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/19-kid-mode/19-HUMAN-UAT.md (canonical HUMAN-UAT shape — most recent precedent)
    - .planning/phases/20-decision-explanation/20-HUMAN-UAT.md (alternate precedent — decision-explanation surface)
    - .planning/phases/15.5-wait-up-flex/15.5-HUMAN-UAT.md (Phase 15.5 — DVR + sport-mode UAT precedent, similar surface complexity)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Interaction States — what to verify visually + behaviorally)
    - .planning/phases/24-native-video-player/24-CONTEXT.md (`<specifics>` — silent UX on dead-end branches)
  </read_first>
  <action>
    Create the HUMAN-UAT tracker at `.planning/phases/24-native-video-player/24-HUMAN-UAT.md`. Mirrors the Phase 19 / 20 / 15.5 shape (frontmatter + UAT-script table + resume-signal protocol).

    Structure:

      ---
      phase: 24-native-video-player
      created: <today's date>
      cache_version: couch-v37-native-video-player
      deploy_ts: <hh:mmZ from Task 4.3 stdout>
      status: pending
      total_scripts: 9
      ---

      # Phase 24 — Native Video Player — HUMAN-UAT

      > Real-device UAT tracker. Resume signal "uat passed" -> trigger `/gsd-verify-work 24`.

      ## Test environment

      - Two iPhones (or one iPhone + one Android, but iPhone is the primary surface per CLAUDE.md)
      - One device runs in iOS Safari (in-tab); the other in PWA standalone (home-screen icon)
      - Both signed into the SAME Couch family
      - Pre-test cache flush: long-press the home-screen icon -> Remove App -> reinstall, OR Settings -> Safari -> Clear History

      ## UAT scripts

      | # | Script | Branch | Pre-condition | Steps | Expected | Status |
      |---|---|---|---|---|---|---|
      | 1 | Schedule wp with YouTube link (movie flow) | YouTube | Open Couch -> tap a movie title -> Schedule a watchparty | (a) tap "Schedule a watchparty" (b) set Date & time (c) paste a YouTube URL e.g. `https://www.youtube.com/watch?v=dQw4w9WgXcQ` (d) tap Send invites | wp persists with videoUrl + videoSource='youtube'; modal closes; wp appears in upcoming list | ⬜ pending |
      | 2 | Open watchparty live modal — YouTube branch loads inline | YouTube | Script 1's wp is at start time | (a) tap the wp in upcoming list (b) wait for modal to slide up | Player iframe renders as the FIRST CHILD of the modal (above .wp-live-header); .wp-live-poster is HIDDEN; YouTube poster thumbnail is visible inside the iframe | ⬜ pending |
      | 3 | YouTube plays inline on iOS (no fullscreen yank) | YouTube | Script 2 modal open | (a) tap the YouTube play button | Video plays INLINE in the modal — does NOT yank to native iOS fullscreen on tap | ⬜ pending |
      | 4 | Schedule wp with .mp4 link | MP4 | Open Couch -> any title not behind DRM | (a) Schedule a watchparty (b) paste a public .mp4 URL e.g. `https://download.samplelib.com/mp4/sample-5s.mp4` (c) tap Send invites | wp persists with videoUrl + videoSource='mp4' | ⬜ pending |
      | 5 | MP4 plays inline on iOS (no fullscreen yank) | MP4 | Script 4's wp open | (a) tap the <video> play button | Video plays inline. Native iOS controls visible. No fullscreen takeover | ⬜ pending |
      | 6 | DRM-hide branch — silent dead-end | DRM | Open Couch -> a Netflix-only title (no rent/buy provider) -> Schedule a wp with a YouTube URL pasted | Open the wp's live modal | Modal renders WITHOUT the player surface — looks byte-for-byte like today's coordination-only modal. No "we couldn't show the player" copy. Reactions panel + roster + footer all unchanged | ⬜ pending |
      | 7 | Invalid URL — submit-blocked inline error | Validation | Open schedule-modal | (a) paste `not-a-url-at-all` (b) tap Send invites | Input border turns red (.field-invalid); inline error reads "That link doesn't look like YouTube or an .mp4 file. Skip it or try another."; modal does NOT close | ⬜ pending |
      | 8 | Empty URL — silent skip | Validation | Open schedule-modal | (a) leave URL field empty (b) tap Send invites | Modal closes; wp persists with videoUrl=null + videoSource=null; opening the live modal shows today's coordination-only shape (no player surface) | ⬜ pending |
      | 9 | Two-device currentTime broadcast (host writes, non-host reads) | Broadcast | Two-phone test: phone A is host, phone B is participant. Both join the same wp from Script 1 or 4 | (a) phone A plays the video (b) phone B reloads after ~10s (c) check Firestore Console at families/{code}/watchparties/{wpId} for currentTimeMs + currentTimeUpdatedAt fields | currentTimeMs grows by ~5000 every 5s; currentTimeUpdatedAt is recent (within 5s of last play). Phase 26 will consume these fields without migration | ⬜ pending |

      ## Player error overlay (non-blocking polish UAT)

      - Pasting a 404 .mp4 URL should surface the inline italic-serif "Player couldn't load that link." + Try again link at the bottom of the player wrapper. Tapping Try again re-issues the load.

      ## Resume protocol

      - Mark each script ⬜ pending -> ✅ pass -> ❌ fail with notes
      - On full pass: reply `uat passed` -> Claude triggers `/gsd-verify-work 24` automatically
      - On any fail: reply with the script number + observation; Claude triages

      ## Notes

      - Per CONTEXT.md `<specifics>`: silent UX on DRM-hide branch is BY DESIGN, not a bug. Do NOT add explanatory copy.
      - YouTube live-stream URLs (e.g. `youtube.com/watch?v=<live-id>`): currentTime broadcast is GATED OFF at runtime per RESEARCH Open Question 1; broadcast loop skips writes when YT.Player.getDuration() returns 0 or Infinity. Acceptable; documented.
      - Plex / Jellyfin HTTP URLs will be browser-blocked (mixed content); player will fall through to error-overlay branch. Acceptable per RESEARCH Pitfall 5.

    Use today's date for the `created` field (Couch project lives in mid-2026; check STATE.md `last_updated` for canonical "today").
  </action>
  <verify>
    <automated>test -f .planning/phases/24-native-video-player/24-HUMAN-UAT.md && grep -c "⬜ pending" .planning/phases/24-native-video-player/24-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-native-video-player/24-HUMAN-UAT.md` exists
    - File contains 9 numbered scripts (one per row in the UAT table)
    - File contains the cache version `couch-v37-native-video-player` in frontmatter
    - File line count >= 60
    - `grep "uat passed" 24-HUMAN-UAT.md` returns 1 match (resume signal)
    - `grep "DRM-hide branch" 24-HUMAN-UAT.md` returns at least 1 match (silent dead-end script)
    - `grep "currentTimeMs\|currentTimeUpdatedAt" 24-HUMAN-UAT.md` returns at least 2 matches (Phase 26 contract verification)
  </acceptance_criteria>
  <done>
    HUMAN-UAT tracker scaffolded with 9 scripts; cache + deploy timestamp recorded; user knows to reply `uat passed` to trigger /gsd-verify-work 24; Phase 26 schema-contract anchor documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev -> Firebase Hosting | Deploy via firebase CLI; auth via firebase login (token in user's env). |
| Firebase Hosting -> end users | sw.js + app.html + js/* served via CDN; PWA cache invalidation triggered by CACHE constant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | sw.js CACHE bump skipped -> stale PWA installs miss new player surface | mitigate | deploy.sh auto-bumps sw.js via sed (lines 128-137); Task 4.3 verifies the bump locally + verifies via curl that production serves the new CACHE. |
| T-24-04-02 | Denial of Service | Smoke gate fails mid-deploy -> partial mirror state | mitigate | deploy.sh aborts on smoke failure (`exit 1`); deploy mirrors files BEFORE firebase deploy, so a smoke abort means no bad files on production. Task 4.1 pre-flights all gates before checkpoint. |
| T-24-04-03 | Spoofing | firebase deploy auth gate (token expired / not logged in) | accept | Auth-gate is a runtime checkpoint — Claude surfaces the auth prompt to the user; user runs `firebase login` and Claude retries. |
| T-24-04-04 | Information Disclosure | Sentry DSN exposed in client | accept | Public-by-design per CLAUDE.md; deploy.sh has a Sentry DSN guard but the DSN itself is in client source. |
| T-24-04-05 | Repudiation | Deploy without commit | mitigate | Plan 04 close-out commits all Phase 24 files; the deploy mirror to queuenight/public/ is also committed in queuenight repo per RUNBOOK §H ritual. |
</threat_model>

<verification>
- All Task 4.1 gates green
- Task 4.3 sw.js + curl verification confirms production deploy
- Task 4.4 HUMAN-UAT scaffold exists with 9 scripts
- couchtonight.app live curl returns `couch-v37-native-video-player`
</verification>

<success_criteria>
- sw.js bumped to `couch-v37-native-video-player` (verified locally + via production curl)
- couchtonight.app deploy completes without errors
- HUMAN-UAT.md tracker scaffolded for real-device verification
- User knows to reply `uat passed` -> trigger `/gsd-verify-work 24`
- Phase 26 hard-ordering contract documented in UAT script 9 (currentTimeMs + currentTimeUpdatedAt schema)
</success_criteria>

<output>
After completion, create `.planning/phases/24-native-video-player/24-04-SUMMARY.md` capturing:
- sw.js CACHE before/after
- Deploy timestamp + URL
- Smoke chain count + summary
- 24-HUMAN-UAT.md scripts shipped
- Sentry breadcrumb category to monitor: `videoBroadcast.write.failed` (post-deploy 7-day soak signal)
- Phase 26 schema-contract reaffirmation
- Any deviation from this plan
</output>
