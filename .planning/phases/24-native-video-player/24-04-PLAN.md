---
phase: 24-native-video-player
plan: 04
type: execute
wave: 3
depends_on:
  - 24-03
files_modified:
  - sw.js
  - tests/rules.test.js
  - firestore.rules
  - .planning/phases/24-native-video-player/24-HUMAN-UAT.md
# REVIEWS L3 clarification: autonomous=false here means "the checkpoint:human-verify
# task is honored (Task 4.2 surfaces a brief smoke-summary review before deploy)" —
# NOT "Claude has no autonomy at all". Per Couch deploy autonomy memory, Claude may
# proceed with `firebase deploy` without per-deploy approval; absence-of-objection
# at Task 4.2 is treated as approval (resume_signal in the checkpoint).
autonomous: false
requirements:
  - VID-24-13  # sw.js CACHE bump
  - VID-24-18  # Firestore rules verification + tightening (REVIEWS M2 — NEW)
tags:
  - phase-24
  - native-video-player
  - deploy
  - cache-bump
  - uat
  - rules-verification
  - reviews-revision
must_haves:
  truths:
    - "sw.js CACHE constant reads couch-v37-native-video-player after deploy"
    - "couchtonight.app live curl returns the same v37 cache string in service worker fetch"
    - "All 8 smoke contracts pass before deploy gate fires (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player)"
    - "Firestore rules-tests verify host can update wp.videoUrl/videoSource/currentTimeMs/currentTimeUpdatedAt/currentTimeSource/durationMs/isLiveStream AND non-host CANNOT spoof currentTimeMs (REVIEWS M2)"
    - "If rules-tests reveal that current rules permit non-host writes to currentTimeMs, firestore.rules is tightened with a host-only branch and the cross-repo deploy reminder is surfaced"
    - "24-HUMAN-UAT.md exists with iOS Safari + iOS PWA standalone test scripts for both branches"
  artifacts:
    - path: "sw.js"
      provides: "PWA cache invalidation marker"
      contains: "couch-v37-native-video-player"
    - path: "tests/rules.test.js"
      provides: "Phase 24 watchparty wp-record rules-tests (host can update video fields; non-host cannot spoof currentTimeMs)"
      contains: "videoUrl"
    - path: "firestore.rules"
      provides: "Phase 24 host-only branch for currentTime fields IF the rules-tests reveal current rules are too permissive (REVIEWS M2)"
      contains: "watchparties"
    - path: ".planning/phases/24-native-video-player/24-HUMAN-UAT.md"
      provides: "Human-side UAT scripts (iOS Safari in-tab + iOS PWA standalone for both YouTube and MP4 branches + DRM-hide silent-branch verification + currentTime broadcast verification + REVIEWS H1 player-survives-reactions verification + REVIEWS M1 late-join seek verification)"
      min_lines: 80
  key_links:
    - from: "scripts/deploy.sh ${TAG}"
      to: "sw.js CACHE constant"
      via: "auto-bump via sed at deploy.sh:128-137"
      pattern: "couch-v37-native-video-player"
    - from: "tests/rules.test.js"
      to: "firestore.rules"
      via: "@firebase/rules-unit-testing assertSucceeds / assertFails"
      pattern: "videoUrl|currentTimeMs"
    - from: "Production curl"
      to: "Service worker"
      via: "PWA registration revalidates on next online activation"
      pattern: "v37-native-video-player"
---

<objective>
Close out Phase 24: pre-flight smoke gates, **Firestore rules verification + tightening (REVIEWS M2 — NEW)**, bump sw.js CACHE, deploy to couchtonight.app, scaffold the HUMAN-UAT tracker.

Per CLAUDE.md: any user-visible app shell change requires a CACHE bump. Phase 24 introduces a NEW player surface inside the watchparty live modal -> user-visible -> bump REQUIRED.

Per RESEARCH §Project Constraints: recommended tag is `37-native-video-player` -> `couch-v37-native-video-player`. The deploy script auto-bumps via sed.

Per Phase 18 / 19 / 20 / 22 / 23 precedent: code-level acceptance + deferred device UAT in a HUMAN-UAT.md tracker.

**REVIEWS-revision additions (per 24-REVIEWS.md):**
- **M2 (NEW Task 4.2):** Add Firestore rules-tests in `tests/rules.test.js` for the new wp-record fields. Verify (a) host can update videoUrl/videoSource/currentTimeMs/currentTimeUpdatedAt/currentTimeSource/durationMs/isLiveStream; (b) non-host CANNOT spoof currentTimeMs. Couch's existing rules at `firestore.rules:561-565` allow `attributedWrite(familyCode)` for any family member — that means non-hosts CAN write currentTimeMs at the rules layer today. The client gate (`state.me.id === wp.hostId` in Plan 03 Task 3.4) is the only barrier. If the rules-test confirms this gap, **tighten firestore.rules** with a host-only branch for the currentTime* fields. Cross-repo: `firestore.rules` is also mirrored in queuenight; the deploy ritual handles it.
- **L3 (frontmatter comment):** Clarified autonomous=false meaning above.
- **HUMAN-UAT additions:** Two new scripts for REVIEWS H1 (player-survives-reactions) + REVIEWS M1 (late-join seek).

**autonomous=false interpretation (REVIEWS L3):** Task 4.3 is a `checkpoint:human-verify` (smoke-summary review). Per Couch deploy autonomy memory, absence-of-objection within session is treated as approval. The flag means "checkpoint task exists" not "no autonomy".

Output: sw.js bumped (auto via deploy.sh) + tests/rules.test.js extended + firestore.rules tightened (if needed) + production deploy + HUMAN-UAT.md scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-native-video-player/24-CONTEXT.md
@.planning/phases/24-native-video-player/24-RESEARCH.md
@.planning/phases/24-native-video-player/24-PATTERNS.md
@.planning/phases/24-native-video-player/24-UI-SPEC.md
@.planning/phases/24-native-video-player/24-REVIEWS.md
@.planning/phases/24-native-video-player/24-03-SUMMARY.md
@CLAUDE.md

<interfaces>
sw.js CACHE constant (current line 8 — verbatim):

  const CACHE = 'couch-v36.7-live-scoreboard';

Will become:

  const CACHE = 'couch-v37-native-video-player';

deploy.sh auto-bump pattern at scripts/deploy.sh:128-137 (verbatim):

  if [ -n "$TAG" ]; then
    CACHE_NEW="couch-v${TAG}"
    if grep -q "const CACHE = '${CACHE_NEW}';" sw.js; then
      echo "sw.js CACHE already at ${CACHE_NEW}; skipping bump."
    else
      sed -i.bak -E "s|const CACHE = '[^']+';|const CACHE = '${CACHE_NEW}';|" sw.js
      rm -f sw.js.bak
      echo "Bumped sw.js CACHE -> ${CACHE_NEW}"
    fi
  fi

Existing firestore.rules watchparties block (verbatim, lines ~561-565):

  match /watchparties/{wpId} {
    allow read: if isMemberOfFamily(familyCode);
    allow create, update: if attributedWrite(familyCode);
    allow delete: if isOwner(familyCode) || (isMemberOfFamily(familyCode) && graceActive());
  }

This rule allows ANY family member to update wp fields. REVIEWS M2 flagged that non-hosts can technically spoof currentTimeMs at the rules layer. The Plan 04 M2 task will verify via emulator-tests then tighten.

Existing tests/rules.test.js harness (verbatim, lines 1-50):

  const { initializeTestEnvironment, assertSucceeds, assertFails } = require('@firebase/rules-unit-testing');
  ...
  const owner = testEnv.authenticatedContext(UID_OWNER).firestore();
  const member = testEnv.authenticatedContext(UID_MEMBER).firestore();
  ...
  await it('test name', async () => { await assertSucceeds(...); });

Test harness signals: `cd tests && npm install && npm test` runs the suite via `firebase emulators:exec`. Existing seed at tests/rules.test.js:50-99 covers `families/fam1` with owner + member. Phase 24 tests need to seed a watchparty with a hostId field too.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Pre-flight smoke gate verification</name>
  <files></files>
  <read_first>
    - package.json (read in full — confirm `npm run smoke` chain includes all 8 contracts)
    - scripts/deploy.sh (read offset 80, limit 50 — confirm 8th smoke if-block from Plan 01 still present)
    - .planning/phases/24-native-video-player/24-01-SUMMARY.md (Plan 01 close-out)
    - .planning/phases/24-native-video-player/24-02-SUMMARY.md (Plan 02 close-out)
    - .planning/phases/24-native-video-player/24-03-SUMMARY.md (Plan 03 close-out)
  </read_first>
  <action>
    Run the full smoke chain to verify the deploy gate will pass. Dry-run gate — no file changes.

    Execute:
    1. `npm run smoke` — must exit 0 with all 8 contracts green
    2. `node --check js/app.js` — must exit 0
    3. `node --check js/native-video-player.js` — must exit 0
    4. `bash -n scripts/deploy.sh` — must exit 0
    5. `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — must exit 0

    If ANY of the above fails, STOP. Report the failure to the user and DO NOT proceed.
  </action>
  <verify>
    <automated>npm run smoke && node --check js/app.js && node --check js/native-video-player.js && bash -n scripts/deploy.sh && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run smoke` exits 0
    - All 8 smoke contracts run + pass
    - `node --check js/app.js` exits 0
    - `node --check js/native-video-player.js` exits 0
    - `bash -n scripts/deploy.sh` exits 0
    - JSON.parse on package.json exits 0
  </acceptance_criteria>
  <done>
    All pre-flight gates green. Ready for rules verification.
  </done>
</task>

<task type="auto">
  <name>Task 4.2: Firestore rules verification + tightening (REVIEWS M2 — NEW)</name>
  <files>tests/rules.test.js, firestore.rules</files>
  <read_first>
    - tests/rules.test.js (read full — see existing seed at lines 50-99; assertion harness; describe/it structure)
    - tests/package.json (small file — verify test command shape)
    - firestore.rules (read full — pay attention to `match /watchparties/{wpId}` block ~lines 561-565 + the `attributedWrite` helper definition)
    - .planning/phases/24-native-video-player/24-REVIEWS.md (M2 — full fix description)
  </read_first>
  <action>
    Two-phase task: verify, then tighten if needed.

    **A. Extend `tests/rules.test.js` with Phase 24 wp coverage.**

    Step A.1 — extend the existing seed function (around `tests/rules.test.js:50-99`) to also seed a watchparty doc with the host being UID_OWNER:

    Find the existing seed body. After the existing `vetoHistory` seed call, ADD:

    ```javascript
    // Phase 24 — seed a watchparty doc owned by UID_OWNER (host).
    // Used by the Phase 24 wp rules-tests to assert host-can / non-host-cannot.
    await db.doc('families/fam1/watchparties/wp_phase24_test').set({
      id: 'wp_phase24_test',
      titleId: 't_1',
      titleName: 'Test Title',
      titlePoster: '',
      hostId: 'm_UID_OWNER',
      hostName: 'Owner',
      hostUid: UID_OWNER,
      startAt: now,
      createdAt: now,
      status: 'active',
      participants: { 'm_UID_OWNER': { name: 'Owner', joinedAt: now, rsvpStatus: 'in' } },
      reactions: [],
      videoUrl: null,
      videoSource: null
    });
    ```

    Step A.2 — add a new `describe(...)` block at the end of `run()`, after the existing describe blocks but BEFORE `await testEnv.cleanup()` (or wherever the existing test suite ends — read tests/rules.test.js to identify the exact insertion spot):

    ```javascript
    await describe('Phase 24 — watchparty video fields (REVIEWS M2)', async () => {
      const wpRef = (db) => db.doc('families/fam1/watchparties/wp_phase24_test');
      const att = (uid, mid) => ({ actingUid: uid, memberId: mid, memberName: 'name', actingAt: Date.now() });

      await it('#wp1 host updates wp.videoUrl + wp.videoSource → ALLOWED', async () => {
        await assertSucceeds(
          wpRef(owner).update({
            videoUrl: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ',
            videoSource: 'youtube',
            ...att(UID_OWNER, 'm_UID_OWNER')
          })
        );
      });

      await it('#wp2 host updates wp.currentTimeMs + wp.currentTimeUpdatedAt → ALLOWED', async () => {
        await assertSucceeds(
          wpRef(owner).update({
            currentTimeMs: 12345,
            currentTimeUpdatedAt: Date.now(),
            currentTimeSource: 'youtube',
            durationMs: 240000,
            isLiveStream: false,
            ...att(UID_OWNER, 'm_UID_OWNER')
          })
        );
      });

      await it('#wp3 non-host member updates wp.currentTimeMs → REJECTED (REVIEWS M2)', async () => {
        // This is the threat — non-host trying to spoof position.
        // If current rules are TOO PERMISSIVE (allow update: attributedWrite(familyCode))
        // this assertion will FAIL (write succeeds when it should be rejected).
        // That signal triggers Step B — tighten firestore.rules.
        await assertFails(
          wpRef(member).update({
            currentTimeMs: 999999,
            currentTimeUpdatedAt: Date.now(),
            ...att(UID_MEMBER, 'm_UID_MEMBER')
          })
        );
      });

      await it('#wp4 non-host member updates wp.reactions → ALLOWED (member can react; not a host-only field)', async () => {
        // Confirms the tightening doesn't over-restrict. Reactions remain a member-write.
        await assertSucceeds(
          wpRef(member).update({
            reactions: [{ emoji: '🎉', actingUid: UID_MEMBER, memberId: 'm_UID_MEMBER', actingAt: Date.now() }],
            ...att(UID_MEMBER, 'm_UID_MEMBER')
          })
        );
      });

      await it('#wp5 stranger (not in family) updates wp.videoUrl → REJECTED', async () => {
        await assertFails(
          wpRef(stranger).update({
            videoUrl: 'https://malicious.example.com/video.mp4',
            videoSource: 'mp4',
            ...att(UID_STRANGER, 'm_UID_STRANGER')
          })
        );
      });
    });
    ```

    Step A.3 — Run the rules-tests:

    ```bash
    cd tests && npm install && npm test
    ```

    (If `npm install` was already run in tests/, just `npm test` works. The `pretest` script copies `../firestore.rules` into `tests/firestore.rules` — make sure the local copy is the latest from the project root.)

    **B. Inspect the rules-test results.**

    Two outcomes possible:

    **Outcome 1 — All tests pass green** (especially #wp3 — non-host rejected): The current `attributedWrite(familyCode)` rule is tighter than expected somehow (perhaps via a helper I missed) and the documentation in the REVIEWS.md was overly cautious. **Document this in the SUMMARY** ("Rules already host-restrict per existing helpers") and proceed to Task 4.3 — no `firestore.rules` changes needed.

    **Outcome 2 — Test #wp3 FAILS** (non-host write succeeds where assertFails expected rejection): This is the predicted state per REVIEWS M2 + the planner's read of firestore.rules:561-565. **Tighten `firestore.rules`.**

    Tighten by adding a stricter conditional to the watchparties update rule. Replace the existing block:

    ```
    match /watchparties/{wpId} {
      allow read: if isMemberOfFamily(familyCode);
      allow create, update: if attributedWrite(familyCode);
      allow delete: if isOwner(familyCode) || (isMemberOfFamily(familyCode) && graceActive());
    }
    ```

    With a host-only branch for the currentTime* fields:

    ```
    match /watchparties/{wpId} {
      allow read: if isMemberOfFamily(familyCode);
      allow create: if attributedWrite(familyCode);
      // Phase 24 / REVIEWS M2 — Host-only fields can only be updated by the
      // wp's hostUid. All other fields (reactions, participants, etc.) remain
      // writable by any family member with attribution.
      allow update: if attributedWrite(familyCode) && (
        // Path A: Member is the host (any field allowed for the host)
        (resource.data.hostUid == request.auth.uid)
        ||
        // Path B: Non-host write — must NOT touch host-only fields
        (
          resource.data.hostUid != request.auth.uid
          && !request.resource.data.diff(resource.data).affectedKeys().hasAny([
            'currentTimeMs',
            'currentTimeUpdatedAt',
            'currentTimeSource',
            'durationMs',
            'isLiveStream',
            'videoUrl',
            'videoSource',
            'hostId',
            'hostUid',
            'hostName'
          ])
        )
      );
      allow delete: if isOwner(familyCode) || (isMemberOfFamily(familyCode) && graceActive());
    }
    ```

    NOTE on hostUid: The seed sets `hostUid: UID_OWNER`. Confirm via grep that production wp records also stamp `hostUid` — Plan 03 Task 3.2 referenced `hostUid: (state.auth && state.auth.uid) || null` in the existing wp-record build at js/app.js:10861-10887. If hostUid is NOT consistently set on legacy wps, the rule may break legacy wps. **In that case, fall back to comparing `request.auth.uid` against a stamp the wp creator does write reliably** — but this needs caution. If hostUid is unreliable, the safer approach is:

    **Outcome 2-fallback — hostUid not reliably set on all wps:** Surface this finding in the SUMMARY and add a note in 24-HUMAN-UAT.md to capture the rules-test results. Do NOT ship a rules change that could break legacy wps. The client gate `state.me.id === wp.hostId` in Plan 03 remains the primary defense; rules-tightening is a defense-in-depth layer that needs a clean migration window. Open a follow-up Phase 24.1 if needed.

    Re-run the rules-tests after the tightening:

    ```bash
    cd tests && npm test
    ```

    All 5 new tests should now pass.

    **C. If firestore.rules was modified, surface the cross-repo deploy reminder.**

    Couch's `firestore.rules` is also mirrored in the sibling `queuenight/` repo. The deploy ritual (Task 4.4) covers the couch hosting deploy but does NOT auto-deploy Firestore rules. Document in the SUMMARY:

    > NOTE: `firestore.rules` was modified in this phase. Production rules deploy is a SEPARATE ritual: from the queuenight/ repo, run `firebase deploy --only firestore:rules` after copying the updated rules file. Surface this to user in the Plan 04 close-out.

    **Do NOT auto-deploy Firestore rules in this plan.** Rules deploy is cross-repo and needs explicit user confirmation per CLAUDE.md project rules.
  </action>
  <verify>
    <automated>cd tests && npm test 2>&1 | tail -50 && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rules.test.js` contains 5 new test cases under "Phase 24 — watchparty video fields"
    - `grep "wp_phase24_test" tests/rules.test.js` returns at least 2 matches (seed + tests reference the same wp doc)
    - `grep "videoUrl\|currentTimeMs" tests/rules.test.js` returns at least 5 matches
    - `cd tests && npm test` exits 0 with all assertions green (Outcome 1: rules already strict OR Outcome 2: rules were tightened in this task)
    - If firestore.rules was modified: `grep "REVIEWS M2" firestore.rules` returns at least 1 match
    - If firestore.rules was modified: `grep "currentTimeMs" firestore.rules` returns at least 1 match (host-only branch references the field)
    - SUMMARY documents which outcome occurred (rules already strict / rules tightened / hostUid-unreliable fallback)
  </acceptance_criteria>
  <done>
    Phase 24 wp rules-tests live in tests/rules.test.js; all 5 assertions green. If rules needed tightening: firestore.rules updated with host-only branch + cross-repo deploy reminder logged in SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.3: Pre-deploy human-verify gate (smoke + rules-test summary review)</name>
  <what-built>
    Plans 01 + 02 + 03 are complete. The smoke chain has run green (all 8 contracts pass, including production-module import per REVIEWS H2 and production-code sentinels for H3/H4/H5/M1/M3/M4/C1/C2). js/app.js + js/native-video-player.js + app.html + css/app.css are syntactically valid. tests/rules.test.js was extended (REVIEWS M2) and the rules-test suite is green (rules already strict OR tightened in this plan). Phase 24 is staged for production deploy via `bash scripts/deploy.sh 37-native-video-player`.

    Per Couch deploy autonomy policy (MEMORY.md feedback): Claude MAY proceed with `firebase deploy` to queuenight-84044 without per-deploy approval. **REVIEWS L3 clarification:** the autonomous=false flag on this plan means "this checkpoint is honored", not "no autonomy". This checkpoint is a SHORT smoke + rules-test summary review — not a hold-and-wait gate. If the user does not respond within a reasonable window, treat absence-of-objection as approval (consistent with the Couch deploy-autonomy memory).
  </what-built>
  <how-to-verify>
    Confirm the smoke + rules-test summary above looks reasonable. If the user wants to inspect a file before deploy, surface the file path.

    Specifically: any concern about
    - The cache tag (`couch-v37-native-video-player`)?
    - YouTube div-placeholder binding (REVIEWS H3)?
    - Schedule-modal copy strings (locked from UI-SPEC)?
    - The host-only broadcast cadence (5s) + extended schema (REVIEWS C2)?
    - The DOM scaffold split (REVIEWS H1) — confirm UI-SPEC.md was inline-revised?
    - Whether firestore.rules was modified — and if so, the cross-repo deploy reminder?

    If any concern surfaces, address it before proceeding to Task 4.4.
  </how-to-verify>
  <resume-signal>Type "approved" or "deploy" or describe issues. Absence of objection within session = treated as approval per Couch deploy autonomy.</resume-signal>
</task>

<task type="auto">
  <name>Task 4.4: Deploy to couchtonight.app via deploy.sh (auto-bumps sw.js)</name>
  <files>sw.js</files>
  <read_first>
    - scripts/deploy.sh (read offset 1, limit 50 — confirm the deploy ritual)
    - sw.js (read offset 1, limit 15 — confirm CACHE line 8 currently reads `couch-v36.7-live-scoreboard`)
  </read_first>
  <action>
    Run the canonical Couch deploy ritual (single-repo couch-only — no queuenight changes per RESEARCH §Project Constraints + CONTEXT.md canonical_refs):

    Execute: `bash scripts/deploy.sh 37-native-video-player`

    The script will:
    1. Auto-stamp BUILD_DATE in app.html if needed
    2. Auto-bump sw.js CACHE: `couch-v36.7-live-scoreboard` -> `couch-v37-native-video-player` (via sed at deploy.sh:128-137)
    3. Sentry DSN guard
    4. Run §2.5 smoke gate (8 contracts; will exit 1 if any fail — Task 4.1 already verified green)
    5. Mirror modified files (`app.html`, `js/`, `css/`, `sw.js`, etc.) to `queuenight/public/`
    6. `firebase deploy --only hosting` against project queuenight-84044

    Expected output: deploy completes; final line shows hosting deploy URL.

    **CROSS-REPO RULES DEPLOY (REVIEWS M2):** If Task 4.2 modified `firestore.rules`, the deploy.sh ritual does NOT auto-deploy rules. After this task, surface to the user:

    > Firestore rules were updated in this phase. To deploy them: `cd ../queuenight && cp ../couch/firestore.rules . && firebase deploy --only firestore:rules`. This is intentionally NOT automated to keep rules deploys explicit.

    If the deploy fails:
    - Sentry DSN missing -> Sentry dashboard env var; surface to user
    - Firebase auth missing -> `firebase login` prompt; surface to user (auth gate)
    - Smoke contract fails mid-deploy -> deploy aborts; investigate the contract regression

    On success, capture:
    - The deploy URL printed by firebase
    - The final sw.js CACHE line (should now read `couch-v37-native-video-player`)
    - The BUILD_DATE stamped in app.html

    Do NOT manually edit sw.js — let deploy.sh own the bump.
  </action>
  <verify>
    <automated>grep "couch-v37-native-video-player" sw.js && curl -fsSL https://couchtonight.app/sw.js | grep -q "couch-v37-native-video-player" && echo "deploy verified live"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "couch-v37-native-video-player" sw.js` returns 1 match (local sw.js bumped)
    - `curl -fsSL https://couchtonight.app/sw.js | grep "couch-v37-native-video-player"` returns 1 match (production sw.js serves new CACHE)
    - No `firebase deploy` errors in stdout
    - BUILD_DATE in app.html updated to today's date
    - sw.js previous tag (`couch-v36.7-live-scoreboard`) is GONE from sw.js (`grep -c "couch-v36.7-live-scoreboard" sw.js` returns 0)
    - If firestore.rules was modified in Task 4.2: SUMMARY surfaces the cross-repo rules-deploy reminder
  </acceptance_criteria>
  <done>
    couchtonight.app is live on couch-v37-native-video-player. PWA users will revalidate on their next online activation. Phase 24 is shipped at code level. (Rules deploy is a separate, explicit cross-repo step if needed.)
  </done>
</task>

<task type="auto">
  <name>Task 4.5: Create 24-HUMAN-UAT.md scaffold (device-UAT tracker, including REVIEWS H1 + M1 verification)</name>
  <files>.planning/phases/24-native-video-player/24-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/19-kid-mode/19-HUMAN-UAT.md (canonical HUMAN-UAT shape)
    - .planning/phases/20-decision-explanation/20-HUMAN-UAT.md (alternate precedent)
    - .planning/phases/15.5-wait-up-flex/15.5-HUMAN-UAT.md (Phase 15.5 — DVR + sport-mode UAT precedent)
    - .planning/phases/24-native-video-player/24-UI-SPEC.md (Interaction States — what to verify visually + behaviorally)
    - .planning/phases/24-native-video-player/24-CONTEXT.md (`<specifics>` — silent UX on dead-end branches)
    - .planning/phases/24-native-video-player/24-REVIEWS.md (H1 + M1 — adds new UAT scripts)
  </read_first>
  <action>
    Create the HUMAN-UAT tracker at `.planning/phases/24-native-video-player/24-HUMAN-UAT.md`. Mirrors the Phase 19 / 20 / 15.5 shape (frontmatter + UAT-script table + resume-signal protocol).

    Structure:

    ```
      ---
      phase: 24-native-video-player
      created: <today's date>
      cache_version: couch-v37-native-video-player
      deploy_ts: <hh:mmZ from Task 4.4 stdout>
      status: pending
      total_scripts: 11
      reviews_addressed: H1, H2, H3, H4, H5, M1, M2, M3, M4, C1, C2 (L1 mitigated by H1; L2 noted; L3 frontmatter clarified)
      ---

      # Phase 24 — Native Video Player — HUMAN-UAT

      > Real-device UAT tracker. Resume signal "uat passed" -> trigger `/gsd-verify-work 24`.

      ## Test environment

      - Two iPhones (or one iPhone + one Android, but iPhone is the primary surface per CLAUDE.md)
      - One device runs in iOS Safari (in-tab); the other in PWA standalone (home-screen icon)
      - Both signed into the SAME Couch family
      - Pre-test cache flush: long-press the home-screen icon -> Remove App -> reinstall, OR Settings -> Safari -> Clear History

      ## UAT scripts

      | # | Script | Branch | Pre-condition | Steps | Expected | Status |
      |---|---|---|---|---|---|---|
      | 1 | Schedule wp with YouTube link (movie flow) | YouTube | Open Couch -> tap a movie title -> Start a watchparty | (a) tap "Start a watchparty" (b) set timing (c) paste a YouTube URL e.g. `https://www.youtube.com/watch?v=dQw4w9WgXcQ` into "Video URL (optional)" — note the field id is `wp-video-url-movie` (REVIEWS M3) (d) tap Send invites | wp persists with videoUrl + videoSource='youtube'; modal closes; wp appears in upcoming list | ⬜ pending |
      | 2 | Open watchparty live modal — YouTube branch loads inline | YouTube | Script 1's wp is at start time | (a) tap the wp in upcoming list (b) wait for modal to slide up | Player div renders inside `#wp-video-surface` (persistent surface — REVIEWS H1); YouTube IFrame API replaces the div with its own iframe (REVIEWS H3 binding pattern); .wp-live-poster is HIDDEN; YouTube poster thumbnail visible inside the iframe | ⬜ pending |
      | 3 | YouTube plays inline on iOS (no fullscreen yank) | YouTube | Script 2 modal open | (a) tap the YouTube play button | Video plays INLINE in the modal — does NOT yank to native iOS fullscreen on tap | ⬜ pending |
      | 4 | **REVIEWS H1 verification** — player survives reactions | YouTube | Script 3 video playing | (a) while video is playing, tap reaction emojis 5+ times in a row (b) wait through a timer tick (every 1s the live-modal repaints the timer) (c) observe the player | Player KEEPS PLAYING through every reaction post and timer tick. Video does NOT reset to 0:00. Audio uninterrupted. (Pre-fix: this was catastrophic — the player would reset on every reaction.) | ⬜ pending |
      | 5 | Schedule wp with .mp4 link (game-picker flow — REVIEWS M3) | MP4 | Open Couch -> Game Mode picker -> pick a league | (a) "Tonight's game" picker open (b) paste a public .mp4 URL e.g. `https://download.samplelib.com/mp4/sample-5s.mp4` into "Video URL (optional)" — note the field id is `wp-video-url-game` (REVIEWS M3) (c) tap "Use this game" | wp persists with videoUrl + videoSource='mp4' on the game wp record | ⬜ pending |
      | 6 | MP4 plays inline on iOS (no fullscreen yank) | MP4 | Script 5's wp open | (a) tap the <video> play button | Video plays inline. Native iOS controls visible. No fullscreen takeover | ⬜ pending |
      | 7 | **REVIEWS M1 verification** — late-join seek for non-host | YouTube or MP4 | Two-phone test: phone A is host playing for ~30s (player at ~30s position); phone B is a non-host who has NOT yet joined the live modal | (a) Phone A starts playing the video; let it run 30s. (b) Phone B taps the wp in upcoming list to open the live modal | Phone B's player starts at ~30s position, NOT 0:00. Within ~5s of phone A's last broadcast. Confirms seekToBroadcastedTime helper fires for non-hosts on player init. | ⬜ pending |
      | 8 | DRM-hide branch — silent dead-end | DRM | Open Couch -> a Netflix-only title (no rent/buy provider) -> Start a wp with a YouTube URL pasted | Open the wp's live modal | Modal renders WITHOUT the player surface — looks byte-for-byte like today's coordination-only modal. No "we couldn't show the player" copy. Reactions panel + roster + footer all unchanged. `#wp-video-surface` is empty (CSS rule `.wp-video-surface:empty{display:none}` collapses it). | ⬜ pending |
      | 9 | Invalid URL — submit-blocked inline error | Validation | Open wp-start modal | (a) paste `not-a-url-at-all` (b) tap Send invites | Input border turns red (.field-invalid); inline error reads "That link doesn't look like YouTube or an .mp4 file. Skip it or try another."; modal does NOT close | ⬜ pending |
      | 10 | **REVIEWS C1 verification** — HTTP MP4 mixed-content warning | MP4 | Open wp-start modal | (a) paste `http://192.168.1.20:32400/library/parts/123/file.mp4` (typical Plex LAN URL) (b) tap Send invites | Modal closes (NOT blocked — http:// is valid syntax) AND a non-blocking warning toast appears: "Note: HTTP links may be blocked by the browser. Use https if available." | ⬜ pending |
      | 11 | Two-device currentTime broadcast (REVIEWS C2 schema) | Broadcast | Two-phone test: phone A is host, phone B is participant. Both join the same wp from Script 1 or 5 | (a) phone A plays the video (b) phone B reloads after ~10s (c) check Firestore Console at families/{code}/watchparties/{wpId} for the extended schema | wp record contains: currentTimeMs (number, ms), currentTimeUpdatedAt (number, ms-since-epoch), currentTimeSource ('youtube' or 'mp4'), durationMs (number or null), isLiveStream (boolean). Phase 26 will consume these fields without migration. | ⬜ pending |

      ## Player error overlay (non-blocking polish UAT — REVIEWS H4)

      - Pasting a 404 .mp4 URL should surface the inline italic-serif "Player couldn't load that link." + Try again link. **Tap the Try again link** — it should re-issue the load (REVIEWS H4 fix wires the previously-decorative link). Pre-fix: tap did nothing.

      ## Resume protocol

      - Mark each script ⬜ pending -> ✅ pass -> ❌ fail with notes
      - On full pass: reply `uat passed` -> Claude triggers `/gsd-verify-work 24` automatically
      - On any fail: reply with the script number + observation; Claude triages

      ## Notes

      - Per CONTEXT.md `<specifics>`: silent UX on DRM-hide branch is BY DESIGN, not a bug. Do NOT add explanatory copy.
      - YouTube live-stream URLs (e.g. `youtube.com/watch?v=<live-id>`): currentTime broadcast is GATED OFF per-sample at runtime (REVIEWS M4 — isFinite + getDuration > 0 check inside the broadcaster callback). Acceptable; documented.
      - Plex / Jellyfin HTTP URLs surface a non-blocking submit-time warning (REVIEWS C1). User can still proceed — some setups work.
      - **REVIEWS L1 (host sync loop):** Mitigated by REVIEWS H1 — the persistent #wp-video-surface is immune to coordination re-renders, including those triggered by host's own Firestore snapshot.
      - **REVIEWS L2 (YT global overwrite):** Noted as deferred risk. Couch has no other YT API consumer in 2026-04. If a future phase adds one, switch to chained-handler pattern.
      - **REVIEWS M2 (rules verification):** Plan 04 Task 4.2 added rules-tests covering host-can / non-host-cannot for currentTime fields. If firestore.rules was tightened, deploy rules cross-repo via queuenight/.
    ```

    Use today's date for the `created` field (Couch project lives in mid-2026; check STATE.md `last_updated` for canonical "today").
  </action>
  <verify>
    <automated>test -f .planning/phases/24-native-video-player/24-HUMAN-UAT.md && grep -c "⬜ pending" .planning/phases/24-native-video-player/24-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-native-video-player/24-HUMAN-UAT.md` exists
    - File contains 11 numbered scripts (one per row in the UAT table)
    - File contains the cache version `couch-v37-native-video-player` in frontmatter
    - File line count >= 80
    - `grep "uat passed" 24-HUMAN-UAT.md` returns 1 match (resume signal)
    - `grep "DRM-hide branch" 24-HUMAN-UAT.md` returns at least 1 match (silent dead-end script)
    - `grep "currentTimeMs\|currentTimeUpdatedAt\|currentTimeSource\|durationMs\|isLiveStream" 24-HUMAN-UAT.md` returns at least 5 matches (REVIEWS C2 verification)
    - `grep "REVIEWS H1\|REVIEWS M1\|REVIEWS H3\|REVIEWS H4\|REVIEWS C1\|REVIEWS M3" 24-HUMAN-UAT.md` returns at least 6 matches (one per addressed review finding mentioned in scripts)
    - `grep "wp-video-url-movie\|wp-video-url-game\|wp-video-url-sport" 24-HUMAN-UAT.md` returns at least 3 matches (M3 distinct IDs documented)
    - `grep "player survives reactions\|persistent surface\|wp-video-surface" 24-HUMAN-UAT.md` returns at least 1 match (H1 verification script)
  </acceptance_criteria>
  <done>
    HUMAN-UAT tracker scaffolded with 11 scripts; cache + deploy timestamp recorded; user knows to reply `uat passed` to trigger /gsd-verify-work 24; REVIEWS H1 + M1 + C1 + C2 verification scripts present; M3 distinct-ID flow paths documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev -> Firebase Hosting | Deploy via firebase CLI; auth via firebase login. |
| Firebase Hosting -> end users | sw.js + app.html + js/* served via CDN; PWA cache invalidation triggered by CACHE constant. |
| Firestore emulator -> rules-tests | Rules-test harness validates the rules at the security boundary. |
| Production firestore.rules | If modified by Task 4.2, requires cross-repo deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | sw.js CACHE bump skipped -> stale PWA installs miss new player surface | mitigate | deploy.sh auto-bumps via sed; Task 4.4 verifies via curl. |
| T-24-04-02 | Denial of Service | Smoke gate fails mid-deploy -> partial mirror state | mitigate | deploy.sh aborts on smoke failure; Task 4.1 pre-flights all gates before checkpoint. |
| T-24-04-03 | Spoofing | firebase deploy auth gate (token expired / not logged in) | accept | Auth-gate is a runtime checkpoint — Claude surfaces the auth prompt. |
| T-24-04-04 | Information Disclosure | Sentry DSN exposed in client | accept | Public-by-design per CLAUDE.md. |
| T-24-04-05 | Repudiation | Deploy without commit | mitigate | Plan 04 close-out commits all Phase 24 files. |
| T-24-04-06 | Repudiation | Non-host spoofs currentTimeMs (REVIEWS M2) | mitigate | Task 4.2 rules-tests verify the security boundary; if too permissive, firestore.rules tightened with host-only branch. Cross-repo deploy reminder surfaced. |
| T-24-04-07 | Spoofing | Rules-deploy skipped after firestore.rules modification | mitigate | Cross-repo deploy is intentionally NOT automated; Plan 04 Task 4.4 surfaces the reminder explicitly to user. Client gate (state.me.id === wp.hostId) remains the primary defense even if rules deploy is delayed. |
</threat_model>

<verification>
- All Task 4.1 gates green
- Task 4.2 rules-tests all 5 assertions green (rules already strict OR tightened)
- Task 4.4 sw.js + curl verification confirms production deploy
- Task 4.5 HUMAN-UAT scaffold exists with 11 scripts
- couchtonight.app live curl returns `couch-v37-native-video-player`
</verification>

<success_criteria>
- sw.js bumped to `couch-v37-native-video-player` (verified locally + via production curl)
- couchtonight.app deploy completes without errors
- Firestore rules-tests prove host-only currentTime writes (REVIEWS M2 closure)
- HUMAN-UAT.md tracker scaffolded for real-device verification
- User knows to reply `uat passed` -> trigger `/gsd-verify-work 24`
- Phase 26 hard-ordering contract documented in UAT script 11 (extended schema — REVIEWS C2 closure)
</success_criteria>

<output>
After completion, create `.planning/phases/24-native-video-player/24-04-SUMMARY.md` capturing:
- sw.js CACHE before/after
- Deploy timestamp + URL
- Smoke chain count + summary
- **REVIEWS M2 closure log:** rules-test outcome (already strict / tightened / hostUid-fallback) + cross-repo deploy reminder if applicable
- 24-HUMAN-UAT.md scripts shipped (now 11 — added H1 + M1 + C1 verification)
- Sentry breadcrumb category to monitor: `videoBroadcast.write.failed` (post-deploy 7-day soak signal)
- Phase 26 schema-contract reaffirmation (extended schema per REVIEWS C2)
- REVIEWS L3 closure: autonomous=false flag re-interpreted as "checkpoint exists" per memory
- Any deviation from this plan
</output>
</content>
