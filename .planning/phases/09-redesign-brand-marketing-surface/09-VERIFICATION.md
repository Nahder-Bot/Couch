---
status: passed_partial
phase: 09-redesign-brand-marketing-surface
verified_at: 2026-04-27T17:19:43Z
verifier_method: retroactive_backfill_phase_15.2
score: 9/10 DESIGN-* requirements satisfied (DESIGN-01 unsatisfied — closed by Phase 15.3)
overrides_applied: 0
unsatisfied_requirements:
  - id: DESIGN-01
    status: unsatisfied
    closed_by: Phase 15.3
    rationale: >
      Canonical SVG logo + wordmark source-of-truth was never produced. PNG icon set
      (mark-{32,48,96,128,144,152,180,192,384,512}.png + apple-touch-icon variants at
      app.html:40-52) IS shipped and wired to manifest + head assets — functional
      acceptance "icon set ships and is wired to manifest + head assets" is met via
      PNGs. However, the rebuild-from-source SVG artifact is absent, making PNG
      re-export, marketing-asset regeneration, or future native-icon-set rebuild
      impossible. Per integration finding F-W-2 + v33.3-MILESTONE-AUDIT.md lines 39-50.
human_verification: []
---

# Phase 09: Redesign / Brand / Marketing Surface — Verification Report

**Phase Goal:** Visual identity + full UI refresh + landing page + marketing assets + onboarding polish — "warm, cinematic, lived-in" design system covering every existing in-app screen.

**Verified:** 2026-04-27T17:19:43Z
**Status:** passed_partial (9/10 DESIGN-* requirements satisfied; DESIGN-01 unsatisfied — closed by Phase 15.3)
**Verifier method:** retroactive_backfill_phase_15.2
**Re-verification:** No — initial verification (retroactive backfill — original VERIFICATION.md never produced when Phase 9 shipped 2026-04-22..24)

## Goal Achievement

### Observable Truths (REQUIREMENTS.md DESIGN-* contract)

The phase ROADMAP entry doesn't list explicit Success Criteria; the 10 DESIGN-* requirements at REQUIREMENTS.md lines 76-85 + the 7 plan SUMMARYs (09-02..06 + 09-07a + 09-07b) provide the verification contract. Goal-backward: "Did Phase 9 deliver the brand-system + landing-page + marketing-asset + onboarding refresh that turns Couch from a working PWA into a 'warm, cinematic, lived-in' product surface?"

| #   | Truth (DESIGN-* requirement)                                                                                                       | Status                                                                                                                          | Evidence |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | DESIGN-01 — Final logo + app icon set + favicon shipped + wired to manifest/head/iOS-Android home-screen icons                     | **✗ UNSATISFIED — closed by Phase 15.3**                                                                                        | PNG icon set ships + wired to manifest (mark-{32,48,96,128,144,152,180,192,384,512}.png + apple-touch-icon variants at `app.html:40-52` + manifest data-URL at `app.html:47`). PNGs satisfy *functional* acceptance — the icon set IS shipped and IS wired. **However**, the canonical SVG source-of-truth is absent (`find . -name '*.svg' -not -path './.planning/*' -not -path './.claude/*'` returns zero matches) — rebuild-from-source contract is unmet. Per integration finding F-W-2 + v33.3-MILESTONE-AUDIT.md lines 39-50. **09-01-SUMMARY.md NOT produced — Phase 15.3 closes both the canonical SVG production AND the 09-01-SUMMARY backfill** (per ROADMAP §239 + Phase 15.2 Pitfall 7). DO NOT mark this requirement satisfied via PNG-only acceptance. |
| 2   | DESIGN-02 — Warm dark palette canonicalized into design tokens (colors, type, spacing, motion, radius)                             | ✓ VERIFIED                                                                                                                      | 47-token semantic alias layer added to `css/app.css` :root block (Plan 09-02). 3-tier hierarchy (primitive → semantic → component). 1834 `var(--…)` references confirm token adoption (`grep -c 'var(--' css/app.css` at execute time; audit YAML line 504 saw 1840 — drift is normal, growth not regression). Cite SUMMARY 09-02 + commits `790e5cf` (palette reconcile) + `5ad9ea3` (alias layer) + `0d7262a` (docs). |
| 3   | DESIGN-03 — Typography tokens (Fraunces + Instrument Serif + Inter) applied; no one-off inline font declarations on brand surfaces | ✓ VERIFIED                                                                                                                      | Inline-style purge — 119 → 36 style attributes; 53 token-backed utility classes added (Plan 09-03). 83 brand-critical inline declarations migrated. Zero inline Fraunces / Instrument Serif declarations remain. Font-family role tokens (`--font-display`, `--font-serif`, `--font-sans`) consumed via `var()` chain. Cite SUMMARY 09-03 + REQUIREMENTS.md line 78 completion note. |
| 4   | DESIGN-04 — Every existing in-app screen re-rendered against the canonical token system + desktop responsive layer                 | ✓ VERIFIED                                                                                                                      | `.phone-shell` wrapper + single `@media (min-width: 900px)` block in `css/app.css` (Plan 09-04). Tonight, Title Detail, Members, Settings, Watchparty, Mood filter, Veto modals all re-rendered. Phase 7 UAT-deferred watchparty-modal gap closed (`.wp-live-modal` 520→800px on desktop). Cite SUMMARY 09-04 + REQUIREMENTS.md line 79. |
| 5   | DESIGN-05 — Landing page at `couchtonight.app` root — mobile-first, warm cinematic                                                 | ✓ VERIFIED                                                                                                                      | `landing.html` (233 lines, marketing surface at `/`); `app.html` (1426 lines, PWA shell at `/app`); Firebase Hosting rewrites; JSON-LD `SoftwareApplication` schema; install-redirect for standalone + `?invite=` / `?claim=` deep-link forwarding. All 7 automated post-deploy gates green. Cite SUMMARY 09-05 + REQUIREMENTS.md line 80. |
| 6   | DESIGN-06 — App-Store-ready marketing asset set (hero + feature screenshots + icon variants + promo graphics)                      | ✓ VERIFIED                                                                                                                      | 5 iPhone marketing screenshots captured + sharp-optimized + wired to `landing.html` with CSS phone-frame treatment (border-radius + warm-amber drop shadow + figcaption overlay) (Plan 09-06). `og.png` deferred to ship with wordmark redesign per user decision (long-term refresh tracked in Phase 12 docket). Cite SUMMARY 09-06 + REQUIREMENTS.md line 81. |
| 7   | DESIGN-07 — First-run onboarding polished + matches redesign + introduces feature surfaces tastefully                              | ✓ VERIFIED                                                                                                                      | 3-step overlay (couch silhouette / spin-pick-veto / 4-grid feature tour); `seenOnboarding` Firestore flag persistence; "Replay intro" Settings button; guest-skip Pitfall 5 double-defense (paired with 09-07b's `consumeGuestInvite` writing `seenOnboarding:true` at member creation). Cite SUMMARY 09-07a + commits `5f1b0fc` + REQUIREMENTS.md line 82. |
| 8   | DESIGN-08 — Invite-flow onboarding (new user accepts invite to existing family) polished + brand-aligned                           | ✓ VERIFIED                                                                                                                      | `?invite=<token>` bootstrap detour; branded redeem screen + expired dead-end (NEVER routes to sign-in); `consumeGuestInvite` Cloud Function (admin-SDK, transactional idempotency, two-mode preview/consume); `seenOnboarding:true` set at guest member creation; defense-in-depth `&family=` URL strip (server emits token-only + client sanitizes). Cite SUMMARY 09-07b + commit `41b18a9` + REQUIREMENTS.md line 83. **Cross-repo deploy artifact lives in `queuenight/functions/` — see Pitfall 2 cross-repo nuance + 09-07b Drift-1/Drift-3 resolutions.** |
| 9   | DESIGN-09 — Motion language defined + applied (toast, modal open, spin flicker, veto shimmer, watchparty transitions)              | ✓ VERIFIED                                                                                                                      | Motion token ladder: short tier (`--t-instant` 50ms / `--t-quick` 150ms / `--t-base` 220ms / `--t-deliberate` 300ms / `--t-cinema` 400ms) added by Plan 09-02; long-form tier (`--t-spin` 600ms / `--t-celebrate` 1.2s / `--t-shimmer` 1.6s / `--t-pulse` 2s / `--t-story` 5s) added by Plan 09-07b motion audit. 47 lines normalized / 66 raw-millisecond replacements. `prefers-reduced-motion` block hardened + documented. Cite SUMMARYs 09-02 + 09-07b + REQUIREMENTS.md line 84. |
| 10  | DESIGN-10 — Brand-system documentation in `.planning/` (token cheatsheet, do/don't, voice guide)                                   | ✓ VERIFIED                                                                                                                      | `.planning/BRAND.md` exists at 185 lines / 1497 words / 8 sections (identity, color tokens with WebAIM contrast, typography, spacing+radius+shadow, motion catalog, voice guide, screen patterns, do/don't gallery). Cite SUMMARY 09-07a + commit `ab56ba2` + REQUIREMENTS.md line 85. |

**Score:** 9/10 truths verified; 1/10 (DESIGN-01) explicitly UNSATISFIED with forward-pointer to Phase 15.3. The DESIGN-01 gap is **not a documentation gap** — it is a **legitimate scope-deferral**: Phase 9 shipped PNG-only icon assets, the canonical SVG production was never executed by Plan 09-01, and Phase 15.3 was scoped specifically to close it.

### Required Artifacts

| Artifact                                                                                                                          | Expected                                                                                                                            | Status                                | Details |
| --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------- |
| `css/app.css` :root token layer                                                                                                   | 47-token semantic alias layer + 5-tier motion ladder + font-family role tokens + WCAG-AA contrast notes                             | ✓ VERIFIED                            | Layer present at top of file (Plan 09-02 reframed `:root` from ~90 lines to ~172 lines). Confirmed via `grep -c 'var(--' css/app.css` → 1834 (audit saw 1840; current = 1834; growth/drift normal — not regression). |
| `css/app.css` `.phone-shell` + desktop `@media`                                                                                   | `.phone-shell` wrapper + single `@media (min-width: 900px)` block                                                                   | ✓ VERIFIED                            | `grep -c 'phone-shell\|@media (min-width:.*900px)' css/app.css` → 3 matches. Plan 09-04 deliverable. CLAUDE.md line 26 cites the block at `~css/app.css:2330` (architecture-time line; current file is 3938 lines after Phases 14/15/15.1 growth — line drift is expected). |
| `css/app.css` motion audit + `prefers-reduced-motion` block                                                                       | Zero raw ms/s in transition/animation declarations outside :root + reduced-motion; 4 cubic-bezier definitions in :root              | ✓ VERIFIED (with documented drift)    | Plan 09-07b post-audit state: 0 raw ms/s + 4 cubic-bezier (canonical token defs). Current `grep -c cubic-bezier css/app.css` → 5 (1 added by post-Phase-9 work — Phase 14/15 introduced new tokens; not a Phase-9 regression — tracked under Phase 14/15 motion-audit followups). |
| `landing.html` (marketing surface)                                                                                                | ~160 lines (per RESEARCH); zero-JS install-redirect + JSON-LD + OG + `?invite=`/`?claim=` deep-link forward                         | ✓ VERIFIED                            | 233 lines (grew from initial ~160 with marketing-screenshot wiring + audience grid expansion). Plan 09-05 deliverable. |
| `css/landing.css`                                                                                                                 | Standalone marketing-page styles (~86 lines per CLAUDE.md)                                                                          | ✓ VERIFIED                            | 96 lines. Plan 09-05 deliverable. |
| `app.html` (PWA shell)                                                                                                            | Renamed from `index.html` in commit `0c39e21`; 990+ lines; sign-in screen + footer with Privacy / Terms; onboarding overlay DOM     | ✓ VERIFIED                            | 1426 lines (post-Phase-14 V5 roster + Phase 15 tracking-layer additions; Phase 9 baseline was ~990). `app.html:40-52` icon-link block confirmed. Rename commit per CLAUDE.md architecture note. |
| `couchtonight.app/changelog.html`, `/privacy.html`, `/terms.html`                                                                  | Production-deployed marketing siblings                                                                                              | ✓ VERIFIED                            | Live at couchtonight.app per Plan 09-05 deploy + Phase 13 footer Privacy/Terms wiring. |
| PNG icon set + apple-touch-icon (DESIGN-01 functional acceptance)                                                                  | `mark-{32,48,96,128,144,152,180,192,384,512}.png` + apple-touch-icon at repo root + wired to manifest + head                         | ✓ SHIPPED (PNGs)                      | 5 favicon links + 4 apple-touch-icon links + 4 manifest icon entries (data-URL inline) at `app.html:40-52`. **PNGs cover functional acceptance.** |
| **Canonical SVG source-of-truth** (DESIGN-01 rebuild-from-source contract)                                                        | `logo.svg` / `wordmark.svg` / `assets/*.svg` brand source artifacts                                                                  | **✗ ABSENT — UNSATISFIED**            | `find . -name '*.svg' -not -path './.planning/*' -not -path './.claude/*'` returns zero matches. **Phase 15.3 closes this requirement** (per ROADMAP §247-258). |
| `.planning/BRAND.md`                                                                                                              | 185 lines / 8 sections / token cheatsheet + do-don't + voice guide                                                                  | ✓ VERIFIED                            | 185 lines confirmed via `wc -l`. Plan 09-07a deliverable (commit `ab56ba2`). |
| `queuenight/functions/index.js` `consumeGuestInvite` Cloud Function                                                                | onCall, admin SDK, transactional idempotency, two-mode (preview / consume), seenOnboarding:true at member creation                  | ✓ VERIFIED (production-deployed)      | Plan 09-07b deliverable. Lives in sibling `queuenight/` repo (deploy-only, not git-tracked on user's machine — see Pitfall 2 cross-repo nuance + 09-07b Drift-1 reframe). Production-live evidence: invite redemption flow works at couchtonight.app. |
| `queuenight/firestore.rules` guest-invite redemption rules + architecture note                                                     | CF+admin-SDK path documented; legacy-self-claim branch; existing picker-only branch preserved verbatim                              | ✓ VERIFIED (production-deployed)      | Plan 09-07a (legacy-self-claim branch, `5f1b0fc`) + Plan 09-07b architecture note (commit `46843ae`). Mirror lives in sibling queuenight repo. |
| `sw.js` CACHE bumped through Phase 9 lifecycle                                                                                    | `couch-v19-phase9-rename` (post-09-05) → `couch-v20-09-07a-onboarding` → `couch-v21-09-07b-guest-invite`                            | ✓ VERIFIED                            | Sequence preserved in 09-07a + 09-07b SUMMARY commit logs. (Current sw.js cache is `couch-v35.1-security-hardening` post-Phase-15.1 — Phase 9's bumps were stable handoffs.) |

### Key Link Verification

| From                                                              | To                                                             | Via                                                                          | Status         | Details |
| ----------------------------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------------- | ------- |
| `app.html` head                                                    | PNG icon set (mark-*.png + apple-touch-icon)                   | `<link rel="icon" />` × 5 + `<link rel="apple-touch-icon" />` × 4             | ✓ WIRED        | Lines 40-52 confirmed. Browser tab + iOS home-screen + Android home-screen all served. |
| `app.html` head `<link rel="manifest" />`                         | inline data-URL manifest with mark-{144,192,384,512}           | data:application/manifest+json data-URL at app.html:47                        | ✓ WIRED        | PWA install prompts use these. |
| **`app.html` head + manifest**                                     | **canonical SVG source-of-truth**                              | **N/A — SVG never produced**                                                 | **✗ UNSATISFIED** | **Phase 15.3 closes — DO NOT soften** |
| Firebase Hosting `/`                                              | `landing.html` (marketing surface)                             | `firebase.json` rewrites                                                     | ✓ WIRED        | Plan 09-05 + production-live. |
| Firebase Hosting `/app`                                           | `app.html` (PWA shell)                                         | `firebase.json` rewrites                                                     | ✓ WIRED        | Plan 09-05 + production-live. |
| Landing page                                                       | App shell with deep-link preserved                             | inline JS `?invite=`/`?claim=` forward + standalone-PWA install redirect      | ✓ WIRED        | Plan 09-05; verified by Plan 09-07b's invite-redeem flow against production. |
| `landing.html` `<head>`                                            | JSON-LD `SoftwareApplication` schema + Open Graph meta          | inline `<script type="application/ld+json">` + `<meta property="og:*">`        | ✓ WIRED        | Plan 09-05. |
| `landing.html` figures                                              | 5 iPhone marketing screenshots + phone-frame CSS treatment      | `<img src=...>` + sharp-optimized assets                                     | ✓ WIRED        | Plan 09-06. |
| First-run gate `maybeShowFirstRunOnboarding`                       | `members/{id}.seenOnboarding` Firestore flag                   | onSnapshot read + write on completion + Replay-intro re-show                  | ✓ WIRED        | Plan 09-07a (commit `5f1b0fc`). Pitfall 5 double-defense (09-07b sets `seenOnboarding:true` at guest creation). |
| Guest invite link `?invite=<token>`                                | `consumeGuestInvite` Cloud Function (admin SDK)                | `app.js boot()` invite detour BEFORE sign-in gate → `httpsCallable`           | ✓ WIRED        | Plan 09-07b (commit `41b18a9`). Token-only deep links (no `&family=`). |
| `consumeGuestInvite` CF                                             | `families/{code}/members/{id}` doc with `type:'guest'` + `seenOnboarding:true` | admin-SDK transactional write + idempotency via `invite.consumedAt`           | ✓ WIRED        | CF+admin-SDK bypasses Firestore rules entirely (architecture note documented at `firestore.rules`). |
| Motion tokens `--t-spin / --t-celebrate / --t-shimmer / --t-pulse / --t-story` | css/app.css rule bodies (`.spin-spinner`, `.flash-highlight`, `.sk` skeleton, `.wp-banner.imminent`, `yir-story-fill`) | `var()` references                                                            | ✓ WIRED        | Plan 09-07b motion audit. |

### Data-Flow Trace (Level 4)

| Artifact                                  | Data Variable                                       | Source                                                                                           | Produces Real Data                                                       | Status      |
| ----------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ----------- |
| Token consumption across `css/app.css` rules | 1834 `var(--…)` references                           | :root primitive + semantic alias layer (Plans 09-02 + 09-07b motion tokens)                       | ✓ Yes — every brand-critical surface resolves through tokens             | ✓ FLOWING   |
| Landing page hero / sections              | static + JSON-LD schema + OG meta                    | `landing.html` inline content                                                                     | ✓ Yes — production-live at couchtonight.app                              | ✓ FLOWING   |
| Marketing screenshots in landing          | 5 phone-frame `<figure>` blocks                      | sharp-optimized PNGs at repo root                                                                | ✓ Yes — render in landing footer + audience grid                         | ✓ FLOWING   |
| First-run onboarding overlay              | `state.me.seenOnboarding`                             | `members/{id}` Firestore doc onSnapshot                                                           | ✓ Yes — overlay shows once per user, persists across sign-out/sign-in     | ✓ FLOWING   |
| Replay-intro Settings button              | manual `replayOnboarding()` call                     | user tap                                                                                         | ✓ Yes — re-shows overlay on demand                                       | ✓ FLOWING   |
| Guest-invite redeem screen                | `fetchInvitePreview` CF result                       | `consumeGuestInvite({preview:true})` admin-SDK read                                              | ✓ Yes — branded redeem renders with family name + duration               | ✓ FLOWING   |
| Guest signup completion                   | `submitGuestRedeem` → `signInAnonymously` + CF mutate | `consumeGuestInvite({consume:true})` admin-SDK transactional write                                | ✓ Yes — guest member created with `seenOnboarding:true`, lands on Tonight | ✓ FLOWING   |
| Expired-invite dead-end                   | `invite.expiresAt` < now OR `invite.consumedAt`       | preview path detects + status renders branded "ask for a new one" screen                          | ✓ Yes — never routes to sign-in (intentional UX)                          | ✓ FLOWING   |
| Motion tokens                              | `:root` token definitions + `var()` resolution       | css/app.css cascade                                                                              | ✓ Yes — all transitions/animations resolve through tokens                | ✓ FLOWING   |
| `.planning/BRAND.md`                       | static reference doc                                 | manual authoring (Plan 09-07a)                                                                   | ✓ Yes — 185 lines / 1497 words / 8 sections                              | ✓ FLOWING   |
| **Canonical SVG source-of-truth**         | **N/A — never produced**                              | **N/A — Plan 09-01 deferred**                                                                     | **✗ NO — rebuild-from-source impossible**                                | **✗ ABSENT** |

No HOLLOW or DISCONNECTED artifacts identified for the satisfied requirements (DESIGN-02..10). The single ABSENT artifact is the canonical SVG (DESIGN-01) — Phase 15.3 closes it.

### Behavioral Spot-Checks

Auto-mode + production-deployed code; live at `couchtonight.app` running `couch-v35.1-security-hardening` (Phase 15.1 cache; Phase 9's `couch-v21-09-07b-guest-invite` was stably superseded). Behavioral verification of Phase 9 surfaces is implicit in every subsequent phase that consumed them (Phase 11/12/13/14/15/15.1 all built on Phase 9's tokens + landing rewrites + onboarding gates without regression).

| Behavior                                                              | Command/Source                                                                                                | Result                                                                                  | Status |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------ |
| `var(--…)` token adoption count                                        | `grep -c 'var(--' css/app.css`                                                                                | 1834 (audit YAML line 504 saw 1840; current 1834 — drift normal, not regression)         | ✓ PASS |
| `.phone-shell` wrapper + desktop `@media (min-width:900px)` block      | `grep -c 'phone-shell\|@media (min-width:.*900px)' css/app.css`                                               | 3 matches                                                                               | ✓ PASS |
| PNG icon set wired to manifest + head                                  | `grep -E 'mark-[0-9]+\.png\|apple-touch-icon' app.html`                                                       | 13 matches across head links + manifest data-URL (lines 40-52)                          | ✓ PASS |
| `landing.html` exists + non-empty                                      | `wc -l landing.html`                                                                                          | 233 lines                                                                               | ✓ PASS |
| `css/landing.css` exists + non-empty                                   | `wc -l css/landing.css`                                                                                       | 96 lines                                                                                | ✓ PASS |
| `app.html` (renamed from index.html in commit `0c39e21` per CLAUDE.md) | `wc -l app.html`                                                                                              | 1426 lines (Phase 9 baseline ~990; growth from Phases 14/15 acceptable)                  | ✓ PASS |
| `.planning/BRAND.md` 185 lines                                         | `wc -l .planning/BRAND.md`                                                                                    | 185 lines                                                                               | ✓ PASS |
| Motion tokens (`--t-spin`, `--t-celebrate`, `--t-shimmer`, `--t-pulse`, `--t-story`) | grep across `css/app.css` :root                                                                                | All 5 long-form tokens present + 5 short-tier tokens preserved                          | ✓ PASS |
| Cubic-bezier canonical token definitions                               | `grep -c cubic-bezier css/app.css`                                                                            | 5 (Phase 9 ended at 4; +1 added post-Phase-9 by 14/15 work — not a Phase-9 regression)   | ✓ PASS (drift documented) |
| **Canonical SVG source-of-truth absent**                                | `find . -name '*.svg' -not -path './.planning/*' -not -path './.claude/*'`                                    | **Zero matches**                                                                        | **✗ UNSATISFIED — Phase 15.3 closes** |
| Phase 9 SUMMARYs present (09-02..06 + 09-07a + 09-07b)                  | `ls .planning/phases/09-redesign-brand-marketing-surface/09-0[2-7]*-SUMMARY.md`                                | 7 SUMMARY files present                                                                 | ✓ PASS |
| `09-01-SUMMARY.md` deferred to Phase 15.3                                | `! test -f .planning/phases/09-redesign-brand-marketing-surface/09-01-SUMMARY.md`                              | Does NOT exist (per ROADMAP §239 + Pitfall 7)                                           | ✓ PASS (intentional) |

### Requirements Coverage

| Requirement | Source Plan        | Description                                                                                                         | Status                                | Evidence |
| ----------- | ------------------ | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------- |
| DESIGN-01   | 09-01              | Final logo + app icon set + favicon + manifest/head/iOS-Android wiring                                              | **✗ UNSATISFIED**                     | **Status: UNSATISFIED. Closed by: Phase 15.3 (canonical SVG logo + wordmark sources). Per integration finding F-W-2 + audit YAML lines 39-50. PNG icon set covers functional acceptance only; rebuild-from-source contract unmet.** |
| DESIGN-02   | 09-02              | Warm dark palette canonicalized into design tokens                                                                  | ✓ SATISFIED                           | SUMMARY 09-02; commits `790e5cf` + `5ad9ea3` + `0d7262a`; 1834 `var(--…)` adoptions |
| DESIGN-03   | 09-02 + 09-03      | Typography tokens applied; no one-off inline font declarations on brand surfaces                                    | ✓ SATISFIED                           | SUMMARY 09-03; 119→36 inline-style purge; 53 token-backed classes; 83 brand-critical migrations |
| DESIGN-04   | 09-03 + 09-04      | Every existing in-app screen re-rendered against canonical token system + desktop responsive                        | ✓ SATISFIED                           | SUMMARY 09-04; `.phone-shell` + `@media (min-width:900px)` block; closed Phase 7 watchparty-modal UAT gap |
| DESIGN-05   | 09-05              | Landing page at couchtonight.app root                                                                                | ✓ SATISFIED                           | SUMMARY 09-05; landing.html (233 lines) + Firebase Hosting rewrites + JSON-LD + OG; 7 post-deploy gates green |
| DESIGN-06   | 09-06              | App-Store-ready marketing asset set                                                                                  | ✓ SATISFIED                           | SUMMARY 09-06; 5 iPhone screenshots + sharp optimization + landing wiring; og.png deferred to wordmark redesign |
| DESIGN-07   | 09-07a             | First-run onboarding polished + matches redesign                                                                     | ✓ SATISFIED                           | SUMMARY 09-07a; commit `5f1b0fc`; 3-step overlay + `seenOnboarding` Firestore flag + Replay-intro Settings |
| DESIGN-08   | 09-07b             | Invite-flow onboarding polished + brand-aligned                                                                      | ✓ SATISFIED                           | SUMMARY 09-07b; commit `41b18a9`; `consumeGuestInvite` CF + branded redeem screen + token-only deep links |
| DESIGN-09   | 09-02 + 09-07b     | Motion language defined + applied                                                                                    | ✓ SATISFIED                           | SUMMARYs 09-02 + 09-07b; 5-tier short ladder + 5-token long-form ladder; 47 lines / 66 replacements; reduced-motion hardened |
| DESIGN-10   | 09-07a             | Brand-system documentation in .planning/                                                                             | ✓ SATISFIED                           | SUMMARY 09-07a; `.planning/BRAND.md` 185 lines / 1497 words / 8 sections (commit `ab56ba2`) |

**Coverage:** 9/10 DESIGN-* requirements SATISFIED with shipping evidence in 09-02..06 + 09-07a + 09-07b SUMMARYs + production-live state at couchtonight.app. 1/10 (DESIGN-01) UNSATISFIED with explicit forward-pointer to Phase 15.3 — **DO NOT count toward Phase 9 close-out as satisfied**. Zero ORPHANED requirements (every DESIGN-* has either a satisfying SUMMARY chain or an explicit Phase-15.3 forward pointer).

### Anti-Patterns Found

| File                                                | Line / Surface                  | Pattern                                                                                  | Severity                            | Impact |
| --------------------------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------- | ------ |
| (DESIGN-01)                                         | brand asset pipeline             | Canonical SVG source-of-truth absent — only PNG-derived assets shipped                   | ⚠️ Tracked (NOT a verification defect) | Already scoped to Phase 15.3 per ROADMAP §247-258. PNGs satisfy *functional* acceptance ("icon set ships and is wired") but the rebuild-from-source contract is unmet. Phase 15.3 produces canonical `logo.svg` + `wordmark.svg` + re-exports the PNG matrix from source. |
| `queuenight/` repo                                  | (whole sibling repo)             | Sibling functions / firestore.rules edited in-place + deployed without git tracking on user's machine | ⚠️ Warning (Pitfall 2 cross-repo nuance) | Pattern repeated across Phase 9 (09-07a tz fix + 09-07b consumeGuestInvite + rules architecture note) and later phases (07-05/06/07/08, 14-06/09/10, 15.1-03). Functional behavior unaffected — files were edited in-place + deployed via `firebase deploy`. **Code IS live in production**. Auditing risk only. STATE.md tracks remediation: "two-repo discipline" follow-up backlog. |
| `css/app.css` cubic-bezier count                    | post-Phase-9 drift                | Audit YAML expected 4; current count is 5 (1 added by Phases 14/15 token work)            | ℹ️ Info (post-Phase-9 drift)         | NOT a Phase-9 regression. Phase 9's motion-audit acceptance gate at end of Plan 09-07b was 4 cubic-bezier definitions in :root (per `audit script` PASS in 09-07b SUMMARY). Subsequent phases added a 5th cubic-bezier token for 14/15 motion needs; that addition is a Phase-14/15 followup concern, not a Phase-9 verification gap. |

**No 🛑 Blocker anti-patterns found.** The DESIGN-01 SVG gap is a tracked deferral with explicit forward-pointer; the queuenight git-tracking gap is a documented project-wide pattern (not Phase-9-specific); the cubic-bezier drift is post-Phase-9 noise.

### Human Verification Required

`human_verification: []` — empty list. All 9 satisfied DESIGN-* requirements have shipped evidence in the 09-02..06 + 09-07a + 09-07b SUMMARYs plus production-live state at couchtonight.app (running `couch-v35.1-security-hardening` as of 2026-04-27, having stably succeeded Phase 9's `couch-v21-09-07b-guest-invite` cache through 14 subsequent SW bumps without Phase 9 surface regression).

Multi-device runtime UAT was NOT part of Phase 9's scope contract — Phase 9 ships visual + brand + onboarding surface; behavioral UAT lives in feature phases that consume the design system (Phase 14 multi-device Flow A/B UAT, Phase 12 push-toggle UAT, etc.). Phase 9's own Task 4 human-verify checkpoints (09-07a 7-track UAT + 09-07b track A invite redeem + track B reduced-motion) were absorbed by user during the Phase 9 execution window 2026-04-22..24 and resolved against production at deploy time. No carry-over UAT items survived Phase 9 close-out.

### Gaps Summary

**One legitimate unsatisfied requirement: DESIGN-01.** Canonical SVG logo + wordmark source-of-truth was never produced when Phase 9 shipped 2026-04-22..24. Functional acceptance was met via PNG icon set (wired to manifest + head + iOS/Android home-screen). Rebuild-from-source contract is unmet. **Phase 15.3 closes both the SVG production AND the missing 09-01-SUMMARY.md backfill** (per ROADMAP §239 + Phase 15.2 Pitfall 7). DO NOT mark DESIGN-01 satisfied via PNG-only acceptance (per Pitfall 4 + audit YAML lines 39-50).

**No other gaps.** DESIGN-02..10 all satisfied with shipping evidence in 09-02..06 + 09-07a + 09-07b SUMMARYs + production-live at `couchtonight.app`. The 1840 → 1834 token-adoption drift is normal post-phase variance (not regression). The 4 → 5 cubic-bezier drift is post-Phase-9 token addition by Phases 14/15 (not a Phase-9 concern).

**No goal-blocking gaps.** Phase 9's underlying user goal — "warm, cinematic, lived-in design system + landing page + marketing assets + onboarding polish covering every existing in-app screen" — is fully delivered at the 9/10 satisfied level. The 10th item (DESIGN-01 canonical SVG) is a known gap with an explicit closure plan in Phase 15.3.

### Recommendation

**Phase 9 goal achieved at the 9/10 satisfied level.** Status: **SHIPPED 2026-04-24 (passed_partial — DESIGN-01 → Phase 15.3)**. Verifier method: `retroactive_backfill_phase_15.2` — original VERIFICATION.md never produced when Phase 9 shipped; this artifact reconstructs the verification trail from existing 09-02..06 + 09-07a + 09-07b SUMMARYs + production-live state at couchtonight.app + audit YAML evidence blocks (lines 39-50 + 478-485 + 491-505).

The orchestrator should:

1. **Mark Phase 9 as SHIPPED with passed_partial verdict** in ROADMAP.md Progress table (`9/10 DESIGN-*; DESIGN-01 → Phase 15.3`).
2. **DO NOT mark DESIGN-01 as satisfied via PNG-only acceptance** (per Pitfall 4 + audit YAML lines 39-50). The REQUIREMENTS.md row for DESIGN-01 should remain **Pending — assigned to Phase 15.3** until Phase 15.3 ships the canonical SVG + 09-01-SUMMARY.md.
3. **DO NOT trigger a gap-closure cycle for DESIGN-01 within Phase 15.2** — that work is explicitly scoped to Phase 15.3.
4. **Apply the 8 DESIGN-02..09 + DESIGN-10 row flips** to REQUIREMENTS.md traceability table (handled by Phase 15.2 cross-cutting traceability plan, not this VERIFICATION backfill).
5. **Proceed with Phase 15.2 wave-1 parallel completion** — this VERIFICATION artifact is the Phase 9 deliverable; sibling per-phase backfills (5/6/7/8) close the remaining missing VERIFICATION + SUMMARY gaps.

Phase 9 represents the largest single design-system phase shipped to date in the GSD workflow on this project (8 plans, 10 requirements, ~30 atomic commits, full design-token migration + landing-page production + marketing-asset shoot + dual onboarding overlay + motion audit), and it landed cleanly with **0 goal-blocking gaps** and exactly **1 explicitly-scoped legitimate gap** (DESIGN-01) that already had a closure plan locked in Phase 15.3 at audit time.

---

**09-01-SUMMARY.md NOT produced by this plan.** Per ROADMAP §239 + Phase 15.2 Pitfall 7, the 09-01 SUMMARY backfill is **deferred to Phase 15.3** because it depends on producing the canonical SVG source-of-truth that Plan 09-01 was supposed to ship. Backfilling 09-01-SUMMARY from PNG-only state would lock in incorrect content that Phase 15.3 would have to overwrite — and would conflict with the audit YAML's explicit "DESIGN-01 unsatisfied" verdict.

---

_Verified: 2026-04-27T17:19:43Z_
_Verifier: Claude (gsd-executor — Phase 15.2 Plan 05 retroactive backfill)_
_Verifier method: retroactive_backfill_phase_15.2_
