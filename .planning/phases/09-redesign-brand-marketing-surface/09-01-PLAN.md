---
phase: 09-redesign-brand-marketing-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brand/couch-mark.svg
  - brand/couch-wordmark.svg
  - brand/README.md
  - index.html
autonomous: false
requirements: [DESIGN-01]
commit_strategy: "3 commits — (1) canonical SVG sources committed under brand/, (2) regenerated PNG asset set copied to sibling queuenight/public/ + deploy, (3) index.html head wiring updates if any filename/size changed"
batching_rationale: "SVG source, binary PNG outputs (sibling repo), and head-wiring each warrant their own commit. PNG commits live in the sibling repo — keeps this repo's diff to source-of-truth only. Head wiring lands only if a filename/size shifts (most likely a no-op if regeneration preserves the current set)."
user_setup:
  - service: iPhone (physical device)
    why: "iOS PWA icon cache-bust verification (STATE.md environment note — deletes Safari website data + reinstalls PWA)"
  - service: queuenight/public/ (sibling repo)
    why: "Brand PNG outputs deploy from sibling repo, not this repo. User must have local clone + Firebase deploy permissions"
  - service: Node/npx (or RealFaviconGenerator.net browser fallback)
    why: "Asset regeneration pipeline — either npx @vite-pwa/assets-generator CLI or the browser-based RealFaviconGenerator alternative"

must_haves:
  truths:
    - "A canonical SVG logo source exists inside this repo under brand/ — single source of truth going forward"
    - "Every PNG referenced by index.html (logo-h300, logo-h200, mark-{96,128,144,152,180,192,512}, favicon-{16,32,48}, favicon.ico) exists in queuenight/public/ and is no older than the SVG source"
    - "Safari on iOS + Chrome on Android + Desktop Chrome all render the refreshed icon after deploy + cache bust"
    - "PWA manifest data URL theme_color + background_color remain in sync with the canonical #14110f"
    - "At least one maskable icon variant (purpose=maskable) is produced with the mark centered inside a 40% safe-zone, wired into the inline manifest so Android adaptive icons render without clipping"
  artifacts:
    - path: "brand/couch-mark.svg"
      provides: "Canonical square mark (the C / film-reel symbol) — source of truth for mark-*.png and favicon-*.png"
      contains: "svg xmlns"
    - path: "brand/couch-wordmark.svg"
      provides: "Canonical wordmark lockup (mark + 'ouch' type treatment) — source for logo-h300.png + logo-h200.png"
      contains: "svg xmlns"
    - path: "brand/README.md"
      provides: "Regeneration instructions + pixel targets + iOS cache-bust procedure + maskable-safe-zone diagram"
      min_lines: 30
    - path: "index.html"
      provides: "Head-tag icon set wired to regenerated assets; unchanged filenames if the current set is preserved; inline manifest gains a purpose=maskable entry"
      contains: "apple-touch-icon"
  key_links:
    - from: "brand/couch-mark.svg"
      to: "queuenight/public/mark-180.png"
      via: "npx @vite-pwa/assets-generator or RealFaviconGenerator export"
      pattern: "mark-180\\.png"
    - from: "brand/couch-wordmark.svg"
      to: "queuenight/public/logo-h300.png, queuenight/public/logo-h200.png"
      via: "manual SVG export at exact pixel dimensions (634x300 and 423x200)"
      pattern: "logo-h(300|200)\\.png"
    - from: "index.html head"
      to: "every icon PNG served by Firebase Hosting at couchtonight.app"
      via: "<link rel=...> tags at lines 38-52"
      pattern: "href=\"/(mark|logo|favicon)-?"
    - from: "inline manifest icons array"
      to: "queuenight/public/mark-512-maskable.png"
      via: "purpose=\"maskable\" manifest entry; mark inside 40% safe-zone"
      pattern: "maskable"
---

<objective>
Ship the canonical brand identity pipeline for Phase 9: commit source-of-truth SVG logo + wordmark under `brand/` in this repo, regenerate the full PNG icon set (including a maskable variant with the mark inside a 40% safe-zone), copy outputs to the sibling `queuenight/public/` deploy repo, and verify the refresh lands cleanly on iOS home-screen PWA + Chrome Android + Desktop Chrome.

Purpose: DESIGN-01 closes (with one documented caveat if user selects option-c — see risk_notes). Every subsequent Phase 9 plan references the logo/mark (landing, marketing assets, onboarding, brand docs). Locking identity FIRST avoids rework.

Output:
- `brand/couch-mark.svg` + `brand/couch-wordmark.svg` — committed to THIS repo
- Full regenerated PNG set in `queuenight/public/` (sibling repo) — committed + deployed, including at least one maskable variant
- `brand/README.md` regeneration instructions + iOS cache-bust procedure + maskable-safe-zone rules
- `index.html` head-wiring updates (add a `purpose=maskable` entry to the inline manifest icons array; other filename changes only if regeneration renames)
- Verified apple-touch-icon-180 renders sharp on iOS standalone PWA after cache bust
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-redesign-brand-marketing-surface/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Icon set currently wired in index.html (lines 38-52). Regeneration must preserve these filenames unless index.html is updated in lockstep. -->

From index.html:38-52:
```html
<link rel="icon" type="image/x-icon" href="/favicon.ico">
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png">
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png">
<link rel="icon" type="image/png" sizes="48x48" href="/favicon-48.png">
<link rel="icon" type="image/png" sizes="96x96" href="/mark-96.png">
<link rel="icon" type="image/png" sizes="192x192" href="/mark-192.png">
<link rel="icon" type="image/png" sizes="512x512" href="/mark-512.png">
<link rel="manifest" href="data:application/manifest+json;charset=utf-8,...theme_color:#14110f..."><!-- inline -->
<link rel="apple-touch-icon" sizes="180x180" href="/mark-180.png">
<link rel="apple-touch-icon" sizes="152x152" href="/mark-152.png">
<link rel="apple-touch-icon" sizes="144x144" href="/mark-144.png">
<link rel="apple-touch-icon" sizes="128x128" href="/mark-128.png">
```

From index.html:93, 118, 130, 151 (wordmark usages):
```html
<img class="brand-logo" src="/logo-h300.png" alt="Couch" width="634" height="300">  <!-- mode-pick hero -->
<img class="brand-logo" src="/logo-h200.png" alt="Couch" width="423" height="200">  <!-- family-join, name, claim-confirm -->
```

Canonical theme color: `#14110f` (CLAUDE.md + manifest). Note: css/app.css:7 has `--bg:#14110d` — that's a separate drift reconciled in plan 09-02, not this one.

Maskable icon reference: Android Chrome's adaptive icon system clips the outer ~20% of any icon marked purpose="maskable". W3C spec recommends keeping the critical mark inside the CENTER 40% of the canvas (i.e. a 40% safe-zone radius from center, equivalent to 80x80 inside a 200x200 canvas — or 205 inside 512). See w3c/manifest#icon-purposes.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide SVG sourcing path (refresh vs. regenerate-only)</name>
  <decision>Does a canonical SVG logo source already exist outside this repo (Figma, Illustrator, a previous export), or does Claude need to derive one from the existing PNGs?</decision>
  <context>
    Research (09-RESEARCH.md Open Question #1 + #2) flags this as the single user-blocking question for Wave 1. PNG set is live in queuenight/public/ but the research could not verify an SVG source anywhere. Planner default (per invocation): "assume canonical SVG does NOT exist; create it as part of this plan. Refresh concept, not just regenerate."

    Two viable paths (option-c — vectorize-regenerate only — has been DROPPED because it would not deliver the ROADMAP-stated "full visual identity refresh"; see risk_notes DESIGN-01 caveat if you need to revive it as an explicit user deferral):
  </context>
  <options>
    <option id="option-a">
      <name>User provides canonical SVG + wordmark SVG (offline handoff)</name>
      <pros>Fastest path to a definitive source of truth; brand stays fully user-owned</pros>
      <cons>Blocks this task until user delivers files; requires user action outside Claude</cons>
    </option>
    <option id="option-b">
      <name>Claude drafts SVG from scratch — refresh concept — user reviews before regeneration</name>
      <pros>Matches planner default (refresh, not regenerate); delivers a real redesign; one round of user review</pros>
      <cons>Claude-generated brand work is a weak form of "Warm · Cinematic · Lived-in" — user may reject the aesthetic; adds a review cycle</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (provide SVG path) or option-b (Claude drafts + user reviews). If option-a, commit the files to brand/ and type "provided". If option-b, type "draft" and Claude writes candidate SVGs for your review. If you want the deferred "vectorize-only" path instead of a refresh, type "vectorize-defer" — Claude will document DESIGN-01 as partial-complete per risk_notes and the full refresh becomes a v2 task.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit canonical SVG sources + regeneration README under brand/</name>
  <files>
    brand/couch-mark.svg
    brand/couch-wordmark.svg
    brand/README.md
  </files>
  <action>
    Based on Task 1 decision:
    - option-a: User committed the SVGs. Claude moves them into brand/ if not already there and verifies viewBox + no rasterized content.
    - option-b: Claude drafts two SVG files honoring "Warm · Cinematic · Lived-in. Restraint is the principle." — warm dark on warm light, Instrument-Serif-inspired wordmark, single-color mark (no gradients in the source so PWA maskable icon variants work). Use `--accent:#e8a04a` as primary + `--bg:#14110f` as dark background. Both must be square (mark) or 634x300 ratio (wordmark) so the existing image elements at index.html:93, 118, 130, 151 don't require width/height changes. Produce a SECOND checkpoint after drafting to let the user approve before regeneration runs.
    - vectorize-defer: Claude writes a vectorization procedure into brand/README.md pointing at SVGOMG or Potrace for the user to run on their local copy of mark-512.png, then places the produced SVG at brand/couch-mark.svg. Wordmark derives from logo-h300.png similarly. Claude also stamps the SUMMARY with a DESIGN-01-partial disposition per risk_notes.

    Regardless of path, brand/README.md must contain:
    1. "Regeneration pipeline" section — exact commands to run `npx --yes @vite-pwa/assets-generator --preset minimal-2023 brand/couch-mark.svg` (or RealFaviconGenerator.net fallback) to produce mark-{96,128,144,152,180,192,512}.png + favicon.ico + favicon-{16,32,48}.png + at least one mark-512-maskable.png (purpose=maskable). Verify `npx --version` before attempting.
    2. "Wordmark export" section — SVG → PNG at exact pixel dimensions (634x300 for logo-h300.png, 423x200 for logo-h200.png). Tool-agnostic (Inkscape `inkscape --export-filename=... --export-width=634 brand/couch-wordmark.svg`, or any SVG-capable editor's export-as-PNG dialog).
    3. "iOS cache-bust procedure" — copied verbatim from STATE.md environment note: "(1) Delete PWA from home screen, (2) Safari → Advanced → Website Data → delete couchtonight.app, (3) Reopen in Safari, (4) Re-add to Home Screen". Document this is REQUIRED after every icon change — iOS Safari caches apple-touch-icon at install time and no Hosting deploy invalidates it (research Pitfall 1).
    4. "Pixel targets reference" — table of every target PNG filename, size, purpose (PWA manifest / apple-touch-icon / favicon / wordmark / maskable), source SVG, and whether it's square or wordmark-ratio.
    5. "Maskable safe-zone rules" — W3C adaptive-icon spec: the critical mark MUST sit inside the CENTER 40% of the square canvas (Android clips the outer ~20% aggressively). For a 512x512 maskable source, the mark should fit in the center 205x205 px safe-zone with the remaining canvas filled by solid `--bg:#14110f`. Include an ASCII diagram:
       ```
       +---------------------------+   <- 512x512 canvas (bg #14110f)
       |         clip zone         |
       |   +---------------+       |
       |   |  safe zone    |       |   <- 205x205 center (40% radius from center)
       |   |   ← mark →    |       |      mark sits here
       |   +---------------+       |
       |         clip zone         |
       +---------------------------+
       ```
       When drafting couch-mark.svg: either keep the mark tight in the center 40% and rely on the tool to pad, OR produce a second SVG explicitly mapped for maskable with pre-padded canvas.
    6. "Do not commit rasterized PNGs to THIS repo" — the PNG outputs live in `queuenight/public/` only. This repo holds SVG source + docs only. Rationale: source-of-truth separation from build artifacts.

    Constraints baked into SVG sources:
    - Square mark has NO rounded corners baked in — iOS clips corners itself (research: logofoundry.app/blog/pwa-icon-requirements-safe-areas).
    - No drop shadows or gradient fills in the mark file (would break maskable icon + favicon.ico conversion).
    - Wordmark uses Instrument Serif italic as the reference typeface — convert glyphs to paths so the SVG doesn't require a webfont at render time.
    - Mark geometry sits inside the center 40% safe-zone OR the maskable export pads it to fit (see README rule 5).
  </action>
  <verify>
    <automated>test -f brand/couch-mark.svg &amp;&amp; test -f brand/couch-wordmark.svg &amp;&amp; test -f brand/README.md &amp;&amp; head -c 100 brand/couch-mark.svg | grep -q '&lt;svg' &amp;&amp; head -c 100 brand/couch-wordmark.svg | grep -q '&lt;svg' &amp;&amp; grep -q 'iOS cache-bust' brand/README.md &amp;&amp; grep -q 'Pixel targets' brand/README.md &amp;&amp; grep -qi 'maskable' brand/README.md &amp;&amp; grep -q '40%' brand/README.md</automated>
  </verify>
  <done>
    Two SVG files exist under brand/, each starting with &lt;svg and containing a valid viewBox.
    brand/README.md covers regeneration, wordmark export, iOS cache-bust, pixel targets, AND maskable safe-zone rules with the 40% safe-zone diagram.
    Neither SVG contains &lt;image&gt; rasterized fallbacks (grep -q 'xlink:href' returns empty) — both are pure vector.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run regeneration pipeline + copy outputs to queuenight/public/ + deploy + verify iOS</name>
  <what-built>
    Claude wrote the SVG source + README. The user runs the regeneration pipeline on their machine (the tooling is developer-machine, not CI) and copies the outputs to the sibling queuenight/public/ repo.
  </what-built>
  <how-to-verify>
    1. Run `npx --yes @vite-pwa/assets-generator --preset minimal-2023 brand/couch-mark.svg` in the repo root. Copy all produced PNG files (mark-*, favicon*, apple-touch-icon-*, mark-*-maskable.png) into sibling `queuenight/public/`, OVERWRITING existing files with matching names. Current expected set: mark-{96,128,144,152,180,192,512}.png + favicon.ico + favicon-{16,32,48}.png + at least one mark-512-maskable.png. If the generator preset doesn't produce a maskable variant automatically, export it manually from the SVG with the mark pre-centered inside the 40% safe-zone per brand/README.md rule 5.
    2. Export wordmark from brand/couch-wordmark.svg at 634x300 → queuenight/public/logo-h300.png, and at 423x200 → queuenight/public/logo-h200.png. Use Inkscape, Figma, or any SVG-capable editor.
    3. `cd ../queuenight && git status && git add public/mark-*.png public/favicon*.png public/favicon.ico public/logo-h200.png public/logo-h300.png && git commit -m "chore(brand): regenerate icon set from Phase 9 canonical SVG" && firebase deploy --only hosting`.
    4. Desktop Chrome verification: Open https://couchtonight.app in incognito. Hard refresh (Ctrl+Shift+R). Check favicon in tab (should be refreshed mark). Check DevTools → Application → Manifest → icons preview. Check DevTools → Network filtered to "Img" and confirm mark-192.png + mark-512.png fetch with recent Last-Modified header.
    5. Android Chrome verification: Open in regular Chrome → menu → "Install app" / "Add to Home Screen". Confirm home-screen icon reflects the refreshed mark (should be apple-touch-icon-sized mark-192 or mark-512). Long-press the installed icon; if the launcher shows adaptive-icon rendering, confirm the mark is not clipped (maskable variant lands correctly).
    6. iOS Safari verification (CRITICAL — this is the gate):
       a. If you already have the PWA installed: delete it from home screen, Safari → Advanced → Website Data → delete couchtonight.app entirely.
       b. Reopen https://couchtonight.app in Safari.
       c. Share → Add to Home Screen. Confirm the preview icon shows the refreshed mark.
       d. Tap the added home-screen icon. Confirm icon renders sharp (no pixelation at ~120px render size) and with expected crop (iOS clips corners — make sure the mark sits in a safe zone, not clipped).
       e. Launch standalone from home screen → confirm the splash + in-app iOS status bar theme is #14110f (no drift back to an older color).
    7. If ANY of #4-6 shows a stale icon: double-check that queuenight/public/ has the new files committed and that `firebase deploy` output confirms the files shipped. If you see old icons despite fresh files: that's the documented iOS cache (research Pitfall 1) — redo step 6a.
  </how-to-verify>
  <resume-signal>
    Type "approved" if icons render refreshed on all three surfaces (desktop Chrome + Android Chrome + iOS home-screen PWA). Type "stale on iOS" if the iOS home-screen icon is still the OLD mark after cache-bust — Claude will investigate the iOS-specific apple-touch-icon rendering path. Type "wrong color" if the icon looks right but the splash/theme drifted. Type "maskable clipped" if Android adaptive icon shows clipping — Claude will adjust safe-zone padding. Type any other issue to log it.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Sync index.html head-tags + inline manifest (add maskable purpose entry; adjust if filenames/sizes shifted)</name>
  <files>
    index.html
  </files>
  <action>
    After Task 3 deploy confirms icons render, determine whether the regenerated output set matches the filenames/sizes currently referenced in index.html:38-52.

    Current references (12 head tags + inline manifest icons): favicon.ico, favicon-16, favicon-32, favicon-48, mark-96, mark-128 (apple-touch), mark-144 (apple-touch), mark-152 (apple-touch), mark-180 (apple-touch), mark-192, mark-512, logo-h300.png (index.html:93), logo-h200.png (index.html:118/130/151).

    Reconciliation rules:
    - **If the regenerated set EXACTLY matches the current list** (same filenames, same sizes): no head-tag changes needed, but the inline manifest icons array STILL needs a new entry for the maskable variant — see below.
    - **If the regenerated set EXCLUDES a file the HTML references** (e.g. @vite-pwa/assets-generator doesn't produce mark-96 by default): either (a) manually export that size from the SVG to fill the gap OR (b) remove the corresponding head tag from index.html. Preference: fill the gap — legacy iOS support.
    - **If the regenerated set ADDS new sizes** (e.g. mark-167 for iPad): add apple-touch-icon head tag below the existing mark-180 tag. Do NOT touch the inline manifest for iOS-facing sizes — only add to head (iOS ignores manifest icons per research).
    - **Inline manifest data URL (index.html:47)**: The icons array MUST gain a new entry for the maskable variant. Example JSON fragment (URL-encoded and concatenated into the data URL):
      ```json
      {
        "src": "/mark-512-maskable.png",
        "sizes": "512x512",
        "type": "image/png",
        "purpose": "maskable"
      }
      ```
      Keep the existing `{ src: "/mark-512.png", sizes: "512x512", purpose: "any" }` entry (or add `purpose: "any"` explicitly if missing — the two purposes must be distinct so the browser can choose). theme_color + background_color stay `#14110f`.
    - **Wordmark usages** (4 sites: index.html:93, 118, 130, 151): no change unless logo-h300.png or logo-h200.png changed ratio. Width/height attributes MUST stay at 634x300 and 423x200 to avoid layout shift.

    After any HTML edit: `node --check index.html` won't work (not JS). Instead verify with `grep -c 'href="/mark-'` matches the expected count (currently 8: favicon-16/32/48 + mark-96/128/144/152/180/192/512 = actually 3 favicon + 7 mark = 10; recount after edit) and `grep -c 'logo-h'` = 4 (unchanged).
  </action>
  <verify>
    <automated>grep -q 'theme_color%22%3A%22%2314110f' index.html &amp;&amp; grep -qE 'href="/mark-(180|192|512)\.png"' index.html &amp;&amp; grep -c 'logo-h200.png' index.html | grep -q '^3$' &amp;&amp; grep -c 'logo-h300.png' index.html | grep -q '^1$' &amp;&amp; grep -qi 'maskable' index.html</automated>
  </verify>
  <done>
    index.html head tags reference ONLY PNG filenames that now exist in queuenight/public/.
    Inline manifest icons array gains a maskable-purpose entry pointing at the new mark-*-maskable.png file.
    Inline manifest theme_color + background_color remain #14110f (verified via URL-encoded grep).
    Wordmark `<img class="brand-logo">` tags unchanged: 3 instances of logo-h200.png + 1 instance of logo-h300.png.
    If HTML changed, change is scoped to head + inline manifest only (diff does not touch body content).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Firebase Hosting | Regeneration + deploy path; user runs `firebase deploy` locally |
| Firebase Hosting → end-user device | PNG binary files + HTML head-tag references; cached per-device |
| iOS Safari standalone PWA → iOS home-screen | apple-touch-icon cached AT INSTALL TIME; survives hosting deploys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | SVG source files under brand/ | accept | Rationale: brand SVGs are public-by-design (wordmark is visible in logged-out HTML). No secrets. Git history provides integrity. |
| T-09-01-02 | Denial of Service | PNG file rename during regen | mitigate | Task 4 reconciles HTML head tags with regenerated filenames; mismatched references would break icon loading silently. Grep acceptance enforces presence. |
| T-09-01-03 | Information Disclosure | brand/ directory leaks design-in-progress | accept | Rationale: not a secret. Repo already public-by-intent. |
| T-09-01-04 | Spoofing | Attacker drops a look-alike icon into queuenight/public/ | accept | Rationale: requires attacker to already have deploy access (same trust boundary as code writes); out of scope for this phase. |
| T-09-01-05 | Denial of Service | Maskable variant missing or mis-padded — Android clips the mark | mitigate | brand/README.md rule 5 documents the 40% safe-zone with an ASCII diagram. Task 3 step 5 explicitly checks Android adaptive rendering. Task 4 ensures the inline manifest carries the `purpose=maskable` entry so the browser picks the correct variant. |

</threat_model>

<verification>
Post-deploy verification (manual, in Task 3 checkpoint):
- `brand/couch-mark.svg` + `brand/couch-wordmark.svg` + `brand/README.md` committed to this repo.
- Regenerated PNG set (including at least one `mark-*-maskable.png`) committed to `queuenight/public/` (sibling repo) and deployed via `firebase deploy --only hosting`.
- Desktop Chrome favicon + DevTools manifest preview reflect the refreshed mark and show BOTH `purpose: "any"` and `purpose: "maskable"` icon entries.
- Android Chrome "Add to Home Screen" pulls the refreshed mark; adaptive-icon rendering does not clip the mark.
- iOS Safari "Add to Home Screen" shows refreshed mark in preview AND home-screen icon renders sharp at ~120px after cache-bust.
- `grep -c 'style="' index.html` is unchanged from baseline (119 — plan 09-03 addresses this).
- `node -e "const m=require('fs').readFileSync('brand/couch-mark.svg','utf8'); if(!m.includes('viewBox')) process.exit(1)"` passes.
- `grep -qi 'maskable' index.html` passes (inline manifest now includes the purpose=maskable entry).
</verification>

<success_criteria>
DESIGN-01 closes — "Final logo + app icon set + favicon shipped in the repo; wired to manifest, head, and iOS/Android home-screen icons":
- ✓ Canonical SVG source committed (brand/couch-mark.svg + brand/couch-wordmark.svg).
- ✓ Full PNG asset set regenerated and deployed from sibling queuenight/public/ (including at least one maskable variant with 40% safe-zone).
- ✓ Wired to inline manifest (added purpose=maskable entry), HTML head tags (unchanged or additively extended), iOS home-screen (verified post cache-bust), Android home-screen (adaptive-icon-safe), desktop favicon.
- ✓ brand/README.md documents the regeneration pipeline + maskable safe-zone rules so future refreshes are ≤15 min of work.
- ✓ iOS standalone PWA theme color stays #14110f (no drift).
</success_criteria>

<risk_notes>
- **No bundler:** regeneration tooling is developer-machine one-shot (`npx --yes @vite-pwa/assets-generator`). Does NOT install, does NOT add to any package.json. If Node unavailable, fall back to RealFaviconGenerator.net browser workflow.
- **Two-repo sync:** SVG source lives in THIS repo under `brand/`; PNG outputs live in SIBLING `queuenight/public/`. Plan explicitly scopes each commit to its correct repo. Outputs committed to this repo would double the repo size and create confusion about source-of-truth.
- **iOS PWA primary surface:** iOS caches apple-touch-icon at install time. STATE.md cache-bust procedure is MANDATORY in verification; without it, user will think the icon deploy failed when it actually shipped.
- **Dynamic className logic preserved:** This plan only touches `<head>` + brand/ + sibling repo. Does NOT touch body markup, does NOT remove any inline style= attributes, does NOT change any classList-driven UI (act-as-active, is-readonly, wp-ontime-revert, .on toggle — all preserved until plan 09-03).
- **Palette drift tolerance:** css/app.css:7 has `--bg:#14110d` (NOT #14110f). This plan does NOT reconcile — plan 09-02 owns that one-line fix. Current inline-manifest uses the correct #14110f already.
- **Checkpoint-gated deploy:** Task 3 is checkpoint:human-verify, not auto. Rationale: `firebase deploy` affects production; iOS physical-device verification requires user action; researcher + planner both flag this as "must pause for user" per GSD automation-first rule.
- **Maskable icon addition:** per Minor-7 checker feedback + W3C adaptive-icon spec. Without a `purpose=maskable` entry, Android Chrome's adaptive icon system pads or clips the non-maskable variant unpredictably, and some launchers render the icon inside a default white circle that ignores our warm-dark theme. The maskable variant closes this silent UX gap.
- **DESIGN-01 partial-completion caveat (vectorize-defer path):** If the user selects "vectorize-defer" at Task 1, this plan ships a no-refresh regenerate path — the current PNG mark gets cleaned up into an SVG, existing filenames preserved, maskable variant produced. DESIGN-01's "Final logo" clause is only PARTIALLY met: icons ship + are wired, but the "visual identity refresh" that the ROADMAP goal calls for is deferred to a future v2 pass. The SUMMARY records this as `DESIGN-01: partial (regenerate-only; refresh deferred per user decision)` so downstream plans and Phase 9 closure reviewers know option-a/-b wasn't taken. Every other Phase 9 plan still closes normally (landing, marketing, etc.) — only the identity-artistry portion of DESIGN-01 defers. The user can initiate a v2 refresh later by rerunning this plan with option-a or option-b.
</risk_notes>

<output>
After completion, create `.planning/phases/09-redesign-brand-marketing-surface/09-01-SUMMARY.md`
</output>
