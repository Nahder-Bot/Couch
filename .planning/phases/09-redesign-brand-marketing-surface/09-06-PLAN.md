---
phase: 09-redesign-brand-marketing-surface
plan: 06
type: execute
wave: 3
depends_on: [09-01, 09-03]
files_modified:
  - landing.html
  - css/landing.css
  - .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md
autonomous: false
requirements: [DESIGN-06]
commit_strategy: "2 commits — (1) marketing-screenshot-plan.md capture + production instructions + OG image production, (2) landing.html + css/landing.css updates that wire real screenshots into the .screenshot-grid + add any additional OG/meta refinement. The raw PNG screenshots themselves commit in the sibling queuenight/public/ repo, not here."
batching_rationale: "Asset production (screenshots + OG image) is a developer-machine workflow; wiring those assets into landing is an HTML/CSS edit. Separating lets the screenshot-plan doc land first (reproducible capture pipeline) then the landing-HTML updates land once real images exist."
user_setup:
  - service: iPhone (physical) OR Chrome DevTools iPhone simulation at 1170x2532
    why: "Screenshot capture at App-Store-canonical dimensions. Real device preferred (higher fidelity, sharper text). Simulator acceptable."
  - service: AppScreens.com OR AppLaunchpad (browser-based, free tier)
    why: "Add device frames + optional marketing copy overlays to raw screenshots. No install required."
  - service: queuenight/public/marketing/ (sibling repo subdirectory)
    why: "PNG assets land in sibling repo under marketing/. This repo only holds production docs + HTML/CSS wiring."

must_haves:
  truths:
    - "A marketing-screenshot-plan.md documents the capture + production + commit pipeline so any future re-capture is ≤30 min of work"
    - "Five REQUIRED screenshots exist in queuenight/public/marketing/ (tonight-hero, watchparty-live, mood-filter, title-detail, intent-rsvp) at 1170x2532 PNG"
    - "Three OPTIONAL screenshots documented as stretch assets (year-in-review — gated on Phase 10 shipping; watchparty-desktop — post plan 09-04 proof; invite-redeem — post plan 09-07b)"
    - "An og.png (1200x630) exists in queuenight/public/ at the root, composed from the hero screenshot + branded background"
    - "The landing page .screenshot-grid placeholders are replaced with real <img> references to the 5 required marketing/ PNGs"
    - "Link-preview renders correctly in iMessage, Slack, and Twitter compose (tested post-deploy)"
    - "File size budget: each marketing PNG ≤ 400KB after optimization; og.png ≤ 300KB. Total marketing payload ≤ 3MB"
  artifacts:
    - path: ".planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md"
      provides: "Capture procedure, production pipeline, commit checklist for every future screenshot refresh — lists 5 required + 3 optional"
      min_lines: 60
    - path: "landing.html"
      provides: "Real screenshot references replace the .screenshot-placeholder divs (5 required images on the grid; optional assets slotted in when/if captured)"
      contains: "marketing/"
    - path: "css/landing.css"
      provides: "Any screenshot-specific styling refinements (aspect ratio, loading=lazy hint, hover)"
      contains: "screenshot"
  key_links:
    - from: "landing.html .screenshot-grid"
      to: "queuenight/public/marketing/*.png (5 required)"
      via: "src=\"/marketing/tonight-hero.png\" etc."
      pattern: "src=\"/marketing/"
    - from: "landing.html og meta"
      to: "queuenight/public/og.png"
      via: "<meta property=og:image content=...>"
      pattern: "og:image"
---

<objective>
Produce and wire App-Store-ready marketing assets. Capture 5 required screenshots at 1170x2532 (iPhone 14/15 Pro reference) showing Couch's signature surfaces, pass them through a browser-based device-framing tool (AppScreens or AppLaunchpad), commit the framed PNGs to sibling queuenight/public/marketing/, and wire them into the landing page's .screenshot-grid section (replacing plan 09-05's placeholders). Produce a single canonical og.png (1200x630) from the hero screenshot. Document the entire pipeline in a reproducible .md so any future refresh is a quick run. Three OPTIONAL assets (YIR teaser, desktop watchparty, invite-redeem) are tracked as stretch captures — YIR is gated on Phase 10 shipping the UI surface, so it's deferred to Phase 10 (Major-6 checker closure).

Purpose: DESIGN-06 closes. Landing page surface feels finished; link previews look right; the app is "App-Store-ready if we submitted tomorrow" even though no listing exists yet.

Output:
- New file: `.planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md` (capture + production + commit pipeline for 5 required + 3 optional)
- Sibling repo: `queuenight/public/marketing/` populated with 5 framed screenshot PNGs (required set)
- Sibling repo: `queuenight/public/og.png` (1200x630 hero composite)
- This repo: `landing.html` .screenshot-grid wires 5 real images (grid adapts cleanly to 5 — 1 col mobile, 3 at 900px, 5 at 1200px)
- This repo: `css/landing.css` any screenshot refinement (aspect-ratio, loading hints)
- 2 commits (doc+wiring / HTML+CSS)
- Checkpoint before deploy: user validates the 5 screenshots look right + og preview renders in iMessage/Slack
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-redesign-brand-marketing-surface/09-RESEARCH.md
@.planning/phases/09-redesign-brand-marketing-surface/09-01-SUMMARY.md
@.planning/phases/09-redesign-brand-marketing-surface/09-03-SUMMARY.md
@.planning/phases/09-redesign-brand-marketing-surface/09-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Research deliverables list (revised per Major-6 checker feedback — YIR moved to optional because Phase 10 UI has not shipped):

Required (5):
1. Tonight screen hero — picker card visible, poster row visible — 1170x2532
2. Watchparty live with reactions — 1170x2532
3. Mood filter active with narrowed results — 1170x2532
4. Title detail with vote grid — 1170x2532
5. Intent / Tonight-at-time RSVP card — 1170x2532

Optional / Stretch (3):
6. Year-in-Review teaser card — 1170x2532 (blocked until Phase 10 ships; NOT a Phase 9 requirement — Major-6 downgrade)
7. Desktop view of Watchparty live modal at ≥900px — 2560x1440 (proves plan 09-04 landed; nice-to-have, not required)
8. Invite redemption screen — 1170x2532 (blocked until plan 09-07b ships the redemption UI; defer or use the existing placeholder)

Production pipeline:
1. Capture raw screenshots on iPhone 14/15 Pro running the PWA standalone (or Chrome DevTools iPhone 14 Pro simulation at 1170x2532 device scale)
2. Clean status bar (remove battery/time indicator clutter) in any image editor or within AppScreens
3. Frame + caption in AppScreens.com or AppLaunchpad.com (free tier, browser-based, no account needed)
4. Export as PNG 72dpi (App Store minimum) or 144dpi (Retina)
5. Optimize via squoosh.app or ImageOptim to get under 400KB per file
6. Commit to queuenight/public/marketing/ with lowercase hyphen-separated names

OG image production:
- 1200x630 composite, branded background with the Tonight hero screenshot mocked into a device frame on the right, wordmark + tagline on the left
- Output as /og.png at repo root level (not under marketing/; it's referenced by og meta as /og.png)

Naming convention for marketing/:
- tonight-hero.png (required)
- watchparty-live.png (required)
- mood-filter.png (required)
- title-detail.png (required)
- intent-rsvp.png (required)
- (optional) year-in-review.png — gated on Phase 10
- (optional) watchparty-desktop.png
- (optional) invite-redeem.png — gated on plan 09-07b
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write marketing-screenshot-plan.md capture + production doc (5 required + 3 optional)</name>
  <files>
    .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md
  </files>
  <behavior>
    - A reproducible capture + production + commit pipeline doc exists under the phase dir.
    - Lists all 5 required screenshots with source screen + key visual elements to highlight.
    - Lists the 3 optional screenshots with dependency callouts (YIR gated on Phase 10, invite-redeem gated on plan 09-07b, watchparty-desktop stretch).
    - Documents the framing tool (AppScreens primary, AppLaunchpad alt), device-frame preset (iPhone 15 Pro), optimization step, and naming convention.
  </behavior>
  <action>
    Create `.planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md` with this structure. Keep it practical and reference-oriented, not narrative.

```
# Phase 9 Marketing Screenshot Production Pipeline

Capture + frame + optimize + commit procedure for the App-Store-ready marketing asset set. Every future refresh (new features, rebrand, App Store submission) reruns this playbook.

## Required assets (5)

| # | Filename | Screen | Key elements | Notes |
|---|----------|--------|-------------|-------|
| 1 | tonight-hero.png | Tonight | who-mini strip, picker card with 3+ member chips, mood filter chip, poster row with at least 4 posters | HERO SHOT — most-visited surface. Everything must feel alive (real votes, real moods). |
| 2 | watchparty-live.png | Watchparty live modal (during active session) | Participant timer strip with 3+ participants, reaction palette visible, one recent reaction bubble above the feed | Requires an active watchparty; capture on the "starter" phone with 2 other devices joined. |
| 3 | mood-filter.png | Tonight with mood filter applied | Active mood row showing 2-3 moods (e.g. "cozy", "short", "action"), filtered poster row showing narrower results vs tonight-hero.png | Contrasts with #1 to communicate filtering |
| 4 | title-detail.png | Title detail modal | Poster hero, title + year, providers row, mood pills visible, vote grid (Yes/Maybe/No) with at least one vote cast | Pick a title with rich metadata (lots of moods, multiple providers) |
| 5 | intent-rsvp.png | Intent RSVP modal | "Tonight at 9pm" card with 3+ member RSVPs (mix of Yes/Maybe/No) | Captures the Phase 8 differentiator |

## Optional / Stretch assets (3)

| # | Filename | Screen | Dependency | Notes |
|---|----------|--------|------------|-------|
| 6 | year-in-review.png | YIR teaser card on Tonight OR first real YIR screen from Phase 10 | **Phase 10 (YIR) ships** | **Deferred per Major-6 checker closure.** Phase 10 has not shipped any YIR UI — capturing a placeholder contradicts "App-Store-ready". Capture this during Phase 10 execution using the same pipeline (rerun this doc). |
| 7 | watchparty-desktop.png | Watchparty live on desktop (>=900px viewport) | Plan 09-04 shipped | Proves the responsive expansion works; 2560x1440. Stretch — not required for DESIGN-06 closure. |
| 8 | invite-redeem.png | Guest invite redemption screen | Plan 09-07b ships the redemption UI | Deferred until 09-07b lands; rerun pipeline then. |

## Capture pipeline

### Step 1 — Capture raw screenshots

**Preferred: physical iPhone 14/15 Pro running the standalone PWA.**
- Launch installed PWA from home screen.
- Set up the scenario in the app (cast a few votes, join a watchparty, etc.). Use the same dev family with realistic content.
- Take screenshots with iPhone (volume up + side button). Raw output is 1170x2532 (or 1290x2796 for 15 Pro Max — rescale in step 2).
- Transfer via AirDrop or iCloud. DO NOT use screen mirroring to capture (quality degrades).

**Fallback: Chrome DevTools iPhone 14 Pro simulation.**
- DevTools → Toggle device toolbar → "iPhone 14 Pro" → 393x852 viewport at 3x DPR (captured 1179x2556).
- Navigate to couchtonight.app/app, sign in on the dev family, set up each scenario.
- Three-dot menu in DevTools → "Capture full-size screenshot" or "Capture screenshot".
- Raw resolution may differ from 1170x2532 — resize to 1170x2532 exactly in step 2 or the frame tool.

### Step 2 — Clean + resize raw captures

- Crop out the iOS status bar area (time / battery / reception icons) OR use AppScreens' built-in status bar removal.
- If raw output isn't exactly 1170x2532, resize with an image editor (preserve aspect ratio; pad with #14110f background if the source aspect differs).

### Step 3 — Frame + caption in AppScreens.com

1. Visit https://appscreens.com (no account needed for basic exports).
2. Upload raw screenshot.
3. Select iPhone 15 Pro device frame (Titanium Black variant — closest to the warm dark aesthetic).
4. Optional: add a one-line caption above the frame (e.g. "Tonight, together"). Keep typography subtle — Inter 400, 32px, ink-warm color.
5. Background: use #14110f solid OR a subtle brand gradient (color stops e8a04a -> d97757 -> 8b3a4e at 8% opacity overlay).
6. Export at full App-Store resolution (1242x2688 or 1170x2532 depending on template). We pick 1170x2532 for the committed files.

**Alternative: AppLaunchpad.com** — same workflow, slightly different template library. Use if AppScreens has rate-limits.

### Step 4 — Optimize

- Run each PNG through https://squoosh.app (free, browser-based) with "OxiPNG" effort level 4 OR ImageOptim locally.
- Target: under 400KB per screenshot. If a file is over, reduce frame-background gradient fidelity or crop unneeded padding.

### Step 5 — Commit to sibling queuenight/public/marketing/

- Create `queuenight/public/marketing/` if missing.
- Copy all 5 required optimized PNGs with their canonical filenames.
- `cd queuenight && git add public/marketing/ && git commit -m "chore(marketing): Phase 9 App-Store-ready screenshot set (5 required)"`.
- Do NOT commit to THIS repo.
- If/when optional assets are captured (Phase 10 for YIR, post-09-07b for invite-redeem, opportunistic for desktop watchparty): rerun this pipeline and commit additional files in a separate commit labeled accordingly.

## OG image (1200x630) production

The landing page references /og.png for link previews.

- Composition: vertical split layout. LEFT half: wordmark (Couch, 200px height) + tagline ("Who's on the couch tonight?" Instrument Serif italic 28px) on warm dark background. RIGHT half: tonight-hero.png in an iPhone 15 Pro frame, rotated slightly (5 degrees) for dynamic feel.
- Tool: AppScreens "social preview" template OR any design tool the user has (Figma, Canva, Photopea browser-based).
- Export 1200x630 PNG. Optimize via squoosh to under 300KB.
- Commit to queuenight/public/og.png (root, not under marketing/).

## Acceptance (Phase 9 DESIGN-06 closure)

- 5 required PNGs in queuenight/public/marketing/ at 1170x2532 each
- 1 og.png in queuenight/public/ at 1200x630
- All files under the 400KB / 300KB budgets
- All filenames match the required table above (lowercase hyphen-separated)
- landing.html references updated to use the 5 real images (plan 09-06 task 3)
- iMessage / Slack / Twitter preview tests pass post-deploy

Optional assets do NOT block Phase 9 closure. They are tracked here so the pipeline can be rerun when their blocking dependencies resolve.

## Refresh cadence

- On every brand refresh (new logo, new palette) — rerun all 5 required captures + og composite
- On every new feature visible in a screenshot surface — re-capture affected frames only
- When Phase 10 ships YIR UI — capture asset #6 and add it to the landing grid
- When plan 09-07b ships invite redemption — capture asset #8 and (optionally) add to grid
- Annual "state of the product" refresh is a reasonable default

## Anti-patterns (do NOT do)

- Do NOT hand-build screenshot frames in HTML/CSS + Playwright. It is tempting (in-repo, reproducible) but will eat days of CSS tuning. Stick with AppScreens.
- Do NOT add App Store captions like "Free forever!" — contradicts "Warm, Cinematic, Lived-in. Restraint is the principle."
- Do NOT use motion-blur or over-saturated color grading in the frames. The raw warm-dark app palette IS the mood.
- Do NOT auto-rotate multiple frames inside one asset (parallax gallery). One screenshot per PNG. Clean.
- Do NOT include a placeholder YIR screenshot captured against Phase 9 mock UI — wait for real Phase 10 surfaces. The required set is only 5 for exactly this reason (Major-6 checker closure).

## Notes

- Current deploy layout post-plan-09-05: landing.html at /, app.html at /app. Marketing assets served from /marketing/* path. OG image at /og.png.
- If AppScreens changes its free-tier limits, fall back to AppLaunchpad. Both produce identical output quality for our use case.
- No Figma license assumption — everything here is browser-based + free.
```

    Commit message: `docs(09-06): marketing screenshot production pipeline doc (5 required + 3 optional)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q 'Required assets (5)' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q 'Optional / Stretch assets (3)' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q 'OG image' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q 'AppScreens' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q '1170x2532' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md &amp;&amp; grep -q 'Phase 10 (YIR) ships' .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md</automated>
  </verify>
  <done>
    marketing-screenshot-plan.md created with capture + framing + optimization + commit procedure.
    Required table has exactly 5 rows; optional table has 3 (YIR, desktop, invite-redeem).
    OG image composition documented.
    Anti-patterns section warns against hand-rolled Playwright captures AND against shipping placeholder YIR captures.
    Commit 1 of 2 landed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User captures, frames, optimizes, and commits the 5 required screenshots + og.png</name>
  <what-built>
    marketing-screenshot-plan.md lists the exact 5 required screens + filenames + sizes. Claude cannot capture on a physical iPhone nor run the AppScreens browser tool. User executes the pipeline.
  </what-built>
  <how-to-verify>
    Follow the .md doc created in task 1. High-level steps:
    1. Set up each of the 5 required scenarios in the dev family (real votes, real watchparty, mood filter active, intent RSVP with multiple replies).
    2. Capture raw screenshots (iPhone preferred, DevTools simulation OK).
    3. Run each through AppScreens or AppLaunchpad with iPhone 15 Pro Titanium Black device frame + warm-dark background.
    4. Optimize via squoosh.app under the 400KB budget.
    5. Create queuenight/public/marketing/ and copy all 5 PNGs with canonical filenames.
    6. Produce og.png (1200x630) using the landing hero composition described in the doc.
    7. Commit to sibling repo: `cd queuenight && git add public/marketing/ public/og.png && git commit -m "chore(marketing): Phase 9 App-Store-ready asset set (5 required)"`.
    8. DO NOT deploy yet — task 3 wires the references in landing.html; deploy happens once in the final checkpoint.
    9. OPTIONAL: if you're feeling generous and plan 09-04 is live, capture asset #7 (watchparty-desktop.png) as a bonus. Skip assets #6 (YIR, blocked on Phase 10) and #8 (invite-redeem, blocked on 09-07b).

    Quality check:
    - Every screenshot is recognizably "Couch" (wordmark visible somewhere, warm-dark palette obvious).
    - Every screenshot feels lived-in (real content, multiple member chips, actual posters — not empty states).
    - No status-bar clutter (battery, time, reception icons clean or hidden).
    - File sizes all within budget.
  </how-to-verify>
  <resume-signal>
    Type "captured" when all 5 required screenshots + og.png exist in sibling queuenight/public/marketing/ + queuenight/public/og.png and committed to the sibling repo. Type "captured +desktop" if you bagged the optional watchparty-desktop.png too. Type "need AppScreens alternative" if AppScreens is rate-limited and AppLaunchpad behaves differently.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire real screenshots into landing.html + tighten css/landing.css (5 images)</name>
  <files>
    landing.html
    css/landing.css
  </files>
  <behavior>
    - landing.html .screenshot-grid replaces placeholder divs with real img tags referencing 5 /marketing/* PNGs.
    - css/landing.css adjusts .screenshot-card aspect-ratio and adds loading=lazy hint for perf.
    - Grid adapts: 1 column on mobile, 3 at 900px, 5 at 1200px.
    - OG meta content="https://couchtonight.app/og.png" already present; verify.
  </behavior>
  <action>
    Step A — Edit landing.html .screenshot-grid section (from plan 09-05). Replace the 4 placeholder divs with 5 real img references.

    Before (from plan 09-05):
```
<div class="screenshot-grid">
  <div class="screenshot-card"><div class="screenshot-placeholder">Tonight</div></div>
  <div class="screenshot-card"><div class="screenshot-placeholder">Watchparty</div></div>
  <div class="screenshot-card"><div class="screenshot-placeholder">Mood filter</div></div>
  <div class="screenshot-card"><div class="screenshot-placeholder">Year-in-Review</div></div>
</div>
```

    After (5 required — YIR deferred to Phase 10 per Major-6 closure):
```
<div class="screenshot-grid">
  <figure class="screenshot-card"><img src="/marketing/tonight-hero.png" alt="Tonight screen: picker card with member chips and poster row" loading="lazy" width="1170" height="2532"><figcaption>Tonight</figcaption></figure>
  <figure class="screenshot-card"><img src="/marketing/watchparty-live.png" alt="Watchparty live: participant timer strip with reactions feed" loading="lazy" width="1170" height="2532"><figcaption>Watchparty</figcaption></figure>
  <figure class="screenshot-card"><img src="/marketing/mood-filter.png" alt="Mood filter active: cozy and short moods narrowing the poster row" loading="lazy" width="1170" height="2532"><figcaption>Mood filter</figcaption></figure>
  <figure class="screenshot-card"><img src="/marketing/title-detail.png" alt="Title detail: poster, providers, mood pills, and vote grid" loading="lazy" width="1170" height="2532"><figcaption>Title detail</figcaption></figure>
  <figure class="screenshot-card"><img src="/marketing/intent-rsvp.png" alt="Tonight-at-time intent: RSVP card with member replies" loading="lazy" width="1170" height="2532"><figcaption>Tonight at time</figcaption></figure>
</div>
```

    When Phase 10 ships: append a 6th figure for year-in-review.png. When plan 09-07b ships: optionally append invite-redeem. Both follow the same structure.

    Step B — Update css/landing.css .screenshot-grid + .screenshot-card rules:
    - .screenshot-card already has `aspect-ratio: 9 / 19.5` from plan 09-05. Change to `aspect-ratio: 1170 / 2532` for exact pixel fidelity (same ratio, more explicit about the source asset).
    - Add styling for the new `<figcaption>`:

```
.screenshot-card { position: relative; aspect-ratio: 1170 / 2532; overflow: hidden; }
.screenshot-card img { width: 100%; height: 100%; object-fit: cover; display: block; }
.screenshot-card figcaption { position: absolute; left: 0; right: 0; bottom: 0; padding: 12px; background: linear-gradient(to top, rgba(20,17,15,0.85), rgba(20,17,15,0)); color: var(--ink-warm); font-family: 'Instrument Serif', serif; font-style: italic; font-size: 14px; text-align: center; }
```

    - Adjust .screenshot-grid breakpoints so 5 images fit cleanly. Update the @media (min-width: 900px) rule in landing.css:

```
@media (min-width: 900px) {
  .screenshot-grid { grid-template-columns: repeat(3, 1fr); }
}
@media (min-width: 1200px) {
  .screenshot-grid { grid-template-columns: repeat(5, 1fr); }
}
```

    (At 900px, 3 cols — images 4 + 5 wrap onto a second row of 2; visually balanced. At 1200px, 5 cols — all in a single row. Acceptable. If a future YIR screenshot is added to make 6, bump the 1200px rule to `repeat(6, 1fr)`.)

    The existing `.screenshot-grid { grid-template-columns: repeat(4, 1fr); }` at @media 900px from plan 09-05 gets REPLACED with repeat(3). A new 1200px breakpoint adds repeat(5) for wide desktops.

    Step C — Verify og meta in landing.html head still references `/og.png` (added by plan 09-05). No change needed unless content was different.

    Step D — Remove the old .screenshot-placeholder CSS rule since it no longer has a target:
    - Before: `.screenshot-placeholder { color: var(--ink-dim); font-family: 'Instrument Serif', serif; font-style: italic; font-size: 18px; }`
    - Action: delete this rule.

    Commit message: `feat(09-06): wire 5 real marketing screenshots into landing .screenshot-grid (YIR deferred to Phase 10)`.
  </action>
  <verify>
    <automated>grep -q '/marketing/tonight-hero.png' landing.html &amp;&amp; grep -q '/marketing/watchparty-live.png' landing.html &amp;&amp; grep -q '/marketing/mood-filter.png' landing.html &amp;&amp; grep -q '/marketing/title-detail.png' landing.html &amp;&amp; grep -q '/marketing/intent-rsvp.png' landing.html &amp;&amp; ! grep -q '/marketing/year-in-review.png' landing.html &amp;&amp; grep -q 'loading="lazy"' landing.html &amp;&amp; grep -q 'aspect-ratio: 1170' css/landing.css</automated>
  </verify>
  <done>
    5 screenshot img tags in landing.html with alt text + loading=lazy + explicit dimensions.
    Year-in-review img is INTENTIONALLY absent (Major-6 closure — captured in Phase 10).
    css/landing.css aspect-ratio updated to 1170/2532.
    figcaption styled via gradient-overlay.
    Grid: 1 col mobile / 3 at 900px / 5 at 1200px.
    Old .screenshot-placeholder CSS rule removed.
    Commit 2 of 2 landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Deploy + verify link previews + landing.html renders with real screenshots</name>
  <what-built>
    Marketing asset set (5 required) captured + committed to sibling repo. landing.html wired to real PNG references. Ready to deploy + verify.
  </what-built>
  <how-to-verify>
    1. Copy updated landing.html + css/landing.css from this repo to queuenight/public/.
    2. `cd queuenight && git add public/landing.html public/css/landing.css && git commit -m "deploy(09-06): wire marketing assets into landing" && firebase deploy --only hosting`.
    3. Open https://couchtonight.app/ in incognito Chrome. Scroll to "See it in use" — verify all 5 screenshots load, are sharp, and figcaptions display at the bottom of each frame.
    4. DevTools Network panel: verify all 5 PNGs load under 400KB. Lighthouse Performance still ≥ 90 (same target as 09-05 post Minor-12 relaxation; image payload at lazy-load shouldn't drop it below 90).
    5. DevTools responsive mode: 1 column on mobile, 3 columns at 900px (rows: 3 + 2), 5 columns at 1200px (single row).
    6. OG preview test (pick two):
       a. iMessage: paste https://couchtonight.app into a chat draft. Preview should show og.png (warm dark wordmark + device-framed tonight-hero). Headline "Couch" and description visible.
       b. Slack: same test in a Slack compose. Same preview.
       c. Twitter card validator: https://cards-dev.twitter.com/validator (may need login) — paste URL, confirm summary_large_image card renders.
    7. Mobile device test: open landing in iPhone Safari. Screenshots visible, taps don't trigger any JS errors, tapping "Pull up a seat" still forwards to /app correctly.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 screenshots load + OG preview renders + Lighthouse still at 90+. Type "og preview broken: <platform>" if iMessage/Slack/Twitter render the wrong image or no image. Type "screenshot <n> off" if any specific screenshot needs recapture. Type "lighthouse dropped below 90" if image payload is too heavy — Claude can recommend re-optimization.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screenshot capture source → public marketing surface | Raw app screenshots become public assets |
| landing.html static references → Firebase Hosting PNG serve | Marketing PNGs crawlable by any visitor |
| OG crawlers (iMessage, Slack, Twitter) → og.png | Third-party services fetch + cache the OG image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Information Disclosure | Screenshots contain real user data (family names, real votes, personal watchlists) | mitigate | User captures from dev family only. Doc explicitly says "use the dev family with realistic content" — not personal/private family data. Alternative: create a demo family before capturing. |
| T-09-06-02 | Information Disclosure | Screenshots contain Firebase config or TMDB key in visible text | accept | Rationale: both keys are public-by-design per PROJECT.md. No secret exposure from screenshots. |
| T-09-06-03 | Denial of Service | Marketing PNG payload makes landing Lighthouse performance drop below acceptable threshold | mitigate | Per-file 400KB budget + squoosh optimization step. loading=lazy on all screenshot images. Verification task checks Lighthouse ≥ 90. |
| T-09-06-04 | Tampering | A screenshot file is corrupted during transfer and renders broken | mitigate | Verify task 4 step 3 explicitly checks all 5 load. Browser fallback: broken <img> shows alt text, not crash. |
| T-09-06-05 | Spoofing | Crawler caches old og.png after refresh | accept | Standard OG cache behavior. Refresh cycles: Twitter ~7d, iMessage ~1d, Slack permanent until manual unfurl refresh. User can force-refresh via Twitter card validator if critical. |
| T-09-06-06 | Denial of Service | Placeholder YIR screenshot captured against mock/empty UI ships and undermines "App-Store-ready" claim | mitigate | Major-6 closure. YIR is OPTIONAL and explicitly blocked on Phase 10 shipping real UI. Anti-patterns section in the production doc forbids placeholder captures. Grid renders 5 images cleanly at every breakpoint. |

</threat_model>

<verification>
End-of-plan checks:
- `test -f .planning/phases/09-redesign-brand-marketing-surface/marketing-screenshot-plan.md` passes.
- `grep -c 'src="/marketing/' landing.html` returns exactly 5 (not 4, not 6 — the 5 required).
- `! grep -q '/marketing/year-in-review.png' landing.html` passes (YIR absent per Major-6).
- `grep -q 'loading="lazy"' landing.html` passes.
- `grep -q 'aspect-ratio: 1170' css/landing.css` passes.
- Task 4 user-verification passes all 7 points (5 screenshots load + OG preview + responsive grid + Lighthouse ≥ 90 + mobile Safari render).
- Screenshot file-size budget honored: each under 400KB, og.png under 300KB.
</verification>

<success_criteria>
DESIGN-06 closes — "App-Store-ready marketing asset set (hero screenshot, feature screenshots, icon variants, promo graphics) produced and stored alongside the landing page":
- ✓ 5 required screenshots at 1170x2532 in queuenight/public/marketing/.
- ✓ og.png at 1200x630 in queuenight/public/.
- ✓ Wired into landing.html .screenshot-grid with alt text + loading hints.
- ✓ iMessage / Slack / Twitter previews render correctly with the new OG image.
- ✓ Reproducible production pipeline documented for future refreshes — including when to capture the 3 optional assets as their dependencies resolve.
- ✓ YIR screenshot deferred to Phase 10 (Major-6 closure); doc captures the re-run plan.
</success_criteria>

<risk_notes>
- **No bundler:** assets are raw PNGs. HTML references them via static <img src>. No build pipeline.
- **Two-repo sync:** Screenshot PNGs live in sibling queuenight/public/marketing/ — never in this repo. Production doc (marketing-screenshot-plan.md) + HTML wiring (landing.html) + CSS (css/landing.css) live here.
- **iOS PWA primary surface preserved:** landing.html's install redirect from plan 09-05 still fires for standalone visitors. Screenshot additions are below-fold content — no impact on the inline redirect execution path.
- **Performance budget defense:** 400KB per screenshot + loading=lazy + object-fit cover. Lighthouse should stay ≥ 90 even with 5 large-looking assets, because lazy loading defers off-screen images and each is optimized. (Target relaxed to 90 per 09-05 Minor-12; 95 is stretch via self-hosted fonts.)
- **YIR deferred to Phase 10 (Major-6 closure):** Checker flagged that asset #5 (YIR) had no shipped source UI — capturing a placeholder contradicts "App-Store-ready". Downgraded to optional. The required set is 5, not 6. When Phase 10 ships the YIR surfaces, rerun this pipeline to capture asset #6 + optionally add it to the landing grid.
- **Invite-redeem screenshot deferred:** #8 blocked on plan 09-07b shipping the redemption UI. Document explicitly. If user wants a 6th screenshot post-09-07b, that's a screenshot-only refresh rerun of this pipeline.
- **Checkpoint:human-action for capture:** Claude cannot drive a physical iPhone or use browser-based AppScreens. Task 2 is explicit user-action. Document was written by Claude in task 1 to give the user a reproducible playbook.
- **Dynamic className logic preserved:** zero JS changes. zero app-behavior changes. Pure asset + marketing-surface work.
- **Landing perf regression defense:** if Lighthouse drops from 09-05's ≥90 baseline, Claude runs a follow-up re-optimization pass. Reasonable target with 5 screenshots is 88-92.
</risk_notes>

<output>
After completion, create `.planning/phases/09-redesign-brand-marketing-surface/09-06-SUMMARY.md`
</output>
