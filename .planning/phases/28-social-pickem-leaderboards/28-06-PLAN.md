---
phase: 28-social-pickem-leaderboards
plan: 06
type: execute
wave: 4
depends_on:
  - 28-03  # gameResultsTick + pickReminderTick CFs ready to deploy
  - 28-04  # firestore.rules + rules-tests ready to deploy
  - 28-05  # UI surface ready; sw.js bump triggers PWA cache invalidation
files_modified:
  - scripts/smoke-pickem.cjs                                      # raise FLOOR=1 → FLOOR=13 (production-code sentinel count gate)
  - sw.js                                                         # CACHE = 'couch-v47-pickem' (auto-bumped via deploy.sh)
  - .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md  # NEW — 11 device-UAT scripts
autonomous: false   # cross-repo deploy + cache bump touch production; user must approve via deploy.sh argv
requirements:
  - PICK-28-21  # smoke-pickem.cjs FLOOR meta-assertion locked at >=13 production-code sentinels (mirrors RPLY-26-17 pattern)
  - PICK-28-28  # sw.js cache bump to couch-v47-pickem via `bash scripts/deploy.sh 47-pickem` (auto-bumps + commits + mirrors + deploys)
  - PICK-28-29  # cross-repo deploy ritual: queuenight functions FIRST → queuenight rules → couch hosting
  - PICK-28-30  # 28-HUMAN-UAT.md scaffold with 11 device-UAT scripts covering all PICK-28-* requirements + D-17 update 2 explicit "no UFC variant" verification

must_haves:
  truths:
    - "smoke-pickem.cjs FLOOR meta-assertion gates the deploy: total production-code sentinel count must be >= 13 OR the deploy aborts (mirrors Phase 26 RPLY-26-17 pattern). Helper-behavior assertions (Groups 1-4b — slateOf/scorePick/etc.) DO NOT count toward the floor; only production-code sentinels (Groups 5-8: 18+4+14 = 36 sentinels) count."
    - "queuenight Cloud Functions deploy lands BEFORE the couch hosting deploy. Order matters: gameResultsTick + pickReminderTick must exist server-side before the client UI starts writing picks (else the first pick never settles)."
    - "queuenight Firestore rules mirror is in sync via `bash scripts/deploy.sh --sync-rules` (Phase 30 Wave 4 process gate); Phase 28 picks/leaderboards/picks_reminders rules MUST be live in production before any client write attempt"
    - "sw.js CACHE bumps from 'couch-v46-wave-5-hotfix' (current production per STATE.md 2026-05-04) to 'couch-v47-pickem' (next in sequence). PWA installs auto-revalidate on next online activation."
    - "Production curl verifies the new CACHE literal at couchtonight.app post-deploy (mirrors Phase 27 v39 + Phase 30 v41 verification ritual)"
    - "28-HUMAN-UAT.md exists with at least 11 numbered device-UAT scripts covering: (a) Pick'em tab discoverable from Tonight tab inline link; (b) team_winner picker variant; (c) team_winner_or_draw picker variant including draw chip; (d) f1_podium picker with Jolpica roster + fail-soft; (e) D-09 pre-fill from teamAllegiance; (f) D-04 lock at gameStartTime; (g) inline wp pick row visibility/teardown; (h) leaderboard render + rank ordering; (i) past-seasons archive hide-when-empty; (j) push reminder T-15min; (k) NO UFC variant explicitly verified — open Pick'em tab, scroll all leagues, confirm zero UFC card per D-17 update 2"
  artifacts:
    - path: scripts/smoke-pickem.cjs
      provides: locked FLOOR=13 production-code sentinel gate
      contains:
        - "const FLOOR = 13"
        - "productionCodeAssertions"
    - path: sw.js
      provides: cache version bump to couch-v47-pickem
      contains:
        - "const CACHE = 'couch-v47-pickem'"
    - path: .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md
      provides: 11 numbered device-UAT scripts; resume signal "uat passed" → /gsd-verify-work 28
      min_lines: 200
      contains:
        - "Script 1"
        - "Script 11"
        - "no UFC variant"
        - "couch-v47-pickem"
        - "Jolpica"
  key_links:
    - from: scripts/smoke-pickem.cjs FLOOR meta-assertion
      to: scripts/deploy.sh §2.5 smoke gate (aborts deploy on smoke fail)
      via: process.exit(1) when productionCodeAssertions < FLOOR
      pattern: "process.exit\\(1\\)"
    - from: sw.js CACHE
      to: PWA cache invalidation (browser auto-revalidate on next online activation)
      via: service worker install/activate event
      pattern: "couch-v47-pickem"
    - from: scripts/deploy.sh
      to: queuenight/public/ mirror + firebase deploy --only hosting
      via: standard cross-repo ritual (RUNBOOK §H)
      pattern: "couch-v47-pickem"
    - from: 28-HUMAN-UAT.md Script 11 (NO UFC variant)
      to: D-17 update 2 lock in 28-CONTEXT.md
      via: explicit user-facing verification that no UFC card renders in Pick'em tab
      pattern: "no UFC"
---

<objective>
Wave 4 phase close-out — smoke FLOOR lock + cross-repo deploy + sw.js cache bump + UAT scaffold.

Purpose: Ship Phase 28 to production. Three coordinated outcomes:

1. **Smoke gate hardening** — FLOOR meta-assertion in `scripts/smoke-pickem.cjs` raises from
   the placeholder `FLOOR = 1` to the locked `FLOOR = 13` production-code sentinels (mirrors
   Phase 26 RPLY-26-17 + Phase 27 RSVP-27-17 pattern). After Plans 28-03/04/05, the actual
   sentinel count is 36 (Group 5: 9 + Group 6: 9 + Group 7: 4 + Group 8: 14) — well above the
   13-floor. The floor exists to catch future regressions if a refactor accidentally removes
   sentinel groups.

2. **Cross-repo deploy ritual** (cannot be auto-executed; requires user approval at the
   `bash scripts/deploy.sh 47-pickem` step). Sequence (RUNBOOK §H + Phase 30 Wave 4 gate):
   - `firebase deploy --only functions` from `~/queuenight` (lands gameResultsTick + pickReminderTick)
   - Verify rules mirror via `bash scripts/deploy.sh --sync-rules 47-pickem` (auto-syncs queuenight rules + deploys + continues with hosting)
   - This single command also bumps `sw.js` CACHE → `couch-v47-pickem`, mirrors all assets to
     `~/queuenight/public/`, runs all 11 smoke contracts (including the new FLOOR=13 gate),
     and runs `firebase deploy --only hosting`.
   - Production curl-verifies the new cache literal at couchtonight.app (mirrors Phase 27 v39 ritual).

3. **HUMAN-UAT scaffold** — `28-HUMAN-UAT.md` with 11 device-UAT scripts covering all PICK-28-*
   requirements + the explicit "NO UFC variant" verification per D-17 update 2.

**Critical scope deltas vs the original (now-deleted) Plan 28-06:**

1. **Cache target = `couch-v47-pickem`** (NOT the original `couch-v40-pickem`). The original
   plan was authored 2026-05-02 when production was at v39-guest-rsvp. Since then, Phase 30
   shipped v41-couch-groups and 5 hotfix waves carried us through v42 → v43 → v44 → v45 → v46
   per STATE.md 2026-05-04. Phase 28 picks up the next sequential cache value: **v47-pickem**.

2. **smoke FLOOR = 13** (locked). Total production-code sentinels post-Plans 03/04/05 = 36
   (Group 5: 9 lockstep + Group 6: 9 CF + Group 7: 4 rules + Group 8: 14 UI = 36). Well above
   the 13 floor; floor catches future regression if a refactor removes a sentinel group.

3. **UAT script 11 explicitly tests "NO UFC variant"** — open Pick'em tab on a real device,
   scroll all 15 league sections (NBA/NFL/MLB/NHL/WNBA/NCAAF/NCAAB/EPL/LaLiga/Bundesliga/SerieA/Ligue1/UCL/MLS/F1),
   confirm ZERO UFC card renders. This is the user-facing verification of the D-17 update 2 scope drop.

4. **UAT script 4 covers Jolpica F1** — both happy path (race entry list renders 22 drivers in
   3 dropdowns) and fail-soft (toggle airplane mode mid-render → "Race entry list unavailable;
   check back later." renders + other leagues' pickers still work).

5. **NO UFC fail-soft script** — would be testing absent functionality. The negative-sentinel
   smoke assertion (8.H from Plan 28-05) is sufficient at the production-code layer.

This plan is `autonomous: false` because Tasks 6.2 + 6.3 require user approval before deploying
to production. Task 6.1 (smoke FLOOR lock) is autonomous; Task 6.4 (UAT scaffold) is autonomous.

Output:
- `scripts/smoke-pickem.cjs` (modified): FLOOR=1 → FLOOR=13 + productionCodeAssertions calculation.
- `sw.js` (auto-bumped via deploy.sh): `const CACHE = 'couch-v47-pickem';`
- `.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md` (NEW, ~250 lines): 11 scripts.
- 28-06-SUMMARY.md (post-execute) records the production deploy curl-receipt + smoke-gate green
  + rules-tests count + cross-repo commit hashes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-social-pickem-leaderboards/28-CONTEXT.md
@.planning/phases/27-guest-rsvp/27-HUMAN-UAT.md
@.planning/phases/30-couch-groups-affiliate-hooks/30-HUMAN-UAT.md
@scripts/deploy.sh
@sw.js

<interfaces>
<!-- Current sw.js (line 8): -->
```js
const CACHE = 'couch-v46-wave-5-hotfix';
```
Target: `const CACHE = 'couch-v47-pickem';` (auto-bumped via `bash scripts/deploy.sh 47-pickem`).

<!-- Current smoke-pickem.cjs floor block (post-Plan 28-05 — Group 9 floor): -->
```js
{
  const FLOOR = 1; // placeholder — Plan 28-06 raises to 13
  if (passed >= FLOOR) {
    console.log(`  ok floor met (${passed} >= ${FLOOR})`);
    passed++;
  } else { ... }
}
```

<!-- Target floor block (FLOOR=13 + production-code-sentinel calculation per smoke-guest-rsvp.cjs convention): -->
```js
{
  const FLOOR = 13;  // Plan 28-06 lock — mirrors RPLY-26-17 pattern.
  // Helper-behavior assertions from Groups 1-4b are pure-helper tests; FLOOR counts
  // PRODUCTION-CODE sentinels only (Groups 5-8: cross-repo lockstep + CF sentinels +
  // Firestore rules sentinels + UI render-function sentinels).
  // After Plans 28-03/04/05, count is 36; well above 13 floor. Floor exists to catch
  // future regressions if a refactor accidentally removes sentinel groups.
  const HELPER_ASSERTIONS = 27;  // Group 1 (6) + Group 2 (3) + Group 3 (8) + Group 4 (4) + Group 4b (2) + Constants (2) + 2 misc
  const productionCodeAssertions = passed - HELPER_ASSERTIONS;
  if (productionCodeAssertions >= FLOOR) {
    console.log(`  ok floor met (${productionCodeAssertions} production-code sentinels >= ${FLOOR})`);
    passed++;
  } else {
    console.error(`  FAIL floor NOT met (${productionCodeAssertions} < ${FLOOR})`);
    failed++;
  }
}
```

<!-- HUMAN-UAT scaffold pattern (mirrors 27-HUMAN-UAT.md + 30-HUMAN-UAT.md structure): -->
```markdown
# Phase 28 — Social Pick'em + Leaderboards — Device UAT

> Real-device verification scripts for Phase 28 production deploy `couch-v47-pickem`.
> Resume signal after pass: `uat passed` → triggers `/gsd-verify-work 28`.

## Setup
- Device: iOS Safari (PWA installed) + Android Chrome (PWA installed)
- Pre-state: at least 2 family members + 1 active sports watchparty in mode='game'
- Cache verified: `curl -s https://couchtonight.app/sw.js | grep CACHE`
- Expected line: `const CACHE = 'couch-v47-pickem';`

## Script 1 — Pick'em discoverability from Tonight tab
...

## Script 11 — NO UFC variant (D-17 update 2 verification)
1. Open Pick'em tab.
2. Scroll through all visible league sections.
3. Expected: NBA, NFL, MLB, NHL, WNBA, NCAAF, NCAAB, EPL, LaLiga, Bundesliga, SerieA,
   Ligue1, UCL, MLS, F1 — 15 sections (only sections with upcoming games render).
4. **Expected: ZERO UFC section, ZERO UFC picker card, ZERO UFC text.**
5. **DO NOT mark this as a bug** — UFC is intentionally dropped from Phase 28 v1
   per CONTEXT.md D-17 update 2 (2026-05-04). UFC remains accessible in the
   Add → Schedule a watchparty flow per Phase 22 (existing watchparty mode).
```

<!-- Cross-repo deploy command sequence (RUNBOOK §H standard): -->
```bash
# Step 1: Deploy CFs (queuenight) — gameResultsTick + pickReminderTick land
cd ~/queuenight && firebase deploy --only functions --project queuenight-84044

# Step 2: Sync rules + deploy hosting + bump cache (couch repo, single command)
cd ~/claude-projects/couch && bash scripts/deploy.sh --sync-rules 47-pickem

# Step 3: Curl-verify production cache literal
curl -s https://couchtonight.app/sw.js | grep CACHE
# Expected: const CACHE = 'couch-v47-pickem';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 6.1: Lock smoke-pickem.cjs FLOOR meta-assertion at >=13 production-code sentinels</name>
  <files>scripts/smoke-pickem.cjs</files>
  <read_first>
    - C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs (post-Plan 28-05 state with all 8 groups landed; floor placeholder at FLOOR=1)
    - C:/Users/nahde/claude-projects/couch/scripts/smoke-guest-rsvp.cjs (Phase 27 floor lock pattern reference — see lines 197-207 per RESEARCH OQ-7 reference)
    - C:/Users/nahde/claude-projects/couch/scripts/smoke-position-anchored-reactions.cjs (Phase 26 RPLY-26-17 floor pattern reference)
  </read_first>
  <behavior>
    Replace the FLOOR=1 placeholder block with the locked FLOOR=13 + productionCodeAssertions
    calculation per the interfaces section.

    The HELPER_ASSERTIONS count (27) sums all helper-behavior assertions from Groups 1-4b plus
    constants (Plan 28-02 baseline):
    - Group 1 slateOf: 6 (1A-1F)
    - Group 2 latestGameInSlate: 3 (2G-2I)
    - Group 3 scorePick: 8 (3J-3O including Pitfall 6 partial-match coverage)
    - Group 4 compareMembers: 4 (4P-4S)
    - Group 4b summarizeMemberSeason: 2 (4T-4U)
    - Constants: 2 (K1-K2)
    - Misc/extra Plan 28-02 assertions: 2 buffer

    Total helper count: 25 (per Plan 28-02 SUMMARY) + 2 buffer = **27**. The buffer accounts for
    any additional helper-behavior assertions added in Plans 03-05 (e.g., a sanity helper for
    Jolpica response shape parsing — none currently planned, but the buffer prevents an off-by-one
    floor break if Plan 28-05 owner adds one).

    Production-code sentinel count after Plans 03/04/05:
    - Group 5 (cross-repo lockstep): 9 assertions
    - Group 6 (CF sentinels): 9 assertions
    - Group 7 (Firestore rules sentinels): 4 assertions
    - Group 8 (UI render-function sentinels): 14 assertions
    - **Total: 36 production-code sentinels** (well above floor 13)

    Floor at 13 is conservative — picked to mirror Phase 26 RPLY-26-17 (≥13) and Phase 27
    RSVP-27-17 (≥13) patterns. If a future refactor accidentally removes a sentinel group
    (~9-14 assertions), the floor still holds at 22-27 (above 13). Only if 23+ sentinels
    are removed does the floor break — at which point the regression is large enough to
    warrant intentional re-evaluation.

    The block also includes a brief comment block linking back to the design rationale.
  </behavior>
  <action>
    Edit `C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs`:
    1. Use Grep `'FLOOR = 1; // placeholder'` to find the placeholder line (Plan 28-02 left this).
    2. Replace the entire floor block per the interfaces target above:
       - Set `const FLOOR = 13`.
       - Add `const HELPER_ASSERTIONS = 27;` declaration.
       - Add `const productionCodeAssertions = passed - HELPER_ASSERTIONS;`.
       - Update the success log to mention `production-code sentinels`.
       - Update the failure log to print the actual productionCodeAssertions count.
       - Add a 4-line comment block citing RPLY-26-17 + RSVP-27-17 precedent.
    3. Run smoke and verify exit 0 with passed >= 63 (Plan 28-05's tally) AND productionCodeAssertions
       >= FLOOR (36 >= 13).

    No other smoke-file edits in this task — only the floor block.
  </action>
  <verify>
    <automated>node C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs</automated>
    <automated>grep -c "const FLOOR = 13" C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs</automated>
    <automated>grep -c "productionCodeAssertions" C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs</automated>
  </verify>
  <done>
    - `node scripts/smoke-pickem.cjs` exits 0 with `productionCodeAssertions >= 13` log line
    - `grep -c "const FLOOR = 13"` returns 1
    - `grep -c "productionCodeAssertions"` returns >= 2 (declaration + comparison + log)
    - Total smoke passed count >= 63 + 1 floor met = 64
    - `npm run smoke` aggregate (all 11 contracts) passes — no regressions
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.2: Scaffold 28-HUMAN-UAT.md (11 device-UAT scripts including NO-UFC verification)</name>
  <files>.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</files>
  <read_first>
    - C:/Users/nahde/claude-projects/couch/.planning/phases/27-guest-rsvp/27-HUMAN-UAT.md (10-script structural analog)
    - C:/Users/nahde/claude-projects/couch/.planning/phases/30-couch-groups-affiliate-hooks/30-HUMAN-UAT.md (11-script structural analog including the W5 follow-on script pattern)
    - .planning/phases/28-social-pickem-leaderboards/28-CONTEXT.md (D-17 update 2 — explicit verification target for Script 11)
    - .planning/phases/28-social-pickem-leaderboards/28-UI-SPEC.md (copy literals for Script verification)
  </read_first>
  <behavior>
    Create `28-HUMAN-UAT.md` with 11 numbered scripts plus standard front-matter pattern:

    Header section:
    ```markdown
    ---
    phase: 28-social-pickem-leaderboards
    type: human-uat-scripts
    created: 2026-05-04
    deploy_cache: couch-v47-pickem
    resume_signal: 'uat passed' triggers /gsd-verify-work 28
    ---

    # Phase 28 — Social Pick'em + Leaderboards — Device UAT

    Real-device verification scripts for Phase 28 production deploy.

    ## Pre-flight
    - Device A: iOS Safari (PWA installed via Add to Home Screen)
    - Device B: Android Chrome (PWA installed)
    - Family: at least 2 active members signed in
    - Optional: at least 1 active wp.mode === 'game' watchparty for Scripts 7-8
    - Cache check: `curl -s https://couchtonight.app/sw.js | grep CACHE`
      Expected: `const CACHE = 'couch-v47-pickem';`
    ```

    Then 11 scripts (each ~15-25 lines):

    **Script 1 — Pick'em discoverability from Tonight tab (PICK-28-03)**
    1. Open app at couchtonight.app/app
    2. Verify Tonight tab loads
    3. If any of the 15 active leagues has upcoming games this week, verify
       `Open pick'em →` italic link appears in the Tonight tab hero sub area
    4. Tap the link
    5. Expected: Pick'em surface (`#screen-pickem`) opens with at least one league section visible

    **Script 2 — team_winner picker variant (PICK-28-04)**
    1. From Pick'em surface, locate any NBA/NFL/MLB/NHL/WNBA/NCAAF/NCAAB league section
    2. Verify each picker card shows: matchup line "Away @ Home" in italic serif + 2-chip row (home + away)
    3. Tap a chip → verify it gets `--accent` 2px border (selected state)
    4. Tap the OTHER chip → verify the previous chip resets, new chip gets the accent ring
    5. Verify Edit/Submit affordance fires; pick row disappears from "active" + reappears in
       "Locked games" collapsed section after gameStartTime

    **Script 3 — team_winner_or_draw picker variant including draw chip (PICK-28-04)**
    1. From Pick'em surface, locate any EPL/LaLiga/Bundesliga/SerieA/Ligue1/UCL/MLS league section
    2. Verify each picker card shows: 3-chip row (home / Draw / away)
    3. Tap the Draw chip → verify accent ring + sub-line shows the chip is selected
    4. Submit → verify pick saved (toast `Pick saved`)

    **Script 4 — f1_podium picker with Jolpica roster + fail-soft (PICK-28-05 / D-17 update 2)**
    1. From Pick'em surface, locate the F1 league section (only renders during F1 race weekends)
    2. Happy path: verify 3 dropdown rows labeled P1 / P2 / P3, each populated with 22 drivers
       in form `Max Verstappen`, `Lando Norris`, etc. (no `VER`/`NOR` codes — full names per D-02 update)
    3. Pick 3 distinct drivers; submit; verify pick saved
    4. Fail-soft test: toggle airplane mode ON, force a picker re-render (close + reopen Pick'em),
       verify the F1 card shows `Race entry list unavailable; check back later.` empty state
    5. Verify OTHER league pickers (NBA/NFL/etc.) STILL work normally — F1 outage does not cascade

    **Script 5 — D-09 soft pre-fill from teamAllegiance (PICK-28-07)**
    1. As a member with `participants[mid].teamAllegiance === 'Lakers'` (set during a prior Lakers wp)
    2. Open Pick'em tab; navigate to an NBA Lakers game
    3. Verify the Lakers chip auto-renders with `--team-color` 2px border (teamColor from member's participation)
    4. Verify the brand-voice italic-serif sub-line: `Lakers — your pick from your team commitment. Tap to change.`
    5. Tap the OTHER team's chip → verify pre-fill clears + accent ring moves to the new selection

    **Script 6 — D-04 lock at gameStartTime (PICK-28-10)**
    1. Pick a game whose `gameStartTime` is within 5 minutes
    2. Wait for `gameStartTime - now < 60min` → verify start-time meta line shifts to `--accent` (lock countdown)
    3. Wait until `gameStartTime` passes → verify the chip row goes to `.locked` state + `Picks closed` pill appears
    4. Attempt to tap a chip post-lock → verify NO write fires (Firestore rule denies)
    5. Verify the row migrates to the "Locked games" collapsed section at the bottom of the league section

    **Script 7 — inline wp pick row visibility/teardown (PICK-28-13)**
    1. Open a `wp.mode === 'game'` watchparty before its `sportEvent.startTime`
    2. Verify the inline pick row appears between the participant strip and reactions area:
       `Picks · You: Lakers · Mom: Bucks · Dad: Lakers`
    3. Wait until `sportEvent.startTime` passes → verify the inline row DISAPPEARS (lock teardown)
    4. After settlement (run gameResultsTick manually via Firebase Console scheduler page) → verify
       the inline row reappears with `--good`/`--bad` chip-pip indicators

    **Script 8 — leaderboard render + rank ordering (PICK-28-25)**
    1. From Pick'em surface, tap "View leaderboard" link inside any league section
    2. Verify rank pills render: `1` / `2` / `T-3` etc. with `#1` styled in `--accent` + `--velvet-glow`
    3. Verify each row shows: `{Member} {points} pts · {settled} of {total} settled · {missed} missed`
    4. Verify rank ordering: higher pointsTotal always wins; ties broken by lower avgTiebreakerDelta
    5. Verify member-row team-color ring on avatars (D-09 ring continuation)

    **Script 9 — past-seasons archive hide-when-empty (PICK-28-26)**
    1. Navigate to any league with NO frozen leaderboards (e.g. NBA before its first season turnover)
    2. Verify the `View past seasons` link is HIDDEN entirely (not just disabled)
    3. After at least one season turnover (will only be testable in production after season-end CF runs):
       verify the link appears + tap → renders the frozen leaderboard read-only

    **Script 10 — push reminder T-15min (PICK-28-16)**
    1. As a member who has NOT picked on an upcoming game whose `gameStartTime` is in ~16 minutes
    2. Wait for the next pickReminderTick CF run (within 5 min)
    3. Verify a push notification arrives with title `Game starting soon — make your pick` and
       body `Heads-up — {GameMatchup} tips off in 15. Make your pick.`
    4. Tap the notification → verify it deep-links to `/app?tab=pickem`
    5. Repeat: verify the SAME (memberId, gameId) does NOT receive a second reminder
       (idempotency via picks_reminders/{id} doc)

    **Script 11 — NO UFC variant (D-17 update 2 verification — DO NOT mark as bug)**
    1. Open Pick'em tab on a real device
    2. Scroll through ALL visible league sections
    3. Expected sections (when each has games): NBA, NFL, MLB, NHL, WNBA, NCAAF, NCAAB,
       EPL, LaLiga, Bundesliga, SerieA, Ligue1, UCL, MLS, F1 — **15 sections maximum**
    4. **Verify ZERO UFC section, ZERO UFC picker card, ZERO UFC text appears.**
    5. **CRITICAL: this is intentional, NOT a bug.** UFC was dropped from Phase 28 v1 per
       `28-CONTEXT.md` D-17 update 2 (2026-05-04) because no free UFC API exposes structured
       roster + settlement at zero cost (TheSportsDB Patreon = $108/yr; API-Sports MMA free
       tier blocks 2025-2026 dates). UFC remains accessible in the Add → Schedule a
       watchparty flow per Phase 22.
    6. Verify: open Add tab → Schedule sports watchparty → confirm UFC IS still selectable in
       that flow (Phase 22 catalog unchanged).

    Resume signal section at end:
    ```markdown
    ## Resume signal

    After all 11 scripts pass on at least one device:
    - Reply `uat passed` in the chat to trigger `/gsd-verify-work 28`
    - Verifier auto-confirms phase completion + bumps STATE.md milestone progress
    ```

    Total target: ~250 lines.
  </behavior>
  <action>
    Create `C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md` per the behavior block.

    Concrete copy values (verbatim from UI-SPEC §Copywriting):
    - Tonight tab inline-link copy: `Open pick'em →`
    - F1 fail-soft copy: `Race entry list unavailable; check back later.`
    - D-09 brand-voice sub-line: `{Team} — your pick from your team commitment. Tap to change.`
    - Lock countdown copy: `Picks close in {N}m`
    - Picks-closed pill: `Picks closed`
    - pickReminder push title: `Game starting soon — make your pick`
    - pickReminder push body: `Heads-up — {GameMatchup} tips off in 15. Make your pick.`
    - pickResults push body: `Your {LeagueLabel} pick just settled. Tap to see how you did.`

    Script 11 MUST include the explicit `DO NOT mark as bug` language — mirrors Phase 30
    UAT Script 11 (T-30-14 v1 limitation) pattern from STATE.md.

    Script numbers MUST be sequential 1 through 11.
  </action>
  <verify>
    <automated>test -f C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md && echo EXISTS</automated>
    <automated>wc -l C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</automated>
    <automated>grep -c "^## Script " C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</automated>
    <automated>grep -c "no UFC\|NO UFC\|ZERO UFC" C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</automated>
    <automated>grep -c "Jolpica" C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</automated>
    <automated>grep -c "couch-v47-pickem" C:/Users/nahde/claude-projects/couch/.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</automated>
  </verify>
  <done>
    - File exists
    - File length >= 200 lines (target ~250)
    - `grep -c "^## Script "` returns 11 (exactly 11 numbered scripts)
    - At least one `NO UFC` / `no UFC` / `ZERO UFC` mention (Script 11 verification)
    - `Jolpica` mentioned in Script 4
    - `couch-v47-pickem` mentioned in pre-flight section
    - Resume signal section present at end
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6.3: Cross-repo deploy ritual (queuenight CFs FIRST → couch hosting + cache bump)</name>
  <files>queuenight (functions deploy), couch firestore.rules + sw.js + hosting deploy</files>
  <what-built>
    Plan 28-03 produced `queuenight/functions/src/gameResultsTick.js` + `pickReminderTick.js` plus
    NOTIFICATION_DEFAULTS extension in `queuenight/functions/index.js`.

    Plan 28-04 produced `firestore.rules` updates (3 new match blocks) — these need to land in the
    queuenight production project via the `--sync-rules` deploy flag (Phase 30 Wave 4 process gate).

    Plan 28-05 produced the full UI surface in `js/app.js` + `css/app.css` + `app.html`.

    Plan 28-06 Tasks 6.1 + 6.2 locked the smoke FLOOR + scaffolded the UAT.

    Everything is ready to ship. The cross-repo deploy ritual is user-gated because:
    1. It writes new Cloud Functions to production (cost + log volume implications)
    2. It updates Firestore rules in production (defense surface change)
    3. It bumps the PWA cache (forces re-fetch on every installed app on next online cycle)
    4. It creates an irreversible commit in the deploy mirror (queuenight/public/) and the
       couch hosting deploy receipt
  </what-built>
  <how-to-verify>
    Execute the cross-repo deploy ritual per RUNBOOK §H. The user must run these commands in
    sequence and verify each step's output:

    **Step A — Deploy CFs from queuenight (must complete BEFORE Step B):**
    ```bash
    cd ~/queuenight
    firebase deploy --only functions --project queuenight-84044
    ```
    Expected output: `+ functions[gameResultsTick(us-central1)] Successful create operation.`
    Expected output: `+ functions[pickReminderTick(us-central1)] Successful create operation.`
    Expected: NO ERR for any other CF (rsvpSubmit/rsvpStatus/etc. should all redeploy cleanly).

    Verify in Firebase Console scheduler-page:
    - `firebase-schedule-gameResultsTick-us-central1` exists with 5-min cadence
    - `firebase-schedule-pickReminderTick-us-central1` exists with 5-min cadence

    **Step B — Sync rules + bump cache + deploy hosting (single command from couch):**
    ```bash
    cd ~/claude-projects/couch
    bash scripts/deploy.sh --sync-rules 47-pickem
    ```

    Expected behavior (per RUNBOOK §H + Phase 30 Wave 4 add):
    1. `--sync-rules`: pre-flight diff between source-of-truth `couch/firestore.rules` and
       `queuenight/public/firestore.rules`. If drifted, auto-mirror + commit + deploy rules first.
    2. Smoke gate (§2.5): all 11 contracts pass including the new FLOOR=13 gate (>= 13
       production-code sentinels for smoke-pickem).
    3. sw.js auto-bump: `const CACHE = 'couch-v46-wave-5-hotfix';` → `const CACHE = 'couch-v47-pickem';`
    4. Mirror to queuenight/public/: app.html, css/app.css, js/app.js, js/pickem.js, sw.js, etc.
    5. BUILD_DATE auto-stamp on the mirror's js/constants.js
    6. `firebase deploy --only hosting` from queuenight/

    Expected output tail: `Project Console: https://console.firebase.google.com/project/queuenight-84044/overview`
    Expected output: `+ Deploy complete!`
    Expected output: NO ERR.

    **Step C — Production curl verification:**
    ```bash
    curl -s https://couchtonight.app/sw.js | grep CACHE
    ```
    Expected line: `const CACHE = 'couch-v47-pickem';`

    Capture the exact ISO timestamp of the curl confirmation for the SUMMARY.

    **Step D — Smoke production paths (no auth required):**
    ```bash
    curl -s https://couchtonight.app/app | grep -c "screen-pickem"
    ```
    Expected: returns 1 (the new HTML section landed).

    ```bash
    curl -s https://couchtonight.app/js/pickem.js | grep -c "PICK_TYPE_BY_LEAGUE"
    ```
    Expected: returns >= 1 (production js/pickem.js carries the Plan 02 module).
  </how-to-verify>
  <resume-signal>
    Reply `deploy verified` after Step C curl returns the new cache literal.

    On `deploy verified`: proceed to Task 6.4 (commit + summary). On any error or unexpected
    output: pause and surface the exact failure mode for triage (most likely culprits: jq not
    installed → see Plan 28-01 SUMMARY workaround; emulator port 8080 stuck → see deploy.sh
    pre-flight; queuenight CF deploy fails on cold-start → re-run after 60s).
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 6.4: Commit close-out + cross-repo summary</name>
  <files>(commit only — no new files)</files>
  <read_first>
    - .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md (post-Task 6.2)
    - C:/Users/nahde/claude-projects/couch/sw.js (post-Task 6.3 auto-bump — should now read couch-v47-pickem)
    - C:/Users/nahde/claude-projects/couch/scripts/smoke-pickem.cjs (post-Task 6.1 FLOOR=13 lock)
    - .planning/STATE.md (current Phase 28 paused-state line — needs update post-deploy)
  </read_first>
  <behavior>
    Atomic commits to capture Phase 28 close-out. The deploy.sh run in Task 6.3 already committed
    the sw.js bump + mirror copies to the queuenight repo. This task adds the couch-side commits:

    1. Commit Plan 28-06 changes (smoke FLOOR lock + UAT scaffold). All Plan 28-03/04/05 commits
       should already exist from prior task executions; this is the close-out ribbon.

    2. Commit message format (mirrors Phase 27 + Phase 30 close-out commits):
    ```
    docs(28-06): phase close-out — smoke FLOOR lock + 28-HUMAN-UAT scaffold + cross-repo deploy ritual

    - smoke-pickem.cjs FLOOR meta-assertion locked at >=13 production-code sentinels
      (mirrors RPLY-26-17 + RSVP-27-17 patterns; current count: 36, well above floor)
    - 28-HUMAN-UAT.md scaffold with 11 device-UAT scripts including Script 11
      explicit verification of NO UFC variant per D-17 update 2
    - Cross-repo deploy executed: queuenight functions {gameResultsTick + pickReminderTick}
      live at us-central1; firestore.rules synced via --sync-rules pre-flight; couch
      hosting deployed to couchtonight.app at couch-v47-pickem (curl-verified)

    Closes Phase 28 code surface. UAT pending.
    ```

    3. Update STATE.md:
       - "Current Position" line: change `Phase: 28 — Social pick'em + leaderboards / Plan: 02 SHIPPED`
         to `Phase: 28 — Social pick'em + leaderboards / Plans 01-06 SHIPPED + UAT pending`
       - Add a new "Most recent close-out" entry citing the v47-pickem deploy + curl-receipt timestamp
         + the 9 PICK-28-* requirements newly closed in this phase
       - Move the previous "Most recent close-out" (Phase 30 hotfix Wave 5) to "Previous close-out"
       - Add a new HUMAN-VERIFY row to the "Open follow-ups" table for Phase 28 UAT (11 scripts)
       - Update the "sw.js cache version" section with the new v47-pickem entry in the progression

    4. Update ROADMAP.md Phase 28 section:
       - Mark all 6 plan rows as [x] (currently 28-01 + 28-02 are [x]; mark 28-03 → 28-06 as [x])
       - Update Phase 28 status line / footer to reflect "shipped 2026-05-04 as couch-v47-pickem"
       - Add Phase 28 to the "Recent shipped phases (lookback)" table in STATE.md

    5. Update REQUIREMENTS.md Coverage block:
       - Add the new PICK-28-* IDs (PICK-28-09 through PICK-28-30) with status `Complete - HUMAN-VERIFY pending`
       - Total Phase 28 requirements: 21 (PICK-28-01..28 with some gaps for renumbering)
  </behavior>
  <action>
    From `C:/Users/nahde/claude-projects/couch/`:

    1. `git status` — confirm working tree state post-deploy. Expected modified:
       - `sw.js` (auto-bumped to couch-v47-pickem by Task 6.3)
       - `scripts/smoke-pickem.cjs` (Task 6.1 FLOOR lock)
       - `.planning/STATE.md` (Task 6.4 post-edit)
       - `.planning/ROADMAP.md` (Task 6.4 post-edit)
       - `.planning/REQUIREMENTS.md` (Task 6.4 post-edit)
       Expected untracked:
       - `.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md` (Task 6.2)

    2. Update STATE.md per behavior section item 3 (add Phase 28 close-out, demote Phase 30
       hotfix Wave 5 to Previous, update progress percent).

    3. Update ROADMAP.md per behavior section item 4 (mark all 6 Phase 28 plans [x]).

    4. Update REQUIREMENTS.md per behavior section item 5 (Coverage block + traceability rows).

    5. `git add -A` (stage all the changes including the new UAT file).

    6. `git commit -m "docs(28-06): phase close-out — smoke FLOOR lock + 28-HUMAN-UAT scaffold + cross-repo deploy ritual"` with the full body per behavior section item 2.

    7. `git status` — verify clean working tree.

    8. Output the commit hash + cross-repo curl-receipt timestamp from Task 6.3 for the SUMMARY.

    Do NOT push to origin/main without user confirmation. The PR for the existing
    `hotfix/phase-30-cross-cutting-wave` branch (per STATE.md Open follow-ups) is the natural
    integration target — alternatively, user may want a new `phase-28-pickem` branch off main.
    Surface this decision to the user post-commit.
  </action>
  <verify>
    <automated>cd C:/Users/nahde/claude-projects/couch && git log -1 --pretty=%s</automated>
    <automated>cd C:/Users/nahde/claude-projects/couch && git status --porcelain | wc -l</automated>
    <automated>grep -c "couch-v47-pickem" C:/Users/nahde/claude-projects/couch/sw.js</automated>
    <automated>grep -c "Phase 28" C:/Users/nahde/claude-projects/couch/.planning/STATE.md</automated>
  </verify>
  <done>
    - Latest commit subject contains `docs(28-06)` and `phase close-out`
    - Working tree clean post-commit (`git status --porcelain | wc -l` returns 0)
    - sw.js contains `couch-v47-pickem` (Task 6.3 deploy worked)
    - STATE.md has Phase 28 close-out entry with v47-pickem deploy timestamp
    - ROADMAP.md Phase 28 section shows all 6 plans [x]
    - REQUIREMENTS.md gained PICK-28-* rows (Complete - HUMAN-VERIFY pending)
    - User informed of branch decision (PR vs new branch)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → production Firebase project queuenight-84044 (CFs deploy) | Cross-repo deploy ritual; user must approve via deploy.sh argv. |
| Local dev → couch hosting (couchtonight.app) | Same. |
| Local dev → queuenight Firestore rules production | Same; --sync-rules pre-flight catches drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-28 | Tampering | Deploy proceeds with rules drift between source-of-truth + queuenight mirror | mitigate | `--sync-rules` flag (Phase 30 Wave 4 / CR-12) — pre-flight diff aborts if drifted; auto-mirror + commit + deploy if flag set. |
| T-28-29 | Denial of Service | Deploy lands CFs that crash on first invocation (e.g. typo in import) → next 5-min tick fails repeatedly | mitigate | smoke gate at deploy.sh §2.5 catches `node --check` failures; CF code review surfaces typos; if CF crashes post-deploy, Sentry alerts within 15min and rollback is `firebase functions:delete` + redeploy old. |
| T-28-30 | Information Disclosure | sw.js cache literal exposes internal cache progression to anyone curling the URL | accept | Public-by-design per CLAUDE.md; cache version is intentionally human-readable. No PII / secrets in the cache version string. |
| T-28-31 | Tampering | UAT scripts checked into repo could be modified by future contributors to skip verification | accept | UAT scripts are human-driven; the `uat passed` resume signal is user-controlled, not parsed from the file. The file is documentation, not enforcement. |
</threat_model>

<verification>
- `node scripts/smoke-pickem.cjs` exits 0 with `productionCodeAssertions >= 13` (FLOOR met)
- `bash scripts/deploy.sh --sync-rules 47-pickem` succeeds (smoke gate green; rules synced; sw.js bumped; hosting deployed)
- `curl -s https://couchtonight.app/sw.js | grep CACHE` returns `const CACHE = 'couch-v47-pickem';`
- `28-HUMAN-UAT.md` exists with 11 numbered scripts including explicit "NO UFC variant" verification
- STATE.md Phase 28 close-out entry captures the curl-receipt timestamp + commit hash
- ROADMAP.md Phase 28 section shows all 6 plans [x]
- REQUIREMENTS.md gains PICK-28-09..30 rows
</verification>

<success_criteria>
1. Smoke FLOOR=13 lock holds; `productionCodeAssertions` calculation correctly subtracts the
   27 helper-behavior assertions to derive the production-code sentinel count (36 currently).
2. Cross-repo deploy ritual completes in correct order: CFs FIRST, then rules sync, then
   hosting + cache bump. NO partial-deploy state where CFs are missing but client UI tries
   to write picks.
3. Production cache literal verified at couchtonight.app via curl post-deploy.
4. 28-HUMAN-UAT.md exists with 11 scripts; Script 11 explicitly tests "NO UFC variant" with
   the `DO NOT mark as bug` language per Phase 30 Script 11 (T-30-14 v1) precedent.
5. STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 28 close-out.
6. Phase 28 SHIPPED status = code-complete + deployed; UAT pending (11 scripts).
7. cache progression: v46-wave-5-hotfix → v47-pickem (sequential, no gaps).
</success_criteria>

<output>
After completion, create `.planning/phases/28-social-pickem-leaderboards/28-06-SUMMARY.md` per the standard SUMMARY template.

Key Summary fields to surface:
- Cross-repo deploy chain: queuenight functions commit + couch hosting commit
- Production curl-receipt timestamp + cache literal
- smoke FLOOR=13 lock confirmed (productionCodeAssertions = 36)
- 28-HUMAN-UAT.md exists with 11 scripts; Script 11 = explicit NO-UFC verification
- All 21 PICK-28-* requirements transitioned to Complete - HUMAN-VERIFY pending
- D-17 update 2 (UFC drop) verified end-to-end: smoke negative sentinel (8.H) + production-code
  filter (renderPickemSurface line) + UAT Script 11 (user-facing)
- Resume signal documented: `uat passed` → /gsd-verify-work 28
- Cross-repo branch decision: surfaced to user (PR against hotfix/phase-30-cross-cutting-wave OR new phase-28-pickem branch)
</output>
