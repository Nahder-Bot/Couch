---
phase: 28-social-pickem-leaderboards
plan: 06
type: execute
wave: 4
depends_on: [28-03, 28-04, 28-05]
files_modified:
  - scripts/smoke-pickem.cjs
  - sw.js
  - .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md
autonomous: false
requirements:
  - PICK-28-21
  - PICK-28-23
  - PICK-28-24
user_setup:
  - service: queuenight-firebase
    why: "Phase 28 is cross-repo because gameResultsTick + pickReminderTick CFs live in sibling queuenight/functions/ repo. Per CLAUDE.md project context, the user pre-authorized firebase deploy to queuenight-84044; this plan executes the deploy ritual."
    env_vars: []
    dashboard_config: []
  - service: thesportsdb-free-tier
    why: "Settlement CF polls TheSportsDB free key '1' for game scores. No env var required (key is public per CONTEXT.md D-01)."
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "scripts/smoke-pickem.cjs floor meta-assertion is RAISED to FLOOR=13 (Plan 01 placeholder was 1) and the smoke gate passes with at least 13 production-code sentinels"
    - "sw.js CACHE constant is bumped to 'couch-v40-pickem' (next in sequence after 'couch-v39-guest-rsvp' per CONTEXT.md Specifics)"
    - "queuenight/functions deploy ritual executed FIRST: `firebase deploy --only functions` from ~/queuenight ships gameResultsTick + pickReminderTick to queuenight-84044/us-central1 (4 existing rsvp* CFs unchanged; 2 new CFs land)"
    - "Firestore rules deploy ritual executed: `firebase deploy --only firestore:rules` from ~/queuenight ships the new picks/leaderboards/picks_reminders rule blocks (rules tests 56/56 PASS verified locally before deploy)"
    - "couch deploy ritual executed SECOND: `bash scripts/deploy.sh 40-pickem` from couch repo bumps sw.js CACHE auto-magically + runs smoke gate (must be all 11 contracts green) + mirrors to queuenight/public + firebase deploy --only hosting"
    - "Production cache verification: `curl -s https://couchtonight.app/sw.js | grep CACHE` returns `const CACHE = 'couch-v40-pickem';` within 2 minutes of deploy completion"
    - "28-HUMAN-UAT.md scaffolded with at least 10 device-UAT scripts covering: pick'em discoverability from Tonight tab + 4 pickType variants + tiebreaker designation + D-09 pre-fill + D-10 real-time visibility (two-device test) + pick lock at gameStartTime + leaderboard ranking + inline wp pick row + push reminder T-15min + cache bump activation"
    - "All 24 PICK-28-* IDs are codified at the smoke layer OR rules-test layer OR HUMAN-UAT layer (full traceability matrix in 28-06-SUMMARY.md)"
  artifacts:
    - path: "scripts/smoke-pickem.cjs"
      provides: "FLOOR=13 production-code sentinel gate; total assertions ≥45 (Groups 1-8); deploy gate fails if floor not met"
      contains: "FLOOR = 13"
    - path: "sw.js"
      provides: "CACHE = 'couch-v40-pickem' — service worker invalidates installed PWAs on next online activation"
      contains: "couch-v40-pickem"
    - path: ".planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md"
      provides: "≥10 device-UAT scripts covering Phase 28 user-facing behaviors with resume signal 'uat passed' → /gsd-verify-work 28"
      min_lines: 200
  key_links:
    - from: "scripts/smoke-pickem.cjs FLOOR=13 meta-assertion"
      to: "scripts/deploy.sh §2.5 smoke gate"
      via: "If floor not met, deploy fails; mirrors Phase 26 RPLY-26-17 + Phase 27 RSVP-27-17 patterns"
      pattern: "FLOOR = 13"
    - from: "queuenight functions deploy"
      to: "couch hosting deploy"
      via: "Cross-repo lockstep ritual: functions FIRST (so client code arriving second can rely on new CF endpoints), client SECOND (sw.js CACHE bump invalidates installed PWAs)"
      pattern: "firebase deploy --only functions"
    - from: "production curl-verification"
      to: "Phase 28 SHIPPED status in STATE.md + ROADMAP.md update"
      via: "Verify CACHE matches expected value within 2 minutes of deploy; no rollback needed if smoke gate green"
      pattern: "couch-v40-pickem"
---

<objective>
Cross-repo close-out: raise the smoke-pickem.cjs floor meta-assertion to ≥13 (locking the deploy gate), bump sw.js CACHE to `couch-v40-pickem`, execute the cross-repo deploy ritual (functions FIRST → rules → client), curl-verify production, scaffold 10+ device-UAT scripts. This is the only plan in Phase 28 with `autonomous: false` because the deploy ritual surfaces 4 checkpoints (queuenight functions deploy / queuenight rules deploy / couch hosting deploy / production curl verify).

Purpose: Without this plan, Phase 28 code stays in source-only state and never reaches production. The smoke floor lock prevents future plans from accidentally regressing the contract surface (Phase 26 RPLY-26-17 + Phase 27 RSVP-28-17 precedent — floor meta-assertion fails the deploy gate when total < FLOOR). The HUMAN-UAT scaffold sets up the device-verification pattern for `/gsd-verify-work 28` per the established Phase 18 / 19 / 20 / 24 / 26 / 27 UAT-then-verify pattern.

Output: 1 modified smoke contract (FLOOR raised) + 1 modified sw.js (CACHE bump via deploy.sh auto-bump) + 1 new HUMAN-UAT scaffold + 4 cross-repo deploy commits + production curl evidence in commit message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-social-pickem-leaderboards/28-CONTEXT.md
@.planning/phases/28-social-pickem-leaderboards/28-RESEARCH.md
@.planning/phases/28-social-pickem-leaderboards/28-PATTERNS.md
@.planning/phases/28-social-pickem-leaderboards/28-VALIDATION.md
@.planning/phases/28-01-SUMMARY.md
@.planning/phases/28-02-SUMMARY.md
@.planning/phases/28-03-SUMMARY.md
@.planning/phases/28-04-SUMMARY.md
@.planning/phases/28-05-SUMMARY.md
@.planning/phases/27-guest-rsvp/27-05-SUMMARY.md
@.planning/phases/27-guest-rsvp/27-HUMAN-UAT.md
@.planning/phases/26-position-anchored-reactions-async-replay/26-05-SUMMARY.md
@CLAUDE.md
@scripts/deploy.sh
@sw.js
@scripts/smoke-pickem.cjs

<interfaces>
<!-- Critical contracts from CONTEXT + STATE.md + Phase 26/27 close-out precedents. -->

From CONTEXT.md Specifics:
- Service worker CACHE bump at deploy: `couch-v40-pickem` (next in sequence after `couch-v39-guest-rsvp`)
- bash scripts/deploy.sh auto-bumps sw.js CACHE when given a short-tag arg

From STATE.md sw.js cache version section:
- Current source: `couch-v39-guest-rsvp` (Phase 27 — DEPLOYED 2026-05-02)
- Phase progression chain: v37 (P24) → v38 (P26) → v39 (P27) → **v40 (P28)** ← this plan
- Bump command: `bash scripts/deploy.sh <short-tag>` → e.g. `bash scripts/deploy.sh 40-pickem`

From scripts/deploy.sh §3 (CACHE bump section — auto-bumped via short-tag arg):
- Reads $1 short-tag arg
- Replaces existing `const CACHE = 'couch-vNN-...';` line in sw.js with `const CACHE = 'couch-v${ARG}';`
- The deploy script is the ONLY supported way to bump CACHE (don't hand-edit sw.js)

From Phase 27 close-out (27-05-SUMMARY.md):
- Cross-repo deploy ritual order:
  1. `firebase deploy --only functions` from ~/queuenight (CFs go FIRST)
  2. `firebase deploy --only firestore:rules` from ~/queuenight (rules deploy independently from functions)
  3. `bash scripts/deploy.sh <short-tag>` from couch (client + sw.js CACHE bump + smoke gate + hosting deploy)
- Phase 27 used `bash scripts/deploy.sh --allow-dirty 39-guest-rsvp`
- Production curl: `curl -s https://couchtonight.app/sw.js | grep CACHE` returns `const CACHE = 'couch-v39-guest-rsvp';`

From Phase 26 RPLY-26-17 floor meta-assertion pattern (precedent):
```js
const FLOOR = 13;
const productionCodeAssertions = passed - HELPER_BEHAVIOR_COUNT;  // tally only sentinels
if (productionCodeAssertions >= FLOOR) {
  console.log(`  ok floor met (${productionCodeAssertions} >= ${FLOOR})`);
  passed++;
} else {
  console.error(`  FAIL floor NOT met (${productionCodeAssertions} < ${FLOOR})`);
  failed++;
}
```

From Phase 27 27-HUMAN-UAT.md (scaffold pattern — copy structure):
- Frontmatter: phase, status, created, scripts (count)
- 10 device-UAT scripts; each has Title + Setup + Steps + Pass criteria + Status field
- Resume signal documented: "When all scripts pass, reply 'uat passed' to trigger /gsd-verify-work 28"
- Cross-platform coverage: iOS Safari (PWA + non-PWA), Android Chrome, two-device flows for D-10
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Raise smoke-pickem.cjs floor meta-assertion to FLOOR=13</name>
  <files>scripts/smoke-pickem.cjs</files>
  <read_first>
    - `scripts/smoke-pickem.cjs` (entire file as it stands after Plans 01-05 — confirm current floor placeholder + total assertion count from Groups 1-8)
    - `scripts/smoke-position-anchored-reactions.cjs` last 30 lines (Phase 26 RPLY-26-17 floor pattern — exact count tallying logic)
    - `scripts/smoke-guest-rsvp.cjs` last 30 lines (Phase 27 floor pattern)
  </read_first>
  <action>
**Step A — Tally current assertion count after Plans 01-05.**

Expected breakdown (per assertion plans in earlier plans):
- Group 1 slateOf: 6
- Group 2 latestGameInSlate: 3
- Group 3 scorePick: 7 (3J/3K/3L/3M-partial/3M-full/3N-both/3N-fighter-only/3O — wait that's 8; reconcile during execution by counting actual eq() calls)
- Group 4 compareMembers: 4
- Group 4b summarizeMemberSeason + constants: 4
- Group 5 cross-repo lockstep: 9
- Group 6 firestore.rules sentinels: 5
- Group 7 UI production-code: 10
- Group 8 CSS sentinels: 7

Helper-behavior assertions (Groups 1-4b): ~24
Production-code sentinels (Groups 5-8): ~31
Total target: ~55

**Step B — Update the floor block in smoke-pickem.cjs:**

Locate the floor meta-assertion block from Plan 01 (currently `FLOOR = 1` placeholder). Replace with:

```js
  // === Group 9 - Floor meta-assertion (PICK-28-21; matches Phase 26 RPLY-26-17 + Phase 27 RSVP-27-17 patterns) ===
  // Production-code sentinels are Groups 5-8 (cross-repo lockstep + rules + UI + CSS).
  // Helper-behavior assertions are Groups 1-4b. To compute the production-code
  // tally, subtract the helper count from the running passed counter.
  // The FLOOR=13 gate fails the deploy when production-code sentinels < 13.
  // Future phases that touch the smoke contract MUST keep production-code count >= 13
  // or this floor will fire (intended safeguard against silent surface regression).
  {
    const FLOOR = 13;
    // Hard-coded helper count from Groups 1-4b (slateOf 6 + latestGameInSlate 3
    // + scorePick 8 + compareMembers 4 + summarizeMemberSeason 2 + constants 2 = 25;
    // adjust during execution to MATCH the actual number of eq() calls in Groups 1-4b).
    const HELPER_COUNT = 25;
    const productionCodeAssertions = passed - HELPER_COUNT;
    if (productionCodeAssertions >= FLOOR) {
      console.log(`  ok floor met (${productionCodeAssertions} production-code sentinels >= ${FLOOR})`);
      passed++;
    } else {
      console.error(`  FAIL floor NOT met (${productionCodeAssertions} production-code sentinels < ${FLOOR})`);
      failed++;
    }
  }
```

**IMPORTANT:** During execution, count the actual eq() calls in Groups 1-4b and adjust `HELPER_COUNT` to match exactly. If Group 3 ended up with 8 assertions instead of 7 (the partial/full pair counted as 2), set HELPER_COUNT accordingly. The math must be: `passed - HELPER_COUNT >= 13` after the running counter reflects all sentinel groups.

**Step C — Verify floor passes.**

Run `node scripts/smoke-pickem.cjs` — must exit 0 with `ok floor met (N >= 13)` where N >= 13.

If floor fails (N < 13), AUDIT the smoke contract and confirm Plans 03/04/05 actually emitted enough sentinels. If a sentinel was lost during execution drift, surface to user before proceeding to deploy. Do NOT lower the floor to make it pass — fix the missing sentinel.

**Step D — Run full smoke aggregate** (sanity check before deploy):

```bash
bash -c 'for f in scripts/smoke-*.cjs; do echo "=== $f ==="; node "$f" || exit 1; done'
```

All 11 contracts must exit 0 (positionToSeconds + matches/considerable + availability + kid-mode + decision-explanation + conflict-aware-empty + sports-feed + native-video-player + position-anchored-reactions + guest-rsvp + pickem).
  </action>
  <verify>
    <automated>node scripts/smoke-pickem.cjs && bash -c 'for f in scripts/smoke-*.cjs; do node "$f" > /dev/null || { echo "FAIL: $f"; exit 1; }; done' && echo OK</automated>
  </verify>
  <done>
    - scripts/smoke-pickem.cjs FLOOR raised from placeholder (1) to FLOOR=13
    - HELPER_COUNT set to actual count of helper-behavior assertions (Groups 1-4b)
    - `node scripts/smoke-pickem.cjs` exits 0 with `ok floor met (N >= 13)` where N >= 13
    - All 11 smoke contracts pass when run sequentially
    - One commit: `test(28-06): smoke-pickem.cjs floor meta-assertion locked at FLOOR=13 (PICK-28-21 - matches Phase 26 RPLY-26-17 + Phase 27 RSVP-27-17 patterns)`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.2: CHECKPOINT — Cross-repo deploy ritual (functions FIRST → rules → client)</name>
  <what-built>
    Plans 01-05 + 06.1 have committed all Phase 28 code to BOTH repositories:
    - couch repo: js/sports-feed.js (additive) + js/pickem.js (NEW) + js/app.js (5 render functions + 3-map lockstep + import) + css/app.css (.pe-* family) + app.html (#screen-pickem block) + firestore.rules (3 new blocks) + tests/rules.test.js (Phase 28 describe block, 56/56 PASS) + scripts/smoke-pickem.cjs (FLOOR=13 locked) + scripts/deploy.sh (§2.5 wired with smoke-pickem + smoke-guest-rsvp catch-up)
    - queuenight repo: queuenight/functions/index.js (NOTIFICATION_DEFAULTS + 2 CF exports) + queuenight/functions/src/gameResultsTick.js (NEW settlement CF) + queuenight/functions/src/pickReminderTick.js (NEW reminder CF)

    Pre-deploy gates GREEN:
    - All 11 smoke contracts pass (~5s aggregate runtime)
    - tests/rules.test.js 56/56 PASS (Firestore emulator)
    - Cross-repo lockstep grep verified (9 sentinels: 3 keys × 3 maps)
    - node --check passes on all 5 modified files

    The cross-repo deploy ritual is the user-pre-authorized step (per CLAUDE.md memory: "For Couch project, Claude may run firebase deploy to queuenight-84044 without per-deploy approval"). The CHECKPOINT exists because the deploy is a multi-step ritual where the ORDER matters (functions FIRST, then rules, then client). User confirmation acknowledges that the smoke gate is green AND that Plan 28-06.1's floor lock is in place.
  </what-built>
  <how-to-verify>
The executor performs these 4 deploy steps in order. User confirms after each is observed-green OR after all 4 complete:

**1. Deploy queuenight Cloud Functions FIRST (so the client code arriving second can rely on the new CF endpoints):**
```bash
cd ~/queuenight && firebase deploy --only functions --project queuenight-84044
```
Expected output:
- `Deploying functions (queuenight-84044, us-central1)...`
- 2 NEW deployments: `i  functions: creating Node.js 20 (2nd Gen) function gameResultsTick(us-central1)...` and `pickReminderTick(us-central1)...`
- Existing CFs unchanged: `i  functions: skipped` for rsvpSubmit / rsvpStatus / rsvpRevoke / rsvpReminderTick / providerRefreshTick / watchpartyTick / etc.
- Final line: `+ Deploy complete!` with no errors

If any error: STOP and surface to user. DO NOT proceed to step 2 with broken function deploy.

**2. Deploy queuenight Firestore rules SECOND (covers the new picks/leaderboards/picks_reminders rule blocks):**
```bash
cd ~/queuenight && cp ~/couch/firestore.rules . && firebase deploy --only firestore:rules --project queuenight-84044
```
Expected output:
- `i  cloud.firestore: checking firestore.rules for compilation errors...`
- `+ cloud.firestore: rules file firestore.rules compiled successfully`
- `i  cloud.firestore: uploading rules firestore.rules...`
- `+ Deploy complete!`

If rules compilation fails: rules file syntax error from Plan 04 — fix in source, re-copy, retry.

**3. Deploy couch hosting THIRD (sw.js CACHE auto-bumped + smoke gate runs + hosting deploy + queuenight/public mirror):**
```bash
cd ~/couch && bash scripts/deploy.sh 40-pickem
```
Expected output:
- BUILD_DATE auto-stamp
- node --check on every js file (no failures)
- §2.5 smoke gate runs all 11 contracts; final line: `Smoke contracts pass (...positionToSeconds...+ guest-rsvp + pickem).`
- §3 sw.js CACHE bumped: `const CACHE = 'couch-v40-pickem';`
- §4 mirror to queuenight/public/
- §5 firebase deploy --only hosting --project queuenight-84044
- Final line: `+ Deploy complete!` with hosting URL

If smoke gate fails or hosting deploy errors: STOP and surface to user.

**4. Production curl verification (within 2 minutes of step 3 completion):**
```bash
curl -s https://couchtonight.app/sw.js | grep CACHE
```
Expected output (exactly):
```
const CACHE = 'couch-v40-pickem';
```

If output shows old cache (`couch-v39-guest-rsvp` or earlier): wait 30s, retry. Cloudflare/Firebase Hosting CDN can take up to 60s to propagate. If still stale after 2 min, investigate.

**User confirms** by replying with one of:
- `approved` — all 4 steps observed green; proceed to commit deploy evidence
- `step N failed: <details>` — surface failure for triage
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 6.3: Commit deploy evidence + scaffold 28-HUMAN-UAT.md</name>
  <files>.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/27-guest-rsvp/27-HUMAN-UAT.md` (entire file — exact scaffold structure to copy)
    - `.planning/phases/26-position-anchored-reactions-async-replay/26-HUMAN-UAT.md` (alternate scaffold reference)
    - `.planning/phases/24-native-video-player/24-HUMAN-UAT.md` (alternate scaffold reference)
    - `.planning/phases/28-social-pickem-leaderboards/28-VALIDATION.md` § Manual-Only Verifications (5 manual UAT items already pre-defined; expand into device-UAT scripts)
    - `.planning/phases/28-social-pickem-leaderboards/28-UI-SPEC.md` § Surface map + § Discoverability constraint (the surface entry points UAT must verify)
  </read_first>
  <action>
**Step A — Capture deploy evidence in commit message body** (the deploy steps from Task 6.2 are already executed; this commit codifies the result):

Three atomic commits codifying the deploy evidence:
- queuenight commit (already made during Task 6.2 step 1): `chore(28-06): deploy gameResultsTick + pickReminderTick CFs to queuenight-84044/us-central1`
- queuenight commit (already made during Task 6.2 step 2): `chore(28-06): deploy firestore.rules with Phase 28 picks/leaderboards/picks_reminders rule blocks`
- couch commit (made during Task 6.2 step 3 via bash scripts/deploy.sh which auto-commits the sw.js CACHE bump): `chore(28-06): bump sw.js CACHE to couch-v40-pickem via bash scripts/deploy.sh 40-pickem (PICK-28-23 + PICK-28-24)`

If the deploy.sh did not auto-commit, manually commit:
```bash
cd ~/couch && git add sw.js && git commit -m "chore(28-06): bump sw.js CACHE to couch-v40-pickem via bash scripts/deploy.sh 40-pickem (PICK-28-23 + PICK-28-24)

Production curl-verified at 2026-05-XXTXX:XX:XXZ:
$ curl -s https://couchtonight.app/sw.js | grep CACHE
const CACHE = 'couch-v40-pickem';

Cross-repo deploy chain:
1. queuenight: firebase deploy --only functions (gameResultsTick + pickReminderTick)
2. queuenight: firebase deploy --only firestore:rules (picks/leaderboards/picks_reminders blocks)
3. couch: bash scripts/deploy.sh 40-pickem (sw.js bump + smoke gate + hosting)
"
```

**Step B — Create `.planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md`:**

Copy the structure from `.planning/phases/27-guest-rsvp/27-HUMAN-UAT.md` and adapt for Phase 28. Target ≥10 device-UAT scripts.

```markdown
---
phase: 28-social-pickem-leaderboards
status: pending-device-uat
created: 2026-05-02
deployed: 2026-05-02
cache: couch-v40-pickem
scripts: 11
resume_signal: "uat passed"
next_workflow: /gsd-verify-work 28
---

# Phase 28 — Social pick'em + leaderboards — Device UAT scripts

11 scripts covering Phase 28 user-facing behaviors. Run on real devices (iOS Safari PWA + Android Chrome where indicated). Reply "uat passed" when all scripts pass to trigger `/gsd-verify-work 28`.

> **Pre-UAT setup:** Force PWA cache refresh on all test devices: install screen → close → reopen. The new CACHE `couch-v40-pickem` should activate within 1-2 reloads (service worker auto-revalidates on next online activation).

---

## Script 1 — Pick'em discoverability from Tonight tab (PICK-28-03 / D-07 / UI-SPEC Discoverability)

**Setup:** Family with at least one upcoming game in any league this week (NFL/NBA/EPL/etc.).
**Steps:**
1. Open the app on Tonight tab.
2. Look in the `.tab-hero-sub` area for the inline italic link "Tonight: {LEAGUE} {SLATE_CONTEXT} has {N} games. *Open pick'em.*"
3. Tap "Open pick'em."
**Pass:** Pick'em sub-surface opens within 1 tap from Tonight; surface URL/state shows screen-pickem; <=2 taps from app launch.
**Fail signal:** Inline link missing OR opening pick'em takes >2 taps.

---

## Script 2 — team_winner picker variant (PICK-28-04 / D-02)

**Setup:** Pick'em surface open; an NBA / NFL / MLB / NHL / WNBA / NCAAF / NCAAB game upcoming this week.
**Steps:**
1. Locate a game in one of the team_winner leagues.
2. Confirm picker card shows 2 chips with team names ({Away} @ {Home} or similar).
3. Each chip is at least 44px tall (visual approximation OK).
4. Tap one chip.
**Pass:** Tapped chip gets 2px solid amber/--accent border; pick is saved within 2s; chip stays "selected" on re-render.
**Fail signal:** Chip tap doesn't register OR border doesn't appear OR pick reverts.

---

## Script 3 — team_winner_or_draw soccer picker variant (PICK-28-04 / D-02)

**Setup:** Pick'em surface open; an EPL / La Liga / Bundesliga / Serie A / Ligue 1 / UCL / MLS game upcoming.
**Steps:**
1. Locate a soccer game.
2. Confirm picker card shows 3 chips: {Home} / Draw / {Away}.
3. Tap "Draw."
**Pass:** Draw chip gets --accent ring; pick saved with selection.result === 'draw'.

---

## Script 4 — F1 podium variant (PICK-28-05 / D-02)

**Setup:** F1 race upcoming this week.
**Steps:**
1. Locate the F1 race card.
2. Confirm picker shows 3 rows labeled P1 / P2 / P3 (each a tap-to-open dropdown).
3. Select 3 distinct drivers.
**Pass:** Pick saved with selection.p1/p2/p3 set; UI confirms selection on re-render.
**Note:** If TheSportsDB free tier doesn't expose driver list (per RESEARCH Risk 7), executor's fallback to free-text input is acceptable; report which mode is active.

---

## Script 5 — UFC winner+method variant (PICK-28-06 / D-02)

**Setup:** UFC event upcoming this week.
**Steps:**
1. Locate UFC card.
2. Confirm picker shows 2-chip fighter row + 3-chip method row "KO" / "SUB" / "DEC".
3. Select fighter + method.
**Pass:** Both selections required to enable submit; pick saved with selection.winningFighter + selection.method.

---

## Script 6 — Tiebreaker designation + numeric input (PICK-28-07 / D-05)

**Setup:** Slate with at least 2 games (e.g. NFL Sunday with multiple games).
**Steps:**
1. Open the slate (e.g. NFL Sunday).
2. Confirm exactly ONE game has the italic "(tiebreaker)" annotation next to the matchup line.
3. Confirm that game shows a "Predict total points (tiebreaker only)" numeric input.
4. Verify the tiebreaker game is the chronologically-LAST game (Sunday Night Football for NFL Sunday).
5. Enter a number; confirm it persists.
**Pass:** Exactly 1 tiebreaker per slate; numeric input renders; latestGameInSlate logic correctly identifies last game.

---

## Script 7 — D-09 soft pre-fill from teamAllegiance

**Setup:** Member has set teamAllegiance to a team in an upcoming team_winner game (e.g. allegiance = "Lakers" + game = "Lakers @ Bucks").
**Steps:**
1. Open the picker for that game.
2. Confirm the Lakers chip renders with --team-color border (NOT --accent).
3. Confirm the brand-voice italic sub-line below: "*Lakers - your pick from your team commitment. Tap to change.*"
4. Tap the OTHER chip (Bucks).
**Pass:** Pre-fill renders correctly; tapping other chip transfers --accent ring; pick is saved as selection.winningTeam = 'away' (or whichever Bucks corresponds to).

---

## Script 8 — D-10 real-time pick visibility (TWO-DEVICE TEST)

**Setup:** Two devices logged in as different family members in the same family.
**Steps:**
1. Device A: Submit a pick on game X.
2. Device B (within ~1s): Observe the picker card update to show Device A's pick visible (e.g. inline wp pick row updates OR leaderboard pick count tick).
**Pass:** Pick appears on Device B within 1-2 seconds (real-time onSnapshot per D-10).
**Fail signal:** Device B requires manual refresh to see Device A's pick.

---

## Script 9 — Pick lock at gameStartTime (PICK-28-10)

**Setup:** Game with gameStartTime within the next 5-10 min (use a real upcoming game).
**Steps:**
1. Submit a pick before gameStartTime; confirm chip shows --accent ring.
2. Wait until gameStartTime passes.
3. Reload the surface.
4. Try to tap the chip to change the pick.
**Pass:** Picker shows "Picks closed" pill (--warn) after gameStartTime; chip is disabled; tap does nothing OR shows the friendly Pitfall 3 message ("This pick locked while you were tapping").
**Fail signal:** Pick can be edited after gameStartTime (Firestore rules failure).

---

## Script 10 — Inline wp pick row in live-wp modal (PICK-28-13 / D-07)

**Setup:** A wp.mode === 'game' watchparty exists; at least 1 family member has picked.
**Steps:**
1. Open the live-wp modal for the game.
2. Look for "Picks · You: {Team} · Mom: {Team} · Dad: {Team}" row inside the modal (between participant strip and reactions area).
3. If you haven't picked: confirm the row shows "You haven't picked yet — *make your pick*" inline link; tap the link.
**Pass:** Inline pick row renders pre-tip-off; disappears at gameStartTime; updates real-time as members pick.

---

## Script 11 — Cache bump activation (PICK-28-23)

**Setup:** Phase 27 PWA installed (couch-v39-guest-rsvp).
**Steps:**
1. Force-quit the PWA on iOS / clear app cache on Android.
2. Reopen the PWA.
3. Open Settings/About OR run `curl -s https://couchtonight.app/sw.js | grep CACHE` from a desktop browser.
**Pass:** Cache version is now `couch-v40-pickem`; service worker has activated the new cache.
**Fail signal:** Old cache `couch-v39-guest-rsvp` still showing after 2 reload cycles.

---

**Resume signal:** When all 11 scripts pass, reply `uat passed` to trigger `/gsd-verify-work 28`. If any script fails, document the failure and which PICK-28-* requirement it surfaces; planner will scope a gap-closure plan via `/gsd-plan-phase 28 --gaps`.
```

**Step C — Commit the HUMAN-UAT scaffold:**

```bash
cd ~/couch && git add .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md && git commit -m "docs(28-06): scaffold 28-HUMAN-UAT.md with 11 device-UAT scripts (PICK-28-03..23 user-facing coverage)"
```
  </action>
  <verify>
    <automated>[ -f .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md ] && [ "$(wc -l < .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md)" -ge 100 ] && grep -q "uat passed" .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md && grep -q "couch-v40-pickem" .planning/phases/28-social-pickem-leaderboards/28-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <done>
    - 28-HUMAN-UAT.md exists, ≥100 lines, ≥10 device-UAT scripts (target 11)
    - Resume signal documented: "uat passed" → /gsd-verify-work 28
    - Cache version `couch-v40-pickem` referenced in script 11
    - Scripts cover ALL 9 user-facing requirements: PICK-28-03 (discoverability) + PICK-28-04 (team_winner) + PICK-28-05 (f1) + PICK-28-06 (ufc) + PICK-28-07 (tiebreaker) + PICK-28-08 (D-09 pre-fill) + PICK-28-12 (D-10 real-time) + PICK-28-13 (inline wp) + PICK-28-23 (cache bump)
    - One commit: docs(28-06): scaffold 28-HUMAN-UAT.md with 11 device-UAT scripts
    - All 4 deploy commits exist (2 in queuenight + 1 sw.js bump in couch + 1 HUMAN-UAT scaffold in couch)
    - Production curl evidence captured in commit message body
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local source tree → queuenight-84044 production | firebase deploy is the trust transition; deployment requires authenticated firebase CLI session |
| Production sw.js cache → installed PWAs | Cache bump triggers service-worker re-activation on next online; old PWAs revalidate |
| Production curl verification → STATE.md "shipped" status | Sole automated proof that the deploy actually landed at the user-facing URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-27 | Tampering | Deploy ritual order swapped (client first, then functions) | mitigate | CHECKPOINT enforces explicit order; functions deploy MUST go first because client code references new CF endpoints |
| T-28-28 | Repudiation | Deploy succeeds but production cache stays stale (CDN propagation) | mitigate | Production curl verification step (Task 6.2 step 4) confirms within 2 min; documented retry on stale cache |
| T-28-29 | Tampering | Smoke floor lowered to make broken code pass | mitigate | FLOOR=13 hardcoded; failing the gate forces fixing missing sentinels rather than reducing the floor (intended safeguard against silent regression) |
| T-28-30 | Information Disclosure | Production deploy logs leak family-data context | accept | firebase CLI logs only deployment metadata (function names, region, runtime version); no Firestore data exposed |
| T-28-31 | DoS | gameResultsTick deploys but TheSportsDB free-tier hits rate limit at scale | accept | v1 family count (~5) is well under free-tier limits per RESEARCH OQ-4; per-league active-window gate documented for ~100-family activation point |
</threat_model>

<verification>
- scripts/smoke-pickem.cjs FLOOR raised to 13; floor passes locally
- All 11 smoke contracts pass (full aggregate ~5s)
- queuenight functions deploy: 2 NEW CFs land (gameResultsTick + pickReminderTick) in us-central1
- queuenight rules deploy: picks/leaderboards/picks_reminders blocks live; rules tests 56/56 PASS locally
- couch hosting deploy: sw.js bumped to couch-v40-pickem; smoke gate green; hosting deploy succeeded
- Production curl: `curl -s https://couchtonight.app/sw.js | grep CACHE` returns `const CACHE = 'couch-v40-pickem';`
- 4 atomic commits across both repos (2 queuenight deploy chores + 1 couch sw.js bump + 1 couch HUMAN-UAT scaffold)
- 28-HUMAN-UAT.md scaffolded with 11 scripts; resume signal documented
</verification>

<success_criteria>
- All 24 PICK-28-* IDs codified at smoke / rules-test / HUMAN-UAT layer (full traceability)
- Production cache version is couch-v40-pickem (curl-verified)
- 2 new Cloud Functions live in queuenight-84044/us-central1 (gameResultsTick + pickReminderTick)
- Firestore rules updated in production with Phase 28 picks/leaderboards/picks_reminders blocks
- 11 device-UAT scripts ready for user execution
- Phase 28 status: SHIPPED end-to-end (code + deploy + smoke gate locked + UAT scaffold)
- Wave 5 / next phase unblocked: user runs through 28-HUMAN-UAT.md → reply "uat passed" → /gsd-verify-work 28 promotes Phase 28 to fully verified
- STATE.md update follows Phase 26 / 27 close-out pattern: SHIPPED 2026-05-XX (v40-pickem; cross-repo deploy ritual queuenight functions + queuenight rules + couch hosting; 11 device-UAT scripts pending)
</success_criteria>

<output>
After completion, create `.planning/phases/28-social-pickem-leaderboards/28-06-SUMMARY.md` with:
- Final tally of all 24 PICK-28-* IDs and where each was codified (smoke layer / rules-test layer / HUMAN-UAT layer)
- Cross-repo deploy commit hashes (4 total)
- Production curl evidence (timestamp + CACHE value)
- Smoke contract count: 11 contracts; smoke-pickem.cjs final assertion total
- Reference to 28-HUMAN-UAT.md as the next user-facing handoff
</output>
