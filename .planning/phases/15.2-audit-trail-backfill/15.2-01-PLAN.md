---
phase: 15.2-audit-trail-backfill
plan: 01
plan_id: 15.2-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-auth-groups/05-VERIFICATION.md
  - .planning/phases/05-auth-groups/05-01-SUMMARY.md
  - .planning/phases/05-auth-groups/05-09-SUMMARY.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
gap_closure: false
verifier_method: retroactive_backfill_phase_15.2
must_haves:
  truths:
    - "05-VERIFICATION.md exists with all 5 AUTH-* requirements documented"
    - "05-01-SUMMARY.md exists with canonical requirements_completed frontmatter key"
    - "05-09-SUMMARY.md exists (or this plan documents its absence as deferred-UAT-summary)"
    - "AUTH-* requirement evidence cites function definitions + grep-verifiable counts (NOT enumerated call sites)"
    - "DESIGN-01-equivalent unsatisfied marker NOT introduced (Phase 5 has no unsatisfied REQs)"
  artifacts:
    - path: ".planning/phases/05-auth-groups/05-VERIFICATION.md"
      provides: "Phase 5 retroactive verification report mirroring 14-VERIFICATION.md style"
      contains: "AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05"
    - path: ".planning/phases/05-auth-groups/05-01-SUMMARY.md"
      provides: "Plan 05-01 summary backfill — auth infrastructure (firebase.js + js/auth.js + state extensions)"
      contains: "requirements_completed: [AUTH-01]"
    - path: ".planning/phases/05-auth-groups/05-09-SUMMARY.md"
      provides: "Plan 05-09 summary backfill — iOS PWA UAT checkpoint"
      contains: "requirements_completed: [AUTH-05]"
  key_links:
    - from: "05-VERIFICATION.md Required Artifacts table"
      to: "writeAttribution function definition + grep-verified call-site count"
      via: "function-name + count citation pattern (Pitfall 1)"
      pattern: "writeAttribution.*[0-9]+ call sites"
    - from: "05-VERIFICATION.md Requirements Coverage table"
      to: "Plan SUMMARYs 05-01..09"
      via: "Evidence column citing SUMMARY filenames + commits"
      pattern: "05-0[1-9]-SUMMARY"
    - from: "05-01-SUMMARY.md frontmatter"
      to: "requirements_completed key"
      via: "canonical YAML key per Pitfall 5"
      pattern: "^requirements_completed:"
---

<objective>
Backfill Phase 5 (Auth + Groups) verification artifacts that were never produced when the phase shipped 2026-04-21..22. The code is in production (couchtonight.app v35.1-security-hardening; Firebase Auth Google + Email-link + Phone live; 6 Cloud Functions deployed to queuenight-84044 us-central1; 109 writeAttribution call sites). Only the audit trail is missing.

Purpose: Close the orphaned-REQ gap for AUTH-01..05 surfaced by the v33.3 milestone audit. Restore confidence in the GSD verification trail without re-running the phase.
Output: 05-VERIFICATION.md (mirrors 14-VERIFICATION.md style) + 05-01-SUMMARY.md (auth infrastructure plan) + 05-09-SUMMARY.md (iOS PWA UAT checkpoint plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v33.3-MILESTONE-AUDIT.md
@.planning/phases/15.2-audit-trail-backfill/15.2-RESEARCH.md
@.planning/phases/14-decision-ritual-core/14-VERIFICATION.md
@.planning/phases/05-auth-groups/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill 05-VERIFICATION.md from audit evidence + existing SUMMARYs</name>
  <files>.planning/phases/05-auth-groups/05-VERIFICATION.md</files>
  <read_first>
    - .planning/v33.3-MILESTONE-AUDIT.md (lines 103-141 — AUTH-01..05 evidence blocks; lines 470-486 — phase verification picture)
    - .planning/phases/14-decision-ritual-core/14-VERIFICATION.md (CANONICAL STYLE TEMPLATE — mirror its YAML frontmatter + section order: Goal Achievement / Required Artifacts / Key Link Verification / Data-Flow Trace / Behavioral Spot-Checks / Requirements Coverage / Anti-Patterns Found / Human Verification Required / Gaps Summary / Recommendation)
    - .planning/phases/05-auth-groups/05-02-SUMMARY.md (existing SUMMARY for AUTH-01 baseline; cite must_haves block)
    - .planning/phases/05-auth-groups/05-03-SUMMARY.md, 05-04-SUMMARY.md, 05-05-SUMMARY.md, 05-06-SUMMARY.md, 05-07-SUMMARY.md, 05-08-SUMMARY.md (the 6 already-shipped SUMMARYs that document AUTH-02..05 coverage)
    - .planning/phases/05-auth-groups/05-CONTEXT.md (locked decisions for Phase 5)
    - .planning/REQUIREMENTS.md lines 36-39 (AUTH-01..05 acceptance criteria)
  </read_first>
  <action>
Create `.planning/phases/05-auth-groups/05-VERIFICATION.md` mirroring the structure of `14-VERIFICATION.md`. Use **forensic-backfill** semantics — the verifier method is reconstruction from existing evidence, NOT live re-verification.

YAML frontmatter (verbatim shape — DO NOT improvise field names):
```yaml
---
status: passed
phase: 05-auth-groups
verified_at: <today's ISO timestamp>
verifier_method: retroactive_backfill_phase_15.2
score: 5/5 AUTH-* requirements code-verified
overrides_applied: 0
human_verification:
  - test: "Multi-account runtime UAT (Plan 05-09 iOS PWA UAT checkpoint)"
    expected: "Sign-in flows (Google + Email-link + Phone) work on installed iOS Safari PWA; family create/join/leave + password-protected joining + claim-flow migration all functional"
    why_human: "Multiple Firebase Auth providers + iOS PWA standalone mode + multi-device claim flow require physical hardware sessions; deferred per project-pattern 'deploy first, UAT later' (referenced in 07-05-SUMMARY) and rolled into broader environmental-UAT closure noted in ROADMAP §22"
---
```

Body sections (in this exact order, mirroring `14-VERIFICATION.md`):

1. **# Phase 5: Auth + Groups — Verification Report** with sub-line "**Phase Goal:** Add account-backed identity + group ownership + claim-flow migration; preserve all pre-auth feature surfaces zero-data-loss." Note `**Re-verification:** No — retroactive backfill per Phase 15.2; original verification artifact never produced (v33.3 milestone audit gap).`

2. **## Goal Achievement → ### Observable Truths** — table with columns `# | Truth (AUTH-* requirement) | Status | Evidence`. Five rows, one per REQ-ID. Status values: `✓ VERIFIED` for AUTH-01..04; AUTH-05 `✓ CODE / ⏳ HUMAN-UAT` (multi-account runtime per Pitfall 3). Evidence column cites:
    - AUTH-01: `js/firebase.js` Firebase Auth init + `onAuthStateChanged` listener + `signInAnonymously` fallback. Plans 05-01 + 05-02 + 05-06; F-W-5 from audit.
    - AUTH-02: `writeAttribution` function + grep-verified call-site count. Run `grep -c writeAttribution js/app.js` at execute time and embed the live number (research saw 109; audit YAML said 103 — use whatever live grep returns). Plans 05-03 + 05-04 + 05-05 + 05-06 + 05-07.
    - AUTH-03: Cloud Functions `setGroupPassword` + `joinGroup` deployed to queuenight-84044. Plans 05-03 + 05-04 + 05-07.
    - AUTH-04: Cloud Functions `claimMember` + `mintClaimTokens` deployed. Plans 05-03 + 05-04 + 05-08.
    - AUTH-05: Three E2E flows still working post-migration (Tonight + Watchparty + Tracking) per audit YAML lines 372-381. Plans 05-04 + 05-05 + 05-08 + 05-09.

3. **### Required Artifacts** — table `Artifact | Expected | Status | Details`. Cite per Pitfall 1 (function name + count, NOT call-site enumeration):
    - `js/firebase.js` (Firebase init + db + Auth exports) — ✓ VERIFIED — file exists per CLAUDE.md architecture line 22
    - `js/app.js` `writeAttribution` function — ✓ VERIFIED — definition + N call sites verified via `grep -c writeAttribution js/app.js`
    - `js/auth.js` (if it exists) — verify with `ls js/auth.js` at execute time; cite presence/absence
    - `queuenight/functions/index.js` 4 auth CFs (setGroupPassword + joinGroup + claimMember + mintClaimTokens) — ✓ VERIFIED — deployed to queuenight-84044 us-central1; "queuenight repo not git-tracked from couch — deploy itself is shipping surface" caveat per Pitfall 2
    - `queuenight/firestore.rules` — auth-aware rules from Plan 05-04 — ✓ VERIFIED
    - `app.html` sign-in screen + footer with Privacy/Terms links — ✓ VERIFIED — per 05-02-SUMMARY
    - `couchtonight.app/privacy.html` + `couchtonight.app/terms.html` — ✓ VERIFIED (deployed via 05-02 — Google OAuth verification footer requirement)

4. **### Key Link Verification** — table `From | To | Via | Status | Details`. Map: app.html sign-in → Firebase Auth via `signInWithPopup` ✓ WIRED; `onAuthStateChanged` → `state.auth` slot via boot listener ✓ WIRED; member writes → `writeAttribution(uid)` via 109 call-site policy ✓ WIRED; `setGroupPassword` CF → family doc `passwordHash` via callable invoke ✓ WIRED; claim-flow `mintClaimTokens` → email delivery via SendGrid (or whatever 05-08-SUMMARY documents).

5. **### Data-Flow Trace (Level 4)** — table `Artifact | Data Variable | Source | Produces Real Data | Status`. Sign-in screen → state.auth.uid; family create → families/{code} doc with ownerUid; member writes → writeAttribution stamps uid + actingMemberId; claim flow → consumeClaimToken CF mints membership.

6. **### Behavioral Spot-Checks** — `Behavior | Command/Source | Result | Status`. Include: `grep -c writeAttribution js/app.js` returns N≥100 ✓ PASS; `grep "writeAttribution" js/app.js | head -3` shows function call pattern ✓ PASS; production sign-in works (per F-W-5 audit + STATE.md production line "couchtonight.app accepts Google + Email-link + Phone sign-in").

7. **### Requirements Coverage** — table `Requirement | Source Plan | Description | Status | Evidence`. One row per AUTH-01..05. Match exactly to REQUIREMENTS.md lines 36-39.

8. **### Anti-Patterns Found** — note: AUTH-05 multi-account runtime UAT deferred (informational); queuenight/ git-tracking gap (warning — same pattern as 07-05-SUMMARY deviation block).

9. **### Human Verification Required** — single deferred entry: AUTH-05 multi-account runtime UAT deferred per project-pattern (per Pitfall 3 — preserve UAT-DEFERRED context).

10. **### Gaps Summary** — "**No goal-blocking gaps.** Phase 5's underlying user goal — account-backed identity + group ownership + claim-flow migration with zero data loss — is fully delivered. AUTH-01..05 all have implementation evidence in code + are deployed to production. Multi-account runtime UAT is deferred per project pattern (not a gap)."

11. **### Recommendation** — "Phase 5 goal achieved at the code + production-deploy level. Status: **SHIPPED 2026-04-21..22 with HUMAN-UAT pending (multi-account runtime)**. Verifier method: retroactive_backfill_phase_15.2 — original VERIFICATION.md never produced; this artifact reconstructs the verification trail from existing SUMMARYs (05-02..08) + audit YAML evidence (lines 103-141) + production-live state."

12. **Footer** — `_Verified: <ISO timestamp>_` and `_Verifier: Claude (gsd-planner / Phase 15.2 retroactive backfill)_`.

DO NOT enumerate individual writeAttribution call sites. Cite the count + the grep command. DO NOT create a Validation Architecture section (Nyquist N/A per RESEARCH §1; Phase 15.2 is pure-docs).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-auth-groups/05-VERIFICATION.md && grep -c "AUTH-01\|AUTH-02\|AUTH-03\|AUTH-04\|AUTH-05" .planning/phases/05-auth-groups/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md exists; grep returns ≥5 (one mention per REQ-ID); grep "verifier_method.*retroactive_backfill_phase_15.2" returns 1; no `## Validation Architecture` section (`grep -c "## Validation Architecture" 05-VERIFICATION.md` returns 0).</done>
</task>

<task type="auto">
  <name>Task 2: Backfill 05-01-SUMMARY.md (auth infrastructure plan)</name>
  <files>.planning/phases/05-auth-groups/05-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-auth-groups/05-01-PLAN.md (extract scope + must-haves; this is the source-of-truth for what 05-01 was supposed to ship)
    - .planning/phases/05-auth-groups/05-02-SUMMARY.md (CANONICAL STYLE TEMPLATE — clean ~100-line format; references "code-side SDK wiring (from 05-01)" which corroborates 05-01 shipped Firebase SDK init + onAuthStateChanged scaffolding)
    - .planning/phases/05-auth-groups/05-RESEARCH.md (web-push vs auth-provider research)
    - .planning/phases/05-auth-groups/05-CONTEXT.md (locked decisions affecting 05-01 scope)
  </read_first>
  <action>
Create `.planning/phases/05-auth-groups/05-01-SUMMARY.md` mirroring `05-02-SUMMARY.md` style.

YAML frontmatter (CANONICAL key — `requirements_completed`, NOT `requirements_addressed`, NOT tag-only — per Pitfall 5):
```yaml
---
phase: 05-auth-groups
plan: 05-01
status: complete
completed: <date — derive from git log of js/firebase.js between 2026-04-20 and 2026-04-21; if unknown, use "2026-04-21">
executor: Claude (retroactive backfill via Phase 15.2)
requirements_completed: [AUTH-01]
verifier_method: retroactive_backfill_phase_15.2
---
```

Body sections (mirror 05-02-SUMMARY.md ordering):

1. **# Plan 05-01 — Auth infrastructure: firebase.js + authDomain flip + js/auth.js module + state/constants extensions**

2. **## What landed** — bullet list reconstructed from 05-01-PLAN.md must-haves. Likely includes: Firebase Auth SDK initialized in `js/firebase.js`; `onAuthStateChanged` listener wired in `js/app.js` (or `js/auth.js` if that module was created — verify file existence at execute time via `ls js/auth.js`); `state.auth` slot added; bootstrap path enabling anonymous fallback for pre-auth backwards-compat; authDomain configured to `queuenight-84044.firebaseapp.com`.

3. **## Commits** — best-effort reconstruction. Run `git log --oneline --all --since=2026-04-20 --until=2026-04-22 -- js/firebase.js js/app.js | head -20` and cite the relevant ones. If exact hashes can't be located, use the Pitfall 10 fallback: cite the date range + "precise hashes lost to retroactive backfill — production deploy is the canonical shipping surface (couch-v35.1-security-hardening live at couchtonight.app)."

4. **## Must-haves checklist** — copy from 05-01-PLAN.md must_haves (read it first to extract).

5. **## Smoke tests** — "Sign-in screen renders; Firebase Auth SDK initialized without console errors; anonymous fallback triggers on onAuthStateChanged when uid is null." Reference per 05-02-SUMMARY's "code-side SDK wiring (from 05-01)" line.

6. **## What this enables** — Plan 05-02 (sign-in providers + privacy/terms footer) + 05-03 onwards (everything that depends on a uid being available).

7. **## Follow-ups / known gaps** — "None — 05-01 was infrastructure only; consumers in 05-02..09 took on UAT scope. Multi-account runtime UAT for AUTH-05 deferred per project pattern."

8. **## Reconstruction note** — "This SUMMARY was produced retroactively by Phase 15.2 (audit-trail backfill) on <today's date>. Original SUMMARY was never written when Plan 05-01 shipped 2026-04-20..21 (v33.3 milestone audit identified this gap, audit YAML lines 103-113). Evidence sources: 05-01-PLAN.md must-haves, 05-02-SUMMARY.md cross-references, production-live Firebase Auth at couchtonight.app, audit YAML."

Use canonical `requirements_completed` key — DO NOT use `requirements_addressed` (Phase 14 drift) or tag-only (Phase 15 drift).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-auth-groups/05-01-SUMMARY.md && grep -c "^requirements_completed:" .planning/phases/05-auth-groups/05-01-SUMMARY.md</automated>
  </verify>
  <done>File exists; grep "^requirements_completed:" returns 1 (canonical key present in YAML); grep "AUTH-01" returns ≥1; grep "verifier_method.*retroactive_backfill_phase_15.2" returns 1.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill 05-09-SUMMARY.md (iOS PWA UAT checkpoint)</name>
  <files>.planning/phases/05-auth-groups/05-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-auth-groups/05-09-PLAN.md (extract scope of UAT checkpoint plan)
    - .planning/phases/05-auth-groups/05-UAT.md (test plan)
    - .planning/phases/05-auth-groups/05-UAT-RESULTS.md (results — likely template-only or partial; document whichever applies)
    - .planning/phases/05-auth-groups/05-08-SUMMARY.md (preceding-plan-summary style reference)
  </read_first>
  <action>
First confirm the file is missing: `ls .planning/phases/05-auth-groups/05-09-SUMMARY.md` should return "No such file or directory" (per Assumption A2). If it already exists, abort this task and document in plan execution log.

Create `.planning/phases/05-auth-groups/05-09-SUMMARY.md` documenting the UAT checkpoint plan that was likely deferred (multi-account runtime UAT requires physical hardware + multiple Firebase Auth providers).

YAML frontmatter:
```yaml
---
phase: 05-auth-groups
plan: 05-09
status: deferred-uat
completed: <date if 05-UAT-RESULTS.md has any results; otherwise "deferred">
executor: Claude (retroactive backfill via Phase 15.2)
requirements_completed: [AUTH-05]
verifier_method: retroactive_backfill_phase_15.2
human_verification:
  - test: "Multi-account iOS PWA UAT — Google + Email-link + Phone provider parity"
    expected: "All 3 providers complete sign-in on installed iOS Safari PWA; family create + join + leave + claim-flow all work post-auth-migration; Tonight + Mood + Veto + Watchparty all functional"
    why_human: "Multiple physical devices + multiple Firebase Auth providers + iOS PWA standalone mode + multi-account claim flow — none programmatically reproducible from developer machine"
---
```

Body sections:

1. **# Plan 05-09 — iOS PWA UAT checkpoint: all providers + feature regression + migration scenarios**

2. **## What landed** — describe the UAT plan as written in 05-09-PLAN.md. The plan exists; the SUMMARY backfill documents the checkpoint state. If `05-UAT-RESULTS.md` shows any PASS results, document those; if it's template-only, note "UAT results template was created but not filled — runtime UAT deferred per project pattern of 'deploy first, environmental UAT later' (referenced in 07-05-SUMMARY + ROADMAP §22)."

3. **## UAT result rollup** — read `05-UAT-RESULTS.md` and reflect what's there. Whatever shape the results doc has at execute time, summarize.

4. **## Commits** — Plan 05-09 itself likely shipped no code (UAT-only checkpoint plan). Cite "no code commits — UAT plan execution only."

5. **## Must-haves checklist** — copy from 05-09-PLAN.md must_haves.

6. **## What this enables** — "Closes Phase 5 verification cycle. AUTH-05 acceptance: 'All existing features continue to work end-to-end after the auth migration.' Verified at code level by audit YAML lines 372-381 (3 E2E flows complete: Tonight + Watchparty + Tracking)."

7. **## Follow-ups / known gaps** — "Multi-account runtime UAT deferred. Tracked under ROADMAP §22 'multi-account runtime UAT items deferred as environmental.' Roll into broader environmental-UAT closure scheduled post-Phase-15.4."

8. **## Reconstruction note** — same shape as Task 2's reconstruction note, adjusted for 05-09 context.

Use canonical `requirements_completed: [AUTH-05]` key.

If at execute time `05-UAT-RESULTS.md` shows the UAT was actually run + passed, change `status: deferred-uat` to `status: complete` and remove the human_verification block. The truth on disk wins; this task description is a starting hypothesis per Pitfall 6 ("disk is correct; ROADMAP is stale").
  </action>
  <verify>
    <automated>test -f .planning/phases/05-auth-groups/05-09-SUMMARY.md && grep -c "AUTH-05" .planning/phases/05-auth-groups/05-09-SUMMARY.md</automated>
  </verify>
  <done>File exists; grep "AUTH-05" returns ≥1; grep "^requirements_completed:" returns 1; grep "verifier_method.*retroactive_backfill_phase_15.2" returns 1.</done>
</task>

</tasks>

<threat_model>
N/A — pure documentation phase, no new code paths or attack surface introduced; affects only `.planning/` artifacts and project notes.
</threat_model>

<verification>
Phase 5 verification artifacts complete when:
- 05-VERIFICATION.md exists with all 5 AUTH-* REQ-IDs documented (truths + artifacts + key links + coverage tables)
- 05-01-SUMMARY.md + 05-09-SUMMARY.md exist with canonical `requirements_completed` frontmatter key
- All three new files carry `verifier_method: retroactive_backfill_phase_15.2` to distinguish forensic backfill from live verification
- No `## Validation Architecture` section anywhere (Nyquist N/A per RESEARCH §1)
- No queuenight/ commits referenced (queuenight is deploy-only, not git-tracked from couch — per Pitfall 2)

Acceptance grep:
```bash
test -f .planning/phases/05-auth-groups/05-VERIFICATION.md
test -f .planning/phases/05-auth-groups/05-01-SUMMARY.md
test -f .planning/phases/05-auth-groups/05-09-SUMMARY.md
grep -c "AUTH-0[1-5]" .planning/phases/05-auth-groups/05-VERIFICATION.md  # ≥5
grep -c "^requirements_completed:" .planning/phases/05-auth-groups/05-01-SUMMARY.md  # =1
grep -c "^requirements_completed:" .planning/phases/05-auth-groups/05-09-SUMMARY.md  # =1
grep -c "## Validation Architecture" .planning/phases/05-auth-groups/05-VERIFICATION.md  # =0
```
</verification>

<success_criteria>
ROADMAP §238 success criterion 1 (partial): `05-VERIFICATION.md exists and documents satisfied REQ-IDs with code-evidence references` — DONE.
ROADMAP §239 success criterion 2 (partial): missing 05-01-SUMMARY backfilled — DONE.
Audit YAML lines 103-141 (AUTH-01..05 orphaned status): closes 5 of 32 orphaned REQ-IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-audit-trail-backfill/15.2-01-SUMMARY.md` with:
- requirements_completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
- artifacts_created: [05-VERIFICATION.md, 05-01-SUMMARY.md, 05-09-SUMMARY.md]
- audit_yaml_lines_closed: "lines 103-141 (5 orphaned REQ-IDs flipped to satisfied)"
- verifier_method: retroactive_backfill_phase_15.2
</output>
