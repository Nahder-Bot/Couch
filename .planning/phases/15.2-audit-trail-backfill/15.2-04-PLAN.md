---
phase: 15.2-audit-trail-backfill
plan: 04
plan_id: 15.2-04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-intent-flows/08-VERIFICATION.md
  - .planning/phases/08-intent-flows/08-01-SUMMARY.md
  - .planning/phases/08-intent-flows/08-02-SUMMARY.md
  - .planning/phases/08-intent-flows/08-03-SUMMARY.md
  - .planning/phases/08-intent-flows/08-04-SUMMARY.md
  - .planning/phases/08-intent-flows/08-05-SUMMARY.md
autonomous: true
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04, INTENT-05, INTENT-06]
gap_closure: false
verifier_method: retroactive_backfill_phase_15.2
must_haves:
  truths:
    - "08-VERIFICATION.md exists with all 6 INTENT-* requirements documented"
    - "All 5 plan SUMMARYs (08-01..08-05) exist with canonical requirements_completed frontmatter — Phase 8 had ZERO SUMMARYs pre-15.2 per audit"
    - "Phase 9 / 09-07a 'absorbed 08x-intent-cf-timezone seed' cross-phase closure noted in 08-VERIFICATION.md (NOT counted as Phase 8 gap)"
    - "Cross-repo deploy nuance documented (queuenight/functions onIntentCreated/onIntentUpdate per Pitfall 2)"
  artifacts:
    - path: ".planning/phases/08-intent-flows/08-VERIFICATION.md"
      provides: "Phase 8 retroactive verification report"
      contains: "INTENT-01, INTENT-02, INTENT-03, INTENT-04, INTENT-05, INTENT-06"
    - path: ".planning/phases/08-intent-flows/08-01-SUMMARY.md"
      provides: "Plan 08-01 summary backfill"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-intent-flows/08-02-SUMMARY.md"
      provides: "Plan 08-02 summary backfill"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-intent-flows/08-03-SUMMARY.md"
      provides: "Plan 08-03 summary backfill"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-intent-flows/08-04-SUMMARY.md"
      provides: "Plan 08-04 summary backfill"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-intent-flows/08-05-SUMMARY.md"
      provides: "Plan 08-05 summary backfill (likely UAT/checkpoint)"
      contains: "requirements_completed:"
  key_links:
    - from: "08-VERIFICATION.md Required Artifacts"
      to: "queuenight/functions/index.js (onIntentCreated ~:354, onIntentUpdate ~:408, intent-expiry sweep ~:494-512) + js/app.js (createIntent, convertIntent, openIntentScreen, maybeOpenIntentFromDeepLink)"
      via: "function-name + line-anchor citations"
      pattern: "onIntent(Created|Update)"
    - from: "08-VERIFICATION.md notes"
      to: "Phase 9 / 09-07a-SUMMARY.md (absorbs 08x-intent-cf-timezone seed)"
      via: "cross-phase closure reference per audit YAML line 191"
      pattern: "08x.*timezone|09-07a"
---

<objective>
Backfill Phase 8 (Watch-Intent Flows) verification artifacts. Phase 8 is the **largest single audit-trail gap** in the project: 5 PLANs exist (08-01..05), zero SUMMARYs, only template-only UAT-RESULTS.md. Code is in production: `intentsRef` collection populated, `onIntentCreated` CF deployed, 4 flow discriminators wired (later extended by Phase 14 for `rank-pick`/`nominate`), `intentProposed`/`intentMatched` push lockstep, deep-link routing live. Phase 14 absorbed the `08x-intent-cf-timezone` seed via Plan 09-07a.

Purpose: Close 6 orphaned INTENT-* REQ-IDs and produce phase-level VERIFICATION.md.
Output: 08-VERIFICATION.md + 5 plan SUMMARYs (08-01..08-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v33.3-MILESTONE-AUDIT.md
@.planning/phases/15.2-audit-trail-backfill/15.2-RESEARCH.md
@.planning/phases/14-decision-ritual-core/14-VERIFICATION.md
@.planning/phases/05-auth-groups/05-02-SUMMARY.md
@.planning/phases/07-watchparty/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill 08-VERIFICATION.md from audit evidence + production-live state</name>
  <files>.planning/phases/08-intent-flows/08-VERIFICATION.md</files>
  <read_first>
    - .planning/v33.3-MILESTONE-AUDIT.md (lines 180-226 — INTENT-01..06 evidence blocks; line 191 — Phase 9 / 09-07a absorbed 08x-intent-cf-timezone seed)
    - .planning/phases/14-decision-ritual-core/14-VERIFICATION.md (CANONICAL STYLE TEMPLATE)
    - .planning/phases/08-intent-flows/08-CONTEXT.md (locked decisions for Phase 8)
    - .planning/phases/08-intent-flows/08-UAT.md (test plan)
    - .planning/phases/08-intent-flows/08-UAT-RESULTS.md (results — likely template-only per audit)
    - .planning/phases/09-redesign-brand-marketing-surface/09-07a-SUMMARY.md (absorbs 08x-intent-cf-timezone seed)
    - .planning/REQUIREMENTS.md lines 63-70 (INTENT-01..06 acceptance criteria)
  </read_first>
  <action>
Create `.planning/phases/08-intent-flows/08-VERIFICATION.md` mirroring `14-VERIFICATION.md` structure.

YAML frontmatter:
```yaml
---
status: passed
phase: 08-intent-flows
verified_at: <today's ISO timestamp>
verifier_method: retroactive_backfill_phase_15.2
score: 6/6 INTENT-* requirements code-verified
overrides_applied: 0
human_verification:
  - test: "Multi-device intent flow E2E (08-UAT.md test plan)"
    expected: "Tonight @ time intent → other-member RSVP → threshold-aware auto-conversion to watchparty (rank-pick) or schedule (interest poll); orphan-doc cleanup confirmed; per-member RSVP shape persisted for Phase 10 YIR consumption"
    why_human: "Multi-device + push delivery + threshold edge cases require physical hardware. ROADMAP §25 indicates Phase 8 UAT was run via browser-Claude autonomous session 2026-04-22 — partial coverage. Multi-device runtime UAT remains environmentally deferred per project pattern."
---
```

Body sections — 11 sections mirroring 14-VERIFICATION.md.

**Goal:** "Split the existing 'who wants to watch' primitive into two first-class intent flows ('Tonight @ time' + 'Watch-this-title' interest poll); orchestrate threshold-aware conversion to watchparty/schedule; orphan-doc cleanup; YIR-consumable RSVP shape."

**Observable Truths table** — 6 rows, one per INTENT-01..06. Status mapping per audit YAML lines 180-226:
- INTENT-01: ✓ VERIFIED. Evidence: `intentsRef` collection + `onIntentCreated` CF + 4 flow discriminators wired (audit lines 184-191, integration finding F-W-3). Tonight @ time flow discriminator. RSVP yes/no/maybe.
- INTENT-02: ✓ VERIFIED. Same code path, different `flow` value (Watch-this-title interest poll). Yes/No/Later RSVP shape. Audit line 198.
- INTENT-03: ✓ VERIFIED. `convertIntent` routes to `openWatchpartyStart` (Tonight @ time) or `openScheduleModal` (interest poll). Audit line 205.
- INTENT-04: ✓ VERIFIED. Group-size threshold rules wired in CF `onIntentUpdate` (any-yes for duos; majority for families; configurable per family). Audit line 212.
- INTENT-05: ✓ VERIFIED. Cleanup pattern shared with VETO-01/PARTY-06 lineage; intent-expiry sweep in `watchpartyTick` (audit line 219).
- INTENT-06: ✓ VERIFIED. Per-member RSVP recording in `intents/{id}.rsvps[mid]` shape. Phase 14 extends this for `flow: rank-pick / nominate`. Audit line 226.

**Required Artifacts table** — line-anchored citations:
- `js/app.js` — `createIntent`, `convertIntent`, `openIntentScreen`, `?intent=` deep-link router (`maybeOpenIntentFromDeepLink`). Verify line numbers via grep at execute time.
- `queuenight/functions/index.js` — `onIntentCreated` (~`:354`), `onIntentUpdate` (~`:408`), intent-expiry sweep in `watchpartyTick` (~`:494-512`). Cite per Pitfall 2: "queuenight repo not git-tracked from couch — deploy itself is shipping surface."
- `queuenight/firestore.rules` — `intents` collection rules at `:338-386` (Phase 14 widened these — this Phase 8 verification reflects the **pre-Phase-14 ruleset state** for INTENT-01..06 originals; do NOT cite Phase 14's `flow` enum widening as Phase 8 work).
- `app.html` — intent CTA surfaces (verify in app.html at execute time).

**Special note (per audit YAML line 191):** "Phase 14 / Plan 09-07a absorbed seed `08x-intent-cf-timezone` (timezone fix in `onIntentCreated`). This was a follow-up issue surfaced during Phase 8 UAT and closed cross-phase via 09-07a. **NOT counted as a Phase 8 gap** — closed in a subsequent shipped phase per ROADMAP §25 'Phase 8 ✓ 2026-04-22 (UAT autonomous via browser-Claude; 1 seed for CF timezone echo)'."

**Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks** — fill per template.

**Requirements Coverage table** — 6 rows.

**Anti-Patterns Found:**
- queuenight/ git-tracking gap (warning, Pitfall 2 — same as Phase 5/6/7 pattern)
- 08x-intent-cf-timezone seed closed cross-phase via Plan 09-07a (informational, not a gap)
- 08-UAT-RESULTS.md likely template-only (per audit line 187) — UAT was browser-Claude autonomous per ROADMAP §25; document the actual state on disk

**Recommendation** — "Phase 8 goal achieved at the code + production-deploy level. Status: **SHIPPED 2026-04-22 (UAT browser-Claude autonomous + 1 cross-phase seed closed by 09-07a)**. Verifier method: retroactive_backfill_phase_15.2 — Phase 8 had ZERO SUMMARYs pre-15.2 per v33.3 audit (lines 180-191); this artifact reconstructs verification trail entirely from production-live CF deployment + audit YAML evidence + 09-07a cross-phase closure note."

DO NOT create a Validation Architecture section.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-intent-flows/08-VERIFICATION.md && grep -c "INTENT-0[1-6]" .planning/phases/08-intent-flows/08-VERIFICATION.md</automated>
  </verify>
  <done>08-VERIFICATION.md exists; grep "INTENT-0[1-6]" returns ≥6 (one per REQ-ID); grep "08x-intent-cf-timezone\|09-07a" returns ≥1 (cross-phase closure documented); verifier_method line present; no Validation Architecture section.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill all 5 plan SUMMARYs (08-01 through 08-05)</name>
  <files>.planning/phases/08-intent-flows/08-01-SUMMARY.md, .planning/phases/08-intent-flows/08-02-SUMMARY.md, .planning/phases/08-intent-flows/08-03-SUMMARY.md, .planning/phases/08-intent-flows/08-04-SUMMARY.md, .planning/phases/08-intent-flows/08-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-intent-flows/08-01-PLAN.md (~8663 bytes per disk listing — read for scope + must-haves)
    - .planning/phases/08-intent-flows/08-02-PLAN.md (~6384 bytes)
    - .planning/phases/08-intent-flows/08-03-PLAN.md (~8988 bytes)
    - .planning/phases/08-intent-flows/08-04-PLAN.md (~6819 bytes)
    - .planning/phases/08-intent-flows/08-05-PLAN.md (~3491 bytes — likely UAT/checkpoint plan)
    - .planning/phases/08-intent-flows/08-CONTEXT.md (~13855 bytes — locked decisions; D-XX → REQ-ID mapping)
    - .planning/phases/05-auth-groups/05-02-SUMMARY.md (CANONICAL STYLE TEMPLATE)
    - .planning/phases/07-watchparty/07-05-SUMMARY.md (cross-repo deploy nuance template)
  </read_first>
  <action>
For each of the 5 PLAN files, produce one SUMMARY.

**Per-plan REQ-ID mapping** (from RESEARCH §2.4 starting hypothesis — verify by reading actual PLAN frontmatter + 08-CONTEXT.md):

| File | Plan content (verify via PLAN.md read) | REQ-IDs (starting hypothesis) |
|------|----------------------------------------|-------------------------------|
| 08-01-SUMMARY.md | (read 08-01-PLAN.md to confirm) | INTENT-01, INTENT-05 |
| 08-02-SUMMARY.md | (read 08-02-PLAN.md) | INTENT-02 |
| 08-03-SUMMARY.md | (read 08-03-PLAN.md) | INTENT-03, INTENT-04 |
| 08-04-SUMMARY.md | (read 08-04-PLAN.md) | INTENT-04, INTENT-06 |
| 08-05-SUMMARY.md | (read 08-05-PLAN.md — likely UAT) | (UAT roll-up — likely all INTENT-*) |

**Critical:** REQ-ID-to-plan mapping above is a starting hypothesis. The planner MUST:
1. Open each `08-0N-PLAN.md` and extract the explicit `requirements:` field if present
2. Cross-reference with `08-CONTEXT.md` (13855 bytes — substantial doc with D-XX → REQ-ID mapping)
3. Cite the production-live evidence (CF deployed, `intentsRef` populated, deep-link routing working in v34.1+)

If 08-CONTEXT.md uses D-XX style decisions but no explicit REQ-ID mapping, infer mapping from acceptance criteria in REQUIREMENTS.md lines 63-70 (INTENT-01..06 descriptions) matched against PLAN scope language.

YAML frontmatter (canonical key per Pitfall 5):
```yaml
---
phase: 08-intent-flows
plan: 08-0N
status: complete
completed: 2026-04-22  # ROADMAP §25 says "Phase 8 ✓ 2026-04-22"
executor: Claude (retroactive backfill via Phase 15.2)
requirements_completed: [INTENT-XX, INTENT-YY]  # actual from PLAN scope + 08-CONTEXT cross-ref
verifier_method: retroactive_backfill_phase_15.2
---
```

For 08-05 (likely UAT checkpoint plan), set `status: complete` if 08-UAT-RESULTS.md shows results, OR `status: deferred-uat` if template-only. ROADMAP §25 says UAT was run via browser-Claude — adjust based on what 08-UAT-RESULTS.md actually contains on disk.

Body sections (mirror 05-02-SUMMARY.md style):

1. **# Plan 08-0N — <PLAN title from frontmatter>**
2. **## What landed** — extracted from 08-0N-PLAN.md must-haves
3. **## Commits** — best-effort. Run `git log --oneline --all --since=2026-04-21 --until=2026-04-23 --grep="08-0N\|INTENT\|intent" -- js/app.js queuenight/functions/index.js | head -10`. Phase 8 commits will be sparse on couch side (most CF work is queuenight). Apply Pitfall 10 fallback liberally.
4. **## Cross-repo note** — almost every Phase 8 plan touched queuenight CFs. Cite per Pitfall 2: "queuenight repo not git-tracked from couch — deploy itself is shipping surface; production-live in us-central1."
5. **## Smoke tests** — what UAT exercised
6. **## Must-haves checklist** — copy from 08-0N-PLAN.md
7. **## What this enables** — downstream consumers. For 08-04 + 08-05: "Phase 14 extended this primitive (DR-1 — `flow: rank-pick / nominate` discriminator). Phase 9 / 09-07a closed the 08x-intent-cf-timezone follow-up seed."
8. **## Reconstruction note** — "Produced retroactively by Phase 15.2 (audit-trail backfill) on <today>. Phase 8 had ZERO SUMMARYs prior to Phase 15.2 per v33.3 milestone audit (lines 180-191) — the largest single audit-trail gap in the project. UAT was browser-Claude autonomous run 2026-04-22 (per ROADMAP §25). Evidence sources: 08-0N-PLAN.md, 08-CONTEXT.md (D-XX decisions), production-live `intentsRef` + onIntentCreated CF at queuenight-84044, audit YAML lines 180-226."

**Special handling for 08-05-SUMMARY.md** (likely UAT checkpoint):
- Document UAT as browser-Claude autonomous run (per ROADMAP §25)
- 1 seed surfaced (08x-intent-cf-timezone) → closed cross-phase by Plan 09-07a
- Reference 09-07a-SUMMARY.md for the timezone fix details

ALL 5 SUMMARYs use canonical `requirements_completed` key. Coverage check after writing:
```bash
grep -h "^requirements_completed:" .planning/phases/08-intent-flows/08-0[1-5]-SUMMARY.md
```
Aggregate union should cover INTENT-01..06.
  </action>
  <verify>
    <automated>ls .planning/phases/08-intent-flows/08-0[1-5]-SUMMARY.md | wc -l</automated>
  </verify>
  <done>5 SUMMARY files exist (`ls` returns 5); grep "^requirements_completed:" across all 5 returns 5 lines; aggregate union covers INTENT-01 through INTENT-06; each file has `verifier_method: retroactive_backfill_phase_15.2`; 08-05-SUMMARY (UAT) references 08x-intent-cf-timezone seed + 09-07a closure.</done>
</task>

</tasks>

<threat_model>
N/A — pure documentation phase, no new code paths or attack surface introduced; affects only `.planning/` artifacts and project notes.
</threat_model>

<verification>
Phase 8 verification artifacts complete when:
- 08-VERIFICATION.md exists with all 6 INTENT-* REQ-IDs documented
- All 5 plan SUMMARYs (08-01..08-05) exist with canonical `requirements_completed` frontmatter
- All 6 files carry `verifier_method: retroactive_backfill_phase_15.2`
- Cross-phase closure (08x-intent-cf-timezone via 09-07a) documented in 08-VERIFICATION.md AND 08-05-SUMMARY.md
- queuenight cross-repo deploy nuance documented per Pitfall 2 in CF-touching SUMMARYs
- No `## Validation Architecture` section anywhere

Acceptance grep:
```bash
test -f .planning/phases/08-intent-flows/08-VERIFICATION.md
[ "$(ls .planning/phases/08-intent-flows/08-0[1-5]-SUMMARY.md 2>/dev/null | wc -l)" = "5" ]
grep -c "INTENT-0[1-6]" .planning/phases/08-intent-flows/08-VERIFICATION.md  # ≥6
grep -lc "verifier_method: retroactive_backfill_phase_15.2" .planning/phases/08-intent-flows/08-VERIFICATION.md .planning/phases/08-intent-flows/08-0[1-5]-SUMMARY.md  # 6 files match
grep -l "08x-intent-cf-timezone\|09-07a" .planning/phases/08-intent-flows/08-VERIFICATION.md  # =1
```
</verification>

<success_criteria>
ROADMAP §238 success criterion 1 (partial): `08-VERIFICATION.md exists` — DONE.
ROADMAP §239 success criterion 2 (partial): missing 08-01..05 SUMMARYs backfilled (the largest single SUMMARY gap) — DONE.
Audit YAML lines 180-226: closes 6 of 32 orphaned REQ-IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-audit-trail-backfill/15.2-04-SUMMARY.md` with:
- requirements_completed: [INTENT-01, INTENT-02, INTENT-03, INTENT-04, INTENT-05, INTENT-06]
- artifacts_created: [08-VERIFICATION.md, 08-01-SUMMARY.md, 08-02-SUMMARY.md, 08-03-SUMMARY.md, 08-04-SUMMARY.md, 08-05-SUMMARY.md]
- audit_yaml_lines_closed: "lines 180-226 (6 orphaned REQ-IDs flipped to satisfied)"
- cross_phase_closure_noted: "08x-intent-cf-timezone seed → Plan 09-07a"
- verifier_method: retroactive_backfill_phase_15.2
</output>
