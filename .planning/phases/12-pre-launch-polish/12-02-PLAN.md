---
phase: 12-pre-launch-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.html
  - js/constants.js
  - js/app.js
  - css/app.css
  - landing.html
  - changelog.html
  - C:/Users/nahde/queuenight/firebase.json
autonomous: true
requirements_addressed: [POL-02]
must_haves:
  truths:
    - "An ABOUT sub-section renders at the BOTTOM of the ADMIN cluster on Account tab, AFTER the .tab-footer Sign-out / Leave-family pills (per D-09)."
    - "Version line displays 'Couch v{N} — deployed {YYYY-MM-DD}' where {N} comes from APP_VERSION constant + {YYYY-MM-DD} from BUILD_DATE constant in js/constants.js."
    - "Feedback link is a mailto:nahderz@gmail.com?subject=Couch%20feedback%20v{N} where {N} is the live version."
    - "What's-new link is an https://couchtonight.app/changelog href that resolves to changelog.html via Firebase Hosting rewrite."
    - "TMDB attribution line ('Powered by The Movie Database (TMDB)') ships in BOTH the new ABOUT sub-section AND the landing.html footer (per D-19, POL-05 bundled into POL-02)."
    - "/changelog.html is a standalone page following landing.html posture: no app shell, no Firebase SDK, no /css/app.css, lists last 5 releases."
    - "firebase.json hosting.rewrites includes {source:'/changelog', destination:'/changelog.html'} placed BEFORE the catch-all '/' → '/landing.html' rewrite."
    - "sw.js CACHE is NOT bumped in this plan — Plan 12-01 owns the v32-pre-launch-polish bump."
  artifacts:
    - path: "js/constants.js"
      provides: "APP_VERSION + BUILD_DATE exported constants"
      contains: "export const APP_VERSION"
    - path: "app.html"
      provides: "ABOUT sub-section markup INSIDE settings-cluster[data-cluster=admin], AFTER .tab-footer"
      contains: "id=\"settings-about-section\""
    - path: "js/app.js"
      provides: "renderAboutSection that injects version line + feedback mailto + changelog link + TMDB attribution into #settings-about-section"
      exports: ["renderAboutSection"]
    - path: "css/app.css"
      provides: ".settings-about-section + .about-version + .about-link-row + .about-attribution token-backed styles"
      contains: ".settings-about-section"
    - path: "changelog.html"
      provides: "Standalone /changelog.html page mirroring landing.html posture (Phase 9 Plan 05). 5 release entries (v32, v31, v29, v23, v21)."
      contains: "v32 — Pre-launch polish"
    - path: "landing.html"
      provides: "TMDB attribution line added to .landing-footer"
      contains: "Powered by The Movie Database"
    - path: "C:/Users/nahde/queuenight/firebase.json"
      provides: "Hosting rewrite /changelog → /changelog.html"
      contains: "/changelog"
  key_links:
    - from: "app.html #settings-about-section"
      to: "js/app.js renderAboutSection"
      via: "renderAboutSection() invocation in renderSettings"
      pattern: "renderAboutSection"
    - from: "js/app.js renderAboutSection version line"
      to: "js/constants.js APP_VERSION + BUILD_DATE"
      via: "ES module import"
      pattern: "import.*APP_VERSION|import.*BUILD_DATE"
    - from: "app.html /changelog href"
      to: "queuenight/firebase.json hosting rewrite"
      via: "Firebase Hosting source-pattern match"
      pattern: "\"source\":\\s*\"/changelog\""
    - from: "landing.html .landing-footer"
      to: "TMDB attribution"
      via: "static HTML link with rel=noopener"
      pattern: "themoviedb\\.org"
---

<objective>
POL-02 + POL-05 (TMDB attribution bundled): Add an ABOUT sub-section at the bottom of the ADMIN cluster (Account tab) showing version + deploy date + Send-feedback mailto + What's-new link + TMDB attribution. Ship `/changelog.html` as a standalone page with the matching landing.html posture (zero JS, zero Firebase SDK). Wire the Firebase Hosting rewrite. Add TMDB attribution to landing.html footer too (currently missing — passive ToS violation per D-20).

Purpose: Cheap, high-signal user-trust polish per D-09..D-12. The version line gives users (and future-Claude diagnosing bug reports) a stable anchor. The mailto pre-fills the version in the subject line for traceability. The changelog page is a tiny static surface; no Firebase SDK keeps it small + cacheable.

Output: ABOUT section + version constants + changelog.html + landing TMDB attribution + firebase.json rewrite.

**This plan does NOT bump sw.js CACHE — Plan 12-01 owns the v32 bump.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/BRAND.md
@.planning/phases/12-pre-launch-polish/12-CONTEXT.md
@.planning/phases/09-redesign-brand-marketing-surface/09-05-SUMMARY.md
@CLAUDE.md
@landing.html

<interfaces>
<!-- Existing landing.html footer pattern, lines 153-157 -->
```html
<footer class="landing-footer">
  <!-- TODO pre-launch: wire footer legal links (privacy + terms) when legal copy lands. -->
  <p>© Couch</p>
</footer>
```

<!-- Existing ADMIN cluster end, app.html lines 704-712 -->
```html
<!-- Footer: sign out / leave family / replay intro, grouped quietly at the bottom -->
<div class="tab-footer">
  <button class="pill" onclick="replayOnboarding()">Replay intro</button>
  <button class="pill" onclick="signOut()">Sign out</button>
  <button class="pill" id="leave-family-btn" onclick="confirmLeaveFamily()">Leave family</button>
</div>
</div>  <!-- close settings-cluster admin -->
</div>  <!-- close screen-settings -->
```

<!-- Existing renderSettings call paths in js/app.js — find with: grep -n "function renderSettings" js/app.js -->
<!-- The existing render is invoked when the Account tab opens. New renderAboutSection
     plugs into the existing render lifecycle by being called at the end of renderSettings. -->

<!-- queuenight/firebase.json existing rewrites (DO NOT REORDER /app or /rsvp/**) -->
```json
"rewrites": [
  { "source": "/app", "destination": "/app.html" },
  { "source": "/app/**", "destination": "/app.html" },
  { "source": "/rsvp/**", "destination": "/rsvp.html" },
  { "source": "/", "destination": "/landing.html" }
]
```
<!-- /changelog rewrite must be inserted BEFORE the / catch-all per Firebase Hosting
     spec. Order will become: /app, /app/**, /rsvp/**, /changelog, /. -->

<!-- js/constants.js export pattern -->
```javascript
// Existing exports e.g. TMDB_KEY, MOODS, COUCH_NIGHTS_PACKS …
// Add: export const APP_VERSION = 32;
//      export const BUILD_DATE = 'YYYY-MM-DD';   (set at deploy/commit time)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add APP_VERSION + BUILD_DATE constants + render ABOUT sub-section markup + handler</name>
  <files>js/constants.js, app.html, js/app.js</files>
  <read_first>
    - js/constants.js (full — small file, ~770 lines: where to add new exports)
    - app.html lines 700-720 (existing .tab-footer end of ADMIN cluster + settings-cluster close tags)
    - js/app.js — Grep for "function renderSettings" and read 30 lines around it to find the lifecycle hook for ABOUT injection
    - .planning/BRAND.md (§6 voice — sentence case, italic for descriptions, mailto copy posture)
    - .planning/phases/12-pre-launch-polish/12-CONTEXT.md (D-09, D-10, D-19)
  </read_first>
  <action>
    **Step 1 (js/constants.js):** Add at the END of js/constants.js (after `COUCH_NIGHTS_PACKS` array):

    ```javascript
    // Phase 12 / POL-02 — App version + build date.
    // Keep APP_VERSION in lockstep with sw.js CACHE major version (e.g. CACHE
    // 'couch-v32-pre-launch-polish' → APP_VERSION 32). BUILD_DATE is the deploy
    // date in ISO 8601 (UTC); set at hand-edit time when shipping a release.
    // Surfaced in Account → ABOUT and used as mailto subject suffix.
    export const APP_VERSION = 32;
    export const BUILD_DATE = '2026-04-25';
    ```

    **Step 2 (app.html):** Add a NEW `<div class="tab-section" id="settings-about-section">` block AFTER the existing `.tab-footer` (currently at app.html line 704-711) but BEFORE the closing `</div>` of the `settings-cluster[data-cluster=admin]` block. Per D-09 the eyebrow reads "ABOUT" small dim caps. Use existing `.settings-cluster-h` and `.tab-section-h` patterns observed at app.html lines 583, 587. Markup:

    ```html
    <!-- Phase 12 / POL-02 — ABOUT sub-section (version + feedback + changelog + TMDB attribution).
         Rendered by renderAboutSection() at the end of renderSettings(). The version line
         pulls from APP_VERSION + BUILD_DATE in js/constants.js. -->
    <div class="tab-section settings-about-section" id="settings-about-section">
      <div class="tab-section-h"><span>ABOUT</span></div>
      <div class="about-body">
        <p class="about-version" id="about-version-line">—</p>
        <div class="about-link-row">
          <a class="about-link" id="about-feedback-link" href="mailto:nahderz@gmail.com">Send feedback</a>
          <span class="about-link-sep">·</span>
          <a class="about-link" id="about-changelog-link" href="/changelog">See what's new</a>
        </div>
        <p class="about-attribution"><em>Powered by <a href="https://www.themoviedb.org/" target="_blank" rel="noopener noreferrer">The Movie Database (TMDB)</a></em></p>
      </div>
    </div>
    ```

    Insertion location: after line 711 (closing `</div>` of `.tab-footer`) and BEFORE the line that closes the `settings-cluster[data-cluster=admin]` div.

    **Step 3 (js/app.js):** Add `APP_VERSION, BUILD_DATE` to the existing import line at js/app.js:2 (which already imports many constants from `./constants.js`). Then add a render function. Locate the existing `renderSettings` function via `grep -n "function renderSettings\|window.renderSettings" js/app.js` and add a call to `renderAboutSection()` at its end. The new function:

    ```javascript
    // Phase 12 / POL-02 — Inject version + feedback + changelog + TMDB attribution
    // into #settings-about-section. Idempotent — safe to call on every renderSettings.
    function renderAboutSection() {
      const versionEl = document.getElementById('about-version-line');
      const feedbackEl = document.getElementById('about-feedback-link');
      const changelogEl = document.getElementById('about-changelog-link');
      if (!versionEl) return;
      const v = APP_VERSION;
      const d = BUILD_DATE;
      versionEl.textContent = `Couch v${v} — deployed ${d}`;
      if (feedbackEl) {
        const subj = encodeURIComponent(`Couch feedback v${v}`);
        feedbackEl.href = `mailto:nahderz@gmail.com?subject=${subj}`;
      }
      if (changelogEl) {
        // Absolute URL when on production; relative otherwise (dev). The hosting rewrite
        // will resolve /changelog → /changelog.html on the prod CDN.
        changelogEl.href = '/changelog';
      }
    }
    window.renderAboutSection = renderAboutSection;
    ```

    Then call `renderAboutSection();` at the END of the existing `renderSettings` function body (after all existing renders complete).

    **Note:** Do NOT add any new ES module imports beyond `APP_VERSION` and `BUILD_DATE` to the existing destructured import line. Do NOT touch sw.js — Plan 12-01 owns the CACHE bump.
  </action>
  <verify>
    <automated>
      grep -c "export const APP_VERSION = 32" js/constants.js                  # 1
      grep -c "export const BUILD_DATE" js/constants.js                        # 1
      grep -c "APP_VERSION, BUILD_DATE\|BUILD_DATE, APP_VERSION\|APP_VERSION,\|, BUILD_DATE" js/app.js    # ≥1
      grep -c 'id="settings-about-section"' app.html                            # 1
      grep -c 'about-feedback-link' app.html                                    # 1
      grep -c 'about-changelog-link' app.html                                   # 1
      grep -c 'themoviedb.org' app.html                                         # ≥1
      grep -c "function renderAboutSection" js/app.js                           # 1
      grep -c "renderAboutSection()" js/app.js                                  # ≥2 (def call + invocation in renderSettings)
      grep -c "Couch feedback v" js/app.js                                       # 1
      node --check js/app.js                                                    # exit 0
      node --check js/constants.js                                              # exit 0
    </automated>
  </verify>
  <acceptance_criteria>
    - js/constants.js exports const APP_VERSION = 32 (number, NOT string)
    - js/constants.js exports const BUILD_DATE matching regex /^\d{4}-\d{2}-\d{2}$/
    - js/app.js line 2 import statement includes APP_VERSION and BUILD_DATE
    - app.html contains '<div class="tab-section settings-about-section" id="settings-about-section">'
    - app.html '#settings-about-section' is INSIDE settings-cluster[data-cluster=admin]
    - app.html '#settings-about-section' is AFTER the .tab-footer block (placement check via line ordering)
    - app.html contains href "https://www.themoviedb.org/" with rel="noopener noreferrer"
    - js/app.js function renderAboutSection sets versionEl.textContent matching pattern 'Couch v\d+ — deployed \d{4}-\d{2}-\d{2}'
    - js/app.js renderAboutSection sets feedbackEl.href starting with 'mailto:nahderz@gmail.com?subject='
    - js/app.js renderAboutSection is called at the end of renderSettings
    - node --check js/app.js exits 0
    - node --check js/constants.js exits 0
  </acceptance_criteria>
  <done>
    Version constants + ABOUT section markup + render function shipped. Mailto pre-fills version in subject. TMDB attribution rendered with proper rel attributes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Token-backed CSS for .settings-about-section + landing.html TMDB attribution</name>
  <files>css/app.css, landing.html, css/landing.css</files>
  <read_first>
    - css/app.css offsets 2308-2360 (Phase 11 last addition, where to append) and offsets 1199-1210 (.tab-section-h to mirror eyebrow style)
    - landing.html lines 153-160 (current footer)
    - css/landing.css offsets 68-72 (existing .landing-footer styles)
    - .planning/BRAND.md (§2 colors, §4 spacing, §6 voice)
    - .planning/phases/12-pre-launch-polish/12-CONTEXT.md (D-09, D-10, D-19)
  </read_first>
  <action>
    **Step 1 (css/app.css):** Append after the existing last block (place AFTER the Plan 12-01 `Phase 12 / POL-01` notif block from plan 12-01 if 12-01 ran first — otherwise just at file end). Use only BRAND tokens:

    ```css
    /* ===========================================================================
       Phase 12 / POL-02 — ABOUT sub-section (version + feedback + changelog +
       TMDB attribution) at the bottom of the ADMIN cluster on Account tab.
       Token-backed; serif italic for the attribution per BRAND.md §6.
       Plan 12-02 / D-09, D-10, D-19.
       =========================================================================== */
    .settings-about-section{
      margin-top:var(--space-section,40px);
      padding-top:var(--space-stack-md,20px);
      border-top:1px solid var(--border)
    }
    .settings-about-section .tab-section-h span{
      font-size:var(--t-eyebrow,11px);
      letter-spacing:0.14em;
      text-transform:uppercase;
      color:var(--ink-dim);
      font-weight:600
    }
    .about-body{display:flex;flex-direction:column;gap:var(--space-stack-sm,10px);margin-top:var(--space-stack-sm,12px)}
    .about-version{
      font-family:var(--font-sans);
      font-size:var(--t-meta,13px);
      color:var(--ink-warm);
      margin:0
    }
    .about-link-row{
      display:flex;align-items:center;gap:var(--space-inline-sm,8px);
      font-size:var(--t-meta,13px);
      flex-wrap:wrap
    }
    .about-link{
      color:var(--accent);
      text-decoration:none;
      border-bottom:1px solid transparent;
      transition:border-color var(--duration-fast,150ms) var(--easing-standard,ease)
    }
    .about-link:hover,.about-link:focus-visible{
      border-bottom-color:var(--accent);
      outline:none
    }
    .about-link-sep{color:var(--ink-dim);user-select:none}
    .about-attribution{
      font-family:var(--font-serif);
      font-style:italic;
      font-size:var(--t-meta,13px);
      color:var(--ink-dim);
      margin:var(--space-stack-sm,8px) 0 0
    }
    .about-attribution a{color:var(--ink-warm);text-decoration:underline;text-decoration-color:var(--border);text-underline-offset:2px}
    .about-attribution a:hover,.about-attribution a:focus-visible{color:var(--ink);text-decoration-color:var(--accent)}
    ```

    **Step 2 (landing.html):** Replace the footer block (currently lines 153-157 ending with `<p>© Couch</p>`):

    REPLACE:
    ```html
    <footer class="landing-footer">
      <!-- TODO pre-launch: wire footer legal links (privacy + terms) when legal copy lands.
           Deliberately omitted in Phase 9 to avoid shipping 404 links. Tracked in plan 09-05 risk_notes. -->
      <p>© Couch</p>
    </footer>
    ```

    WITH:
    ```html
    <footer class="landing-footer">
      <!-- TODO pre-launch: wire footer legal links (privacy + terms) when legal copy lands.
           Deliberately omitted in Phase 9 to avoid shipping 404 links. Tracked in plan 09-05 risk_notes. -->
      <p>© Couch</p>
      <p class="landing-tmdb-attr"><em>Powered by <a href="https://www.themoviedb.org/" target="_blank" rel="noopener noreferrer">The Movie Database (TMDB)</a></em></p>
    </footer>
    ```

    **Step 3 (css/landing.css):** Append after the existing `.landing-footer` block (~line 70):

    ```css
    .landing-tmdb-attr{font-family:'Instrument Serif',serif;font-style:italic;color:var(--ink-dim);font-size:13px;margin:8px 0 0}
    .landing-tmdb-attr a{color:var(--ink-warm);margin:0;border-bottom:1px solid var(--border)}
    .landing-tmdb-attr a:hover{color:var(--ink);border-bottom-color:var(--accent)}
    ```

    Note: css/landing.css uses raw values (it predates the Phase 9 token unification) — the only tokens defined in landing.css :root are `--bg`, `--ink`, `--accent`, etc. So the attribution can reference `var(--ink-dim)`, `var(--ink-warm)`, `var(--accent)`, `var(--border)` — all of which already exist in css/landing.css :root (verified at css/landing.css:1-17). 13px raw is consistent with existing landing.css `.landing-footer { font-size: 13px }` so no token break.
  </action>
  <verify>
    <automated>
      grep -c "Phase 12 / POL-02" css/app.css                                   # 1
      grep -c ".settings-about-section" css/app.css                             # ≥2
      grep -c ".about-attribution" css/app.css                                  # ≥2
      grep -c ".about-link" css/app.css                                         # ≥3
      grep -c "themoviedb.org" landing.html                                     # 1
      grep -c "rel=\"noopener noreferrer\"" landing.html                        # ≥1
      grep -c "landing-tmdb-attr" landing.html                                  # 1
      grep -c "landing-tmdb-attr" css/landing.css                               # ≥1
      node -e "const c=require('fs').readFileSync('css/app.css','utf8');const b=c.split('Phase 12 / POL-02')[1]||'';if(/#[0-9a-fA-F]{3,6}/.test(b))process.exit(1)"   # no raw hex in Phase 12 POL-02 block
    </automated>
  </verify>
  <acceptance_criteria>
    - css/app.css contains comment header 'Phase 12 / POL-02'
    - css/app.css '.settings-about-section' uses 'border-top: 1px solid var(--border)' (NOT raw color)
    - css/app.css '.about-link' uses 'color: var(--accent)' (NOT raw hex)
    - css/app.css Phase 12 POL-02 section contains zero raw hex color values
    - css/app.css '.about-attribution' uses 'font-family: var(--font-serif)' AND 'font-style: italic'
    - landing.html .landing-footer contains '<p class="landing-tmdb-attr">'
    - landing.html TMDB link has 'rel="noopener noreferrer"'
    - landing.html TMDB link has 'target="_blank"'
    - css/landing.css contains '.landing-tmdb-attr' selector
    - landing.html still contains '<p>© Couch</p>' (preserved)
  </acceptance_criteria>
  <done>
    ABOUT section CSS shipped with token-backed styles, BRAND-italic attribution. landing.html + css/landing.css carry matching TMDB attribution. No raw hex in new CSS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create /changelog.html standalone page + Firebase Hosting rewrite</name>
  <files>changelog.html, C:/Users/nahde/queuenight/firebase.json</files>
  <read_first>
    - landing.html (full — 162 lines; serves as the posture template per D-11)
    - css/landing.css (full — 90 lines; can be reused)
    - C:/Users/nahde/queuenight/firebase.json (full — current rewrite ordering)
    - .planning/phases/12-pre-launch-polish/12-CONTEXT.md (D-11, D-12)
    - .planning/STATE.md (Phase 11 sw.js progression v21→v29 for accurate changelog entries; Phase 12 will be v32)
    - .planning/BRAND.md (§6 voice — italic descriptions, sentence case)
  </read_first>
  <action>
    **Step 1 — Create C:/Users/nahde/claude-projects/couch/changelog.html** (NEW file at repo root, mirror of landing.html posture per D-11):

    ```html
    <!doctype html>
    <html lang="en">
    <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <title>Couch — Changelog</title>
    <meta name="description" content="What's new on Couch.">
    <meta name="theme-color" content="#14110f">
    <link rel="canonical" href="https://couchtonight.app/changelog">
    <meta name="robots" content="index,follow">

    <!-- Icons (mirror landing.html) -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/mark-32.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/mark-180.png">

    <!-- Same font subset as landing -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Fraunces:wght@400;600;800&family=Inter:wght@400;500;600&family=Instrument+Serif:ital@0;1&display=swap">

    <link rel="stylesheet" href="/css/landing.css">

    <style>
      /* Page-local styles — keep changelog tiny. Reuses landing.css tokens. */
      .changelog-main{max-width:760px;margin:0 auto;padding:24px 20px 56px}
      .changelog-hero{text-align:center;padding:32px 0 24px}
      .changelog-hero h1{font-family:'Fraunces',serif;font-weight:600;font-size:36px;color:var(--ink);margin:0 0 8px}
      .changelog-hero .tagline{font-family:'Instrument Serif',serif;font-style:italic;color:var(--ink-warm);font-size:18px;margin:0}
      .release{padding:28px 0;border-top:1px solid var(--border)}
      .release:first-of-type{border-top:none}
      .release-h{display:flex;align-items:baseline;gap:12px;flex-wrap:wrap;margin-bottom:12px}
      .release-version{font-family:'Fraunces',serif;font-weight:600;font-size:22px;color:var(--ink)}
      .release-date{font-family:'Inter',sans-serif;font-size:12px;color:var(--ink-dim);text-transform:uppercase;letter-spacing:0.12em}
      .release-summary{font-family:'Instrument Serif',serif;font-style:italic;color:var(--ink-warm);font-size:17px;margin:0 0 12px}
      .release-list{margin:0;padding-left:20px;color:var(--ink-warm);font-size:15px;line-height:1.6}
      .release-list li{margin-bottom:6px}
      .release-list li strong{color:var(--ink);font-weight:600}
      .changelog-back{text-align:center;padding:48px 0 0;color:var(--ink-dim);font-size:13px}
      .changelog-back a{color:var(--ink-warm);border-bottom:1px solid var(--border)}
      .changelog-back a:hover{color:var(--ink);border-bottom-color:var(--accent)}
    </style>
    </head>
    <body>

    <main class="changelog-main">

      <section class="changelog-hero">
        <h1>What's new</h1>
        <p class="tagline">Recent releases on Couch.</p>
      </section>

      <article class="release">
        <header class="release-h">
          <span class="release-version">v32</span>
          <span class="release-date">2026-04-25 · Pre-launch polish</span>
        </header>
        <p class="release-summary">Three small fixes ahead of v1.</p>
        <ul class="release-list">
          <li><strong>Notification preferences:</strong> per-event toggles + quiet hours moved to Account → Your Couch.</li>
          <li><strong>Couch Nights:</strong> Halloween Crawl pack now actually starts with Hocus Pocus.</li>
          <li><strong>About:</strong> version + feedback + this changelog.</li>
        </ul>
      </article>

      <article class="release">
        <header class="release-h">
          <span class="release-version">v31</span>
          <span class="release-date">2026-04-24 · Couch Nights heroes</span>
        </header>
        <p class="release-summary">Pack hero images now load reliably.</p>
        <ul class="release-list">
          <li>Cached TMDB poster URLs at compile time so the Couch Nights row never flashes empty.</li>
        </ul>
      </article>

      <article class="release">
        <header class="release-h">
          <span class="release-version">v29</span>
          <span class="release-date">2026-04-24 · Couch Nights packs</span>
        </header>
        <p class="release-summary">Eight themed ballot packs you can spin in one tap.</p>
        <ul class="release-list">
          <li><strong>Couch Nights:</strong> Studio Ghibli Sunday, Cozy Rainy Night, Date Night Classics, Kids' Room Classics, A24 Night, Oscars Short List, Halloween Crawl, Dad's Action Pantheon.</li>
          <li>Tap a pack to preview titles; tap "Start this pack" to seed the ballot in Vote mode.</li>
        </ul>
      </article>

      <article class="release">
        <header class="release-h">
          <span class="release-version">v28</span>
          <span class="release-date">2026-04-24 · Sports Game Mode</span>
        </header>
        <p class="release-summary">Live score strip + amplified reactions on scoring plays + per-user DVR slider.</p>
        <ul class="release-list">
          <li>Pick a live game from NFL / NBA / MLB / NHL.</li>
          <li>Score updates poll every five seconds while plays are firing, fifteen otherwise.</li>
          <li>Late joiners see the score and the last three plays.</li>
        </ul>
      </article>

      <article class="release">
        <header class="release-h">
          <span class="release-version">v27</span>
          <span class="release-date">2026-04-24 · Watchparty lifecycle</span>
        </header>
        <p class="release-summary">RSVP page, pre-session lobby, late-joiner catch-me-up, post-session photo album.</p>
        <ul class="release-list">
          <li><strong>Web RSVP:</strong> share a link; non-members can RSVP without installing.</li>
          <li><strong>Lobby:</strong> roster + countdown + Ready check; majority Ready auto-starts at T-0.</li>
          <li><strong>Catch me up:</strong> 30-second recap of reactions you missed.</li>
          <li><strong>Post-session:</strong> rate it, drop a photo, schedule the next one.</li>
        </ul>
      </article>

      <p class="changelog-back"><a href="/">Back to Couch</a></p>

    </main>

    </body>
    </html>
    ```

    **Step 2 — Edit C:/Users/nahde/queuenight/firebase.json** to add the `/changelog` rewrite. INSERT the new entry BEFORE the catch-all `/` rewrite (per Firebase Hosting rule: specific patterns must precede catch-alls). New rewrites array:

    ```json
    "rewrites": [
      { "source": "/app", "destination": "/app.html" },
      { "source": "/app/**", "destination": "/app.html" },
      { "source": "/rsvp/**", "destination": "/rsvp.html" },
      { "source": "/changelog", "destination": "/changelog.html" },
      { "source": "/", "destination": "/landing.html" }
    ]
    ```

    Do NOT modify the `headers`, `functions`, `firestore`, or `storage` sections of firebase.json.

    Note: this firebase.json is in the SIBLING repo `C:/Users/nahde/queuenight/`, which is the deploy mirror (not a git repo per Phase 9 / 11 conventions). The orchestrator deploy step (separate from this plan) will mirror changelog.html to queuenight/public/ alongside the firebase.json edit.
  </action>
  <verify>
    <automated>
      ls changelog.html                                                          # exists
      grep -c '<title>Couch — Changelog</title>' changelog.html                  # 1
      grep -c 'firebase' changelog.html                                          # 0 — no Firebase SDK
      grep -c '/css/app.css' changelog.html                                      # 0 — no app stylesheet
      grep -c '/js/app.js' changelog.html                                        # 0 — no app JS
      grep -c '<article class="release">' changelog.html                         # ≥5
      grep -c 'v32' changelog.html                                               # ≥1
      grep -cE '\bv29\b|>v29<' changelog.html                                    # ≥1
      grep -c 'release-version' changelog.html                                   # ≥5
      grep -c '"/changelog"' /c/Users/nahde/queuenight/firebase.json             # 1
      python -c "import json;j=json.load(open(r'C:/Users/nahde/queuenight/firebase.json'));rw=j['hosting']['rewrites'];idx_cl=next(i for i,r in enumerate(rw) if r['source']=='/changelog');idx_root=next(i for i,r in enumerate(rw) if r['source']=='/');assert idx_cl<idx_root, 'changelog must be before /'"
      node -e "JSON.parse(require('fs').readFileSync('C:/Users/nahde/queuenight/firebase.json','utf8'))"   # valid JSON
    </automated>
  </verify>
  <acceptance_criteria>
    - changelog.html exists at repo root C:/Users/nahde/claude-projects/couch/
    - changelog.html contains zero references to '/css/app.css'
    - changelog.html contains zero references to '/js/app.js'
    - changelog.html contains zero references to 'firebase' (no Firebase SDK)
    - changelog.html contains exactly one <link rel="stylesheet" href="/css/landing.css">
    - changelog.html contains <link rel="canonical" href="https://couchtonight.app/changelog">
    - changelog.html contains 5 or more <article class="release"> blocks
    - changelog.html contains the literal string 'v32'
    - changelog.html contains the literal string 'v29'
    - changelog.html .release-summary uses 'Instrument Serif' italic (per BRAND voice)
    - C:/Users/nahde/queuenight/firebase.json hosting.rewrites contains {source:"/changelog", destination:"/changelog.html"}
    - C:/Users/nahde/queuenight/firebase.json /changelog rewrite index < / rewrite index (specific before catch-all)
    - C:/Users/nahde/queuenight/firebase.json is valid JSON
    - No file in queuenight/ deploys was deleted
  </acceptance_criteria>
  <done>
    /changelog.html standalone page at repo root mirrors landing.html posture. Firebase Hosting rewrite added in correct ordering. JSON valid. Zero Firebase SDK / app shell deps in changelog page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous web → /changelog.html | Static HTML served via Firebase Hosting; no Firestore reads, no auth |
| User-typed mailto subject | mailto: opens client mail app — no server roundtrip, no XSS surface in app |
| External link (themoviedb.org) | Outbound link with rel="noopener noreferrer" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information disclosure | changelog.html public exposure | accept | Page intentionally public; lists last 5 deployed versions. No PII; aligns with landing.html posture which is also public. Per CONTEXT.md threat hint: "XSS-safe by being static HTML; no user-typed content." |
| T-12-02-02 | Tampering | mailto subject query string | accept | Subject is hard-coded as `Couch feedback v${APP_VERSION}` with `encodeURIComponent`. APP_VERSION is a hard-coded module constant — no user input flows into the mailto. |
| T-12-02-03 | XSS via target=_blank | TMDB attribution link | mitigate | Both attribution links (app.html + landing.html) use `rel="noopener noreferrer"` to block window.opener access from themoviedb.org. |
| T-12-02-04 | Broken redirect | Firebase Hosting rewrite ordering | mitigate | Test gate: assert `/changelog` rewrite index < `/` rewrite index in firebase.json. Specific-before-catch-all preserved. |
| T-12-02-05 | DoS via crawl | /changelog meta robots index,follow | accept | Page is small (~5KB) and cacheable; bot traffic is negligible at couch-scale. Indexable is desirable for SEO. |

No new high-severity attack surface.
</threat_model>

<verification>
**Cumulative grep gates (post Tasks 1–3):**

```bash
# Task 1
grep -c "export const APP_VERSION = 32" js/constants.js                         # 1
grep -c "export const BUILD_DATE" js/constants.js                               # 1
grep -c 'id="settings-about-section"' app.html                                  # 1
grep -c "function renderAboutSection" js/app.js                                 # 1
grep -c "renderAboutSection()" js/app.js                                        # ≥2

# Task 2
grep -c "Phase 12 / POL-02" css/app.css                                         # 1
grep -c "themoviedb.org" landing.html                                           # 1

# Task 3
test -f changelog.html
grep -c "/changelog" /c/Users/nahde/queuenight/firebase.json                    # ≥1
node -e "JSON.parse(require('fs').readFileSync('C:/Users/nahde/queuenight/firebase.json','utf8'))"

# sw.js untouched (Plan 12-01 owns the bump)
grep -c "couch-v32-pre-launch-polish\|couch-v31-fix-couch-nights-heroes" sw.js  # ≥1 (pre-12-01: v31 / post-12-01: v32; either acceptable for THIS plan)

# Syntax
node --check js/app.js
node --check js/constants.js
```

**Manual smoke (post-deploy, deferred):**
1. Visit https://couchtonight.app/changelog — page renders, no Firebase SDK in network tab.
2. App → Account tab → scroll to bottom of ADMIN cluster — ABOUT shows "Couch v32 — deployed 2026-04-25".
3. Tap "Send feedback" — mail client opens with subject "Couch feedback v32".
4. Tap "See what's new" — navigates to /changelog.html via rewrite.
5. Tap TMDB link in About — opens themoviedb.org in new tab.
6. Visit https://couchtonight.app/ — landing footer shows "Powered by The Movie Database (TMDB)".
</verification>

<success_criteria>
- ABOUT sub-section ships at the bottom of ADMIN cluster (after .tab-footer pills) with version + feedback + changelog + TMDB attribution.
- /changelog.html standalone page lists last 5 releases, no app shell.
- Firebase Hosting rewrite /changelog → /changelog.html, ordered before catch-all /.
- TMDB attribution shipped on landing.html footer (closes D-19 ToS gap).
- All BRAND tokens; no raw hex in new app.css block.
- sw.js NOT modified (Plan 12-01 owns the bump).
</success_criteria>

<output>
After completion, create `.planning/phases/12-pre-launch-polish/12-02-SUMMARY.md` covering:
- Files modified + LOC delta
- D-09..D-12 + D-19 closure traceability
- Confirmation that sw.js was NOT touched (Plan 12-01 owns v32 bump)
- Deploy steps documented for orchestrator: mirror app.html + js/app.js + js/constants.js + css/app.css + landing.html + css/landing.css + changelog.html (NEW) + firebase.json to queuenight/public/ then `firebase deploy --only hosting` from C:/Users/nahde/queuenight/
- Note: future BUILD_DATE bumps require manual constants.js edit on each release; suggest a tiny Phase 13 follow-up to auto-stamp from `git log -1 --format=%cs HEAD`.
</output>
