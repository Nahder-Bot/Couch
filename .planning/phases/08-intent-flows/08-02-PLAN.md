---
phase: 08-intent-flows
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - js/app.js
  - index.html
  - css/app.css
autonomous: true
requirements: [INTENT-01, INTENT-02]
must_haves:
  truths:
    - "Title-detail action sheet on any unwatched title shows: 📅 'Propose tonight @ time' and 💭 'Ask the family'"
    - "Propose-tonight modal: title + 4 quick-pick times (8pm / 9pm / 10pm / Custom) + Note field + Create button"
    - "Ask-the-family creates a watch_this_title intent directly (no modal)"
    - "Tonight screen renders an open-intents strip when state.intents has open entries"
    - "Strip cards show title + type + RSVP tally ('2 of 3 yes') + your-RSVP state"
    - "Tap a card → opens RSVP modal showing full detail + Yes / Maybe / No (/ Later for poll) buttons"
    - "RSVP buttons call setIntentRsvp; re-tapping the same value clears it (write 'unset' value or delete the field)"
    - "No regression: existing action-sheet entries (Watchparty, Schedule, Veto) remain"
  artifacts:
    - path: js/app.js
      provides: "Entry points + propose modal + RSVP modal + renderIntentsStrip"
      contains: "renderIntentsStrip"
    - path: index.html
      provides: "Intents strip container + propose modal + RSVP modal DOM"
      contains: "intent-propose-modal"
    - path: css/app.css
      provides: "Intent strip + modals warm-palette styling"
      contains: ".intents-strip"
---

<objective>
Ship all user-visible intent-flow surfaces. Action sheet entries for both flow types; propose-tonight modal with quick-pick times; standing open-intents strip on Tonight; RSVP modal reachable from strip. No threshold or match logic yet (Plan 08-03).

Purpose: INTENT-01 and INTENT-02 are "a member can start / RSVP" — this plan makes that true end-to-end without yet covering the match prompt.
Output: 3 DOM containers, 4 new render functions, 2 new window handlers (propose-tonight + RSVP modal controls), new CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-intent-flows/08-CONTEXT.md
@.planning/phases/07-watchparty/07-CONTEXT.md
@js/app.js
@index.html
@css/app.css

<interfaces>
<!-- Action sheet additions — locate openActionSheet in js/app.js near line 8600: -->
<!--
  if (!t.watched && state.me) {
    items.push(`<button class="action-sheet-item" onclick="closeActionSheet();openProposeIntent('${titleId}','tonight_at_time')"><span class="icon">📅</span>Propose tonight @ time</button>`);
    items.push(`<button class="action-sheet-item" onclick="closeActionSheet();askTheFamily('${titleId}')"><span class="icon">💭</span>Ask the family</button>`);
  }
-->

<!-- window.openProposeIntent(titleId, type):
       - tonight_at_time → open #intent-propose-modal-bg with time quick-picks
       - watch_this_title → call askTheFamily directly (shortcut) -->
<!-- window.askTheFamily(titleId): createIntent({type:'watch_this_title',titleId}); flashToast('Posted to your family') -->

<!-- Propose-tonight modal: uses same lead-time grid pattern as wp-start-modal-bg (index.html line 791). -->

<!-- RSVP modal: #intent-rsvp-modal-bg with buttons wired to setIntentRsvp. -->

<!-- renderIntentsStrip() called from onSnapshot callback + existing Tonight render. Renders open intents sorted by createdAt desc. -->
</interfaces>

</context>

<tasks>

<task type="edit-code" gate="blocking">
  <name>Task 1: Action-sheet entries</name>
  <files>js/app.js</files>
  <how-to-verify>
    openActionSheet's item list gains two new entries behind `!t.watched && state.me`. Positioned before the Watchparty entry (higher intent = earlier in list).
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 2: Propose-tonight modal DOM + JS</name>
  <files>index.html, js/app.js</files>
  <how-to-verify>
    `#intent-propose-modal-bg` + `#intent-propose-modal-content` added to index.html. Modal contains: title slot, 4 quick-pick time buttons (8pm / 9pm / 10pm / Pick time), optional note textarea, Create + Cancel buttons. `openProposeIntent(titleId, 'tonight_at_time')` opens + hydrates the modal with title name/poster. `confirmProposeIntent()` reads the chosen time + note + calls `createIntent({type:'tonight_at_time', titleId, proposedStartAt, proposedNote})`.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 3: Ask-the-family shortcut</name>
  <files>js/app.js</files>
  <how-to-verify>
    `window.askTheFamily(titleId)` exists. Calls `createIntent({type:'watch_this_title', titleId})` + `flashToast('Asked your family')`. No modal, no extra input.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 4: Intents strip on Tonight</name>
  <files>index.html, js/app.js, css/app.css</files>
  <how-to-verify>
    Container `#tonight-intents-strip` added to Tonight screen (below or near `#wp-banner-tonight`). `renderIntentsStrip()` iterates `state.intents.filter(i => i.status === 'open')`, sorted by createdAt desc. Each card: poster thumbnail + title + type icon (📅 for tonight_at_time, 💭 for watch_this_title) + RSVP tally + your-RSVP state (or "Tap to RSVP"). Click routes to `openIntentRsvpModal(intentId)`. CSS matches warm-palette card pattern from .wp-banner.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 5: RSVP modal DOM + JS</name>
  <files>index.html, js/app.js, css/app.css</files>
  <how-to-verify>
    `#intent-rsvp-modal-bg` + `#intent-rsvp-modal-content` added. `openIntentRsvpModal(intentId)` renders: title + proposed time (if any) + current RSVP tally per-member + 3-or-4 buttons based on type. `window.setMyRsvp(intentId, value)` calls `setIntentRsvp` + closes modal (or keeps open for re-tap-to-unvote flow — planner picks). Existing modal-close pattern reused (✕ button + tap-outside).
  </how-to-verify>
</task>

</tasks>

<verification>
1. All grep checks pass
2. Commit: `feat(08-02): intent-flow UX — action sheet entries, propose modal, RSVP modal, Tonight strip`
</verification>

<success_criteria>
- User can create an intent from any unwatched title's action sheet (both types)
- User can see open intents on Tonight screen
- User can RSVP via the modal
- No threshold/match logic yet — that's 08-03
</success_criteria>

<output>
Stage-only. Deploy: hosting.
</output>
