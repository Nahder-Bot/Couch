---
phase: 08-intent-flows
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - .planning/phases/08-intent-flows/08-UAT-RESULTS.md
autonomous: false
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04, INTENT-05, INTENT-06]
must_haves:
  truths:
    - "08-UAT-RESULTS.md exists with per-scenario results"
    - "Propose tonight @ time flow creates intent visible on both devices"
    - "Watch-this-title flow creates poll visible on both devices"
    - "RSVP updates propagate <2s"
    - "Threshold match fires creator-only toast + push"
    - "Conversion to watchparty works end-to-end"
    - "Conversion to schedule works end-to-end"
    - "Expired intent archives via CF tick"
---

<objective>
UAT the intent flows end-to-end across two devices. Gates Phase 8 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-intent-flows/08-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Tonight @ time flow — proposal + RSVP + match + convert-to-watchparty</name>
  <how-to-verify>
    Device A: action sheet on a title → Propose tonight @ time → pick 8pm → Create. Device B receives `intentProposed` push. Device B opens the app, sees the intent in Tonight strip, taps → RSVP modal → Yes. Device A sees match toast (duo rule → any_yes → threshold met) + gets `intentMatched` push. Device A taps "Start watchparty" → watchparty modal opens pre-populated → Start. Live watchparty session begins.
  </how-to-verify>
  <resume-signal>
    - `tonight-flow-pass`
    - `fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Watch-this-title flow — ask + RSVP + match + convert-to-schedule</name>
  <how-to-verify>
    Device A: Ask the family on another title. Device B RSVPs Yes. Device A sees match toast → taps "Schedule it" → schedule modal opens → save for tomorrow 8pm. Intent doc shows status=converted.
  </how-to-verify>
  <resume-signal>
    - `watch-flow-pass`
    - `fail: <details>`
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Threshold rule + override</name>
  <how-to-verify>
    In a family with ≥5 members (or a test family you temporarily expand), verify default rule is 'majority'. Go to Settings → override to 'any_yes'. Propose a new intent → single yes triggers match.
  </how-to-verify>
  <resume-signal>
    - `threshold-pass`
    - `threshold-pending` if hard to test with only 2 devices (note partial)
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Expiry cleanup</name>
  <how-to-verify>
    Firestore Console: edit an intent doc, set status=open, expiresAt=Date.now()-1000 (1s ago). Wait 5 min. Doc should update to status=expired.
  </how-to-verify>
  <resume-signal>
    - `expiry-pass`
    - `expiry-pending` (save for logs later)
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Write 08-UAT-RESULTS.md</name>
  <how-to-verify>
    Follow Phase 6/7 UAT template. Commit.
  </how-to-verify>
</task>

</tasks>

<success_criteria>
- Both flows work end-to-end
- Push events deliver to right recipients
- Conversion paths reuse existing modals correctly
- Expiry archives stale intents
</success_criteria>

<output>
`08-UAT-RESULTS.md`.
</output>
