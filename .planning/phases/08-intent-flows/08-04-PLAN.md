---
phase: 08-intent-flows
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - js/app.js
  - queuenight/functions/index.js
autonomous: true
requirements: [INTENT-01, INTENT-02, INTENT-03]
must_haves:
  truths:
    - "DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_DEFAULTS gain two keys: intentProposed (default true), intentMatched (default true)"
    - "NOTIFICATION_EVENT_LABELS gains two entries with warm human-readable copy"
    - "New CF onIntentCreated fires on families/{code}/intents/{id} doc create; pushes eventType=intentProposed to non-creator members"
    - "New CF onIntentUpdate fires on status→matched transition; pushes eventType=intentMatched to creator only"
    - "Both triggers pass excludeUid + excludeMemberId for self-echo discipline (D-20)"
    - "No regression on existing Phase 6 eventTypes"
  artifacts:
    - path: js/app.js
      provides: "Client prefs keys + labels"
      contains: "intentProposed"
    - path: queuenight/functions/index.js
      provides: "onIntentCreated + onIntentUpdate triggers"
      contains: "onIntentCreated"
---

<objective>
Wire intent-flow push events into the Phase 6 infrastructure. Two new eventTypes, two new CF triggers. Per-event opt-out Just Works because Phase 6's sendToMembers already reads prefs keyed by eventType.

Purpose: Without this, intents are silent unless a user is actively looking at the app. With it, intents and matches reach people on their home screens.
Output: 2 client map updates; 2 CF triggers; 2 NOTIFICATION_DEFAULTS server-side entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-push-notifications/06-CONTEXT.md
@.planning/phases/08-intent-flows/08-CONTEXT.md
@js/app.js
@queuenight/functions/index.js

<interfaces>
<!-- Client: add to DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS: -->
<!--
  intentProposed: true,
  intentMatched: true,

  intentProposed: { label: 'New intent posted', hint: 'When someone proposes a tonight-watch or asks the family about a title' },
  intentMatched:  { label: 'Intent match', hint: 'When your proposed watch reaches the family threshold — time to schedule' },
-->

<!-- Server NOTIFICATION_DEFAULTS: same two keys with matching defaults -->

<!-- CF onIntentCreated: -->
<!--
exports.onIntentCreated = functions.firestore
  .document('families/{familyCode}/intents/{intentId}')
  .onCreate(async (snap, context) => {
    const intent = snap.data();
    if (!intent) return null;
    const { familyCode, intentId } = context.params;
    const typeLabel = intent.type === 'tonight_at_time' ? 'Tonight' : 'Watch-this';
    const body = intent.type === 'tonight_at_time'
      ? `${intent.createdByName || 'A family member'} proposed "${intent.titleName}" for ${new Date(intent.proposedStartAt || Date.now()).toLocaleTimeString([], {hour:'numeric', minute:'2-digit'})}`
      : `${intent.createdByName || 'A family member'} asked: "${intent.titleName}"?`;
    const membersSnap = await db.collection('families').doc(familyCode).collection('members').get();
    const recipientIds = membersSnap.docs.map(d => d.id).filter(id => id !== intent.createdBy);
    return sendToMembers(familyCode, recipientIds, {
      title: typeLabel,
      body,
      tag: `intent-${intentId}`,
      url: `/?intent=${intentId}`
    }, {
      excludeUid: intent.createdByUid || null,
      excludeMemberId: intent.createdBy || null,
      eventType: 'intentProposed'
    });
  });
-->

<!-- CF onIntentUpdate for match: -->
<!--
exports.onIntentUpdate = functions.firestore
  .document('families/{familyCode}/intents/{intentId}')
  .onUpdate(async (change, context) => {
    const before = change.before.data();
    const after = change.after.data();
    if (!before || !after) return null;
    if (before.status === 'matched' || after.status !== 'matched') return null;
    const { familyCode, intentId } = context.params;
    return sendToMembers(familyCode, [after.createdBy], {
      title: 'Your intent matched!',
      body: `"${after.titleName}" — everyone's in. Time to schedule.`,
      tag: `intent-match-${intentId}`,
      url: `/?intent=${intentId}`
    }, {
      eventType: 'intentMatched'
      // no self-echo exclusion — we explicitly WANT the creator to be the sole recipient
    });
  });
-->

<!-- Note on createdByName: client createIntent should also store memberName on the intent doc for CF to read. Add to 08-01 interfaces or patch in 08-04. -->
</interfaces>

</context>

<tasks>

<task type="edit-code" gate="blocking">
  <name>Task 1: Client prefs + labels</name>
  <files>js/app.js</files>
  <how-to-verify>
    DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS both gain `intentProposed` and `intentMatched`. Existing Settings renderer auto-includes the new toggles via its existing Object.keys iteration.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 2: Server NOTIFICATION_DEFAULTS parity</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    Server NOTIFICATION_DEFAULTS map adds both keys with defaults matching the client.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 3: onIntentCreated trigger</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    Trigger on `families/{familyCode}/intents/{intentId}` onCreate. Reads intent; resolves memberName fallback; composes type-specific body; pushes to non-creator members with eventType='intentProposed' + excludeUid/excludeMemberId for safety. `node --check` passes.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 4: onIntentUpdate trigger for match</name>
  <files>queuenight/functions/index.js</files>
  <how-to-verify>
    Trigger on `families/{familyCode}/intents/{intentId}` onUpdate. Fires only on status open→matched transition. Pushes to `[after.createdBy]` with eventType='intentMatched'. No self-echo args — creator is the intended sole recipient.
  </how-to-verify>
</task>

<task type="edit-code" gate="blocking">
  <name>Task 5: createdByName on intent doc</name>
  <files>js/app.js</files>
  <how-to-verify>
    createIntent stamps `createdByName: state.me.name` alongside `createdBy`. Makes CF push bodies clean without extra lookups.
  </how-to-verify>
</task>

</tasks>

<verification>
1. Syntax OK
2. Commit: `feat(08-04): push integration — intentProposed + intentMatched eventTypes`
</verification>

<success_criteria>
- Non-creator members get push when an intent is created (if opted in; default is opted in)
- Creator gets push when their intent matches (if opted in)
- Per-event opt-out works via existing Phase 6 prefs machinery
</success_criteria>

<output>
Stage-only. Deploy: functions + hosting.
</output>
