---
phase: 19-kid-mode
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - C:/Users/nahde/claude-projects/couch/sw.js
autonomous: false
requirements: [KID-19-08]
must_haves:
  truths:
    - "sw.js CACHE constant bumped from 'couch-v36-availability-notifs' to 'couch-v36.1-kid-mode' (per D-22)"
    - "deploy ritual is single-repo couch-only (no queuenight changes; no firestore.rules; no Cloud Function deploy) per D-21/D-23"
    - "scripts/deploy.sh 36.1-kid-mode handles auto-bump confirmation + 4-smoke gate (including new smoke-kid-mode contract from 19-01) + mirror to couch-deploy + firebase deploy --only hosting"
    - "Live couchtonight.app serves CACHE 'couch-v36.1-kid-mode' (verified via curl post-deploy)"
    - "End-to-end UAT on a real device with at least one kid avatar in the family confirms all 8 ROADMAP success criteria pass (toggle visibility + session-scope + tier-cap + Library filter + amber tint + parent override + cache bump + reset boundaries)"
  artifacts:
    - path: "C:/Users/nahde/claude-projects/couch/sw.js"
      provides: "Bumped CACHE constant for PWA invalidation"
      contains: "couch-v36.1-kid-mode"
  key_links:
    - from: "scripts/deploy.sh 36.1-kid-mode"
      to: "sw.js CACHE auto-bump"
      via: "stamp-build-date.cjs auto-bump path"
      pattern: "couch-v36.1-kid-mode"
    - from: "scripts/deploy.sh 36.1-kid-mode"
      to: "couchtonight.app live"
      via: "firebase deploy --only hosting (couch-only — D-23)"
      pattern: "firebase deploy --only hosting"
---

<objective>
Plan 03 — sw.js cache bump + couch-only production deploy + multi-condition
device UAT. Closes Phase 19.

Single-repo deploy (D-21..D-23):
1. Bump `sw.js` CACHE from `couch-v36-availability-notifs` to `couch-v36.1-kid-mode` — decimal continuation since Phase 18 took v36 (per D-22).
2. Run `bash scripts/deploy.sh 36.1-kid-mode` from the couch repo root. Script handles BUILD_DATE auto-stamp + 4-smoke gate (smoke-position + smoke-tonight + smoke-availability + smoke-kid-mode contract from Plan 19-01) + Sentry DSN guard + mirror to couch-deploy/public/ + `firebase deploy --only hosting`.
3. NO queuenight changes (per D-21). NO Cloud Function deploy. NO firestore.rules update.
4. Post-deploy curl verification confirms `couch-v36.1-kid-mode` is live at couchtonight.app.
5. Multi-condition device UAT — pass-fail walkthrough on a real device with the author's family roster (which has 6 young members per CONTEXT.md §specifics).

This plan is **non-autonomous** because UAT requires a real iOS device + a family with at least one kid avatar (the author's signed-in family qualifies). Multi-AI cross-feedback flagged kid-mode as P0 for the family brand → UAT is the gate that validates the user-experience contract before declaring shipped.

Purpose: ship the user-visible change to production + confirm end-to-end behavior against the 8 ROADMAP success criteria.

Output: `sw.js` CACHE bumped + production deploy + UAT walkthrough captured in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/RUNBOOK.md
@.planning/phases/19-kid-mode/19-CONTEXT.md
@.planning/phases/19-kid-mode/19-01-PLAN.md
@.planning/phases/19-kid-mode/19-02-PLAN.md
@.planning/phases/18-availability-notifications/18-04-SUMMARY.md
@CLAUDE.md

INTERFACES (verified shapes — read 2026-04-29 pre-plan):

sw.js:8 — current CACHE constant:
```javascript
const CACHE = 'couch-v36-availability-notifs';
```
Bump target per D-22:
```javascript
const CACHE = 'couch-v36.1-kid-mode';
```

scripts/deploy.sh — auto-bumps sw.js CACHE when invoked with a tag arg. The
correct invocation is `bash scripts/deploy.sh 36.1-kid-mode` (no `couch-v` prefix
— the script prepends it via `couch-v$TAG` per RUNBOOK §H).

scripts/deploy.sh §2.5 — 4-smoke gate (after Plan 19-01 wires in
smoke-kid-mode.cjs). All 4 must pass before deploy proceeds.

NO cross-repo deploy ritual. The Phase 18 R-8 ordering (queuenight FIRST →
couch SECOND) does NOT apply here — this is a pure-client phase.

iOS PWA cache-bust note (BRAND.md §1): when CACHE changes, installed PWAs
auto-revalidate on next online activation — typically within seconds of the
next foreground. UAT should happen on a freshly-deployed device that has
been backgrounded + foregrounded once after deploy goes live (or fully
relaunched).

Author's family roster: per CONTEXT.md §specifics — 6 young members
(Brody, Arianna, Zoey, Ellie, Brynlee + one more). At least one has
effectiveMaxTier ≤ 3 → familyHasKids() returns true → kid-mode toggle visible.

UAT verification environment:
- Real iOS Safari PWA on couchtonight.app (primary surface per CLAUDE.md)
- At least one Android device or desktop browser as a secondary smoke
- The author's signed-in family (Nahder + Ashley + 6 kids = 8 members)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump sw.js CACHE + run deploy + verify live</name>
  <files>C:/Users/nahde/claude-projects/couch/sw.js</files>
  <read_first>
- Confirm sw.js:8 still reads `const CACHE = 'couch-v36-availability-notifs';` (Phase 18 baseline).
- Confirm `git status` shows a clean working tree (deploy.sh aborts on dirty tree per Pitfall 7 unless --allow-dirty).
- Confirm Plans 19-01 + 19-02 are committed (smoke-kid-mode.cjs exists + node --check passes + node scripts/smoke-kid-mode.cjs exits 0).
  </read_first>
  <action>
Step 1 — Update sw.js:8 CACHE constant. Two acceptable paths:

(a) **Manual edit** — Edit sw.js directly:
```javascript
const CACHE = 'couch-v36.1-kid-mode';
```

(b) **deploy.sh auto-bump** — Pass the tag to deploy.sh and let scripts/stamp-build-date.cjs handle it. The deploy script will modify sw.js as part of its run.

Recommendation: take path (b) — single command runs everything. If that fails for any reason (e.g., stamp-build-date.cjs version-string regex mismatch with the decimal `.1`), fall back to path (a) and re-run deploy.sh.

Step 2 — Commit any pre-deploy edits (if path (a) was used). Single atomic commit per Phase 14/15.5/18 precedent:
```bash
git add sw.js
git commit -m "chore(19-03): bump sw.js CACHE to couch-v36.1-kid-mode"
```

Step 3 — Run the deploy. From couch repo root:
```bash
bash scripts/deploy.sh 36.1-kid-mode
```

Expected output sequence (mirrors Phase 18 deploy):
- "Tests passed" (rules suite if `tests/` present)
- "node --check passed for js/app.js + sw.js + scripts/stamp-build-date.cjs"
- 4 smoke contracts all pass: position-transform + tonight-matches + availability + kid-mode (NEW from Plan 19-01)
- BUILD_DATE auto-stamp (or "no change") in couch-deploy/public/js/constants.js
- sw.js CACHE bumped (or "already at target") in couch-deploy/public/sw.js
- Sentry DSN guard passes (no placeholder leak)
- Mirror complete to couch-deploy/public/
- "firebase deploy --only hosting" runs successfully against couchtonight.app

Step 4 — Post-deploy curl verification. From any shell:
```bash
curl -s https://couchtonight.app/sw.js | grep "const CACHE"
```
Expected output: `const CACHE = 'couch-v36.1-kid-mode';`

If the curl returns the OLD cache string, it's likely Firebase Hosting CDN cache lag — wait 30 seconds and re-run. If still stale after 5 minutes, investigate Firebase Hosting deploy log.

Step 5 — Update STATE.md sw.js cache version line:
```
Current source: `couch-v36.1-kid-mode` (Plan 19-03 — Wave 3 close-out **DEPLOYED 2026-04-XX** via couch-only deploy ritual: `bash scripts/deploy.sh 36.1-kid-mode` at HH:MMZ)
```
Append to existing phase progression chain on the same line.
  </action>
  <verify>
    <automated>curl -s https://couchtonight.app/sw.js | grep "couch-v36.1-kid-mode"</automated>
    <automated>node scripts/smoke-kid-mode.cjs</automated>
  </verify>
  <done>
- sw.js CACHE = 'couch-v36.1-kid-mode' both in source + couch-deploy/public/ mirror
- deploy.sh exited 0 with all 4 smoke contracts passing
- couchtonight.app serves the new cache (curl confirmed)
- STATE.md sw.js cache version line updated
- Single chore commit + deploy commit on couch main
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Multi-condition device UAT against the 8 ROADMAP success criteria</name>
  <files>(none — UAT is browser-side; outputs captured in 19-03-SUMMARY.md)</files>
  <action>
**What was built (recap of Plans 19-01 + 19-02 + 19-03 Task 1):**

Phase 19 Kid Mode is live at couchtonight.app on cache `couch-v36.1-kid-mode`. The change ships:

1. A "Kid mode" toggle pill on the V5 roster surface (below the existing Mark everyone in / Clear couch / Send pushes to the rest action row), visible only when ≥1 family member has effectiveMaxTier ≤ 3.
2. When toggled on: tier cap of 2 (TIER_PG) applied to Tonight matches + considerable + spin-pick + Flow A picker T1/T2/T3 + swipe-mode + Library 'unwatched' + Library 'forme' filters. PG-13/R/TV-MA titles disappear from those surfaces.
3. Subtle amber tint (≤ 5% opacity) on the V5 roster when active, per BRAND warm-restraint.
4. State is session-only — does NOT persist to Firestore or localStorage. Resets when navigating away from the Tonight tab OR when "Clear couch" runs.
5. Vetoed titles still hidden + already-watched titles still hidden + provider filter still applies — kid-mode does NOT bypass these (D-18..D-20).
6. Per-title parent override on the detail modal: when a parent opens a kid-mode-blocked (PG-13+) title's detail modal, a "Show this anyway for tonight" link surfaces. Tapping adds the title to a session-only override Set + closes the modal + re-renders Tonight + Library so the previously-blocked title now appears.
7. Override is parent-only — non-parents do not see the link. Override is session-scoped — clears when kid-mode toggles off.
8. sw.js CACHE bumped to invalidate installed PWAs.

Smoke contract `scripts/smoke-kid-mode.cjs` (24+ assertions across 4 scenarios) wired into deploy.sh §2.5 and `npm run smoke` chain (now 4 contracts).

**Resume the task by running the 8 UAT scripts below.** Capture pass/fail per UAT in 19-03-SUMMARY.md. UATs 1+2+3+6 are the four critical user-experience gates — all must pass to declare Phase 19 shipped. UATs 4+5+7+8 are pass-or-document-as-deferred. After completing the walkthrough, type "uat passed" or describe failure modes for triage.
  </action>
  <verify>
    <manual>
**Setup:**
- iOS Safari PWA on couchtonight.app — sign in as the author's family (has Brody, Arianna, Zoey, Ellie, Brynlee + adult members).
- If installed PWA shows old cache, force-revalidate: foreground after deploy, or fully relaunch the PWA (delete + re-add for stubborn cache, per BRAND §1).
- Confirm `couch-v36.1-kid-mode` is the active SW (DevTools → Application → Service Workers if testing on desktop; or check `curl -s https://couchtonight.app/sw.js | grep CACHE` returns the new value).

**UAT 1 — Toggle visibility (D-02 / Success Criterion 1):**
1. Open the Tonight tab.
2. Below the existing roster action row (Mark everyone in / Clear couch / Send pushes to the rest), confirm a "Kid mode" pill button renders with helper hint "Hide R + PG-13 from tonight's pool" beneath it.
3. PASS = pill visible. FAIL = no pill (potential cause: familyHasKids() returning false despite kid members in roster — investigate ageToMaxTier mapping for the youngest member).

**UAT 2 — Toggle interaction + visual state (D-05 / D-14..16 / Criteria 1+6):**
1. Tap the "Kid mode" pill.
2. Confirm: pill flips to amber-filled style + label changes to "Kid mode on" + helper hint disappears + roster surface gets subtle amber tint + flashToast says "Kid mode on — hiding R + PG-13".
3. Tap again. Confirm reversion: pill back to dashed-border + label back to "Kid mode" + tint gone + flashToast says "Kid mode off — full pool back".
4. PASS = both transitions render cleanly + toasts confirm. FAIL = no visual feedback OR copy mismatch.

**UAT 3 — Tier-cap filter on Tonight (D-07/D-08 / Criterion 3):**
1. Pre-condition: at least one R-rated and one PG-13-rated title with a couch yes-vote exists in the family library. (If not, vote on Inception (PG-13) and any R-rated title to set them up.)
2. Activate kid-mode. Confirm: Tonight matches + considerable lists exclude the R-rated and PG-13-rated titles (any G/PG yes-voted titles still visible).
3. Vetoed titles + already-watched titles remain hidden (D-18..D-20 guard — sanity check by vetoing a kid-eligible title; should still be hidden when kid-mode is on).
4. Deactivate kid-mode. Confirm: PG-13 + R titles re-appear in matches/considerable.
5. PASS = filter applies + reverts cleanly. FAIL = wrong tier cap (e.g., R passes through), OR vetoed/watched titles bypass kid-mode gate, OR no change at all.

**UAT 4 — Library filter (Criterion 5):**
1. Activate kid-mode + navigate to Library tab.
2. Set filter to 'For me' — confirm PG-13/R titles excluded for kid-mode test member.
3. Set filter to 'Unwatched' — confirm same.
4. PASS = both filters apply kid-mode cap. FAIL = either filter ignores kid-mode.

**UAT 5 — Session-scope reset (D-04 / Criterion 2):**
1. Activate kid-mode.
2. Tap the Library tab (navigate away from Tonight). Confirm flashToast does NOT fire (silent reset).
3. Return to Tonight. Confirm: kid-mode is now OFF (pill is in idle state). Couch state is preserved (members still in/out as before — kid-mode toggle does not clear couch).
4. Activate kid-mode again. Tap "Clear couch" action link. Confirm: kid-mode is now OFF + couch is empty.
5. PASS = kid-mode resets cleanly on both showScreen-away AND couchClearAll. FAIL = either boundary fails to reset.

**UAT 6 — Parent override on detail modal (D-10..D-13 / Criterion 7):**
1. As a PARENT account, activate kid-mode. Open a PG-13 title's detail modal.
2. Confirm: "Show this anyway for tonight" link visible immediately below "+ Add to list".
3. Tap the link. Confirm: detail modal closes + flashToast says "Showing this for tonight" + the previously-blocked title now appears in Tonight matches.
4. Re-open the same title's detail modal. Confirm: the link is replaced with "Showing this for tonight (kid-mode override)" italic line — NOT re-clickable.
5. As a NON-PARENT account (or switch to kid sub-profile), repeat step 1. Confirm: NO override link appears.
6. Toggle kid-mode off + back on. Open the same title's detail. Confirm: the override has been cleared (link visible again, not the "active" hint).
7. PASS = all 6 sub-conditions hold. FAIL = link visible to non-parents (security), OR override doesn't unblock the title, OR override persists across kid-mode toggles.

**UAT 7 — Cross-device session isolation (D-06):**
1. On Device A: activate kid-mode.
2. On Device B (same family, different signed-in user OR same user different device): confirm kid-mode is NOT active there. (D-06 — kid-mode is per-device session state, not family-wide.)
3. PASS = independence confirmed. FAIL = kid-mode propagated.

**UAT 8 — sw.js cache bump (Criterion 8):**
1. From DevTools (or Safari Web Inspector) → Application → Service Workers, confirm active worker matches `couch-v36.1-kid-mode`.
2. Or via curl: `curl -s https://couchtonight.app/sw.js | grep "const CACHE"` returns the new value.
3. PASS = new cache live. FAIL = old cache still serving (CDN lag or deploy issue).

**Failure modes to watch for:**
- Toggle visible on adult-only families (familyHasKids() bug)
- Override link visible to non-parents (defense-in-depth bypassed at render-side gate)
- Vetoed/watched titles bypassing kid-mode (gate evaluation order wrong)
- Kid-mode persisting across PWA cold-start (someone wired Firestore by accident)

**Resume signal:** Type "uat passed" to declare Phase 19 shipped + trigger /gsd-verify-work 19, OR describe any failure mode for triage.

**If any UAT fails:** capture the failure mode in 19-03-SUMMARY.md, file a follow-up under .planning/todos/pending/, and decide whether to revert the deploy or ship a hotfix patch. Phase 18 precedent: code-level approval can ship even with deferred device UAT items, BUT for Phase 19 the toggle is the ENTIRE feature — UAT 1+2+3+6 must pass to declare shipped.
    </manual>
  </verify>
  <done>
- All 8 UAT walkthroughs completed (UAT 1+2+3+6 must PASS; UAT 4+5+7+8 PASS or documented as deferred)
- 19-03-SUMMARY.md captures per-UAT pass/fail matrix
- User signaled "uat passed" (resume signal) OR captured triage notes for failures
  </done>
</task>

</tasks>

<verification>
Phase 19 / Plan 03 ships when:

1. sw.js CACHE = 'couch-v36.1-kid-mode' both in source and live at couchtonight.app
2. All 4 smoke contracts pass during deploy
3. UAT 1 + UAT 2 + UAT 3 + UAT 6 (the four critical user-experience UATs) all pass on a real device
4. UATs 4, 5, 7, 8 either pass or are documented as deferred with rationale
5. STATE.md sw.js cache version line updated
6. 19-03-SUMMARY.md captures the deploy + UAT pass-fail matrix
</verification>

<success_criteria>
- [ ] sw.js CACHE bumped to couch-v36.1-kid-mode in source + mirror
- [ ] couchtonight.app serves new cache (curl-verified)
- [ ] 4-smoke gate passed during deploy
- [ ] UAT 1+2+3+6 all PASS (toggle visibility + interaction + tier-cap + parent override)
- [ ] UATs 4/5/7/8 documented (PASS or deferred with rationale)
- [ ] STATE.md updated
- [ ] User signals "uat passed" (resume signal)
</success_criteria>

<output>
After completion, create `.planning/phases/19-kid-mode/19-03-SUMMARY.md` documenting deploy timing + cache curl confirmation + per-UAT pass/fail matrix.
</output>
