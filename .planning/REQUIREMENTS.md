# Requirements: Couch

**Defined:** 2026-04-19
**Core Value:** Turn "what do you want to watch?" from a 20-minute argument into a 30-second ritual that everyone on the couch trusts.

> **Brownfield note:** Phases 1 and 2 shipped prior to GSD adoption. Their capabilities are captured under **Validated** in PROJECT.md. This file scopes **v1 Requirements** to Phase 3-6 work (the remaining milestone toward commercial release).

> ⚠ **Coverage state acknowledged stale per v33.3 milestone audit (2026-04-27).** Many requirements below are marked `Pending` even though the underlying work shipped to production — Phase 5/6/8 entire ranges, plus most PARTY-* + POL-* + COMP-13-* + OPS-13-* rows. The v33.3 audit (`.planning/v33.3-MILESTONE-AUDIT.md`, commit 001b259) found this is verification-artifact debt, not implementation-debt: the cross-phase integration check confirmed all 3 critical E2E flows wired and 0 BLOCKERS. Comprehensive traceability refresh is tracked under **Phase 15.2** (audit-trail backfill). Only **DESIGN-01** is genuinely unsatisfied — being closed by **Phase 15.3** (canonical SVG logo production). The 2 integration FLAGS (F-W-1 sendCouchPing fake fan-out + D-3 friendly-UI Settings parity) are being closed by **Phase 15.4** (integration polish).

## v1 Requirements

### Mood Tags (Phase 3)

- [x] **MOOD-01**: User can view automatically-suggested moods on a title (derived from TMDB genres + runtime)
- [x] **MOOD-02**: User can add a personal mood tag to a title from the title's detail view
- [x] **MOOD-03**: User can remove a personal mood tag they previously added
- [x] **MOOD-04**: Mood tags added by any member in a family are visible to all members on that title
- [x] **MOOD-05**: Tonight screen shows a mood filter control in the filter bar
- [x] **MOOD-06**: Selecting one or more moods in the filter narrows the candidate pool for the spin
- [x] **MOOD-07**: Active mood filters are visible and easily clearable without opening the filter panel

### Veto System (Phase 4)

- [x] **VETO-01**: Any member can veto a title before the spin, removing it from the candidate pool for that session
- [x] **VETO-02**: Any member can veto the spin result after it's picked, triggering a re-spin from the remaining pool
- [x] **VETO-03**: When a veto happens, all devices in the family see it in real-time with who vetoed
- [x] **VETO-04**: Veto can carry an optional reason (short text) so the family has context
- [x] **VETO-05**: Vetoes are recorded per-member so they can surface in Year-in-Review
- [x] **VETO-06**: A member who vetoed a title cannot be the one to re-spin it for that same session (fairness rule)

### Auth + Groups (Phase 5)

Skeleton — refined in /gsd-discuss-phase 5.

- [x] **AUTH-01**: A user can create an account and sign in (provider TBD in discuss — likely Firebase Auth with Google + Apple + email link)
- [x] **AUTH-02**: A family can be created, joined, or left by a signed-in user; every Firestore member write carries a stable `uid`
- [x] **AUTH-03**: A family owner can optionally set a password on the group; joining a password-protected group requires the password in addition to any invite code
- [x] **AUTH-04**: Existing families (created pre-auth with code-only + local member profiles) can complete a one-time claim flow that links each current member to a real account with zero data loss
- [x] **AUTH-05**: All existing features (Tonight, Mood, Veto, existing Sports Watchparty) continue to work end-to-end after the auth migration

### Push Notifications (Phase 6)

Skeleton — refined in /gsd-discuss-phase 6. Starts with a research spike comparing web-push vs Capacitor-wrapped vs FCM/OneSignal against iOS-first PWA reality and competitor patterns (Teleparty, Plex, Kast, Scener, Letterboxd, TV Time).

- [x] **PUSH-01**: A documented research artifact compares delivery options and recommends an implementation path
- [x] **PUSH-02**: A signed-in user can opt in to push on each device they use, with per-event-type granularity and a clear permission prompt
- [x] **PUSH-03**: At least one event (watchparty-starting) delivers a real push within ~5s on iOS (home-screen PWA) and Android
- [x] **PUSH-04**: Quiet-hours / do-not-disturb state suppresses push per user and per family
- [x] **PUSH-05**: Self-echo guard — a user never receives push for their own action (same pattern as VETO-03)

### Watchparty (Phase 7)

- [x] **PARTY-01**: A member can start a watchparty session from the "Tonight's Pick" card once a title is chosen
- [x] **PARTY-02**: Other members in the family can join an active watchparty session from their own device; a push invite reaches those opted in
- [x] **PARTY-03**: Play, pause, and seek signals sync across connected devices (sync model — advisory per-member timer vs host-broadcast vs consensus — decided in /gsd-discuss-phase 7) ✓ 2026-04-22 (advisory per-member timer with on-time inference + manual override — Plans 07-03 + 07-08)
- [x] **PARTY-04**: Members can send reactions (emoji or preset set) during the session
- [x] **PARTY-05**: Reactions surface on all devices in the session in real-time
- [x] **PARTY-06**: Session ends gracefully — host-ended, all-members-left, or timeout — with no orphan sessions in Firestore
- [x] **PARTY-07**: The Tonight screen shows a watchparty banner when a session is active (scaffolding already exists in the code)

### Watch-Intent Flows (Phase 8)

Skeleton — refined in /gsd-discuss-phase 8. Splits the current "who wants to watch" primitive into two first-class flows.

- [x] **INTENT-01**: A member can start a "Tonight @ time" flow with an optional scheduled time; other members see it and RSVP yes/no/maybe
- [x] **INTENT-02**: A member can start a "Watch-this-title" interest poll on any title; other members see it and mark Yes/No/Later without time commitment
- [x] **INTENT-03**: A positive match (threshold-defined per group size) surfaces as a prompt that can auto-schedule a Tonight (for interest polls) or auto-launch a Watchparty (for Tonight @ time flows)
- [x] **INTENT-04**: Thresholds are group-size-aware — duos need any-yes; families default to majority (configurable per family)
- [x] **INTENT-05**: Both flows cleanly expire or convert; no orphan docs remain in Firestore (same pattern as VETO/PARTY cleanup)
- [x] **INTENT-06**: Intent activity is recorded per-member in a shape consumable by Phase 10 Year-in-Review ("most-anticipated," "most-wanted-never-watched")

### Redesign / Brand / Marketing Surface (Phase 9)

Skeleton — refined in /gsd-discuss-phase 9. Covers visual identity + full UI refresh + landing page + marketing assets + onboarding polish.

- [ ] **DESIGN-01**: Final logo + app icon set + favicon shipped in the repo; wired to manifest, head, and iOS/Android home-screen icons
- [x] **DESIGN-02**: Warm dark palette audited and canonicalized into design tokens (colors, type, spacing, motion, radius) ✓ Phase 9 Plan 02 (2026-04-22)
- [x] **DESIGN-03**: Typography tokens (Fraunces + Instrument Serif + Inter) applied consistently across every screen; no one-off inline font declarations for brand-critical surfaces ✓ Plan 02 token layer (2026-04-22) + Plan 03 inline-style purge (2026-04-22) — 83 brand-critical inline declarations migrated to 53 token-backed classes; zero inline Fraunces/Instrument Serif remain
- [x] **DESIGN-04**: Every existing in-app screen (Tonight, Title Detail, Members, Settings, Watchparty, Mood filter, Veto modals) re-rendered against the canonical token system ✓ Plan 03 tokenized brand-critical surfaces + Plan 04 desktop responsive layer (2026-04-22) — `.phone-shell` wrapper + single `@media (min-width: 900px)` block; Phase 7 UAT deferred watchparty-modal gap closed (`.wp-live-modal` 520→800px on desktop)
- [x] **DESIGN-05**: A landing page exists at `couchtonight.app` root — mobile-first, warm cinematic, explains what Couch is and drives to install / signup ✓ Plan 05 (2026-04-22) — landing.html (162 lines) at /, app.html at /app via Firebase Hosting rewrites; JSON-LD SoftwareApplication schema; install-redirect for standalone + ?invite=/?claim= deep links; user-locked copy with "Why we built it" blended-family hook + "Two ways to couch" SPIN/NOMINATION sections + 5-card audience grid; deployed to couchtonight.app, all 7 automated post-deploy gates green
- [x] **DESIGN-06**: App-Store-ready marketing asset set (hero screenshot, feature screenshots, icon variants, promo graphics) produced and stored alongside the landing page ✓ Plan 06 (2026-04-23) — 5 iPhone screenshots captured by user, optimized via sharp, wired to landing.html with CSS phone-frame treatment (border-radius + warm-amber drop shadow + figcaption overlay); deployed to couchtonight.app. og.png deferred to ship with wordmark redesign per user decision. Long-term: refresh screenshots when product surfaces evolve (tracked in Phase 12 docket).
- [x] **DESIGN-07**: First-run onboarding polished to match the redesign and introduces feature surfaces tastefully (moods, veto, watchparty, push opt-in, intent flows) ✓ Plan 07a (2026-04-23) — 3-step overlay (couch silhouette / spin-pick-veto / 4-grid feature tour), guest-skip Pitfall 5 defense, seenOnboarding gate, "Replay intro" in Settings; deployed to couchtonight.app
- [x] **DESIGN-08**: Invite-flow onboarding (when a new user accepts an invite to an existing family) polished and brand-aligned ✓ Plan 07b (2026-04-24) — `?invite=<token>` bootstrap detour, branded redeem screen + expired dead-end, `consumeGuestInvite` CF (admin-SDK, idempotent), `seenOnboarding:true` on guest creation (Pitfall 5 paired with 09-07a), defense-in-depth `&family=` URL strip (server emits token-only + client sanitizes); deployed to couchtonight.app
- [x] **DESIGN-09**: Motion language defined and applied — micro-interactions (toast, modal open, spin flicker, veto shimmer, watchparty state transitions) all use the canonical easing + duration tokens ✓ Plan 02 token layer (2026-04-22) + Plan 07b motion audit (2026-04-24) — 47 lines normalized / 66 replacements / 5 new motion tokens (`--t-spin`, `--t-celebrate`, `--t-shimmer`, `--t-pulse`, `--t-story`); zero raw ms or cubic-bezier in rule bodies; prefers-reduced-motion block hardened
- [x] **DESIGN-10**: Brand-system documentation captured in `.planning/` for future-phase reference (token cheatsheet, do/don't, voice guide) ✓ Plan 07a (2026-04-23) — `.planning/BRAND.md` 185 lines / 1497 words; 8 sections (identity, color tokens with WebAIM contrast, typography, spacing+radius+shadow, motion catalog, voice guide, screen patterns, do/don't gallery)

### Year-in-Review (Phase 10)

- [ ] **YEAR-01**: End-of-year recap page aggregates each member's watch history for the year
- [ ] **YEAR-02**: Per-member stats include titles watched, total runtime, top genres, and mood distribution
- [ ] **YEAR-03**: Per-member "favorites" section highlights top-rated titles (from Yes votes + Trakt ratings)
- [ ] **YEAR-04**: Family-level stats include most-watched-together, most-vetoed, longest watchparty, and most-anticipated-but-never-watched (from Phase 8 intent data)
- [ ] **YEAR-05**: Recap has a shareable surface (export image, copy-link, or save-to-device) — rendered against Phase 9 brand tokens

### Feature Refresh & Streamline (Phase 11)

Scoped via /gsd-discuss-phase 11 (2026-04-23); 6 user decisions locked; RESEARCH.md + CONTEXT.md + 3 appendices (CATEGORIES, SMS-OPTIONS, TABS-AUDIT) complete. Two parallel tracks: declutter + streamline (REFR-01/02/03, REFR-11/12) and moat-expansion (REFR-04, REFR-05/06, REFR-07/08/09, REFR-10, REFR-13).

- [x] **REFR-01**: Mood filter chip spacing tightened; chip padding reduced; visually denser without sacrificing tap target [Plan 11-01, complete 2026-04-24]
- [x] **REFR-02**: "Whose turn to pick" UI surfaces hidden across Tonight + Family tabs (feature-flag reversible via body class); backend spinnership writes preserved [Plan 11-01, complete 2026-04-24]
- [x] **REFR-03**: "Who's on the couch" card redesigned to a cleaner, denser format (avatar-stack or compact horizontal row — variant locked at plan-phase) [Plan 11-01, complete 2026-04-24]
- [x] **REFR-04**: Add-tab discovery expanded from 4 to a 25-row DISCOVERY_CATALOG organized in 7 buckets (always-on / trending / discovery auto / discovery curated / use-case / theme-of-day / seasonal / personalization-group-aware); 7-10 rows visible per day via per-user-per-day hash-seeded rotation; auto categories + curated lists + Browse-all + pin-up-to-3 + personalization ALL shipped across 11-03a+11-03b [Plans 11-03a + 11-03b, complete 2026-04-24]
- [x] **REFR-05**: Web RSVP route (`/rsvp/<token>`) + Web Share API integration — lightweight HTML+JS RSVP page (no full app shell, no Firebase SDK); schedule modal wires `navigator.share()`; non-members land on RSVP page, convert to push-receiving members on first interaction. Twilio + SMS automation deferred to Milestone 2 per user decision 4. [Plan 11-04, code-complete 2026-04-24; host-triggered "remind unresponsive" button deferred to Plan 11-05 lobby scope]
- [x] **REFR-06**: Asymmetric push reminder cadence keyed to RSVP state (members only) — Yes = 2 touches (T-24h, T-1h); Maybe = 3 touches (T-7d, T-24h, T-1h); Not-responded = 2 (T-48h, T-4h); No = silence. Host-triggered "remind unresponsive" button (push for members, re-share-link for non-members). [Plan 11-04, code-complete 2026-04-24 — auto-cadence shipped via rsvpReminderTick scheduled CF; host-triggered manual remind button deferred to Plan 11-05]
- [x] **REFR-07**: Pre-session lobby upgrade — evolve participant timer strip into a T-15min "green room": who's here, who's en-route, countdown, per-person Ready check, democratic auto-start at T-0 once majority Ready [Plan 11-05, code-complete 2026-04-24; deploy deferred with 11-04 pending Blaze billing confirmation]
- [x] **REFR-08**: Late-joiner "Catch me up" recap — 30-second summary of reactions that fired before they joined. Preserves Couch's per-user reaction-delay moat while eliminating timeline-loss friction. [Plan 11-05, code-complete 2026-04-24]
- [x] **REFR-09**: Post-session loop — auto-prompt 5-star rating + "Add photo to album" + one-tap "Schedule next" using same roster. Converts one party into recurring ritual. [Plan 11-05, code-complete 2026-04-24; Firebase Storage first use — Variant A rules pending deploy + Firebase Console Storage enablement + emulator test matrix]
- [x] **REFR-10**: Dedicated Sports Game Mode — `SportsDataProvider` abstraction (ESPN hidden API primary + BALLDONTLIE stub for v2 swap), game picker modal with 4 league tabs, live score strip (sticky-top, tabular-nums), score-delta polling (adaptive 5s on-play / 15s off-play), play-scoped amplified reactions (4 burst emojis, 3s auto-dismiss), late-joiner sports Catch-me-up variant (score + last 3 plays), team-flair avatar badges (border in team color via runtime CSS custom property), per-user DVR slider writing to participants[mid].dvrOffsetMs AND reusing Phase 7 reactionDelay anchor. [Plan 11-06, code-complete 2026-04-24; deploy deferred — ESPN ToS-gray accepted per threat model; BALLDONTLIE swap path wired for when/if ESPN denies]
- [x] **REFR-11**: Family tab restructured per `11-APPENDIX-TABS-AUDIT.md` — current 6 sections → 5 (Tonight status NEW / Approvals / Members split active vs sub-profiles / Couch history consolidated / Group settings footer). Picker section cut per REFR-02. Couch calendar + ADDs deferred to Phase 12. [Plan 11-02, complete 2026-04-24]
- [x] **REFR-12**: Account tab restructured per `11-APPENDIX-TABS-AUDIT.md` — current 9 flat sections → 3 cognitive clusters (You / Couch-wide / Admin & maintenance). NEW sections (per-event notif prefs detail, data export/delete, theme prefs, about/version+feedback) deferred to Phase 12. [Plan 11-02, complete 2026-04-24]
- [x] **REFR-13**: "Couch Nights" themed ballot packs — 8 curated packs (Studio Ghibli Sunday, Cozy Rainy Night, Halloween Crawl, Date Night Classics, Kids' Room Classics, A24 Night, Oscars Short List, Dad's Action Pantheon) with hero poster + BRAND-voice description + mood token + 10-12 curated TMDB IDs. Add-tab tile row + pack-detail sheet + "Start this pack" seeds ballot + launches Vote mode. [Plan 11-07, complete 2026-04-24]

### Pre-launch polish (Phase 12)

Tight ~1-day patch closing UAT-surfaced gaps from Phase 11 + Phase 6.

- [x] **POL-01**: Per-event notification preferences UI in Account → YOUR COUCH → Notifications. Master toggle expand/collapse + 6 per-event toggles (watchpartyScheduled, watchpartyStartingNow, intentRsvpRequested, inviteReceived, vetoCapReached, tonightPickChosen) + quiet hours start/end pickers. Writes to `users/{uid}.notificationPrefs` immediately with optimistic UI. Closes Phase 6 PUSH-02 + PUSH-04 runtime UAT loop (server-side enforcement already shipped in functions/index.js). ✓ 2026-04-25 (commit b1ab9a9; acceptance met — partial-parity-with-Phase-14-D-3 closed by Phase 15.4)
- [x] **POL-02**: About / version / feedback / changelog footer in Account ADMIN cluster. Version line ("Couch v{N} — deployed {YYYY-MM-DD}"), mailto feedback link, "What's new" link to `/changelog.html` (NEW standalone page following landing.html posture). Hosting rewrite `/changelog` → `/changelog.html`. ✓ 2026-04-25 (commit 6323530)
- [x] **POL-03**: Halloween Crawl pack ID curation — Plan 11-07 used `tmdbId 9532` for "Hocus Pocus" but TMDB resolves it to "Final Destination". Replace with the real Hocus Pocus ID; pass-through verify the other 9 IDs in the pack against their comment labels. ✓ 2026-04-25 (commit 004255b)
- [x] **POL-04** (stretch): Other-7-packs curation pass — verify each remaining pack's tmdbIds match their comment labels via TMDB API. Apply fixes where mismatched. Optional: refactor to drop comment labels in favor of a build-time test that asserts title matches, eliminating drift permanently. ✓ 2026-04-25 (stretch goal landed; 14 mismatches corrected via commit 004255b)

### Compliance & Ops Sprint (Phase 13)

Solo-actionable post-v1 hardening sprint locked via /gsd-discuss-phase 13 + /gsd-plan-phase 13 (6 decisions locked 2026-04-25). Not a feature phase — operational hygiene + compliance hardening only.

- [x] **COMP-13-01**: Self-serve account deletion. A signed-in user can delete their account from Account → ADMIN; soft-delete enters a 14-day grace window during which they can sign back in to cancel; after 14 days a scheduled reaper hard-deletes Firestore (members + votes + queues + watchparty participations + intent rsvps + couch-album doc refs) + Storage (couch-albums photos owned by the uid) + Firebase Auth user record + writes an audit doc. Closes the manual privacy.html "email privacy@couchtonight.app" promise within the 30-day CCPA/GDPR window. Trigger: HTTPS callable + Cloud Scheduler reaper (NOT auth.user().onDelete — firebase-functions v2 has no auth triggers). ✓ 2026-04-25 (commits ccd3eec + 4fc49ee + 6665648; HUMAN-VERIFY pending — 4 CFs live in us-central1)
- [x] **OPS-13-02**: BUILD_DATE auto-stamp on deploy. `scripts/deploy.sh` invokes `scripts/stamp-build-date.cjs` automatically before `firebase deploy`, eliminating the "I forgot to bump the date" failure mode that bit Phase 12. Wired via `npm run deploy` + `npm run stamp` shortcuts; documented in RUNBOOK §H. Pitfall 7 dirty-tree guard with `--allow-dirty` escape hatch. ✓ 2026-04-25 (commits 7df42a2 + 45a9da7 + cc8b784)
- [x] **OPS-13-04**: Content-Security-Policy in Report-Only mode. Additive 4th security header in queuenight/firebase.json allow-listing all third-party origins (Firebase + TMDB + Trakt + Google Fonts + Sentry + Cloud Functions). Ships in `Content-Security-Policy-Report-Only` mode for v1 — 2-week observation window — before flipping to enforcement in a later sprint. Existing X-Content-Type-Options + Referrer-Policy + X-Frame-Options preserved verbatim. Updates TECH-DEBT.md TD-4 status. ✓ 2026-04-25 (commits 5149c7d + 2a8ceed; HUMAN-VERIFY — TD-4 audit window active; CSP Report-Only live)
- [x] **OPS-13-05**: Sentry error reporting integration. CDN loader script (zero-bundler, public-by-design DSN) added to app.html + landing.html `<head>` with PII-scrubbing `beforeSend` (strips event.user.id (uid) + event.user.email + event.user.username + event.user.ip_address + family-code regex `/\b[a-f0-9]{6}\b/gi` + invite/claim/rsvp tokens in URLs) and Firestore-noise-filtering `beforeBreadcrumb` (Pitfall 5 defense). Vendor: Sentry, NOT Crashlytics — Firebase Crashlytics has no web SDK as of 2026 (firebase-js-sdk#710). Free tier (5K errors/month) sufficient for v1. ✓ 2026-04-25 (commits 19f9f6a + 7b9aa5e + bd2890f + e4f2c89; HUMAN-VERIFY pending Sentry inbox observation)
- [x] **OPS-13-06**: GitHub branch protection ruleset. `protect-main` Ruleset on github.com/<owner>/couch with empty bypass list: Restrict deletions + Block force pushes + Require linear history + Require PR (0 reviewers, solo-dev gate) + Require `syntax-check` CI status check from `.github/workflows/ci.yml` + Require conversation resolution. Direct push to main rejected with GH013. Configured via web UI (CONTEXT.md addendum #2 locks the path). ✓ 2026-04-25 (branch protection live; repo public per RUNBOOK §L)
- [x] **OPS-13-07**: Scheduled Firestore export + restore drill. Cloud Scheduler HTTP job `daily-firestore-export` hits `firestore.googleapis.com/v1/.../databases/(default):exportDocuments` directly (no Pub/Sub + Cloud Function detour). Exports daily to `gs://queuenight-84044-backups/` (us-central1, matches Firestore region) with 30-day lifecycle delete rule + uniform-bucket-level-access. Idempotent setup script `scripts/firestore-export-setup.sh`. Restore drill documented in RUNBOOK §I + setup runbook in §K. ✓ 2026-04-25 (commits 4813bf2 + 69a9ed0; HUMAN-VERIFY pending gcloud auth on user's machine)

**Deferred to Phase 14:** OPS-13-01 (firebase-functions SDK 4.9.0 → 7.x — TECH-DEBT TD-1; sufficiently risky to deserve its own session).
**Deferred to a later milestone:** OPS-13-03 (Variant-B storage rules tightening — TECH-DEBT TD-2; gated on independent Phase 5 member-uid migration decision).

### Decision Ritual Core (Phase 14)

Scoped via /gsd-discuss-phase 14 (2026-04-25) — 12 user decisions locked (D-01..D-12) + 3 reconciled DRs (DR-1 extend `intents` collection rather than fork, DR-2 polish queue rather than rebuild, DR-3 add 7 push categories in 3 places). Replaces the vote-prominent Tonight tile with a couch-centered, conversation-first ritual. Two parallel flows (group rank-pick when together, solo nominate when apart) backed by the unified `intents` primitive (extended from Phase 8). 13 DECI-* requirements minted at /gsd-plan-phase 14 (2026-04-25).

- [x] **DECI-14-01**: Strict member-aware "already-watched" filter for the couch's discovery surfaces (Browse / Spin / Swipe). 4 sources count as watched: (1) Trakt sync flipped `t.watched=true`, (2) any member voted Yes, (3) any member voted No, (4) manually marked-watched. Per-title rewatch override via `t.rewatchAllowedBy[memberId]=ts` (same-day) re-enables a single title for the active session. Invitation bypass: filter applies only to MY discovery view — pushes from Flow A/B nominators to me are NEVER filtered (I can join a rewatch). Filter helper `isWatchedByCouch(t, couchMemberIds)` co-located with `getMyQueueTitles` near `js/app.js:6827`. (D-01) ✓ 2026-04-25 (commits 8ddb347 + 5a9ea75; js/app.js:7647)
- [x] **DECI-14-02**: 3-tier candidate filter on the new ranked picker surface. Tier 1 (default visible): titles where EVERY couch member has the title in their personal queue, sorted by mean of member-queue ranks, ties broken by `t.rating`. Tier 2 (default visible, below T1): ≥1 couch member has it in queue. Tier 3 (hidden behind expand): only off-couch members have it in queue ("watching her movie without her"). New `getTierOneRanked(couchMemberIds)` + `getTierTwoRanked` + `getTierThreeRanked` aggregators near `js/app.js:6852`. Most-restrictive-wins T3 toggle hierarchy across account-level / family-level / group-level — ANY level set to "hide T3" wins; "show T3" only takes effect when no level says hide. (D-02) ✓ Plan 14-03 complete 2026-04-26 — pure data layer shipped (commit ccf600f, +102 lines js/app.js); 4 functions co-located after getGroupNext3; UI consumer is 14-07.
- [x] **DECI-14-03**: Polish the existing per-member queue infrastructure (DR-2 reframe — primitive already shipped via `t.queues[memberId]` map at `js/app.js:12340`, drag-reorder at `js/app.js:4664`, persist at `js/app.js:4648`). Phase 14 scope: (a) add discoverability toast on Yes-vote queue auto-add — `flashToast("Added X to your queue")` only when actor is `state.me`; (b) verify Add-tab insertion path actually wires the new title into the actor's personal queue at the bottom (audit + fix if missing); (c) iOS Safari touch-DnD UAT — keep hand-rolled HTML5 DnD if it works, otherwise swap to Sortable.js via CDN. (D-03 reframed per DR-2) ✓ Plan 14-02 complete 2026-04-25 — UAT-DEFERRED (Tasks 1+2 code-complete and committed: 2e1ca4b applyVote toast + 2b670d2 createTitleWithApprovalCheck addToMyQueue opt-in across 4 user-Add call sites; Task 3 iOS Safari touch-DnD UAT deferred to batch UAT pass — must run before v34 production deploy)
- [x] **DECI-14-04**: Tile redesign. Tile face: poster + title + year + runtime + new "X want it" pill (3 micro-avatars + "+N" overflow chip computed from `t.queues` membership) + ▶ Trailer button (lazy-loaded on tap, uses existing `trailerKey` field). Vote button REMOVED from tile face. Single tap on tile body opens new `openTileActionSheet(titleId, e)` (4 buckets: Watch tonight / Schedule for later / Ask family / Vote de-emphasized) + secondary entries (Show details / More options falling back to existing `openActionSheet` at `js/app.js:11853`). Detail modal surfaces formerly-hidden items: Trailer, Providers, Synopsis, Cast, Reviews. Includes carry-over UAT bug close-out: verify `.detail-close{position:fixed}` already at `css/app.css:1540` covers the Library/Queue context (or extend if a different scroll container is involved). (D-04) ✓ Plan 14-05 complete 2026-04-26 — Tasks 1-4 in commits f90155d + d1762e1 + 09db482 + 6d630c4; carry-over UAT bug CLOSED by pre-existing CSS at css/app.css:1540 (NOT deferred); soft iOS Safari render confirmation rolled into existing batch UAT pass as non-blocking
- [x] **DECI-14-05**: Vote mode preserved in Add tab as bulk curation surface. "Catch up on votes (N)" CTA renders automatically when ≥10 unvoted family titles exist; tap launches existing `openVoteModal()` swipe flow. Vote mode is a sibling to (not replacement for) the new tile action sheet's "Vote" entry. (D-05) ✓ Plan 14-05 complete 2026-04-26 — commit 6d630c4; unvotedCount conditional CTA spliced at top of Add-tab innerHTML composition; .add-catchup-cta CSS uses warm-amber border + token-aligned background
- [x] **DECI-14-06**: Couch viz on Tonight tab. **Approach pivoted via sketch 001 (2026-04-25):** original spec was procedural inline SVG `renderCouchSvg(seatCount)` with adaptive seat count 2-8; sketch 001 (.planning/sketches/001-couch-shape/) concluded that hero-icon + avatar-grid (Variant P) is the right architecture — the existing app icon (`/mark-512.png`) carries the C-sectional couch shape as a brand hero element, and a CSS-grid avatar layer below handles arbitrary 1-10 capacity cleanly without per-cushion SVG geometry. `renderCouchViz()` + `renderCouchAvatarGrid(couchSize)` injects hero (`<img src='/mark-512.png'>`) + Fraunces "On the couch tonight" headline + Instrument Serif sub-count ("{N} of {couchSize} here") + grid (5×2 phone wrap, 10×1 desktop at >=768px). Filled cells = colored circle with initial + warm-amber outline on the "me" cell; empty cells = dashed amber `seat-empty` with `＋` glyph + 2.4s `couch-empty-pulse` animation (disabled under `prefers-reduced-motion`). `claimCushion(idx)` toggles take/vacate (no clones invariant). `openCouchOverflowSheet()` + `claimCushionByMember(mid)` cover shared-device "claim on behalf of" via existing `#action-sheet-bg` primitive at js/app.js:11853. `persistCouchSeating()` writes `families/{code}.couchSeating = { [memberId]: index }` via `writeAttribution()`-attributed `updateDoc`. Family-doc onSnapshot hydrates `state.couchMemberIds` (indexed-array shape) for downstream consumers (14-01 isWatchedByCouch, 14-03 tier aggregators, 14-07 Flow A). Hero swap = zero-code `mark-512.png` replacement (Fiverr brand-refresh ETA 2026-04-28). (D-06) ✓ Plan 14-04 complete — UAT-DEFERRED (2026-04-26; commits 1234624 + 3a561bd + cb31155; Tasks 1-4 shipped — state slot + renderer + claim handlers + persistence + Tonight render hook + family-doc hydration in js/app.js +157 lines + #couch-viz-container in app.html + ~140-line D-06 CSS block in css/app.css + firestore.rules Case A additive third UPDATE branch with attributedWrite() + affectedKeys allowlist + 4 new sibling tests in tests/rules.test.js 22/22 passing; Task 5 visual + iOS PWA UAT deferred to batch UAT pass — must run before v34 production deploy; recap in 14-04-SUMMARY.md; `firebase deploy --only firestore:rules` from queuenight/ also required before v34 production deploy)
- [x] **DECI-14-07**: Flow A — group rank-pick + push-confirm. (1) Members claim cushions on Couch viz (cushion-tap → `claimCushion(idx)` writes `intents/{id}.expectedCouchMemberIds` once intent created); (2) App shows ranked list per tier; (3) Picker selects a title → roster screen → picker proxy-confirms members "in person" (taps to mark them "in") → only unmarked members get push (`flowAPick` eventType); (4) Push payload allows In / Reject / Drop; (5) Reject-majority auto-prompts picker for #2 from ranked list (1 retry, then expire); rejector can attach counter-nomination via `rsvps[me].counterTitleId` (push to picker as `flowAVoteOnPick`); (6) Counter-chain capped at 3 levels via `counterChainDepth` field (rules-enforced — see DECI-14-09); (7) Quorum to convert intent → watchparty: picker + ≥1 confirmed `state==='in'`; (8) Hard expire 11pm same-day if not converted. (D-07) ✓ Plan 14-07 complete — UAT-DEFERRED (2026-04-26; commit d97a81e; Tasks 1-4 shipped in one coupled commit matching the 14-08 Flow B precedent — entry CTA + picker 3-tier with T3 collapse gated by resolveT3Visibility() + roster proxy-confirm + send-picks + recipient response in/reject/drop/counter + counter-nom chain 3-cap + reject-majority retry + quorum convert; Task 5 multi-device UAT deferred to batch UAT pass — must run before v34 production deploy; recap in 14-07-SUMMARY.md; bundled conceptually with the 14-08 multi-device Flow B UAT entry — same staging deploy + 2-device session covers both flows)
- [x] **DECI-14-08**: Flow B — solo-nominate + invite + counter-time. (1) Member nominates a title with proposed time; (2) Push to other members (`flowBNominate`): join @ proposed time / counter-suggest (later/earlier with note via `rsvps[me].counterTime`) / decline; (3) Counter-time decision belongs to nominator only — UI lets nominator accept / reject / pick-compromise per counter (push to nominator: `flowBCounterTime`); (4) Auto-convert intent → watchparty at T-15min if any `state==='in'` RSVPs exist (CF-driven via `watchpartyTick` extension, see DECI-14-09; push: `flowBConvert`); (5) All-No edge case auto-cancels intent + immediate push to nominator; (6) Hard expire T+4hr past proposed start time. (D-08) ✓ Plan 14-08 complete — UAT-DEFERRED (2026-04-26; commit af168f1; multi-device UAT deferred to batch UAT pass — must run before v34 production deploy; recap captured in 14-08-SUMMARY.md)
- [x] **DECI-14-09**: Extend the existing `families/{code}/intents/{intentId}` collection (DR-1 — NOT a new collection). Schema additions: `flow: 'tonight_at_time' | 'watch_this_title' | 'rank-pick' | 'nominate'` discriminator (legacy `type` field preserved verbatim; reads accept both vocabularies; new flows use `flow`); `expectedCouchMemberIds[]` (Flow A); `counterChainDepth` (0-3, rules-capped at 3); `convertedToWpId`; widened `rsvps[mid]` shape — accepts Phase 8 `{value:'yes'|'no'|'maybe'|'later'}` AND new `{state:'in'|'reject'|'drop'|'maybe', counterTime?, counterTitleId?, note?}`; widened `status` enum to include `'converted'`. `firestore.rules:338-386` widened: create accepts 4 types, new update branch (5) for counter-chain bump enforces `counterChainDepth <= 3`, convert branch (4) extended to allow `open → converted` (Flow A) and adds `convertedToWpId` to allowed keys. CF: extend `onIntentCreated` (`queuenight/functions/index.js:354`) and `onIntentUpdate` (`:408`) with flow-aware branches; extend the existing intent expiry sweep inside `watchpartyTick` (`:494-512`) with 3 new behaviors — Flow A 11pm hard-expire, Flow B T-15min auto-convert when any `state==='in'`, T-30min `intentExpiring` warning push. NO new collection, NO new CFs, NO new onSchedule trigger. (D-09 reframed per DR-1)
- [x] **DECI-14-10**: Hybrid onboarding for the new primitives. (a) Changelog v34 entry inserted into `changelog.html:67` describing the redesign (for users who go looking); (b) 3 in-context, dismiss-on-interaction tooltips at moment of first encounter — `couchSeating` (Tonight tab Couch viz first render), `tileActionSheet` (first tile tap post-update), `queueDragReorder` (first Library tab visit when filter==='myqueue') — anchored to target via new `showTooltipAt(el, msg, opts)` primitive in `js/utils.js`. Per-tooltip gates via `members/{id}.seenTooltips: { [primId]: true }` Firestore sub-map (write via `updateDoc(doc(membersRef(), state.me.id), {[\`seenTooltips.${primId}\`]: true})`); guest members skipped. NO mandatory modal. (D-10)
- [x] **DECI-14-11**: 5 empty-state surfaces with action-leading CTAs (no dead ends): (a) brand-new family — "Your couch is fresh. What should be the first?" → "Add a title" + "Connect Trakt to import history"; (b) empty personal queue — "Your queue is empty. Vote on a few titles to fill it up." → "Open Vote mode" + 3-5-trending carousel; (c) Flow A entry, no couch — "Who's on the couch tonight? Tap to seat yourself + invite family." → Couch viz with all cushions glowing as +add affordances; (d) all-watched (T1+T2 empty) — "You've seen everything in queue. Revisit a favorite or expand?" → "Show rewatch options" (reveal T3) + "Discover more" → Add tab; (e) Flow A reject-majority no-#2 — "No alternative pick. Try again or anyone nominate?" → "Cancel for tonight" + "Open Flow B". Each uses the existing `<div class="queue-empty">` shell at `js/app.js:4592`. (D-11)
- [x] **DECI-14-12**: 7 new push notification categories added in THREE places per DR-3 (server defaults + client defaults + client labels) — NOT two. Categories: `flowAPick`, `flowAVoteOnPick`, `flowARejectMajority`, `flowBNominate`, `flowBCounterTime`, `flowBConvert`, `intentExpiring`. All default ON (these fire only when the user has actively engaged with a flow). Place 1: `NOTIFICATION_DEFAULTS` at `queuenight/functions/index.js:74` (server-side gate; missing here = `defaultOn=true` per `hasDefault` check at `:114` and user toggle is silently ignored). Place 2: `DEFAULT_NOTIFICATION_PREFS` at `js/app.js:100` (client merge baseline). Place 3: `NOTIFICATION_EVENT_LABELS` at `js/app.js:113` (BRAND-voice copy per D-12; warm family-tapping-shoulder tone, no "Notification:" prefix, no UTC timestamps). Cadence: critical-immediate for state changes needing user action; contextual-batched (60-90s coalesced digest) for info-only state changes. Quiet hours respected via existing Phase 6 `isInQuietHours` enforcement in `sendToMembers`. (D-12 + DR-3)
- [x] **DECI-14-13**: `sw.js` CACHE constant bumped to `couch-v34.0-decision-ritual` and deployed (via `bash scripts/deploy.sh 34.0-decision-ritual`) so installed PWAs invalidate and pick up the redesigned tiles + Couch viz + tooltip primitive + new push wiring + extended intents schema. Cross-cutting requirement covering all user-visible changes shipped across DECI-14-01..12. Bump and deploy happen at the end of the phase, NOT per-plan, to avoid CACHE thrash during execution.

### Tracking Layer (Phase 15)

Scoped via /gsd-discuss-phase 15 (2026-04-26) — 16 user decisions locked
(D-01..D-16). Per-watching-group watch progress (tracks GROUPS, not just
individuals — Couch's primary differentiator vs Trakt) + new-episode air-date
push notifications + live-release watchparty auto-prompts. Builds on Phase 14's
intents collection + 7-key push framework (extends to 8 keys). 14 TRACK-15-*
requirements minted at /gsd-plan-phase 15 (2026-04-26); revised at /gsd-plan-phase
15 --reviews (2026-04-26) to incorporate cross-AI peer feedback (REVIEW HIGH-1
through MEDIUM-12).

- [x] **TRACK-15-01**: Firestore data foundation — 5th UPDATE branch on `/families/{familyCode}` permitting attributedWrite of `tupleNames` map (D-02) AND `coWatchPromptDeclined` map (REVIEW MEDIUM-9 cross-plan coordination via 15-08 Task 4); composite index on `watchparties (titleId asc, startAt asc)` for D-16 suppression query; 10 sibling tests (#23-#32) covering tupleNames + per-member tupleProgress isolation + per-member mutedShows isolation + liveReleaseFiredFor client-DENY (REVIEW HIGH-1). (D-02 / D-06 / D-12 / D-16 / REVIEW HIGH-1)
- [x] **TRACK-15-02**: Tuple read/write helpers in js/app.js — `tupleKey` (with REVIEW HIGH-2 isSafeTupleKey character-safety regex), `tupleProgressFromTitle`, `tuplesContainingMember`, `tupleDisplayName` (derived fallback), `tupleCustomName` (REVIEW MEDIUM-6 — returns null when no custom name set, fixing the placeholder render gate in 15-04), `writeTupleProgress`/`clearTupleProgress`/`setTupleName`/`writeMutedShow`. setTupleName performs REVIEW MEDIUM-8 optimistic local state update. State.family.tupleNames hydrated from family-doc onSnapshot. Coexists with existing per-individual t.progress[memberId] for v1. (D-02 / D-03 / D-08 / REVIEW HIGH-2 / REVIEW MEDIUM-6 / REVIEW MEDIUM-8)
- [x] **TRACK-15-03**: D-01 watchparty-end auto-track stash — extends `openPostSession` with `state._pendingTupleAutoTrack` populated via REVIEW MEDIUM-5 episode-resolution waterfall (wp.episode → wp.queuedEpisode → wp.intent → host-progress-plus-1 → null). Records `sourceField` for confidence telemetry. Plus `window.toggleMutedShow(titleId)` click handler. (D-01 / REVIEW MEDIUM-5)
- [x] **TRACK-15-04**: S2 detail-modal "YOUR COUCH'S PROGRESS" section — `renderCv15TupleProgressSection(t)` with REVIEW MEDIUM-6 placeholder gate (uses tupleCustomName(tk) === null) + REVIEW MEDIUM-7 data-* attributes + delegated event listener (cv15HandleDetailModalClick on #detail-modal-body). Rows sorted watchedAt desc, max 4 visible, "View all (N)" affordance for >4. (D-04 / Q2 / REVIEW MEDIUM-6 / REVIEW MEDIUM-7)
- [x] **TRACK-15-05**: S3 inline tuple rename — pencil glyph reveals input via delegated listener; Enter/blur saves via setTupleName (REVIEW MEDIUM-8 optimistic local state means re-render reads new value immediately). flashToast on success/error per UI-SPEC. (D-02 / Q3 / REVIEW MEDIUM-7 / REVIEW MEDIUM-8)
- [x] **TRACK-15-06**: S6 per-show kill-switch — `renderCv15MutedShowToggle(t)` text-link with REVIEW MEDIUM-7 data-cv15-action attribute. Off: "Stop notifying me about this show"; on: "Notifications off · Re-enable". (D-12 / Q5 / REVIEW MEDIUM-7)
- [x] **TRACK-15-07**: S1 Tonight tab "PICK UP WHERE YOU LEFT OFF" widget — `<div id="cv15-pickup-container">` between #couch-viz-container and #flow-a-entry-container; `renderPickupWidget()` cross-show roll-up filtering tuples-containing-me, sorted desc, max 3 visible. Hides on zero tuples. Coexists with legacy #continue-section. (D-04 / Q1 / Q7)
- [x] **TRACK-15-08**: CF live-release sweep — per-titles loop in queuenight/functions/index.js watchpartyTick AFTER intents loop, BEFORE outer-family-loop close. REVIEW HIGH-3 hourly throttle (families/{code}.liveReleaseSweepLastRunAt cursor — once per hour, not every 5 min). REVIEW HIGH-4 stale-data guard (skip titles with t.nextEpisodeRefreshedAt > 7 days, Sentry breadcrumb). REVIEW LOW/MEDIUM-11 widened window (T-26h to T-23h, was T-25h). REVIEW MEDIUM-12 push framing ("New episode tonight/tomorrow/{Weekday}" — describes per-EPISODE prompt). D-14 ≥2 trackers, D-16 already-scheduled-wins, idempotency via t.liveReleaseFiredFor. CF admin SDK bypasses 15-01's HIGH-1 client-DENY rule for liveReleaseFiredFor. (D-11 / D-13 / D-14 / D-15 / D-16 / REVIEW HIGH-3 / REVIEW HIGH-4 / REVIEW LOW-MEDIUM-11 / REVIEW MEDIUM-12)
- [x] **TRACK-15-09**: Server NOTIFICATION_DEFAULTS triad place 1 of 3 — adds `newSeasonAirDate: true` 8th key to queuenight/functions/index.js:74-96. Lockstep contract with client DEFAULT_NOTIFICATION_PREFS (TRACK-15-10). Per Phase 14-09 DR-3, NOT mirrored to friendly-UI maps. (D-09 / D-12 / DR-3)
- [x] **TRACK-15-10**: Client DEFAULT_NOTIFICATION_PREFS triad place 2 of 3 — adds `newSeasonAirDate: true` 8th key. Default-on per D-09. (D-09 / D-12 / DR-3)
- [x] **TRACK-15-11**: Client NOTIFICATION_EVENT_LABELS triad place 3 of 3 — adds entry with REVIEW MEDIUM-12 reworded label "New episode alerts" + hint "When a tracked show drops a new episode." (was "New season air dates" — Codex flagged the original label as overpromising). KEY stays 'newSeasonAirDate' for back-compat. (D-12 / DR-3 / REVIEW MEDIUM-12)
- [x] **TRACK-15-12**: Trakt co-watch overlap detection + S5 prompt — extends trakt.ingestSyncData with `lastWatchedAt` capture; `trakt.detectAndPromptCoWatchOverlap` walks state.titles after every sync. REVIEW MEDIUM-10 single-comparator (cv15EpisodeOrdinal + cv15SelectHigherProgress) — both season AND episode copied from the winning progress object (fixes S5E1-vs-S4E10 mismatch bug). REVIEW MEDIUM-9 durable decline persistence at families/{code}.coWatchPromptDeclined[tupleKey] — declined pairs do NOT re-prompt on subsequent syncs. S5 modal verbatim from UI-SPEC. (D-05 / D-06 / REVIEW MEDIUM-9 / REVIEW MEDIUM-10)
- [x] **TRACK-15-13**: sw.js CACHE constant bumped to `couch-v35.0-tracking-layer` and deployed via the cross-repo 4-step ritual. REQUIREMENTS.md mint + Traceability table updates + ROADMAP.md narrative scrub (REVIEW LOW-narrative-fix: "13 TRACK-15-*" → "14 TRACK-15-*") + REVIEW MEDIUM-9 cross-plan allowlist extension to firestore.rules + changelog.html v35 article + scripts/deploy.sh invocation. (cross-cutting / REVIEW MEDIUM-9 / REVIEW LOW-narrative-fix)
- [x] **TRACK-15-14**: D-07 Trakt opt-in Settings disclosure — `renderTraktCard()` disconnected branch prepends literal `JUMP-START YOUR COUCH'S HISTORY WITH TRAKT` eyebrow + italic Instrument Serif `Optional — tracking works without it.` sub-line above the existing `Already tracking on Trakt?` body and Connect button at app.html:679. Permanent (not dismissable). Verbatim from UI-SPEC §Discretion Q9. (D-07 / Q9)

### Security Hardening (Phase 15.1)

Inserted post-Phase-15 holistic security audit (2026-04-27). Phase 15
shipped with 2 HIGH + 2 MEDIUM open findings — F-OPEN-01 (`t.progress[X].source`
forgeable, breaking the Trakt-overlap trust model), WR-02 (regex injection via
attacker-controlled `memberId`), F-OPEN-02 (`tupleNames` not write-scoped to
participants), WR-03 (no rules-test coverage for `coWatchPromptDeclined`).
Scope locked in `.planning/todos/pending/2026-04-27-phase-15-security-hardening-pass.md`;
evidence in `.planning/phases/15-tracking-layer/15-SECURITY.md` + `15-REVIEW.md`.
5 SEC-15-1-* requirements minted at /gsd-plan-phase 15.1 (2026-04-27). Decimal-phase
insertion (mirrors 03.5 precedent) — not a slot reassignment.

- [x] **SEC-15-1-01**: Per-member isolation on `t.progress` map (closes F-OPEN-01) — title-doc UPDATE rule extended with a 4th sub-rule mirroring the existing `mutedShows` pattern: writes that touch `progress` must use a dotted-path diff whose affected inner keys are `hasOnly([memberId])` (or `[managedMemberId]` when proxying). `writeMemberProgress` (`js/app.js:8407`) and any Trakt-sync ingest callsites refactored from full-map writes to dotted-path writes (`{ [`progress.${meId}`]: ... }`) so the diff exposes a single inner key. Closes the cross-member spoof path that the Trakt overlap detector trusted as authentic Trakt origin. (F-OPEN-01 / 15-SECURITY.md) ✓ 2026-04-27 / 15.1-02
- [x] **SEC-15-1-02**: `memberId` regex anchor + `auth.uid` cross-validation (closes WR-02) — every rule branch that consumes `request.resource.data.memberId` adds two preconditions: (a) `memberId is string && memberId.matches('^m_[A-Za-z0-9_-]+$')` to defang RE2 meta-character injection in the title-doc `actingTupleKey` runtime regex; (b) `validAttribution()` extended to verify `memberId == request.auth.uid` (or `managedMemberId(uid)` for proxy-acted writes) so a member can no longer spoof another member's id. Same anchor applied to `mutedShows.hasOnly([memberId])` and the 5th UPDATE branch family-doc allowlist. (WR-02 / 15-REVIEW.md) ✓ 2026-04-27 / 15.1-02 (anchor lives entirely in this plan; identity-proof via members/{memberId}.uid == uid() deferred to Phase-5 hardening per CONTEXT.md §Deferred Ideas)
- [x] **SEC-15-1-03**: `tupleNames.<tk>` participant check on the family-doc 5th UPDATE branch (closes F-OPEN-02 server-side) — rule peeks into the nested map's affected keys and verifies `actingMemberId in tk.split(',')` for each tupleKey being written, mirroring the title-doc HIGH-1 `actingTupleKey` echo + regex pattern. Requires `setTupleName` (`js/app.js:8517`) to stamp `actingTupleKey: tupleKeyStr` on its dotted-path write. Same enforcement extended to `coWatchPromptDeclined.<tk>` writes (the only other tupleKey-keyed map under that branch). (F-OPEN-02 / 15-SECURITY.md) ✓ 2026-04-27 / 15.1-02
- [x] **SEC-15-1-04**: Rules tests #33-#35 cover the new isolation paths (closes WR-03) — `tests/rules.test.js` extended with #33 (authed member writes own `coWatchPromptDeclined` tuple → ALLOWED), #34 (authed member writes `coWatchPromptDeclined` for tuple they're NOT in → DENIED, gated on SEC-15-1-03), #35 (forged `memberId: '.*'` regex injection writing `tupleProgress` → DENIED, gated on SEC-15-1-02). Plus regression tests for any rule branches changed by SEC-15-1-01..03. Pins the contract and blocks future regressions. (WR-03 / 15-REVIEW.md WR-03 fix block) ✓ 2026-04-27 / 15.1-02 (shipped 6 tests #33-#38 — 3 originally specced + 3 progress-isolation regression tests; 38/38 passing)
- [x] **SEC-15-1-05**: Defense-in-depth UI surfacing for tupleNames (defense for F-OPEN-02 client-side) — TWO changes: (a) gate the `.cv15-tuple-rename` pencil glyph in `cv15ShowRenameInput` + `renderCv15TupleProgressSection` so it only renders when `state.me.id ∈ tk.split(',')` (~10 lines client-only, closes the casual-griefing path even before SEC-15-1-03 lands); (b) surface the existing `setBy` field as a subtle "Renamed by {memberName}" attribution beside the tuple name on hover/tap — turns silent vandalism into an attributable action. Both per the 15-SECURITY.md F-OPEN-02 §Recommendation. (F-OPEN-02 / 15-SECURITY.md) ✓ 2026-04-27 / 15.1-01

## v2 Requirements

Deferred to post-v1. Tracked but not in current roadmap.

### Modularization

- **MOD-01**: Split `index.html` into bundled modules with a build step
- **MOD-02**: FlutterFlow port of the app for native distribution

### Monetization

- **PAY-01**: Plan tiers (free / paid) with feature gating
- **PAY-02**: Billing integration and subscription management
- **PAY-03**: Per-family account ownership and plan assignment

### Social / Public

- **SOC-01**: Public discovery surface (browse other families' public lists)
- **SOC-02**: Cross-family sharing (recommend a title to another family)

### Watchparty + sports v2 (Phase 24+)

Promoted to ROADMAP from `seeds/v2-watchparty-sports-milestone.md` 2026-04-30. v2 milestone scope locks 6 phases: 22 (sports feed abstraction — shipped) + 23 (live scoreboard — shipped) + 24 (native player) + 26 (position-anchored reactions) + 27 (guest RSVP) + 28 (social pick'em) + 30 (couch groups + affiliate hooks). Phase 24's 18 VID-24-* requirements were locked via the `/gsd-discuss-phase 24` → `/gsd-research-phase 24` → `/gsd-ui-phase 24` → `/gsd-plan-phase 24` → `/gsd-review --phase 24 --all` → `/gsd-plan-phase 24 --reviews` chain on 2026-04-30. Provisional ID format mirrors the project's existing per-phase prefix (cf. REQ-18-*, KID-19-*, REQ-20-*).

- [x] **VID-24-01**: YouTube iframe player branch surfaces inside watchparty live modal when `wp.videoUrl` is set + title is not DRM-restricted (uses div-placeholder + `videoId`-param `new YT.Player()` binding per REVIEWS H3)
- **VID-24-02**: `parseVideoUrl(raw)` helper in `js/native-video-player.js` parses 5 YouTube URL shapes (`youtube.com/watch?v=`, `youtu.be/`, `youtube.com/shorts/`, `youtube.com/embed/`, `m.youtube.com/`) and rejects malformed / `javascript:` / `data:` / unknown-host URLs
- [x] **VID-24-03**: HTML5 `<video>` player branch surfaces inside watchparty live modal for MP4 sources with `playsinline` for iOS PWA
- **VID-24-04**: `parseVideoUrl` detects `.mp4` extension and classifies as `source: 'mp4'`
- [x] **VID-24-05**: `titleHasNonDrmPath(t)` helper returns false only when ALL providers are flat-rate DRM brands AND no rent/buy paths exist; gates the player surface render
- [x] **VID-24-06**: Optional Video URL field on movie watchparty schedule modal (`#wp-video-url-movie` in `confirmStartWatchparty` flow)
- [x] **VID-24-07**: Optional Video URL field on game / sport watchparty schedule modals (`#wp-video-url-game` in `confirmGamePicker` modern flow + `#wp-video-url-sport` in `scheduleSportsWatchparty` legacy flow per REVIEWS M3)
- [x] **VID-24-08**: `wp.videoUrl` + `wp.videoSource` schema fields stored on watchparty record at creation
- [x] **VID-24-09**: Host-only `currentTime` broadcast writes throttled at 5s cadence per `VIDEO_BROADCAST_INTERVAL_MS`; cadence well under Firestore quotas (~720 writes/hr/wp)
- **VID-24-10**: `makeIntervalBroadcaster(interval, fn)` helper in `js/native-video-player.js` provides leading-edge + trailing-edge throttle pattern
- [x] **VID-24-11**: iOS PWA `playsinline` attribute / URL-param honored on both branches (YouTube `?playsinline=1&enablejsapi=1`; HTML5 `<video playsinline>`)
- **VID-24-12**: Smoke contract `scripts/smoke-native-video-player.cjs` (≥28 assertions) imports the production module via `await import('../js/native-video-player.js')` per REVIEWS H2; wired into `scripts/deploy.sh` §2.5 + `package.json` smoke chain
- **VID-24-13**: `sw.js` CACHE bumped to `couch-v37-native-video-player` on Phase 24 ship via `bash scripts/deploy.sh 37-native-video-player`
- [x] **VID-24-14**: `renderPlayerErrorOverlay()` renders inline italic-serif "Player couldn't load that link." + delegated `[data-action="retry-video"]` "Try again" link wired to `reloadWatchpartyPlayer()` per REVIEWS H4
- [x] **VID-24-15**: `seekToBroadcastedTime(player, wp, source)` helper seeks non-host participants to `wp.currentTimeMs` on player init for late-join coordination per REVIEWS M1
- [x] **VID-24-16**: `#wp-live-content` in `app.html` split into persistent `#wp-video-surface` (player container, untouched by re-renders) + re-renderable `#wp-live-coordination` sibling per REVIEWS H1; `renderWatchpartyLive` retargets only the coordination surface
- [x] **VID-24-17**: Submit-time `flashToast("Note: HTTP links may be blocked by the browser. Use https if available.")` on `http://` MP4 URL submit per REVIEWS C1 (consensus across reviewers)
- **VID-24-18**: Firestore rules verification — 5 rules-tests in `tests/rules.test.js` covering host-write success + non-host-write spoof rejection on `wp.currentTimeMs`; conditional `firestore.rules` tightening + cross-repo deploy reminder for `queuenight/` mirror per REVIEWS M2

## Out of Scope

| Feature | Reason |
|---------|--------|
| Modularization / bundler / framework migration | Single-file `index.html` is deliberate for v1 — ease of deploy, iteration speed; deferred to v2 |
| Native rewrites (FlutterFlow, React Native, Swift) | FlutterFlow port planned post-v1; this milestone stays single-file web |
| Monetization wiring (paywall, billing, plan tiers) | "Free, figure out later" — no billing logic in this milestone |
| Public discovery / cross-family social feed | Couch is family-scoped by design for v1 |
| Moving TMDB key / Firebase config server-side | Public-by-design; no security benefit, adds ops cost |
| Public onboarding funnel / landing conversion work | Invite-driven growth through v1 |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| MOOD-01 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-02 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-03 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-04 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-05 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-06 | Phase 3 | Complete ✓ 2026-04-20 |
| MOOD-07 | Phase 3 | Complete ✓ 2026-04-20 |
| VETO-01 | Phase 4 | Complete ✓ 2026-04-20 (04-01) |
| VETO-02 | Phase 4 | Complete ✓ 2026-04-20 (04-02) |
| VETO-03 | Phase 4 | Complete ✓ 2026-04-20 (04-03) |
| VETO-04 | Phase 4 | Complete ✓ 2026-04-20 (04-01) |
| VETO-05 | Phase 4 | Complete ✓ 2026-04-20 (04-01) |
| VETO-06 | Phase 4 | Complete ✓ 2026-04-20 (04-02) |
| AUTH-01 | Phase 5 / 05-01 + 05-02 + 05-06 | Complete (couchtonight.app sign-in live; F-W-5; backfilled by Phase 15.2 / 15.2-01) |
| AUTH-02 | Phase 5 / 05-03 + 05-04 + 05-05 + 05-06 + 05-07 | Complete (`writeAttribution` × 109 call sites; CFs live in us-central1; backfilled by Phase 15.2 / 15.2-01) |
| AUTH-03 | Phase 5 / 05-03 + 05-04 + 05-07 | Complete (`setGroupPassword` + `joinGroup` CFs live; backfilled by Phase 15.2 / 15.2-01) |
| AUTH-04 | Phase 5 / 05-03 + 05-04 + 05-08 | Complete (`claimMember` + `mintClaimTokens` CFs live; backfilled by Phase 15.2 / 15.2-01) |
| AUTH-05 | Phase 5 / 05-04 + 05-05 + 05-08 + 05-09 | Complete — UAT-DEFERRED (3 E2E flows confirm post-migration regression-clean per audit; backfilled by Phase 15.2 / 15.2-01) |
| PUSH-01 | Phase 6 / 06-01 | Complete (research spike — `06-RESEARCH.md` artifact; web-push recommendation adopted; backfilled by Phase 15.2 / 15.2-02) |
| PUSH-02 | Phase 6 / 06-02 + 06-03 | Complete (per-event opt-in via notificationPrefs map; UI surface in 12-01; backfilled by Phase 15.2 / 15.2-02) |
| PUSH-03 | Phase 6 / 06-03 + 06-04 | Complete (watchparty-starting push live; backfilled by Phase 15.2 / 15.2-02) |
| PUSH-04 | Phase 6 / 06-02 + 06-05 | Complete (`isInQuietHours` at queuenight/functions/index.js:45; backfilled by Phase 15.2 / 15.2-02) |
| PUSH-05 | Phase 6 / 06-04 | Complete (self-echo guard via `excludeUid` + `excludeMemberId`; backfilled by Phase 15.2 / 15.2-02) |
| PARTY-01 | Phase 7 / 07-02 | Complete ("Watch together" CTA wired; backfilled by Phase 15.2 / 15.2-03) |
| PARTY-02 | Phase 7 / 07-01 + 07-03 | Complete (wp lifecycle + push invite; backfilled by Phase 15.2 / 15.2-03) |
| PARTY-03 | Phase 7 | Complete (07-03 + 07-08) |
| PARTY-04 | Phase 7 / 07-03 + 07-06 + 07-07 | Complete (SUMMARYs 07-06 + 07-07 list PARTY-04 in `requirements_completed`; backfilled by Phase 15.2 / 15.2-03) |
| PARTY-05 | Phase 7 / 07-03 | Complete (reactions array wired; backfilled by Phase 15.2 / 15.2-03) |
| PARTY-06 | Phase 7 / 07-01 + 07-05 | Complete (SUMMARY 07-05 lists PARTY-06; commit 8ac8c9b; backfilled by Phase 15.2 / 15.2-03) |
| PARTY-07 | Phase 7 / 07-02 | Complete (Tonight banner wired; backfilled by Phase 15.2 / 15.2-03) |
| INTENT-01 | Phase 8 / 08-01 | Complete (`intentsRef` + `onIntentCreated` CF; backfilled by Phase 15.2 / 15.2-04) |
| INTENT-02 | Phase 8 / 08-02 | Complete (`flow:'watch_this_title'` discriminator; backfilled by Phase 15.2 / 15.2-04) |
| INTENT-03 | Phase 8 / 08-03 | Complete (`convertIntent` routes; backfilled by Phase 15.2 / 15.2-04) |
| INTENT-04 | Phase 8 / 08-03 + 08-04 | Complete (group-size thresholds in CF onIntentUpdate; backfilled by Phase 15.2 / 15.2-04) |
| INTENT-05 | Phase 8 / 08-01 + 08-04 | Complete (orphan-doc cleanup; backfilled by Phase 15.2 / 15.2-04) |
| INTENT-06 | Phase 8 / 08-04 | Complete (per-member rsvps shape persists; backfilled by Phase 15.2 / 15.2-04) |
| DESIGN-01 | Phase 9 → Phase 15.3 (gap closure) | Pending — assigned to Phase 15.3 (canonical SVG logo + wordmark production) per v33.3 audit |
| DESIGN-02 | Phase 9 | Complete |
| DESIGN-03 | Phase 9 | Complete |
| DESIGN-04 | Phase 9 | Complete |
| DESIGN-05 | Phase 9 | Complete |
| DESIGN-06 | Phase 9 | Complete |
| DESIGN-07 | Phase 9 | Complete |
| DESIGN-08 | Phase 9 | Complete |
| DESIGN-09 | Phase 9 | Complete |
| DESIGN-10 | Phase 9 | Complete |
| YEAR-01 | Phase 10 | Pending |
| YEAR-02 | Phase 10 | Pending |
| YEAR-03 | Phase 10 | Pending |
| YEAR-04 | Phase 10 | Pending |
| YEAR-05 | Phase 10 | Pending |
| REFR-01 | Phase 11 Plan 01 | Complete 2026-04-24 |
| REFR-02 | Phase 11 Plan 01 | Complete 2026-04-24 |
| REFR-03 | Phase 11 Plan 01 | Complete 2026-04-24 |
| REFR-04 | Phase 11 Plans 03a + 03b | Complete 2026-04-24 |
| REFR-05 | Phase 11 Plan 04 | Complete (code) 2026-04-24 |
| REFR-06 | Phase 11 Plan 04 | Complete (code) 2026-04-24 |
| REFR-07 | Phase 11 Plan 05 | Complete (code) 2026-04-24 |
| REFR-08 | Phase 11 Plan 05 | Complete (code) 2026-04-24 |
| REFR-09 | Phase 11 Plan 05 | Complete (code) 2026-04-24 |
| REFR-10 | Phase 11 Plan 06 | Complete (code) 2026-04-24 |
| REFR-11 | Phase 11 Plan 02 | Complete 2026-04-24 |
| REFR-12 | Phase 11 Plan 02 | Complete 2026-04-24 |
| REFR-13 | Phase 11 Plan 07 | Complete (code) 2026-04-24 |
| POL-01 | Phase 12 / 12-01 | Complete (acceptance met — 6 toggles + quiet hours shipped) — partial-parity-with-Phase-14-D-3 closed by Phase 15.4 (commit b1ab9a9; backfilled by Phase 15.2 / 15.2-06) |
| POL-02 | Phase 12 / 12-02 | Complete (commit 6323530; backfilled by Phase 15.2 / 15.2-06) |
| POL-03 | Phase 12 / 12-03 | Complete (commit 004255b; backfilled by Phase 15.2 / 15.2-06) |
| POL-04 | Phase 12 / 12-03 | Complete — stretch goal landed (commit 004255b; 14 mismatches corrected; backfilled by Phase 15.2 / 15.2-06) |
| COMP-13-01 | Phase 13 / 13-01 | Complete — HUMAN-VERIFY pending (commits ccd3eec + 4fc49ee + 6665648; 4 CFs live in us-central1; backfilled by Phase 15.2 / 15.2-06) |
| OPS-13-02 | Phase 13 / 13-03 | Complete (commits 7df42a2 + 45a9da7 + cc8b784; backfilled by Phase 15.2 / 15.2-06) |
| OPS-13-04 | Phase 13 / 13-05 | Complete — TD-4 audit window active (commits 5149c7d + 2a8ceed; CSP Report-Only live; backfilled by Phase 15.2 / 15.2-06) |
| OPS-13-05 | Phase 13 / 13-02 | Complete (commits 19f9f6a + 7b9aa5e + bd2890f + e4f2c89; backfilled by Phase 15.2 / 15.2-06) |
| OPS-13-06 | Phase 13 / 13-03 | Complete (branch protection live; repo public per RUNBOOK §L; backfilled by Phase 15.2 / 15.2-06) |
| OPS-13-07 | Phase 13 / 13-04 | Complete — HUMAN-VERIFY pending gcloud auth (commits 4813bf2 + 69a9ed0; backfilled by Phase 15.2 / 15.2-06) |
| DECI-14-01 | Phase 14 / 14-01 | Complete (commits 8ddb347 + 5a9ea75; js/app.js:7647; backfilled by Phase 15.2 / 15.2-06) |
| DECI-14-02 | Phase 14 Plan 03 | Complete (2026-04-26; commit ccf600f; pure data layer — 4 functions getTierOneRanked / getTierTwoRanked / getTierThreeRanked / resolveT3Visibility added after getGroupNext3 at js/app.js:7006-7106; +102 lines; UI consumer 14-07 must read resolveT3Visibility() to gate T3 expand affordance) |
| DECI-14-03 | Phase 14 Plan 02 | Complete — UAT-DEFERRED (2026-04-25; commits 2e1ca4b + 2b670d2; iOS Safari touch-DnD UAT deferred to batch UAT pass — must run before v34 production deploy; Sortable.js fallback documented) |
| DECI-14-04 | Phase 14 Plan 05 | Complete (2026-04-26; commits f90155d + d1762e1 + 09db482 + 6d630c4; Tasks 1-4 shipped — card(t) X-want-it pill + Trailer btn + body→openTileActionSheet rewire + Vote primaryBtn removed at js/app.js:4333-4486; window.openTileActionSheet sibling primitive inserted ABOVE the existing window.openActionSheet at js/app.js:11853 with existing primitive UNCHANGED; detail-modal TMDB community reviews lazy-fetch + render with per-title cache + escapeHtml + 360-char truncate; D-05 Catch-up-on-votes CTA at top of Add-tab innerHTML when unvotedCount >= 10; Task 5 carry-over UAT bug CLOSED by pre-existing CSS fix at css/app.css:1540 — NOT deferred — global .detail-close{position:fixed} rule covers all entry surfaces (Library/Queue / Tonight / Add tab) + intentional .avatar-picker-modal exception at css/app.css:2380; soft iOS Safari render confirmation rolled into existing 14-02 iOS Safari follow-up entry as non-blocking) |
| DECI-14-05 | Phase 14 Plan 05 | Complete (2026-04-26; commit 6d630c4; D-05 Vote-mode preservation in Add tab — unvotedCount conditional Catch-up-on-votes CTA renders when ≥10 unvoted titles exist; tap launches existing openVoteModal(); .add-catchup-cta CSS with warm-amber border + token-aligned background) |
| DECI-14-06 | Phase 14 Plan 04 | Complete — UAT-DEFERRED (2026-04-26; commits 1234624 + 3a561bd + cb31155; Tasks 1-4 shipped — sketch 001 winner P hero-icon + avatar-grid pattern with renderCouchViz + claim handlers + persistCouchSeating in js/app.js + #couch-viz-container in app.html + ~140-line D-06 CSS block in css/app.css + firestore.rules Case A third UPDATE branch + 4 new sibling tests 22/22 passing; Task 5 visual + iOS PWA UAT deferred to batch UAT pass — must run before v34 production deploy; recap in 14-04-SUMMARY.md; `firebase deploy --only firestore:rules` from queuenight/ also required before v34 production deploy) |
| DECI-14-07 | Phase 14 Plan 07 | Complete — UAT-DEFERRED (2026-04-26; commit d97a81e; Tasks 1-4 shipped in one coupled commit — Flow A entry CTA + picker 3-tier with T3 collapse gated by resolveT3Visibility() + roster proxy-confirm + send-picks calling createIntent({flow:'rank-pick'}) from 14-06 + recipient response In/Reject/Drop + counter-nom sub-flow with counterChainDepth 3-cap + reject-majority retry with state.flowARejectedTitles Set + quorum convert creating watchparty doc with convertedFromIntentId + opens Phase 11-05 lobby; Task 5 multi-device UAT deferred to batch UAT pass — must run before v34 production deploy; recap in 14-07-SUMMARY.md; bundled with 14-08 multi-device Flow B UAT entry) |
| DECI-14-08 | Phase 14 Plan 08 | Complete — UAT-DEFERRED (2026-04-26; commit af168f1; Tasks 1-4 shipped — openFlowBNominate UI + recipient response + nominator status screen + counter-decision UI + ?intent= deep-link routing; Task 5 multi-device UAT deferred to batch UAT pass — must run before v34 production deploy; recap in 14-08-SUMMARY.md) |
| DECI-14-09 | Phase 14 Plan 06 | Complete (2026-04-25; commits 3c6b4b9 + 9a09872; queuenight CF edits uncommitted — see 14-06-SUMMARY.md deviation 1) |
| DECI-14-10 | Phase 14 Plan 09 | Complete — DEPLOY-DEFERRED (2026-04-26; commits 8a36336 + ae7f0c0; D-10 hybrid onboarding shipped — showTooltipAt + hideTooltip primitive in js/utils.js with .coach-tip CSS in css/app.css; maybeShowTooltip gate near maybeShowFirstRunOnboarding; 3 callsites — couchSeating in renderCouchViz / tileActionSheet in openTileActionSheet / queueDragReorder in renderFullQueue; firestore.rules audit confirmed members/{id} update branch is permissive — seenTooltips writes pass without rules change; guests skip; recap in 14-09-SUMMARY.md) |
| DECI-14-11 | Phase 14 Plan 09 | Complete — DEPLOY-DEFERRED (2026-04-26; commit fdcb9dc; 5 D-11 empty states shipped — (a) brand-new family "Your couch is fresh" + Add tab + Trakt connect CTAs, (b) empty personal queue "Your queue is empty" + Open Vote mode CTA, (c) Flow A no-couch "Who's on the couch tonight?" + Find a seat CTA + cushion-glow pulse animation, (d) all-watched "You've seen everything in queue" + Show rewatch options (state.flowARevealRewatch flag + tier aggregator includeWatched opt) + Discover more, (e) reject-majority no-#2 "No alternative pick" + Cancel for tonight + Open Flow B handing off titleId; recap in 14-09-SUMMARY.md) |
| DECI-14-12 | Phase 14 Plan 09 | Complete in legacy NOTIFICATION_EVENT_LABELS list per DR-3 follow-up override; friendly-UI parity captured as polish item — DEPLOY-DEFERRED (2026-04-26; commit e572467 + queuenight uncommitted; 7 D-12 push categories added in THREE places per DR-3 — server NOTIFICATION_DEFAULTS in queuenight/functions/index.js (uncommitted on this machine — same pattern as 14-06 deviation 1) + client DEFAULT_NOTIFICATION_PREFS at js/app.js:100 + client NOTIFICATION_EVENT_LABELS at js/app.js:113 with BRAND-voice copy; all 7 default ON per RESEARCH §5; DR-3 follow-up override applied — friendly-UI maps NOT mirrored to avoid dual-Settings-screen collision RESEARCH §5 flagged; friendly-UI parity captured as polish backlog; recap in 14-09-SUMMARY.md) |
| DECI-14-13 | Phase 14 Plan 09 | Complete — DEPLOY-DEFERRED (2026-04-26; commit 9132144; sw.js CACHE bumped from couch-v33.3-sentry-dsn to couch-v34.0-decision-ritual + changelog.html v34 release article inserted ABOVE existing v32 article in reverse-chronological order with BRAND-voice tagline + 4 highlight items per CONTEXT.md D-10 / PATTERNS §16; deploy date placeholder pending Task 7 deploy; recap in 14-09-SUMMARY.md) |
| TRACK-15-01 | Phase 15 Plan 01 | Complete |
| TRACK-15-02 | Phase 15 Plan 02 | Complete |
| TRACK-15-03 | Phase 15 Plan 03 | Complete |
| TRACK-15-04 | Phase 15 Plan 04 | Complete |
| TRACK-15-05 | Phase 15 Plan 04 | Complete |
| TRACK-15-06 | Phase 15 Plan 04 | Complete |
| TRACK-15-07 | Phase 15 Plan 05 | Complete |
| TRACK-15-08 | Phase 15 Plan 06 | Complete |
| TRACK-15-09 | Phase 15 Plan 06 | Complete |
| TRACK-15-10 | Phase 15 Plan 07 | Complete |
| TRACK-15-11 | Phase 15 Plan 07 | Complete |
| TRACK-15-12 | Phase 15 Plan 07 | Complete |
| TRACK-15-13 | Phase 15 Plan 08 | Complete |
| TRACK-15-14 | Phase 15 Plan 07 | Complete |
| SEC-15-1-01 | Phase 15.1-01 + 15.1-02 | Complete |
| SEC-15-1-02 | Phase 15.1-02 | Complete |
| SEC-15-1-03 | Phase 15.1-01 + 15.1-02 | Complete |
| SEC-15-1-04 | Phase 15.1-02 | Complete |
| SEC-15-1-05 | Phase 15.1-01 | Complete |
| REQ-18-01 | Phase 18 / 18-01 + 18-04 | Complete — UAT-DEFERRED (daily `providerRefreshTick` scheduled CF live in us-central1 since 2026-04-29 22:48Z; queuenight commit `d622dc6` + couch deploy `adcebfc`; smoke contract regression-locks helpers via 18-03 commits `c5fb713` + `fac3d71`; UAT 4 scripts pending real-device verification per 18-HUMAN-UAT.md; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-02 | Phase 18 / 18-01 + 18-04 | Complete — UAT-DEFERRED (TMDB watch/providers re-fetch + `addedBrandsFor` diff + push fan-out via `sendToMembers` eventType-gate live; queuenight commit `d622dc6`; couch deploy `adcebfc` 2026-04-29 22:50Z; UAT push-delivery test pending real-device verification; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-03 | Phase 18 / 18-02 | Complete (manual refresh affordance — '↻ Refresh availability' button label + `lastProviderRefreshAt: Date.now()` write + `flashToast('Availability refreshed.')` + 'Provider data via TMDB' italic attribution footnote shipped at js/app.js:7531-7539; couch commit `416ab32` 2026-04-29; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-04 | Phase 18 / 18-01 + 18-02 | Complete (`titleAvailable` push event-type registered in 6 maps lockstep — server NOTIFICATION_DEFAULTS at queuenight commit `d622dc6` + client DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS + NOTIF_UI_TO_SERVER_KEY + NOTIF_UI_LABELS + NOTIF_UI_DEFAULTS at couch commit `5a86045` + `7f04d2f`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-05 | Phase 18 / 18-01 + 18-02 | Complete (per-event opt-out via existing `sendToMembers` eventType-gate — Phase 6 contract reused; client toggle parity in 5 maps via couch commits `5a86045` + `7f04d2f`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-06 | Phase 18 / 18-01 | Complete (push fan-out to members whose `m.services` intersects newly-added brands — system push, no sender, self-echo guard logically n/a; queuenight commit `d622dc6`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-07 | Phase 18 / 18-01 | Complete (quiet-hours respected via existing `isInQuietHours` helper at queuenight/functions/index.js:45 — Phase 6 baseline reused; queuenight commit `d622dc6`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-08 | Phase 18 / 18-01 | Complete (TMDB rate-limit handling — 429 detection in providerRefreshTick aborts remainder of tick + Sentry breadcrumb; queuenight commit `d622dc6`; soak verification pending per 18-HUMAN-UAT.md; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-09 | Phase 18 / 18-04 | Complete (sw.js CACHE bumped to `couch-v36-availability-notifs`; couch commit `adcebfc`; live curl-verified post-deploy 2026-04-29 22:50Z; backfilled by Phase 15.6 / 15.6-01) |
| REQ-18-10 | Phase 18 / 18-04 | Complete (cross-repo deploy ordering — queuenight functions deployed 22:48Z THEN couch hosting 22:50Z per RUNBOOK §H + Phase 15.4/15.5 precedent; 2-minute window between, ordering preserved; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-01 | Phase 19 / 19-01 | Complete (Kid-mode toggle visible on V5 roster — pill-style button below action row, dashed-border idle / amber-filled active per BRAND.md `--color-amber`; visibility gate: ANY family member has `effectiveMaxTier ≤ 3`; couch commit `0ce1e59` 2026-04-29; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-02 | Phase 19 / 19-01 | Complete (session-scoped toggle — `state.kidMode` default false, never persisted to Firestore or localStorage; resets on `showScreen()` away from Tonight + on `couchClearAll`; couch commit `2930ff8`; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-03 | Phase 19 / 19-01 | Complete (tier cap ≤ TIER_PG (2) when active — `getEffectiveTierCap()` helper applied at 7 filter call sites: passesBaseFilter / getCurrentMatches / getNeedsVoteTitles / getTierOneRanked / getTierTwoRanked / getTierThreeRanked / Library 'unwatched' + 'forme'; vetoes + already-watched still hidden per D-18/D-19; couch commit `2930ff8`; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-04 | Phase 19 / 19-01 | Complete (toggle visibility gate — hidden entirely when zero family members have `effectiveMaxTier ≤ 3`; re-evaluated on every `renderCouchViz` call per D-02 + D-03; couch commit `0ce1e59`; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-05 | Phase 19 / 19-01 | Complete (Library filter respects kid-mode — 'unwatched' + 'forme' filters apply tier-cap when active; provider filter still applies per D-20; couch commit `2930ff8`; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-06 | Phase 19 / 19-01 | Complete (visual amber tint on V5 hero icon area when active — subtle 0.05 opacity overlay per BRAND.md warm-restraint principle; couch commit `0ce1e59`; smoke-kid-mode.cjs (23 assertions) regression-locks behavior via couch commit `1585208`; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-07 | Phase 19 / 19-02 | Complete (per-title parent override — 'Show this anyway for tonight' link in detail modal renders only when `state.kidMode` AND tier > 2 AND `isCurrentUserParent()` returns true; tap adds titleId to `state.kidModeOverrides` Set + flashToast 'Showing this for tonight' + Sentry breadcrumb category=kid-mode-override; per-title session-scoped, clears with kid-mode itself; couch commit `3e3b7d4` 2026-04-29; backfilled by Phase 15.6 / 15.6-01) |
| KID-19-08 | Phase 19 / 19-03 | Complete (sw.js CACHE bumped to `couch-v36.1-kid-mode`; couch-only single-repo deploy via `bash scripts/deploy.sh 36.1-kid-mode` 2026-04-29 23:39Z; smoke gate 4 contracts / 92 assertions PASS + 43 rules tests PASS + curl-verified live; couch commit `59c3fe6`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-01 | Phase 20 / 20-01 | Complete (`buildMatchExplanation(t, couchMemberIds, opts)` is a pure function at js/app.js:112 — verifier confirmed mutation scan = 0; couch commit `9b2c1f7`; smoke A1-A2 pass via commit `98358b8`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-02 | Phase 20 / 20-01 | Complete (voter phrase covers 1 / 2 / 3+ / all-of-couch cases per D-03 — `"{Name} said yes"` / `"{Name1} + {Name2} said yes"` / `"All of you said yes"` / `"{count} of you said yes"`; smoke A3-A6 pass; couch commit `9b2c1f7`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-03 | Phase 20 / 20-01 | Complete (provider phrase prefers `m.services` intersection brand; falls back to `"Streaming on {firstProvider}"` when no intersection; omitted when `t.providers` empty per D-04; smoke A7-A9 pass; couch commit `9b2c1f7`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-04 | Phase 20 / 20-01 | Complete (runtime phrase formats ≥60 min as `"{H} hr {M} min"` (or `"{H} hr"` when M==0); else `"{M} min"`; skipped when t.runtime is null OR 0 per D-05 + RESEARCH Pitfall 5; smoke A10-A14 pass; couch commit `9b2c1f7`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-05 | Phase 20 / 20-01 | Complete (output capped at 3 phrases joined with ` · ` middle-dot separator per D-02 + D-06; drop priority on overflow voters > provider > runtime; smoke A13-A14 pass; couch commit `9b2c1f7`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-06 | Phase 20 / 20-01 | Complete (considerable variant uses `"Some of you said yes"` for 1-of-N case per D-10; smoke A15 pass; couch commit `9b2c1f7`; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-07 | Phase 20 / 20-02 | Complete — UAT-DEFERRED (spin-pick result modal renders explanation as italic Instrument Serif sub-line below picked title's name per D-07; visible always, no tap-to-reveal; verifier cited js/app.js:8165-8173 + css/app.css:1652-1661; couch commit `783c946` 2026-04-30; device UAT pending per 20-HUMAN-UAT.md; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-08 | Phase 20 / 20-02 | Complete — UAT-DEFERRED (Tonight matches list renders dim-text footer on every match card under tile content per D-08; considerable variant via `{ considerableVariant: true }` flag plumbed through renderTonight; verifier cited js/app.js:5541-5559 + css/app.css:1509-1514; couch commit `62bb033` 2026-04-30; device UAT pending; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-09 | Phase 20 / 20-02 | Complete — UAT-DEFERRED (detail modal 'Why this is in your matches' section renders ONLY when `t.id` is currently in matches list per D-09; section omitted when title opened from Library, History, or considerable list; verifier cited js/app.js:7752-7780 + css/app.css:1581-1601; couch commit `81113b5` 2026-04-30; device UAT pending; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-10 | Phase 20 / 20-03 | Complete (sw.js CACHE bumped to `couch-v36.2-decision-explanation` per D-17; verifier cited sw.js:8 with old cache absent; couch commit `1c02ea0` 2026-04-30; live curl-verified post-deploy; backfilled by Phase 15.6 / 15.6-01) |
| REQ-20-11 | Phase 20 / 20-01 + 20-03 | Complete (smoke contract `scripts/smoke-decision-explanation.cjs` (15 assertions A1-A15) wired into deploy.sh §2.5 5th smoke if-block; package.json chains all 5 contracts; `npm run smoke` exit 0 with all 5 contracts green; couch commits `cc0bb6f` (deploy.sh wiring) + `1c02ea0` (sw.js + final gate); backfilled by Phase 15.6 / 15.6-01) |
| VID-24-01 | Phase 24 / 24-03 | Complete — YouTube iframe player branch shipped via `attachVideoPlayer` at js/app.js:11119 with REVIEWS H3 div-placeholder pattern (`<div id="wp-yt-player" data-video-id="${encodeURIComponent(parsed.id)}">` + `new YT.Player('wp-yt-player', { videoId, playerVars: { playsinline: 1, enablejsapi: 1 } })`). YT.Player binds via videoId param, NOT iframe src interpolation. Gate: `wp.videoUrl && wp.videoSource && titleHasNonDrmPath(t)`. Surface attaches into persistent `#wp-video-surface` (REVIEWS H1) so coordination repaints don't tear it down. couch commit `90de0ad` 2026-04-30 |
| VID-24-02 | Phase 24 / 24-02 | Complete — `parseVideoUrl(input)` shipped in `js/native-video-player.js` (5 YT shapes via host=youtube.com /watch+/shorts+/embed paths + youtu.be host + m. host-prefix strip; protocol allowlist `if (u.protocol !== 'https:' && u.protocol !== 'http:') return null`; bounded-regex `/^[A-Za-z0-9_-]{6,}$/` for IDs; URL constructor pre-rejects malformed); 14 inline behavioral assertions verified (5 YT shapes + 7 rejection cases incl. javascript:/data:/file:/empty/null/malformed/unknown-host); couch commit `9c1f511` 2026-05-01 |
| VID-24-03 | Phase 24 / 24-03 | Complete — HTML5 `<video>` MP4 branch shipped via `attachVideoPlayer` at js/app.js:11119: `<video id="wp-mp4-player" controls playsinline preload="metadata" src="${escapeHtml(wp.videoUrl)}">`. iOS PWA `playsinline` attribute prevents fullscreen-yank. Wired with REVIEWS H5 named handler refs (`_wpVideoTimeHandler` + `_wpVideoErrorHandler`) for clean teardown via `removeEventListener`. couch commit `90de0ad` 2026-04-30 |
| VID-24-04 | Phase 24 / 24-02 | Complete — `parseVideoUrl` `.mp4` detection branch (`if (/\.mp4$/i.test(u.pathname))` returns `{ source: 'mp4', url: trimmed }`) shipped in `js/native-video-player.js`; query-string allowed (Plex/Jellyfin auth tokens); 2 inline assertions verified (plain `https://example.com/movie.mp4` + auth-token `https://plex.local:32400/library/x.mp4?token=abc`); couch commit `9c1f511` 2026-05-01 |
| VID-24-05 | Phase 24 / 24-02 + 24-03 | Complete — `titleHasNonDrmPath(t)` shipped in `js/native-video-player.js` per D-03 detection threshold (couch commit `9c1f511` 2026-05-01); render-gate wired in Plan 24-03 at two sites: (1) renderWatchpartyLive computes `headerExtraClass = ' wp-live-header--has-player'` only when `wp.videoUrl && wp.videoSource && titleHasNonDrmPath(t)` (js/app.js:11434-11440); (2) attachVideoPlayer bails early when `!titleHasNonDrmPath(t)` so DRM-only titles never get a player surface (js/app.js:11119). Surface stays empty + collapses via `.wp-video-surface:empty{display:none}` CSS rule when DRM-restricted. couch commits `9c1f511` (helper) + `f58f186` (renderer gate) + `90de0ad` (attach gate) 2026-04-30 to 2026-05-01 |
| VID-24-06 | Phase 24 / 24-03 | Complete — `<input type="url" id="wp-video-url-movie">` shipped in app.html inside `#wp-start-modal-bg` with `.field-help` + `.field-error` siblings; `confirmStartWatchparty` reads via `readAndValidateVideoUrl('movie')` (js/app.js:10908) and persists `videoUrl` + `videoSource` on the wp record. couch commits `44d55a7` (HTML field) + `c6de53e` (JS validation + persist) 2026-04-30 |
| VID-24-07 | Phase 24 / 24-03 | Complete — Distinct flow-prefixed IDs per REVIEWS M3: `#wp-video-url-game` (modern Game Mode in `#game-picker-modal-bg`) + `#wp-video-url-sport` (legacy sports in `#sports-picker-bg`). Each flow validates via shared `readAndValidateVideoUrl(flow)` helper reading `'wp-video-url-' + flow` template; bails on invalid + non-empty input with `.field-invalid` + unhide error div. couch commits `44d55a7` (HTML fields) + `c6de53e` (JS validation + persist in confirmGamePicker:10423 + scheduleSportsWatchparty:10231) 2026-04-30 |
| VID-24-08 | Phase 24 / 24-03 | Complete — `wp.videoUrl` + `wp.videoSource` fields persisted on wp record at creation across all 3 flows (movie + modern game + legacy sports) via `videoUrl: parsedVideoUrl ? parsedVideoUrl.url : null, videoSource: parsedVideoUrl ? parsedVideoUrl.source : null`. Existing setDoc paths under host-write rules accept the new fields per RESEARCH Assumption A3 (Firestore rules at queuenight already allow attributedWrite for any wp field). couch commit `c6de53e` 2026-04-30 |
| VID-24-09 | Phase 24 / 24-02 + 24-03 | Complete — Host-only currentTime broadcast at 5s cadence (`VIDEO_BROADCAST_INTERVAL_MS = 5000` constant from js/native-video-player.js) wired in `attachVideoPlayer` at js/app.js:11119. YT branch arms `_wpVideoBroadcaster = makeIntervalBroadcaster(VIDEO_BROADCAST_INTERVAL_MS, sec => broadcastCurrentTime(...))` + 2.5s sampler `setInterval` polling `_wpYtPlayer.getCurrentTime()`. MP4 branch wires `timeupdate` listener invoking the broadcaster. `broadcastCurrentTime` writes extended schema (REVIEWS C2): `currentTimeMs` + `currentTimeUpdatedAt` + `currentTimeSource` + `durationMs` + `isLiveStream` + writeAttribution. Sentry breadcrumb on failure category `videoBroadcast.write.failed`. couch commits `9c1f511` (helper) + `90de0ad` (wiring) 2026-04-30 to 2026-05-01 |
| VID-24-10 | Phase 24 / 24-02 | Complete — `makeIntervalBroadcaster(intervalMs, fn)` shipped in `js/native-video-player.js` as Pattern 3 leading + trailing edge throttle (closure-scoped `lastWriteAt` + `pendingValue` + `pendingTimer`; first call fires `fn()` immediately; subsequent calls within interval queue setTimeout for the LAST value); 3 inline behavioral assertions verified (leading edge fires / trailing not yet fired during queue / trailing fires with last value after queue overwrite); couch commit `9c1f511` 2026-05-01 |
| VID-24-11 | Phase 24 / 24-03 | Complete — iOS PWA `playsinline` honored on both branches: YouTube via `playerVars: { playsinline: 1, enablejsapi: 1 }` passed to `new YT.Player()`; HTML5 via `<video controls playsinline preload="metadata">` attribute. Both prevent the iOS Safari fullscreen-yank that would tear users out of the watchparty modal. Smoke contract asserts `playsinline=1` URL-param assertion via mock build. couch commit `90de0ad` 2026-04-30 |
| VID-24-12 | Phase 24 / 24-01 | Complete — smoke contract `scripts/smoke-native-video-player.cjs` (286 lines, 46 assertions: 35 helper-behavior + 11 production-code sentinels) imports production module via `await import(pathToFileURL(absolutePath).href)` (Windows-portable file-URL form preserving the `'../js/native-video-player.js'` literal in inline comments for grep-contract match); 8th smoke-gate added to `scripts/deploy.sh` §2.5 + recap echo extended; `package.json` scripts.smoke chain extended + `smoke:native-video-player` alphabetical alias added; 35 helper-behavior assertions all green on standalone Wave 2 Plan 01 run (parseVideoUrl 5 YT shapes + 2 MP4 + 6 rejections / titleHasNonDrmPath 4 cases / makeIntervalBroadcaster leading + trailing edge / seekToBroadcastedTime 4 cases per REVIEWS M1 / YouTube embed URL XSS guards); 11 production-code sentinels (REVIEWS C1/C2/H3/H4/H5/M1/M3/M4) fail by design pre-Plan-03 — flip to OK once Plan 24-03 lands its js/app.js edits; closes REVIEWS H2 at smoke layer; couch commits `0a0e3e8` (feat — smoke file) + `2fb5ae0` (chore — deploy.sh + package.json wire) 2026-05-01 |
| VID-24-13 | Phase 24 / 24-04 | Pending — `sw.js` CACHE bump to `couch-v37-native-video-player` via `bash scripts/deploy.sh 37-native-video-player`; locked 2026-04-30 |
| VID-24-14 | Phase 24 / 24-03 | Complete — `renderPlayerErrorOverlay(playerElementId)` at js/app.js:11261 injects `<em>Player couldn't load that link.</em> <a class="link-like" href="#" data-action="retry-video">Try again</a>` into a `.wp-video-error` overlay inside `.wp-video-frame`. REVIEWS H4 retry wiring: delegated click handler factory `makeRetryClickHandler` matches `[data-action="retry-video"]` and dispatches `window.reloadWatchpartyPlayer()` which tears down + re-attaches the current wp's player. Handler stored in `_wpRetryClickHandler` module-scope ref so teardown removes it cleanly. couch commit `90de0ad` 2026-04-30 |
| VID-24-15 | Phase 24 / 24-02 + 24-03 | Complete — `seekToBroadcastedTime(player, wp, source)` helper shipped in js/native-video-player.js (couch commit `9c1f511`); call sites wired in Plan 24-03 at two locations in `attachVideoPlayer`: (1) YouTube branch onReady event invokes `seekToBroadcastedTime(_wpYtPlayer, wp, 'youtube')` for non-hosts (`!isHost`) before arming the broadcaster; (2) MP4 branch invokes `seekToBroadcastedTime(video, wp, 'mp4')` for non-hosts immediately after element resolved. Late-joining participants enter at host's current playback position rather than always starting at 0. couch commits `9c1f511` (helper) + `90de0ad` (call sites) 2026-04-30 to 2026-05-01 |
| VID-24-16 | Phase 24 / 24-03 | Complete — `app.html:1213-1224` splits `#wp-live-content` into two siblings: `<div id="wp-video-surface" class="wp-video-surface">` (persistent — only attachVideoPlayer/teardownVideoPlayer touch it) + `<div id="wp-live-coordination" class="wp-live-coordination">` (re-renderable — renderWatchpartyLive writes the existing live-modal HTML here). renderWatchpartyLive at js/app.js:11424 retargets `getElementById('wp-live-coordination')` exclusively; verified renderer body has 0 DOM accesses to `#wp-video-surface`. Catastrophic player-reset on every reaction/timer/snapshot now MITIGATED. couch commits `44d55a7` (HTML scaffold + CSS) + `f58f186` (renderer retarget) 2026-04-30 |
| VID-24-17 | Phase 24 / 24-03 | Complete — `readAndValidateVideoUrl` helper at js/app.js:10871 surfaces `flashToast('Note: HTTP links may be blocked by the browser. Use https if available.', { kind: 'warn' })` when `parsed.source === 'mp4' && raw.toLowerCase().startsWith('http://')`. Non-blocking warning — Plex/Jellyfin LAN URLs may still work in some PWA contexts; we don't reject submission. Verbatim copy from REVIEWS C1 consensus. couch commit `c6de53e` 2026-04-30 |
| VID-24-18 | Phase 24 / 24-04 | Pending — Firestore rules verification (5 rules-tests in `tests/rules.test.js`) + conditional `firestore.rules` tightening + cross-repo deploy reminder for queuenight mirror (REVIEWS M2); locked 2026-04-30 |

**Coverage:**
- v1 requirements: 136 total (116 complete across Phases 3+4+5+6+7+8+9+11+12+13+14+15+15.1+18+19+20; 20 pending: DESIGN-01 → Phase 15.3, YEAR-01..05 deferred Phase 10, AUTH-05 + COMP-13-01 + OPS-13-04/05/07 carry HUMAN-VERIFY tags, REQ-18-01..02 + REQ-18-03..08 + REQ-20-07..09 carry UAT-DEFERRED tags but are code-complete, Phase 15.2 itself in-flight). Phase 15.6 traceability backfill 2026-04-30 added 29 new rows from Phases 18+19+20 (REQ-18-01..10 + KID-19-01..08 + REQ-20-01..11). Phase 15.2 traceability backfill 2026-04-27 closed 28 stale-Pending rows from v33.3 milestone audit.
- v2 requirements (in-flight): 18 total (17 complete — VID-24-12 closed via Plan 24-01 ship 2026-05-01; VID-24-02/04/10 closed via Plan 24-02; VID-24-01/03/05/06/07/08/09/11/14/15/16/17 closed via Plan 24-03 ship 2026-04-30; 1 pending VID-24-13 + VID-24-18 → Plan 24-04). VID-24-01..18 added 2026-04-30 alongside Phase 24 planning chain completion. Plan 24-03 closed 11 reqs in one commit-cluster (44d55a7 + c6de53e + f58f186 + 90de0ad on 2026-04-30) — REVIEWS H1/H3/H4/H5/M1/M3/M4/C1/C2 all resolved end-to-end. Remaining: VID-24-13 (sw.js CACHE bump) + VID-24-18 (Firestore rules verification) → Plan 24-04 deploy ritual. Phases 22 + 23 (also v2) shipped as direct-ship without REQ-IDs; future audit-trail backfill phase will mint REQ-22-* + REQ-23-* if useful (mirrors Phase 15.6 retroactive pattern).
- Mapped to phases: 154 ✓ (136 v1 + 18 v2)
- Unmapped: 0

---
*Last updated: 2026-04-30 — Phase 24 pre-execute traceability backfill (one of 4 tidy items applied alongside ROADMAP heading refresh + STATE frontmatter fix + RESEARCH heading nit). Added new `## v2 Requirements > Watchparty + sports v2 (Phase 24+)` section with 18 VID-24-01..18 requirements (full descriptions per the locked planning chain). Appended 18 traceability rows after REQ-20-11; all marked `Pending — locked 2026-04-30; will execute via /gsd-execute-phase 24`. Coverage line refreshed: 136 v1 / 116 complete + 18 v2 / 0 complete = 154 total / 116 complete. v2 milestone scope (Phases 22+23 shipped + 24 pending + 26+27+28+30 SCOPED) now traceable via REQUIREMENTS in addition to ROADMAP. Mirrors Phase 15.2 + 15.6 pattern of REQ-row backfill but applied PRE-execute rather than post (planner anticipated this would be a future audit-trail phase; doing it now is cheaper than later). Prior update 2026-04-30 — Phase 15.6 / Plan 01 (audit-trail backfill 2) added 29 new traceability rows: REQ-18-01..10 (Availability Notifications, Phase 18; commits d622dc6 queuenight + 5a86045/7f04d2f/416ab32/c5fb713/fac3d71/adcebfc couch), KID-19-01..08 (Kid Mode, Phase 19; commits 2930ff8/0ce1e59/1585208/3e3b7d4/59c3fe6 couch), REQ-20-01..11 (Decision Explanation, Phase 20; commits 9b2c1f7/98358b8/cc0bb6f/783c946/62bb033/81113b5/1c02ea0/7b67299 couch). All marked Complete; UAT-DEFERRED tags preserved on REQ-18-01..02 + REQ-20-07..09 per shipped-but-pre-device-UAT pattern. Coverage line refreshed: 107 total / 87 complete → 136 total / 116 complete. Schema unchanged (D-05); no source-code changes (D-12); no deploy (D-17). Prior update Phase 15.2 / Plan 06 2026-04-27.*
*Requirements defined: 2026-04-19*
*Last updated: 2026-04-27 — Phase 15.2 / Plan 06 (traceability + ROADMAP refresh) closed: 28 stale Pending rows flipped to Complete (AUTH-01..05 + PUSH-01..05 + PARTY-01/02/04/05/06/07 + INTENT-01..06 + POL-01..04 + COMP-13-01 + OPS-13-02/04/05/06/07 + DECI-14-01) per v33.3 milestone audit reconciliation. POL-01 marked 'Complete (acceptance met) — partial-parity-with-Phase-14-D-3 closed by Phase 15.4'. DESIGN-01 preserved as 'Pending — Phase 15.3'. TRACK-15-01..14 unchanged (already Complete; 'partial' status in audit referred to SUMMARY-frontmatter convention drift, accepted as v1 tech-debt per ROADMAP §330). Inline checkbox tally refreshed; Coverage line updated from 59→87 complete. Prior update 2026-04-27 — Phase 15.1 / Plan 03 (Wave 3 Close-out + REVERSED cross-repo deploy) closed: Traceability table rows for SEC-15-1-01..05 reformatted to canonical `Phase 15.1-XX` plan-ref shape; Phase 15.1 removed from Coverage pending-phase list (5/5 SEC-15-1-* now Complete). Hosting deployed (couch-v35.1-security-hardening live at couchtonight.app) → rules deployed second (validAttribution anchor + 4th title-doc sub-rule + replaced 5th family-doc branch live on queuenight-84044). REVERSED ordering vs Phase 15: writers conformed BEFORE rules tightened. changelog.html v35.1 article shipped with 2026-04-27 deploy date. Phase 15.1 auditably CLOSED. Prior update 2026-04-27 — Phase 15.1 / Plan 02 (Wave 2 Rules + Tests) closed: SEC-15-1-01 + SEC-15-1-02 + SEC-15-1-03 + SEC-15-1-04 marked complete. Three rule edits landed atomically in source + mirror byte-identically (validAttribution() anchored regex, title-doc 4th progress sub-rule, family-doc 5th-branch participant check). Six new tests #33-#38 added; 38/38 passing. Wave 3 (15.1-03) close-out + cross-repo deploy follows. Prior update 2026-04-27 — Phase 15.1 (Security Hardening) minted: 5 SEC-15-1-* requirements added in new Security-Hardening-Phase-15.1 section + 5 Traceability table rows (plans TBD; resolved by /gsd-plan-phase 15.1) + Coverage line refreshed from 102 to 107 total. Decimal-phase insertion mirrors 03.5 precedent — not a slot reassignment. Closes 4 findings from the Phase 15 holistic security audit + code review (F-OPEN-01 HIGH + WR-02 HIGH + F-OPEN-02 MEDIUM + WR-03 MEDIUM). Prior update 2026-04-27 — Phase 15 (Tracking Layer) minted: 14 TRACK-15-* requirements added in new Tracking-Layer-Phase-15 section + 14 Traceability table rows + Coverage line refreshed to 102 total. Bumped via Plan 15-08 close-out (REVIEW LOW-narrative-fix: companion ROADMAP.md row 15 narrative scrubbed from "13 TRACK-15-*" to "14 TRACK-15-*"). Prior update 2026-04-26 — DECI-14-10 + DECI-14-11 + DECI-14-12 + DECI-14-13 (onboarding tooltips + 5 empty states + 7 push categories + sw.js v34 CACHE bump, plan 14-09) marked complete DEPLOY-DEFERRED — 5 atomic couch commits (8a36336 + ae7f0c0 + fdcb9dc + e572467 + 9132144) + queuenight/functions/index.js NOTIFICATION_DEFAULTS edit (uncommitted on this machine — same pattern as 14-06 deviation 1); Task 7 deploy gate deferred to a queuenight-equipped session matching the Phase 14 batch-UAT-and-deploy pattern; recap in 14-09-SUMMARY.md; **Phase 14 — Decision Ritual Core: 9/9 plans CODE-COMPLETE, status flips to COMPLETE pending v34 deploy + batch UAT**. Prior update 2026-04-26 — DECI-14-07 (Flow A group rank-pick + push-confirm UI, plan 14-07) marked complete UAT-DEFERRED — 4th deferral in Phase 14 matching the 14-02 + 14-04 + 14-08 pattern; multi-device UAT bundled conceptually with the 14-08 multi-device Flow B UAT entry (same staging deploy + 2-device session covers both flows). Prior update 2026-04-26 — DECI-14-04 + DECI-14-05 (tile redesign + Vote-mode preservation + carry-over UAT bug closure, plan 14-05) marked complete — carry-over UAT bug CLOSED by pre-existing CSS fix at css/app.css:1540 (NOT deferred). Prior update 2026-04-26 — DECI-14-06 (Couch viz hero-icon + avatar-grid per sketch 001 winner P, plan 14-04) marked complete UAT-DEFERRED. Prior update 2026-04-26 — DECI-14-08 (Flow B solo-nominate + counter-time, plan 14-08) marked complete UAT-DEFERRED. Prior update 2026-04-26 — DECI-14-02 (tier aggregators, plan 14-03) marked complete. Prior update 2026-04-25 — added Phase 14 (Decision Ritual Core) with 13 DECI-14-* items locked via /gsd-discuss-phase 14 (12 user decisions D-01..D-12 + 3 reconciled DRs) and minted at /gsd-plan-phase 14. Prior update 2026-04-25 — added Phase 13 (Compliance & Ops Sprint) with 6 COMP-13-* + OPS-13-* items locked via /gsd-discuss-phase 13 + /gsd-plan-phase 13. Prior update 2026-04-25 — added Phase 12 (Pre-launch polish) with 4 POL-* items repurposing the original parking-lot Phase 12 slot (parking-lot docket archived to seeds/phase-12-original-docket-archive-2026-04-23.md). Prior update 2026-04-24 — added Phase 11 (Feature Refresh & Streamline) with 13 REFR-* items scoped via /gsd-discuss-phase 11 (6 decisions locked 2026-04-23). Prior update 2026-04-20 — roadmap restructure added Auth (5), Push (6), Intent (8), Redesign (9) and pushed Year-in-Review to Phase 10.*
