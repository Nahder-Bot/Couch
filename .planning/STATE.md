---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: Phase 14 CODE-COMPLETE — 9/9 plans shipped; pending v34 deploy + batch UAT
stopped_at: "**PHASE 14 READY TO DEPLOY** — 9/9 plans CODE-COMPLETE 2026-04-26 (14-09 closed via defer-deploy resume signal). Next session must run the v34 cross-repo deploy ritual + batch multi-device UAT pass before status flips to COMPLETE. Deploy order locked in Open follow-ups + 14-09-SUMMARY.md Task 7 section: (1) queuenight CFs → (2) queuenight firestore.rules → (3) couch hosting via scripts/deploy.sh 34.0-decision-ritual → (4) cross-flow smoke test → (5) changelog date patch. 5 deferred UAT items bundle into the same staging session: 14-02 iOS Safari touch-DnD + 14-04 Couch viz visual+iOS PWA + 14-07 Flow A multi-device + 14-08 Flow B multi-device + 14-09 tooltip+empty-state+push toggles. PRIOR (14-09): Phase 14 Plan 07 (Flow A — group rank-pick + push-confirm UI, D-07 / DECI-14-07) SHIPPED 2026-04-26. Couch repo: 1 coupled commit (d97a81e) matching the 14-08 Flow B precedent (af168f1) — entry CTA / picker / roster / response / counter-nom / convert all cross-reference each other so they ship as one block. Surfaces: app.html (#flow-a-entry-container under #couch-viz-container) + js/app.js (~480 lines appended pre-boot — renderFlowAEntry hooked into renderTonight after renderCouchViz + into state.unsubIntents handler alongside maybeRerenderFlowAResponse; window.openFlowAPicker entry guards on sign-in + couch claim; renderFlowAPickerScreen consumes 14-03 aggregators with T3 expand toggle gated by resolveT3Visibility() per D-02 privacy posture; renderFlowARosterScreen with state.flowAProxyConfirmed Set + state.me auto-confirmed; onFlowASendPicks calls createIntent({flow:'rank-pick', expectedCouchMemberIds}) from 14-06 then pre-seeds proxy-confirmed members' rsvps[mid]={state:'in', proxyConfirmedBy} so 14-06's onIntentCreated CF skips them in flowAPick fan-out push; window.openFlowAResponseScreen + renderFlowAResponseScreen branches between picker view (live tally + reject-majority CTA + counter-cap banner + convert button + end-nomination) and recipient view (In/Reject+counter/Reject/Drop) with isClosed gate for converted/cancelled status; submitCounterNom writes rsvps[me]={state:'reject', counterTitleId} + atomically increments counterChainDepth client-capped at 3 mirroring server rule from 14-06; onFlowAConvert quorum-checks ins>=1 then addDoc(watchpartiesRef(), {convertedFromIntentId,...}) + intent.status='converted' with convertedToWpId + back-compat convertedTo + opens openWatchpartyLive so Phase 11-05 lobby flow takes over; onFlowARetryPick stashes rejected titleId in state.flowARejectedTitles Set for next picker render — 1 retry then second intent expires naturally at 11pm EOD per 14-06's createIntent rank-pick branch js/app.js:1419-1421) + css/app.css (~155 lines: .flow-a-entry / .flow-a-active / .flow-a-picker-modal / .flow-a-row / .flow-a-row-poster / .flow-a-section-h / .flow-a-t3-toggle / .flow-a-roster-* / .flow-a-response-* / .flow-a-rejected-cta / .flow-a-counter-cap / .flow-a-status-closed; reuses existing .modal-bg + .modal-content shell from css/app.css:478). 3 Rule-1 plan-sample corrections (intentRef(id) used unwrapped per js/app.js:1388 NOT doc(intentRef(...)) which would error; per-tier metaBits builder reading meanRank/meanPresentRank/meanOffCouchRank per 14-03's actual return shapes; isClosed gate on response screen so status pill replaces stale primary buttons after status flips to converted/cancelled). Cross-plan dependencies all satisfied upstream: 14-01 isWatchedByCouch (consumed via 14-03 tier aggregators) + 14-03 getTierOneRanked/getTierTwoRanked/getTierThreeRanked + resolveT3Visibility + 14-04 state.couchMemberIds (populated by claimCushion) + 14-06 createIntent({flow:'rank-pick'}) + onIntentCreated push CF + onIntentUpdate counter-chain CF + watchpartyTick rank-pick branch hard-expire at 11pm EOD + 14-08 maybeOpenIntentFromDeepLink already typeof-guards openFlowAResponseScreen so deep-links to Flow A intents now work end-to-end. Task 5 multi-device UAT DEFERRED via the plan's explicit `skip-uat` resume signal — 4th deferral in Phase 14 matching the 14-02 + 14-04 + 14-08 pattern; recap captured in 14-07-SUMMARY.md (Flow A entry CTA → picker 3-tier with T3 collapse → roster proxy-confirm → send-picks → recipient response in/reject/drop/counter → counter-nom chain 3-cap → reject-majority retry → quorum convert). UAT remains valid and MUST run before Phase 14 ships to v34 production deploy (tracked in Open follow-ups below; bundled conceptually with the existing 14-08 multi-device Flow B UAT entry). With 14-07 shipped, **8 of Phase 14's 9 plans are complete** — only 14-09 (onboarding tooltips + 5 empty states + 7 push categories + sw.js v34 CACHE bump) remains before Phase 14 ships to v34 production deploy. PRIOR (14-05): Phase 14 Plan 05 (Tile redesign + Vote-mode preservation + carry-over UAT bug closure, D-04 + D-05 / DECI-14-04 + DECI-14-05) SHIPPED 2026-04-26. Couch repo: 4 commits — f90155d (Task 1: card(t) X-want-it pill replacing the bare yes-count pill at js/app.js:4353 with up-to-3 micro-avatars + +N overflow chip + memberColor() backgrounds + singular/plural aria-label; ▶ Trailer button gated on t.trailerKey && !t.watched with event.stopPropagation; tile body onclick rewire openDetailModal→openTileActionSheet at js/app.js:4469; Vote primaryBtn case removed; node --check exits 0), d1762e1 (Task 2: window.openTileActionSheet sibling primitive inserted IMMEDIATELY ABOVE the existing window.openActionSheet at js/app.js:11853 — existing primitive UNCHANGED, verified via diff; reuses #action-sheet-bg DOM container; 4 D-04 buckets 🎬 Watch tonight / 📅 Schedule for later / 💭 Ask family / 🗳 Vote-de-emphasized + secondary divider + ℹ Show details + ⋯ More options falling back to full openActionSheet + D-01 cross-link 🔁 Rewatch this one when state.me && t.watched wires to 14-01's setRewatchAllowed; grep marker `// === D-04 openTileActionSheet — DECI-14-04 ===` added), 09db482 (Task 3: detail-modal TMDB community reviews lazy-fetch — inspection confirmed 4 of 5 D-04-required surfaces (Trailer / Providers / Synopsis / Cast) ALREADY present in the modal — only Reviews was net-new; ensureTitleCastAndReviews(t) hits TMDB /{seg}/{tmdbId}/reviews per-title-cached via t.castFetchedAt + t.reviewsFetchedAt timestamps preventing repeat fetches per T-14.05-02; renderDetailReviewsSection composes .detail-review cards with author + optional rating + 360-char-truncated body; all TMDB content escapeHtml-passed per T-14.05-01; DOM appendation gated on data-section='reviews' to prevent duplicate append on re-open; ~80 lines of CSS — .tc-want-pill / .tc-want-avatar / .tc-want-overflow / .tc-trailer-btn + .detail-section-h / .detail-cast-grid / .detail-cast-item / .detail-reviews-list / .detail-review / .detail-review-h / .detail-review-body), 6d630c4 (Task 4: D-05 Catch-up-on-votes CTA — unvotedCount = state.titles.filter(t => !t.watched && state.me && (t.votes||{})[state.me.id] == null).length; <div class='add-catchup-cta'> renders conditionally when unvotedCount >= 10 with heading + count line + tc-primary CTA wired to openVoteModal(); spliced at the top of the Add-tab innerHTML composition; .add-catchup-cta CSS uses warm-amber border + token-aligned background). Task 5 (carry-over UAT bug) CLOSED — NOT DEFERRED — by pre-existing CSS fix at css/app.css:1540: `.detail-close{position:fixed;top:calc(var(--s3) + env(safe-area-inset-top, 0px));right:calc(var(--s3) + env(safe-area-inset-right, 0px));...;z-index:10}` with explanatory comment block at lines 1537-1539 documenting why fixed (not absolute) was chosen and why detail-modal-bg is safe (no transform parent). The rule is global to .detail-close so every entry surface (Library/Queue, Tonight, Add tab) is covered uniformly because all 4 surfaces open the same #detail-modal element with the same .detail-close button. The only intentional scoped exception is .avatar-picker-modal .detail-close{position:absolute} at css/app.css:2380 — required because that modal IS rendered inside a transformed parent. Task 3's TMDB community-reviews extension just added ~3 review cards to the detail body length, validating the position:fixed rule continues to hold against the new longer content. Live iOS Safari render confirmation under iOS PWA standalone mode + iOS-specific safe-area-inset behavior is a SOFT non-blocking step rolled into the existing batch pre-v34 UAT pass under the existing 'iOS Safari' Open follow-ups entry — NOT a separate gate because the CSS rule is already analyzed to work. Zero deviations during execution. PRIOR (14-04): Phase 14 Plan 04 (Couch viz — hero icon + avatar grid, D-06 / DECI-14-06; sketch 001 winner P) SHIPPED 2026-04-26. Couch repo: 3 commits — 1234624 (renderCouchViz + renderCouchAvatarGrid + claimCushion + openCouchOverflowSheet + claimCushionByMember + persistCouchSeating in js/app.js, +157 lines; Tonight render hook at end of renderTonight() + family-doc onSnapshot hydration into indexed-array state.couchMemberIds shape; hero asset = /mark-512.png so Fiverr brand-refresh swap is zero-code), 3a561bd (#couch-viz-container in app.html at top of #screen-tonight above picker-card + ~140 lines D-06 CSS in css/app.css: .couch-viz-container / .couch-hero with radial halo + heavy shadow / .couch-headline Fraunces / .couch-sub Instrument Serif / .couch-avatar-grid 5-col phone wrap + 10-col >=768px desktop / .seat-cell + .seat-avatar filled / .seat-empty dashed amber + couch-empty-pulse 2.4s + prefers-reduced-motion guard; production palette doesn't define --c-* tokens so the hardcoded fallbacks drive rendering), cb31155 (firestore.rules Case A — additive third UPDATE branch on /families/{code} for couchSeating writes requiring attributedWrite() + affectedKeys allowlist hasOnly(['couchSeating','actingUid','managedMemberId','memberId','memberName']); picker + ownerUid branches preserved verbatim + 4 new sibling tests in tests/rules.test.js: #19 allow / #20 missing-attribution deny via withSecurityRulesDisabled grace-state reset / #21 extra-key deny / #22 stranger deny — 22/22 passing in firebase emulators:exec). Task 5 visual + iOS PWA UAT DEFERRED via the plan's explicit `skip-uat` resume signal — same pattern as 14-02 and 14-08 — recap captured in 14-04-SUMMARY.md (hero icon renders / headline + sub-count / grid scales 2-10 cells with 5x2 phone wrap and 10x1 desktop / claim/vacate/already-claimed flows / Firestore round-trip / iOS PWA tap targets + reduced-motion). UAT remains valid and MUST run before Phase 14 ships to v34 production deploy. Zero deviations. Approach note: original D-06 plan called for procedural inline SVG renderCouchSvg() — sketch 001 (.planning/sketches/001-couch-shape/) ran 2026-04-25 and locked hero-icon + avatar-grid (Variant P) as the right architecture; the SVG renderer was never built. Test-isolation finding worth preserving for next rules.test.js author: long graceUntil + withSecurityRulesDisabled() reset pattern is the documented workaround for the legacyGraceWrite() false-allow trap (test #19 stamps memberId on fam1, which would otherwise satisfy legacyGraceWrite() during the seeded grace window and let test #20's missing-attribution write through as a false-allow). DEPLOY GATE for v34: `firestore.rules` was modified by Task 4 — `bash scripts/deploy.sh` only handles `--only hosting` (see scripts/deploy.sh:142), so a separate `firebase deploy --only firestore:rules --project queuenight-84044` from sibling queuenight/ repo is REQUIRED before v34 production deploy or live couchSeating writes from persistCouchSeating() will be denied. Tracked in Open follow-ups below. PRIOR (14-08): Phase 14 Plan 08 (Flow B — solo-nominate + invite + counter-time, D-08 / DECI-14-08) SHIPPED 2026-04-26. Couch repo: 1 commit (af168f1) adding Flow B's full client surface to js/app.js + css/app.css — openFlowBNominate UI (tile action sheet entry + datetime-local time picker + 200-char note + createIntent({flow:'nominate', ...}) submit), recipient response screen (Join @ proposed / Counter-suggest / Decline; counter writes rsvps[me]={state:'maybe',counterTime,note} + atomic counterChainDepth bump capped at 3), nominator status screen (live tally + T-15 auto-convert indicator + all-No banner with cancelReason='all-no' + counter rows with Accept/Reject/Compromise actions), maybeRerenderFlowBStatus() wired into state.unsubIntents tick, and ?intent= deep-link handler routing creator → status screen / recipient → response screen with defensive typeof guard so 14-08 lands cleanly before 14-07. CF side already shipped in 14-06 (onIntentCreated flowBNominate push, onIntentUpdate flowBCounterTime push, watchpartyTick T-15 auto-convert + T-30 hard-expire warning). One auto-fix Rule 2 deviation: added .tc-secondary CSS class to css/app.css (existing .tc-primary at css/app.css:1403 had no design-system partner). Task 5 multi-device UAT DEFERRED via the plan's explicit `skip-uat` resume signal — same pattern as 14-02 — multi-device + push UAT batched as a single pre-v34-production-deploy verification pass; recap captured in 14-08-SUMMARY.md (nominate flow / push delivery / counter-time / nominator decision / T-15 auto-convert / all-No edge / deep-link routing). UAT remains valid and MUST run before Phase 14 ships to v34 production deploy (tracked in Open follow-ups below). Cross-plan dependency: Plan 14-09 must ship the 7 push category prefs (DR-3) before user pref toggles take effect; until then Flow B pushes (flowBNominate / flowBCounterTime / flowBConvert / intentExpiring) default-ON via fallback at queuenight/functions/index.js:114-115 (defaultOn=true when hasDefault=false). Remaining Phase 14 plans: W1 (14-04 SVG couch viz, 14-05 tile redesign) → W3 (14-07 Flow A — unblocked since 14-03 done) → W4 (14-09 onboarding+empty+push+CACHE). DEPLOY ORDER REMINDER (when v34 ships): deploy CFs from ~/queuenight FIRST, then bash scripts/deploy.sh from couch repo. PRIOR: 14-02 Task 3 iOS Safari touch-DnD UAT still DEFERRED to batch UAT pass (must run before v34 production deploy; Sortable.js fallback documented). PRIOR (14-06): queuenight CF edits to onIntentCreated/onIntentUpdate/watchpartyTick uncommitted (queuenight has no .git on this machine — see 14-06-SUMMARY.md deviation 1); 7 new eventTypes wired but pending NOTIFICATION_DEFAULTS/DEFAULT_NOTIFICATION_PREFS/NOTIFICATION_EVENT_LABELS additions in 14-09. ORIGINAL stopped_at (14-03) preserved for reference: Phase 14 Plan 03 (tier aggregators T1/T2/T3 + T3 visibility resolver, D-02 / DECI-14-02) SHIPPED 2026-04-26. Couch repo: 1 commit (ccf600f) adding 4 pure functions getTierOneRanked / getTierTwoRanked / getTierThreeRanked / resolveT3Visibility immediately after getGroupNext3 at js/app.js:7006-7106 (+102 lines, plan minimum was 80). All 3 tier aggregators consume isWatchedByCouch (14-01) before classifying so already-watched titles never leak into any tier. T1 = intersection across couch. T2 = strict T1 complement within any-couch-presence. T3 = zero couch overlap + >=1 off-couch. resolveT3Visibility = explicit-false-wins across state.me/family/group.preferences.showT3 (privacy-leaning default-show). UI consumer is 14-07 (Flow A picker) which must call resolveT3Visibility() to gate T3 expand affordance per D-02 privacy posture."
last_updated: "2026-04-26T19:00:00.000Z"
last_activity: 2026-04-26 -- Phase 14 Plan 10 (gap closure — sketch 003 V5 redesign; Roster IS the control) CODE-COMPLETE. 7 atomic couch commits (efd2739 Task 1 couchInTonight Firestore shape + dual-write + Bug B onSnapshot wiring; 9569c33 Task 3 V5 renderCouchViz with toggleCouchMember + sendCouchPing stub + 700ms long-press timer + 3 bulk actions; 0f45602 Task 4 .roster + .pill out/in/me + .ping-hint + .couch-hero-v5 CSS, deletes legacy .couch-avatar-grid + .seat-cell + .seat-empty + couch-empty-pulse + 14-09 cushion-glow keyframe; 5f17ebc Task 5 Path C sendCouchPing toast + Sentry breadcrumb stub — Path A rejected because no notifyMember helper exists, Path B rejected because new CF event type 'couchPing' would need cross-repo additions in NOTIFICATION_DEFAULTS + onIntentCreated fan-out + client subscription handler that deserves its own plan; 603ac28 Bug A — delete .who-card from app.html line 334 + applyModeLabels who-title-label orphan + renderTonight who-list emitter + rewire sticky who-mini IIFE from .who-card → #couch-viz-container; 860dfea Bug B — collapse renderFlowAEntry empty-state-c branch from 14-line CTA card + cushion-glow forEach to single-line clearout, V5 dashed-pill roster IS the empty state; 81c6700 Task 7 sw.js CACHE bump v34.0-decision-ritual → v34.1-roster-control). 1 cross-repo queuenight/firestore.rules edit applied in-place — UNCOMMITTED on this machine same pattern as 14-06/14-09 (4th UPDATE branch on /families/{code} mirroring 14-04's couchSeating branch with affectedKeys allowlist hasOnly couchInTonight + couchSeating + 4 attribution fields; couchSeating in allowlist because persistCouchInTonight dual-writes both shapes for one PWA cache cycle then drop in follow-up). UAT bugs from tests 1+3+5+24 closed: composite Issue #1 (.who-card double-render), Issue #3 ('7 blank spots... cluttered' on desktop — V5 pills wrap-flex naturally without empty-cell sprawl), Issue #5 ('I can only click once' — vacate is now the same gesture as claim, tap your own pill to flip out), Issue #24 ('Find-a-seat empty-state card still showing despite 1 seat claimed' — Bug B fix Task 1 added renderFlowAEntry call inside family-doc onSnapshot block, Task 6 collapsed redundant CTA card under V5). Migration: families/{code}.couchSeating (positional 14-04) → families/{code}.couchInTonight (member-keyed { in, at, proxyConfirmedBy? }) with backward-compat read of legacy field via couchInTonightFromDoc + dual-write of both shapes for one PWA cache cycle. Downstream consumers (14-01 isWatchedByCouch + 14-03 tier aggregators + 14-07 Flow A roster) all keep working via the same state.couchMemberIds derivation contract (couchInTonightToMemberIds filter in===true). Pending HUMAN-VERIFY UAT bundled with the existing v34 deploy gate — Step 1 cross-repo deploy ritual (queuenight CFs first, queuenight firestore:rules including 14-10 4th branch, then couch hosting via scripts/deploy.sh 34.1-roster-control) → Step 2 cold-start V5 visual UAT → Step 3 multi-device proxy UAT → Step 4 downstream contract regression UAT. PRIOR (14-09): Phase 14 Plan 09 CODE-COMPLETE (D-10 + D-11 + D-12 + D-13 / DECI-14-10..13 — onboarding tooltips + 5 empty states + 7 push categories + sw.js v34 CACHE bump). 5 atomic couch commits (8a36336 add showTooltipAt+hideTooltip primitive in js/utils.js + .coach-tip CSS + .queue-empty-cta + .cushion-glow keyframes; ae7f0c0 maybeShowTooltip gate near maybeShowFirstRunOnboarding + 3 callsites couchSeating/tileActionSheet/queueDragReorder; fdcb9dc 5 D-11 empty states (a-e) including tier aggregator includeWatched opt + state.flowARevealRewatch flag + Flow A→B handoff; e572467 DR-3 places 2+3 — 7 D-12 keys in DEFAULT_NOTIFICATION_PREFS + NOTIFICATION_EVENT_LABELS with BRAND-voice copy; 9132144 v34 changelog article above v32 + sw.js CACHE bump couch-v33.3-sentry-dsn → couch-v34.0-decision-ritual). queuenight/functions/index.js NOTIFICATION_DEFAULTS edit applied in-place (UNCOMMITTED on this machine — same pattern as 14-06 deviation 1). Task 7 deploy gate DEFERRED via defer-deploy resume signal — matches Phase 14 batch-UAT-and-deploy pattern (CFs+rules+hosting bundled with 4 outstanding multi-device UATs before v34 ships). DR-3 follow-up override applied per CONTEXT.md note + /gsd-plan-phase 14 user override 2026-04-25 — 7 new D-12 keys surface ONLY in legacy NOTIFICATION_EVENT_LABELS Settings UI; Phase 12 friendly-UI maps NOT mirrored to avoid dual-Settings-screen collision RESEARCH §5 flagged; friendly-UI parity captured as polish backlog item. PHASE 14 — DECISION RITUAL CORE: 9/9 plans CODE-COMPLETE 2026-04-26, status flips to COMPLETE pending v34 deploy + batch UAT.
progress:
  total_phases: 11
  completed_phases: 4
  total_plans: 67
  completed_plans: 43
  percent: 64
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-19)

**Core value:** Turn "what do you want to watch?" from a 20-minute argument into a 30-second ritual that everyone on the couch trusts.
**Current focus:** Phase 13 SHIPPED — manual UAT + gcloud Firestore export remain
**Active milestone:** v1 Commercial Release (Phases 3-10)

## Current Position

**Phase 13 — Compliance & Ops Sprint:** SHIPPED 2026-04-25 (5/5 plans, 33 commits squash-merged via PR #1 db03573 + 1 cleanup commit via PR #2 3b6082c).

| Requirement | Status |
|---|---|
| COMP-13-01 self-serve account deletion | ✅ Code-complete + 4 CFs live in us-central1 |
| OPS-13-02 BUILD_DATE auto-stamp | ✅ scripts/deploy.sh handles |
| OPS-13-04 CSP Report-Only | ✅ Live on couchtonight.app/* (audit window TD-4) |
| OPS-13-05 Sentry | ✅ DSN substituted + deployed (couch-v33.3-sentry-dsn) |
| OPS-13-06 GitHub branch protection | ✅ Legacy Branch Protection on main; repo flipped public |
| OPS-13-07 Firestore daily export | ⏸ scripts/firestore-export-setup.sh ready; awaits gcloud auth on user's machine |

**Deferred (CONTEXT.md):** OPS-13-01 (firebase-functions SDK 4→7 → Phase 14, TD-1), OPS-13-03 (Variant-B storage rules → post-Phase-5, TD-2).

**For Phase 13 details:** see .planning/phases/13-compliance-and-ops/13-VERIFICATION.md (HUMAN_NEEDED verdict, per-requirement evidence) + 13-0[1-5]-SUMMARY.md.

## Recent shipped phases (lookback)

| Phase | Status | Reference |
|---|---|---|
| 12 — Pre-launch polish | SHIPPED 2026-04-25, 8/8 smoke tests | .planning/phases/12-pre-launch-polish/12-VERIFICATION.md |
| 11 — Feature refresh & streamline | CODE-COMPLETE 2026-04-24 (deploys bundled, pending Blaze + Storage enablement) | .planning/phases/11-feature-refresh-and-streamline/11-COMPLETION.md |
| 7 — Watchparty | All gap-closure deployed; awaiting consolidated /gsd-verify-work 7 UAT | .planning/phases/07-watchparty/ |
| 4 — Veto System | Implementation complete; awaiting /gsd-verify-work | .planning/phases/04-veto-system/ |
| 3 — Mood Tags | Implementation complete; awaiting /gsd-verify-work | .planning/phases/03-mood-tags/ |

## sw.js cache version

Current source: `couch-v34.1-roster-control` (Plan 14-10 — Sketch 003 V5 redesign committed but **NOT YET DEPLOYED** — deploy bundled with the v34 cross-repo deploy gate). Live on couchtonight.app: `couch-v33.3-sentry-dsn` until v34.1 ships. Bumped via `bash scripts/deploy.sh <short-tag>`. Phase progression: v29 (P11 final) → v32 (P12) → v33 (P13/13-01 account deletion) → v33.1 (P13/13-02 Sentry) → v33.2 (P13/13-05 CSP) → v33.3-sentry-dsn (P13/post-merge DSN substitution) → v34.0-decision-ritual (P14 / 14-09 — pending deploy) → v34.1-roster-control (P14 / 14-10 — pending deploy).

## Production services

See RUNBOOK §M for the full inventory (Firebase project, Sentry org/project/DSN, GitHub repo + branch protection + CI). Quick refs:
- Firebase: `queuenight-84044` (us-central1)
- Sentry: `couchtonight.sentry.io` project `couch`
- GitHub: `Nahder-Bot/Couch` (public; legacy Branch Protection on main)

## Open follow-ups

| Type | Item | Where |
|---|---|---|
| HUMAN-VERIFY | iOS Safari touch-DnD UAT for Library queue drag-reorder (Phase 14 / 14-02 Task 3 — must run before v34 production deploy; fallback if broken: load Sortable.js via CDN per 14-02-SUMMARY.md). **Soft confirmation absorbed (Phase 14 / 14-05 Task 5):** while iOS Safari is already in hand for this UAT, also confirm the `.detail-close{position:fixed}` rule at css/app.css:1540 renders correctly under iOS PWA standalone mode + iOS-specific safe-area-inset behavior on a long detail body (Library/Queue title with TMDB community reviews). NON-BLOCKING — the carry-over UAT bug is already CLOSED by source analysis (see 14-05-SUMMARY.md Task 5); this is an additive smoke check, not a separate gate. | .planning/phases/14-decision-ritual-core/14-02-SUMMARY.md + 14-05-SUMMARY.md |
| HUMAN-VERIFY | ~~Couch viz visual + iOS PWA UAT (Phase 14 / 14-04 Task 5)~~ — **OBSOLETED by 14-10 redesign**. Original 14-04 cushion-grid surface no longer exists. New surface is V5 roster pills (per sketch 003 V5 winner) — re-verify under 14-10 below. | .planning/phases/14-decision-ritual-core/14-04-SUMMARY.md |
| HUMAN-VERIFY | Couch viz V5 roster UAT (Phase 14 / 14-10 — must run before v34.1 production deploy; recap: hero icon 84px renders + Fraunces headline + dynamic Instrument Serif sub-line by state; pills wrap-flex with no row-of-empty-cells problem on desktop; OUT pills dashed + dim, IN pills filled member-color, ME pill amber outline + YOU tag; tap any pill flips in/out for self AND proxy; long-press out-pill 700ms shows progress underline + sends push (toast confirmation); tally bar shows num/total; 3 action links visibility-gated; couchInTonight Firestore round-trip + dual-write to couchSeating; iOS PWA tap targets >=44pt; prefers-reduced-motion respected; Bug A regression check — no .who-card double-render; Bug B regression check — Find-a-seat CTA card no longer appears under V5; downstream consumers 14-01 / 14-03 / 14-07 still work because state.couchMemberIds keeps the same shape) | .planning/phases/14-decision-ritual-core/14-10-SUMMARY.md |
| Two-repo discipline | queuenight commit pending — `C:/Users/nahde/queuenight/firestore.rules` 14-10 4th UPDATE branch added in-place but uncommitted on this machine (same pattern as 14-06/14-09 deviation). User must `cd ~/queuenight && git add firestore.rules && git commit -m "feat(14-10): allow proxy writes to couchInTonight (sketch 003 V5)"` from a queuenight-equipped session before deploy. | .planning/phases/14-decision-ritual-core/14-10-SUMMARY.md |
| Polish backlog | Couch-ping push channel wiring — sendCouchPing in js/app.js currently shows toast + Sentry breadcrumb only (Path C from 14-10 Task 5). Real push fan-out requires queuenight/functions/index.js new event type 'couchPing' added to NOTIFICATION_DEFAULTS + NOTIFICATION_EVENT_LABELS, fan-out branch in onIntentCreated, client subscription handler. Defer to follow-up plan; Sentry breadcrumb under category='couch-ping' captures attempted fires for product analytics in the meantime. | .planning/phases/14-decision-ritual-core/14-10-SUMMARY.md |
| Polish backlog | Drop legacy couchSeating dual-write — persistCouchInTonight writes BOTH couchInTonight (canonical V5 shape) AND couchSeating (legacy 14-04 positional shape) for backward-compat with v33/v34.0 PWAs in transit. After one PWA cache cycle (~1-2 weeks of v34.1 deployed), drop the couchSeating write AND the legacy 3rd UPDATE branch in queuenight/firestore.rules. | .planning/phases/14-decision-ritual-core/14-10-SUMMARY.md |
| HUMAN-VERIFY | Multi-device Flow B UAT (Phase 14 / 14-08 Task 5 — must run before v34 production deploy; recap: nominate flow / push delivery / counter-time / nominator decision / T-15 auto-convert / all-No edge / deep-link routing) | .planning/phases/14-decision-ritual-core/14-08-SUMMARY.md |
| HUMAN-VERIFY | Multi-device Flow A UAT (Phase 14 / 14-07 Task 5 — must run before v34 production deploy; recap: Flow A entry CTA → picker 3-tier with T3 collapse → roster proxy-confirm → send-picks → recipient response in/reject/drop/counter → counter-nom chain 3-cap → reject-majority retry → quorum convert). Bundle conceptually with the 14-08 multi-device Flow B UAT entry above — same staging deploy + 2-device session covers both flows. | .planning/phases/14-decision-ritual-core/14-07-SUMMARY.md |
| HUMAN-VERIFY | Tooltip + empty-states + push toggles UAT (Phase 14 / 14-09 — must run before v34 production deploy; recap: (1) 3 D-10 tooltips fire once per user — couchSeating on Couch viz first render / tileActionSheet on first tile tap / queueDragReorder on first Library queue render — and DON'T re-fire after re-login (Firestore flag wrote correctly); (2) 5 D-11 empty states render with verbatim CONTEXT.md D-11 copy and CTAs work end-to-end — brand-new family / empty queue / Flow A no-couch (cushion-glow pulses) / all-watched (Show rewatch options resurfaces watched titles) / reject-majority no-#2 (Open Flow B hands off titleId to nominate); (3) Settings notifications shows the 7 new D-12 toggles with BRAND-voice labels + hints, toggling one off suppresses the corresponding push). Bundle into the same staging deploy + multi-device session that covers 14-07 + 14-08. | .planning/phases/14-decision-ritual-core/14-09-SUMMARY.md |
| Two-repo discipline | queuenight commit pending — `C:/Users/nahde/queuenight/functions/index.js` NOTIFICATION_DEFAULTS edit applied in-place but uncommitted on this machine (same pattern as 14-06 deviation 1). User must `cd ~/queuenight && git add functions/index.js && git commit -m "feat(14-09): add 7 D-12 push categories to NOTIFICATION_DEFAULTS"` from a queuenight-equipped session before deploy. | .planning/phases/14-decision-ritual-core/14-09-SUMMARY.md |
| Polish backlog | DR-3 friendly-UI parity — the 7 new D-12 keys surface ONLY in the legacy NOTIFICATION_EVENT_LABELS Settings UI per the DR-3 follow-up override. Phase 12 friendly-UI maps (`NOTIF_UI_TO_SERVER_KEY` / `NOTIF_UI_LABELS` / `NOTIF_UI_DEFAULTS` at js/app.js:128-155) need a follow-up polish plan to mirror them in. RESEARCH §5 flagged the dual-Settings-screen collision risk; a future polish plan must resolve which Settings surface wins (legacy vs friendly-UI) before adding the keys to friendly-UI. | .planning/phases/14-decision-ritual-core/14-09-SUMMARY.md DR-3 override note |
| Deploy gate | **v34/v34.1 cross-repo deploy ritual** — order: (1) `firebase deploy --only functions` from `~/queuenight` (server-side NOTIFICATION_DEFAULTS knows the 7 D-12 keys + 14-06 CF branches live); (2) `firebase deploy --only firestore:rules --project queuenight-84044` from `~/queuenight` (now also covers 14-10's couchInTonight 4th UPDATE branch; per 14-04 Task 4 + 14-10 Task 2 — couchSeating + couchInTonight writes denied without it); (3) `bash scripts/deploy.sh 34.1-roster-control` from couch repo (auto-confirms sw.js CACHE + mirrors to queuenight/public/ + runs firebase deploy --only hosting; this single deploy ships v34.0 + v34.1 surfaces together since neither has been deployed yet). After deploy, edit `changelog.html` to replace `[deploy date YYYY-MM-DD]` placeholder and re-run `bash scripts/deploy.sh 34.1.1-changelog-date` (optional, recommended for clean release notes). Recommended future remediation (out of scope): extend `scripts/deploy.sh` to add `--only hosting,firestore:rules` with mirror-check guard between the two repos AND optionally chain the queuenight `--only functions` deploy if `QUEUENIGHT_PATH` env var is set. | .planning/phases/14-decision-ritual-core/14-04 + 14-09 + 14-10 SUMMARYs |
| HUMAN-VERIFY | gcloud Firestore export setup (run `bash scripts/firestore-export-setup.sh`) | RUNBOOK §K |
| Manual UAT | Trigger account-deletion flow E2E from a real signed-in user | — |
| Tech debt | TD-1 (firebase-functions SDK 4→7), TD-2 (Variant-B storage rules), TD-4 (CSP audit window 2 weeks then enforcement flip), TD-6 (Sentry Replay re-enable +30 days), TD-7 (Firestore index spec record-only) | .planning/TECH-DEBT.md |
| Scheduled | 1-week follow-up agent on 2026-05-02 to check HUMAN-VERIFY progress | trig_015twvB1JTH1zEFvyeQhTmnj |
