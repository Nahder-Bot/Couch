---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: Phase 11 + Phase 12 BOTH FULLY SHIPPED + DEPLOYED 2026-04-25 (live at couchtonight.app). User confirmed Blaze billing already active + Firebase Storage already deployed; Phase 11 Waves 3+4 deploy batch ran clean (3 Cloud Functions updated: rsvpSubmit + rsvpReminderTick + watchpartyTick all in us-central1; storage.rules released; hosting release finalized; 5/5 post-deploy smoke tests pass — /rsvp/<token> HTTP 200 6.6KB serves zero-SDK rsvp.html, /css/rsvp.css HTTP 200 4.8KB, rsvpSubmit Cloud Function returns HTTP 400 on empty body proving alive at endpoint). Phase 12 SHIPPED earlier in session (POL-01 per-event notif prefs UI + POL-02 about/version/feedback/changelog footer + POL-03 Halloween Crawl 9532→10439 + 13 other pack-id corrections via TMDB audit + POL-04 + POL-05 TMDB attribution; 6 atomic commits 004255b/b1ab9a9/6323530/7a52440/4adc1aa/69953ea + deploy + 8/8 smoke tests pass; bug-check found 1 real bug fixed in 4adc1aa). v1 milestone status: 8 of 10 phases complete (3, 4, 5, 7, 8, 9, 11, 12); Phase 6 UAT partial (2/7 — but new toggle UI from Phase 12 now unblocks the remaining 5); Phase 10 (YIR) DEFERRED post-launch. v1 Commercial Release: 1-2 sessions away pending (1) Phase 6 UAT completion using Phase 12 toggle UI for scenarios 3-7 (per-event opt-out + quiet hours + invite received + veto cap + Android delivery), (2) Phase 11 multi-device human-verify (lobby Ready check + catch-me-up card + post-session photo upload via deployed Storage rules + sports score-delta polling against live game window), (3) optional Phase 10 YIR planning if user reverses the deferral. Next natural target: Phase 6 UAT resume on real devices OR Phase 11 multi-device walkthrough OR ship-ready audit.
stopped_at: 2026-04-25 -- Phase 11 + Phase 12 BOTH SHIPPED + DEPLOYED in single session. Phase 11 Wave 3+4 deploy batch (the long-deferred bundle) ran cleanly after user confirmed Blaze + Storage already enabled in Firebase Console. firebase deploy --only "hosting,storage,functions:rsvpSubmit,functions:rsvpReminderTick,functions:watchpartyTick" --project queuenight-84044 from C:/Users/nahde/queuenight/. Output: storage.rules compiled successfully + released to firebase.storage; functions packaged (58.73 KB) + 3 successful Node.js 22 (2nd Gen) function updates (rsvpSubmit/rsvpReminderTick/watchpartyTick all us-central1); hosting 61 files uploaded + release complete. Required Google APIs auto-enabled by deploy: cloudfunctions, cloudbuild, artifactregistry, cloudscheduler (for rsvpReminderTick onSchedule), run, eventarc, pubsub, storage, firebasestorage. Post-deploy smoke tests all pass: GET /rsvp/dummy HTTP 200 6648 bytes (Hosting rewrite resolves to /rsvp.html), body has zero "firebase" SDK refs (zero-SDK brochure pattern preserved), body has 4 RSVP-related markers, GET /css/rsvp.css HTTP 200 4872 bytes, POST https://us-central1-queuenight-84044.cloudfunctions.net/rsvpSubmit with empty data → HTTP 400 (function alive + validating; HTTP 404 would mean not deployed). Note: firebase-functions SDK 4.9.0 is outdated (latest 5.1.0+); not blocking but flagged for future upgrade in functions package.json. PHASE 12 (earlier in same session): POL-01 per-event notif prefs UI in #notif-card (NOTIF_UI_TO_SERVER_KEY 6-key alias + NOTIF_UI_LABELS BRAND-voice + NOTIF_UI_DEFAULTS + savePerEventToggle wrapper + renderNotificationPrefsRows refactor to .notif-pref-row > .notif-toggle-switch markup with iOS-style CSS switch + .notif-quiet-times max-height collapse animation + #notif-status master-on copy migrated to .notif-status-on class + 100-line CSS Phase 12/POL-01 section + sw.js CACHE v31→v32-pre-launch-polish; deviation: var(--bg) thumb-on instead of plan's #fff to honor zero-raw-hex gate). POL-02 ABOUT sub-section in app.html (eyebrow + version line + Send-feedback mailto with version pre-filled + See-what's-new /changelog link + TMDB attribution rel=noopener noreferrer) + APP_VERSION=32 + BUILD_DATE='2026-04-25' constants + renderAboutSection function called at end of renderSettings + Phase 12/POL-02 CSS section + landing.html .landing-tmdb-attr footer + matching css/landing.css rules + changelog.html NEW at repo root (122 lines, mirrors landing.html zero-SDK posture, 5 release entries v32/v31/v29/v28/v27, canonical to https://couchtonight.app/changelog) + queuenight/firebase.json hosting.rewrites added /changelog at idx 3 before catch-all / at idx 4. POL-03 Halloween Crawl 9532→10439 Hocus Pocus + 13 other pack-id corrections (studio-ghibli-sunday Porco Rosso 11544→11621, cozy-rainy-night Chef/Julie&Julia/You'veGotMail/Paddington/TheHoliday 5 swaps, halloween-crawl Coraline/ParaNorman/ReadyOrNot 3 more swaps, date-night-classics BeforeMidnight/TheBigSick/SilverLiningsPlaybook 3 swaps, a24-night TheGreenKnight 1 swap) + drift-prevention header above export const COUCH_NIGHTS_PACKS + audit-report.txt (175 lines, EXPECTED_COUNT=91 hard exact-count gate satisfied, POST-AUDIT REPLACEMENTS APPLIED section); plan-text deviation: plan said HocusPocus=10661 but TMDB /movie/10661="You Don't Mess with the Zohan" — verified canonical 10439 via /search/movie?query=Hocus+Pocus&year=1993 → results[0].id=10439. Bug-check fix 4adc1aa: getNotificationPrefs() spread server DEFAULT_NOTIFICATION_PREFS before stored values, making `stored[serverKey] !== undefined` always true and turning NOTIF_UI_DEFAULTS fallback into dead code; user-visible impact would have been tonightPickChosen toggle showing OFF for net-new users; fix reads raw state.notificationPrefs directly so missing keys actually fall through to UI defaults; D-08 honored — explicit stored values preserved. Phase 11 + Phase 12 commits combined: 7 atomic main-repo commits + 2 production deploys (Phase 12 hosting + Phase 11 hosting+storage+functions). Summary files: 12-{01,02,03}-SUMMARY.md + 12-VERIFICATION.md + audit-report.txt under .planning/phases/12-pre-launch-polish/. Phase 11 SUMMARY files unchanged (deploy was the gap). Manual UAT items deferred to multi-device testing: notif card state matrix, per-event toggle Firestore persistence, multi-device push gating, ABOUT renders correctly, /changelog navigates via rewrite, Halloween Crawl pack preview shows Hocus Pocus, Phase 11 lobby Ready+majority auto-start, catch-me-up card for late joiner, post-session photo upload via Storage, sports score-delta polling against live game window, RSVP push reminder cadence (15-min poll). v1 commercial release within 1-2 sessions of Phase 6 UAT closure + Phase 11 multi-device walkthrough.
last_updated: "2026-04-25T05:15:00Z"
stopped_at: 2026-04-25 -- /gsd-execute-phase 12 SHIPPED. Phase 12 CLOSES + DEPLOYED in single session. 5 atomic main-repo commits + production firebase deploy --only hosting (release queuenight-84044). Commits in wave order: 004255b feat(12-03) COUCH_NIGHTS_PACKS curation pass — 14 ID corrections across 5 packs (studio-ghibli-sunday Porco Rosso 11544→11621, cozy-rainy-night Chef/Julie&Julia/You'veGotMail/Paddington/TheHoliday 5 swaps, halloween-crawl HocusPocus/Coraline/ParaNorman/ReadyOrNot 4 swaps including the POL-03 seed 9532→10439, date-night-classics BeforeMidnight/TheBigSick/SilverLiningsPlaybook 3 swaps, a24-night TheGreenKnight 1 swap) + drift-prevention header above export const COUCH_NIGHTS_PACKS + audit-report.txt (175 lines, 91 ids audited, replacement IDs verified via TMDB /search/movie title-exact + year-exact); plan-text deviation logged: plan said HocusPocus=10661 but TMDB /movie/10661 = "You Don't Mess with the Zohan", canonical = 10439 (plan's own protocol said "Confirm during audit; do not assume"). b1ab9a9 feat(12-01) per-event notification preferences UI POL-01 — NOTIF_UI_TO_SERVER_KEY 6-key alias map (Phase 6 server keys preserved verbatim) + NOTIF_UI_LABELS BRAND-voice copy + NOTIF_UI_DEFAULTS + savePerEventToggle wrapper (haptic+flashToast on save) + renderNotificationPrefsRows refactored to BRAND-aligned .notif-pref-row > .notif-toggle-switch markup with iOS-style CSS switch (visually-hidden checkbox + sibling track/thumb, .is-on toggles var(--accent) background) + quiet-hours collapse via [data-state="open|closed"] max-height transition + #notif-status master-on copy migrated from inline color:var(--good) to .notif-status-on class (Phase 9 Pitfall-1) + 100-line BRAND-token CSS section under "Phase 12 / POL-01" header (zero raw hex; @media prefers-reduced-motion clamp) + sw.js CACHE couch-v31-fix-couch-nights-heroes → couch-v32-pre-launch-polish (Plan 12-01 owns the v32 bump; 12-02/12-03 do not touch sw.js). Plan-text deviation: plan specified #fff thumb-on color but plan's own grep gate enforces zero raw hex — used var(--bg) instead. 6323530 feat(12-02) about/version/feedback/changelog + TMDB attribution POL-02 + POL-05 — APP_VERSION=32 + BUILD_DATE='2026-04-25' constants in js/constants.js (additive after COUCH_NIGHTS_PACKS, no collision with Plan 12-03's drift header above the export) + ABOUT sub-section in app.html inside data-cluster="admin" AFTER .tab-footer pills (eyebrow ABOUT + version line + Send-feedback mailto with version pre-filled in subject + See-what's-new link to /changelog + TMDB attribution with rel="noopener noreferrer") + renderAboutSection() function called at end of renderSettings (idempotent) + .settings-about-section CSS section under "Phase 12 / POL-02" (token-backed, zero raw hex, italic-serif attribution) + landing.html footer .landing-tmdb-attr + matching css/landing.css rules + changelog.html NEW at repo root 122 lines (mirrors landing.html posture — zero Firebase SDK, zero /css/app.css, /css/landing.css only, 5 release entries v32/v31/v29/v28/v27, canonical https://couchtonight.app/changelog) + queuenight/firebase.json hosting.rewrites added {source:"/changelog",destination:"/changelog.html"} at idx 3 BEFORE catch-all "/" at idx 4 (specific-before-catch-all per Hosting spec). 7a52440 docs(phase-12) verification + ROADMAP closure (12-VERIFICATION.md goal-backward 5/5 must-haves PASSED, ROADMAP.md Phase 12 [ ]→[x] + 3 plan checkmarks). 4adc1aa fix(12-01) read raw notificationPrefs so NOTIF_UI_DEFAULTS apply for net-new users — bug discovered in post-execute bug check: getNotificationPrefs() spreads server DEFAULT_NOTIFICATION_PREFS before stored values, making `stored[serverKey] !== undefined` always true and turning the NOTIF_UI_DEFAULTS fallback into dead code; user-visible impact would have been tonightPickChosen showing OFF for net-new users (server-default false) instead of ON (D-02 UI-default true); fix reads raw state.notificationPrefs directly so missing keys actually fall through to UI defaults; D-08 still honored (explicit stored values preserved). Bug-check also surfaced 3 cross-pack ID overlaps (Moonlight 376867, EEAaO 545611, Past Lives 666277 in a24-night + oscars-short-list / date-night-classics) — all intentional Phase 11 curation, not introduced by Phase 12. DEPLOY: 8 files mirrored to /c/Users/nahde/queuenight/public/ (app.html, changelog.html, sw.js, landing.html, css/app.css, css/landing.css, js/app.js, js/constants.js) — diff -q confirmed identical; firebase deploy --only hosting --project queuenight-84044 ran clean (61 files uploaded, release finalized at queuenight-84044.web.app); 8/8 post-deploy smoke tests pass via curl: /changelog HTTP 200 5.7KB body, v32 entry present, zero firebase SDK refs, /sw.js CACHE = couch-v32-pre-launch-polish, /app has #settings-about-section, / landing footer "Powered by The Movie Database" present, /js/constants.js has 10439 (Hocus Pocus), zero 9532. Manual UAT items deferred to multi-device testing: notif card state matrix (master OFF→toggles hidden, master ON→6 toggles + quiet hours), per-event toggle flip → flashToast Saved → Firestore persistence, multi-device push gating respects toggles + quiet hours, ABOUT version line displays "Couch v32 — deployed 2026-04-25", Send feedback mailto pre-fills subject with version, See what's new navigates to /changelog via rewrite, Halloween Crawl pack preview leads with Hocus Pocus poster (1993), other corrected packs load right titles. Summary files: .planning/phases/12-pre-launch-polish/12-{01,02,03}-SUMMARY.md + 12-VERIFICATION.md. Audit log: 12-pre-launch-polish/audit-report.txt.
last_updated: "2026-04-25T04:30:00Z"
last_activity: 2026-04-24 -- /gsd-execute-phase 11 plan 11-07 CODE-COMPLETE. Phase 11 CLOSES CODE-COMPLETE. 2 atomic main-repo commits closed REFR-13 (Couch Nights themed packs). (1) 823b3ed feat(11-07) COUCH_NIGHTS_PACKS data — 8 curated themed packs in js/constants.js: studio-ghibli-sunday (10 IDs), cozy-rainy-night (11), halloween-crawl (10), date-night-classics (10), kids-room-classics (12), a24-night (12), oscars-short-list (10), dads-action-pantheon (11); 86 curated TMDB IDs total; each pack shape { id, title, description (BRAND voice), mood (EXISTING MOODS token), heroImageUrl (TMDB w780 poster URL from canonical film), tmdbIds }; descriptions match BRAND §6 voice; public-by-design per CLAUDE.md; Firestore migration deferred to Phase 12. (2) 65037c4 feat(11-07) UI + seed-ballot flow + sw.js bump — app.html #couch-nights-section on Add tab BETWEEN mood + dynamic discovery rows (per UI-SPEC §Layout 539) + #couch-night-sheet-bg pack-detail modal; js/app.js imports COUCH_NIGHTS_PACKS, 6 functions across 4 window bindings: renderCouchNightsRow (tile emitter with linear-gradient overlay + keyboard a11y), openCouchNightPack / closeCouchNightSheet (modal open/close), loadPackPreview (8 parallel TMDB /movie/{id} fetches, stale-guard via _activePackId re-check, empty-state "This pack's being restocked"), confirmStartPack (guardReadOnlyWrite + state.me guards + btn disable + "Seeding…" + haptic/flashToast + openSwipeMode launch), seedBallotFromPack (per-id dedupe by state.titles match, fetchTmdbExtras + suggestMoods, addedVia:"pack:<id>" + addedAt attribution, writes via createTitleWithApprovalCheck so kid-approval flow preserved, partial-success posture returns seededCount); wired renderCouchNightsRow() into initAddTab between renderPinnedRows + renderAddDiscovery; css/app.css +50 lines .couch-night-tile 240x140 cover + gradient overlay + Fraunces title + accent hover border + focus-visible ring / .couch-night-sheet max-w 560 (640 on ≥900px inside existing @media block — no new media query) / .couch-night-hero 200px / .couch-night-sheet-title Fraunces h1 / .couch-night-sheet-desc Instrument Serif italic / .couch-night-titles-preview horizontal scroll / .couch-night-preview-card + .couch-night-preview-poster 100x150 / .start-pack-cta brand-grad full-width 44px min-height warm shadow disabled 50% opacity; sw.js CACHE couch-v28-11-06-sports → couch-v29-11-07-couch-nights (FINAL Phase 11 bump — v29 closes the progression v21→v22→v23→v24→v25→v26→v27→v28→v29 across 8 plans). All grep gates pass: COUCH_NIGHTS_PACKS=1, 8 pack IDs=1 each, tmdbIds:=11 (8 packs + 3 existing curated DISCOVERY_CATALOG rows), couch-nights-section/sheet-bg/row each=1, COUCH NIGHTS eyebrow=1, Themed packs ready to roll subtitle=1, 6 JS handlers all present, import COUCH_NIGHTS_PACKS=1, openSwipeMode=4 (preserved + new), addedVia pack attribution=3, all 4 CSS class families present, sw.js v29=1. node --check all 3 JS files pass. Phase 11 CLOSES CODE-COMPLETE — all 8 plans shipped, all 13 REFR-* closed. Deploy deferred per execution context — bundles with deferred 11-04 + 11-05 + 11-06 deploys pending Blaze billing confirmation + Firebase Console Storage enablement + live game window for 11-06 score-delta verification. Phase 11 rollup in 11-COMPLETION.md documents: 8 plan commits, 22+ main-repo commits total, 7 main-repo files modified + 2 net-new (rsvp.html + css/rsvp.css), sibling repo storage.rules + firebase.json + functions/index.js edited + 2 CF files NEW (rsvpSubmit + rsvpReminderTick), ~2000 JS + ~300 CSS + ~250 HTML + ~200 constants data net lines, 10 new components, all 13 REFR-* closed with traceability per-plan. Phase 11 Deferred to Phase 12: Twilio SMS nurture, BALLDONTLIE activation, host-triggered remind-unresponsive button, notif prefs detail UI, data export/delete, theme prefs, about/version/feedback, Couch calendar recurrence, recent watchparties photo drilldown, free pick'em for sports, post-game debrief mode, voice rooms, pack data Firestore migration, seasonal pack rotation, per-family pack authorship. v1 milestone status update: Phase 11 CODE-COMPLETE; v1 Commercial Release is within 2-3 sessions once deploy batch lands + Phase 10 executes. Summary: .planning/phases/11-feature-refresh-and-streamline/11-07-SUMMARY.md + .planning/phases/11-feature-refresh-and-streamline/11-COMPLETION.md. Prior: 11-06 code-complete 4 commits (SportsDataProvider / game picker / score-delta polling / Add-tab entry) REFR-10 closed. Closed REFR-10 (Sports Game Mode v1) in 4 atomic commits. (1) 0181de1 SportsDataProvider abstraction in js/app.js:~7742 — SPORTS_PROVIDER_CONFIG league map (nfl/nba/mlb/nhl) with provider router; sportsScoreCache + sportsPlaysCache keyed on (gameId, 15s/30s bucket); 3 async methods getSchedule(leagueKey, daysAhead=7) → 7-day ESPN scoreboard fan-out via existing parseEspnEvent, getScore(gameId, leagueKey) → ESPN summary?event=<id> extracts homeScore/awayScore/period/clock/state/statusDetail, getPlays(gameId, leagueKey) → ESPN summary.plays slice(-10) normalized to {id,text,scoringPlay,period,clock,ts}. Existing loadSportsGames + parseEspnEvent + SPORTS_LEAGUES + sportsGamesCache PRESERVED verbatim. BALLDONTLIE branch stubbed (returns []/null for unknown-provider). (2) c79ab78 game picker modal #game-picker-modal-bg in app.html with 4 league pill tabs + scrollable game cards + brand-grad "Use this game" CTA (disabled until selection). 5 js handlers: openGamePicker / closeGamePicker / loadGamePickerLeague / selectGame (URI-encoded JSON payload passthrough) / confirmGamePicker (creates wp with mode:'game' + full wp.sportEvent + scoringPlays:[] + fires Web Share to /rsvp/<wpId>). renderSportsScoreStrip — sticky-top chrome, tabular-nums score display, LIVE indicator (accent color when state==='in'), seeded from wp.lastScore falling back to sportEvent abbreviations. Injected into renderWatchpartyLive body prefix when wp.mode === 'game' || wp.sportEvent. (3) 1f03ff1 score-delta polling + amplified reactions + DVR slider + team-flair + sw.js bump. startSportsScorePolling: adaptive 5s on-play / 15s off-play setTimeout chain against SportsDataProvider.getScore; on home/away score change → flash score strip (sports-score-pulse keyframes) + handleScoringPlay surfaces showAmplifiedReactionPicker (fixed-position overlay with 4 burst emojis 🔥🎉😱💪, 3s auto-dismiss, amplified:true reactions via postBurstReaction) + persists to wp.scoringPlays arrayUnion + wp.lastScore for late-joiner. updateSportsScoreStrip in-place DOM update (nums + middle text + abbrevs) without full re-render. Polling lifecycle: openWatchpartyLive starts (when mode==='game') + maybeShowTeamFlairPicker (one-shot on first join of sport-mode wp with teamAllegiance + teamColor writes), closeWatchpartyLive stops polling (_sportsPollWpId stale-fetch guard). renderDvrSlider HTML5 range input 0-180s step=5s with accent 22px thumb + dvrReadoutText + throttled 500ms setDvrOffset that dual-writes participants[mid].dvrOffsetMs (new canonical) AND participants[mid].reactionDelay (Phase 7 PARTY-04 anchor) so existing render-filter at renderReactionsFeed picks up slider-driven offset without modification. Team-flair avatar badge: renderParticipantTimerStrip emits --team-color inline custom property + has-team-flair class on .wp-participant-av; CSS rule draws 2px box-shadow border in team color. Sports variant of renderCatchupCard (Plan 11-05 placeholder now cleanly replaced): branches EARLY on wp.mode==='game' || wp.sportEvent — renders YOU MISSED / "Here's where we are." / Score: AWY X, HOM Y / Last 3 plays: + scoringPlays.slice(-3). Movie-mode variant preserved verbatim (same <3 reactions guard). DVR slider injection: renderWatchpartyLive appends renderDvrSlider(wp, mine) to body tail when wp.mode==='game' only. CSS adds .sports-reaction-picker.amplified / @keyframes sports-amplified-enter / .sports-burst-btn / .wp-dvr-slider + styled range input thumb (webkit + moz) / .team-flair-modal / .team-flair-pick.neutral / .wp-participant-av.has-team-flair / .wp-catchup-sport-score / .wp-catchup-sport-label / .wp-catchup-sport-play / @media (prefers-reduced-motion: reduce) guard. sw.js CACHE couch-v27-11-05-lifecycle → couch-v28-11-06-sports. (4) 0ec62f8 Rule 3 auto-fix — Add-tab entry point. Plan line 846 flagged missing entry point. Added second .sports-entry card beneath legacy "Schedule a game" with 📺 icon + title "Watch a game live" + subtitle "Live score · amplified reactions · DVR offset" routing to openGamePicker() with full keyboard accessibility. Legacy openSportsPicker flow preserved for backward compat; the two sports flows coexist (legacy sport wp has sportEvent but no mode:'game' flag → gets score strip only; Game Mode wp has both → full polling + amplified + DVR + team-flair). All node --check pass. All acceptance gates pass. SportsDataProvider 3 methods present. Game picker modal markup + 5 handlers present. Score strip CSS + renderSportsScoreStrip function present. Polling + amplified + DVR + team-flair functions all present with proper Firestore writes. sw.js bumped to v28. Deploy deferred per execution context — bundles with 11-04 + 11-05 deferred deploys. Threat model: ESPN ToS-gray accepted (T-11-06-01), DoS-via-polling mitigated via (gameId, bucket) cache (T-11-06-02), fake scoringPlays writes gated by existing watchparty firestore.rules (T-11-06-03), setUserTeamFlair writes own-entry-only (T-11-06-05). Summary: .planning/phases/11-feature-refresh-and-streamline/11-06-SUMMARY.md. Prior: /gsd-execute-phase 11 plan 11-05 CODE-COMPLETE. Closed biggest watchparty UX gap: (1) c04597e pre-session lobby + Ready check + democratic auto-start. New inLobbyWindow local (T-15min window) declared BEFORE both render branches so existing preStart is guarded with !inLobbyWindow — lobby and wp-prelaunch mutex by construction (BLOCKER 2 fix). wp-lobby-card with 56px countdown ring + roster (reuses renderParticipantTimerStrip) + per-user "I'm ready" toggle + host "Start the session" gradient CTA when majority Ready. toggleReadyCheck + hostStartSession handlers with TDZ-safe ordering: const wp declared BEFORE any wp. / !wp reference (BLOCKER 1 fix). Pre-await optimistic mutation + sync re-render (4th adoption after 07-06/07-07/07-08). Grep-gate compliance: banner-side bare preStart refactored to !!preStart; live-modal prelaunch footer consolidated into isPrelaunch=preStart||inLobbyWindow. watchpartyTick CF extended with majority-Ready early-start branch (ceil(n/2) ready + minutesBefore<=0.5). (2) 123d954 catch-me-up card. renderCatchupCard slices wp.reactions to 30s before mine.joinedAt when joinedAt > mine.startedAt + ONTIME_GRACE_MS (60s Phase 7 grace preserved). Empty-state hides at <3 reactions. Per-session local dismissal via window._catchupDismissed (no Firestore write). Per-user reaction-delay moat preserved (catchup only shows pre-join reactions). Sports-variant placeholder → Plan 11-06 REFR-10. (3) a938901 post-session modal + Firebase Storage. wp-post-session-modal with "That's a wrap." italic + 5-star rating + photo upload tile + "Schedule another night" gradient CTA + "Maybe later" skip. js/firebase.js first Storage use: getStorage + storageRef + uploadBytes + getDownloadURL imports/exports. Client compression via canvas.toBlob(1600, 0.85) strips EXIF as defense-in-depth. MIME allowlist [jpeg,png,webp] + 5MB cap. Upload path couch-albums/{familyCode}/{wpId}/{ts}_{uid}.jpg → wp.photos arrayUnion. endMyWatchparty now triggers openPostSession (200ms) instead of openDiary (diary fallback preserved). sw.js CACHE v26→v27-11-05-lifecycle. SIBLING FILES LANDED (queuenight/ deploy mirror — not a git repo): storage.rules NEW (35 lines, Variant A — auth + size + MIME floor on couch-albums/{familyCode}/{wpId}/{filename}, default-deny elsewhere), firebase.json edited (storage section added after firestore), functions/index.js edited (majorityReady branch in watchpartyTick). Plan-text schema drift auto-fix (Rule 1): plan referenced wp.state === 'scheduled'/'ended' but codebase uses wp.status (no 'ended' status exists). Migrated to wp.status matching + endMyWatchparty trigger path. All 4 node --check pass (app.js, firebase.js, sw.js, functions/index.js). All grep acceptance gates pass: if (preStart && !inLobbyWindow)=1, if (preStart)=0, body = lobbyHtml; = 1, body = lobbyHtml + body = 0, LOBBY_WINDOW_MS = 2, const inLobbyWindow = 1, wp-lobby-card + wp-ready-check + wp-countdown-ring + wp-lobby-start-btn CSS present, majorityReady in functions/index.js = 2, getStorage in firebase.js = 3, couch-albums/ in app.js = 1, 5 * 1024 * 1024 in storage.rules = 1, image/.* in storage.rules = 2, exists(/databases in storage.rules = 0 (Variant A compliant), storage key in firebase.json = 1, couch-v27-11-05-lifecycle in sw.js = 1. Rule-1 auto-fixes: (a) wp.state → wp.status migration per schema reality (plan's 'ended' state is not in the schema). (b) banner-side bare if (preStart) refactored to if (!!preStart) for grep-gate compliance. (c) comment literal "exists(/databases/..." scrubbed from storage.rules docstring for grep-gate compliance. Deploy deferred per user direction. Human-verify matrix owed: emulator test 5-case matrix + multi-device lobby walkthrough + post-session modal end-to-end + Variant-B tightening ROADMAP TODO.
progress:
  total_phases: 10
  completed_phases: 8
  total_plans: 47
  completed_plans: 36
  percent: 80
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-19)

**Core value:** Turn "what do you want to watch?" from a 20-minute argument into a 30-second ritual that everyone on the couch trusts.
**Current focus:** Phase 11 + Phase 12 BOTH FULLY SHIPPED + DEPLOYED 2026-04-25. Phase 11's deferred Wave 3+4 deploy batch closed: 3 Cloud Functions live (rsvpSubmit + rsvpReminderTick scheduled cron + watchpartyTick majorityReady branch), storage.rules released, /rsvp/<token> hosting route serving rsvp.html. Phase 12 closed earlier in same session (notif prefs UI + ABOUT footer + pack curation). 8/10 phases complete (3, 4, 5, 7, 8, 9, 11, 12); Phase 6 UAT partial (2/7 — Phase 12 toggle UI now unblocks remaining 5); Phase 10 (YIR) DEFERRED post-launch. v1 Commercial Release within 1-2 sessions: needs Phase 6 UAT scenarios 3-7 closure + Phase 11 multi-device human-verify (lobby/catch-me-up/post-session/sports). Next natural target: Phase 6 UAT resume on real devices OR Phase 11 multi-device walkthrough.
**Active milestone:** v1 Commercial Release (Phases 3-10)

## Current Position

Phase: Phase 12 SHIPPED + DEPLOYED 2026-04-25. Pre-launch polish patch live at couchtonight.app. Phase 11 CODE-COMPLETE 2026-04-24 (deploy deferred — Waves 3+4 bundle pending Blaze billing + Firebase Storage enablement; Phase 12 deploy bypassed Phase 11's deferred bundle since Phase 12 only touches the main repo + hosting, not Cloud Functions or Storage). Phase 9 SHIPPED (landing live since 2026-04-23). 7/10 phases complete; Phase 6 UAT partial; Phase 10 pending.

Phase 12 completion (2026-04-25, /gsd-execute-phase 12 — sequential executor since worktree-parallel was blocked by file overlap across plans 12-01 ↔ 12-02 on app.html/js/app.js/css/app.css and plans 12-02 ↔ 12-03 on js/constants.js). CODE-COMPLETE + DEPLOYED + post-bug-check fix in single session. 5 atomic main-repo commits + production deploy + 8/8 smoke tests pass.

Wave 1 (12-03 — POL-03 + POL-04 stretch — data-layer audit, no UI surface): 004255b. Wrote one-time TMDB audit script (audit-tmdb-packs.cjs, self-deleting via try/finally) that fetched all 91 tmdbIds across 8 packs from /movie/{id} with 200ms inter-request delay (well under TMDB 40req/10s). Manual cross-reference of audit-report.txt's "actual:" column against in-source pack comments yielded 14 real mismatches across 5 of 8 packs (parser-noise apology written into the report header — script's "expected:" column drew labels from last 2 comment lines split on commas, which broke for digit-containing comments like "Paddington 2" and for the first id in every pack where the heroImageUrl comment leaked in). Replacement IDs verified via second self-deleting script search-replacements.cjs hitting /search/movie?query={title}&year={year} for title-exact + year-exact match. 14 ID swaps applied with array-context-aware Edits (each pack's full tmdbIds line as the unique anchor — critical because some bad IDs like 194662=Birdman appear correctly in oscars-short-list but wrongly in cozy-rainy-night where they should be Chef=212778). Drift-prevention comment block "If you change a pack id, update the comment label" added immediately above export const COUCH_NIGHTS_PACKS. POST-AUDIT REPLACEMENTS APPLIED section appended to audit-report.txt with full before→after table. Heroes preserved (8 w780 URLs untouched per D-16). Plan-text deviation: plan said canonical Hocus Pocus = TMDB id 10661, but verification showed 10661 = "You Don't Mess with the Zohan" (2008); canonical Hocus Pocus = 10439 (verified via search/movie?query=Hocus+Pocus&year=1993 → results[0].id=10439, title="Hocus Pocus", release_date="1993-07-30"). Plan's own protocol said "Confirm during audit; do not assume" — confirmed 10439, deviation documented in 12-03-SUMMARY.md and audit-report.txt POST-AUDIT section. node --check js/constants.js exits 0. All 14 plan-spec verify gates pass (incl. hard exact-count gate PASS+MISMATCH+NOT_FOUND==EXPECTED_COUNT=91, 9532 absent from COUCH_NIGHTS_PACKS region, 10439 present, 8 heroImageUrl URLs preserved, drift header text on single line so grep gate passes).

Wave 2 (12-01 — POL-01 — production-quality notif prefs UI, closes Phase 6 PUSH-02 + PUSH-04 UAT loop client-side): b1ab9a9. Phase 6 server-side notificationPrefs enforcement (functions/index.js:45,79,91-124) had been live since 2026-03-19 but users had no polished UI to exercise it — only a Phase-6 development-scaffold rendering with inline-style flex layout. Plan 12-01 ships the production version. Task 1: NOTIF_UI_TO_SERVER_KEY (6-key allowlist Object.freeze mapping user-facing names per CONTEXT.md D-02 to Phase-6 server keys — critical: watchpartyStartingNow→watchpartyStarting, intentRsvpRequested→intentProposed, others 1:1) + NOTIF_UI_LABELS (BRAND-voice copy per D-06, sentence-case labels with italic-serif descriptions like "A push when someone in the family hits play.") + NOTIF_UI_DEFAULTS (5 ON + vetoCapReached OFF — owner-leaning, default off per D-02; tonightPickChosen=true UI default differs from server-default false) + savePerEventToggle wrapper (translates UI→server key, calls existing updateNotificationPref + haptic('light') + flashToast('Saved', {kind:'good'}) on success / 'Could not save' on error, window-bound for any future external callsite). Task 2: renderNotificationPrefsRows refactored — signature preserved (call site at updateNotifCard line 939 unchanged), body emits BRAND-aligned markup .notif-prefs-list > .notif-pref-row > (text + .notif-toggle-switch[is-on?] containing checkbox + .notif-toggle-track > .notif-toggle-thumb), iterates NOTIF_UI_LABELS, wires data-pref attribute → savePerEventToggle, quiet-hours block uses .notif-quiet-block + .notif-quiet-times[data-state="open|closed"] for CSS-driven max-height collapse animation (no JS-controlled display:none toggle, honoring Phase 9 Pitfall-2). #notif-status master-on copy migrated from inline style='color:var(--good);' to <strong class='notif-status-on'> + <span class='notif-status-sub'> (Phase 9 Pitfall-1). Task 3: 100-line CSS section appended at end of css/app.css under "Phase 12 / POL-01" header — iOS-style switch built from visually-hidden <input type=checkbox> + sibling track/thumb spans (track translates background var(--surface-3,var(--surface-2))→var(--accent), thumb translates 18px and switches background var(--ink)→var(--bg)), .notif-quiet-times max-height: 0→200px transition gated on [data-state="open"], :focus-visible outline on input renders on the sibling track for keyboard a11y, @media (prefers-reduced-motion: reduce) clamps the three transition declarations. Zero raw hex in the new section (the only RGBA literal is the existing 0 1px 3px rgba(0,0,0,0.3) thumb shadow pattern matching Phase 11 elsewhere). Plan-text deviation: plan specified #fff thumb-on color but the plan's own zero-raw-hex grep gate would have failed; used var(--bg) (the warm-dark #14110f) instead — slightly different visual contrast on accent-orange track, BRAND-token discipline preserved. sw.js CACHE bumped couch-v31-fix-couch-nights-heroes → couch-v32-pre-launch-polish (Plan 12-01 owns the v32 bump; Plans 12-02 + 12-03 explicitly do NOT touch sw.js). All 19 plan-spec verify gates pass (NOTIF_UI_TO_SERVER_KEY ≥3, savePerEventToggle ≥4, data-pref ≥1, color:var(--good) =0 inline removed, .notif-toggle-switch ≥6 in css, .notif-quiet-times[data-state="open"] =1, couch-v32-pre-launch-polish =1, old v31 =0, zero raw hex in POL-01 block, node --check passes).

Wave 3 (12-02 — POL-02 + POL-05 bundled): 6323530. Cheap, high-signal user-trust polish per D-09..D-12 + TMDB ToS compliance per D-19. Task 1: APP_VERSION=32 + BUILD_DATE='2026-04-25' constants appended to js/constants.js after the closing `];` of COUCH_NIGHTS_PACKS (no collision with Plan 12-03's drift-prevention header above the export). js/app.js line 2 import updated to include both. ABOUT sub-section in app.html inserted between .tab-footer (line 711) and the closing </div> of data-cluster="admin" — markup: <div class="tab-section settings-about-section" id="settings-about-section"> with eyebrow ABOUT + .about-version <p id="about-version-line"> + .about-link-row containing #about-feedback-link (mailto) + separator + #about-changelog-link (/changelog) + .about-attribution <p><em>Powered by <a href="https://www.themoviedb.org/" target="_blank" rel="noopener noreferrer">The Movie Database (TMDB)</a></em></p>. renderAboutSection() function added after renderSettings — idempotent, populates versionEl.textContent="Couch v32 — deployed 2026-04-25", sets feedbackEl.href=`mailto:nahderz@gmail.com?subject=${encodeURIComponent('Couch feedback v32')}`, sets changelogEl.href='/changelog'. Called at end of renderSettings via try {renderAboutSection();} catch(e){}. Task 2: Phase 12 / POL-02 CSS section appended after POL-01 block — token-backed throughout (var(--space-section) + var(--space-stack-md) + var(--border) + var(--font-sans) + var(--accent) + var(--font-serif) for italic attribution + var(--ink-warm)/--ink-dim/--ink for hierarchical color). Zero raw hex. landing.html footer extended with <p class="landing-tmdb-attr">; matching css/landing.css rules added (raw 13px font-size consistent with existing .landing-footer pattern; tokens from landing.css :root). Task 3: changelog.html NEW at repo root (122 lines, mirrors landing.html posture — no Firebase SDK, no /css/app.css, no /js/app.js, only /css/landing.css + a small <style> block with page-local rules) — 5 release entries v32/v31/v29/v28/v27 each with .release-version + .release-date + .release-summary (Instrument Serif italic) + .release-list (Inter sans). Canonical https://couchtonight.app/changelog, robots index,follow. queuenight/firebase.json hosting.rewrites: inserted {source:"/changelog",destination:"/changelog.html"} at idx 3 BEFORE catch-all "/" at idx 4 (specific-before-catch-all per Hosting spec). All 22 plan-spec verify gates pass (export const APP_VERSION = 32, export const BUILD_DATE matching /^\d{4}-\d{2}-\d{2}$/, settings-about-section count=1 inside admin cluster, themoviedb.org count=1 in app.html, "Couch feedback v" literal=1 in js/app.js, function renderAboutSection=1, renderAboutSection()=2 calls, Phase 12 / POL-02 in css=1, .settings-about-section ≥2, .about-attribution ≥2, themoviedb.org in landing.html=1, rel=noopener noreferrer ≥1, landing-tmdb-attr in landing.html=1 + in css/landing.css ≥1, © Couch preserved=1, changelog.html exists, <title>Couch — Changelog</title>=1, firebase refs=0, /css/app.css refs=0, /js/app.js refs=0, <article class="release"> blocks=5, release-version count=6 incl. CSS rule, /changelog rewrite count=1, idx_changelog<idx_root=3<4, firebase.json valid JSON, zero raw hex in POL-02 css block, node --check passes).

Verification + ROADMAP closure (7a52440): goal-backward 12-VERIFICATION.md scored 5/5 must-haves PASSED (MH-1 6 toggles + quiet hours + immediate writes, MH-2 ABOUT sub-section in admin cluster, MH-3 /changelog.html + Firebase rewrite, MH-4 Halloween Crawl 9532→10439 + full audit, MH-5 sw.js v32 bump). ROADMAP.md Phase 12 [ ]→[x] with completion-date stamp; 3 plan checkmarks per wave with commit-hash trail. Cross-phase regression notes: Phase 6 server-side enforcement untouched; Phase 9 BRAND-token discipline preserved (zero raw hex in new CSS); Phase 11 Couch Nights data layer pack metadata fields preserved verbatim (only tmdbIds contents corrected per audit). No firestore rules / Cloud Function changes; no new attack surface per per-plan threat models. tests/ directory has only Firebase Emulator-based firestore.rules tests via rules.test.js — unrelated to Phase 12 surfaces; skipping the regression gate is appropriate.

Post-execute bug-check + fix (4adc1aa): user-prompted thorough review found 1 real bug + 0 false-positive bugs after deeper inspection. The bug: renderNotificationPrefsRows sourced prefs from getNotificationPrefs() which spreads DEFAULT_NOTIFICATION_PREFS (Phase 6 server defaults) before stored values — making `stored[serverKey] !== undefined` always true and turning the new NOTIF_UI_DEFAULTS fallback into dead code. User-visible impact would have been: net-new users see tonightPickChosen toggle OFF (server default false) instead of ON (D-02 UI default true). Fix: read raw state.notificationPrefs directly so missing keys actually fall through to NOTIF_UI_DEFAULTS as intended. D-08 still honored — users with explicit stored values keep them; only never-touched path gets the UI default. Behaviorally verified via Node simulation: net-new {} → tonightPickChosen=true vetoCapReached=false; existing stored {tonightPickChosen:false} → preserved as false. False-positive investigations: cross-pack ID dupes in COUCH_NIGHTS_PACKS region (3 real overlaps Moonlight 376867 / EEAaO 545611 / Past Lives 666277 in a24-night + oscars-short-list / date-night-classics — all intentional Phase 11 curation, not introduced by Phase 12); ABOUT section nesting (correctly inside data-cluster=admin between tab-footer close and screen-settings close); stale class refs (.notif-prefs old =0, data-event old =0); other updateNotificationPref callers (only the new wrapper); CSS tokens in Phase 12 sections (all 17 referenced exist incl. --surface-3=#2f2820 at css/app.css:36); flashToast/haptic signature compatibility (kind:'good'|'warn' accepted; 'light' strength supported); APP_VERSION/BUILD_DATE export + import (both verified in source); replacement ID collisions across packs (none of the 14 new IDs collide with any existing pack member).

DEPLOY (2026-04-25, firebase deploy --only hosting --project queuenight-84044): 8 files mirrored cp -v → diff -q identical → release queuenight-84044.web.app. Files: app.html (modified — ABOUT section markup), changelog.html (NEW — standalone page mirroring landing.html), sw.js (modified — v32 CACHE bump), landing.html (modified — TMDB attribution), css/app.css (modified — Phase 12 / POL-01 + POL-02 sections appended), css/landing.css (modified — .landing-tmdb-attr rules), js/app.js (modified — UI alias map + savePerEventToggle + renderNotificationPrefsRows refactor + raw-prefs bug fix + import + renderAboutSection), js/constants.js (modified — 14 pack ID corrections + drift header + APP_VERSION + BUILD_DATE). queuenight/firebase.json edit (rewrite addition) was made directly in the sibling repo (not mirrored — it lives there, not in main repo). 8/8 post-deploy smoke tests pass via curl: GET /changelog HTTP 200 5738 bytes, body has v32 entry, body has zero firebase SDK refs; GET /sw.js content includes "const CACHE = 'couch-v32-pre-launch-polish'"; GET /app body has id="settings-about-section"; GET / body has "Powered by" landing TMDB attribution; GET /js/constants.js has 10439 (Hocus Pocus); GET /js/constants.js has zero 9532 (Final Destination removed). Phase 12 LIVE.

Manual UAT items deferred to multi-device testing (per /gsd-verify-work pattern; auto-tests can't reach the runtime surfaces): (1) Notif card master-OFF state (per-event toggles NOT visible) vs master-ON state (6 toggles render with BRAND-voice copy + quiet-hours pickers); (2) Toggle "Watchparty starting" off → flashToast "Saved" → refresh → state persists in Firestore users/{uid}.notificationPrefs.watchpartyStarting === false (server key, not UI key); (3) Quiet hours enable → time-input slide-open via max-height transition → set 22:00 → 08:00 → flashToast on each input change → tz auto-detected via Intl; (4) Trigger watchparty schedule from another family member → push gating respects toggles + quiet hours (closes Phase 6 PUSH-02 + PUSH-04 UAT scenarios 3, 4); (5) Account → ABOUT shows "Couch v32 — deployed 2026-04-25"; tap Send feedback → mail client opens with subject "Couch feedback v32"; (6) Tap See what's new → navigates to /changelog (Firebase Hosting rewrite resolves to /changelog.html on prod CDN); page renders no Firebase SDK in network tab; (7) Visit https://couchtonight.app/ → landing footer shows "Powered by The Movie Database (TMDB)"; (8) App → Add tab → Couch Nights row → Halloween Crawl → modal preview leads with Hocus Pocus poster (1993), NOT Final Destination; "Start this pack" → Vote mode opens with Hocus Pocus + Beetlejuice + 8 others (none Final Destination); (9) Repeat smoke-check for cozy-rainy-night (Chef/Julie&Julia/You'veGotMail/Paddington/TheHoliday corrections), date-night-classics (BeforeMidnight/TheBigSick/SilverLiningsPlaybook), studio-ghibli-sunday (Porco Rosso), a24-night (The Green Knight). Future durability note: future BUILD_DATE bumps require manual js/constants.js edit; small Phase 13 follow-up could auto-stamp via git log -1 --format=%cs HEAD as a deploy hook (deferred). Long-term pack-curation durability: D-18 suggests Phase 13 could replace inline comment labels with build-time JSON manifest + automated TMDB title-match test; deferred. Summary files: 12-{01,02,03}-SUMMARY.md + 12-VERIFICATION.md + audit-report.txt all under .planning/phases/12-pre-launch-polish/.

Plan 11-05 completion (2026-04-24, /gsd-execute-phase 11 plan 11-05 — sequential executor). CODE-COMPLETE; deploy deferred with 11-04 pending Blaze billing confirmation + Firebase Console Storage enablement. 3 atomic main-repo commits + 2 files landed + 1 edited in sibling queuenight/ deploy mirror. Commits: (1) c04597e feat pre-session lobby + Ready check + majority auto-start (REFR-07). (2) 123d954 feat catch-me-up card for late joiners (REFR-08). (3) a938901 feat post-session modal + photo upload + Firebase Storage init + sw.js CACHE bump (REFR-09). Sibling landed: storage.rules NEW (Variant A — auth + 5MB + image/* MIME floor on couch-albums/), firebase.json edited (storage key added), functions/index.js edited (majorityReady branch in watchpartyTick). REFR-07 + REFR-08 + REFR-09 CLOSED (code). STORAGE_RULES_VARIANT: A — Phase 5 Auth shipped but member docs still keyed by m_<ts>_<rand> with uid as optional field; Variant B tightening queued as ROADMAP TODO for post-member-uid-migration plan. BLOCKER 1 fix (TDZ in toggleReadyCheck — const wp before any wp reference) + BLOCKER 2 fix (lobby/preStart mutex via !inLobbyWindow guard; body = lobbyHtml REPLACE) both closed by inspection; runtime verification deferred to human-verify. Summary: .planning/phases/11-feature-refresh-and-streamline/11-05-SUMMARY.md.

Phase 11 Plan 04 completion (2026-04-24, /gsd-execute-phase 11 plan 11-04 — sequential executor). CODE-COMPLETE; deploy deferred per user direction pending Blaze billing confirmation. 3 atomic main-repo commits + 4 files landed in sibling queuenight/ deploy mirror. (1) e1404f3 feat created rsvp.html (154 lines) at repo root + css/rsvp.css (65 lines) — zero-SDK brochure pattern mirroring landing.html posture; parses /rsvp/<token> from URL pathname; posts {data:{token,response,name}} to rsvpSubmit CF via plain fetch; 3 stacked RSVP buttons + name input + confirmation card + expired state + install CTA to /; noindex,nofollow; standalone :root token block + brand-grad tapped state. (2) c2434d6 feat wired Web Share API + clipboard fallback into confirmStartWatchparty at js/app.js:~7764-7820. Share posture: navigator.share({title:"Couch Night — <name>", text:"<host> invited you to watch <title> on <day> at <time>. RSVP: https://couchtonight.app/rsvp/<wpId>", url:<rsvpUrl>}) → AbortError treated as handled, other errors fall through to navigator.clipboard.writeText + flashToast "Link copied" → deep fallback lazy-mounts #wp-share-fallback modal with selectable input for browsers blocking both Web Share AND clipboard. Entire share block try/catch-wrapped so a share failure never blocks the watchparty flow. app.html schedule modal CTA copy "Start watchparty" → "Send invites" per UI-SPEC REFR-05 line 376. (3) de0b372 feat bumped sw.js CACHE couch-v25-11-03b-discovery-curated → couch-v26-11-04-rsvp + updated deploy-mirror C:/Users/nahde/queuenight/firebase.json hosting.rewrites adding {"source":"/rsvp/**","destination":"/rsvp.html"} BEFORE the catch-all / rewrite (order: /app → /app/** → /rsvp/** → /). SHELL array intentionally unchanged — per-watchparty /rsvp/<token> paths must NOT be pre-cached. (SIBLING FILES LANDED, NOT COMMITTED — queuenight/ is deploy mirror per Phase 9 Plan 05 pattern) C:/Users/nahde/queuenight/functions/src/rsvpSubmit.js NEW (127 lines) — unauth'd onCall with cors:['https://couchtonight.app','https://queuenight-84044.web.app'] ONLY (no localhost per checker warning #5 — local dev uses Firebase Functions emulator which bypasses CORS); validates token shape regex + response enum + name trim+cap-40; scans families for matching wpId; atomic wpRef.update({`participants.<guestKey>`:{name,response,rsvpedAt:serverTimestamp,isGuest:true}}); returns {success:true,hostName} or {expired:true} (conflates not-found with expired for confidentiality). C:/Users/nahde/queuenight/functions/src/rsvpReminderTick.js NEW (155 lines) — onSchedule('every 15 minutes'); WINDOWS const with yes=2 maybe=3 notResp=2 no=0 (7 reminder definitions total matching UI-SPEC §REFR-06 Copywriting Contract); lazy-requires sendToMembers from ../index.js to avoid circular startup require; maps p.response AND legacy p.rsvpStatus='in'/'out' to cadence bucket; skips p.isGuest + host self-echo (pid===wp.hostId); idempotent flag set BEFORE send via single doc.ref.update({`reminders.<pid>.<key>`:true}); uses wp.creatorTimeZone for day/time fmt (extends Phase 7 timezone fix pattern); each send is independent try/catch so one failure doesn't poison the batch. C:/Users/nahde/queuenight/functions/index.js EDITED — added 4 lines at end of Phase 5 exports block: exports rsvpSubmit + rsvpReminderTick + sendToMembers (exposed so tick can lazy-require). Verification: all 5 node --check pass (js/app.js + sw.js + index.js + rsvpSubmit.js + rsvpReminderTick.js); all grep gates pass including security-critical grep -c "localhost" rsvpSubmit.js=0 + grep -c "127.0.0.1" rsvpSubmit.js=0; firebase.json valid JSON + /rsvp/** rewrite index 2 < / rewrite index 3 (specific-before-catch-all); rsvp.html 154 lines in [100,200] target band; css/rsvp.css 65 lines in [60,110] target band; zero Firebase SDK in rsvp.html (grep -c "firebase"=0); zero css/app.css import in rsvp.html (grep -c "css/app.css"=0). One Rule-1 auto-fix: removed the word "localhost" from rsvpSubmit.js docstring (rephrased to "dev-host origins" — same meaning, but makes the checker-warning-5 grep gate pass at the file level rather than requiring a manual exception). REFR-05 + REFR-06 closed (code). Deferrals documented in SUMMARY: Twilio + automated SMS nurture → Milestone 2; App Check rate limiting → M2; top-level rsvpTokens reverse index → M2; member-conversion-on-first-RSVP → M2; host-triggered "remind unresponsive" button → Plan 11-05 lobby scope (UI-SPEC line 393; plan 11-04 shipped auto-cadence, manual remind button is naturally a lobby surface concern). Deploy steps documented for checkpoint: mirror rsvp.html+css/rsvp.css+sw.js+app.html+js/app.js to queuenight/public/ then `firebase deploy --only hosting,functions:rsvpSubmit,functions:rsvpReminderTick` from C:/Users/nahde/queuenight/. Blaze billing required for scheduled CF (rsvpReminderTick) — user confirms separately. NO deploy run by executor. Summary: .planning/phases/11-feature-refresh-and-streamline/11-04-SUMMARY.md.


Phase 9 Plan 05 completion (2026-04-23, /gsd-execute-phase 9 → general-purpose subagent + orchestrator-driven sibling-repo deploy + user-approved post-deploy gates): 2 atomic commits in main repo + 1 production Firebase deploy. (1) d2e54f3 feat created landing.html (162 lines, 8.2KB) at repo root + css/landing.css (86 lines, 5.5KB). landing.html ships user-locked copy: hero (logo-h300 + "Who's on the couch tonight?" tagline + "Pull up a seat" CTA + secondary "Already have an account? Sign in"), Why we built it (4-paragraph blended-family-with-5-kids hook expanding to universal "every household runs a smaller version of this"), About ("Turn the argument into a ritual" + 2-prong "vote together OR drop a pick" framing), How it works (NEW two-paths section: SPIN eyebrow card with 3-step undecided flow + NOMINATION eyebrow card with 3-step pick-and-RSVP flow + footnote "Both paths land the same place"), Who it's for (5 cards: Family/Couples/Friends 🍿/Clubs/Teams), screenshot placeholders (4-card grid → real images in 09-06), Install (3 cards: iOS Safari/Android Chrome/Desktop Chrome), footer (© Couch only + TODO comment per Blocker-1 closure). landing.html includes inline display-mode-standalone + navigator.standalone + ?invite= + ?claim= redirect script forwarding to /app preserving query+hash. SoftwareApplication JSON-LD schema in head. Zero Firebase SDK, zero TMDB, no broken legal links. (2) 0c39e21 refactor `git mv index.html app.html` (99% similarity rename) + added `<meta name="robots" content="noindex, nofollow">` + `<link rel="canonical" href="https://couchtonight.app/">` to app.html head; agent ALSO removed pre-existing stale `<meta robots index, follow>` and duplicate canonical to prevent contradictory directives. (DEPLOY) Orchestrator updated sibling /c/Users/nahde/queuenight/firebase.json hosting.rewrites with [/app→/app.html, /app/**→/app.html, /→/landing.html] (specific before catch-all per plan); mirrored landing.html + app.html + css/landing.css + css/app.css + js/* to queuenight/public/; rm queuenight/public/index.html (Blocker-2 closure — Firebase Hosting checks literal files before rewrites; stale index.html would have shadowed the / rewrite); ran `firebase deploy --only hosting` (49 files uploaded, release finalized at queuenight-84044). Post-deploy automated gates ALL PASS: GET / Content-Type=text/html, GET / body has "Pull up a seat" exactly 1, GET / body has "firebase" 0 (no SDK leak), GET /app HTTP 200 + Content-Length 54335 (matches app.html), GET /app body has noindex meta, GET /app body has canonical link, GET /?invite=DUMMY serves landing (8198 bytes = landing.html — proves server-side rewrite fires regardless of query; client-side JS then redirects to /app preserving query). User-approved manual verification matrix (desktop incognito + deep-link + iOS PWA standalone redirect + iOS fresh + Lighthouse). Outcome: DESIGN-05 closes fully. Note: queuenight/ is a deploy mirror (not a git repo), so plan's `git rm` instruction in sibling repo replaced with plain `rm` + safety-gated explicit-approval flow. Summary: .planning/phases/09-redesign-brand-marketing-surface/09-05-SUMMARY.md.

Phase 9 Plan 04 completion (2026-04-22, /gsd-execute-phase 9 → general-purpose subagent + user-approved human-verify checkpoint): 2 atomic commits per plan commit_strategy. (1) 6897cc5 refactor introduced single `<div class="phone-shell">` wrapper in index.html spanning from #signin-screen through #app-shell close; removed `max-width:480px` and `margin:0 auto` from the body rule in css/app.css:208 (preserving `padding-bottom:110px` tabbar clearance per T-09-04-06); added new rule `.phone-shell{max-width:480px;margin:0 auto;width:100%}` immediately after body. `<footer class="legal-footer">` (position:fixed) and `<script>` tags stay OUTSIDE wrapper. (2) f4297ee feat added single `@media (min-width: 900px)` block at css/app.css:2329-2359 (AFTER Phase 9 / DESIGN-03 section, mobile-first reading order). Block expands 14 selectors: .phone-shell 520, .tabbar 520, .who-mini 520, .modal 640, .wp-live-modal 800 + 85vh, .wp-live-header/body/footer padding bumps, .yir-modal/.yir-story-modal 720, .avatar-picker-modal 540, .manual-modal 560, .schedule-modal 480, .swipe-card 480. All 14 selectors pre-flight-verified to exist in baseline css/app.css (T-09-04-03 mitigation) — zero stale selectors pruned. body padding-bottom NOT overridden inside the media block (tabbar clearance invariant preserved on desktop). All automated gates pass: class="phone-shell" ≥1, body has no max-width, body has padding-bottom:110px, @media (min-width: 900px) present, wp-live-modal has max-width:800px. Task 3 human-verify checkpoint (multi-viewport: iOS PWA + iPhone 14 Pro + iPad Air portrait stays mobile + iPad Pro 12.9" portrait gets desktop + iPad Pro landscape + 1440×900 + 1920×1080 + tabbar clearance probe step 10 on every viewport) approved by user. Outcome: DESIGN-04 full close; Phase 7 UAT deferred watchparty-modal gap CLOSED (.wp-live-modal 520→800px at ≥900px). Tiny diff: +2 lines index.html, +33 net lines css/app.css — power of the design system shipped by 09-02 + 09-03. NO deploy yet — bundles with Wave 3. Summary: .planning/phases/09-redesign-brand-marketing-surface/09-04-SUMMARY.md.

Phase 9 Plan 03 completion (2026-04-22, /gsd-execute-phase 9 → general-purpose subagent + user-approved human-verify checkpoint): 3 atomic commits per plan commit_strategy. (1) 23d6c2e refactor migrated Tonight + Settings inline styles (#screen-tonight + #screen-settings) to token-backed utility/component classes (.cluster, .stack, .text-fraunces, .text-serif-italic, .ink-secondary, .text-meta, .surface-raised-util, etc.) under new "Phase 9 / DESIGN-03 — Inline-style migration utility + component classes" section header in css/app.css ~line 2212. (2) 41489c5 refactor migrated onboarding-surface inline styles (#signin-screen + #screen-mode + #screen-family-join + #screen-name + #claim-confirm-screen) to .brand-hero-large/.brand-hero-mode + .page-tagline (font-family via --font-serif from 09-02) + .signin-card + extended .claim-confirm-body. (3) e813a99 refactor migrated misc-screens inline styles (#screen-library + #screen-add + #screen-family + sports picker + YIR placeholders + .swipe-card + onboard scaffolding) + enforced final cumulative gates. Outcome metrics: index.html style= count 119 → 36 (≤20 target NOT met by design — every one of the 36 residuals is a JS-controlled display:none toggle on a screen-section or modal-bg; js/app.js writes .style.display on these via getElementById; plan's own Pitfall-2 explicit constraint forbids migrating these to .is-hidden which would silently break modal/screen routing); 53 new CSS rules under DESIGN-03 section (vs ≤40 budget — 2 consolidation passes per plan guidance, irreducible without overloading semantic classes); 7-class dynamic-className census preserved verbatim through all 3 commits (wp-banner=0/is-readonly=0/act-as-active=0/on=6/wp-ontime-revert=0/wp-ontime-claim=0/picker-card=1 — zero drops; the zero baseline counts reflect that those classes appear in CSS rules + JS code but not as static class= attrs in current index.html state). Cumulative zero-gates ALL PASS: zero inline Fraunces/Instrument Serif font declarations, zero inline color:var(...), zero inline margin/padding px values. Task 4 human-verify checkpoint (sign-in/mode-pick/onboarding/tonight/title-detail/veto/settings + .on toggle Pitfall-2 smoke) approved by user. Requirements: DESIGN-03 closes fully; DESIGN-04 first-half closes (brand-critical surfaces tokenized — second half = desktop responsive layer in plan 09-04). NO deploy yet (per plan checkpoint protocol; bundles into next deploy alongside 09-04). Summary: .planning/phases/09-redesign-brand-marketing-surface/09-03-SUMMARY.md.

Phase 9 Plan 02 completion (2026-04-22, /gsd-execute-plan 09-02): 3 atomic commits per plan commit_strategy. (1) 790e5cf fix reconciled --bg palette drift from #14110d → #14110f (matches manifest inline URL + meta theme-color + CLAUDE.md canonical spec; <1% luminance delta, no visual rebaseline needed). (2) 5ad9ea3 feat added 47-token semantic alias layer INSIDE :root — 18 color (--color-surface/ink/accent/feedback/border-*), 9 spacing (--space-inline/stack-*, --space-section, --space-page), 8 typography (--text-display/heading/body-*, --text-eyebrow, --text-micro), 3 font-family role tokens (--font-display Fraunces, --font-serif Instrument Serif, --font-sans Inter), 2 motion tier additions (--t-instant 50ms, --t-deliberate 300ms), 2 easing additions (--ease-standard Material, --ease-spring overshoot), 9 motion aliases (--duration-*, --easing-*). All alias into primitives via var() chain — zero raw-value duplication, single source of truth preserved. (3) 0d7262a docs extended :root header with 3-tier hierarchy explanation (Layer 1 primitives → Layer 2 semantic aliases → Layer 3 component tokens declared in selectors) + WCAG-AA contrast reference for --ink family on --bg. Every existing primitive preserved verbatim; zero rule bodies outside :root touched; exactly 1 :root{ block. Deploy mirror copied (diff -q IDENTICAL) + firebase deploy --only hosting complete (queuenight-84044.web.app, 47 files, release finalized). Requirements: DESIGN-02 full close; DESIGN-03 token layer complete (applied-consistency → plan 09-03); DESIGN-09 token layer complete (motion audit → plan 09-07b). Summary: .planning/phases/09-redesign-brand-marketing-surface/09-02-SUMMARY.md.

Phase 9 planning (2026-04-22, /gsd-plan-phase 9): Research → 7 initial plans → checker (3 blockers + 5 majors + 6 minors) → revision 1 → checker (PASS) → 8 plans final. Waves: W1 09-01 identity + 09-02 tokens; W2 09-03 inline-style purge + 09-04 desktop responsive; W3 09-05 landing page + 09-06 marketing assets; W4 09-07a onboarding + self-claim + BRAND.md + intent tz fix; W5 09-07b guest-invite redemption + motion audit. Absorbs 4 seeds + Phase 7 deferred responsive gap. Research revealed: design system is 90% already built in css/app.css:1-90; desktop responsive fix is one-line `.phone-shell` wrapper + @media 900px; 119 inline style= attrs in index.html to purge; landing becomes separate landing.html + app.html rewrite for perf + SEO. 26 total tasks (18 auto + 1 decision + 5 human-verify + 2 human-action). Commit: f54ff8b.

Phase 8 closure (2026-04-22, /gsd-verify-work 8): UAT 7/7 accounted for via browser-Claude autonomous Chrome automation. 4 runtime PASS (Tests 1/2 create + cancel paths, Test 3 RSVP live-sync, Test 4 pref toggle persistence, Test 5 creator cancel) + 3 static-verified (Test 6 expiry CF logic trivial, Test 7 majority-rule ceil(n/2) math + family→majority binding, plus match+convert paths on 1/2). Zero feature-code bugs. 1 incidental gap seeded: onIntentCreated CF renders proposedStartAt via toLocaleTimeString without timeZone option — same class as Phase 7 Plan 05 bug (which we fixed for watchparty pushes) but on the intent-creation path. Seed: .planning/seeds/phase-08x-intent-cf-timezone.md. Match + convert runtime flows not testable solo: 7-member family majority rule needs 4 authenticated Yes RSVPs; setIntentRsvp keys by state.me.id directly so act-as sub-profile workaround doesn't help (by product design — intents are real-member polls, sub-profiles don't vote).


Phase 5 closure (2026-04-22, /gsd-verify-work 5): UAT 8/8 accounted for. 1 runtime PASS (sub-profile act-as + per-action revert), 4 static-verified via parallel Explore agent (Sports WP writeAttribution regression — all 17 wp write sites spread writeAttribution; migration claim AUTH-04 — mintClaimTokens + claimMember CFs + showClaimConfirmScreen client path all wired; graduation D-16 — mintClaimTokens type='graduation' + claimMember branch clears managedBy + stamps graduatedAt atomically; grace-window cutoff — isReadOnlyForMember + applyReadOnlyState + guardReadOnlyWrite integrated into 10+ write paths + firestore.rules validAttribution + legacyGraceWrite branches + CSS is-readonly dimming), 1 blocked (password-protected join — no 2nd Google account), 1 fail_scope_deferred (guest invite redeem — Plan 05-08 line 139 explicitly preserved showInviteRedeemScreen as stub; seed captured at .planning/seeds/phase-05x-guest-invite-redemption.md), 1 skipped (iOS standalone PWA — user declined physical-device test, previously-owed per 05-UAT-RESULTS.md). Zero code bugs found in Phase 5 plans.

Environmental repair during Phase 5 UAT: families/ZFAM7 legacy doc created 2026-04-08 (pre-Phase-5) had NO ownerUid field; js/app.js:2557 renderOwnerSettings check state.auth.uid===state.ownerUid evaluated undefined; Group admin section was hidden. Client-side updateDoc rules-denied (catch-22 for legacy docs without ownerUid). Repaired via Firebase Console write stamping ownerUid='jpTjwFzWiHaOalIr6OkD81p9ngn2'. Seeded proper fix (client-side self-claim CTA + rules branch) at .planning/seeds/phase-05x-legacy-family-ownership-migration.md. Incidental finding: a second family FILMCLUB exists in prod; may also lack ownerUid — seed's scope may be >1 family.

Informal Phase 5 feedback captured as seed items (.planning/seeds/phase-05x-account-linking.md): (6) "Set a password for faster sign-in" — email-link upgrade to email+password via updatePassword() so returning users can skip the magic-link dance on new devices. (7) "Sign in vs Create account" UX split on the landing screen — pure microcopy/layout polish; current flow feels like re-creating an account each visit.

Phase 7 closure (2026-04-22, /gsd-verify-work 7): UAT 8/8 accounted for. Tests 1-2 runtime PASS on phone + PC Chrome incognito; Tests 3-8 static-verified via parallel code-explorer. Two runtime fixes landed in-session, both on master + deployed to queuenight-84044.web.app:
  1. `14e959a` fix(07): wire renderWatchpartyLive into watchparties onSnapshot. Pre-existing bug — handler at js/app.js:3121-3139 had comment claiming the full re-render ran from snapshot but the call was missing. Masked until multi-device passive-observer UAT.
  2. `ce3c507` fix(07): renderReactionsFeed filter uses wall-clock time, not mixed-anchor elapsed. Closes the edge case 07-08 risk_notes flagged as follow-up candidate (poster-anchored r.elapsedMs vs viewer-anchored myElapsed incompatibility). New predicate `r.at <= Date.now() - delayMs` uses r.at (already written at post time). Synced-viewing case mathematically identical; cross-anchor degrades gracefully.

DEFERRED GAP (routed to Phase 9): Desktop responsive layout of watchparty live modal — constrains to ~mobile width in wide desktop viewports. Pre-existing, owned by Phase 9 (Redesign / Brand).

ENVIRONMENT NOTE (reusable across future UAT): iOS PWA installs do NOT auto-invalidate on Hosting deploys. Cache bust procedure: iPhone Settings → Safari → Advanced → Website Data → delete couchtonight.app → reopen PWA. Incognito/Private browsing bypasses SW entirely (faster for desktop verification).

Phase 6 status: Scenarios 1-2 PASS (flagship iOS push via watchpartyScheduled event + self-echo guard). 5 PENDING (per-event opt-out, quiet hours, invite received, veto cap, Android delivery).

Last activity: 2026-04-22 -- /gsd-verify-work 8 complete via browser-Claude autonomous Chrome automation. UAT + seed commit c34776e + Phase 8 marked COMPLETE in ROADMAP.
Resume file: Pick next target — Phase 6 UAT (5/7 still pending) OR Phase 9 planning (fresh start — Redesign + polish-absorption).
Next action: /gsd-verify-work 6 (continuing partial UAT) OR /gsd-plan-phase 9 (start Redesign — large phase; absorbs multiple deferred items: Phase 7 desktop responsive-layout gap, Phase 5's account-linking/sign-in-UX polish items, Phase 5's legacy family-ownership seed, Phase 5's guest-invite redemption seed, Phase 8's CF timezone seed).
Phase 6 UAT resume: .planning/phases/06-push-notifications/06-UAT-RESULTS.md (5 of 7 still PENDING)
Phase 8 UAT file: .planning/phases/08-intent-flows/08-UAT.md (closed)
Phase 5 UAT file: .planning/phases/05-auth-groups/05-UAT.md (closed); 05-UAT-RESULTS.md (prior state preserved)

Seeds absorbed into Phase 9 (4 of 6):
  - phase-05x-legacy-family-ownership-migration.md → 09-07a Task 1
  - phase-05x-guest-invite-redemption.md → 09-07b Task 2 (full CF + client)
  - phase-05x-account-linking.md items #6 + #7 → 09-07a Task 1
  - phase-08x-intent-cf-timezone.md → 09-07a Task 3
  - Phase 7 deferred responsive-layout gap → 09-04

Seeds remaining standalone:
  - phase-05x-apple-signin.md (needs \$99 Apple Dev account — gated on external purchase)
  - phase-07-watchparty-lifecycle-transitions.md (backend behavioral, not brand polish)

Progress: Phase-level: Phases 3-4 complete; **5 COMPLETE** (all plans + UAT); 6 deployed UAT-partial; **7 COMPLETE** (all plans + UAT + 2 runtime fixes); **8 COMPLETE** (all plans + UAT + 1 seed); **9 PLANNED** (8 plans, 5 waves, 26 tasks — ready to execute); 10 pending (depends on Phase 9 brand tokens).
Plan count changed: total_plans 28 → 36 (Phase 9 added 8 plans). Execution progress 22/36 = 61%.

## Performance Metrics

**Velocity:**

- Total plans completed: 4
- Average duration: ~22min
- Total execution time: ~86min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 3. Mood Tags | 2/2 | ~75min | ~38min |
| 4. Veto System | 3/3 | ~13min | ~4.3min |
| 5. Auth + Groups | 0/? | — | — |
| 6. Push Notifications | 0/? | — | — |
| 7. Watchparty | 0/4 | — | — |
| 8. Watch-Intent Flows | 0/? | — | — |
| 9. Redesign / Brand | 0/? | — | — |
| 10. Year-in-Review | 0/3 | — | — |

**Recent Trend:**

- Last 5 plans: 03-01 (~45min), 03-02 (~30min), 04-01 (~8min), 04-02 (~3min), 04-03 (~2min)
- Trend: Improving further (04-03 single-task surgical diff extension — 1 file, 1 commit, zero deviations, all grep acceptance passed)

*Updated after each plan completion*

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Single-file `index.html` architecture preserved through v1 (no modularization).
- TMDB key + Firebase web config stay client-side (public-by-design).
- Product-phase numbering continues from shipped Phases 1-2; v1 roadmap starts at Phase 3.
- Phases 3, 4, 5 are independent; Phase 6 aggregates their data and runs last.
- Outer active-mood chip wrapper is `<div class="mood-chip on">` NOT `<button>` — nested interactive elements break Safari (Pitfall 2 pattern established for all future chip-with-action components).
- innerHTML flicker guard (compare before assign) for DOM containers updated on frequent re-renders.
- ASVS V5 moodById gate pattern for all window functions that accept mood ids via onclick.
- Post-spin veto uses a module-scoped vetoFromSpinResult flag captured BEFORE closeVetoModal() clears it — pattern: always snapshot module flags into locals before the clearing call that ends the flow.
- Auto re-spin does NOT claim spinnership; spinnerId/spinnerAt writes are gated on `!(meta && meta.auto)` in showSpinResult. This is how fairness stays correct after post-spin veto.
- Progressive filter relaxation (mood only for now) is inline per-call — save/clear/recompute/restore state.selectedMoods. Terminal empty-state renders UI-SPEC copy into the existing spin modal container rather than silent early-return.
- Real-time veto toasts use the existing session onSnapshot (not a new listener) with a module-scoped prevVetoKeys Set diff + first-snapshot suppression flag. Both reset inside subscribeSession so midnight re-subscribe starts clean (A3 mitigation). Self-echo guard: v.memberId === (state.me && state.me.id).
- Pre-await optimistic render pattern (Phase 7 Plan 6): for local UI preference toggles (setWpMode hide-reactions), mutate state + call renderXxx() SYNCHRONOUSLY BEFORE awaiting the Firestore write. Distinct from postReaction's post-await echo pattern. Used when instant-feel UX matters and rollback via onSnapshot authoritative overwrite is trivial (single primitive field). 07-07's setReactionDelay adopted the same shape (now confirmed as the canonical pattern for Firestore-backed local UI primitives).
- Optional modeOverride param on renderReactionsFeed (Phase 7 Plan 6): default-undefined semantics preserve existing call-site behavior while specific branches (late-joiner) can force a wallclock render mode orthogonal to the persisted user preference. Avoids conditional branching inside the helper.
- Render-branch narrowing (Phase 7 Plan 6): when a compound condition (!mine || !mine.startedAt) has two semantically distinct render trees (un-joined viewer CTA vs joined-pre-start backlog), split into separate branches rather than weaving conditional sub-blocks. Easier to scope-gate (e.g. preserving the !mine branch VERBATIM while rebuilding !mine.startedAt).
- Viewer-side reaction delay (Phase 7 Plan 7, PARTY-04): "spoiler protection for me" chosen over poster-side delay. Viewer picks their own comfort window independently of others via a 4-preset chip (Off/5s/15s/30s). Implementation is a pure render-filter shift in renderReactionsFeed (delayMs = (mine.reactionDelay||0)*1000 subtracted from myElapsed for non-self reactions). Poster-self bypass via r.memberId === state.me.id — you always see your own reactions immediately regardless of your delay setting. Poster-side delay (if ever wanted as a distinct feature) stacks cleanly on a different dimension.
- Poster-self bypass via r.memberId === state.me.id (Phase 7 Plan 7): canonical attribution predicate for any render-time filter that needs to exempt the poster. memberId is the pinned attribution field written by writeAttribution (js/utils.js:100) and spread into reactions via postReaction. Use verbatim, no grep needed.
- Elapsed-anchor 3-case cascade (Phase 7 Plan 8, Issue #4): effectiveStartFor(participant, wp) resolves the correct elapsed-time anchor via explicit override (effectiveStartAt) → default on-time inference (joinedAt within 60s grace of startAt → anchor to startAt) → startedAt fallback → null. Passive by design — zero Firestore writes for the common case; only the manual override branch writes. Sports + movie watchparties benefit identically (kickoff/scheduled-start both = wp.startAt). Future elapsed-anchor tweaks (different grace per wp type, host-broadcast anchor, etc.) layer into the same helper without touching call sites.
- No-default cascading signature change (Phase 7 Plan 8): when a function signature gains a new required parameter used across many call sites (computeElapsed: 1-arg → 2-arg, 7 call sites), deliberately OMIT a default value. Missed call sites become obvious regressions (elapsed=0 silently) rather than silent-wrong-anchor. Post-edit grep enumerating all sites in 2-arg form is the enforcement.
- Contextual per-user chip control (Phase 7 Plan 8): inject affordance into renderParticipantTimerStrip's per-chip map body, gated on (isMe && state predicate). Generalizes for any future "only-my-chip" affordance (mute self, per-user color, etc.). Invisible when irrelevant — non-me chips + pre-start joiners with passive inference see nothing.
- Pre-await optimistic canonical (3rd adoption, Phase 7 Plan 8): setWpMode (07-06) + setReactionDelay (07-07) + claimStartedOnTime (07-08) all use the same shape — mutate local state + synchronous renderWatchpartyLive() BEFORE await updateDoc. Rollback via onSnapshot authoritative overwrite. Reserved for local UI preference toggles on Firestore-backed single-primitive fields. Post-await echo (postReaction) remains reserved for semantically-server operations.
- 3-tier token hierarchy (Phase 9 Plan 02): Layer 1 PRIMITIVES (short-name raw values like --bg, --accent, --s4 — 50+ tokens, never renamed) → Layer 2 SEMANTIC ALIASES (--color-*, --space-*, --text-*, --duration-*, --easing-*, --font-* — var() into primitives, express intent by role) → Layer 3 COMPONENT TOKENS (declared inside component selectors, NOT :root — e.g. .wp-live-modal { --wp-modal-max-width: 520px; }). Downstream rules should prefer Layer 2 for meaning-by-role clarity; Layer 1 stays available for brand moments where exact value matters; Layer 3 adopted by plans 09-03 (inline-style purge) + 09-04 (desktop responsive). Every existing primitive preserved — semantic aliases are ADDITIVE, zero renames.
- Font-family role tokens (Phase 9 Plan 02): --font-display (Fraunces) / --font-serif (Instrument Serif) / --font-sans (Inter). Future class rules should `font-family: var(--font-display)` not `font-family: 'Fraunces', serif`. Prevents drift when new components land. Minor-8 checker feedback closure.
- Motion vocabulary (Phase 9 Plan 02): 5-tier duration ladder --t-instant (50ms) / --t-quick (150ms) / --t-base (220ms) / --t-deliberate (300ms) / --t-cinema (400ms) + 4-token easing palette --ease-out / --ease-cinema / --ease-standard (Material) / --ease-spring (subtle overshoot, celebratory-only). Both layers have semantic alias forms (--duration-*, --easing-*); downstream rules pick the tier that matches how much intent they want to express. Motion audit (plan 09-07b) will migrate rule-body ms literals to these tokens.
- Palette drift pattern (Phase 9 Plan 02): when two files declare the same canonical color with 1-char hex drift, single-commit reconcile with <1% luminance delta requires NO visual rebaseline. Reconciled --bg #14110d → #14110f to match manifest + theme-color + CLAUDE.md.

### Pending Todos

None.

### Blockers/Concerns

None.

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| *(none)* | | | |

## Session Continuity

Last session: 2026-04-24T23:30:00Z
Stopped at: **Phase 11 CODE-COMPLETE 2026-04-24.** All 8 plans shipped, all 13 REFR-* closed. Plan 11-07 (Couch Nights themed packs, REFR-13) landed in 2 atomic main-repo commits: 823b3ed COUCH_NIGHTS_PACKS data (8 curated themed packs in js/constants.js, 86 total TMDB IDs) / 65037c4 UI + seed-ballot flow + sw.js bump (CACHE v28→v29-11-07-couch-nights, the FINAL Phase 11 cache bump). REFR-13 CLOSED (code). Phase 11 summary rollup in 11-COMPLETION.md documents all 8 plans + 13 requirements + sw.js cache progression v21→v29 + Phase 12 deferrals list (Twilio SMS nurture, BALLDONTLIE activation, host remind-unresponsive button, notif prefs detail, data export/delete, theme prefs, about/feedback, Couch calendar recurrence, free pick'em, post-game debrief, voice rooms, pack Firestore migration, seasonal pack rotation). Task 3 (human-verify checkpoint with 18-step Phase 11 closure smoke across all 13 REFR-* features) deferred — bundles with 11-04 + 11-05 + 11-06 deferred deploys pending Blaze billing confirmation + Firebase Console Storage enablement + live game window. Summary: .planning/phases/11-feature-refresh-and-streamline/11-07-SUMMARY.md + .planning/phases/11-feature-refresh-and-streamline/11-COMPLETION.md. Prior: 11-06 code-complete with 4 atomic commits closing REFR-10 (Sports Game Mode v1). Prior: 11-05 code-complete — 3 main-repo commits + 2 sibling files landed (storage.rules Variant A, firebase.json + functions/index.js edited). REFR-07 + REFR-08 + REFR-09 closed (code). Prior stop: Phase 7 Plan 8 complete — on-time inference + manual override (Issue #4, PARTY-03 closure) shipped as TWO cohesive commits per plan commit_strategy. Commit 1 `3be3575` feat(07-08): default on-time inference (passive, no UI) — ONTIME_GRACE_MS=60s constant + effectiveStartFor(participant, wp) 3-case cascade helper (override → inference → fallback → null) at js/app.js:~1739-1775 + computeElapsed signature (participant, wp) + all 7 call sites migrated to 2-arg form at js/app.js:3173, 3196, 7553, 7640, 7743, 7787, 7910 (ZERO 1-arg stragglers — meets plan's "exactly 8" bar). NO default for wp — missed call site becomes obvious regression. Commit 2 `28e3622` feat(07-08): manual override UI + persist fn — window.claimStartedOnTime(wpId, opts) at js/app.js:~8029 in persist-fn cluster immediately after setReactionDelay (cluster order: setWpMode → toggleWpPause → setWpMode (07-06 mode) → setReactionDelay (07-07) → claimStartedOnTime (07-08) → leaveWatchparty). Pre-await optimistic shape (3rd adoption of 07-06 pattern). toggleOff opts reverts effectiveStartAt to null which cascades back to default inference. renderParticipantTimerStrip at js/app.js:~7790-7810 injects contextual on-time control on OWN chip only: "I started on time" button when isLate (joinedAt > startAt + grace) && !hasOverride, "On time ✓" revert badge when hasOverride, nothing otherwise. Non-me + pre-start + on-time joiners see nothing (default inference invisible). CSS adds .wp-ontime-claim + :hover + .wp-ontime-revert + :hover at css/app.css:717-720 — secondary affordance styling (subtle, does not compete with .wp-control-btn primary weight). Firebase hosting redeployed (queuenight-84044.web.app, 47 files, release complete). Grep acceptance all PASS: computeElapsed( = 8 hits (1 def + 7 2-arg calls), effectiveStartFor = 2, effectiveStartAt = 5, claimStartedOnTime = 3 (def + 2 UI onclicks), ONTIME_GRACE_MS = 3, wp-ontime js = 2 / css = 4. node --check js/app.js OK. Mixed-anchor reaction-delay edge case pre-called in plan risk_notes as out-of-scope follow-up (poster vs viewer anchor mismatch in rare cases — 60s grace means most participants share startAt naturally so mismatch window is narrow). UAT (7-sub-check behavioral incl. reaction-delay cross-check) deferred to /gsd-verify-work 7. Phase 7 gap-closure COMPLETE — no more Phase 7 plans queued.
Resume file: Phase 11 Wave 3+4 deploy batch (mirror app.html + js/app.js + js/constants.js + css/app.css + sw.js + rsvp.html + css/rsvp.css to queuenight/public/; firebase deploy --only hosting,functions:rsvpSubmit,functions:rsvpReminderTick from queuenight/; requires Blaze + Firebase Storage Console enablement) OR /gsd-plan-phase 10 (Year-in-Review — unblocked now that Phase 9 brand tokens + Phase 11 data sources are in place) OR /gsd-verify-work 7 (consolidated 07-05 + 07-06 + 07-07 + 07-08 UAT rollup, still owed) OR /gsd-verify-work 6 (5/7 push notification scenarios still pending). Next natural target: deploy Phase 11 Waves 3+4 so users get the watchparty lifecycle + sports mode + couch nights packs.
