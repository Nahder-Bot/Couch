# OS
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
package-lock.json

# Environment / secrets (none today, defensive)
.env
.env.local
.env.*.local
.envrc

# Firebase
.firebase/
.firebaserc
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log
*.firebase.cache

# Build artifacts (no bundler today, defensive)
dist/
build/
out/

# Test
coverage/
*.lcov
.nyc_output/

# One-time audit scripts that self-delete (defensive)
.planning/phases/**/audit-tmdb-packs.cjs
.planning/phases/**/search-replacements.cjs
.planning/phases/**/verify-replacements.cjs

# Phase 15.3 — evaluation sample outputs (regeneratable via scripts/phase-15.3-*.cjs).
# The scripts themselves are committed as historical record of how the
# transparent-PNG approach was chosen over vector tracing; the sample PNG/SVG
# outputs are large and disposable.
brand/samples/

# Claude Code project state (local-only, not for sharing)
.claude/

# Tests scratch
tests/firestore-debug.log
tests/node_modules/

# PWABuilder package inspections — contain Android signing keystore + cleartext password.
# NEVER commit. Always re-extract from local zip if needed.
.planning/spikes/002-pwabuilder-package-inspect/
.planning/spikes/**/signing.keystore
.planning/spikes/**/signing-key-info.txt
.planning/spikes/**/GoogleService-Info.plist
